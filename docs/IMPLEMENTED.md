# STOCVEST — Implemented (shipped work archive)

**Use with [`BACKLOG.md`](./BACKLOG.md) (planned work) and [`CONTEXT.md`](./CONTEXT.md) (current state & ops).**

This file archives **completed** backlog rows so `BACKLOG.md` stays focused on planned work. Each row is a stable handle (B1, D1, …) for PR/commit history. Sorted newest first within each section. Full implementation narratives, test counts, and commit hashes for any of these items can be reconstructed from `git log` or `docs/CONTEXT.md`'s test-baseline trail.

---

## B — Product / UX

| ID | Theme | Shipped | Summary |
|----|-------|---------|---------|
| B46 | Setup analytics (evolution + outcomes) | Done 2026-05-17 · commits `3eab90f`, `bc69444` (Phase 6 prep) | Replaced user-facing signal-validation / performance marketing with **setup state evolution** surfaces. **`stocvest/analytics/`** (`evolution_stats`, `outcome_stats`, `system_metrics`); **`GET /v1/analytics/setup-outcomes`**, **`GET /v1/admin/system-behavior`**; extended **`GET /v1/watchlists/symbols/{symbol}/setup-evolution`** summary + plan gating. DynamoDB **`ModeTimelineIndex`** GSI on **`WatchlistMaturationTransition`**; **`price_at_event`** + **`setup_continuation`** outcome kind. Frontend: `/dashboard/setup-evolution`, `/dashboard/setup-outcomes` (AppShell), redirects from signal-validation + performance; Evidence + watchlist deep links; admin D2 at `/dashboard/admin/historical-validation`. Removed Signals **Past signal states** tab (`SignalHistory` UI). Spec: **`docs/SETUP_ANALYTICS_SPEC.md`**. Terraform: `infra/dynamodb.tf`, `infra/apigateway_6e.tf` (**applied 2026-05-17** — GSI + 2 API routes). **Phase 6 pending:** retire `SignalHistory` / `record_signal()` (dependents in spec). Tests: `tests/analytics/*`, `tests/api/test_analytics_setup_outcomes.py`, `tests/api/test_admin_system_behavior.py`, `frontend/tests/setup-*`, `admin-historical-validation-page.test.tsx`, `evidence-setup-evolution-link.test.tsx`. |
| FUND-C | Optional FMP fundamentals provider | 2026-05-17 | **`stocvest/data/fmp_client.py`** — `get_revenue_trend` (YoY quarterly revenue) + `get_upcoming_earnings_date` (earnings calendar fallback). Wired into **`fundamental_context`** and **`earnings_calendar`**. `FMP_API_KEY` / Secrets Manager `fmp_api_key`. Tests: `tests/data/test_fmp_client.py`. |
| FUND-D | B45 transitions — fundamental_backdrop + earnings_days_away | 2026-05-16 | Optional fields on **`WatchlistMaturationTransition`** + Dynamo persist/read; populated from swing composite in **`try_log_maturation_transition`** (swing mode only). |
| FUND-B | Swing fundamental backdrop (display-only, paid) | 2026-05-16 | **`stocvest/signals/fundamental_context.py`**; **`fundamental_context`** on swing composite when **`has_full_access`**; reuses **`BenzingaMultiResult`**; **`FundamentalBackdropPanel`** on evidence card. Tests: `test_fundamental_context.py`, `fundamental-backdrop.test.tsx`. |
| FUND-A | Swing earnings horizon (display-only) | 2026-05-16 | **`stocvest/data/earnings_calendar.py`** + Benzinga forward **`get_upcoming_earnings_calendar`**; top-level fields on **`build_swing_composite_response`** only (`upcoming_earnings_date`, `earnings_days_away`, `earnings_risk`, `earnings_report_time`, optional `earnings_chip`). Evidence card banner via **`applySwingCompositeEnrichment`** (≤7 days). Spec: **`docs/FUNDAMENTALS_CONTEXT_SPEC.md`**. Tests: `tests/data/test_earnings_calendar.py`, `tests/api/test_swing_composite.py`, `frontend/tests/signal-evidence.test.ts`. |
| B45 | Setup evolution transition log + warming empty states | 2026-05-16 | DynamoDB **`WatchlistMaturationTransition`** (90d TTL); log on state or alignment change from **`sync_watchlist_maturation_from_composite`**; **`GET /v1/watchlists/symbols/{symbol}/setup-evolution`**. **Superseded UI (B46):** Signals **Past states** tab removed; hub at `/dashboard/setup-evolution`. Tests: `test_watchlist_transition.py`, `test_watchlist_maturation_transition_repository.py`, `test_watchlists_setup_evolution.py`, `setup-evolution-present.test.ts`. |
| B44 | Evidence card diagnostic IA (bias ≠ signal) | 2026-05-16 | **`EvidenceCardHeader`** (prominent **Bias**; alignment as muted context + **`buildEvidenceAnchorLine`** tying bias/alignment/setup validity), **`EvidenceLayerContribution`** (“Layer contribution (directional pressure)”) + primary drivers strip, per-layer **insight** one-liners via `evidence-card-present.ts`; removed Trade readiness / R/R / regime grid, duplicate LAYER ALIGNMENT, decision rationale, and bottom duplicate chart; collapsed **Signal parameters**; **Layer synthesis (informational)**. News empty → **No active catalyst**. **Signals polish (same ship):** sharper **Why not?** R/R copy (`trade-decision.ts`, `signals-page-present.ts`), **`SignalsWatchlistPickerModal`** (search + maturation badges), radar baseline micro-copy, collapsed layers preview = driving + blocking (`pickCollapsedLayerPreview`). Tests: `evidence-card-present.test.ts`, `evidence-card-header.test.tsx`, `evidence-card-restructure.test.tsx`, `signals-watchlist-picker.test.tsx`, updated `signals-page-present.test.ts`, `signals-page-mode-clears-screen.test.tsx`, `signal-evidence.test.ts`. Frontend baseline **1143** tests / **124** files. |
| B43 | Signals page setup-read IA (validate, don't instruct) | 2026-05-16 | Replaced the **“AI Signal Analysis”** prose block with **`Setup read`** (bias, **X/6 alignment**, actionable line, **Why not?** from `deriveDecisionRationale` + blocking layers, **Next** watchlist/history/evidence links). **`SignalsCommandBar`** (symbol, watchlist + maturation line, Day/Swing mode). Collapsed **`SignalsLayerBreakdown`** (blocking/mixed layers first, expand all six). **`SignalsReferenceLevels`** with context hints. Pure helpers: `frontend/lib/signals-page-present.ts`, `use-watchlist-maturation-line.ts`. No API contract change. Tests: `frontend/tests/signals-page-present.test.ts`, `frontend/tests/signals-setup-read-ui.test.ts`, updated `frontend/tests/signals-page-mode-clears-screen.test.tsx`. Frontend baseline **1134** tests / **120** files. |
| B33 | Per-symbol scanner evaluation trace (gate transparency) | 2026-05-16 | **Phase 1:** setups v2 **`evaluation_trace`** (`include_evaluation_trace`) — gate-level detail (session RVOL %, score floor, liquidity, etc.) via `scanner_evaluation_trace.py`. **Phase 2:** DynamoDB **`ScannerEvaluationTrace`** (48h TTL, `userId` + `trace#{desk}#{ET session}`); POST setups persists when authenticated; **`GET /v1/signals/scanner-trace`** (`mode`, `session_date`, `limit`). UI: **`ScannerEvaluationTraceSection`** collapsible expander (“evaluated · did not qualify · not a watchlist”); scanner hydrates from GET when live scan has no rows. Terraform: table + route on `signals` Lambda. Tests: `tests/signals/test_scanner_evaluation_trace.py`, `tests/data/test_scanner_evaluation_trace_store.py`, `tests/api/handlers/test_scanner_trace_handler.py`, `frontend/tests/scanner-evaluation-trace.test.ts`, `frontend/tests/scanner-trace-api.test.ts`, `frontend/tests/scanner-scan-result-ui.test.ts`. Counsel review noted in **BACKLOG** before broad promotion only. |
| B42 | Scanner scan-result IA + setups v2 near-qualification bundle | 2026-05-16 | **`ScannerScanSummary`** (`frontend/lib/scanner-scan-summary.ts`) — hero, next actions, quiet education, separate **near qualification** (engine score band) vs **watchlist progression** (maturation `developing` / `re_evaluating`). Setups v2: **`include_near_qualification`** on **`POST /v1/signals/day/setups`** and **`swing/setups`** → `{ qualifying, near_qualification }` with **`alignment`** from triggers (`scanner_setups_bundle.py`); legacy callers omit flag and still get an array. **`scanner-load`** always requests v2 + builds summary; UI: `ScannerScanResultHero`, `ScannerNearQualificationSection`, `ScannerScanEducation`. Tests: `tests/api/test_scanner_setups_bundle.py`, `frontend/tests/scanner-scan-summary.test.ts`, `frontend/tests/scanner-api.test.ts`. Docs: **`API_CONTRACTS.md`**. |
| B39 | Watchlist maturation — production UX + plan-gated API | 2026-05-15 | **`WatchlistMaturation`** path: dual-write + scheduled refresh (existing). **New:** ``stocvest/api/services/watchlist_maturation_gates.py`` — **free** users omit **``readiness_label``** on ``GET /v1/watchlists/maturation-summary`` unless **``beta_access_active``**; paid **``swing_pro``** / **``swing_day_pro``** get full rows. **Watchlists** RSC (``app/dashboard/watchlists/page.tsx``) passes **``maturationSummaryMode``** from **``scannerSetupLoadModeForSubscription``** (swing for Swing Pro only). Client: fetch loading/error, non-default explainer, dynamic footnote. **Watchlists list UX:** deduped symbols (client + ``WatchlistItem.from_item``), unified add/search typeahead, mode-scoped maturation filter, batch row quotes via BFF **`/api/stocvest/market/snapshots`**, shared pure helpers in **`frontend/lib/watchlist-page-utils.ts`**. Tests: ``tests/api/test_watchlists_maturation_summary.py``, ``tests/api/test_watchlist_maturation_gates.py``, ``frontend/tests/watchlists-page-maturation.test.tsx`` (incl. default ↔ non-default tab switch), ``frontend/tests/watchlist-page-utils.test.ts``, ``tests/api/test_watchlists.py`` (symbol dedupe on ``from_item``). Contract: **`docs/API_CONTRACTS.md` §4.13** (maturation) and **§4.2** (batch snapshots + BFF). Transition email alerts → **B41** (``IMPLEMENTED.md``). |
| B41 | Watchlist maturation — email on state change (evidence path) | 2026-05-15 | **SES** when a symbol on the **default** watchlist moves between maturation states after a successful **``WatchlistMaturation``** write from **evidence** composites (not scheduled refresh). **`AlertType.WATCHLIST_MATURATION`**, **`AlertPreferences.on_watchlist_maturation`**, dedupe **`had_watchlist_maturation_transition_on_et_calendar_day`** (same **America/New_York** calendar day **and** same ``mode`` + ``previous_state`` + ``new_state`` as stored alert body JSON — distinct transitions same day can each email once). Broad per-symbol ET-day helper retained. Quiet hours + **`watchlist_only`**. **`watchlist_maturation_notify.py`**; **`email_on_state_change`** on sync. Default **Watchlists** default list: in-app strip **``watchlist-maturation-alerts-feed``** calls **`GET /v1/alerts/history?alert_type=watchlist_maturation&symbols=…`** (BFF forwards **``limit``** / **``alert_type``** / **``symbols``**); handler reads up to **50** rows when either filter is set; **``symbols``** list capped at **50** tickers client-side. Strip shows **``watchlist_maturation``** rows only (defensive parse). **``frontend/lib/nav/watchlist-signals-deeplink.ts``** — contextual **``/dashboard/signals``** links (**``ref=watchlist``**; **``trading_mode``** from **``maturationSummaryMode``** on Watchlists; Settings **Recent Alerts** omits **``trading_mode``**); **``aria-label``** on ticker links. Tests: **`tests/api/test_alerts.py`**, **`tests/api/test_watchlist_maturation_sync.py`**, **`tests/services/test_email_service.py`**, **`tests/data/test_alert_store_maturation_et_dedupe.py`**, **`frontend/tests/watchlists-page-maturation.test.tsx`**, **`frontend/tests/alerts-history-bff.test.ts`**, **`frontend/tests/watchlist-signals-deeplink.test.ts`**, **`frontend/tests/settings-alert-history.test.tsx`**. |
| B38 | Gap Intelligence lifecycle — scanner batch + DDB cache + warmer + debounced metrics | 2026-05-14 | **`POST /v1/signals/gap-intel/batch`** (max 24 symbols) on `signals` Lambda; **`GapIntelCache`** DynamoDB read-through cache (soft TTL ~120s) wired in `gap_intel_snapshot_handler` + batch; EventBridge **`rate(2 minutes)`** → `signals` Lambda with **`gap_intel_cache_tick`** (`stocvest/workers/gap_intel_cache_tick.py`) warms **`GAP_INTEL_TICK_SYMBOLS`**; **`Stocvest/GapIntel` → `ScenarioBuilderDisabled`** CloudWatch metric on ENABLED/LIMITED → DISABLED transitions (1h debounce per symbol×mode). Frontend: **`useScannerGapIntelBatch`**, BFF **`/api/stocvest/signals/gap-intel/batch`**, scanner gap cards + evidence modal pass **`gapIntelSnapshot`**. Terraform: `GapIntelCache` table, env on `signals` Lambda, **`cloudwatch:PutMetricData`**, API route, EventBridge rule + permission. Tests: `test_gap_intel_handler.py` (batch), `test_lambda_dispatch.py` (tick short-circuit). |
| B37 | Admin Users — per-user error activity (audit) + last-activity UX | 2026-05-13 | **`GET /v1/admin/users/{user_id}/activity-errors`** (`admin_users_activity_errors_handler` in `stocvest/api/handlers/admin_users.py`): rolling window (**`days`** 1–30, default 7), filters **`AuditEvent`** rows with **`status_code` ≥ 400** or **`outcome`** `error`/`failure`, capped read of 1000 newest user-partition events. **`lambda_dispatch.py`** + **`infra/apigateway_6e.tf`** route registration. BFF **`frontend/app/api/stocvest/admin/users/[user_id]/activity-errors/route.ts`**; client **`fetchUserActivityErrors`** in `frontend/lib/api/admin-audit.ts`. **`admin-users-page-client.tsx`**: parallel fetch on detail load, **Show error logs** / **Hide error logs** button (disabled when no rows), inline panel; **Last activity** label + explainer when **`last_active_at`** empty. **`frontend/lib/dashboard-user-subscription.ts`**: import **`stocvestAuthedFetch`** for RSC **`/v1/users/me`** reads (Next build). Tests: `tests/api/handlers/test_admin_users.py` (+activity-errors cases), `frontend/tests/admin-users-bff.test.ts`. |
| B36 | Evidence Card — Signal price drift row (computed-at vs current + Δ%) | 2026-05-13 | Surfaces scanner emit anchor vs live snapshot so users see when reference levels may have drifted. `SignalEvidenceData.priceAtSignal` from `IntradaySetupPayload.last_price` in `buildEvidenceFromSetup`; paired with `lastTradePrice`. Pure helper `frontend/lib/signal-evidence/signal-price-display.ts` (drift tiers on \|Δ%\| at 0/1/3/5%, accessible label). UI row under symbol in `signal-evidence-card.tsx` with `data-testid="signal-evidence-price-drift"`. +47 tests (`tests/signal-price-display.test.ts` 33, `tests/signal-evidence-card-price-drift.test.tsx` 14). Full Vitest 1018/1018, 91 files. See `CONTEXT.md` row 22. |
| B35 | Evidence Card layer-card refactor (collapse empty layers + compress neutral context + visual-emphasis hierarchy + News-neutral parenthetical) | 2026-05-13 | Closed the BRK.B feedback round (items 3/4/6 from row 20). New pure module `frontend/lib/signal-evidence/layer-emphasis.ts` owns the `primary`/`secondary`/`tertiary` tier helper, the per-layer activity truth table, the strict 3-way Context-compression gate, and the decoupled News-neutral parenthetical. Signal Layer Breakdown render in `signal-evidence-card.tsx` now applies tier-driven padding/font/opacity, default-collapses empty neutral layers behind a `<details>` disclosure, replaces News+Macro+Geo (all neutral with no active content) with a single consolidated "Context: Neutral (no macro or news tailwinds present)" card, and renders the soft-negative parenthetical "no catalyst support → lowers continuation probability" alongside any standalone News=Neutral chip. Technical is always primary and never collapses; Sector + Geopolitical default tertiary and promote to secondary when active. +26 new lock-in tests (`tests/layer-emphasis.test.ts` 76, `tests/signal-evidence-card-layer-hierarchy.test.tsx` 12). Frontend full-suite 970/970 PASSED across 89 files. See `CONTEXT.md` row 21. |
| B30 | Evidence-card composite mode routing + dual-engine product decisions | 2026-05-12 (Phases 1–4) | Closed cross-mode data-leakage bug + extracted mode-resolution helpers + per-mode composite weight override schema/resolver + gap-card mode-fit classifier. All B30 Severity-2 items CLOSED. |
| B28 | Dashboard Day Desk (parallel-engine home surface) | 2026-05-11 (Phase 0 chatbot routing + Phase 1 two-desk render) | Dashboard now renders Swing Desk + Day Desk in parallel, each with independent posture (Active/Monitor/Suppressed); day-vocabulary anti-leak enforced via dedicated `dashboard-posture.ts` helpers and lock-in tests. |
| B27 | Mode Separation UI compliance shipment (A1/A2/C/D) | 2026-05-11 · commits `e7c9fe0`, `122ddc8`, `0f31098`, `7bf3775` | Scanner "Both" split into mode-labelled sections; Signals page URL-authoritative on `trading_mode`; Historical validation panel narrowed to mode-specific (no combined headline); Performance surfaces dropped combined-engine accuracy cards. |
| B26 | STOCVEST Assistant (locked-prompt chatbot) | 2026-05-10 · commit `b6f83e7` (+ polish through 2026-05-11) | Anonymous public + authenticated `/v1/signals/assistant/chat`, locked system prompt with two-context safety perimeter + mode separation + historical-validation-context rules, page-context publishers on every dashboard surface, in-memory-only conversation state. |
| B25 | Dashboard empty-swing Primary read + layman tooltips | 2026-05-08 · commit `6eba555` | Posture headline + regime one-liner + suppression line on empty `Top signals`; plain-language InfoTip strings in `ui-tooltips.ts`. |
| B24 | Beta tester full-app override | 2026-05-07 · commit `3e21d5c` | `UserProfile` beta fields, `has_full_access`/`has_ai_explanations`, `PATCH /v1/admin/users/{user_id}/beta-access`, mutation guards on `PATCH /v1/users/me`, `scripts/beta_access.py`. |
| B21 | Public landing v2 + founding paid counter | 2026-05-07 · commit `3e21d5c` | Landing rewrite (hero, engine demo, workflows, transparency, comparison, pricing + founding strip), `GET /v1/signals/founding-members`, `landing-page.test.tsx`. |
| B19 | Ticker news panel + Market Pulse | 2026-05-06 · commit `e1217cd` | `GET /v1/market/news?symbol=` historical panel, `news-panel.tsx` + `ticker-news-panel.ts` per-ticker 2m cache, dashboard Market Pulse (SPY/QQQ/VIX/regime), Vitest `fileParallelism: false`. |
| B17 | PDT Guardian → compact status pill | 2026-05-04 · commit `3b7551f` | Removed large dashboard PDT card; added `pdt-status-pill.tsx` beside Market/VIX with color bands. |
| B16 | Signals page after-hours research panel | 2026-05-04 | `signals-after-hours-panel.tsx` — last-session levels, earnings window, recent news, tomorrow watch levels, watchlist CTA with graceful fallbacks. |
| B15 | Market Intelligence headlines (quality + affected stocks) | 2026-05-04 · follow-ups 2026-05-05 · commits `e9728ad`, `9fe90e2` | Ticker-scoped news + 4h lookback + watchlist merge, catalyst/recency/source scoring, PR-wire penalty, topic dedupe, frontend tabs + emoji + ✓/dimmed-PR source line + wider intel column. |
| B14 | Watchlist monitoring (scanner + scheduled + alerts) | 2026-05-03 | Default watchlist symbols in scanner overview, scheduled scan symbol merge (cap 40), `notify_intraday_setups_for_watchlist_users` with 4h dedupe. |
| B4 | Watchlists | 2026-05-03 | `watchlist_store` + API + dashboard `/dashboard/watchlists`, sidebar nav, BFF routes, "Add to watchlist" on signals + gap cards; limits 5 lists / 50 symbols. |
| B12 | Landing: historical signals + trust sections | 2026-05-02 · commit `4579d1c` · polish `313db1e`, `d5a363e` | `GET /v1/signals/recent?landing=true`, `LandingSignalExplorer` + `LandingBeforeAfterSection` + `LandingActivityFeedSection`, realistic fallback signals with ET wall-clock timestamps. |
| B11 | Confluence Score | 2026-05-02 | `stocvest/signals/confluence.py`, intraday + swing payloads, morning brief `top_watch`, setup-card + evidence-modal + gap-card CONFLUENCE chips. |
| B10 | Gap Intelligence + morning brief | 2026-05-02 · commit `4122c05` | `POST /v1/scanner/gap-intelligence` merges gaps + 24h news + catalyst block + narrative sentiment; structured morning brief from `morning_brief.py`; shared-news headline dedupe + merger/acquirer narrative tweaks. |
| B9 | Earnings calendar UI | 2026-05-02 | Redesign with Upcoming default filter, Mon–Fri This Week grouping, beat/miss bars, monospace figures. |
| B2 | Onboarding | 2026-05-02 | Mandatory full-screen legal ack (5 checkboxes + ToS link, `legal_acknowledged_version = 1.0`), optional onboarding wizard, `GET`/`PATCH /v1/users/me` on brokers Lambda + API Gateway. |
| B1 | Journal automation | 2026-05-02 · commit `3602db3` | Auto-capture on fill + auto-close on exit, optional signal metadata on `POST /v1/orders/submit`, `GET /v1/journal/analytics`, scanner → portfolio order prefill + confirmation modal. |
| B3 (Phase 1) | Alerts delivery (email) | 2026-05-03 | SES email via `email_service.py`, prefs + history in `alert_store`, `AlertTriggerService`, hooks on composite record + order validate (2 trades) / submit PDT 403, settings UI, SES IAM. Phase 2 (push/webhooks/SMS) deferred — see backlog. |
| B18 | Scheduled scanner → portfolio composite pass | Retired 2026-05-08 | Superseded by signal validation ledger; `ModelPortfolio` removed. |

---

## D — Data, performance & moat

| ID | Theme | Shipped | Summary |
|----|-------|---------|---------|
| D12 | Dashboard terminal UX — Tier 1.C Phase 0–5 (terminal-grade `/dashboard`) | 2026-05-14 | **Phase 0:** `load-timing.ts`, `[dashboard-load]` logs. **Phase 1:** nested `<Suspense>` scanner + hydrates. **Phase 2:** **`GET /v1/dashboard/summary`** + `fetchDashboardFirstSegment` (legacy fallback). **Phase 3:** discovery / universe / posture IA + `click-hierarchy.ts`. **Phase 4:** **`dashboard_context` v1** + assistant serializer. **Phase 5 (2026-05-15):** `dashboard-slo.ts`, `dashboard-fetch-resilience.ts`, load/chaos + degraded-shell tests; SLO table in **`PERFORMANCE.md` §1**. **Deploy:** `terraform apply` for summary API route. **Deferred:** full click matrix on all desk surfaces; prod P75 measurement pass. |
| D10 (Phase 5) | Admin hub — consolidated maintenance UI (Cognito-group gated) | 2026-05-12 | Single `/dashboard/admin/*` surface that consolidates every operator tool behind the existing `signal-analytics-admin` Cognito group (NO new auth model — admin status is read from `cognito:groups` exactly as `analysis_authorized()` and `isSessionAdmin()` already do). **Backend**: 8 new admin endpoints across the signals + brokers Lambdas — `GET /v1/admin/users/search` (Cognito `ListUsers` email-prefix lookup; capped + sorted), `GET /v1/admin/users/{user_id}` (Cognito + `UserProfile` + group membership composition), `POST /v1/admin/users/{user_id}/reset-password` (Cognito `AdminResetUserPassword`), `POST` / `DELETE /v1/admin/users/{user_id}/groups/{group}` (whitelist-gated to `ASSIGNABLE_GROUPS={signal-analytics-admin}` at the handler layer — IAM is broader so future whitelist additions don't churn permissions), `GET /v1/admin/audit/recent` (global newest-first feed via bounded `Scan` with optional `module` / `route_prefix` server-side filters, hard ceiling `_MAX_RECENT_SCAN_LIMIT=1000`), `GET /v1/admin/parameters/current` (readable view of live `SignalParameters` via `ParameterStore.get_parameters_sync()`), `GET /v1/admin/system-status` (aggregator: live parameter row + most-recent `ParameterHistory` row + pending-proposal count + admin user count via `ListUsersInGroup` + founding-member count via Cognito `ListUsers` with the `founding-member` group + 5 most-recent audit events). **New service module** `stocvest/api/services/admin_user_directory.py` composes Cognito with `UserProfile`; **new handler module** `stocvest/api/handlers/admin_users.py` for user-mgmt; **new handler module** `stocvest/api/handlers/admin_audit.py` for the global feed; **new handler module** `stocvest/api/handlers/admin_system_status.py` for the aggregator; `admin_parameters.py` extended with `admin_parameters_current_handler`. **Entitlement bump**: `_serialize_user_profile` accepts a new `is_admin` flag that OR-bumps `has_full_access` and `has_ai_explanations` so admins transparently get every paid feature for their **own** profile (load-bearing: the flag is `False` when an admin views **another** user's profile via `admin_beta_access_patch_handler` — bumping target flags based on actor's group would be a privilege confusion bug); `ai_explanations_handler` short-circuits the entitlement gate via the same `analysis_authorized()` check by overlaying `beta_full_access=True` on a *copy* of the calling user's profile (never persisted). **IAM**: new statement `Sid=CognitoAdminUserDirectory` on `lambda_api_execution` scoped to the project Cognito user pool ARN granting the seven Cognito Admin* actions used by the new handlers; the existing Secrets Manager / DynamoDB statements pick up the new endpoints unchanged. **API Gateway**: 8 new routes added in `infra/apigateway_6e.tf` (all under existing `module_key` integrations — `signals` for parameters/system-status, `brokers` for the user-mgmt + audit routes — matching where the underlying handlers' module IAM blast radius lives). **Bootstrap script**: `scripts/grant_admin.py` (boto3 cognito-idp wrapper with `--grant` / `--revoke` / `--list` flags accepting `--email` or `--user-id`, dotenv-loaded pool id + region, `AdminListGroupsForUser` round-trip to verify the mutation reflected) — the **only** way to seed the first admin since the UI's group toggle requires admin to call it. **Frontend**: 4 new pages under `/dashboard/admin` — `/` Hub Overview with `StatusGrid` tiles (current parameter version + last rotation + pending proposals + admin count + founding members + recent audit), `/users` with email-prefix search + detail panel + four mutation actions (grant/revoke admin, reset password, beta toggle, per-user audit feed), `/audit` with global filterable feed (module + route_prefix dropdowns) + pagination, `/parameters` with read-only current `SignalParameters` table + the relocated `ParameterRollbackSection` (moved from `/dashboard/admin/proposals` — proposals page is now review-and-promote only). Server-component pages double-gate via `getDashboardAuthContext()` redirecting `!isAdmin` away to `/dashboard`. **Collapsible Admin sidebar group** in `sidebar.tsx` + `mobile-nav-drawer.tsx` with `useAdminNavExpanded` custom hook persisting state in `localStorage` and auto-expanding when the active route starts with `/dashboard/admin`. **Typed clients**: `lib/api/admin-users.ts` (search + detail + reset-password + group add/remove with discriminated `AdminUserMutationOutcome` carrying upstream status + error code), `lib/api/admin-audit.ts` (`fetchRecentAuditEvents` + `fetchUserAuditEvents` + `statusCodeTone` display helper), `lib/api/admin-parameters-current.ts`, `lib/api/admin-system-status.ts`. **BFF routes**: 8 new thin proxies under `frontend/app/api/stocvest/admin/*` (verbatim status/body/content-type pass-through on top of `stocvestAuthedFetch`; the missing `beta-access` BFF was also added so the existing `PATCH /v1/admin/users/{user_id}/beta-access` is reachable from the new users page without a curl invocation). **`isDashboardNavItemActive`** updated so `/dashboard/admin` (the hub root) uses exact-match for active highlighting, preventing `/dashboard/admin/users` from highlighting both the parent and the child — mirrors the existing exact-match special case for `/dashboard` itself. |
| D10 | Automated weight-tuning proposal pipeline (Phases 1–4) | 2026-05-12 | Phase 1 `ParameterProposal` DDB table + repo (foundation, dark). Phase 2a pure-function walk-forward grid-search optimizer (`weight_optimizer.py`). Phase 2b scheduled worker Lambda + dedicated minimal-privilege IAM (NO `secretsmanager:UpdateSecret`) + EventBridge cron `(0 8 ? * MON *)`. Phase 3a admin review backend (4 routes under `/v1/admin/proposals`, `proposal_review.py` orchestrator, atomic secret rotation + ParameterHistory + sibling auto-supersede). Phase 3b admin review frontend (`/dashboard/admin/proposals` page with JWT-decoded `isSessionAdmin()` gate, 4 thin BFF routes, typed client, role-gated sidebar). **Phase 4 safety net**: daily `weight_rotation_monitor` Lambda publishes `Stocvest/Signals/PostRotationAccuracyDelta` CloudWatch metric (current vs previous parameter_version directional accuracy over trailing 14d, MIN_RESOLVED_SAMPLE=30 guard, closed-set status: ok/degraded/insufficient_sample/baseline_unavailable); CloudWatch alarm fires only on `Status=degraded` ≥ 5pp drop and publishes to a new SNS topic with optional email subscription; new `POST /v1/admin/parameters/rollback` + `GET /v1/admin/parameters/history` admin routes (gated on `analysis_authorized()`); rollback orchestrator reconstructs `SignalParameters` from the prior `ParameterHistory` row and **forward-writes** via the same `ParameterStore.save_parameters_sync` primitive used by promote (audit trail stays monotonic — never deletes or rewrites prior history); `changed_by` audit prefix `d10-rollback:<reviewer>` distinct from promote's `d10-admin:`; same three-IAM-principal posture (monitor Lambda has its own dedicated role with NO `secretsmanager:UpdateSecret`, scoped `cloudwatch:PutMetricData` to `Stocvest/Signals` namespace); frontend `ParameterRollbackSection` panel on the admin proposals page with version-history picker, live-version row disabled, and confirmation dialog explicitly enumerating the four production side-effects. |
| D9 | SIC → sector mapping confidence tiers | 2026-05-08 | `SicMappingTier` enum (`exact`/`prefix`/`coarse`/`fallback_spy`), `resolve_sector_bucket_from_sic` returns `(bucket, tier)`, `SectorMapper.get_sector_etf` 5-tuple, optional `sic_mapping_tier` on composite `layers[]` sector objects. |
| D8 | News + geopolitical reliability hardening | 2026-05-07 | `ticker_name_resolver.py` (SEC variants), `benzinga_client.py` (news/WIM/ratings/guidance/earnings + Polygon fallback), `NewsAnalyzer` neutral-available states, `GeoAnalyzer` structural baseline + cached daily themes, `geo_themes_updater` worker writing to Redis. |
| D7 | Multi-source news ingestion (Benzinga + SEC EDGAR) | 2026-05-05 | `edgar_client.py` polls SEC 8-K Atom (SEC-compliant UA, cached CIK→ticker), `news_triage.py` fast filtering, `news_worker.py` ECS process runs both feeds, `news_consumer_lambda.py` consumes SQS triage queue + scores via Claude. |
| D3 | Weight / prompt iteration | 2026-05-11 (P2 deferred) | Schema + storage (`signal_parameters.py` + Secrets Manager + `ParameterStore` 5m cache + `ParameterHistory` DDB + `parameter_version` snapshot columns), `GET /v1/signals/analysis` admin, scripts. Composite-engine wire-up via `build_composite_score_engine_from_params` single source of truth across all 3 callsites + analyzer wire-is-live lock-ins on all 5 analyzers (Technical/News/Macro/Sector/SwingTechnical). P2 secrets-managed Claude prompts deferred — see backlog P2. |
| D2 | Historical signal validation | 2026-05-11 (Phases 1–4) | Pure aggregation module → service layer over `SignalHistory` → auth-gated `GET /v1/signals/historical-validation/summary` (+ public mirror with overall+by_mode only). **UI (B46):** stratified 6-grid + cross-version diff moved to **admin-only** `/dashboard/admin/historical-validation`; user routes redirect to setup outcomes. Assistant prompts reference setup-outcomes + admin D2. NaN→null JSON safety; `legal_copy.HISTORICAL_VALIDATION_DISCLAIMER`. |
| D6 | Dynamic per-layer reasoning in swing composite | 2026-05-04 | Backend per-layer `reasoning` in `swing_composite_handler` contributions; frontend layer breakdown consumes dynamic reasoning when present. |
| D1 | Signal outcome pipeline | 2026-05-02 · commit `62b7e65` · ledger columns 2026-05-08 | `SignalRecord` + `SignalHistory` DDB + GSI `scope_generated_at`, `signal_recorder.record_signal`/`resolve_signals` (Polygon 1m close at T+1h/T+24h), `GET /v1/signals/me/history`, `/dashboard/signal-validation`, EventBridge `stocvest-signal-resolution` schedule. |

---

## P / PF — Platform & quality

| ID | Theme | Shipped | Summary |
|----|-------|---------|---------|
| P6 | HTTP/API audit DynamoDB + gateway wiring | 2026-05-07 · commit `3e21d5c` | `AuditEvents` DDB table (`pk`/`sk`), `DYNAMODB_AUDIT_EVENTS_TABLE` env, IAM `lambda_dynamodb_resources`, admin audit routes, `x-stocvest-session-id` CORS allow-headers. |
| PF11 | Signal card / composite data integrity (5 bugs) | 2026-05-05 · commit `313cf0f` | Composite contradiction penalty + `alignment_ratio` + `conflicted_layers`; evidence-builder VWAP fallback + `rr_warning`/`rr_quality`; `SignalRecord.status` filter for `incomplete`; lazy `user_profile_store`; Windows test conftest fix. |
| PF10 | Real 6-layer engine + `POST /v1/signals/composite/real` | 2026-05-04 | Server-side analyzers (technical/news/macro/sector + sector_mapper/geo/internals), `real_composite_engine.py`, `SectorCache` DDB, signals UI uses real endpoint, evidence modal `applySwingCompositeEnrichment` merges chips. |
| PF9 | CONTEXT market-data invariants + reusable VIX snapshot helper | 2026-05-04 | `Snapshot`/`_parse_snapshot` contract codified in CONTEXT §7/§13, `morning_brief_fetch.VIX_SNAPSHOT_FALLBACK_SYMBOLS` + `get_vix_snapshot_with_fallback`. |
| PF8 | Scanner API test timeout flake | 2026-05-04 | Frontend scanner tests timeout 5s → 15s. |
| PF7 | Signals stale data leak on insufficient state | 2026-05-04 | `hasValidSignal` gating in `signals-page-client.tsx`; clears composite/evidence/radar on insufficient-data; watchlist CTA preserved. |
| PF6 | Swing composite minimum live layers + insufficient UI | 2026-05-02 | `POST /v1/signals/swing/composite` requires 3 live layers; HTTP 200 with `status: insufficient_data` + `market_status` block; frontend amber callout. |
| PF5 | Gap Intelligence catalyst matching | 2026-05-03 | RTH 24h / off-hours 48h lookback, per-symbol news merge, `NewsCatalystDetector` listicle regex + noise list, `company_name` fallback with 0.8× narrative penalty. |
| PF4 | Scanner low-quality intraday / micro-cap gaps | 2026-05-02 | RVOL vs prior-day vol, min score 50%, ORB only before 10:00 ET + first-30m vol vs ADV, gap scan requires `prev_day_volume ≥ 1M`. |
| PF3 | Scanner ORB EXPIRED badge overlap | 2026-05-02 | Badge row layout `flex-wrap`, `margin-left: auto` on amber badge, dimmed disabled state. |
| PF2 | Regression: reference levels all n/a | 2026-05-02 | Day-bar scale check only runs when `last_trade_price > 0`; keeps session bar when last missing; threshold 5×; mirrors in `snapshot-reference-levels.ts`. |
| PF1 | Production: reference levels wrong vs spot | 2026-05-02 | Drop session bar when scale off vs last — superseded by PF2 (5× threshold, keep session when last missing). |
| MP1 | Model portfolio (signal tracking / notional) | Retired 2026-05-08 | Replaced by signal validation ledger on `SignalHistory` + `/dashboard/signal-validation`. Do not re-add `ModelPortfolio` or signals-lambda `/v1/portfolio/*` book routes. |

---

## ST — Swing trading (multi-day)

| ID | Theme | Shipped | Summary |
|----|-------|---------|---------|
| ST1 | Swing composite Phase 1 (six layers, daily data) | 2026-05-05 · commit `07f3b19` | `SwingTechnicalAnalyzer` + `SwingTechnicalParameters`, `POST /v1/signals/composite/swing` + BFF + Terraform route, swing-mode lookbacks (7d news / 14d econ / weekly sector vs SPY), response `mode` + `signal_expires`. Day composite gains `mode: day` + `signal_valid_until`. |

---

## Earlier waves (pre-2026-05-02)

Core foundations shipped before the first row above is tracked at high level in `CONTEXT.md` §1–§2 — signals engine, brokers, API Gateway, frontend redesign, order safety Step 8, legal compliance pass, earnings integration, public performance page shell, PDT enforcement, Crisp chat, etc. Treat regressions as bugs against `CONTEXT.md`, not as backlog reopens.
