# STOCVEST — Session Context

**Read this file at the start of each session.** It summarizes what exists, what is pending, and what is planned—without duplicating the whole repo tree.

**Cursor AI rules** for agents live in **`.cursorrules`** at the repo root (see **`docs/CURSOR_RULES.md`** for a short pointer—do not duplicate the full rules there).

**Last updated:** 2026-06-29 (latest — **B78 — Target geometry v3 (volatility-normalized, structure-derived T1/T2; SHIPPED live, flag default ON)** — bounded **T1** = nearest structural resistance in an ATR band [α,β] else `entry+α·ATR` (α/β = 0.8/2.0 day, 1.5/3.0 swing) so T1 no longer glues to entry on a high-of-day breakout (the recurring "T1 too tight" report); **T2** = `min(next structural resistance, entry+β₂·ATR, entry+2R)` (β₂ = 2.5 day / 4.0 swing) keeping the R-multiple fallback; **analyst price targets EXCLUDED from the gated T2** — they now carry `analyst_target` provenance and stay informational in `analyst_target_levels` (fixes the CYTK analyst-PT-labeled-"resistance" leak that overstated R/R at new highs); clustered/capped resistance candidates (adaptive ε = `max(0.3·ATR, price·0.2%)`, top-5 nearest within the ATR window, SMA20/50/200 enrich); ATR-normalized `reference_target_1/target_2/stop_distance_atr` exposed for calibration. New pure `stocvest/api/services/target_geometry.py` (+13 unit tests) wired into both long+short geometry in `swing_composite_evidence.py`; `target_provenance.py` + `frontend/lib/target-provenance.ts` gain `atr_extension` + `analyst_target` (gate still resistance-only); `sma200` exposed top-level in both engine builders; flag `STOCVEST_TARGET_GEOMETRY_V3_ENABLED` (`config.py` default **ON**) + Terraform var wired to the signals Lambda (`terraform.tfvars` = true, fmt clean). Requires ATR (falls back to v2 byte-identical when missing). Backend **2616 passed**, **4 skipped** (+17). Prior — **A7 — Assistant breadth-with-restraint (web search + multi-symbol comparison + uncertainty surface + plan-aware watchlist cap)** — the in-app assistant gains (1) a flag-gated Perplexity Sonar **web-search fallback** for out-of-envelope macro/policy/sector/"latest on…" questions (`assistant_web_context.py`, citation-backed, facts-no-verdicts; `STOCVEST_ASSISTANT_WEB_SEARCH_ENABLED`, enabled in `terraform.tfvars` + applied to the signals Lambda 2026-06-28); (2) **multi-symbol comparison** ("compare NVDA vs AMD") that fetches each ticker's price + STOCVEST cached read + analyst consensus in parallel and reports the contrast **without picking a winner** (`assistant_multi_symbol_context.py`, `detect_symbols`, `is_comparison_query`); (3) an explicit **uncertainty surface** — the cached six-layer read now carries plain-English `not_yet_confirmed` caveats (missing/split layers, neutral verdict, staleness) the locked prompt voices alongside the verdict; (4) a **plan-aware** watchlist-add cap (100 for swing_day_pro/beta, was a hardcoded 50). New locked-prompt sections (WEB CONTEXT RULES, MULTI-SYMBOL COMPARISON RULES, the STOCVEST-READ uncertainty rule). All additive; web search ships dark (env var ON now, code goes live on merge → CI deploy-lambda). +38 backend. Prior — **B77 step-1 — real intraday participation metric + override fix** — before doing Phase 2/3 (volume/participation gating), found the only persisted volume feature `tech.volume_vs_adv` is last-bar/ADV (structurally <1; all 343 cohort day signals <1.0) → the Phase-1 RVOL override never fired and a participation gate couldn't be validated. Built `signals/intraday_rvol.py` (time-of-day-normalized RVOL ≈1.0 = average pace, from a static intraday volume curve + already-fetched `sym_snap`/`spy_snap`, no new Polygon calls), now persisted on every day `SignalRecord` as `intraday_rvol`/`market_rvol` (shadow telemetry, additive) and used by the Phase-1 override. Phase 2/3 stay parked until this data accrues for validation. +16 (`tests/signals/test_intraday_rvol.py`). Prior — **B77 Day-desk midday session-phase quality gate (ships dark, flag default OFF)** — user asked how to improve the ~40% profitable rate; approved a read-only attribution + R:R-gate study on the 60-day as-traded replay cohort (`scripts/analyze_signal_attribution.py`). Three findings: **(1)** an R:R surfacing gate is **blocked on B76 geometry v2** — only 4% of stored signals had planned reward > risk (the old session-high-hugging-entry geometry), so gating on `reference_structure_level` throws away 96% for no gain (re-test on v2 targets later); **(2)** no single layer separates winners from losers (all |corr|<0.08 — the composite is saturated, every qualified signal scores ~max); **(3)** the clean lever is **time-of-day** — day-desk expectancy is +0.27% (9:30–10:00), −0.54% (10–12), **−1.03% (midday 12–14)**, −0.21% (14–16). Also corrected two earlier misreads: `setup_type=unavailable` (85%) is **not a bug** (ORB is computed only for the static `STOCVEST_SCANNER_SYMBOLS` list while signals fire across a dynamic universe → it's just a time-of-day proxy; do **not** expand ORB), and the `post_market` vwap label is the **swing desk** (EOD, market closed), not after-hours day trades (do **not** suppress). Per the user's design review the fix is a **soft penalty, not a hard block**: midday day fires (default 12:00–14:00 ET, env-configurable) take a **decision-score penalty** (default −12 on the 0–100 score vs MIN_ACTIONABLE 72) so only low-conviction setups fall below threshold, with an **RVOL override** (volume vs ADV ≥ 2.0 or sector-calibrated `volume_surge`) so news/momentum surges still qualify; suppressed signals are still recorded as **shadow** (no alert). Validated: midday [72,84) band = −1.5%/trade; penalty −12 drops those 50 worst signals → day expectancy **−0.53% → −0.38%** while keeping ~2× more optionality than a hard block. Applied in `evaluate_day_ledger_entry` (new `session_phase_penalty_0_100` + `session_phase_context` args, byte-identical when 0/None) fed from `real_composite_engine` (firing-time + RVOL computed before the gate); `is_day_ledger_dead_zone_et`/`parse_et_hhmm`/`DAY_DEADZONE_*` in `validation_timing.py`. Flags (`config.py`, all dark): `STOCVEST_DAY_SESSION_PHASE_GATE_ENABLED` (OFF), `STOCVEST_DAY_DEADZONE_START_ET`/`_END_ET` (12:00/14:00), `STOCVEST_DAY_DEADZONE_SCORE_PENALTY` (12), `STOCVEST_DAY_DEADZONE_RVOL_OVERRIDE` (2.0); Terraform vars wired to the `signals` Lambda (`infra/ecs_news_worker.tf` + `infra/lambda_6e.tf`). +20 backend (`tests/api/services/test_day_session_phase_gate.py`). **Phase 2/3 (future):** RVOL-confirmation refinements; replace the pure time-gate with a participation/regime gate. Prior — **B76 Swing/day target geometry v2 (analyst-PT T2 cap + degenerate session-high T1 rebuild; flag enabled)** — user asked why ATAI swing showed a **$14** target for a **$5.31** entry. Live probe of the deployed signals Lambda: entry $5.31 **at the high of day**, stop $3.75, **T1 = $5.31** (session high = entry → zero reward), **T2 = $14.0** (`reference_target_2_provenance="resistance"` but `analyst_target_levels=[14.0]`, `analyst_target_source="perplexity"`) → R/R **5.6**. So $14 isn't a chart level — it's a Perplexity ~12-month analyst PT (+163%) that reached T2 because `scan_nearest_resistance_above` lets `extra_levels` (analyst targets) **bypass** its 25% structural proximity band; combined with a session-high T1 collapsed onto entry, the planner promotes the distant T2 → fantasy 5.6:1. Fix in the **shared** `swing_composite_evidence._long_side_geometry` (swing + day both flow through `build_swing_composite_evidence_fields`): (A) analyst `extra_levels` now bounded by a new `extra_proximity_pct` arg on `scan_nearest_resistance_above` — beyond `analyst_max_pct` above entry they're dropped as a swing T2 (ATAI: $14 > $5.31×1.40 → T2 falls to 2R ≈ $8.43, R/R ≈ 2.0; PT stays visible in `analyst_target_levels`); (B) a session-high T1 that doesn't clear entry by `_MIN_T1_REWARD_PCT` (0.3%) is rebuilt from nearest in-band structural resistance → 1R projection → % fallback (T1 never equals entry); (C) `_long_target_provenance_label` words T1 honestly ("Structural level (T1)") when it's no longer the raw session high. Flag-gated **`STOCVEST_SWING_TARGET_GEOMETRY_V2_ENABLED`** (`config.py`, default OFF → served geometry byte-identical to legacy) + tunable `STOCVEST_ANALYST_TARGET_MAX_PCT_FROM_ENTRY` (default 40.0); Terraform `var.swing_target_geometry_v2_enabled` wired to the signals Lambda (`infra/ecs_news_worker.tf` + `infra/lambda_6e.tf`), **enabled in `terraform.tfvars` 2026-06-26**. +5 backend (`tests/api/services/test_analyst_target_levels.py`). Prior — **Trading-room company names: degraded-on-empty BFF fix** — user reported company names missing on the feed cards **and** the center deep-dive (screenshot also showed the center pane in a transient "signal service temporarily unavailable" state). Diagnosis (not a data bug — Polygon `/v3/reference/tickers` and the deployed `market_data` `GET /v1/market/symbol-names` both return the names cleanly when invoked directly): the cards already render `card.company` and the deep-dive `resolvedCompany`, but those are backfilled by a **separate** async lookup (`useSymbolNames` → BFF `/api/stocvest/market/symbol-names`). The desk-leader payload (`DeskDiscoveryLeader`) carries **no** company field, so when that side-channel fails the name silently vanishes. Root-cause trap: the BFF route only set `degraded:true` when the upstream call itself failed (`!upstreamOk`), but the backend handler returns **HTTP 200 with `{names:{}}`** on its own (best-effort) reference miss — a 200-with-empty slips through as non-degraded, and the client hook (`use-symbol-names.ts`) then caches those symbols as **"resolved, no name" permanently** (no retry until full reload). Fix (`frontend/app/api/stocvest/market/symbol-names/route.ts`): return `degraded:true` whenever **zero** names resolved — including the 200-with-empty case — so the client treats it as "retry later" instead of caching empties forever; names now self-heal after any backend blip. Frontend-only, +1 regression test (`tests/symbol-names-bff.test.ts` — 200-with-empty → degraded); relevant suites green (`symbol-names-bff`/`feed-model`/`symbol-name` 15/15). No backend / Terraform change. **Follow-up queued (B75):** for names that are bulletproof even during a full backend outage, enrich `discovery_row.py` desk leaders with cached company names at desk-build time so the name ships inside the desk payload itself. Prior — **Day ledger profit-target exit (ships dark)** — added a take-profit rule to the live day position monitor (`ledger_position_monitor.run_ledger_position_monitor`): when the snapshot last price reaches the reference target (`reference_structure_level`) the row closes via **`day_profit_target`**, checked **before** the VWAP-violation rule. Flag-gated **`STOCVEST_DAY_PROFIT_TARGET_EXIT_ENABLED`** (default **OFF** → byte-identical legacy exits). Motivated by a new read-only as-traded replay (`scripts/replay_qualified_exits.py`): over a 60-day window the qualified **day** cohort runs ~39% hit / R:R ~0.83 / **−0.42%/trade** (~−0.02R, near scratch in R terms) with **avg MFE +1.60% > avg MAE −1.33%** — favorable excursion the VWAP/flatten-only exits give back; adding the target moved replayed day expectancy from −0.79% to ~−0.40%/trade. +2 tests in `tests/api/services/test_ledger_position_monitor.py`. Prior — **Signal outcome resolver throughput fix** — diagnosed why ledger 1d outcomes were stalling: the `signal_resolution` Lambda (EventBridge `rate(30 minutes)`, **120s** timeout) resolved outcomes with **sequential** Polygon REST lookups over a full-table scan (9,656 rows; backlog **~1,215 unresolved 1h + ~1,359 unresolved 1d**). The 1h pass alone (~1,215 × ~0.2s) blew past 120s, so the 1d pass (which runs after) **starved** — Jun 22 day rows that should've resolved by Jun 23 were stuck (8/29). Root cause was **throughput, not a price bug** (the lookup returns valid prices). Fix in `DynamoDBSignalRecorder.resolve_signals`: build the unresolved set network-free, sort **newest-first** (recent rows — what accuracy/reporting reads — resolve promptly; backlog drains over runs), cap at **`_RESOLVE_MAX_PER_PASS=2500`** per horizon, and fetch prices with **bounded concurrency** (`_RESOLVE_CONCURRENCY=8`) in chunks that persist incrementally (durable if the Lambda still times out). Outcome math, ledger-open skip, and PUBLIC-mirror writes unchanged. Backend +2 (`tests/api/test_signal_recorder.py`: concurrent-resolves-all + cap-newest-first). **Code-only — needs deploy (CI `deploy-lambda` on merge) to take effect; backlog clears on the next run after deploy.** Prior latest — **Swing technical display fix — lagging golden cross** — when price has broken below both SMA50 and SMA200, a stale 50>200 "golden cross" is now labeled honestly: the chip reads **"Golden Cross (lagging)"** and the brief says **"broken structure (… golden cross is lagging — price has broken below both)"** instead of misreading it as "uptrend". **Confirmed display-only after a score-path audit** (the reviewer correctly flagged that a lagging cross could be feeding the score, not just the label): the golden-cross structural credit only fires under `durable_uptrend` (price *above* both MAs), so it does **not** enter SPCH's score (raw composite **−42 → clamped to 0**, invariant to `golden_cross_score` ∈ {0,8,200}); the `confluence_pattern="golden_cross"` is inert in `ConfluenceDetector` (requires an "orb" pattern). 0 is the calibrated magnitude here — the B73 graded mean-reversion floor is gated on RSI<30 and SPCH is RSI ~34 (a still-falling "knife", not a stabilized oversold bounce), so it correctly stays at the clamp. Swing Technical **score and verdict unchanged** (SPCH stays 0/bearish). +3 tests in `tests/signals/test_swing_technical_analyzer.py` (36 total, incl. credit-invariance gate). Prior latest — **News relevance × impact × age weighting (B74, ships dark)** — validation-first (`scripts/validate_news_relevance_impact.py`, no-lookahead: weighting each headline by relevance × impact and shrinking thin evidence toward 50 raised forward-10d edge **+2.12% → +2.97%**; the calls it neutralizes were ~coin-flip noise +0.57%/51%, and it tames **79%** of extreme 100/0 prints). New flag **`STOCVEST_NEWS_IMPACT_WEIGHTING_ENABLED`** (OFF by default → News score byte-identical to legacy flat average). When ON, `news_analyzer` scales each quality article by **relevance × impact** (Claude per-article estimates via read-through cache when present, else validated heuristic = publisher credibility × catalyst-keyword magnitude) and, in the pure-headline regime, applies a **confidence shrink** toward neutral when total relevance/impact-weighted evidence is thin (`news_impact.py`). Claude scorer now emits `relevance`/`impact` (back-compat 1.0 defaults); cache stores + reads them (`enrich_rows_with_cached_impact`, wired in both engines); a lone, stale, low-impact headline can no longer print 100. §13 → **2511**/4 backend (+20: `tests/signals/test_news_impact.py` (7) + analyzer flag on/off (3) + scorer relevance/impact parse (1) + cache relevance/impact (4) + assertions). prior — **Layer verdict band on score bars (score-vs-read clarity)** — the per-layer 0–100 score is a *magnitude*; the Bullish/Bearish/Neutral read only leaves neutral once it clears a calibrated cutoff well above the 50 midpoint, so a "positive but neutral" score (e.g. sector 62 / geo 58, both inside their neutral band) is correct, not a bug. Both composite engines now emit per-layer **`bullish_threshold`/`bearish_threshold`** on each `layers[]` row (shared `layer_verdict_band` helper; Technical uses swing vs day params per mode; geo/internals reference new analyzer constants `GEO_*`/`INTERNALS_*_THRESHOLD`), and the deep-dive score bar shades the neutral dead-band + marks the cutoffs + a `Neutral band ≤N – ≥M` caption (`signals-layer-breakdown.tsx`). Additive/display-only contract (API_CONTRACTS §layers[] verdict band); does not change gates. §13 → **2058**/329 frontend (+3), +5 backend unit (`tests/api/services/test_layer_verdict_bands.py`). prior — **Trading-room feed: market-closed Day-lane handling** — when the US regular session is closed (`marketOpen === false` from `marketStatus`), the trading-room signal feed now **defaults to the Swing lane** (one-shot, ref-guarded so it never overrides a manual/deep-linked lane) and the **Day lane** — still reachable — shows a `Market closed · resumes next open` pill on its section header so day session-movers read as not-tradable-now context rather than live entries (`dashboard-trading-room.tsx`: `SignalFeed`/`FeedLaneSection` gain `marketOpen`/`noticeLabel`). Complements the deep-dive, which already showed a "resumes next open" state for day setups. Frontend-only, display-only; build clean + dashboard/trading-room suite green (no test-count change; §13 stays **2055**/329). prior — **Deep-dive brief UX fixes: plain-English summary + bias reconcile** — (1) the Plain-English brief no longer states the same blocker twice (the session-move/ATR fact was emitted once as `Primary check:` and again, lower-cased, inside the backend `watch_for` tail) — new `dedupeWatchForAgainstBlocker()` strips any clause already named in the primary blocker (`deep-dive-present.ts`); (2) a new jargon-free **`buildPlainSummary()`** is now the default brief (deliberately no EMA/VWAP/ATR/R-R/"layers"/"gates" — "what it is / how much agrees / the one thing to do"), with the prior technical brief moved behind a **"Show the detailed read"** toggle in `deep-dive.tsx`; (3) the trading-room **left-rail leader card** pill now follows the composite **signal** (`verdict`/`signal_summary`, via new `leaderBias`) instead of the session/gap **move** direction, and stays **neutral** when no composite read is cached — so a big green day on a name the engine reads short (e.g. WYFI +17.9% / SHORT) no longer shows a contradictory BULLISH pill (`feed-model.ts`). Frontend-only, display-only, no gate/contract change; +5 frontend tests (4 in `deep-dive-present.test.ts`, 1 in `feed-model.test.ts`), full Vitest **2055**/329. prior — **B72-UI / B73-UI: new technical signals surfaced in the deep-dive** — both composite engines now emit a top-level **`sector_technical_calibration`** object (`{sic_bucket, regime: high_beta|normal|defensive, rvol_threshold_multiplier, overbought_penalty_multiplier}`), mirroring the additive B71 `news_geo_sensitivity` contract. The Signals deep-dive threads it onto the **Technical** row and renders a `…-CAL` regime chip + InfoTip + a "Sector calibration" fact-line for non-normal regimes only (`frontend/lib/signals/composite-layer-rows.ts`, `frontend/components/signals/signals-layer-breakdown.tsx`). The **B73 "Oversold bounce watch"** chip already auto-surfaces via the existing layer-chip drawer path (verified — it survives `filterDisplayChips`). Display-only, no gate change; +4 frontend tests (2 threading in `composite-layer-rows.test.ts`, 2 render in `signals-layer-breakdown-ui.test.tsx`). prior — **P66-followup: sector benchmark-ETF cache coverage**: fixed a real sector-layer accuracy gap — the daily relative-strength cache (`sector_daily_cache.py` `SECTOR_ETFS_TO_TRACK`) warmed only the 11 SPDR sectors + SMH/KBE/GLD, but `DEFAULT_SECTOR_TO_ETF` also resolves **biotech→XBI, pharma→XPH, medical-devices→IHI, retail→XRT, airlines→JETS, transport→XTN, defense→ITA, mining→XME**, so those ~8 sectors always missed the cache and degraded to the single-day live-snapshot fallback instead of the intended multi-day **persistence momentum**. Added those 8 (+ `SOXX` defensively for symbol→ETF rows that resolve to SOXX rather than SMH) to the warm list with display names, and added a **drift-guard invariant test** asserting every `DEFAULT_SECTOR_TO_ETF` value is warmed. Confirmed via subagent trace that the **SOXX-vs-SMH split is not a bug** — laggard intelligence uses a separate `PriceCache` (`stocvest:price:SOXX:*`), the composite uses `sector_daily_cache` (SMH); two independent subsystems by design. Backend **2491**/4. No Terraform / no API schema change (one extra daily Polygon call per added ETF on the cache-warm batch). Prior — **B73 Mean-reversion bounce floor (swing)**: addresses "should a stock below SMA20/50/200 score ~0? what about the pull-back/bounce chance?" — the swing scorer floored *every* deep downtrend at 0 with no resolution between a bounce-prone oversold name at support and a free-falling knife. **Validation-first** (`scripts/validate_mean_reversion_credit.py`, no-lookahead sweep, 64 names / ~2.4y / 4,340 confirmed-downtrend cases): RSI depth **monotonically** tracks forward 10-day bounce (RSI<20 → +4.8% / 84% positive; 25–30 → +1.6% / 63%) and still-falling "knives" (fresh lows) underperform stabilizing names — so the credit is **oversold-led** with a knife filter. Implemented in `swing_technical_analyzer.py` as a **non-additive floor** (`max(trend, floor)` — *cannot* stack): when price < SMA50 & SMA200, MACD < signal, **RSI < 30**, and today is **not a fresh 10-day low**, the score is lifted off 0 by an amount set by oversold depth (RSI-led 0.8, downside-extension 0.2), **hard-capped at 38 < bearish_threshold 40** so it adds resolution but **never flips the bearish verdict**. Surfaces an "Oversold bounce watch" chip + reasoning line + `mean_reversion_floor_applied` result flag (UI not yet wired). Swing-only (mean reversion is time-based; day mode untouched); non-oversold downtrends (MSFT/TSLA RSI ~32–50) and all uptrends are unaffected (gate shut → identical behavior). New params on `SwingTechnicalParameters` (`mean_reversion_*`, `atr_period`); no Terraform / no API schema change. Prior — **B72 Technical accuracy: swing recalibration + sector calibration**: fixed the reported **NVDA Technicals = 5/100** — a healthy uptrend (above SMA50/200, golden cross, RSI ~50) was floored by stacked overlapping pullback penalties. The swing technical scorer (`swing_technical_analyzer.py`) now (1) **magnitude-scales** the below-SMA20 penalty (a shallow dip costs far less than a deep break), (2) credits a **durable uptrend** (`golden_cross_score` when price is above both SMA50 & SMA200 with a golden cross), (3) reads a drop off the recent high inside a durable uptrend as a **pullback, not a breakdown** (caps %-from-high at the moderate tier), (4) stops **double-counting MACD** (fading penalty only while the histogram is still ≥ 0), and (5) penalizes **distribution lighter** than accumulation is rewarded (`volume_distribution_penalty`). NVDA now reads ~41 (neutral) — verified live. Added **sector-relative technical calibration** (`stocvest/signals/sector_technical_calibration.py`, reusing `normalize_sector_for_geo`): a coarse `HIGH_BETA / NORMAL / DEFENSIVE` regime scales the **RVOL surge threshold** (day analyzer) and **overbought-penalty persistence** (both analyzers) — same buckets, sector-scaled inputs (fixed structure, mirrors B71). `sic_bucket` is plumbed through both analyzers, the day close-fallback resolver, and both composite engines; unknown sectors stay neutral (1.0 — unchanged). No Terraform / no API schema change (composite response *values* shift for recognized sectors); `sector_technical_calibration_payload` is ready to surface but not yet wired to the UI. Prior — **B71 News/Geo sensitivity UI surfacing**: the composite responses now emit a top-level **`news_geo_sensitivity`** object (`{ sic_bucket, news:{band,multiplier}, geopolitical:{band,multiplier} }`, additive/informational — `layer_sensitivity_payload` in `news_sensitivity.py`, wired in both engines) and the Trading Room deep-dive **Layers** tab renders a per-stock **sensitivity chip** + drawer fact-line on the News & Geopolitical rows (`compositeToSignalsLayerRows` threads it onto the rows; `SensitivityChip` in `signals-layer-breakdown.tsx`) so it's clear what News/Geo weighting a given stock gets today (Phase A sector priors). Display-only — does not change gates. Prior — **B71 Phase C scheduled report**: the news event-study now runs **weekly** as a `news_event_study_report` api-Lambda module (EventBridge Scheduler, Sun 18:00 ET) that scans `SignalHistory` and writes a dated JSON report to a new **reports S3 bucket** (`aws_s3_bucket.reports`, `s3:PutObject` granted on the shared api role, 365-day lifecycle); gated by `var.news_event_study_report_enabled` / `STOCVEST_NEWS_EVENT_STUDY_REPORT_ENABLED` (default OFF), still read-only / not wired into scoring. Prior — **B71 Phase C scaffold + cache observability**: a read-only offline **news event-study** (`stocvest/signals/news_event_study.py` + `scripts/news_event_study_report.py`) estimates per-symbol **sensitivity ratio** (mean |forward return| on news vs quiet days) and **sentiment predictiveness** (sign agreement) from stored `SignalRecord` snapshots + forward returns — **exploratory, not wired into live scoring** (Phase C learn→up-weight stays gated on validating these numbers + data accrual); MAE/MFE columns confirmed unpopulated, so v1 uses signal-co-temporal 1h/1d returns only. Plus **EMF CloudWatch metrics** for the sentiment cache (`CacheCandidates`/`CacheHits`/`PrimeEnqueued`, zero-latency stdout, no IAM). Prior — **B71 Phase D activation + self-prime (dark / flag-gated)**: flags wired in Terraform (`var.news_sentiment_cache_enabled` / `var.news_sentiment_prime_enabled`), cache TTL raised to 6 days to cover the 120h swing lookback, and the composite now **self-primes** the cache — enqueuing abstaining cache-miss articles to the triage SQS for async scoring (`prime_missing_news_sentiment`, Redis-deduped, one `send_message_batch`) so a missed Benzinga-REST headline becomes a hit on a later pass; hit-rate/enqueue counts logged. **Activation still requires the ECS news worker running.** Prior — read-through **Claude sentiment cache** for the composite news layer — the async news consumer now also writes a content-keyed (URL→sha1, normalized) sentiment entry (`stocvest/signals/news_sentiment_cache.py`), and the day+swing engines fill otherwise-abstaining (`insights == []`, e.g. Benzinga-only) headlines from it via one batched `MGET` before scoring, so Claude polarity reaches the composite **without** a synchronous Claude call on the hot path. Gated by `STOCVEST_NEWS_SENTIMENT_CACHE_ENABLED` (**default OFF**) and fully **fail-open** (Redis down / miss / disabled → unchanged behavior); labels normalized bullish/bearish→positive/negative; ships dark pending cache hit-rate validation; prior — **B71 Phase B**: article-level **news-event capture** — the composite engines now persist their top `catalyst_headlines` (published_at + per-ticker sentiment_score + source + catalyst type) into `NewsSnapshot.top_events` on `SignalHistory` (new `NewsEventCapture` model; additive JSON, **no Terraform**, no scoring change) so a later event study (B71 Phase C) can join each headline to the symbol's subsequent price move; prior — **B71 Phase A**: per-symbol **News/Geo sensitivity weighting** — a new `stocvest/signals/news_sensitivity.py` maps the symbol's sector bucket (+ ADR geo override) to coarse `HIGH/MEDIUM/LOW` bands and a **down-only** influence multiplier (`1.0 / 0.85 / 0.6`); `CompositeScoreEngine.compute` gains an optional `sensitivity_multipliers` kwarg that scales **effective layer weight** for News/Geo (renormalizes, never a gate, never flips a layer's sign), wired into both day + swing engines; unknown sectors stay neutral `1.0` (identical to pre-B71); up-weighting + the event-study learning loop are tracked as **B71 Phases B/C/D**; no Terraform / no API change; prior — **B70-fix**: `AiSetupRead` now resets to idle when the host swaps symbol/direction/desk, so switching tickers no longer leaves the previous ticker's AI read on screen; prior — **B70** AI setup read + richer deep-dive brief: the Plain-English brief now threads real specifics (market regime, named confirming/conflicting signals, freshest catalyst) and picks a deterministic per-symbol phrasing variant so tickers read differently (`buildRichBrief` in `deep-dive-present.ts`); a new optional **AI read** (`POST /v1/signals/ai/explanations` `type=setup_read` → `explain_setup_read`, Claude `AI_MODEL_FAST` temp 0.7, Redis-cached, deterministic `fallback_text` for free/outage) is surfaced via the shared `AiSetupRead` expander on both the Trading Room Deep Dive and the Signals evidence card — reuses the existing route/BFF/gating, no Terraform; prior — **B58-fix** causal narrative: a directional layer in a neutral setup now describes its own read — e.g. "Sector reads bullish, but the overall setup is neutral …" — instead of the misleading "no clear leadership versus SPY" canned copy (`causal_narrative.py` + `causal-narrative.ts`); prior — **P68** Signal Math Contract + financial-math accuracy, **B69** tracked trade plans + thesis alerts (**Terraform applied 2026-06-20** — `TradePlans`/`SystemSignalTransition` tables + `trade_plans` Lambda + `/v1/trade-plans/*` routes; `trade_plans` code ships via CI on merge); prior — **B68** Setup Evolution score analytics tab, **B67** Trading Room Market Brief reset, **B66** layer drawer rich evidence; prior — **P66** signal-engine fixes, **P67** Next.js dev cache, **OPS-LEDGER** daily verification; prior **B65** BFF reliability; prior **B64** entry-zone synthesis).
**Repo:** https://github.com/roygurijala/stocvest  
**Test baseline (regression gate — must match §13):** Backend `pytest tests/ -q` → **2616 passed**, **4 skipped** (verified **2026-06-29** after **B78 — Target geometry v3** — bounded ATR-band T1, multi-candidate T2 (`min(structure, entry+β·ATR, 2R)`), analyst PTs excluded from the gated T2 (`analyst_target` provenance), clustered/capped resistance, ATR-normalized distances; +17 across `tests/api/services/test_target_geometry.py` (13) and `tests/api/test_swing_composite_evidence.py` (4 v3-wiring). Prior **2599 passed**, **4 skipped** (verified **2026-06-28** after **A7 — Assistant breadth-with-restraint** — assistant web-search fallback (Perplexity, flag-gated, citation-backed, facts-no-verdicts) + multi-symbol comparison (parallel side-by-side, no winner) + STOCVEST-read `not_yet_confirmed` uncertainty caveats + plan-aware watchlist-add cap (100); +38 across `tests/api/services/test_assistant_web_context.py`, `tests/api/services/test_assistant_multi_symbol_context.py`, `tests/api/services/test_assistant_watchlist_action.py`, `tests/utils/test_symbol_detector.py` (`detect_symbols`), `tests/utils/test_intent_detector.py` (web-search + comparison intents), `tests/api/services/test_assistant_symbol_context.py` + `tests/signals/test_assistant_broader_coverage.py` (read limitations), and `tests/api/handlers/test_signals_assistant_chat.py` (web/comparison/cap wiring). Web search ships dark — env var applied to the signals Lambda; code goes live on merge (CI deploy-lambda). Prior **2561 passed**, **4 skipped** (verified **2026-06-28** after **B77 step-1 — real intraday participation metric (shadow telemetry) + override fix** — added `signals/intraday_rvol.py`: a time-of-day-normalized relative volume (`day_volume / (ADV × typical-cumulative-fraction-by-clock-time)`, ≈1.0 = average pace) using a static US-equity intraday volume curve + the already-fetched `sym_snap`/`spy_snap` (no new Polygon calls). Persisted on every day `SignalRecord` as `intraday_rvol` (symbol) + `market_rvol` (SPY) for later validation. Discovery that forced this: the Phase-1 RVOL override used `tech.volume_vs_adv` (last-bar/ADV) which is structurally <1 — **all 343** cohort day signals were <1.0, so the ≥2.0 override never fired; it now uses the real `intraday_rvol`. Additive optional fields (no contract/Terraform change). +16 (`tests/signals/test_intraday_rvol.py`: curve anchors/interp/monotonic, RVOL pace, bad-input None, SignalRecord round-trip). Prior **2545 passed**, **4 skipped** (verified **2026-06-28** after **B77 Day-desk midday session-phase quality gate** — flag-gated soft decision-score penalty (default −12) on day signals firing 12:00–14:00 ET, with an RVOL-surge override; suppressed signals still recorded as shadow. 60-day as-traded replay drove the design: midday is the −1.0%/trade dead-zone (12:00 hour −1.19%/74), the worst is the [72,84) decision-score band (−1.5%), and penalty −12 lifts day expectancy −0.53% → −0.38% while keeping ~2× more signals than a hard block. All flags default OFF → qualification byte-identical to legacy. +20 in `tests/api/services/test_day_session_phase_gate.py`. Prior **2525 passed**, **4 skipped** (verified **2026-06-26** after **B76 Swing/day target geometry v2** — analyst-PT T2 distance cap + degenerate session-high T1 rebuild, flag-gated `STOCVEST_SWING_TARGET_GEOMETRY_V2_ENABLED` (default OFF); +5 in `tests/api/services/test_analyst_target_levels.py` (v2-off-is-legacy-ATAI, analyst-T2 cap, degenerate-T1 rebuild, keeps-session-high-when-rewarding, scanner extra-proximity cap). Prior **2520 passed**, **4 skipped** (verified **2026-06-25** after **Alert-email hardening + tradeable-only prefs** — execution-actionable emails now render **Stop loss / Take profit / Entry zone**; Postmark send records **FAILED** unless HTTP 200 *and* `ErrorCode 0`; every send attempt emits `Stocvest/Alerts EmailSendOutcome` (Result=sent|failed) with a CloudWatch failure alarm + SNS (`infra/alert_email_health.tf`); all 13 users migrated to execution-actionable-only prefs (`scripts/set_all_users_tradeable_alerts.py`). Root cause of the 10-day silent outage: Postmark account hit its monthly send limit ~2026-06-15 (API kept returning OK; app recorded "sent"). +2 (`tests/services/test_email_service.py`, `tests/services/test_postmark_client.py`). Prior **2518 passed**, **4 skipped** (verified **2026-06-25** after **Day ledger profit-target exit (ships dark)** — flag-gated `day_profit_target` take-profit rule in the day position monitor, default OFF; +2 in `tests/api/services/test_ledger_position_monitor.py`. Prior **2516 passed**, **4 skipped** after **Signal outcome resolver throughput fix** — `DynamoDBSignalRecorder.resolve_signals` now resolves newest-first with a per-pass cap (2500) and bounded concurrency (8) in incrementally-persisted chunks, so the 1d pass no longer starves behind the 1h backlog (was: sequential lookups over a 9.6k-row table > 120s Lambda timeout). +2 in `tests/api/test_signal_recorder.py`. Prior **2514 passed**, **4 skipped** (verified **2026-06-23** after **Swing technical display fix — lagging golden cross** — a stale 50>200 cross on a name that has broken below both key MAs (e.g. SPCH: price −55% from high, below all MAs, RSI ~34) now reads as **broken structure** with a **"Golden Cross (lagging)"** chip instead of the misleading "uptrend"/"Golden Cross". **Verified score-path clean** (not just cosmetic): the golden-cross structural credit is gated behind `durable_uptrend` (price *above* both MAs), so SPCH's raw composite is **−42 → clamped to 0** and is *invariant* to `golden_cross_score` (0/8/200 all → 0); `confluence_pattern="golden_cross"` is inert in `ConfluenceDetector` (needs "orb"). Display-only confirmed (score/verdict unchanged), +3 in `tests/signals/test_swing_technical_analyzer.py` (now 36, incl. a credit-invariance gate test). Prior **2511 passed**, **4 skipped** (verified **2026-06-23** after **B74 News relevance × impact × age weighting (ships dark)** — +20 across `tests/signals/test_news_impact.py` (heuristic/Claude resolve, confidence shrink), `test_news_analyzer.py` (flag OFF identical, thin-stale shrinks, strong-fresh unshrunk), `test_news_sentiment.py` (relevance/impact parse + defaults), `test_news_sentiment_cache.py` (persist + flag-gated read-through); prior **2491 passed**, **4 skipped** (verified **2026-06-22** after **P66-followup sector ETF coverage** — +3 in `tests/workers/test_sector_daily_cache.py` (every benchmark ETF warmed, no dup, display-name coverage); prior **2488**/4 after **B73 Mean-reversion bounce floor** — swing +5 in `tests/signals/test_swing_technical_analyzer.py` (now 33: floor lifts a stabilizing oversold downtrend, knife/RSI gates hold, ceiling caps below bearish_threshold, non-additive); prior **2483**/4 after **B72 Technical accuracy** — swing recalibration +3 in `tests/signals/test_swing_technical_analyzer.py` (then 28) + new `tests/signals/test_sector_technical_calibration.py` (17); prior **2463** after **B71 News/Geo sensitivity UI** (+1 `layer_sensitivity_payload` test in `tests/signals/test_news_sensitivity.py`, now 11); frontend +2 in `frontend/tests/composite-layer-rows.test.ts`; prior **2462** after **B71 Phase C scheduled report** (weekly news event-study Lambda → S3 — `tests/workers/test_news_event_study_report_worker.py` (4)); prior **2458** after **B71 Phase C scaffold** (read-only news event-study report — `tests/signals/test_news_event_study.py` (11)) + **cache EMF metrics** (+3 in `test_news_sentiment_cache.py`, now 21); prior **2444** after **B71 Phase D self-prime** — composite enqueues abstaining cache-miss articles for async scoring, +7 in `tests/signals/test_news_sentiment_cache.py`; prior **2437** after **B71 Phase D** read-through Claude sentiment cache — `tests/signals/test_news_sentiment_cache.py` (11); prior **2426**/4 after **B71 Phase B** news-event capture — `tests/api/services/test_signal_snapshot_news_events.py` (4); prior **2422**/4 after **B71 Phase A** News/Geo sensitivity — `tests/signals/test_news_sensitivity.py` (10); prior **2412**/4 after **B70** AI setup read; prior **2406**/4 after **B58-fix**). Frontend `cd frontend && npm run test` → **2035 passed**, **328** files (verified **2026-06-20** after **B70-fix** AI-read stale-symbol reset, +1 test in `ai-setup-read.test.tsx`; full suite **2034**/328 after **B70**; prior **2029**/327 after **B58-fix**). Prior backend **2274**/5 (2026-06-10, P66). Prior frontend **1909**/290 files (2026-06-10).

---

## 1. Status at a glance

| Track | Status | Notes |
|--------|--------|--------|
| Core data + indicators | ✅ | Polygon client, `stocvest/indicators/` |
| Swing + day-trading signals | ✅ | `stocvest/signals/` (layers, scanner, briefing, journal engine, PDT tracker); **swing vs day composite** — same six layers; `SwingTechnicalAnalyzer` + daily bars; `POST /v1/signals/composite/swing`; dashboard **Day trade / Swing trade** toggle (`localStorage` `stocvest_trading_mode`) |
| Brokers | ✅ | Mock, IBKR, E*TRADE adapters; factory; PDT hook |
| HTTP API (Lambdas) | ✅ | Market, **signals** (HTTP submodule + public/user record routes + **`GET /v1/signals/founding-members`** paid-plan counter for landing + **`GET /v1/signals/me/history`** with optional **`mode=day|swing`** for the signal validation ledger + **`POST /v1/signals/assistant/chat`** (authenticated STOCVEST Assistant — locked server-side system prompt, whitelisted page-context tail, `has_ai_explanations` gating, deterministic fallback for free users; Anthropic **Messages API** via the proper `system` parameter, not a concat-into-user trick) + **`POST /v1/public/assistant/chat`** (unauthenticated STOCVEST Assistant for the marketing surface — same locked prompt with PUBLIC MODE section activated via `session_mode=public`; `page_context` is ignored server-side; deterministic-intro fallback when Claude is unreachable so the homepage chat never appears broken)), **brokers** (**broker portfolio** `POST /v1/portfolio/holdings|summary|allocation`), scanner, journal, PDT, orders, auth; **`UserProfile` beta override** (`beta_full_access`, `beta_access_until`, `beta_access_granted_at`) with **`has_full_access`** / **`has_ai_explanations`**; admin **`PATCH /v1/admin/users/{user_id}/beta-access`** (same auth as signal analysis); **`PATCH /v1/users/me`** blocks client writes to subscription + beta fields; **HTTP audit capture** — `audit_capture.py` + `audit_store.py` (DynamoDB when **`DYNAMODB_AUDIT_EVENTS_TABLE`** set, else in-memory), `_with_cors_and_audit` on HTTP dispatch; admin **`GET /v1/admin/audit/users/{user_id}`** and **`GET /v1/admin/audit/sessions/{session_id}`**; clients may send **`x-stocvest-session-id`** for session-scoped replay; journal `user_id` from JWT only; free-text **`text_sanitize`**; **`log_privacy`** for CloudWatch-safe logs |
| Frontend (Next.js) | ✅ | **STOCVEST Assistant** chatbot mounted globally in **`app/layout.tsx`** for **every** visitor (`isAuthenticated` derived from `getServerSession()` selects which BFF endpoint to call — authenticated → `/api/stocvest/signals/assistant/chat` with full page context, anonymous → `/api/stocvest/public/assistant/chat` with messages only). Anonymous marketing visitors on `/`, `/login`, `/signup`, etc. can now chat — locked system prompt's PUBLIC MODE section answers "what is STOCVEST?", "how is it different from signal-alert services?", and general finance/term explanations while continuing to refuse all trade advice and predictions. (`components/assistant/*` + `lib/assistant/*`): floating lens launcher with slow pulse, glassmorphism panel with **no-bubble** vertical conversation rail (now with **strong role-based visual distinction** — user turns sit in an accent-tinted panel with a solid accent-block "YOU" chip and right-aligned text, STOCVEST turns sit in a surface-muted panel with a quiet tone-tinted label and left-aligned text; the asymmetry plus contrasting backgrounds make the two roles unmistakable even in monochrome), **six-layer constellation context strip**, word-fade reveal on fresh assistant turns (respects **`prefers-reduced-motion`**), explicit **General / Contextual** mode badge; **cross-page isolation** — the assistant tracks the page identifier each conversation belongs to and wipes the history the moment the user navigates to a different page so an answer about TTD on Signals never leaks into a question on Dashboard; **session reset** — sign-out buttons in `sidebar.tsx` + `mobile-nav-drawer.tsx` call `clearAssistantSession()` before the server action and the assistant subscribes to `subscribeSessionExpired` so token expiry / any 401 also clears the conversation; **every** logged-in dashboard page publishes whitelisted context via `usePublishAssistantContext` so the chatbot is contextual mode-aware everywhere the user can navigate — **signals** (`dashboard/signals/layers` or `/history`) publishes per-symbol context (decision_state, decision_rationale, layer_status, trade_readiness, risk_reward, market_regime, layer_alignment_pct, analysis_status), **scanner** (`dashboard/scanner`) publishes the multi-symbol overview context (`scanner_focus`, `market_open`, gap-with/without-catalyst counts, `ranked_setups_count`, qualitative `top_setups`/`top_gaps_with_catalyst` capped at 3 each with bucketed strength/quality — no raw scores, `swing_setups_suppressed`, `setups_empty_message`), **home dashboard** (`dashboard`) publishes `trading_mode: "swing"` + `market_regime` + `ranked_setups_count`, **signal-validation** (`dashboard/signal-validation`) publishes `trading_mode` (from the active **swing/day** tab), **options** (`dashboard/options`) publishes the active `symbol`, **crypto** (`dashboard/crypto`) publishes the active `symbol`; **watchlists** / **earnings** / **portfolio** / **journal** / **performance** / **futures** / **settings** / **legal** publish just the page identifier (which is enough — the locked system prompt's "page identifier only" rule activates and the LLM answers in calm general terms about what that page does); the signals page derives the authoritative Decision through the shared **`lib/signal-evidence/trade-decision.ts`** (single source of truth used by both `signal-evidence-card.tsx` and the assistant context). Auth, dashboard redesign, **Gap Intelligence** scanner panel + **morning brief** on scanner paths when enabled (not on home dashboard); scanner/signals/**watchlists**/**dashboard/portfolio** (broker) / journal/options/crypto/futures, **public landing** (`landing-page.tsx` / `app/page.tsx` — hero, day vs swing modes, live engine **SWING/DAY** tabs, differentiation, workflows, transparency, comparison, yesterday’s signals, pricing + **founding-member** scarcity from **`GET /v1/signals/founding-members`** via **`lib/api/founding-members.ts`**, footer; no home-dashboard morning brief), legal pages; **`/dashboard/signal-validation`** (tracked signal outcomes; legacy **`/portfolio`** redirects); BFF **`/api/stocvest/signals/**`** (swing composite, **`composite/real`**, user history, record by id) + **`/api/stocvest/portfolio/**`** (broker portfolio only); **signals page** uses server **`composite/real`** for layer scores (no client `bullishBias` heuristic); **after-hours research panel** (`signals-after-hours-panel.tsx`) for closed-market insufficient-data states; **compact PDT status pill** beside Market/VIX (`pdt-status-pill.tsx`); **Market Pulse** strip (SPY/QQQ/VIX + regime from scanner overview) replaces the embedded **Market Intelligence** headline grid; **on-demand `NewsPanel`** (`news-panel.tsx`) — fetch on open, per-symbol **2m** client cache, wired from dashboard signal cards, evidence (incl. “View all news”), signals page; **`vercel.json`** security headers (+ HSTS on **stocvest.app**) |
| Journal automation (B1) | ✅ | `journal_order_hooks` on order submit (open/close rows, optional signal fields), `GET /v1/journal/analytics`, entry GET/PATCH, journal UI + scanner → portfolio order prefill |
| Onboarding + legal acknowledgment (B2) | ✅ | `UserProfile` legal + onboarding fields; `GET`/`PATCH /v1/users/me`; mandatory **legal acknowledgment modal** + optional **onboarding wizard** on `app/dashboard/layout.tsx`; BFF `/api/stocvest/users/me` |
| Order safety (Step 8) | ✅ | `order_safety.py`, validate/submit BFF, confirmation modal, paper/live mode |
| **Watchlists (B4)** | ✅ | `stocvest/data/watchlist_store.py` (**`scan_default_watchlists`**, **`find_users_with_default_watchlist_symbol`** for scheduled jobs only), `scan_symbols.get_scan_symbols`, `GET`/`POST`/`PATCH`/`DELETE` watchlists + default symbols API (brokers Lambda), dashboard **Watchlists** page, BFF `/api/stocvest/watchlists/**`, **dashboard scanner** loads default symbols in parallel with PDT and passes them into **`fetchScannerOverview`** / **`loadScannerDataWithoutBrief`**; scanner gap path defers Dynamo until Polygon 401/403 fallback; **B14** scheduled scan merges platform default-watchlist symbols (cap 30) + config + **`SYSTEM_DEFAULTS`** (cap 40 total); **`watchlist_scanner_alerts`** + **`UserProfile.email`** (optional Dynamo mirror) for best-effort signal emails within **2s** `asyncio.wait_for` + **4h** dedupe via **`had_signal_email_for_symbol_within_hours`** |
| **Alerts email (B3)** | ✅ | `stocvest/data/models.py` alert enums + prefs/record, `alert_store.py`, `stocvest/services/email_service.py` (SES), `alert_trigger.py`, `GET`/`PATCH /v1/alerts/preferences`, `GET /v1/alerts/history`, non-blocking signal + PDT emails; settings **Alert Preferences** + `docs/DEPLOYMENT.md` SES notes; Lambda IAM `ses:SendEmail` |
| Legal compliance pass | ✅ | `GlobalDisclaimer`, `SignalDisclaimerChip`, signal-oriented copy, public API field names + `disclaimer` |
| D1 Signal outcome pipeline | ✅ | `SignalRecord` / `SignalHistory`, `signal_recorder` (record + **`resolve_signals`** using Polygon **1m aggregates** at T+1h and next RTH 4:00 PM ET close for 1d via **`get_evaluated_price_after_signal`**), `GET /v1/signals/recent` + performance summary (**`correct_direction_count`** / **`incorrect_direction_count`** / **`neutral_direction_count`**), **`GET /v1/signals/records/{id}`**, **`GET /v1/signals/me/history`**, **`GET /v1/signals/me/records/{id}`**; resolution Lambda + EventBridge **`stocvest-signal-resolution`** **`ENABLED`**, **`rate(30 minutes)`** → **`stocvest-development-api-signal_resolution`**. Apply **2026-05-03** (see `docs/D1_SIGNAL_RESOLUTION_SCHEDULE.md`). **B62 (2026-05-22):** scheduled **`ledger_capture`** (3:55 PM ET) writes qualified + **shadow** gate-attempt rows (`:ledger_capture_shadow`) with **`gate_status_json`** including per-gate outcomes, **`execution_quality`**, **`evaluation_source`** — for Phase 3 validation-rule study; default user history still **`ledger_only=true`**. See **`IMPLEMENTED.md` B62**. **Optional later:** weekly horizon, extra analytics. |
| News ingestion pipeline | ✅ | **Combined** Benzinga WebSocket + SEC EDGAR 8-K feed in one worker: `stocvest/data/edgar_client.py`, `stocvest/data/news_triage.py`, `stocvest/workers/news_worker.py`, `stocvest/workers/news_consumer_lambda.py`; SQS queue `stocvest-news-triage-queue`; EDGAR filings map into canonical `NewsArticle`; Lambda consumer scores pre-triaged articles with Claude and best-effort pushes scored rows to Redis. |
| Terraform / AWS apply | 🚧 | **Development** now includes the news SQS/Lambda pipeline in `infra/` plus **`AuditEvents`** DynamoDB, **`DYNAMODB_AUDIT_EVENTS_TABLE`** on API Lambdas, and API Gateway routes for **`GET /v1/signals/founding-members`**, admin **beta** / **audit** paths, and **`POST /v1/signals/assistant/chat`** (apply to create/update — no new secrets needed: Anthropic key reused from `stocvest/lambda-runtime`). **B69 applied 2026-06-20** — `terraform apply` (**11 added, 21 changed, 0 destroyed**) created **`TradePlans`** + **`SystemSignalTransition`** DynamoDB tables, the **`trade_plans`** API Lambda, the **`/v1/trade-plans/*`** routes (`GET`/`PUT`, `PUT /sync`, `DELETE /{plan_id}`, `POST /thesis-alerts`), and added **`STOCVEST_TRADE_PLANS_TABLE`** / **`DYNAMODB_SYSTEM_SIGNAL_TRANSITION_TABLE`** env + IAM to the API Lambdas (`trade_plans` real code ships via CI **`deploy-lambda`** on merge to `main`). **Still pending for always-on ECS runtime:** publish a worker image and set `news_worker_container_image` / `news_worker_desired_count` so the Fargate service is actually created; also keep production/stage promotion, secret rotation, S3 artifact bucket policy review, and Cognito/Vercel env alignment in sync per environment. |
| Phase 7 (E2E, audits, paper validation) | 🚧 | **HTTP/API audit trail to DynamoDB** (`AuditEvents`, redacted summaries) + admin query routes shipped; E2E suite, security review, load tests, extended paper validation still open (**BACKLOG P1**) |
| **B46 Setup analytics** | ✅ | **2026-05-17** — setup evolution + outcomes replace user-facing validation/performance marketing; admin D2 at `/dashboard/admin/historical-validation`; APIs + GSI in `IMPLEMENTED.md` B46. **Pending:** Phase 6 `SignalHistory` retirement (`BACKLOG` B46-Phase6). |
| **B47 Progress presentation** | ✅ | **2026-05-16** — display-only tiers (**Near ready** @ 4/6); maturation/scanner/dashboard/scenario/onboarding copy; daily pulse; drilldown links; watchlist **↑/↓** progression chips via maturation-summary + transition log. Backend gates unchanged. **B60 (2026-05-21):** persisted **`progress_band`** + maturation-summary **`near_ready_*`** aggregates + dashboard/watchlist engagement — still no gate change. See `IMPLEMENTED.md` B47, B60. **Not shipped:** push/badge/week-in-review (**B41**). |
| **B58 Causal narrative** | ✅ | **2026-05-21** — informational headwind chain on swing/day composite + Signals UI + assistant context. **2026-06-20 fix:** under a neutral setup, a directional layer (e.g. bullish Sector with a high score) now reads "Sector reads bullish, but the overall setup is neutral — it isn't confirming a single direction on its own" instead of the misleading canned "Sector participation is mixed — no clear leadership versus SPY"; `_default_because` (`causal_narrative.py`) + `defaultBecause` (`causal-narrative.ts`) now take the layer verdict/direction. The X/6 directional-alignment count is unchanged (1 bull vs 1 bear ties → 0 aligned is correct). See **`IMPLEMENTED.md` B58**. |
| **B59 Day-desk MTF** | ✅ | **2026-05-21** — shared weekly/intraday alignment on day composite (informational nudge only). See **`IMPLEMENTED.md` B59**. |
| **B60 Near-ready engagement** | ✅ | **2026-05-21** — **`progress_band`** + maturation-summary near-ready rollups + dashboard strip + watchlist filter. See **`IMPLEMENTED.md` B60**. |
| **B61 Signals desk UX** | ✅ | **2026-05-21** — compact **Mode · Last evaluated · ⓘ** line; desk tabs below sticky chrome. See **`IMPLEMENTED.md` B61**. |
| **B62 Ledger capture + execution quality** | ✅ | **2026-05-22** — scheduled gate evaluation for default watchlists; shadow **`SignalHistory`** rows + soft **`execution_quality`** on composites (not a gate). **Extended P65 (2026-06-08):** split schedules — **`ledger_capture_day`** **3:55 PM ET**, **`ledger_capture_swing`** **4:00 PM ET** (fixes swing starvation under combined `desk=both`). **`terraform apply`** + CI **`deploy-lambda`**. See **`IMPLEMENTED.md` B62**, **`SIGNAL_ENGINE.md`** § Signal validation ledger. **Phase 3 deferred:** R/R review after 2–3 weeks telemetry. |
| **B68 Setup Evolution tab (score analytics)** | ✅ | **2026-06-10** — Evolution tab redesigned around score trend (0–100), state journey, layer stability, inflection/momentum, forward extrapolation. API **`analytics`** on setup-evolution; **`signal_score`** on new transition rows. See **`IMPLEMENTED.md` B68**, **`SETUP_ANALYTICS_SPEC.md`** § Evolution tab UX. |
| **B67 Trading Room session reset (Market Brief)** | ✅ | **2026-06-10** — First open each NY day + post-logout → **Market Brief**; SPA return restores last symbol; session expiry re-auth unchanged (`?next=`). See **`IMPLEMENTED.md` B67**, **`DASHBOARD_TERMINAL_UX_PLAN.md` §9**. |
| **B66 Layer drawer rich evidence** | ✅ | **2026-06-10** — Layer drawers show curated headlines, analyst actions, technical readouts (`layer-drawer-present.ts`). See **`IMPLEMENTED.md` B66**. |
| **B63 Trading Room dashboard (redesign — live `/dashboard`)** | ✅ | **2026-06-05** — Production `/dashboard` renders **`DashboardTradingRoom`** (`frontend/components/dashboard/trading-room/*`) via **`TradingRoomPreviewContent`** (`app/dashboard/page.tsx`; also at `/dashboard/preview`). Three-zone terminal: **Session header** (regime + plain-English read, market-status pill, SPY/QQQ/IWM/**VIX** with window labels, desk counts, **global symbol search**, "Updated …"), **Signal feed** (ranked compact day/swing cards from `lib/dashboard/trading-room/feed-model.ts`; **`QuietFeed`** = Session-activity movers + Building-structure quiet leaders when the desk is quiet; per-feed `SymbolSearch`), and a center panel defaulting to the **Market Brief** (`market-brief.tsx` — greeting by **first name**, AI narrative, indices+VIX, **1-day + 5-day** sector chips, movers, headlines, signal summary, "Market pulse" with right-aligned refresh time) that swaps to the **Deep Dive** (`deep-dive.tsx` — verdict header, plain-English brief, **Setup / Layers / Evolution** segmented tabs, analyst ratings) on select. Collapsible **Watchlist rail** (`watchlist-rail.tsx`) is the third column; last selection restored on SPA return (`session-selection.ts`), Brief on hard refresh. Reuses `fetchDashboardFirstSegment` (now returns **`sectorRotation`** 1d/5d) + deferred `DashboardScannerClientFetch`. Backend: **`first_name`** on `UserProfile` (`GET`/`PATCH /v1/users/me`) + AI narrative **`GET /v1/market/brief`** (`stocvest/api/services/market_brief.py`, warm-container cache) with BFF proxies `app/api/stocvest/market/{brief,news}/route.ts`. **Deploy:** `terraform apply` (new `/v1/market/brief` route) + CI `deploy-lambda` (market_data). See **`IMPLEMENTED.md` B63**, **`DASHBOARD_TERMINAL_UX_PLAN.md` §9**. |
| **P65 Pre-beta signal engine P0** | ✅ | **2026-06-08** — Six-layer trust hardening: swing/day technical analyzers (PDH/PDL, session momentum, swing drawdown dominance), macro regime thresholds (`momentum_weight` **0.45** in Secrets v1.1.0), per-mode composite weights (`swing_composite` / `day_composite`), entry-zone validation, weight-proposer cohort filters (exclude shadow/unavailable), shadow-row dedupe script, **split ledger schedules** (`ledger_capture_day` 3:55 PM ET, `ledger_capture_swing` 4:00 PM ET; scanner timeout **300 s**). **Ops:** rebuild API Lambdas with **`scripts/build_lambda_package.ps1`** (Linux wheels — never Windows `pip` + zip). See **`IMPLEMENTED.md` P65**, **`SIGNAL_ENGINE.md`**, **`TUNING_PLAYBOOK.md`**. |
| **B65 Dashboard dev reliability (BFF + tape backfill)** | ✅ | **2026-06-08** — Client Components no longer call API Gateway directly: **`browserApiFetch`** rewrites `/v1/*` → `/api/stocvest/*` via **`api-path-to-bff.ts`**. New BFF routes: `market/{snapshot,macro-context,bars-batch,earnings}`, `scanner/gap-intelligence`, `signals/{day,setups,swing/setups,scanner-trace}`. **`useDashboardTape`** client-backfills SPY/QQQ/IWM/VIX + market status when RSC paints empty on cold Lambda. Watchlist rail retries default symbols on 503; Edge-safe middleware (`atob` only). See **`IMPLEMENTED.md` B65**, **`API_CONTRACTS.md` §4.11**. |
| **P66 Signal engine calculations + sector/maturation/ledger** | ✅ | **2026-06-09** — Merged **PR #121–#122** (`fix/signal-engine-calculations`). **Calculations:** neutral composite skips directional alignment penalty; MACD O(n); EMA crossover uses per-bar snapshot; RSI overbought/oversold symmetric; `DEFAULT_BASE_WEIGHTS` synced with `CompositeParameters`; unmapped sector uses SPY path not locked 50; macro VIX tier comments. **Sector clustering fix:** Redis re-enabled on scanner + signals Lambdas (`STOCVEST_DISABLE_REDIS=0`); Dynamo **`SectorDailyCache`** fallback in `sector_daily_cache.py`; composite-scale sector scores rejected in validation gates; **`sector_layer_score`** on swing shadow rows. **Maturation:** `derive_maturation_state` aligned with composite `decision_state`; sync after ledger capture (`watchlist_ledger_capture.py`); catch-up script **`scripts/catchup_watchlist_maturation.py`** (prod execute: 186 jobs, stale hints 181→25). **Deploy:** `terraform apply` + CI **`deploy-lambda`**; **`sector_daily_cache`** worker invoked (14 ETFs; scores varied — not stuck at 20). See **`IMPLEMENTED.md` P66**, **`SIGNAL_ENGINE.md`**. |
| **P67 Next.js dev static 500 fix** | ✅ | **2026-06-09** — Merged **PR #123**. Stale `.next` webpack chunks caused `/login` static 500s. **`frontend/scripts/clear-next-cache.mjs`**, `npm run clean:next`, `npm run dev:3002`. See **`IMPLEMENTED.md` P67**. |
| **B69 Tracked trade plans + thesis alerts** | ✅ | **2026-06-20** — Code complete + tested; **Terraform applied 2026-06-20** (`terraform apply` → **11 added, 21 changed, 0 destroyed**: `TradePlans` table + `trade_plans` Lambda + 5 routes + `system_signal_transition` table; `trade_plans` Lambda real code ships via CI **`deploy-lambda`** on this merge to `main`). Commit-to-track a frozen entry/stop/T1/T2 snapshot (planning only, not a broker order); `localStorage`-first, synced to DynamoDB **`TradePlans`** via **`trade_plans`** Lambda (`GET`/`PUT /v1/trade-plans`, `PUT /v1/trade-plans/sync`, `DELETE /v1/trade-plans/{plan_id}`, `POST /v1/trade-plans/thesis-alerts`; auth-required, cap 24/user). Frontend `/dashboard/plans` hub + live re-assessment hooks + `components/trade-plan/*`; best-effort **self-notification** thesis-alert emails. See **`IMPLEMENTED.md` B69**. |
| **P68 Signal Math Contract + financial-math accuracy** | ✅ | **2026-06-20** — New internal contract (single source of truth for scoring math), mirrored backend↔frontend: **`stocvest/signals/signal_math_contract.py`** + **`frontend/lib/signal-math/contract.ts`** — defines the three score scales (layer `0..100`/50, directional `-1..+1`/0 verdict ±0.20, unit `0..1`), neutral-handling rule, normalization helpers, canonical six-layer set. Enforced + financial-math fixes: volume-amplifier neutral-at-50, **ORB strength = breakout/ATR + volume-gated tiered scoring** (was unused), confluence tier floors recalibrated to be reachable, **`gap_pct`** wired from prev close (`session_price_guard.session_gap_percent`), **true weekly RSI**, layer alignment ratio→count, composite directional clamps, frontend R/R **T2 provenance** threading. No Terraform, no API schema change (response *values* shift). See **`IMPLEMENTED.md` P68**, **`SIGNAL_ENGINE.md`** § Signal Math Contract. |
| **OPS-LEDGER Daily ledger verification** | ✅ | **2026-06-10** — Plain-English runbook **`docs/LEDGER_DAILY_VERIFICATION.md`** + **`scripts/ledger_signal_report.py`** (daily/weekly/monthly counts from `SignalHistory` PUBLIC scope: qualified/shadow/actionable per desk). Reports saved locally under **`reports/ledger/`** (gitignored `.txt`; folder tracked via `.gitkeep`). **Routine:** Mon–Fri after 4:15 PM ET daily; Monday weekly; 1st of month monthly. **Spot-check 2026-06-09:** day **4** qualified / **108** shadow; swing **0** qualified / **118** shadow (gates strict; jobs running). See **`IMPLEMENTED.md` OPS-LEDGER**. |
| **A7 Assistant Aime-parity gaps** | ✅ | **2026-06-03** — Four additive chat-payload features (all rendered deterministically by the UI, never invented by the model): **(1) In-chat ranked discovery** — `discovery_payload()` in **`stocvest/api/services/assistant_discovery.py`** returns `{mode, source, generated_at, rows[{symbol,context}], scanner_href}`; `DiscoveryCard` in **`assistant-conversation-rail.tsx`** renders a compact ranked table (symbol → `/dashboard/signals?...&ref=assistant`, why-line, "Open Scanner"). **(2) Source-citation chips** — new **`stocvest/api/services/assistant_citations.py`** (`build_citations`) de-dupes + recency-orders ≤4 news/Benzinga items with http URLs; `CitationChips` renders numbered external-link chips. **(3) Clarifying desk chips** — when a mode-sensitive query (`is_mode_sensitive_query`) is desk-ambiguous, handler returns `clarify={prompt,options[]}`; `ClarifyChips` sends a refining message via `onQuickReply` (threaded `stocvest-assistant` → `assistant-panel` → rail). **(4) Light personalization** — new `UserProfile.assistant_preferred_desk` (persisted as `assistantPreferredDesk` in `user_profile_store.py`); explicit desk language (`detect_explicit_desk` in `intent_detector.py`) is stored + resolves ambiguous discovery/watchlist mode; `=== USER PREFERENCE ===` block + `preference_context` param on `AssistantChatService.reply` + `USER PREFERENCE RULES` in `assistant_prompts.py`. Response payload adds `discovery`/`citations`/`clarify`; types in `frontend/lib/assistant/types.ts`. Tests: `test_assistant_citations.py`, `test_assistant_discovery_payload.py`, updated `test_intent_detector.py`/`test_user_profile_plan.py`/`test_signals_assistant_chat.py`/`assistant-panel-redesign.test.tsx`. No Terraform. |
| **A6 Assistant interactive charts** | ✅ | **2026-06-03** — Inline chart mini-card now renders **reference levels** (VWAP, support, resistance, analyst target, prev close, 50-day) as dashed lines + labeled chips in **`assistant-conversation-rail.tsx`** (`ChartCard`); a **lazy-loaded interactive full chart** via **TradingView `lightweight-charts`** (Apache-2.0, ~35 KB, v5) lives in new **`frontend/components/assistant/full-price-chart.tsx`** (candlesticks + volume histogram + 50-day SMA overlay + `createPriceLine` level lines + crosshair/zoom + ResizeObserver; `attributionLogo` on for license; dynamic-imported so the lib only ships when a user **expands**). Surfaced via an **"Expand chart"** toggle on the chat card **and mounted on the Signals page** (`signals-page-client.tsx` — full-width "Price Chart" card reusing `referenceLevels` → `chartLevels`). New BFF **`frontend/app/api/stocvest/market/bars/route.ts`** proxies **`GET /v1/market/bars`** (session-cookie auth via `stocvestAuthedFetch`; 5xx → `{bars:[],degraded:true}`). Backend chart-level computation already in **`assistant_symbol_context.py`** (`build_symbol_chart` + daily bars for SMA50/support/resistance). Types `AssistantChartLevel`/`AssistantChart` in `lib/assistant/types.ts`. Tests: `frontend/tests/assistant-panel-redesign.test.tsx` (level chips + expand toggle), `tests/api/services/test_assistant_symbol_context.py`. No Terraform change. |
| **A5 Assistant market + watchlist intelligence** | ✅ | **2026-06-03** — New intent detectors in **`stocvest/utils/intent_detector.py`** (`is_market_overview_query`, `is_watchlist_status_query`, `is_watchlist_opportunity_query`, `is_watchlist_intelligence_query`); market-pulse context **`stocvest/api/services/assistant_market_context.py`** + watchlist summary/opportunities **`stocvest/api/services/assistant_watchlist_context.py`** fetched + serialized into `AssistantChatService.reply` (`market_context` / `discovery_context` / `watchlist_context` params, extended token budget when present). **PE-style action-ticker fix** — `extract_action_symbol` bypasses the blocklist for explicit add/remove ("add PE", "remove MRVL") only when the token is `$`-prefixed or all-caps; whole-word matching; "WATCH"/"LIST" added to action stopwords. **Broader Benzinga coverage** — `assistant_symbol_context.py` pulls channel-tagged news (M&A / policy / sector / general, 48h) with `_benzinga_channel_category` + `_broader_coverage_lines` serializers; **news recency tightened to ≤2 days** (Polygon `days=2`, Benzinga `hours=48`). Prompt updates in **`assistant_prompts.py`** (NEWS SYNTHESIS RULES relevance-ranking + CHART & LEVELS NARRATION). Handler wiring in **`signals.py`**. Tests: `tests/utils/test_intent_detector.py`, `tests/api/services/test_assistant_market_context.py`, `tests/signals/test_assistant_broader_coverage.py`, `tests/api/handlers/test_signals_assistant_chat.py`. No Terraform change. CI **`deploy-lambda`** (signals module). |
| **A2/A3/A4 Assistant actions + discovery + routing** | ✅ | **2026-06-02** — Watchlist add/remove via chat (intent detection → `execute_watchlist_add/remove` → structured `action` card in rail); discovery queries ("what's moving?") pull from opportunity desk cache; deep-link routing adds `navigate_to=/dashboard/signals?symbol=X` on trade-planning questions + `[OPEN_SIGNALS]` prompt rule + CTA button in conversation rail. New: `stocvest/utils/intent_detector.py`, `stocvest/api/services/assistant_watchlist_action.py`, `stocvest/api/services/assistant_discovery.py`. Updated: `AssistantChatResult.navigate_to`, `AssistantMessage.navigate_to/action`, `AssistantAction` type, `assistant-conversation-rail.tsx` (`NavigateCta`, `ActionCard`). A1 test coverage: `tests/utils/test_symbol_detector.py`, `tests/api/services/test_assistant_symbol_context.py`, updated `test_signals_assistant_chat.py`, `frontend/tests/use-voice-input.test.ts`, `frontend/tests/assistant-panel-redesign.test.tsx`. No Terraform change. CI **`deploy-lambda`** (signals module). |
| **A1 Assistant capability upgrade** | ✅ | **2026-06-02** — Live symbol context (snapshot + news + Benzinga WIM/ratings/earnings/guidance fetched in parallel on detected ticker); image attachment (Claude vision via base64 content block, PNG/JPG/WebP ≤5 MB); Web Speech API voice input (English only, Chrome/Edge); assistant UI redesign (clean composer with + and mic inside input, collapsed constellation strip to dots/chip, 3 quick prompts max, single disclaimer line). Backend: **`stocvest/utils/symbol_detector.py`**, **`stocvest/api/services/assistant_symbol_context.py`**, updated **`assistant_chat.py`** (serialize_symbol_context, image content block, max_tokens 900 with live data) + **`signals.py`** handler (symbol detection + context fetch + image validation) + **`assistant_prompts.py`** (LIVE MARKET CONTEXT RULES + SCOPE/NEWS/ANALYST/EARNINGS/IMAGE synthesis rules). Frontend: **`use-voice-input.ts`** hook, redesigned **`assistant-panel.tsx`**, updated **`stocvest-assistant.tsx`** (AttachedImage state, submit wiring) + BFF route (413 guard) + types (AttachedImage, attachedImage on AssistantMessage). No Terraform change. CI **`deploy-lambda`** (signals module). |
| **L48 Laggard Intelligence Engine** | ✅ | **2026-05-18** — swing-only peer-divergence context (not scored, not trade advice): registry + price cache + detector + narratives + assembler on swing composite (`laggard_signal`, `unlock_forecast`, weekly timeframe alignment); paid HTTP **`/v1/signals/{symbol}/laggard`** + **`/v1/scanner/laggards`**; EventBridge warm/monitor/cluster jobs; frontend evidence + watchlist unlock + scanner section. See **`docs/LAGGARD_AUDIT.md`**, **`IMPLEMENTED.md` L48. |
| **B57 Hero + dashboard logos, crash hardening** | ✅ | **2026-05-19** — Landing **hero** lockup (`full_logo_with_tagline_1200w`); dashboard **top bar** + larger sidebar logos; Edge fetch + scanner hydrate + WebSocket teardown guards. See **`IMPLEMENTED.md` B57**. |
| **B56 Brand pack + footer wordmark** | ✅ | **2026-05-19** — **`public/brand/`** sized assets (header, full+tagline, wordmark, icons); **`StocvestLogo`** variants + landing **footer**. See **`IMPLEMENTED.md` B56**. |
| **B55 Text wordmark (no image logo)** | ✅ | **2026-05-19** — **`StocvestTitle`** replaces PNG **`StocvestLogo`** in sidebar, mobile nav, landing, auth. See **`IMPLEMENTED.md` B55**. |
| **B54 Dashboard loading + nav logo** | ✅ | **2026-05-19** — Compact top **scanner loading strip** (replaces large bottom “setups & gap” block); **desk-gated** copy only after scanner settles (muted, not error-orange). Sidebar **`nav`** logo crop aligned with top bar. See **`IMPLEMENTED.md` B54**. |
| **B53 Brand logo + dashboard stability** | ✅ | **2026-05-19** — Logo uses **`public/brand/stocvest-logo-full.png`** (not placeholder SVG). Dashboard: disable long-lived **SSE** `EventSource` (`useLiveSignals` no-op; **60s** `/api/dashboard` SWR poll only) to stop **Connection closed** client crash; harden `scanSummary` / earnings sort. See **`IMPLEMENTED.md` B53**. |
| **B52 Signals desk tab redesign** | ✅ | **2026-05-19** — Sticky **Setup | Layers | Evolution** tabs (`?tab=`), clickable **Bias / Alignment / Execution** KPI strip (Bias & Execution → Setup, Alignment → Layers), merged **Layers + radar**, Setup tab = forming banner + Why not + reference levels + scenario. See **`IMPLEMENTED.md` B52**. |
| **B51 Signals layer IA + mobile nav** | ✅ | **2026-05-19** — Force summary (supporting / opposing layers), bias-anchored role lines, section jump pills + scroll-snap, single evidence CTA, layers-before-radar on mobile. See **`IMPLEMENTED.md` B51**. |
| **B50 Trade conviction tiers** | ✅ | **2026-05-19** — **A+** / **B+** / **Developing** on Signals setup-read + watchlist decision cards (`trade-conviction-tier.ts`); verdict gates unchanged (swing **≥2.0**, day **≥1.3**). Scenario Builder **`low_risk_reward`** uses same desk minimum; A-tier display still **2.0 : 1**. See **`IMPLEMENTED.md` B50**. |
| **B49 Scanner quiet-desk IA** | ✅ | **2026-05-19** — zero-qualifying `/dashboard/scanner` uses one coherent story: **Near Ready** (structural + volume-proximity cards) → **Market conditions** (regime as context, volume as blocker) → **Scan outcome** (session-volume bars). No redundant full “what would change” block when cause is obvious; **Focus** line + scan-outcome watch hint instead. Gap Intelligence de-emphasized when empty; setups columns secondary with desk interpretive copy + **→ See Near Ready above**. Dashboard **command center** + shared **`lib/market-context`**. See **`IMPLEMENTED.md` B49**, **`DASHBOARD_TERMINAL_UX_PLAN.md`**. |

---

## 2. Implemented (where to look)

**Backend (`stocvest/`)**  
**Also shipped:** **STOCVEST Assistant chatbot** — **`stocvest/signals/assistant_prompts.py`** (system prompt held verbatim as `ASSISTANT_SYSTEM_PROMPT`; **2026-05-11 explanatory-voice upgrade** restructured the prompt into product-anchored sections — PRIMARY GOAL, CORE PRODUCT PHILOSOPHY ("Silence is an intentional system state"), ANALYSIS LAYERS (the six real product layer names — Technical / News / Macro / Sector / Geopolitical / Market Internals), MARKET REGIME (UI Bullish/Neutral/Bearish + engine risk_on/risk_off/neutral/avoid), SECTOR CONFIRMATION (Confirming / Non-confirming / Mixed; Risk-on / Defensive / Narrow), MARKET PULSE (Unavailable / Elevated / Upcoming / Known and absorbed; pulse never authorizes trades), INTERNALS & VOLATILITY (VIX is a signal inside Internals, NOT a standalone "VIC"), TRADE READINESS (0-100 scale, never 0.0-1.0), DECISION STATES (verbatim lines from `lib/signal-evidence/trade-decision.ts`), ALIGNMENT & CONFIDENCE (Layer alignment buckets + Trade Readiness, never an invented "System Confidence"), SUPPRESSION & GATING LOGIC (with the dashboard's exact empty-state copy: "No active swing setups right now", "System posture: Waiting for alignment", "Swing suppressed — risk-off tape", "Signal suppressed — regime not cleared"), REGIME TRANSITIONS (probabilistic language only — never forecast a flip), BACKTESTING & VALIDATION (route to `/performance` and `/dashboard/signal-validation` with the standing disclaimer; never invent a hit-rate), USER INTERACTION RULES (MAY / MUST NOT lists), PAYWALL AWARENESS (swing_pro / swing_day_pro / active beta — the LLM knows it's the paid path and never describes the deterministic free-tier fallback as an error), TONE & STYLE (frame inactivity as intentional and protective; frame silence as discipline; frame patience as skill), END GOAL & FOUNDATIONAL PRINCIPLE; **explicitly forbids invented concepts that do not ship**: "System Confidence", "VIC", "Volatility Control", "Supportive / Neutral / Hostile" volatility states, "Symbol Readiness Score", "Sector Fragmented", a 0.0-1.0 readiness scale, or any made-up "X% accuracy" figure; forbids bullet lists / numbered lists / section headings / markdown formatting in replies by default; **expanded ban-list of self-referential phrases** ("I don't have", "I can't see", "I can't access", "I would need to see", "to give you a precise explanation I would need", "at this moment", "right now I lack", etc.) with an explicit "stop and rewrite" instruction so the LLM cannot fall back to "I don't have access to live page data" even when the conversation history contains a different symbol than the current page; preserves the **banned response shapes** block with the concrete BAD/GOOD pair drawn from the exact failure mode we saw in production (LLM apologizing about TTD swing details after the user navigated to the home page); adds an explicit fallback rule when only a symbol/page is in context: explain STOCVEST in calm general terms instead of refusing; `sanitize_messages` drops any non-`user`/`assistant` turn so a client cannot inject a fake `system` role, caps single message at **2000 chars**, trims history to **12** turns; `serialize_page_context` whitelist-emits only known keys, including the new **`analysis_status`** = `loaded` \| `loading` \| `unavailable` \| `insufficient_data` so the assistant can distinguish "decision visible on screen" from "user has selected a symbol but the analysis has not loaded yet"), **`stocvest/signals/assistant_chat.py`** (`AssistantChatService.reply` builds `system = ASSISTANT_SYSTEM_PROMPT + serialize_page_context(...)` and posts to Anthropic Messages API with the proper top-level `system` parameter; free users — no `has_ai_explanations` — receive deterministic copy and never reach Claude; no conversation cache), **`assistant_chat_handler`** in **`stocvest/api/handlers/signals.py`** dispatched under **`POST /v1/signals/assistant/chat`** (returns `text`, `source`, `mode`, `upgrade_available`, `disclaimer`). **Tests:** **`tests/api/handlers/test_signals_assistant_chat.py`** (unauth 401, invalid body 400, free-user deterministic path, mode-from-context, paid-user AI path with service mock, sanitizer drops client-injected `system` role). **`AuditEvent`** + **`api/services/audit_store.py`** / **`api/services/audit_capture.py`** (writes when **`DYNAMODB_AUDIT_EVENTS_TABLE`** is set); **`get_founding_member_count`** + **`GET /v1/signals/founding-members`** (counts **paid** `swing_pro` / `swing_day_pro` / legacy `founding_*` only; **free** rows never increment the shared “first **100**” pool); admin **`PATCH /v1/admin/users/{user_id}/beta-access`** + audit query routes in **`orders.py`**; **`scripts/beta_access.py`**. **Tests:** **`tests/api/test_audit_capture.py`**, **`tests/api/test_user_profile_plan.py`**, **`tests/api/test_users_me.py`**. **Reliability pass:** `stocvest/data/benzinga_client.py`, `stocvest/data/ticker_name_resolver.py`, and `stocvest/workers/geo_themes_updater.py`; composite engines now merge Benzinga data, `NewsAnalyzer` returns neutral available states (not unavailable) for no-news windows, and `GeoAnalyzer` includes structural baseline + cached daily themes.  
`data/` (models incl. **`UserProfile`** legal/onboarding + optional **`email`** + **beta** fields (Dynamo mirror for scheduled alert delivery), **`SignalRecord`** (optional per-layer **JSON snapshot** columns + **`parameter_version`** for tuning; **`status`** `active` \| `incomplete` for incomplete reference-level payloads; optional **signal-validation ledger** fields such as **`closed_at`**, session dates, rationale, exit labels, MAE/MFE, **`gate_status`**, **`setup_type`**, **`exit_rule`**, **`hold_duration_minutes`**), **`AlertPreferences`** / **`AlertRecord`** / **`AlertType`**, **`EconomicCalendarEvent`**, **`watchlist_store`** + **`alert_store`**, **`edgar_client.py`** (SEC 8-K Atom polling + CIK→ticker cache + `EdgarFiling` → `NewsArticle` mapping), **`news_triage.py`** (high-signal keyword/category/watchlist gate + duplicate suppression), Polygon — snapshot parser may drop `day` OHLC/VWAP vs **`lastTrade.p`** only when last is a positive price and any session field is **>5×** off; missing last keeps session bar; **`get_evaluated_price_after_signal`** for D1 bar-based outcomes; **`get_economic_calendar_for_day`** on Benzinga economics when tier allows; **`BenzingaNewsStream`** in `polygon_client.py` for long-lived websocket ingestion), `indicators/`, `signals/` (sentiment, macro, geo, composite, **real layer analyzers** — `technical_analyzer`, **`swing_technical_analyzer`** (daily SMA/MACD/base/volume regime), `news_analyzer`, `macro_analyzer`, `sector_analyzer`, `sector_mapper`, `geo_analyzer`, `internals_analyzer`; **`confluence`** normalization helpers + multi-signal alignment in `confluence.py`, **`gap_intelligence`** + dynamic news lookback + **`news_catalyst_detector`** listicle-aware noise + company-name headline fallback, **`morning_brief`**, **`trade_journal`** + **`compute_journal_analytics`**, briefing markdown generator still in `daily_briefing.py` for legacy tests), `brokers/` (adapters, gateways, OAuth), **`services/email_service.py`**, **`services/alert_trigger.py`**, **`api/services/watchlist_scanner_alerts.py`** (scheduled intraday/EOD setup → watchlist user email; **4h** dedupe), **`api/services/news_relevance.py`** (**`calculate_article_relevance`**, **`categorize_article`**, **`deduplicate_articles`**, **`source_credibility_meta`** — dashboard headline ranking; distinct from composite **`news_analyzer`** filter), **`api/services/news_quality_filter.py`** (**`passes_market_intelligence_gate`** for `/v1/market/news` pool vs **`is_quality_article`** for analyzers), **`api/services/news_panel_format.py`** (panel **`age_label`**, source bucket + **`source_label`**, sentiment score/label, catalyst type for ticker news), `api/` (handlers incl. **`news_handler`** in **`market_data.py`** — **without `symbol`:** Polygon news limit **50**, **4h** lookback (**24h** widen when empty), merged liquid + watchlist tickers (cap **30**), relevance sort + topic dedupe, response **`{ "headlines": [...] }`** with at most **`min(limit, 20)`** rows (`limit` query **1–1000**), full headline fields incl. **`category`**, **`catalyst_category`**, **`relevance_score`**, **`credibility`**, **`matches_watchlist`**; **with `symbol`:** query **`days`** **1–20** (default **20**), **`limit`** **1–100**, rolling Polygon window, response **`{ "symbol", "articles", "has_recent_news", "recent_cutoff_hours", "total_found", "oldest_included" }`** with lean panel items (**`source`**, **`source_label`**, **`sentiment_score`**, **`sentiment_label`**, **`catalyst_type`**, **`is_recent`**, **`age_label`**, **`url`**); **`signal_resolution`**, **`signals`** → **`signals_http_dispatch`** for signal HTTP surface with **dynamic per-layer `reasoning`** in composite `contributions`, **`GET /v1/signals/analysis`** admin/internal tuning aggregate, **`watchlists`**, **`alerts`**, **`GET`/`PATCH /v1/users/me`**, **`text_sanitize.py`**, **`services/signal_recorder.py`** (public lists exclude **`incomplete`** rows), **`services/composite_market_context.py`** (Polygon market status for swing composite insufficient-data responses), **`services/morning_brief_fetch.py`** (VIX via **`get_vix_snapshot_with_fallback`** / **`VIX_SNAPSHOT_FALLBACK_SYMBOLS`**), **`services/scanner_scheduled_pipeline.py`** (platform watchlist symbol merge + notify), **`services/journal_order_hooks.py`** (auto journal on fill/close; swallowed errors), **`services/gap_intelligence_news.py`** global + per-ticker Polygon news merge, **`POST /v1/scanner/gap-intelligence`**, structured **`POST /v1/signals/day/briefing`** / **`POST /v1/scanner/briefing`**, **`POST /v1/signals/swing/composite`** (client-supplied layer scores) and **`POST /v1/signals/composite/real`** (intraday bars, server-side layers via `real_composite_engine.py`) and **`POST /v1/signals/composite/swing`** (`swing_composite_engine.py`, daily bars, extended news/macro/geo windows) return **`status: insufficient_data`** (HTTP 200) when fewer than three layers are available — no composite record or alert; **`lambda_dispatch.py`** now routes SQS news-consumer events; **`workers/news_worker.py`** is the long-running ECS entrypoint and **`workers/news_consumer_lambda.py`** is the SQS-triggered Claude scoring consumer; journal **`GET /v1/journal/analytics`** + entry by id + PATCH notes, auth, `legal_copy.py`, order safety integration), **`utils/log_privacy.py`**. **Sliding-session auth surface** (2026-05-11) — the user's "I was actively working and got logged out" regression is fixed by **`frontend/lib/auth/refresh-token-cookie.ts`** (server-only refresh-cookie name + reader), **`frontend/lib/auth/refresh-session.ts`** (client-side `refreshSession()` with single-flight coalescing + 5-second post-failure cooldown), **`frontend/app/api/auth/refresh/route.ts`** (BFF POST handler that reads the httpOnly `stocvest_refresh_token` cookie, calls Cognito `REFRESH_TOKEN_AUTH` via `refreshIdToken()` in `frontend/lib/auth/cognito.ts`, rewrites the ID + mirror cookies, returns `200 { exp }` on success or `401` after clearing all cookies on failure), and a **rebuilt `frontend/components/auth/session-expiry-watcher.tsx`** that schedules a refresh ~2 min before the JWT `exp` instead of firing the expired event at `exp`. `frontend/lib/api/browser-api-fetch.ts` now does refresh-and-retry on 401 (one retry); `frontend/lib/auth/surface-auth-error.ts` (used by `fetch-symbol-bars`, `fetch-symbol-snapshot`, `fetch-macro-context`, `ticker-news-panel`, `public-signals`, and the STOCVEST Assistant) is async and attempts a refresh before marking expired. **403 is intentionally not treated as auth failure** — STOCVEST uses 403 for product-rule denials (PDT, margin, paid-tier gating) where the user IS authenticated. Tests: `frontend/tests/refresh-session.test.ts` + `frontend/tests/browser-api-fetch-refresh.test.ts` + `frontend/tests/surface-auth-error.test.ts` (+15 over the 161 baseline). **`stocvest/signals/historical_validation.py`** — **D2 Historical Signal Validation (Phase 1)**: pure deterministic aggregator over a list of `SignalRecord` rows, returns a frozen `HistoricalValidationSummary` per horizon (`1h` / `1d`) with `overall` + six stratifications (Decision state at entry, Macro regime at entry — engine vocabulary `risk_on` / `neutral` / `risk_off` / `avoid`, trading Mode, Pattern family, Trade Readiness bucket high≥70 / moderate 40-69 / low<40, Direction); accuracy is `correct / (correct + incorrect)` with neutrals excluded from the denominator and NaN for empty / all-neutral strata; carries `rows_examined` + sorted `parameter_versions` provenance; no I/O — Phase 2 wraps this with a thin service layer (`stocvest/api/services/historical_validation_service.py`) that slices `SignalHistory` by date range and parameter version. Phase 3a exposes it at `GET /v1/signals/historical-validation/summary` (auth-gated, in `stocvest/api/handlers/signals.py::historical_validation_summary_handler`); Phase 3b ships the dashboard tab (`frontend/components/historical-validation-panel.tsx` + `frontend/lib/api/historical-validation.ts` + BFF route at `frontend/app/api/stocvest/signals/historical-validation/summary/route.ts`); Phase 3c-1 ships the public mirror (`GET /v1/signals/historical-validation/public-summary` in the same handler module, surfaced on `/performance` via `frontend/components/performance-tracking-content.tsx::PublicValidationSection` with a trimmed `overall + by_mode + rows_examined` projection enforced by `_public_summary_to_dict` so per-decision / per-regime / per-pattern / per-readiness / per-direction / per-symbol detail can never reach a logged-out visitor — `symbol` is explicitly 400-rejected at the public endpoint). **Phase 3c-2 (2026-05-11)** — the assistant chatbot is now aware of the user's per-user summary on the LOGGED-IN path: `stocvest/api/handlers/signals.py::assistant_chat_handler` fetches `HistoricalValidationService(...).summarize(user_id=rc.user_id, horizon="1d", from_at=now-90d, to_at=now)` on every authenticated chat turn (defensive try/except — a slow / unavailable signal-history store must NOT break the chat reply, and on failure the handler forwards `historical_validation_summary=None` so the prompt's "if absent, do not comment" rule activates); `stocvest/signals/assistant_chat.py::serialize_historical_validation_summary` emits a calm tail block `=== HISTORICAL VALIDATION ===\nwindow_days=90\nhorizon=1d\noverall=<pct>% (<correct> correct of <resolved> resolved; <neutral> neutral; <total> total)\nswing=...\nday=...\nrows_examined=<n>` — same trimmed projection as the public mirror but scoped to the calling user (overall + by_mode only, no per-decision / per-regime / per-pattern / per-readiness / per-direction stratifications reach the LLM); NaN accuracy renders as the em-dash `—` and is NEVER rendered as `"0.0%"` or the literal `"NaN"` string; empty `by_mode` buckets are omitted from the serialized text (Phase 1 pre-seeds the vocabulary so swing/day always appear as keys even when zero rows, but the serializer drops them to avoid the LLM paraphrasing zero-rows as "your day track has no data"); the `=== HISTORICAL VALIDATION ===` sentinel is referenced inside the new `HISTORICAL VALIDATION CONTEXT (LOGGED-IN ONLY)` section of `stocvest/signals/assistant_prompts.py::ASSISTANT_SYSTEM_PROMPT` so the LLM knows what to pattern-match on. The new prompt section enumerates what the assistant **MAY** reference (the user's overall directional accuracy with denominator transparency, the swing-vs-day shape in qualitative terms, the sample size as a small-window caveat, and a redirect to `/dashboard/signal-validation` for the stratified breakdown) and what it **MUST NOT** do (translate accuracy into dollar P&L / expected returns / win-rates — "Directional accuracy is NOT a return number"; predict whether the trend will continue or mean-revert — "descriptive, not predictive"; compare to "the market" / other users / a benchmark; recommend mode choice based on accuracy — "Mode choice is not an advice surface"; use the figures to claim the system "works" — "evidence of behavior, not promotion"; discuss per-symbol / per-pattern / per-regime / per-decision-state / per-readiness-bucket / per-direction performance because those are "deliberately withheld" from the assistant's context; reference any validation figures when the block is absent — "No block means no comment; never invent a number, never recall a number from a previous turn"). The public chat path (`AssistantChatService.reply_public`) is untouched — it refuses the kwarg by signature, never carries the sentinel block, and the public prompt path's `session_mode=public` marker still routes the LLM into PUBLIC MODE RULES where per-user numbers cannot be discussed regardless. **Phase 4 (2026-05-11)** — cross-version diff view on `/dashboard/signal-validation`'s Historical accuracy tab. Purely a dashboard UI + math-helpers slice; reuses the existing Phase 3a `GET /v1/signals/historical-validation/summary?by_version=true` endpoint and the Phase 3b `fetchHistoricalValidationByVersion` client without any backend / Terraform / IAM / BFF changes. `frontend/lib/api/historical-validation.ts` grew four pure helpers (`diffBucketStats(a, b)` returns a `BucketDelta` with `accuracyDelta` / `totalDelta` / `resolvedDelta` / `neutralDelta` / `smallSampleA` / `smallSampleB`; `formatAccuracyDelta(delta)` renders percentage-point strings; `selectComparableVersions(map)` excludes `__all__` and sorts `v<n>` numerically with `unknown` always last; `defaultCompareSelection(map)` picks the last two comparable versions) and exposed the constants `SMALL_SAMPLE_THRESHOLD = 10` / `ALL_VERSIONS_KEY = "__all__"` / `UNKNOWN_VERSION_KEY = "unknown"`. `frontend/components/historical-validation-panel.tsx` grew an inline **Compare versions** checkbox (off by default — the panel still opens to the calm single-version view) that swaps the fetch from `fetchHistoricalValidationSummary` to `fetchHistoricalValidationByVersion` and replaces the body with a `CompareBody` component: two version selectors (defaulting to the latest pair via `defaultCompareSelection`, preserving the user's explicit selections when only the window changes), an A / B / Δ tri-column overall hero card, a stratification grid with `CompareStratificationCard`s that union the keys across the two versions (a pattern family that only fired on one side still shows as a row with `—` on the side it did not fire), and a `CompareBucketRow` per stratum showing A's accuracy + `(correct/resolved)`, B's accuracy + `(correct/resolved)`, and the Δ formatted as `+5.2pp` / `-3.1pp` / `0.0pp` / `—`. Empty / not-meaningful states are handled inline: fewer than two comparable versions in the window renders a calm "need at least two parameter versions in this window to compare … widen the window" message; same-version-on-both-sides renders an italic "pick a different version on either side to see a delta" banner; user-changes-A-or-B while in compare mode does not refetch (selections are pure local state). Small-sample annotations (`< 10` resolved-non-neutral signals on either side) attach a muted "small sample" pill next to that side's cell without obscuring the delta. Color discipline matches the rest of the validation surface — no red/green (accuracy isn't profit), all calm accent tints and monospace deltas. The same verbatim `HISTORICAL_VALIDATION_DISCLAIMER` from the backend response renders below the compare body so the wording cannot drift between the single-version and compare views. The assistant chatbot is deliberately **NOT** wired to consume cross-version diffs — parameter_versions are an engine implementation detail and surfacing them through the LLM would be a leaky abstraction; the Phase 3c-2 user-scoped historical summary already covers the LLM's needs. **No Terraform, no backend, no BFF, no new dependencies.** Tests: +14 in `tests/historical-validation-api.test.ts` (covered in the frontend baseline cell above). Backend baseline 1015/3 unchanged. **D2 functionally complete end-to-end** with Phase 4 shipped (Phase 1 aggregator → Phase 2 service → Phase 3a auth API → Phase 3b dashboard tab → Phase 3c-1 public mirror → Phase 3c-2 assistant awareness → Phase 4 cross-version diff). **`composite_score.py`**: layer/verdict contradiction penalty, **`alignment_ratio`**, **`conflicted_layers`**; **`swing_composite_evidence.py`**: reference-level completeness, R/R quality + warning, catalyst headline pass-through, derived **`risk_factors`** / **`risk_factors_detailed`**; **`user_profile_store`**: lazy singleton (no boto3 on handler import); **`tests/conftest.py`**: Windows **`platform.uname()`** prime to avoid botocore/WMI stderr noise with Python 3.14. **Tests:** **`tests/api/test_article_relevance.py`**, **`tests/services/test_news_relevance.py`**, **`tests/api/services/test_news_panel_format.py`**, **`tests/api/handlers/test_market_data.py`**, **`tests/data/test_edgar_client.py`**, **`tests/data/test_news_triage.py`**, **`tests/signals/test_composite_scorer.py`**, **`tests/signals/test_signal_validation.py`**, **`tests/signals/test_risk_reward.py`**, **`tests/signals/test_catalyst_headlines.py`**, **`tests/signals/test_risk_factors.py`**. **Signal validation ledger:** DynamoDB **`SignalHistory`** only (**`SignalRecord`**); optional ledger columns (**`closed_at`**, **`ledger_entry_date_et`** / **`ledger_exit_date_et`**, **`entry_rationale`**, **`exit_reason`**, **`decision_state_entry`** / **`decision_state_exit`**, **`market_regime_exit`**, **`gate_status_json`** → API **`gate_status`**, **`setup_type`**, **`exit_rule`**, **`max_adverse_excursion_pct`**, **`max_favorable_excursion_pct`**, **`hold_duration_minutes`**) are populated when enrichment jobs write them; **`signal_resolution`** runs **D1** 1h/1d outcome resolution only. UI: **`/dashboard/signal-validation`** (Swing/Day tabs). The retired notional model book (**`ModelPortfolio`**, signals-lambda **`/v1/portfolio/*`**, portfolio reversal / composite sidecar jobs) has been removed from code and infra.

**A1 Assistant Upgrade (2026-06-02):** `stocvest/utils/symbol_detector.py` — ticker detection from natural language (`detect_symbol`, `detect_symbol_from_messages`; dollar-sign priority, blocklist of 100+ non-ticker words). `stocvest/api/services/assistant_symbol_context.py` — `AssistantSymbolContext` dataclass + `fetch_assistant_symbol_context(symbol)`: parallel asyncio.gather of Polygon snapshot, Polygon news (last 24h, descriptions included), Benzinga WIIM (`get_why_is_it_moving`), Benzinga analyst ratings (`get_analyst_ratings`), Benzinga earnings results (`get_earnings_results`), Benzinga corporate guidance (`get_corporate_guidance`), and 5-min bars; 4-second total timeout; best-effort (partial data returned on timeout). `assistant_chat.py`: `serialize_symbol_context()` renders structured `=== LIVE SYMBOL CONTEXT: {SYM} ===` block (snapshot, news with descriptions, WIIM, analyst ratings, earnings, guidance); `reply()` now accepts `symbol_context` + `attached_image`; `_claude_chat_or_none()` upgrades last user message to multi-part content block when image attached; `max_tokens` raised to **900** when symbol context present (was 320). `signals.py::assistant_chat_handler`: detects symbol from messages + page_context, fetches live context, validates image MIME type. `assistant_prompts.py`: added **LIVE MARKET CONTEXT RULES** section (SCOPE stocks-only, NEWS/ANALYST/EARNINGS/WIIM/TECHNICAL/FRAMING/IMAGE synthesis rules, 4–6 sentence response target for market questions).

**Frontend (`frontend/`)**  
**A1 Assistant UI (2026-06-02):** `frontend/lib/hooks/use-voice-input.ts` — `useVoiceInput` hook: Web Speech API, language locked to `en-US`, Chrome/Edge only (`isSupported` flag surfaces unsupported browsers), toggle record/stop, FRIENDLY_ERRORS map. `frontend/lib/assistant/types.ts`: added `AttachedImage` interface (data, media_type, name) and `attachedImage` optional field on `AssistantMessage`. `frontend/components/assistant/assistant-panel.tsx` — full redesign: (1) removed "Your question" label; (2) collapsed ConstellationStrip to a compact context chip (symbol) or 6 small dots in header; (3) composer row has `<Paperclip>` attach button + `<textarea>` with placeholder "Ask me anything about stocks…" + `<Mic/MicOff>` voice button + `<Send>` button — all in one visually distinct row with focus ring; (4) quick prompts capped at 3, horizontal wrap, above input when empty state; (5) image preview strip with thumbnail + remove button; (6) recording indicator with animated dot; (7) single disclaimer line "Facts and analysis only — not trading advice." `frontend/components/assistant/stocvest-assistant.tsx`: `submit()` now accepts optional `AttachedImage`, includes `attached_image` in POST body on authenticated path. BFF `route.ts`: added 413 guard for oversized image payloads (>6.4M chars base64).

**STOCVEST Assistant chatbot UI** lives under **`frontend/components/assistant/`** + **`frontend/lib/assistant/`**: **`stocvest-assistant.tsx`** (top-level orchestrator — open state, conversation state, sessionStorage hydration, `surfaceAuthErrorIfAny` on 401, **status-aware fallback copy** when the API call fails; **auth-aware endpoint switching** — when the server-rendered `isAuthenticated` is true the component POSTs to `/api/stocvest/signals/assistant/chat` with the full page-context payload, when false it POSTs to the new `/api/stocvest/public/assistant/chat` with `messages` only; **pathname-based cross-surface isolation** — the conversation is anchored to the URL pathname via `usePathname()` and wiped the moment the pathname changes, so a TTD answer on `/dashboard/signals/layers` cannot leak into a question on `/`, `/dashboard`, `/dashboard/scanner`, `/login`, or anywhere else; the hydrate effect also force-clears persisted state when (a) `persisted.isAuthenticated` is missing entirely (legacy build), (b) `persisted.isAuthenticated` does not match the current session, or (c) `persisted.lastPathname` differs from the current pathname — together these three conditions guarantee the anonymous home page never re-hydrates a logged-in dashboard conversation; **session reset** — the assistant subscribes to `subscribeSessionExpired` and `subscribeAssistantReset` so logout / token expiry / any 401 from any fetch clears messages + composer + sessionStorage; the empty state on the marketing surface now opens with a welcoming three-paragraph pitch — "Welcome to STOCVEST" → the six-layer thesis → the kinds of questions visitors can ask — instead of the old one-line "ask me anything"), **`lib/assistant/session-reset.ts`** (shared `clearAssistantSession` + `ASSISTANT_STORAGE_KEY` + `stocvest:assistant-reset` custom event; sign-out buttons in `sidebar.tsx` + `mobile-nav-drawer.tsx` call `clearAssistantSession()` in their `onClick` before the server logout action runs so the next visitor on the same tab never sees the prior conversation), **`assistant-launcher.tsx`** (custom concentric-ring lens glyph + slow 3.6s pulse + tone dot when contextual), **`assistant-panel.tsx`** (glassmorphism panel, header with **Mode badge**, **constellation context strip** showing the six signal layers tone-colored from page state, quick-prompts that change with mode — anonymous visitors get marketing-flavored prompts like "What is STOCVEST?" / "How is STOCVEST different from signal-alert services?" / "Explain risk/reward in plain terms"; calm composer + disclaimer footer), **`assistant-conversation-rail.tsx`** (no-bubble vertical timeline with gradient rail, small filled speaker nodes, **role-colored speaker labels** — accent blue for `YOU`, page-tone for `STOCVEST` — and **a 2px role-tinted left ribbon** on each message body with a faint user-only tint so input vs output is unmissable at a glance; **word-fade reveal** for the latest assistant turn). **`lib/assistant/context.tsx`** exposes **`AssistantContextProvider`**, **`useAssistantContext`**, and **`usePublishAssistantContext`** so any dashboard page can publish a whitelisted page-context payload while mounted. **`lib/signal-evidence/trade-decision.ts`** is the **shared source of truth** for `synthTradeDecision` / `deriveDecisionRationale` — `signal-evidence-card.tsx` and the signals-page assistant context both read from it, so the Decision shown on the Evidence card and the context handed to the chatbot can never drift. Motion is gated behind **`prefers-reduced-motion: reduce`** in **`app/globals.css`**. BFF proxy at **`app/api/stocvest/signals/assistant/chat/route.ts`** forwards to **`/v1/signals/assistant/chat`** via **`stocvestAuthedFetch`**. The launcher is mounted globally in **`app/dashboard/layout.tsx`** inside **`AssistantContextProvider`**, so it follows the user across every dashboard route. App router pages (dashboard **`layout.tsx`** wraps routes with **legal acknowledgment** + **onboarding** gate; **`dashboard-page-content.tsx`** uses time-bounded **`fetchMarketOverview`** / scanner / earnings loads so the shell is not blocked by a slow BFF; incl. **`/dashboard/performance`**, scanner (**`/dashboard/scanner`** server page: **`fetchDefaultWatchlistSymbols`** in parallel with **`fetchPdtStatus`**, then **`fetchScannerOverview(..., watchlistSymbols)`**), **`/dashboard/watchlists`**, signals with **Signal history** tab (BFF history + record fetch) + **`POST /api/stocvest/signals/composite/real`** or **`/api/stocvest/signals/composite/swing`** per **Day trade / Swing trade** toggle (and legacy **`swing-composite`**) check for **`insufficient_data`** (amber “Market Data Unavailable” callout when fewer than three layers have live scores); **`hasValidSignal`** now hides stale AI/radar/reference/evidence when status is insufficient and clears signal state; closed-session insufficient path renders **`components/signals-after-hours-panel.tsx`** with last-session levels/news/watchlist CTA; portfolio, **journal** with analytics + cumulative P&amp;L + trade table, settings (**Alert Preferences** + `#alerts`), earnings, public `/performance`, terms, etc.), **landing** (server **`app/page.tsx`** runs **`Promise.all`**: **`GET /v1/signals/recent?landing=true`**, performance summary, **`getFoundingMemberCount`** → **`GET /v1/signals/founding-members`** with **30m** revalidate; **`landing-signals.ts`** + **`FALLBACK_SIGNALS`** (marketing-only examples, **5** rows with ET wall-clock **`generated_at`**, incl. one **incorrect** demo outcome), **`LandingSignalExplorer`** (dynamic 1h % from prices; “(example data)” when API empty), **`LandingBeforeAfterSection`**, **`LandingActivityFeedSection`** (empty states + accuracy copy), `LandingHowItWorksSection`, `LandingPerformanceSection` + optional **`pattern_breakdown`**; shared glow card utilities in **`app/globals.css`** (`.landing-glow-card`, gate/pledge/before-after variants); marketing cards on **`landing-page.tsx`**; no client polling on `/`), design system + theme, API clients under `lib/api/` (incl. **`client.ts`** default **`apiFetch`** timeout; **`market.ts`** — **`fetchMarketOverview`** loads market status + indices only (no `/v1/market/news`); **`ticker-news-panel.ts`** + **`panel-article-to-news-payload.ts`** for **`GET /v1/market/news?symbol=`**; **`fetch-symbol-news.ts`** uses the panel API; **`watchlists.ts`** default symbols fetch, **`public-signals.ts`**, **`swing-composite.ts`**, client-safe **`fetch-symbol-snapshot.ts`**), **`lib/signal-evidence.ts`** + **`signal-evidence-card.tsx`** (R/R warning styling, incomplete-levels messaging, catalyst/risk empty copy), **`lib/snapshot-reference-levels.ts`** (same **5×** + valid-last guard as backend), **`components/pdt-status-pill.tsx`**, **`components/dashboard-redesign.tsx`** (PDT pill + **Market Pulse**; morning brief + signal-card **News** affordances + **`NewsPanel`**; **empty swing Top signals** “Primary read” card—posture headline, regime-tuned one-liner, institutional **Signal suppressed — …** line; circled‑i on **Primary read** uses layman copy from **`lib/ui-tooltips.ts`** (`PRIMARY_READ_SWING_CONTEXT_TIP`); alignment ladder, watchlist readiness, top-signals card, and **What would re-enable swing setups** (circled‑i beside heading only—no “Why?” label) tips rewritten for non-experts; re-enable tooltip uses intro + **short** bullets only; watchlist readiness detail tip prefixed with plain-language intro), **`components/news-panel.tsx`**, **`components/news-headline-drawer.tsx`** (credibility + relevance line), BFF **`POST /api/stocvest/journal/entries`**, **`GET`/`PATCH /api/stocvest/users/me`**, **`/api/stocvest/watchlists/**`**, **`/api/stocvest/alerts/**`**, **`/api/stocvest/signals/swing-composite`**, **`/api/stocvest/signals/me/history`**, **`/api/stocvest/signals/me/records/[signalId]`**, **`add-to-watchlist-button`** on signals + gap cards, Crisp when `NEXT_PUBLIC_CRISP_WEBSITE_ID` is set. **Sign out** redirects to **`/`** (landing). Scanner: **`fetchScannerOverview`** uses **`POST /v1/scanner/gap-intelligence`**, **`POST /v1/signals/day/setups`** (with snapshots + regime for confluence), structured **`POST /v1/signals/day/briefing`** (context includes gap items + intraday setups); intraday setup cards, signal evidence modal, gap cards, and dashboard morning brief show **confluence** when `is_confluence_alert`; **Open order entry** deep-links to **`/dashboard/portfolio`** with symbol + optional signal query params.

**Docs**  
`docs/API_CONTRACTS.md` — HTTP + broker contracts. **`docs/BACKLOG.md`** — detailed planned work (no duplicate of this file’s status tables). **`docs/SIGNAL_ENGINE.md`** — real composite + layer contracts/limitations (incl. **sector SIC** resolution chain and **`SicMappingTier`**: `exact` → `prefix` → `coarse` → `fallback_spy`, optional **`sic_mapping_tier`** on composite sector layer rows). **`docs/DEPLOYMENT.md`** — SES / email alert setup for production. **`docs/TUNING_PLAYBOOK.md`** — monthly signal-parameter review using **`GET /v1/signals/analysis`** and Secrets **`stocvest/signal-parameters`**. **DynamoDB `ParameterHistory`** (Terraform) stores versioned parameter JSON + audit fields. **User-facing tooltips:** copy for circled‑i **InfoTip** / related dashboard strings is centralized in **`frontend/lib/ui-tooltips.ts`**; **`frontend/components/info-tip.tsx`** renders multiline tips with **`white-space: pre-line`**. **Detailed file-by-file history was removed from this file** to avoid drift; use README + git.

**Public signal API (trust)**  
`GET /v1/signals/recent` (last **50** public `SignalRecord` rows as JSON array when `landing` is absent; **`?landing=true`** returns **`{ "items": [...] }`** with up to **5** rows where **`outcome_1h`** is set — allowlisted fields for marketing only, **`ai_summary`** truncated to **120** chars), `GET /v1/signals/performance/summary` — directional accuracy from **1d** outcomes; JSON **`correct_direction_count`**, **`incorrect_direction_count`**, **`neutral_direction_count`** (and related totals); accuracy = correct ÷ (correct + incorrect). **`GET /v1/signals/founding-members`** — public JSON **`founding_member_count`**, **`founding_spots_total`** (**100**), **`founding_spots_remaining`** (paid subscribers only; see **§1** HTTP row). Authenticated **`GET /v1/signals/me/history`** (optional **`mode`**, **`days`**, **`limit`**, **`symbol`**) and per-id **`GET /v1/signals/me/records/{signal_id}`** for user-scoped rows; optional ledger fields (`closed_at`, `gate_status`, `exit_rule`, MAE/MFE, …) appear when stored on the row (see **`docs/SIGNAL_ENGINE.md`** § Signal validation ledger). Responses use `signal_strength`, `disclaimer`, etc. (see Legal section).

---

## 3. Pending (near-term ops / engineering)

**Deploy checklist (after you push to `main`):** GitHub Actions runs tests, then **`deploy-lambda`** (zip → S3 → `update-function-code` for every `stocvest-development-api-*` module, including **`signal_resolution`**) and optionally **`deploy-vercel`** (production deploy hook). Full secret/variable names and IAM needs: [root `README.md` § CI/CD](../README.md#cicd-github-actions).

| Step | Action |
|------|--------|
| 1 | **Terraform:** `cd infra && terraform apply` for the target env so DynamoDB (incl. **`SignalHistory`**, **`TradeJournal`**, **`PDTState`**, **`AuditEvents`**, **`TradePlans`** (B69, **applied 2026-06-20**), …), **every** `api_handler_modules` Lambda (incl. **`signal_resolution`** → name `stocvest-development-api-signal_resolution`, and **`trade_plans`** (B69, **applied 2026-06-20**)), API Gateway (incl. **`GET /v1/signals/founding-members`**, admin **beta** / **audit** / **`GET /v1/admin/users/{user_id}/activity-errors`** routes, and the **`/v1/trade-plans/*`** routes — B69, **applied 2026-06-20**), and Lambda env (**`DYNAMODB_AUDIT_EVENTS_TABLE`**, etc.) exist. CI updates **code** only (`update-function-code`); it does **not** create functions. If Actions fails with `ResourceNotFoundException` for that name, apply Terraform **before** re-running deploy. |
| 2 | **GitHub:** Repository **variable** `STOCVEST_LAMBDA_S3_BUCKET`; **secrets** `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`, `AWS_REGION`; optional `VERCEL_DEPLOY_HOOK_URL`. |
| 3 | **Push `main`:** Open **Actions** → confirm backend, frontend, `deploy-lambda`, and `deploy-vercel` (or skip if hook unset). |
| 4 | **Smoke:** `GET /v1/health` on the HTTP API base URL; optional `GET /v1/signals/recent` if D1 table is applied. |
| 5 | **Scheduled resolution — APPLIED ✅** **`terraform apply -var-file=terraform.tfvars -input=false -auto-approve`** completed **2026-05-03** (**19 added, 14 changed, 2 destroyed**). Rule **`stocvest-signal-resolution`**: **`ENABLED`**, **`rate(30 minutes)`**, target **`stocvest-development-api-signal_resolution`**. Details: [`docs/D1_SIGNAL_RESOLUTION_SCHEDULE.md`](./D1_SIGNAL_RESOLUTION_SCHEDULE.md). |
| 6 | **Model portfolio retirement (2026-05-08+)** — **`ModelPortfolio`** DynamoDB table, signals-lambda **`/v1/portfolio/*`**, **`portfolio_reversal`** / **`run_portfolio_composite`** scheduler payloads, and related code removed; accountability is **`SignalHistory`** + **`/dashboard/signal-validation`**. Historical note: row 6 previously recorded the **2026-05-05** apply of portfolio-reversal + `run_portfolio_composite`; that stack is **no longer** deployed targets. |
| 7 | **AuditEvents + founding/audit Gateway routes — APPLIED ✅** **`terraform apply -var-file=terraform.tfvars -input=false -auto-approve`** completed **2026-05-07** (**5 added, 14 changed, 0 destroyed**): DynamoDB **`AuditEvents`**, **`DYNAMODB_AUDIT_EVENTS_TABLE`** on all API Lambdas, HTTP API routes **`GET /v1/signals/founding-members`**, **`PATCH /v1/admin/users/{user_id}/beta-access`**, **`GET /v1/admin/audit/users/{user_id}`**, **`GET /v1/admin/audit/sessions/{session_id}`**, CORS header **`x-stocvest-session-id`**. |
| 8 | **STOCVEST Assistant chat route — APPLIED ✅** **`terraform apply -var-file=terraform.tfvars -input=false -auto-approve`** completed **2026-05-10** (**1 added, 0 changed, 0 destroyed**): `aws_apigatewayv2_route.http["POST /v1/signals/assistant/chat"]` (route id `x6uhho2`) under the `signals` module with `authorizer = true`. Verified end-to-end via an unauthenticated probe to the new route that returned **`401 Unauthorized`** — the JWT authorizer rejected the call after the route accepted it, which proves the route is wired (a missing route returns API Gateway's native 404 instead). Signals Lambda code (`assistant_chat_handler` + `AssistantChatService`) deployed via CI `deploy-lambda` on commit `b6f83e7`; subsequent polish in `0485c95` (client-side status-aware fallback + console diagnostic) and `d1b6856` (prompt hardening, role-distinct rail, `analysis_status`) ride the same CI pipeline. No new secret required — `ANTHROPIC_API_KEY` already lives in `stocvest/lambda-runtime |
| 9 | **STOCVEST Assistant public route — APPLIED ✅** **`terraform apply -var-file=terraform.tfvars -input=false -auto-approve`** completed **2026-05-10** (**1 added, 0 changed, 0 destroyed**): `aws_apigatewayv2_route.http["POST /v1/public/assistant/chat"]` (route id `69hco4j`, integration `rnikyps` reusing the existing `signals` Lambda) with `authorizer = false`. The route lets anonymous marketing visitors (`/`, `/login`, `/signup`, etc.) chat with the assistant from the homepage so prospects can ask what STOCVEST is, how it differs from signal-alert services, and for explanations of common finance terms — the locked system prompt's new **PUBLIC MODE** section activates via the appended `session_mode=public` marker and continues to refuse trade recommendations, predictions, and accuracy claims. The route post-deploy returned **`{"error":"not_found","message":"Unknown route: POST /v1/public/assistant/chat"}`** from the signals Lambda — expected, because the Lambda code containing `public_assistant_chat_handler` is shipped on the next `deploy-lambda` CI run that lands the commit. No new secret required.`. |
| 10 | **D10 Phases 1–5 (admin hub backend + automated weight tuning) — APPLIED ✅** **`terraform apply -input=false`** completed **2026-05-13** (**35 added, 18 changed, 0 destroyed**). Catch-up deploy of every D10 phase + admin hub: (a) **14 new API Gateway routes** under `/v1/admin/*` — `GET search`, `GET {user_id}`, `POST {user_id}/reset-password`, `POST/DELETE {user_id}/groups/{group}`, `GET audit/recent`, `GET proposals[ {proposal_id}]`, `POST proposals/{proposal_id}/promote\|reject`, `GET parameters/current\|history`, `POST parameters/rollback`, `GET system-status` — all `authorizer = true`, all routed to the `brokers` Lambda. Smoke probe to `GET /v1/admin/users/search?limit=1` post-apply returned **`{"message":"Unauthorized"}`** which proves the route accepts then the authorizer rejects — exactly the inverse of the **`404 Unknown route`** the user observed pre-apply on `/dashboard/admin/users`. (b) **Two new Lambdas** (`weight_proposer`, `weight_rotation_monitor`) with their EventBridge schedules, CloudWatch log groups, IAM execution roles, and the **`stocvest-development-weight-rotation-degradation`** SNS topic + alarm for the rotation degradation guardrail. (c) **`ParameterProposal`** DynamoDB table for the human-in-the-loop tuning ledger. (d) **All 16 existing `api_handler_modules` Lambdas refreshed in place** with the latest code packages (D10 dispatcher entries for the admin handlers). No destructive changes. Root cause of the user's `404` regression on `/dashboard/admin/users`: source was correct (the routes existed in `infra/apigateway_6e.tf` and the dispatcher in `stocvest/api/lambda_dispatch.py`), but `terraform apply` had not been run since commit `415c7c9` (the Admin Users token-pagination commit) landed. Diagnostic UI shipped in the same chat surfaces the actual HTTP status + an actionable hint (`AdminApiErrorCard` + `classifyAdminReadStatus` in `lib/api/admin-users.ts`) so a future deploy gap is visible immediately — see `frontend/components/admin/admin-api-error-card.tsx` and the lock-in tests in `tests/admin-api-diagnostic.test.tsx`. |
| 11 | **Tier 1.A dashboard prefetch storm — SHIPPED (frontend only) ✅** Commit landing **2026-05-13** (no terraform / no lambda change). Direct cause of the user's **16.78s `/dashboard/signals?symbol=AAPL&ref=dashboard-ribbon` request with 14.61s "Content Download"** captured via DevTools on 2026-05-13. Root cause: every `<Link>` in `components/dashboard-active-signal-ribbon.tsx` (ribbon chips + empty-state "Open scanner"), `components/day-desk-panel.tsx` (per-row "Open Day Signals →" + footer "View day scanner →"), and `components/dashboard-redesign.tsx` (swing-desk footer "View swing scanner →") omitted `prefetch={false}`. Next.js 14.2's default `prefetch="auto"` therefore fired one full SSR prefetch per visible link on dashboard mount — 5–10 parallel prefetches of `/dashboard/signals` and `/dashboard/scanner`, each running `fetchPdtStatus` + `fetchMarketOverview` + `fetchScannerOverview` + `fetchEarningsCalendar` server-side just to warm cache for pages nobody asked for. Fix: `prefetch={false}` on every such `<Link>`. Lock-in: `frontend/tests/dashboard-prefetch.test.tsx` (5 tests) with a `next/link` mock that surfaces the prefetch prop as `data-prefetch="…"` so a future regression fails CI loud with a pointer at **`docs/PERFORMANCE.md` §3.1 + §4**. |
| 12 | **Tier 1.B streaming Suspense islands on `/dashboard/signals` — SHIPPED (frontend only) ✅** Commit landing **2026-05-13** (no terraform / no lambda change). Follow-up to row 11: once Tier 1.A killed the dashboard's prefetch storm, the next bottleneck became the signals page's own load. Pre-Tier-1.B, `app/dashboard/signals/page.tsx` `await`-ed `Promise.all([fetchPdtStatus, fetchMarketOverview, fetchScannerOverview])` + sequential `await fetchEarningsCalendar(symbols, 3)` before rendering ANYTHING, so a ribbon-chip click left the previous page's UI frozen for seconds. Refactor: split the page into an outer fast shell (auth + URL parsing + `<AppShell>` chrome) and an inner async server child (`SignalsPageData`) that owns the four heavy fetches; wrap the inner in `<Suspense fallback={<SignalsPageShell />}>`. Added `app/dashboard/signals/loading.tsx` rendering the same shell for the inter-route transition window. New file `frontend/components/signals-page-shell.tsx` is a pure server component (no hooks, no fetches) whose DOM mirrors the live `signals-grid` two-column breakpoints (`grid-cols-1` / `lg:grid-cols-[1.35fr_1fr]`), renders two mode-tab placeholders + six layer-row placeholders + three named context cards (`news`/`earnings`/`after-hours`), and exposes `role="status"` + `aria-live="polite"` with sr-only "Loading signal data…" for AT users. `SignalsPageClient` itself is unchanged — same props, same chatbot context publishing, same Mode Separation perimeter. Per-section island split (`<MarketOverviewIsland />` / `<ScannerIsland />` / `<EarningsIsland />` each in its own Suspense boundary) is documented as **Tier 1.B+** in `PERFORMANCE.md` §2 and `BACKLOG.md` D11 — parked until a real user reports the current "swap everything at once" feels janky. Lock-in: `frontend/tests/signals-page-streaming.test.tsx` (12 tests) covering the shell DOM contract, the `loading.tsx` default export, and — critically — **the source-level invariant** that the four heavy fetchers (`fetchMarketOverview`, `fetchScannerOverview`, `fetchEarningsCalendar`, `fetchPdtStatus`) all appear in `SignalsPageData` and NONE appear in the outer `DashboardSignalsPage` function. Verified by stripping block + line comments from the source then positionally splitting at the inner-function anchor. The negative half of that invariant is the load-bearing assertion: any future refactor that moves a fetcher back to the outer page fails this test loud with a pointer at `docs/PERFORMANCE.md` §4B. Frontend baseline now **745 passing tests across 73 files** (was 733/72). |
| 13 | **Layer 4 (first slice) — SWR client cache + hover-prefetch — SHIPPED (frontend only) ✅** Commit landing **2026-05-13** (no terraform / no lambda change, no API contract change). Follow-up to rows 11–12 and the third performance tier shipped today. Decision: **SWR over TanStack Query** — SWR is ~5KB vs ~13KB gzipped, has a smaller API surface that exactly fits our Layer-4 use case (pure GET-and-cache), and is authored by Vercel (same shop as Next.js). TanStack Query is reserved for a future feature that needs mutations / optimistic updates / infinite scroll — it can co-exist for that surface without rip-out. Added `swr@^2.4.1` to `frontend/package.json`. **New module `frontend/lib/swr/`** with three files: (a) `config.ts` exports `STOCVEST_SWR_DEFAULTS` (the load-bearing defaults `revalidateOnFocus:false` — our data isn't tick-by-tick so refetching on every alt-tab burns API budget without adding signal; `revalidateOnReconnect:true` — refresh once after a wifi blip ends; `dedupingInterval:30_000` — 30s is the sweet spot so rapid clicks across the ribbon don't fan out to N calls for the same symbol; `errorRetryCount:1` — Lambdas are mostly stateless so a 5xx is usually a code issue not a flake, retrying 3+ times just makes the user wait longer for the error; `keepPreviousData:true` — switching symbols keeps showing the previous symbol's snapshot until the new one resolves, no blank flash; `shouldRetryOnError` predicate **never retries on 401** which would re-fire `surfaceAuthErrorIfAny` and re-surface the session-expired banner — the exact UX bug fixed in the prior session-expiry PR) plus the `STOCVEST_SWR_CACHE_NS = "stocvest:"` namespace prefix; (b) `fetcher.ts` exports `swrFetcher<T>(input, init?)` which always sends `credentials:"include"` + `accept:application/json`, throws a typed `SwrFetcherError` carrying status + parsed body on non-2xx, AND fires `surfaceAuthErrorIfAny(response)` on 401 before throwing (so the auth-error banner fires AND SWR's error slot is populated so the hook can render properly); (c) `provider.tsx` exports the client `<StocvestSwrProvider>` wrapping `<SWRConfig>` with defaults + fetcher. Mounted once in `app/layout.tsx` between `<ThemeProvider>` and `<CrispChat>`. **New module `frontend/lib/hooks/`** with two files: (a) `use-symbol-snapshot.ts` exports `useSymbolSnapshot(symbol)` — wraps the existing imperative `fetchSymbolSnapshot` (which reads a non-HttpOnly WS token from `document.cookie` and adds a Bearer header — keeping that auth plumbing in one place is why we wrap rather than have SWR call the URL directly). Cache key: `["stocvest:symbol-snapshot", upperCaseSymbol]`. Empty/whitespace symbol skips the fetch. Preserves the upstream ticker-mismatch guard from the original effect (upstream sometimes returns a snapshot for a different symbol on ambiguous queries — hook returns `null` instead of misleading data). Returns `{ snapshot, isInitialLoading, isRevalidating, error }`. (b) `use-hover-prefetch.ts` exports `useHoverPrefetch(href, options?)` returning `{ onMouseEnter, onFocus, onPointerDown }` handlers — each calls `router.prefetch(href)` exactly **once** per hook instance (re-hovering does NOT re-fire); optional `router` override for tests; optional `enabled:false`; errors from `router.prefetch` swallowed (best-effort). **Refactored callsites**: (i) `components/signals-page-client.tsx` dropped the `symbolSnapshot` useState + the `useEffect → fetchSymbolSnapshot` block, replaced with `useSymbolSnapshot(symbolForSwr)` where `symbolForSwr` is `""` whenever the market overview already carries the snapshot — preserving the original short-circuit. (ii) `components/dashboard-active-signal-ribbon.tsx` extracted `<RibbonChip>` sub-component (each chip calls `useHoverPrefetch(href)` independently without violating Rules of Hooks in a `.map`). Both chips AND empty-state "Open scanner" CTA carry `data-prefetch="false"` (Tier 1.A) AND `data-hover-prefetch="true"` (Layer 4). (iii) `components/day-desk-panel.tsx` added hover-prefetch to both per-row "Open Day Signals →" links and the desk footer "View day scanner →". (iv) `components/dashboard-redesign.tsx` added hover-prefetch to the swing-desk footer "View swing scanner →". **`vitest.setup.ts` now ships a default `next/navigation` mock** so every test that renders a dashboard subtree containing `useHoverPrefetch` works in jsdom without each test file declaring its own — six existing tests that explicitly mock `next/navigation` for richer router behaviour continue to win via vitest's hoisting precedence. **The Tier 1.A ↔ Layer 4 contract** is now locked in: every heavy-target dashboard `<Link>` carries BOTH `data-prefetch="false"` AND `data-hover-prefetch="true"` — no mount-time speculation but warm-on-intent. Lock-in tests: 34 new tests across **5 new files** — `tests/swr-config.test.ts` (10 — every default pinned individually, retry predicate tested against 401/5xx/4xx-non-401/non-status errors), `tests/swr-fetcher.test.ts` (7 — 2xx parses JSON, non-2xx throws `SwrFetcherError`, 401 fires surface helper, 200 doesn't, 500 doesn't, credentials + accept header always sent), `tests/use-symbol-snapshot.test.tsx` (6 — empty/whitespace short-circuit, happy path, case + whitespace normalisation, ticker-mismatch guard, SWR dedupe of two hooks for same symbol → fetcher called once), `tests/use-hover-prefetch.test.tsx` (6 — fires on first onMouseEnter, never re-fires, onFocus/onPointerDown equivalent, null/empty/undefined href no-op, `enabled:false` no-op, swallows router.prefetch throws), `tests/dashboard-hover-prefetch.test.tsx` (5 — ribbon chip / ribbon empty-state CTA / day-desk per-row / day-desk footer / swing-desk footer ALL carry BOTH markers; the critical invariant — any future PR dropping either marker fails loud with pointer at `docs/PERFORMANCE.md` §4C). Frontend baseline now **779 passing tests across 78 files** (was 745/73). |
| 14 | **Admin Users empty-pool regression fix + sticky TopBar — APPLIED ✅** **`terraform apply -target='aws_lambda_function.api' -auto-approve`** completed **2026-05-13** (**0 added, 17 changed, 0 destroyed**) — every API Lambda picked up three new env vars `COGNITO_USER_POOL_ID=us-east-1_Jl8gQgR2p`, `COGNITO_REGION=us-east-1`, `COGNITO_APP_CLIENT_ID=2ruog68jh83frp7unduufbvgm2`. Commit lands the env wiring + a defense-in-depth backend gate + frontend error-envelope plumbing + a sticky page chrome. **Root cause** of the user's "**No users found in the pool yet.**" screenshot on `/dashboard/admin/users`: the `lambda_common_env` block in `infra/lambda_6e.tf` exported every DynamoDB table name but had never carried the Cognito identifiers, so `settings.cognito_user_pool_id` resolved to `""` inside the brokers Lambda; `admin_user_directory._pool_id()` returned empty; `list_users_page` returned `UserSearchPage(records=[], next_token=None)` from the `if not pool: return UserSearchPage(records=[], next_token=None)` early-out; the BFF round-tripped a 200 with `items: []`; the React empty state rendered the misleading "No users found in the pool yet." copy. **Fix (a) — infra**: added `COGNITO_USER_POOL_ID = aws_cognito_user_pool.main.id`, `COGNITO_REGION = var.aws_region`, `COGNITO_APP_CLIENT_ID = aws_cognito_user_pool_client.frontend.id` to `lambda_common_env`. Values are non-secret (already published via `outputs.tf` for the Vercel `NEXT_PUBLIC_*` rails) so they live in the function environment, not in the `stocvest/lambda-runtime` Secrets Manager payload. **Fix (b) — backend gate**: new helper `_require_cognito_pool()` in `stocvest/api/handlers/admin_users.py` returns a 503 with body `{ "error":"config_error", "code":"cognito_pool_unset", "message":"…", "hint":"Run `terraform apply` from /infra…" }` whenever `settings.cognito_user_pool_id` is empty, AND logs at `error` level so the misconfig shows up in CloudWatch. Wired into all four admin user handlers (search, detail, reset_password, _mutate_group) **after** `_require_admin()` so a non-admin still gets 403 first (defense-in-depth ordering). `admin_user_directory.list_users_page` keeps the empty-page silent fallback for back-compat (tests + the best-effort admin-system-status tile still call it) but now logs at `error` level too. **Fix (c) — frontend error envelope plumbing**: new `readAdminErrorEnvelope(response)` in `frontend/lib/api/admin-users.ts` reads the JSON body on every non-2xx admin response and prefers the backend's `message` / `hint` over the generic `classifyAdminReadStatus` copy. The 503 `cognito_pool_unset` case maps to `code:"not_deployed"` so the existing `AdminApiErrorCard` styles it identically to a 404-route-not-deployed (same "fix the infra" affordance). `searchUsersDiagnostic` and (transitively, in a future tightening) `fetchUserDetail` go through this helper. **Fix (d) — sticky page chrome**: previously `<header>` in `components/top-bar.tsx` scrolled away with the body. Two changes together produce sticky behaviour: (i) `<header>` gains `className="sticky top-0 z-30 … backdrop-blur-sm …"` — `z-30` sits below modals/drawers at 40+ but above page content, `backdrop-blur-sm` softens content scrolling under the chrome; (ii) `components/app-shell.tsx` right-column wrapper drops the legacy `overflow-x-hidden` (which promoted the wrapper to a scroll container and captured `position: sticky` against itself instead of the viewport) and moves that protection to `<main>` so wide content still cannot blow the column out. The wrapper also gains `data-testid="app-shell-right-column"` and `flex flex-col` to lay out the chrome + scroll content vertically. **Tooling**: `frontend/vitest.config.ts` aliases `server-only` to `tests/mocks/server-only-stub.ts` (an empty file) so test suites that render dashboard chrome whose transitive imports touch `lib/auth/cognito.ts` no longer fail at the `import "server-only";` line. **Lock-in tests**: (i) Python `tests/api/handlers/test_admin_users.py` gains autouse fixture `_cognito_pool_configured` that monkeypatches `_require_cognito_pool` to `lambda: None` so the 23 existing tests don't have to set `cognito_user_pool_id` individually, **plus** three new gate tests at the bottom — `test_require_cognito_pool_returns_503_with_actionable_body_when_unset` (uses captured `_REAL_REQUIRE_COGNITO_POOL` to bypass the autouse override and asserts the full body shape: `error:"config_error"`, `code:"cognito_pool_unset"`, message mentions `COGNITO_USER_POOL_ID`, hint mentions `terraform apply`), `test_require_cognito_pool_passes_when_id_is_set`, and a 5-case parametrised `test_admin_handlers_short_circuit_to_503_when_pool_unset` that pins every admin user handler routing through the gate. (ii) New `frontend/tests/admin-users-error-envelope.test.ts` (7 tests) — `readAdminErrorEnvelope` 503 → not_deployed mapping, generic 5xx with body, body without message/hint, non-JSON body, 404 with body, plus end-to-end `searchUsersDiagnostic` 503 + happy-path. (iii) New `frontend/tests/top-bar-sticky.test.tsx` (3 tests) — `<TopBar>` header carries `sticky top-0 z-30`; `<AppShell>` right column does NOT carry `overflow-x-hidden` / `overflow-hidden` / `overflow-auto` / `overflow-y-auto`; `<main>` still carries `overflow-x-hidden`. Backend baseline holds at **257 passing tests in `tests/api/handlers/` + `tests/api/services/test_admin_user_directory.py`** (was 254; +3 gate tests). Frontend baseline now **789 passing tests across 80 files** (was 779/78). **Why this matters operationally**: the previous silent-empty-page failure mode cost a debugging round-trip — the new 503 envelope means the next analogous misconfig (Cognito region pinned wrong, new admin route forgets the gate, etc.) surfaces in `AdminApiErrorCard` with an actionable hint instead of as a misleading "no users" empty state. The sticky header keeps page chrome (page title, theme toggle, mobile menu) on screen during long scrolls — important on long admin pages and the long dashboard. |
| 22 | **B36 — Evidence Card Signal price drift row — SHIPPED ✅** Commit landing **2026-05-13** (frontend only, no API contract change). Surfaces scanner emit anchor (`IntradaySetupPayload.last_price` → `SignalEvidenceData.priceAtSignal`) vs latest snapshot (`lastTradePrice`) with signed Δ% and magnitude tiers in `frontend/lib/signal-evidence/signal-price-display.ts`; compact row under symbol in `signal-evidence-card.tsx` (`data-testid="signal-evidence-price-drift"`). Tests: `tests/signal-price-display.test.ts` (33), `tests/signal-evidence-card-price-drift.test.tsx` (14). Full Vitest **1018/1018**, **91** files (2026-05-13). |
| 23 | **B37 — Admin Users per-user activity errors + last-activity UX — SHIPPED ✅** **2026-05-13**: `GET /v1/admin/users/{user_id}/activity-errors` filters user audit rows (HTTP ≥400 or `outcome` error/failure) in rolling **`days`** (default 7). Code: `stocvest/api/handlers/admin_users.py`, `lambda_dispatch.py`, `infra/apigateway_6e.tf` (**terraform apply** required for API route). Frontend: BFF `activity-errors` route, `fetchUserActivityErrors`, **Show/Hide error logs** on `/dashboard/admin/users` detail (disabled when none), **Last activity** label + empty-state hint; `frontend/lib/dashboard-user-subscription.ts` adds missing `stocvestAuthedFetch` import. Tests: `tests/api/handlers/test_admin_users.py`, `frontend/tests/admin-users-bff.test.ts`. See **`docs/IMPLEMENTED.md` B37** + **`docs/API_CONTRACTS.md` §4.10**. |
| 25 | **B42 — Scanner scan-result IA (future-proof summary + near-qualification v2) — SHIPPED ✅** **2026-05-16**: **`ScannerScanSummary`** (`frontend/lib/scanner-scan-summary.ts`) powers scan hero, next actions, near-qualification + watchlist progression lanes, collapsible education. Setups v2: **`include_near_qualification`** on day/swing setups → `{ qualifying, near_qualification }` with **`alignment`** (`scanner_setups_bundle.py`). **`scanner-load`** builds summary on every scan. UI: `ScannerScanResultHero`, `ScannerNearQualificationSection`, `ScannerScanEducation`. Tests: `tests/api/test_scanner_setups_bundle.py`, `frontend/tests/scanner-scan-summary.test.ts`, `frontend/tests/scanner-api.test.ts`. Docs: **`API_CONTRACTS.md`**. |
| 26 | **B33 — Per-symbol scanner evaluation trace — SHIPPED ✅** **2026-05-16**: Gate-level **`evaluation_trace`** on setups v2 (`include_evaluation_trace`); persisted **`GET /v1/signals/scanner-trace`** + DynamoDB **`ScannerEvaluationTrace`** (48h TTL). UI: **`ScannerEvaluationTraceSection`** on `/dashboard/scanner` (evaluated · did not qualify · not a watchlist). **Ops:** **`terraform apply`** for table + route (see §3 row 27). Tests: `tests/signals/test_scanner_evaluation_trace.py`, `tests/data/test_scanner_evaluation_trace_store.py`, `tests/api/handlers/test_scanner_trace_handler.py`, `frontend/tests/scanner-evaluation-trace.test.ts`, `frontend/tests/scanner-trace-api.test.ts`. Counsel sign-off before broad promotion: **BACKLOG** note only. |
| 33 | **B51 — Signals layer interpretation + mobile section nav — SHIPPED ✅** **2026-05-19** (frontend only): **`groupLayersByForce`** + **`SignalsLayerForceSummary`**; **`layerRoleLabel`** / **`buildLayerRoleHeadline`** on layer rows; **`SignalsSectionNav`** (scroll-spy pills); sticky command bar all breakpoints; scroll-snap ≤1023px; layers before radar on mobile; one evidence CTA; scenario R/R fix null guard. See **`IMPLEMENTED.md` B51**. |
| 32 | **B50 — Trade conviction tiers + mode-aware Scenario Builder R/R — SHIPPED ✅** **2026-05-19** (frontend only): **`trade-conviction-tier.ts`** (A+ / B+ / Developing; mode-aware verdict R/R); **`ConvictionTierBadge`** on **`SignalsSetupRead`** + watchlist cards; **`buildSignalsPageDecision({ mode })`** attaches **`conviction`**; assistant page-context conviction fields. **`isEligibleForScenario`** **`low_risk_reward`** uses **`minRiskRewardForVerdict(mode)`** (day **1.3**, swing **2.0**); B+ shows **`scenarioBuilderNote`** for full-sheet vs A-tier. Tests: `trade-conviction-tier.test.ts`, `scenario-eligibility.test.ts`, `signals-page-present.test.ts`. See **`IMPLEMENTED.md` B50**. |
| 33 | **B62 — Validation ledger capture + execution quality (Phase 1–2) — SHIPPED ✅** **2026-05-22** (commit `4120b60`): **Scheduled capture** — EventBridge **`ledger_capture`** **3:55 PM ET** → **`watchlist_ledger_capture.py`** (default watchlists only; actionable/near-ready priority; round-robin; env caps). **Shadow rows** — `SignalHistory` items with **`ledger_qualified=false`**, pattern **`:ledger_capture_shadow`**, full snapshots + **`gate_status_json`** (`gates`, **`execution_quality`**, **`evaluation_source`**). **Composites** — day/swing responses include soft **`execution_quality`** (informational); Evidence card summary line. **Deploy:** **`terraform apply`** (`scanner_ledger_capture` schedule) + CI **`deploy-lambda`** (**scanner** + **signals**). Tests: `test_execution_quality.py`, `test_ledger_gate_attempt.py`, `test_watchlist_ledger_capture.py`, `execution-quality.test.ts`. See **`IMPLEMENTED.md` B62**. **Not in scope:** all Cognito users; Phase 3 R/R change. |
| 32 | **B58–B61 — Signal intelligence + near-ready engagement — SHIPPED ✅** **2026-05-21** (commits `3b489f4`, `1d29775`): **Causal narrative** on swing/day composite (`causal_narrative`, `stocvest/signals/causal_narrative.py`, Signals **`CausalNarrativePanel`**). **Day-desk MTF** via **`multi_timeframe.py`** on **`real_composite_engine`** (`weekly_timeframe`, `timeframe_alignment`, **`TimeframeContextPanel`**). **Near-ready engagement** — Dynamo **`progress_band`**, maturation-summary **`near_ready_count`** / **`near_ready_symbols`**, **`NearReadyEngagementStrip`**, watchlist **`?near_ready=1`**. **Signals UX** — compact mode/evaluation line + ⓘ tooltip; desk tabs outside sticky header. **Deploy:** push to **`main`** runs CI **`deploy-lambda`** (**signals** + **brokers** modules); no Terraform change. Tests: `test_causal_narrative.py`, `test_multi_timeframe.py`, `test_watchlists_maturation_summary.py`, frontend `causal-narrative.test.ts`, `timeframe-context.test.ts`, `maturation-summary-envelope.test.ts`, `near-ready-engagement-strip.test.tsx`, `signals-command-bar.test.tsx`. See **`IMPLEMENTED.md` B58–B61**. |
| 31 | **B49 — Scanner quiet-desk IA + dashboard market-context command center — SHIPPED ✅** **2026-05-19** (frontend only): **`ScannerQuietDesk`** + **`scanner-quiet-copy.ts`** (volume-led narrative; regime as context); **`RejectionGroups`** labeled **Session volume**; conditional **`WhatWouldChangeFooter`**; volume-proximity near-ready cards; gap empty de-emphasis + active guidance; setups interpretive copy + near-ready connector; dashboard **`lib/market-context`**. Tests: `scanner-quiet-desk.test.ts`, `scanner-market-conditions-card.test.ts`, `scanner-quiet-layout.test.ts`, dashboard command-center tests. See **`IMPLEMENTED.md` B49**. |
| 29 | **B44 — Evidence card diagnostic IA — SHIPPED ✅** **2026-05-16** (frontend only): Prominent **Bias**; alignment de-emphasized (context only; setup validity on Signals); **`buildEvidenceAnchorLine`**; **Layer contribution (directional pressure)** + primary drivers; layer **insight** one-liners (not raw fingerprint text); removed readiness/R/R/regime grid + duplicate alignment; **Layer synthesis (informational)**; news empty → **No active catalyst**. **Signals polish:** sharper **Why not?** R/R copy, **`SignalsWatchlistPickerModal`**, radar baseline note, collapsed layers = driving + blocking. Modules: `evidence-card-present.ts`, `evidence-card-header.tsx`, `evidence-layer-contribution.tsx`, `signals-watchlist-picker-modal.tsx`. Tests: `evidence-card-*.test.*`, `signals-watchlist-picker.test.tsx`, updated `signals-page-mode-clears-screen.test.tsx`. Baseline **1143** frontend tests / **124** files. |
| 28 | **B43 — Signals page setup-read IA — SHIPPED ✅** **2026-05-16** (frontend only): **`Setup read`** replaces **AI Signal Analysis** — bias, **X/6 alignment**, actionable line, **Why not?** (from `trade-decision` + blocking layers), **Next** (watchlist, history, evidence). **`SignalsCommandBar`**, collapsed **`SignalsLayerBreakdown`**, compact **`SignalsReferenceLevels`**. Helpers: `frontend/lib/signals-page-present.ts`, `use-watchlist-maturation-line.ts`. Tests: `frontend/tests/signals-page-present.test.ts`, `signals-setup-read-ui.test.ts`, `signals-page-mode-clears-screen.test.tsx`. Baseline **1134** frontend tests. |
| 27 | **B33 — ScannerEvaluationTrace infra + GET route — APPLIED ✅** **`terraform apply -input=false -auto-approve`** completed **2026-05-16** (**2 added, 2 changed, 0 destroyed**): DynamoDB **`ScannerEvaluationTrace`** (48h TTL), API Gateway **`GET /v1/signals/scanner-trace`**, signals Lambda **`DYNAMODB_SCANNER_EVALUATION_TRACE_TABLE`**. Post-apply smoke: scanner with 0 qualifying setups → expand trace; reload → rows from GET. **CI fix (same day):** `scanner-trace-client.ts` (`browserApiFetch`) — client scanner page must not import server `client.ts` (`next/headers`). |
| 24 | **B39 — Watchlist maturation production slice — SHIPPED ✅** **2026-05-15**: **`WatchlistMaturation`** Dynamo + evidence dual-write, EventBridge **maturation_refresh** on scanner, **`GET /v1/watchlists/maturation-summary`** (default-list filter; **`readiness_label` omitted for free** unless `beta_access_active`; paid plans full rows via `watchlist_maturation_gates.py`), Watchlists RSC **`maturationSummaryMode`** (`swing` for Swing Pro only else `day`) + loading/error + non-default explainer. Tests: `tests/api/test_watchlists_maturation_summary.py`, `tests/api/test_watchlist_maturation_gates.py`, `frontend/tests/watchlists-page-maturation.test.tsx`. Docs: **`WATCHLIST_MATURATION_ARCH.md`**. **B41** — SES maturation emails + ET-day **transition** dedupe + default-watchlists **in-app** strip from alert history (**50-row** filtered scan, **``symbols``** cap, BFF query forward, **`lib/nav/watchlist-signals-deeplink`**, Settings **Recent Alerts** UX): **`IMPLEMENTED.md` B41**. |
| 21 | **B35 — Evidence Card layer-card refactor (collapse empty layers + compress neutral context + visual-emphasis hierarchy + News-neutral parenthetical) — SHIPPED ✅** Commit landing **2026-05-13** (frontend only, no terraform / no lambda change, no API contract change). Direct follow-up to row 20 — closes out the three deeper items (3 / 4 / 6) from the BRK.B second-pass UX polish that were parked as `B35` in `BACKLOG.md` so they could be shipped as one design-coherent refactor rather than bolted onto the surgical fixes. All three live in the same `<section>` in `frontend/components/signal-evidence-card.tsx` ("Signal Layer Breakdown") and share the same data shape (the `EvidenceLayer[]` array), so doing them together avoided three rounds of "this looks weird" rework. **New module `frontend/lib/signal-evidence/layer-emphasis.ts`** (pure, no React, no fetch, no clock — all decisions computed from the `EvidenceLayer` shape alone). Five public helpers + one private: (a) `layerEmphasisTier(layer): "primary" \| "secondary" \| "tertiary"` — Technical is *always* `primary` (load-bearing layer for action), Sector + Geopolitical default to `tertiary` but promote to `secondary` when `layerHasActiveContent` is true OR status is directional (Bullish/Bearish), every other layer (News / Macro / Internals) renders at `secondary` and demotes to `tertiary` only if `Unavailable`. The constants `PRIMARY_LAYER_KEYS = ["technical"]` and `TERTIARY_DEFAULT_LAYER_KEYS = ["geopolitical", "sector"]` are exported and locked-in with their own membership tests so future "let's also promote internals to primary" rewrites have to delete a test first. (b) `layerHasActiveContent(layer): boolean` — per-layer truth table that mirrors what the existing rendering surfaces inline: macro = critical/elevated risk_level with warnings OR any upcoming_events (any status) OR any yield_curve (any regime — normal counts because the row renders); news = wim_summary OR upgrade/downgrade/initiates rating OR raised/lowered guidance OR earnings_result.beat != null OR news_data_state==="stale" with 0 articles (the empty-state copy IS content); sector = resolved + data_available with non-zero leading sessions or persistence or daily sessions; geopolitical = any activeEvents OR geoHasLiveEvents OR baseline summary OR exposureSummary OR geoBaselineScore OR geoPrimaryTheme (covers GeoStructuralBaselinePanel cases); internals = ≥ 2 keyPoints; default = ≥ 2 keyPoints. Technical is always considered active (the layer never collapses by this helper). Unavailable status is never active. (c) `isLayerCompressible(layer): boolean` — true when the layer is Neutral or Unavailable AND has no active content AND is NOT Technical. Compressible layers render only their header (icon + name + status pill) at top level; the explanation + chips + extras are wrapped in a `<details data-testid="layer-{key}-collapsed"><summary>Show evaluation detail</summary>...</details>` disclosure. Directional layers (Bullish/Bearish) never compress. (d) `shouldCompressContextLayers(layers): boolean` — strict 3-way gate: News, Macro, AND Geopolitical must ALL be present in the layers array, ALL Neutral, and NONE have active content. Any directional flip or any active extra (FOMC imminent event, baseline summary, earnings beat, etc.) breaks the grouping immediately. (e) `buildCompressedContextSummary(layers): CompressedContextSummary \| null` — returns `{ title: "Context", headline: "Neutral (no macro or news tailwinds present)", statusLabel: "Neutral", collapsedLayerKeys: ["news","macro","geopolitical"], layers: [...] }`. Headline copy is deliberately data-only ("no macro or news tailwinds present") — never advice-flavored. Returns null if any context layer is missing (defensive — caller is supposed to gate on `shouldCompressContextLayers` but the function degrades safely if not). (f) Private `hasNewsPositiveCatalyst(layer): boolean` — separates the "is there evidence of continuation?" question from the broader "is there anything to render?" question. (g) `buildNewsNeutralParenthetical(layer): string \| null` — returns `"no catalyst support → lowers continuation probability"` when News status is Neutral AND `hasNewsPositiveCatalyst` is false (covers the stale+0-articles case AND truly-empty case), null otherwise. The decoupling from `layerHasActiveContent` is critical: the "stale + 0 articles" case renders an empty-state copy ("No qualifying news for ...") that IS content the user needs to see (so the layer isn't compressible), but the absence of a positive catalyst means the parenthetical still fires. Both belong on the card simultaneously — the empty-state copy is the data face, the parenthetical is the implication. **Refactored `frontend/components/signal-evidence-card.tsx`** — the inline `evidence.layers.map((layer) => <article>...)` block in the Signal Layer Breakdown section is replaced with an IIFE that (1) checks `shouldCompressContextLayers` once at the top, (2) builds the Context summary, (3) walks `evidence.layers` in order; if a layer is in `collapsedLayerKeys` and the Context summary card hasn't been rendered yet, render it ONCE at that position and skip the remaining context layers; otherwise compute `tier` + `compressible` + `newsParen` and render the layer article with tier-based visual overrides. Tier overrides come from a new helper `tierVisualOverrides(tier)` co-located with `elevatedCardStyle`: `primary` → padding `spacing[4]` (1rem), fontScale `base`, opacity 1, header weight 700; `secondary` → padding `spacing[3]` (current default), fontScale `sm`, opacity 1, weight 600; `tertiary` → padding `spacing[2]` (compact), fontScale `xs`, opacity 0.82 (still readable on dark surface), weight 600. The article carries `data-testid="layer-card-{key}"` + `data-layer-tier="{primary\|secondary\|tertiary}"` + `data-layer-compressible="{true\|false}"` for test stability. The News-neutral parenthetical renders as a small italic span between the header row and the body (always visible even when the body is collapsed inside `<details>`), with `data-testid="layer-news-neutral-parenthetical"`. The compressed Context card carries `data-testid="layer-compressed-context"` + `data-collapsed-layer-keys="news,macro,geopolitical"` + a `<details data-testid="layer-compressed-context-details"><summary>Show News, Macro, and Geopolitical layer detail</summary><div>three mini-summary panels</div></details>` disclosure. The inner mini-summaries (`data-testid="layer-compressed-context-inner-{news\|macro\|geopolitical}"`) render only the icon + name + status pill + one-line explanation — they intentionally do NOT re-render the full per-layer panels (yield-curve row, sector momentum, geo baseline panel, etc.) because (a) the whole point of compression is reducing visual noise, and (b) the activity check is broad enough that any layer with rich extras is no longer "compressible" anyway → the standalone card renders instead. **Test posture — +14 new helpers tests + +12 render-level tests = 26 new lock-ins in two new files**: `frontend/tests/layer-emphasis.test.ts` (76 tests total) covers every helper exhaustively — constants membership; tier assignment for every layer key × status combination; promotion rules for sector/geopolitical (active content OR directional status); per-layer activity truth tables including the new "stale+0 articles is active" rule for news + "any yield_curve regime is active" for macro + "any layer.geo with narrative is active" for geopolitical; isLayerCompressible never collapses Technical; shouldCompressContextLayers strict 3-way gating with BRK.B regression test; buildCompressedContextSummary payload shape + headline copy hygiene; buildNewsNeutralParenthetical decoupled from active-content (stale+0 STILL returns parenthetical) + suppressed by every flavor of positive catalyst. `frontend/tests/signal-evidence-card-layer-hierarchy.test.tsx` (12 tests) covers the render-level invariants — BRK.B regression "three neutral context layers compress into Context card"; one-layer-bullish-or-active breaks compression; News-neutral parenthetical renders alongside standalone News=Neutral cards (gated on a directional macro to break compression so news renders standalone); parenthetical suppressed when News is Bullish or has earnings beat; Technical always renders at primary tier and never compressible; Sector at tertiary and compressible by default; Internals at secondary; summary disclosure copy locked. **Focused-run verified**: `npx vitest run tests/layer-emphasis.test.ts tests/signal-evidence-card-layer-hierarchy.test.tsx tests/signal-evidence.test.ts tests/signal-evidence-layers.test.ts tests/why-hold-copy.test.ts tests/conflict-chip-ranking.test.ts tests/scenario-eligibility.test.ts` → **240/240 PASSED** in ~52s (7 files including the seven adjacent suites most likely to regress from a layer-card refactor). Two existing tests in `signal-evidence.test.ts` ("yield curve normal shows green" + "upcoming events listed") needed the activity-check broadening for macro to keep passing — those fixtures specify only the macro layer so news/geo defaulted to Neutral and the previous narrow activity check ate the macro yield-curve panel by folding it into the compressed Context card. The fix was correct (a yield-curve row IS content; an upcoming event IS content) and the lock-in `test_macro_normal_yield_curve_is_active` + `test_macro_any_upcoming_event_is_active_even_when_status_is_upcoming` tests in `layer-emphasis.test.ts` pin the new semantics. Three existing tests in `signal-evidence-layers.test.ts` ("geo baseline panel when no live events" / "geo baseline high band renders" / "news stale empty state wording") needed the broader news + geo activity checks for the same reason. Frontend full-suite re-run **89/89 files PASSED, 970/970 tests PASSED** (confirmed prior to commit). **Why this matters operationally**: this pass closes the loop on the BRK.B feedback round entirely. Items 1/2/5 shipped in row 20 (Build Scenario gating, "Why hold" copy, ranked conflict chips); items 3/4/6 ship here (collapse empty layers, compress neutral context, emphasis hierarchy). The BRK.B card the user originally screenshotted had News + Macro + Geopolitical all rendering as full-size cards each saying "Neutral" with no qualifier — three rows of decision fatigue producing zero information. The same card now renders ONE consolidated `"Context: Neutral (no macro or news tailwinds present)"` row with a disclosure to peek at the three sub-layers, AND the News chip (when standalone elsewhere) carries the soft-negative reframing `"no catalyst support → lowers continuation probability"` so users understand that "neutral news" on a short-term setup is a soft headwind, not a no-op. The visual hierarchy promotes Technical (the layer that decides the Decision line) to clearly the biggest card on screen and demotes Sector + Geopolitical to muted tertiary cards UNLESS they have active content, eliminating the "every layer looks equally important even when most are saying nothing" UX problem. Pure helpers + tier-driven styling mean future product decisions (e.g. promoting Internals to primary for confluence-heavy setups, or adding a fourth context layer) only require updating the constants — the render path stays unchanged. Backend test baseline unchanged. Frontend test baseline +26 (2 new files: `layer-emphasis.test.ts` and `signal-evidence-card-layer-hierarchy.test.tsx`). |
| 20 | **BRK.B Evidence Card — second-pass UX polish (Build Scenario gating + Why-hold copy + ranked conflict chips) — SHIPPED ✅** Commit landing **2026-05-13** (frontend + assistant-prompt only, no terraform / no lambda change, no API contract change). User filed a 6-issue follow-up bug report on the BRK.B card after the row-19 stale-bars fix landed; this pass ships the **three surgical, locally-scoped fixes** (items 1, 2, 5) and parks the deeper layer-card refactor (items 3, 4, 6 — collapse empty layers, compress neutral context into one row, visual-emphasis hierarchy) as **B35** in `BACKLOG.md` so the next pass can do them as one focused refactor instead of bolting them onto the same commit. **Fix (1) — Build Scenario button gating at R/R < 2.0**: in the screenshot the button was clickable even though the Decision line read "Monitor only — confirmation and/or risk gates are not fully cleared" and the R/R card read "0.5:1 — Low R/R - below 2:1". Root cause was a deliberate omission: `frontend/lib/scenario/eligibility.ts` is documented to gate on structural completeness **only** (never on conviction signals — confluence, accuracy, layer alignment, engine verdict), and R/R was historically lumped in with "conviction." That call was wrong on inspection: R/R is pure entry/stop/target arithmetic on the reference levels the signal already carries, NOT a quality verdict. A 0.5:1 sheet plans to lose more than it stands to gain, which is not a coherent **structural** planning sheet regardless of how strong other signal layers are. Fix added `risk_reward?: number \| null` to `ScenarioInput` with a multi-paragraph explanatory comment, added `low_risk_reward` to `SCENARIO_INELIGIBILITY_REASONS`, added the gate (`rr > 0 && rr < 2.0` — defensive on missing/non-finite/zero/negative, only fires when the field is a positive finite number below threshold) inside `isEligibleForScenario`, and added the user-facing label `"Risk/reward does not meet internal thresholds for structured scenario building."` Threshold matches `synthTradeDecision::rrFail` (2.0) so the Build Scenario button is now in lock-step with the Decision line on the same card — they can never disagree on whether the setup clears the R/R gate. Wired `risk_reward: insight.risk_reward` into the evidence-card call site (the only path with access to the engine's computed R/R); gap card + setup row call sites in `scanner-page-client.tsx` are intentionally unchanged because their source payloads don't carry R/R (the field stays `undefined` → defensive path doesn't gate). **Fix (2) — "Why hold:" / "Why blocked:" copy rewrite to drop "trade permission" language**: the previous wording threaded `"STOCVEST … before granting trade permission"` through every rationale variant in `deriveDecisionRationale`. Users read that phrase as STOCVEST claiming gatekeeper authority over their trade decisions — which is exactly the implication our legal posture is built to avoid. STOCVEST does NOT grant or withhold trade permission; we surface signal data and the user decides. All five rationale variants rewritten with one unified framing — `"The setup does not meet internal thresholds for structured scenario building"` — so the gate is described as an internal numerical threshold on whether the platform will scaffold a Scenario Builder sheet, never as a verdict on the trade itself. New copy by category: **risk_reward** → `"Risk/reward (X.X:1) does not meet internal thresholds for structured scenario building."` **data_insufficient** → `"Layer coverage is too thin to evaluate this setup with conviction. The setup does not meet internal thresholds for structured scenario building until additional layers populate."` **confirmation** → `"Layer agreement is mixed across the six signal layers. The setup does not meet internal thresholds for structured scenario building until directional confirmation strengthens."` **regime** → `"Macro or regime context conflicts with this direction. The setup does not meet internal thresholds for structured scenario building while regime alignment is opposed."` **readiness** → `"Signal readiness is not yet decisive across the six layers. The setup does not meet internal thresholds for structured scenario building."` Also rewrote the one matching example in `stocvest/signals/assistant_prompts.py` line 452 so the chatbot speaks the same vocabulary. **Fix (5) — Conflicting signal chip rail ranked Primary / Secondary / Tertiary**: in the BRK.B screenshot the conflicting chips arrived in source-order `[EMA conflict, VWAP conflict, Weak volume]` and the user couldn't tell which one was load-bearing. Added a canonical priority order `CONFLICT_PRIORITY_BY_SOURCE` in `frontend/lib/signal-evidence.ts` — `vwap_position=1 > volume_confirm=2 > ema_9_position=3 > internals_alignment=4 > sector_alignment=5 > market_regime=6 > news_catalyst=7 > gap_confirm=8 > orb_breakout=9` — encoding "what does the user need to know first when assessing the friction on this setup": technical / execution-quality anchors first, broad-market context after, discrete catalysts and entry-timing signals last. New helper `rankConflictingSignals<T extends { source?: string }>(chips)` returns a stably-sorted copy (equal-priority chips keep arrival order via index pairing) without mutating the input; unknown sources sort to the tail with `Number.POSITIVE_INFINITY` so future chip types degrade gracefully. New helper `conflictTierLabel(index, total)` returns `"PRIMARY" \| "SECONDARY" \| "TERTIARY" \| null` — null when total < 2 (a single conflict needs no tier label because there is nothing to rank against) or when index > 2 (only the top three get a label). Frontend `mapConfluenceChipList` updated to pass the backend's `source` enum through to the chip type (was being dropped); the setup-derived path in `buildEvidenceFromSetupRow` updated for the same reason. The evidence-card render in `signal-evidence-card.tsx` now sorts the conflicting chips via `rankConflictingSignals` and renders a small tier badge inside the first three pills — uppercase, fontSize 9, letterSpacing 0.6, padded with a translucent red background so it visually sits "inside" the pill without overpowering the chip label. Confirming chips deliberately keep their natural order (they are co-equal anchors of the setup, not a ranked list — the user only asked for ranking on the conflicts). Carried `data-testid="conflict-chip-{primary\|secondary\|tertiary}"` + `data-conflict-tier` + `data-conflict-source` attributes on each ranked chip for test stability. **Lock-in tests — +73 new across three files**: `frontend/tests/scenario-eligibility.test.ts` gains a `low risk/reward gate (BRK.B feedback, 2026-05-13)` describe block with 10 tests covering rr-below-2 fails, **rr=0.5 BRK.B regression**, rr-at-threshold 2.0 passes, rr=3.0 passes, rr=1.8 fails, missing field does not gate, null does not gate, NaN does not gate, zero/negative does not gate (treated as unknown not low), AND a copy-hygiene test asserting the label uses the internal-thresholds framing and forbids the legacy "trade permission"/"approved" wording. The existing `test_eligibility_input_shape_has_no_conviction_fields` test was extended to allow `risk_reward` in the input shape with an explanatory comment naming this incident by date. New file `frontend/tests/why-hold-copy.test.ts` ships 24 tests across 6 describe blocks: actionable state returns null, every rationale category uses the internal-thresholds framing, the risk_reward variant displays the R/R number AND uses "Why hold:" in monitor state AND uses "Why blocked:" in blocked state, **the BRK.B 0.5:1 regression case**, and an exhaustive sweep that enumerates the full rationale category space and asserts none carry the legacy phrases (`granting trade permission`, `requires favorable asymmetry`, `requires regime alignment`, `requires clearer directional confirmation`, `waits for ... before granting`, `stocvest requires`, `approved`, `we recommend`, `we do not recommend`). New file `frontend/tests/conflict-chip-ranking.test.ts` ships 18 tests across 3 describe blocks: the canonical priority order is locked at vwap=1/volume=2/ema=3, technical sources MUST rank below context sources (the user's explicit "increase emphasis Technical+Risk, reduce emphasis Geo+Sector" feedback), gap/orb sort at-or-near tail; **the exact BRK.B re-ordering test** asserts `[EMA, VWAP, Volume]` → `[VWAP, Volume, EMA]`; the full nine-source rail orders correctly; unknown source sorts to the tail preserving order; missing source field sorts to the tail; stable sort for equal priority; empty array returns empty; does not mutate input. Tier-label tests cover three-conflict tiers, index past 2 returns null, single conflict gets no label, two conflicts get Primary+Secondary, negative index returns null, zero total returns null. **Focused-run verified** — `npx vitest run tests/scenario-eligibility.test.ts tests/why-hold-copy.test.ts tests/conflict-chip-ranking.test.ts` 73/73 PASSED in ~18s. Existing-test smoke `npx vitest run tests/signal-evidence.test.ts tests/build-scenario-button.test.tsx tests/signals-page-mode-clears-screen.test.tsx` 84/84 PASSED — the new chip-ranking logic and the new R/R gate did not regress the evidence-card render, the build-scenario-button enabled/disabled state, or the mode-clears-screen rule. Full frontend + backend suites pending end-of-day re-run per the user's "do not run all tests now" instruction. **Parked for follow-up (B35 in BACKLOG.md)** — items 3 (News=Neutral reframing + collapse empty layers by default), 4 (compress News+Macro+Geo neutrals into a single "Context: Neutral (no macro or news tailwinds present)" row to fight decision fatigue), 6 (visual-emphasis hierarchy: more weight on Technical+Risk cards, less weight on Geo+Sector unless active). Those three are a deeper refactor of the layer-card rendering block in `signal-evidence-card.tsx` (~600 lines) and benefit from being done together as one design-coherent commit instead of bolting onto this surgical fix. **Why this matters operationally**: this pass addresses the user's three highest-priority items from the second BRK.B feedback round (action button gating, advice-implying copy, unranked chip rail). The R/R gate closes a real UX defect — the Build Scenario button was inviting the user to plan around a 0.5:1 reference structure even though the engine itself said "Monitor only — confirmation and/or risk gates are not fully cleared." The copy rewrite removes the most legally-loaded phrase in the evidence card (`"STOCVEST … before granting trade permission"`) and is locked in by an exhaustive forbidden-phrase sweep that will fail loudly if any future rewrite re-introduces it. The chip ranking turns three equal-weight pills into a Primary/Secondary/Tertiary stack so the user knows VWAP rejection > Weak participation > EMA misalignment at a glance. Backend test baseline unchanged (still 1417 passing). Frontend test baseline +73 (3 new test files). |
| 19 | **BRK.B Evidence Card — catastrophic stale-bars root cause + 4 user-reported issues — SHIPPED ✅** Commit landing **2026-05-13** (no terraform / no lambda change, no API contract change). User filed a 5-issue bug report against the BRK.B evidence card (VWAP/EMA chip polarity backwards, broken reconciler grammar, $50.36 VWAP on a stock that trades at $485, internals-bullish counter-signal invisible in the chip rail, AI Signal Analysis section empty) and explicitly asked for "a thorough audit to make sure there are no more bugs in the code". **Root cause** turned out to be a single catastrophic bug in `stocvest/data/polygon_client.py::get_bars` that was poisoning every technical computation in production: the function defaulted `from_date="2000-01-01"` (with a comment claiming "Polygon ignores if limit is set" — wrong) and sent `sort=asc&limit=N`, which Polygon honors by returning the **OLDEST** N bars in the range. Live probe before the fix: `get_bars('BRK.B', MIN_1, limit=60)` returned 60 minute bars from **2003-09-10** (close ~$50, the post-2010-split BRK.B price), and `get_bars('BRK.B', DAY_1, limit=210)` returned 210 daily bars from 2003-09-10 through 2004-07-12. **Every technical layer in production** (SMA50, SMA200, RSI, MACD, VWAP, EMAs, golden/death cross, ATR, volatility regime, breakout detection, base-formation) was therefore running against **22-year-old prices**. This is the actual reason the user saw VWAP $50.36 on BRK.B: the day-trade engine's TechnicalAnalyzer computed VWAP from minute bars from 2003. The hidden second-order effect — and what made this so hard to spot — was that the technical analyzer compared the analyzer-computed historical SMA50 (~$59 from 2003 BRK.B) to today's snapshot price ($485) and trivially decided "price above SMA50" → bullish bias on basically every name in the system, regardless of actual current technical structure. **Fix (a) — `polygon_client.get_bars` semantics split** into two explicit modes: "recent N bars" (both `from_date` and `to_date` are `None`) now sends `sort=desc` to Polygon and **reverses the results in Python** before returning so every downstream analyzer still receives bars in the oldest-first chronological order they all assume (SMA windows, RSI seed, MACD seed all walk forward by index). "Explicit date range" (caller pinned at least one edge — `sector_daily_cache` for a trailing 7-day audit, `orb_compute_worker` for a single trading day, the public `/v1/bars` handler when a user supplies `from`/`to` query params) keeps `sort=asc` so oldest-within-window callers are unchanged. Partial-edge calls (only one of from/to) are conservatively treated as range mode to avoid surprising callers who pinned one edge. The new behaviour is locked in with a multi-paragraph docstring inside `get_bars` that names this incident by date so a future refactor cannot silently revert. **Live verification post-fix** — `get_bars('BRK.B', MIN_1, limit=60)` now returns bars 2026-05-13 19:24 through 21:41 UTC (today's actual session); `get_bars('BRK.B', DAY_1, limit=210)` returns bars 2025-07-10 through 2026-05-13. **End-to-end engine smoke** post-fix on BRK.B: swing now reads SMA50=$479.58, SMA200=$489.78 (the actual recent values — was $59.35 / $57.88 before fix); both engines now correctly report BRK.B verdict=**neutral** (was bearish/short with score 40 in the user's screenshot — that "short signal" was entirely an artifact of comparing today's $485 price to 2003's $50ish VWAP). **Fix (b) — Issue 4, internals chip in confluence**: added `internals_alignment` chip to `stocvest/signals/confluence.py` and wired both composite engines to pass `internals_signal=normalize_direction(internals.verdict)`. Mirror of the sector-chip design from row 16: chip label describes the broad-market state intrinsically (`"Internals bullish"` / `"Internals bearish"`), placement in confirming vs conflicting is decided by setup direction. A bullish broad market on a short setup now lands in `conflicting_signals` with detail "Breadth and participation broadly up — broad market is rising." — exactly the counter-signal that was invisible in the user's screenshot. Backwards-compatible: `internals_signal` parameter defaults to `"neutral"` so `signal_dto.py` and `/v1/signals/composite` callers that don't pass it still work (they just don't emit the chip). **Fix (c) — Issue 2, reconciler text shape** in `frontend/lib/signal-evidence.ts::buildVerdictTagReconciler`: rewrote the sentence to lead with the anchor list instead of the opposite-polarity context. New shape: `"Anchored by X and Y; counterweight(s): Z."` (singular "counterweight" when only one). No-anchor fallback: `"No confirming signals for the {short|long} setup; only opposing context: …"` (now direction-aware). The old "Bullish-leaning context present (X, Y, Z), but the setup is anchored by W" wording is permanently retired — three anti-regression `not.toMatch` assertions guard it across the test file. **Fix (d) — Issue 5, AI Signal Analysis empty state** in `frontend/components/signal-evidence-card.tsx`: hardened `captureDisplayText` with a three-tier fallback chain — (1) live/cached Anthropic capture text → (2) `evidence.aiVerdict` (the deterministic hardcoded direction-keyed sentence built by `buildEvidenceFromSetup`) → (3) an on-the-fly direction-aware sentence (`"{SYMBOL} setup leans {bullish\|bearish} based on layer agreement. Open Evidence for layer detail."`) so the italic quote block under the "AI Signal Analysis" header never renders as empty `""` even if the BFF route or Anthropic call fails entirely. Also removed the dangling `<span>Signal summary</span>` caption that had no value bound to it (just a label rendered next to a freshness timestamp) — users were reading it as "Signal summary: (blank)" and reporting it as a defect. **Issue 1 (VWAP/EMA chip polarity)** turned out to NOT be a separate bug: the chip logic in `confluence.py` is already direction-aware (`if direction == "long" and price > vwap` → confirming; `elif direction == "short" and price < vwap` → confirming; else → conflicting). The user's observation was caused by Issue 3 — the technical analyzer's display string (computed from 2003 minute bars) said "Price below VWAP $50.36" while confluence (using today's snapshot day_vwap) saw price > vwap and correctly emitted "VWAP conflict" — they disagreed only because their data sources disagreed, which is now impossible with the get_bars fix. **Issue 3** (catastrophic VWAP $50.36 value) is fully explained by and resolved by Fix (a). **Broader audit** the user explicitly asked for: walked every `get_bars` caller in the codebase (`swing_composite_engine`, `real_composite_engine`, `market_data` handlers, `sector_daily_cache`, `orb_compute_worker`, `scanner_scheduled_pipeline`, `polygon_client` itself) — every case-A caller (no dates) benefits from the fix; every case-B caller (explicit dates) preserves behaviour. Audited `get_evaluated_price_after_signal` separately — that function uses explicit timestamp ranges and was never affected. Verified `historical_validation` doesn't go through `get_bars`. Verified all chip-generation code paths in `confluence.py` are direction-aware (ORB, VWAP, EMA9, regime, sector, internals, volume, news, gap). **Lock-in tests — +11 new across two files**: `tests/data/test_polygon_client.py` gains 3 tests under "Recency / sort regression (BRK-B Issue 3 fix, 2026-05-13)" — `test_recent_mode_sends_sort_desc_and_returns_oldest_first` (asserts the wire param `sort=desc` AND the caller-facing list is sorted ascending), `test_explicit_date_range_keeps_sort_asc` (asserts both-edges-pinned still uses `sort=asc`), `test_partial_dates_still_treated_as_range_mode` (asserts one-edge-pinned is conservatively `sort=asc` too). `tests/signals/test_confluence.py` gains 7 tests under "Internals-alignment chip invariants (BRK-B Issue 4 fix, 2026-05-13)" — long+bullish-internals lands in confirming, short+bearish-internals lands in confirming, **`test_internals_bullish_on_short_setup_lands_in_conflicting_BRK_B_regression`** (direct regression for the user's reported case), long+bearish-internals lands in conflicting, neutral internals emits no chip, default-argument back-compat, `internals_alignment` listed in `ConfluenceDetector.SIGNAL_SOURCES`. Frontend `tests/signal-evidence.test.ts` reconciler `describe` block fully rewritten — 9 tests covering anchor-first shape on both directions, singular vs plural counterweight label, no-conflicting-chips → null, no-confirming-chips fallback with direction word, neutral verdict → null, parenthetical stripping, geopolitical-drag splicing, geopolitical de-dupe, and the BRK.B-specific regression with `Internals bullish` in the counterweights list. Plus the rendered-card test (`SignalEvidenceCard verdict-tag reconciler rendering`) updated to seed the BRK.B chip layout the engines now actually emit (`confirming=[Sector lags market]`, `conflicting=[Internals bullish, Weak Volume]`) and assert the new anchor-first sentence renders with three anti-regression `not.toMatch` guards on every prior phrasing. **Test posture**: focused runs verified — `pytest tests/data/test_polygon_client.py::TestGetBars` 7/7 PASSED in 3.49s; `pytest tests/signals/test_confluence.py` 34/34 PASSED in 2.87s (was 27, +7 internals); `pytest tests/signals/ tests/api/test_class_share_symbol_normalization.py tests/data/test_polygon_client.py -x -q` 569/569 PASSED in 39.22s (full signals + symbol-normalization + polygon-client surface no-regression); `pytest tests/api/test_real_composite.py tests/api/test_swing_composite.py tests/api/test_swing_composite_evidence.py tests/signals/test_composite_score.py tests/signals/test_composite_scorer.py tests/api/handlers/test_signals.py -x -q` 62/62 PASSED in 17.27s; frontend `npx vitest run tests/signal-evidence.test.ts` 74/74 PASSED; broader frontend smoke across 5 related files 123/123 PASSED. Full backend + frontend suites re-run pending end-of-day per the user's "do not run all tests now" instruction. **Why this matters operationally**: this is by a wide margin the most important bug ever found in this codebase. Every technical-layer score and every chip rail on every evidence card in production has been computed against 2003-era prices since the engine was first written. The bullish-everything bias the user has been reporting for weeks (and was being attributed to other causes) was almost entirely a product of "today's price > 2003 price" being trivially true. With the fix, layer outputs are finally honest — BRK.B day-trade goes from a misleading "short" signal to "neutral" (which is what the actual chart reads as), and we should expect a wave of similar honest-truth corrections across other names. The fix is surgical (no API contract change), backwards-compatible (case-B callers untouched), and tested at three layers (wire-level mock, engine-level smoke, end-to-end live Polygon probe). The internals chip closes the symmetry gap with sector chips so future user-reported "the chip rail is missing the obvious counter-signal" complaints have nowhere to land. The reconciler text rewrite gets rid of the "Bullish-leaning on a short setup" cognitive contradiction permanently. The AI Signal Analysis fallback chain means the section can never render visually empty even under partial backend failure. Backend baseline now **1417 passing** (+7 over 1410 from row 17). |
| 18 | **SPY/QQQ/IWM Shared Context tiles — line sparkline → signed 5-bar daily-returns histogram — SHIPPED ✅** Commit landing **2026-05-13** (frontend only, no terraform / no lambda change, no API contract change). Direct response to the user's question on the dashboard SPY/QQQ/IWM macro strip: "do you think this line graph is good or how about a small horizontal bar graph 5 bars indicating each day. If they go -ve, it should be shown in red on the other direction." **Decision**: the bar histogram wins at this card size. **Why**: the tile is roughly 120px wide; a line sparkline at that resolution conveys direction-only and the headline % label (`+1.19%` etc.) already covers that. The sparkline was filling space without adding information. 5 colored daily-delta bars instead expose the *shape* of the move — "three down days followed by two strong up days" produces the same +1.19% net as "five tiny up days in a row" but they are completely different tape characters, and only the bar viz makes that distinction legible. **Changes**: (a) New component `frontend/components/index-returns-histogram.tsx` (~140 lines, pure SVG, no chart library). Component derives daily returns from consecutive close pairs (N closes → N − 1 bars), per-card magnitude scaling (tallest bar fills the half-canvas budget independent of the other tiles — cross-tile magnitude comparison lives in the % label, not in the bars), bars sit above/below a faint zero baseline so polarity is positional *as well as* chromatic (colorblind-safe positional encoding — up bars grow upward from the baseline, down bars hang downward), color via `getChangeColor(returnPct, colors)` reusing the existing dashboard role-color vocabulary with the same ±0.1% neutral band as the % label next to the histogram (one source of truth for color invariants — no new palette entries). Near-flat sessions inside the ±0.1% band render a 1px-tall stub straddling the baseline so the day count is preserved (we never silently drop a session). Each bar carries a `<title>` child describing "Session N: +X.YZ%" for screen-reader / hover-tooltip access. (b) `frontend/components/dashboard-page-content.tsx::buildWeeklyRows` — bumped the `closes5d` slice from `closes.slice(-5)` to `closes.slice(-6)` so the histogram has 5 daily returns to render (6 closes → 5 close-to-close returns → 5 bars, one per regular trading session over the trailing week). Field name kept as `closes5d` because the *labelled window* is the 5-day window, not the array length. Defensive: when fewer than 6 closes are available, the histogram renders whatever bars the data supports (4 from 5 closes, 3 from 4, etc.) — never throws. (c) `frontend/components/shared-context-master-card.tsx` — swapped `IndexSparkline` import + render for `IndexReturnsHistogram`, updated aria-label from "5-session close trajectory" to "5-session daily returns histogram". (d) `IndexSparkline` (line viz) is intentionally **kept** in `frontend/components/index-sparkline.tsx` — the dashboard hero-strip VIX intraday tile still uses it (different use case: smooth intraday curve over many points within a single session, not 5 discrete daily closes). Pinning that boundary explicitly so a future refactor doesn't accidentally delete the line component. **Lock-in tests — +18 new in `frontend/tests/index-returns-histogram.test.tsx`** across five `describe` blocks: *bar count + axis* (N − 1 bars from N closes, 4 bars from 5 closes defensive case, nothing renders for <2 closes / empty, non-finite/zero/negative stripped before computing returns, zero-line baseline rendered), *polarity (color + position)* (positive → `data-sign="up"` + fill differs from negative, negative → `data-sign="down"` and bar Y at-or-below baseline, positive → bar TOP strictly above baseline, near-flat ±0.1% → `data-sign="flat"` + 1px stub, five-bar mixed week renders each polarity correctly), *magnitude scaling* (larger |%| → taller bar, tallest bar bounded by half-canvas budget), *accessibility* (custom aria-label, default aria-label, per-bar `<title>` describing the session return), *anti-regression* (renders `<rect>` not `<polyline>` — line viz is gone from this surface; does NOT mount under the old `data-testid="index-sparkline"` — anti-rename guard). **Updated existing test** `frontend/tests/dashboard-role-color-language.test.tsx::section_A_renders_three_index_tiles_each_with_a_sparkline` → renamed to `section_A_renders_three_index_tiles_each_with_a_daily_returns_histogram`; assertions switched from `polyline` ≠ null to `rect`-count > 0; added anti-regression that the old `data-testid="index-sparkline"` is NOT in any Section A tile. Existing fixture's 5-close `closes5d` array now produces 4 bars (defensive case), which still satisfies "> 0 bars". **Focused-run verified**: 39 tests / 2 files (18 new histogram lock-ins + 21 existing role-color-language tests) all green. **Adjacent test smoke** — `tests/dashboard-hero-strip.test.tsx` (VIX intraday line still uses `IndexSparkline`) + `tests/shared-context-derivations.test.ts` + `tests/dashboard-two-desk.test.tsx` 63 tests / 3 files all green. **Why this matters operationally**: the macro strip is the first thing every user looks at on the dashboard load — making the 120px of canvas in each tile carry more information density is a high-leverage UX upgrade for a tiny diff. Pattern is also reusable: any future "5-day return at thumbnail size" surface (watchlist row, scanner result card, signal evidence card historical context) can drop in `<IndexReturnsHistogram closes={...} />` and inherit the same color/position invariants. Full frontend suite re-verification pending end-of-day per the user's "do not run all tests now" instruction. |
| 17 | **Class-share dash-form symbol normalization (BRK-B → BRK.B) at every Polygon boundary — SHIPPED ✅** Commit landing **2026-05-13** (no terraform / no lambda change, no API contract change). Direct follow-up to the BRK-B evidence-card investigation in row 16. While verifying the reconciler/sector-chip fix end-to-end I ran BRK-B through both composite engines against the live deployed Polygon REST surface and discovered a second, deeper bug that fully explains the bizarre "Sector Bearish" + non-actionable signal the user originally screenshotted. **Live probe on 2026-05-13** (`GET https://api.polygon.io/v2/...`): `GET /v2/aggs/ticker/BRK-B/range/1/day/...` → `results=[]` (silent — Polygon returns HTTP 200 with an empty array, NOT a 404, so the call doesn't even raise); `GET /v2/aggs/ticker/BRK.B/range/1/day/...` → 10 bars; `GET /v2/snapshot/locale/us/markets/stocks/tickers/BRK-B` → `404 NotFound`; `GET /v2/snapshot/locale/us/markets/stocks/tickers/BRK.B` → 200 OK. **Root cause**: the swing + real (day-trade) composite engines did `sym = symbol.strip().upper()` and then handed that string verbatim to every Polygon call inside `asyncio.gather(..., return_exceptions=True)`. The snapshot 404 became a silent swallowed exception (gather suppresses it); the bars endpoint returned `0 results` without raising. The downstream layer cascade in `build_swing_composite_response` / `run_real_composite_engine_phase` saw `bars=[]` and `sym_snap=None`, then: (a) `TechnicalAnalyzer` short-circuits to `status="unavailable"` when daily history is below the 60-bar swing floor; (b) `sector_layer_api_extras` short-circuits to `status="unavailable"` when either `sym_snap` or `spy_snap` is None (sector resolution can't run without an SIC lookup → can't resolve sector ETF → can't compute relative strength); (c) the remaining macro / news / geopolitical / internals layers still scored normally and the composite-score engine averaged them into a neutral-ish 50/100. End result: a user typing `BRK-B` in the search box got a permanently-degraded "incomplete" composite with two layers blank-rendered, while the *exact same engine* fed the same symbol in dot form returned a fully-scored verdict. The deprecated scanner universe in `stocvest/data/scanner_universe.py` already used `BRK.B`/`BRK.A` (dot form), which is why the scanner path never tripped this — only the user-typed-search path exposed the bug. **Fix**: new module `stocvest/data/symbol_normalize.py` exporting `to_polygon_symbol(sym: str) -> str`. Rule is deliberately conservative — only the class-share dash pattern (regex `^([A-Z]+)-([A-Z])$`, single trailing letter) gets rewritten with a dot. Everything else (preferred shares with multi-letter suffixes like `JPM-PRD`, index symbols like `I:VIX`, legacy `^VIX`, normal tickers like `AAPL`) passes through untouched so the helper does not accidentally mangle anything Polygon happens to accept in a different form. Idempotent (already-canonical `BRK.B` returns unchanged), safe on empty string. **Applied at two call sites only** — both composite engine entry points: `stocvest/api/services/swing_composite_engine.py::build_swing_composite_response` (replaced the `sym = symbol.strip().upper()` line with `sym = to_polygon_symbol(symbol)`) and `stocvest/api/services/real_composite_engine.py` at both `build_real_composite_response` and `run_real_composite_engine_phase` (defensive belt-and-braces — `run_real_composite_engine_phase` is also exported and imported from at least one other call site, so it gets its own normalization independent of `build_real_composite_response`'s wrapper). The Polygon client itself stays unchanged — normalization at the engine boundary is sufficient and keeps `PolygonClient` a thin transport layer. The response payload now surfaces the canonical (Polygon) form everywhere (`"symbol": "BRK.B"`), which matches the universe convention already used elsewhere; the UI/typeahead is free to accept either form on the input side. **Lock-in tests — +23 across two new files**: `tests/data/test_symbol_normalize.py` (20 unit tests parametrised across the rewrite cases — BRK-B, BRK-A, RDS-A, BF-B, HEI-A — and the leave-alone cases — AAPL, MSFT, SPY, XLF, BRK.B, BRK.A, I:VIX, ^VIX, JPM-PRD, GS-PRJ, FOO-BAR, A-BC — plus strip-and-upper, idempotency, empty input); `tests/api/test_class_share_symbol_normalization.py` (3 engine-level regression tests built on a custom `_RecordingPoly` fake that records the symbol form passed to every Polygon method — `test_swing_engine_canonicalises_class_share_dash_at_entry` asserts every captured `get_bars` / `get_snapshot` / `get_market_news` call sees `BRK.B`, never `BRK-B`, AND that the response field `"symbol"` is the canonical form, AND that the composite is not collapsed to `insufficient_data`; `test_real_engine_canonicalises_class_share_dash_at_entry` is the symmetric day-trade variant; `test_swing_engine_leaves_normal_symbols_unchanged` is the hygiene smoke check pinning that `AAPL` survives the normalizer). **Live end-to-end verification (against deployed Polygon)** post-fix: swing `BRK-B` → `symbol='BRK.B'`, `status='active'`, `verdict='bullish'`, `score=63` (bit-for-bit identical to the `BRK.B` direct run earlier in the session); day `BRK-B` → `symbol='BRK.B'`, `status='active'`, `verdict='bearish'`, `score=40` (bit-for-bit identical). The new sector chip from row 16 renders correctly in both: swing → `✓ Sector leads market` (XLF +1.09% vs SPY -0.62% over 5d, sector_alignment confirming for the long); day → `✓ Sector lags market` (XLF -1.03% vs SPY +0.65% over 1d, sector_alignment confirming for the short) — the symmetric mirror, exactly as the test_sector_chip_symmetry_invariant test pins. **Why this matters operationally**: the dash form is what a Bloomberg-trained user types by reflex (`BRK-B`/`RDS-A`/`BF-B` are the standard wire-service notations). Without normalization the engine produced silently-degraded results on these symbols — no error, no warning, just two layers missing and a meaningless neutral score. The pre-existing scanner-universe-only path masked the bug from automated regression coverage. Future symbols that exhibit similar dash/dot quirks (preferred-share variants, ADR class shares) can be added to `to_polygon_symbol` with one regex extension and one parametrised test case — the wrapper is co-located with the rule. Backend baseline now **1410 passing** (+23 over the 1387 baseline from row 16) — verified by `pytest tests/data/test_symbol_normalize.py tests/api/test_class_share_symbol_normalization.py -v` (23/23 PASSED in 11.11s) plus the related `tests/api/test_swing_composite.py` + `tests/signals/test_confluence.py` re-run (31/31 PASSED in 13.70s) as a no-regression check on the engines I touched. |
| 16 | **BRK-B evidence card — reconciler-text contradiction + sector-chip relative-strength relabel — SHIPPED ✅** Commit landing **2026-05-13** (no terraform / no lambda change, no API contract change — pure copy + label fixes). Two coupled bugs the user surfaced on the BRK-B Evidence card. **Bug A — reconciler contradiction**: `frontend/lib/signal-evidence.ts::buildVerdictTagReconciler` produced "Bullish inputs present, but overridden by VWAP conflict + EMA conflict + Weak Volume" on a short setup whose only confirming chip was bearish-aligned (originally "Sector Bearish"). The previous reconciler hard-coded "Bullish inputs" for the confirming list irrespective of setup direction; on a short setup, the confirming list is bearish-aligned by construction, so the sentence read as a logical contradiction. **Fix**: the reconciler now (i) describes the **conflicting** list's opposite-polarity context correctly — "Bullish-leaning context present (…)" for a short setup, "Bearish-leaning context present (…)" for a long setup; (ii) names what's still anchoring the setup — "but the setup is anchored by …" — using the confirming-chip labels verbatim; (iii) handles the no-confirming-chips case with a calmer "no opposing chips support the setup" tail; (iv) returns `null` on neutral direction or empty conflicting list (nothing to reconcile). **Bug B — sector-chip polarity mislabel** (root-cause of repeated user reports of the shape "card says sector is bearish but it should be bullish"): the confluence-engine sector chips (`stocvest/signals/confluence.py`) used the labels "Sector Bullish" / "Sector Bearish" / "Sector conflict" to describe the sector's **alignment with the setup**, but the wording reads as a **polarity verdict on the sector itself**. The sector layer's actual measurement is relative strength *vs SPY* (XLF can be up in absolute terms yet labelled "bearish" if it lags SPY) — the chip text obscured that. **Fix**: the chip label now describes the **sector's intrinsic state relative to SPY** and the ✓/✕ column carries the alignment signal on its own. Bullish-relative sector → "Sector leads market" (detail "Sector ETF outperforming SPY (relative strength)."); bearish-relative sector → "Sector lags market" (detail "Sector ETF underperforming SPY (relative strength)."); whether the chip lands in `confirming_signals` or `conflicting_signals` is determined by the setup direction. Neutral sector signal produces no chip. The frontend renders chip labels verbatim — no production code parses the literal string. **Lock-in tests**: backend +5 in `tests/signals/test_confluence.py` under "Sector chip label invariants (BRK-B fix, 2026-05-13)" — bullish-sector-long pins "Sector leads market" + "SPY" in detail, bearish-sector-short pins "Sector lags market" + explicit anti-regression assertions that the strings "Sector Bearish" and "Sector Bullish" are NEVER in the label, bullish-sector-short pins the symmetry invariant (label still says "Sector leads market" but chip lands in conflicting), bearish-sector-long symmetric counterpart, neutral-sector produces no sector_alignment chip in either list. Frontend +0 net (existing `tests/signal-evidence.test.ts` reconciler tests updated to use the relabelled chip strings — BRK-B regression test now seeds `confirming: [{ label: "Sector lags market" }]` so the test reflects what users actually see on screen; `SignalEvidenceCard` render test asserts both the new label is present AND the old "Sector Bearish" string is NEVER in the rendered HTML). **Why this matters operationally**: the confusing chip wording cost a debugging round-trip every time a user pinged about sector polarity; the new wording is unambiguous (sector leads or lags SPY, ✓ means aligned with this setup, ✕ means against this setup) and the reconciler sentence is symmetric for both setup directions. The deprecated "Sector Bullish" / "Sector Bearish" labels are referenced in the codebase only inside historical-context comments and regression-guard assertions (`expect(html).not.toContain("Sector Bearish")` in the render test, `assert "Sector Bearish" not in matched[0]["label"]` in the backend test). Backend baseline now **1387 passing** (+5 over 1382). |
| 15 | **Layer 4 (second slice) — SWR coverage extended to composite, news, dashboard, macro — SHIPPED (frontend only) ✅** Commit landing **2026-05-13** (no terraform / no lambda change, no API contract change). Direct follow-up to row 13 (Layer 4 first slice). Scope was the second-slice plan parked in `docs/PERFORMANCE.md` §1 layer 4 + §4C + the D11 backlog row: extend the SWR cache surface to the remaining imperative `useEffect → fetch` blocks on the two heaviest-traffic surfaces (`/dashboard/signals` and `/dashboard`). **Four new hooks added under `frontend/lib/hooks/`:** (a) `use-signal-composite.ts` — `useSignalComposite(symbol, mode, { enabled })` wraps the POST to `/api/stocvest/signals/composite/{swing\|real}` with `{ symbol }` body, cache key `["stocvest:signal-composite", upperCaseSymbol, mode]`. Mode is part of the key so Mode Separation invariant #5 in `PERFORMANCE.md` survives the cache layer (a swing-engine composite for AAPL never aliases the day-engine composite for AAPL). Hook overrides the global `keepPreviousData:true` to `keepPreviousData:false` — the signals page has an explicit user-requested "clear screen between mode pills" UX rule (lock-ins in `tests/signals-page-mode-clears-screen.test.tsx`), so a fresh cache-key transition must return `composite:null` synchronously rather than carry the previous mode's data on screen during the transition. (b) `use-symbol-news.ts` — `useSymbolNews(symbol, { limit, mode, enabled })` wraps the existing `fetchSymbolNews`, cache key `["stocvest:symbol-news", upperCaseSymbol, limit, mode]`. Errors collapse to `articles:[]` so the after-hours panel never shows a dangling spinner. Keeps the global `keepPreviousData:true` default — adjacent-symbol toggles within the same mode keep the previous article list on screen until the new one resolves. (c) `use-dashboard-payload.ts` — `useDashboardPayload(mode, { refreshIntervalMs })` wraps `fetchDashboardData`, cache key `["stocvest:dashboard-payload", mode]`. Exposes a stable `dashboardPayloadKey(mode)` builder so the live-hint SSE handler can `mutate(dashboardPayloadKey(mode))` without duplicating the namespace string. Default `refreshIntervalMs:60_000` matches the pre-Layer-4 `setInterval` cadence; SWR's `refreshInterval` pauses automatically when the tab is hidden (free polling-cost reduction). (d) `use-macro-context.ts` — `useMacroContext()` wraps `fetchMacroContext`, cache key `["stocvest:macro-context"]`. Single-tuple key with NO user-id segment — the endpoint is user-agnostic so we deliberately dedupe across users on the same browser (cross-cutting invariant #2 in `PERFORMANCE.md`). **Three call sites refactored**: (i) `components/signals-page-client.tsx` — dropped `[compositeResult, setCompositeResult]` and `[radarData, setRadarData]` `useState` pair plus the entire `[symbol, tab, tradingMode]` composite `useEffect` (~70 lines). `compositeResult` is now read from `useSignalComposite`; `radarData` becomes a pure `useMemo` projection of `compositeResult`; `signalEvidence` lifecycle stays local (it's built from a different source — the user-initiated "Show evidence" download click) but resets via a slimmer `useEffect` keyed on `[symbol, tab, tradingMode, compositeResult]`. The mode-toggle `updateTradingMode` callback no longer needs to imperatively `setCompositeResult(null)` / `setRadarData(null)` — the cache key change does that for us via `keepPreviousData:false`. The after-hours news `fetchSymbolNews` Promise.all path was split: news goes through `useSymbolNews({ enabled: showAfterHoursPanel })`, watchlist membership keeps its own thin `useEffect`. (ii) `components/dashboard-edge-sync.tsx` — replaced the local `setInterval(refresh, 60_000)` polling loop + `useState<DashboardResponse>` + `useCallback refresh` with a single `useDashboardPayload(mode)` call. The SSE hint handler (`useLiveSignals`) now calls `mutate(dashboardPayloadKey(mode))` via `useSWRConfig` instead of running its own refresh closure. Local-mode-state + `storage` event listener stay — SWR is read-only as far as the user's mode pill is concerned. **Note on `/api/signals/live`**: the SSE stream stays as `EventSource` (it's a push channel, not a GET-and-cache) — `useLiveSignals` is unchanged. The "convert `/api/live` to SWR" line in the original D11 backlog row referred to the polling re-fetch that the SSE hint triggers, NOT the SSE stream itself. That part now flows through `mutate(dashboardPayloadKey(mode))`. (iii) `components/dashboard-redesign.tsx` — dropped the `[macroPulse, setMacroPulse] useState` + the one-shot `useEffect → fetchMacroContext()` cleanup-flagged Promise. `macroPulse` now reads from `useMacroContext()`. **Test posture (per user instruction — full suite is run end of day)**: 4 new test files added — `tests/use-signal-composite.test.tsx` (7 tests pinning empty/whitespace short-circuit, `enabled:false`, swing/day endpoint routing, dedupe of same-key hooks, non-2xx → null), `tests/use-symbol-news.test.tsx` (6 tests pinning short-circuit, `enabled:false`, key shape, dedupe, mode-key isolation, error collapse), `tests/use-dashboard-payload.test.tsx` (5 tests pinning happy path, dedupe, mode-key isolation, `dashboardPayloadKey` stability, error path), `tests/use-macro-context.test.tsx` (5 tests pinning happy path, dedupe, null back-compat, error path, `macroContextKey` stability). **23 new tests across 4 files; all 23 pass.** Existing lock-ins that interact with the refactored surface — `tests/signals-page-mode-clears-screen.test.tsx`, `tests/signals-page-streaming.test.tsx`, `tests/dashboard-cache.test.ts`, `tests/dashboard-hover-prefetch.test.tsx`, `tests/dashboard-prefetch.test.tsx`, `tests/use-symbol-snapshot.test.tsx` — all 37 tests across 6 files pass. `signals-page-mode-clears-screen.test.tsx` got one targeted test-infra update: the test now wraps each render in a fresh `<SWRConfig provider={() => new Map()} dedupingInterval={0}>` so SWR cache state does NOT leak between tests. The "clear screen between modes" UX rule applies to UNCACHED flips; a cached flip is instant (a strictly better UX) and the test would otherwise oscillate based on prior-test side effects. **Why this matters operationally**: the most-clicked surface (mode-toggle → composite refetch → 1–2s back-end latency) now hits the SWR cache after the first round trip. The dashboard polling loop is no longer redundant with whatever hits `useDashboardPayload(mode)` elsewhere on the dashboard (currently only the edge-sync observability badge, but the surface is now shaped for future islands without re-introducing parallel polling). Future PRs that add a fifth `useEffect → fetch` block on these surfaces should prefer a new SWR hook over an imperative effect — the patterns are now in-tree. The full frontend suite will be run at end of day per the user's "do not run all tests now" instruction. |

1. **Infrastructure:** Same as checklist rows 1–2; keep API Gateway URLs and Cognito/Vercel env aligned with the deployed stage. **News pipeline note:** queue + consumer Lambda can be applied now; the optional ECS news worker still requires `news_worker_container_image` to be set before Terraform will create the task/service.
2. **Broker/runtime services:** IBKR path needs ECS/Fargate + TWS/ibeam where applicable; the new always-on news worker also needs an image publish/push path plus Redis reachability from the app security group.
3. **CI hardening (if not done):** setuptools upgrade in CI before `pip install -e .`; optional skip for Vercel deploy when hook secret missing. Add worker image build/push automation if you want the ECS runtime deployed from CI.
4. **Legal / launch:** Terms at `/terms` are a **draft** — attorney review before paid subscribers; **mandatory legal acknowledgment (B2)** is implemented before dashboard use; optional onboarding wizard remains dismissible per session until completed.

---

## 4. Legal compliance (product & API)

- STOCVEST is **not** a registered investment adviser; frame as **signal intelligence** and data tooling.
- **UI:** Prefer signal summary, signal strength, reference levels, signal parameters—not “verdict,” “recommendation,” “you should,” or “confidence” in **signal** context.
- **`SignalDisclaimerChip`** on signal cards; **`GlobalDisclaimer`** last in `<body>` in root layout.
- **Performance page:** Directional accuracy only; not dollar P&L as “signal performance.”
- **Serialized API:** Use `signal_summary` / `signal_strength` (and related names) where applicable; include `disclaimer`: *Signal data for informational purposes only. Not investment advice.*
- **Logging / persistence:** Sanitize user-supplied free text before Dynamo (`text_sanitize`); keep CloudWatch logs free of unnecessary PII (`log_privacy`). Server-side identity for journal and protected routes comes from the **JWT**, not from client-supplied `user_id` body fields.

---

## 5. Roadmap & backlog

**Full prioritized themes, sub-tasks, and IDs:** [`docs/BACKLOG.md`](./BACKLOG.md) — **single source** for planned work so this file stays short.

**Directional summary:** After infra/broker production readiness (`§3`), focus shifts to **subscriptions**, **sector/internals**, **D2 backtesting / analytics**, **Phase 7 hardening**, and the post-beta **trade brief** UI (`BACKLOG.md` sections B, D, P, G, M, T). **D1** signal outcomes, **B1** journal automation, **B2** onboarding + legal acknowledgment, **B3** alerts (email), and **B4** watchlists are shipped (see status table). **B13** (SMS) remains pending until phone collection is ready.

---

## 6. Key decisions (unchanged summary)

1. Stack: Next.js (Vercel) + Python Lambdas (AWS).  
2. Brokers: IBKR (TWS / ib_insync) + E*TRADE (OAuth REST).  
3. Market data: Polygon.io (stocks advanced + options + crypto tiers as configured).  
4. Multi-tenant isolation end-to-end.  
5. Asset scope: stocks, ETFs, options, futures, crypto (scope varies by data source).  
6. Six signal layers: Technical, News, Macro, Sector, Geopolitical, Internals.  
7. AI: Claude (Sonnet) → structured JSON for synthesis; **public APIs use compliance field names** (`signal_summary`, `signal_strength`, …).  
8. Domain: **stocvest.ai** ( **`stocvest.app`** redirects; alert email from **`signals@stocvest.ai`** ).  
9. Day trading: Phase 2.5 scanner + PDT tracker are first-class.  
10. News ingestion is now multi-source: Benzinga websocket for live news plus SEC EDGAR 8-K polling, with Claude scoring kept in the SQS-triggered Lambda consumer.  
11. **Composite signal architecture:** **Stage A** = per-layer analyzers (each speaks from its own domain); **Stage B** = `CompositeScoreEngine` and handlers reconcile after the fact. Regime multipliers **scale influence**, not the **sign** of a layer’s directional input (default multipliers stay positive). **Eligibility gates** (e.g. `insufficient_data`) vs **degradation** (e.g. contradiction penalty, R/R warnings) are separate ideas—see **`docs/SIGNAL_ENGINE.md`** (§ Architecture, § Composite, § Future enhancements).

---

## 7. Immutable contracts

Do **not** change without discussion and coordinated updates:

- **`BrokerAdapter`** — eight methods, types, exceptions (`docs/API_CONTRACTS.md` §1).  
- **HTTP paths** — versioned under `/v1/`; breaking changes need `/v2/` or migration.  
- **DynamoDB table names** — `Users`, `BrokerConnections`, `Watchlists`, `Alerts`, `Orders`, **`SignalHistory`**, **`TradeJournal`**, **`PDTState`**, **`AuditEvents`** (HTTP audit items; `pk`/`sk` per `audit_store.py`), etc.  
- **Secrets path** — `/stocvest/{userId}/brokers/{brokerId}`.  
- **Canonical types** — `stocvest/data/models.py` (no raw Polygon dicts in core logic).  
- **Snapshot + intraday + VIX invariants** (verified; do not re‑discover in prompts):
  1. **`Snapshot` model** — Pydantic `Snapshot` in `stocvest/data/models.py` (not a separate parser class). Raw Polygon ticker dicts map only through `PolygonClient._parse_snapshot()` → `Snapshot`. **Do not** add a `SnapshotParser` class, **do not** read raw Polygon keys in layer/analyzer code, **do** use `Snapshot` field names everywhere: `symbol`, `last_trade_price`, `last_trade_size`, `last_quote_bid`, `last_quote_ask`, `day_open`, `day_high`, `day_low`, `day_close`, `day_volume`, `day_vwap`, `prev_close`, `change`, `change_percent`, `pre_market_price`, `pre_market_change_percent`, `after_hours_price`, `after_hours_change_percent`, `market_status`, `company_name`, `prev_day_volume`. Layer analyzers take **`Snapshot`** instances (or derived inputs), not raw dicts.
  2. **`IntradaySetupScanner`** — Does **not** fetch bars. Signature: `scan(bars_by_symbol: dict[str, list[Bar]], …)`. Handlers fetch bars then pass them in. To reuse intraday math inside something like a technical layer, import **`IntradayVWAPCalculator`**, **`IntradayEMA9Calculator`**, **`OpeningRangeBreakoutDetector`** (and related helpers in `day_trading_scanner.py`) — **not** the scanner class as a nested “analyzer.”
  3. **VIX** — Reuse `get_vix_snapshot_with_fallback` and tuple `VIX_SNAPSHOT_FALLBACK_SYMBOLS` from `stocvest/api/services/morning_brief_fetch.py` (`I:VIX` → `^VIX` → `VIX`). **Do not** add another VIX fetcher or a single hardcoded/configured VIX ticker as the primary approach; the **ordered fallback list** is the contract.
  4. **Day vs last trade scale** — `_parse_snapshot` already drops inconsistent `day` OHLC/VWAP when out of line with `lastTrade.p` (5× bound via `_session_day_prices_align_with_last`). Use **`snapshot.last_trade_price`** for current/near price; **do not** duplicate that sanity check downstream.
  5. **Extended hours** — `preMarket`/`premarket` and `afterHours`/`afterhours` key variants are normalized inside `_parse_snapshot` into `pre_market_*` / `after_hours_*` on **`Snapshot`**; use those fields, not raw dict access.

Signal **layer enums** and internal composite types are stable; **customer-facing** naming follows the Legal section above.

---

## 8. Known issues / technical debt

- `pyproject.toml` / setuptools: may need `pip install setuptools --upgrade` before editable install on some machines.  
- PDT rolling window is weekday-based; exchange holidays not modeled—align with broker if needed.  
- Stochastic implementation has a small style debt (not correctness).  
- **Swing ledger qualified = 0 (telemetry soak, post-P66):** Swing capture runs (**118** shadow rows on 2026-06-09) but **0** `ledger_qualified` so far — sector/macro/R:R gates are strict. Do **not** lower swing R/R from **2.0** until **B62-Phase3** shadow-row review (~2–3 weeks post-fix). Track via **`scripts/ledger_signal_report.py --period daily`**.
- **Small P66 follow-ups (P2):** **DONE 2026-06-22** — **sector benchmark-ETF cache coverage** (warm all `DEFAULT_SECTOR_TO_ETF` benchmarks + SOXX; drift-guard test); SOXX-vs-SMH confirmed **not a bug** (laggard `PriceCache` vs composite `sector_daily_cache` are separate by design). **Still open (deliberately deferred):** `is_composite_layer_signal_scale()` heuristic is conservative — refining the [-1,1]-vs-0–100 disambiguation risks the sector gate, no clear accuracy win; **`sector_layer_score` on day shadow rows** is telemetry-only (`evaluate_day_ledger_entry` has **no sector gate by design** — day gates on intraday structure, not daily sector RS; adding one is a contract change); maturation `signal_summary` fallback is **already partially handled** (`watchlist_maturation_sync` derives decision from `signal_summary` when `decision_state` is absent) and affects watchlist display state, not signal accuracy.
- **Deferred P1 (unchanged):** post-window taper, dimmed Evidence layers, `MarketContextWindow` class, mechanical flow classifier, live index calendar — see **`BACKLOG.md`**.
- **SignalHistory retirement blocked:** B62 shadow rows increased write volume — **B46-Phase6** must archive ledger telemetry first (**`SIGNAL_HISTORY_RETIREMENT.md`**).
- **Mitigated (local pytest, Windows + Python 3.14):** botocore’s user-agent path could trigger WMI via `platform.uname()` from threads and print “access violation” to stderr; **`tests/conftest.py`** primes `platform.uname()` on import and clears **`DYNAMODB_USERS_TABLE`** in the autouse fixture so handler imports do not eagerly create Dynamo clients.

---

## 9. Open questions

- **AWS account model:** dedicated org account vs existing (confirm for production).  
- **Crypto on-chain metrics:** deferred.  
- **E*TRADE / IBKR production:** credentials, redirect URLs, and ECS capacity for TWS.

---

## 10. Development environment

```bash
python -m venv .venv
pip install -e ".[dev]"
cp .env.example .env   # fill Polygon, Claude, etc.
pytest tests/ -q
```

Frontend: `cd frontend && npm ci && npm run build && npm run test`

---

## 11. Cost picture (approximate / year)

| Item | ~USD/yr | Purpose |
|------|---------|---------|
| Polygon (stocks + options + crypto tiers) | ~2.7k | Market data |
| IBKR | $0 (account) | Execution, futures, L2 |
| Claude | ~200–600 | Synthesis |
| Vercel + AWS + domain | varies | Hosting + compute |

---

## 12. Day-trading scan rhythm (reference)

- **Pre-market:** gap + catalyst scan → ranked candidates.  
- **RTH:** intraday setup scanner (~5 min cadence per design), VWAP/OR/EMA logic in engine.  
- **PDT:** warn at 2 day trades, block at 3 (non-exempt) in rolling window.

---

## 13. Session rules

### STEP 0 — Read and verify (no guessing)

Do this **before** designing or naming APIs against external data. **§7 “Snapshot + intraday + VIX invariants”** is the canonical checklist (field names, `IntradaySetupScanner` boundaries, VIX helper, no duplicate day/last checks, extended hours on `Snapshot`).

1. **Open the real implementation** — e.g. `polygon_client.py`, `models.py` (`Snapshot`), `day_trading_scanner.py` (`IntradaySetupScanner` + calculators), `morning_brief_fetch.py` (`get_vix_snapshot_with_fallback`).
2. **Polygon JSON keys** — Only keys read in `_parse_snapshot` (and other explicit parsers in `polygon_client.py`) are authoritative. New surface area → extend `_parse_snapshot` + tests.
3. **Decisions in code reviews** — Prefer commit messages or PR notes with **why** and **rejected alternatives** where non-obvious.
4. **Tests assert design, not trivia** — e.g. VIX fallback order, dropped `day` when scale mismatches, scanner RVOL gates.
5. **Commits** — When several separable decisions land in one session, **order commits** for independent `git revert`. One cohesive change may still use a single commit (§13 STEP 4).

### Before writing any code

1. Read `docs/CONTEXT.md` and `docs/BACKLOG.md` in full.
2. Confirm exact test counts:
   - Run: `pytest tests/ -q`
   - Run: `cd frontend && npm run test`
   - Record actual numbers — do not use “~” approximations.
   - If counts are **lower** than the baseline in **§13 Test baseline** (and the summary line under the document title), stop and report the regression before doing anything else.
3. Do not implement anything already marked ✅ in §1.
4. Do not implement anything not in `BACKLOG.md` without flagging it to the user first.
5. Do not change any contract in `docs/API_CONTRACTS.md` without explicit instruction.

### Every prompt must end with ALL of these steps

 **STEP 1 — Tests**  
Run `pytest tests/ -q`  
Run `cd frontend && npm run test` (Vitest; do not pass Jest’s `--watchAll=false`)  
Run `cd frontend && npm run build`  
Report exact counts. If any count dropped, fix before proceeding to documentation updates.

**STEP 2 — Update `docs/CONTEXT.md`**  
- §1 Status table: mark completed tracks ✅; update Notes if anything changed.  
- §2 Implemented: add new files/modules built this session.  
- §3 Pending: remove items that are now resolved; add new blockers discovered this session.  
- §13 Test baseline: update exact backend and frontend test counts (and note if `npm run build` was verified).  
- Document title summary line: keep test baseline in sync with §13.  
- **Last updated** date: set to today.

**STEP 3 — Update `docs/BACKLOG.md`**  
- Items completed this session: change status to **DONE**; add: `Done YYYY-MM-DD · commit: <hash>`.  
- Items discovered this session: add with new ID, description, and acceptance criteria.  
- Items in progress but not finished: change status to **In progress**.  
- **Do not delete any row — ever.**  
- **Last updated** date.

**STEP 4 — Commit**  
- Stage: all code + `docs/CONTEXT.md` + `docs/BACKLOG.md` together.  
- Message format (required):  
  `"<feature> | backend: <N> tests | frontend: <N> tests"`  
  where **backend N** = pytest **passed** count and **frontend N** = Vitest **passed** count (from the respective summaries).  
  Example:  
  `"B2 onboarding wizard | backend: 468 tests | frontend: 50 tests"`  
- Never commit code without committing updated docs.  
- Never commit docs without committing the code they describe.

### What goes where — hard rules

| Content type | File | Rule |
|--------------|------|------|
| What is built and working | CONTEXT.md §1–2 | Reality only |
| Near-term blockers & ops | CONTEXT.md §3 | Keep current; not duplicated in BACKLOG |
| Planned work & acceptance notes | BACKLOG.md | Full detail; **never delete rows** (mark DONE + date + commit) |
| HTTP & broker contracts | API_CONTRACTS.md | Change only with explicit instruction |
| Legal framing (copy & public API) | CONTEXT.md §4 | Align product language and payloads |

**A1 note (2026-06-02):** Backend and frontend test counts are unchanged by A1 — the changes are backend service wiring and frontend UI only. Follow-up test files to add before the next feature: `tests/utils/test_symbol_detector.py` (detect_symbol / detect_symbol_from_messages), `tests/api/services/test_assistant_symbol_context.py` (fetch timeout, partial data), updated `tests/api/handlers/test_signals_assistant_chat.py` (image validation, symbol detection wiring), `frontend/tests/use-voice-input.test.ts` (hook states), `frontend/tests/assistant-panel-redesign.test.tsx` (image preview, mic button, placeholder text).

### Test baseline (exact counts — update after every verified run)

| Suite | Command | Last verified |
|-------|---------|---------------|
| Backend | `pytest tests/ -q` | **2616 passed**, **4 skipped** (full suite verified **2026-06-29** after **B78 — Target geometry v3** — volatility-normalized structure-derived T1/T2: bounded T1 in an ATR band else `entry+α·ATR`; T2 = `min(next structural resistance, entry+β·ATR, entry+2R)`; analyst price targets excluded from the gated T2 (new `analyst_target` provenance, informational only); clustered/capped resistance (adaptive ε, top-5, SMA20/50/200 enrich); ATR-normalized `reference_*_distance_atr`. New `tests/api/services/test_target_geometry.py` (13) + 4 v3-wiring cases in `tests/api/test_swing_composite_evidence.py`; flag `STOCVEST_TARGET_GEOMETRY_V3_ENABLED` default ON, Terraform wired (signals Lambda); requires ATR (falls back to v2 when missing). Frontend `target-provenance.ts` mirror (+1 case in `frontend/tests/target-provenance.test.ts` for `atr_extension`/`analyst_target`). Prior **2599 passed**, **4 skipped** (full suite verified **2026-06-28** after **A7 — Assistant breadth-with-restraint** — assistant web-search fallback (Perplexity, flag-gated, citation-backed) + multi-symbol comparison (parallel side-by-side, never picks a winner) + STOCVEST-read `not_yet_confirmed` uncertainty caveats + plan-aware watchlist-add cap (100); +38 backend (new `tests/api/services/test_assistant_web_context.py`, `test_assistant_multi_symbol_context.py`, `test_assistant_watchlist_action.py` + additions to symbol/intent detectors, symbol-context, broader-coverage, and the assistant-chat handler). Web search ships dark (env var applied; code goes live on merge → CI deploy-lambda). Prior **2520 passed**, **4 skipped** (full suite verified **2026-06-25** after **Alert-email hardening + tradeable-only prefs** — execution-actionable emails render Stop loss / Take profit / Entry zone; send records FAILED unless HTTP 200 *and* ErrorCode 0; emits `Stocvest/Alerts EmailSendOutcome` + CloudWatch failure alarm/SNS; all users set to execution-actionable-only. +2 (`tests/services/test_email_service.py`, `tests/services/test_postmark_client.py`); code needs Lambda deploy. Prior **2518 passed**, **4 skipped** (full suite verified **2026-06-25** after **Day ledger profit-target exit (ships dark)** — flag-gated `day_profit_target` take-profit in the day monitor (default OFF), +2 in `tests/api/services/test_ledger_position_monitor.py`. Prior **2516 passed**, **4 skipped** (full suite verified **2026-06-25** after **Signal outcome resolver throughput fix** — resolver drains newest-first with a 2500/pass cap + concurrency 8 in incremental chunks so 1d outcomes stop starving behind the 1h backlog; +2 in `tests/api/test_signal_recorder.py`; code-only, needs Lambda deploy. Prior **2514 passed**, **4 skipped** (full suite verified **2026-06-23** after **Swing technical display fix — lagging golden cross** — below-both-MAs breakdowns no longer mislabel as "uptrend"/"Golden Cross"; chip becomes "Golden Cross (lagging)" and brief says "broken structure". Verified score-path clean: golden-cross credit is gated on `durable_uptrend` (price above both MAs) so it never fires for a broken-down name (SPCH raw −42 → 0, invariant to `golden_cross_score`); display-only, +3 in `tests/signals/test_swing_technical_analyzer.py`. Prior **2511 passed**, **4 skipped** (full suite verified **2026-06-23** after **B74 News relevance × impact × age weighting (ships dark)** — flag-gated `STOCVEST_NEWS_IMPACT_WEIGHTING_ENABLED` (OFF → byte-identical legacy News score); `news_impact.py` relevance×impact (Claude read-through cache else heuristic) + confidence shrink in `news_analyzer.py`; Claude scorer/cache emit+store `relevance`/`impact`; +20 tests; no Terraform change. Prior **2491 passed**, **4 skipped** (full suite verified **2026-06-22** after **P66-followup sector benchmark-ETF cache coverage** — added biotech/pharma/medical-devices/retail/airlines/transport/defense/mining benchmarks (XBI/XPH/IHI/XRT/JETS/XTN/ITA/XME) + SOXX to `SECTOR_ETFS_TO_TRACK` so they get persistence momentum instead of single-day fallback; +3 invariant tests in `tests/workers/test_sector_daily_cache.py`. No Terraform / API change. Prior **2488 passed**, **4 skipped** (full suite verified **2026-06-22** after **B73 Mean-reversion bounce floor (swing)** — validation-first (`scripts/validate_mean_reversion_credit.py`, no-lookahead sweep: RSI depth monotonically tracks forward bounce; fresh-low "knives" underperform), then `swing_technical_analyzer.py` lifts a confirmed-downtrend (price < SMA50 & SMA200, MACD < signal), deeply-oversold (RSI < 30), **stabilizing** (not a fresh 10-day low) name off the 0 floor via a **non-additive `max(trend, floor)`** sized by oversold depth, **hard-capped at 38 < bearish_threshold** so it adds resolution but never flips the bearish verdict (swing-only; new `mean_reversion_*` + `atr_period` params; "Oversold bounce watch" chip + `mean_reversion_floor_applied` flag); +5 in `tests/signals/test_swing_technical_analyzer.py` (floor lifts stabilizing oversold downtrend, knife filter holds, RSI gate holds, ceiling caps below threshold, non-additive; now 33). No Terraform / API schema change. Prior **2483 passed**, **4 skipped** (full suite verified **2026-06-22** after **B72 Technical accuracy: swing recalibration + sector calibration** — (a) **swing technical recalibration** (`swing_technical_analyzer.py`): magnitude-scaled below-SMA20 penalty (shallow dip ≪ deep break, new `below_sma20_min_penalty` / `below_sma20_full_break_pct`), durable-uptrend structural credit (`golden_cross_score` when price > SMA50 & SMA200 with golden cross), pullback-vs-breakdown softening (%-from-high caps at the moderate tier inside a durable uptrend), MACD de-overlap (fading penalty only while histogram ≥ 0 so a negative histogram isn't double-counted), and a lighter `volume_distribution_penalty` (split from `volume_accumulation_score`) — fixes the reported **NVDA Technicals = 5/100** (now ~41, neutral; verified live: NVDA above SMA50/200 + golden cross + RSI 50 was being floored by stacked pullback penalties); +3 in `tests/signals/test_swing_technical_analyzer.py` (durable-uptrend credit wired/fires, below-SMA20 scales with distance, negative MACD not double-penalized; now 28). (b) **Sector-relative technical calibration** (new `stocvest/signals/sector_technical_calibration.py`, reuses `normalize_sector_for_geo`): coarse `HIGH_BETA / NORMAL / DEFENSIVE` regime → sector-scaled **RVOL surge threshold** (day analyzer: high-beta needs a bigger spike, defensives surge on less) + **overbought-penalty persistence** (high-beta overbought penalized lighter, defensives heavier) — same buckets, sector-scaled inputs (fixed structure, mirrors B71). Plumbed `sic_bucket` through `TechnicalAnalyzer.analyze`, `SwingTechnicalAnalyzer.analyze`, `resolve_day_technical_layer`, and both composite engines; neutral (1.0) for unknown sectors / callers that don't pass one (existing behavior unchanged). New `tests/signals/test_sector_technical_calibration.py` (17): regime resolution, multiplier direction, payload shape, day RVOL threshold sector-relative (asserts `volume_surge` flag), day/swing neutral-default-unchanged, swing overbought sector-relative. Display-only helper `sector_technical_calibration_payload` ready to surface (not yet wired to UI). No Terraform / API schema change (composite response *values* shift for recognized sectors). Prior **2463 passed**, **4 skipped** (full suite verified **2026-06-21** after **B71 News/Geo sensitivity UI** — `news_geo_sensitivity` added to both composite responses (`layer_sensitivity_payload`) + surfaced on the deep-dive News/Geo rows; new `layer_sensitivity_payload` test in `tests/signals/test_news_sensitivity.py` (now 11) + frontend `composite-layer-rows.test.ts` (+2). Prior **2462 passed**, **4 skipped** after **B71 Phase C scheduled report** — weekly `news_event_study_report` Lambda module scans `SignalHistory` and writes a dated JSON report to the new reports S3 bucket; new `tests/workers/test_news_event_study_report_worker.py` (4): no-op when disabled / no bucket, writes report to S3 with horizons + per-symbol payload, returns 200 on failure. Prior **2458 passed**, **4 skipped** (after **B71 Phase C scaffold** — read-only offline news event-study report (`stocvest/signals/news_event_study.py` + `scripts/news_event_study_report.py`); new `tests/signals/test_news_event_study.py` (11): row extraction (news flag, forward return, ATR%, sentiment event-mean fallback, malformed-json safe), sensitivity ratio, sentiment predictiveness (sign agreement, flat-move/flat-sentiment exclusion), min-samples filtering, no-baseline ratio None. Also **cache EMF metrics** (+3 in `test_news_sentiment_cache.py`, now 21): EMF structure/namespace, no-op when empty, enrich emits CacheHits/CacheCandidates. Prior **2444 passed**, **4 skipped** (after **B71 Phase D self-prime** — composite enqueues abstaining cache-miss articles to the triage queue for async scoring; +7 in `tests/signals/test_news_sentiment_cache.py` (now 18): prime gated off when prime/cache flag off or no queue URL, enqueues misses + sets pending markers, dedupes already-pending, fail-open when Redis down, releases markers on SQS failure). Prior **2437 passed**, **4 skipped** (after **B71 Phase D** read-through Claude sentiment cache — `tests/signals/test_news_sentiment_cache.py` (11): content-key stability (URL-preferred, scheme/query/case-insensitive, title fallback), write normalization (bullish→positive) + TTL + invalid-label reject, read-through enrich on hit, skip rows with insights/existing label, and fail-open when disabled / Redis down / cache miss). Prior **2426 passed**, **4 skipped** (full suite verified **2026-06-21** after **B71 Phase B** article-level news-event capture — new `tests/api/services/test_signal_snapshot_news_events.py` (4): events captured with timestamp/sentiment into `NewsSnapshot.top_events`, empty-safe, bounded+truncated, malformed-row skip). Prior **2422 passed**, **4 skipped** (full suite verified **2026-06-21** after **B71 Phase A** per-symbol News/Geo sensitivity — new `tests/signals/test_news_sensitivity.py` (10): sector→band mapping, ADR geo override, unknown-sector neutrality, guardrail clamp, and engine integration (down-only renormalization, default==no-multiplier)). Prior **2412 passed**, **4 skipped** (full suite verified **2026-06-20** after **B70** AI setup read — new `tests/signals/test_ai_explanations.py` setup_read cases + `tests/api/handlers/test_signals_ai_setup_read.py`). Prior **2406 passed**, **4 skipped** (full suite verified **2026-06-20** after **B58-fix** causal-narrative directional-mixed copy — new `tests/signals/test_causal_narrative.py::test_neutral_setup_directional_layer_describes_read_not_no_edge`). Prior **2405 passed**, **4 skipped** (full suite verified **2026-06-20** after **P68** Signal Math Contract + financial-math fixes — new `tests/signals/test_signal_math_contract.py` + updated `test_intraday_setup_scanner.py` / `test_confluence.py` / `test_multi_timeframe.py`; the jump from the prior 2274 baseline also folds in the trade-plan / thesis-alert backend tests landed since 2026-06-10). Prior **2274 passed**, **5 skipped** (full suite verified **2026-06-10** after **P66** + ledger report tests). Prior **2213**/5 (2026-06-06, **B64** entry-zone synthesis — new `tests/api/services/test_entry_zone.py` (10) + entry_zone parsing/override tests in `test_parameter_store.py` + updated swing-handler entry-zone assertions in `test_signals.py`). Prior **2071**/5 (2026-06-02, A2/A3/A4 + A1-tests). Prior long-form note follows. **1760 passed**, **3 skipped** (full suite verified **2026-05-18** after **L48 Laggard Intelligence Engine**). Prior **1615** (B47). Prior long-form **B33** note: **1545 passed**, **3 skipped** (full suite verified **2026-05-16** after **B33** phase 2 — store + `GET /v1/signals/scanner-trace`; prior **1541 passed**, **3 skipped** after B33 phase 1) — +3 in `tests/api/test_scanner_setups_bundle.py`; prior **1461 passed**, **3 skipped** verified **2026-05-14** after **Gap Intelligence** — +27 over 1434; prior focused-run note — **1434 passed**, **3 skipped** (focused-run verified 2026-05-13 after the **BRK.B Evidence Card — catastrophic stale-bars root cause + 4 user-reported issues** shipment — **+7** split between `tests/data/test_polygon_client.py::TestGetBars` (3 new wire-level recency tests pinning `sort=desc` reverse on recent-mode + `sort=asc` preservation on explicit-range + partial-edge conservative range mode) and `tests/signals/test_confluence.py` (7 new internals-chip tests under the "Internals-alignment chip invariants" block, **including** a direct BRK.B regression `test_internals_bullish_on_short_setup_lands_in_conflicting_BRK_B_regression`). Frontend `tests/signal-evidence.test.ts` reconciler `describe` block fully rewritten — 9 tests covering the new anchor-first shape on both directions, singular vs plural counterweight label, the direction-aware no-confirming-chips fallback, neutral verdict → null, parenthetical stripping, geopolitical-drag splicing and de-dupe, and the BRK.B-specific regression with `Internals bullish` in the counterweights list. Plus the `SignalEvidenceCard verdict-tag reconciler rendering` test updated to seed the BRK.B chip layout the engines now actually emit and assert the new sentence renders with three anti-regression `not.toMatch` guards on every prior phrasing. Full suite re-verification still pending per the user's "do not run all tests now" instruction — will run end of day. PRIOR baseline note follows below. **1410 passed**, **3 skipped** (focused-run verified 2026-05-13 after the **Class-share dash-form symbol normalization (BRK-B → BRK.B) at every Polygon boundary** shipment — **+23** split between `tests/data/test_symbol_normalize.py` (20 unit tests parametrised across class-share rewrite cases, leave-alone cases, hygiene, idempotency, empty input) and `tests/api/test_class_share_symbol_normalization.py` (3 engine-level regressions — swing canonicalises at entry, day-trade canonicalises at entry, normal symbols unchanged — built on a custom `_RecordingPoly` fake that records the symbol form passed to every Polygon method). Full-suite re-verification still pending per the user's "do not run all tests now" instruction — will run end of day. Prior baseline note follows below. **1387 passed**, **3 skipped** (verified 2026-05-13 after the **BRK-B evidence card — reconciler text + sector-chip relative-strength relabel** shipment — **+5** in `tests/signals/test_confluence.py` under "Sector chip label invariants (BRK-B fix, 2026-05-13)" locking in the new chip labels: `test_sector_chip_bullish_long_confirms_with_leads_market_label`, `test_sector_chip_bearish_short_confirms_with_lags_market_label` (the BRK-B regression — explicitly asserts "Sector Bearish" is NOT in the label), `test_sector_chip_bullish_short_conflicts_keeping_leads_market_label` (the symmetry invariant — label describes the sector, alignment lives in the column), `test_sector_chip_bearish_long_conflicts_keeping_lags_market_label` (symmetric), `test_sector_chip_absent_on_neutral_signal`. Prior baseline note follows below. **1382 passed**, **3 skipped** (verified 2026-05-12 after the **Admin hub — consolidated maintenance UI (D10 Phase 5)** shipment — **+56** over the prior 1326/3 baseline, covering the 8 new admin endpoints (users search/detail/reset-password/group add+remove, audit recent, parameters current, system-status aggregator), the Cognito-backed `admin_user_directory` composition layer, the `is_admin` entitlement OR-bump in `_serialize_user_profile`, and dispatch wiring across `signals_http_dispatch` + `lambda_dispatch.brokers`. Prior baseline: **1326 passed**, **3 skipped** (verified 2026-05-12 after the **Automated weight-tuning proposal pipeline — post-rotation accuracy alarm + admin rollback (D10 Phase 4)** shipment — **+87** over the prior 1239/3 baseline. All **87** new tests live across five new files. **23 in `tests/api/services/test_post_rotation_monitor.py`** locking in the pure-function core of the daily monitor Lambda: `accuracy_pct_from_bucket` (NaN for empty buckets, NaN for all-neutral buckets — never silently 0%, 0–100 scale lock-in, correct/(correct+incorrect) denominator matches D2 `/performance`), `detect_degradation` (≤ -threshold flags as degraded, signed delta carries direction, NaN on either side propagates to `is_degraded=False`, negative-threshold input treated as absolute defensively), `evaluate_post_rotation_accuracy` end-to-end (returns `baseline_unavailable` when no previous version, `insufficient_sample` when either side below MIN_RESOLVED_SAMPLE=30, `ok` when delta within threshold, `degraded` when drop ≥ threshold; honours the immediate-prior-window baseline shape AND falls back to a wider 2*window search when the prior window is empty so legitimate rollback-of-old-rotations still produces a baseline; 1d horizon is the default and matches D2 `/performance`; threshold lock-in at exactly 5pp = degraded), default constants (`DEFAULT_DEGRADATION_THRESHOLD_PP=5.0`, `DEFAULT_WINDOW_DAYS=14`, `MIN_RESOLVED_SAMPLE=30`, `MONITOR_STATUSES` closed set), `to_dict()` JSON shape. **17 in `tests/api/services/test_parameter_rollback.py`** locking in the orchestrator that is the second production path to mutate the live secret: `history_row_to_summary` projection (live-version flag works correctly when version matches, doesn't flag when no match, defensive on `None` live-version), `to_dict` carries all audit columns; `rollback_to_version` failure modes (empty / whitespace target → `target_version is required`, missing history row → `not found`, malformed `parameters_json` → `invalid history row`, target equals current live version → `already on target version`, `save_parameters_sync` returns False → `parameter save failed` but save was attempted exactly once); `rollback_to_version` happy path (params reconstructed from history JSON and round-trip preserves marker weights, **`changed_by` audit prefix is `d10-rollback:<reviewer>`** distinct from promote's `d10-admin:<reviewer>`, `reason` string mentions both versions + reviewer for grep-ability, `to_dict` payload complete); `list_history_with_live_marker` (marks the live row exactly once, marks nothing live when current isn't in history, returns empty when store returns empty). **20 in `tests/api/handlers/test_admin_parameters.py`** locking in the HTTP shell + dispatch wiring: both handlers gated on `analysis_authorized()` (parameterized 403 test); history handler success path + limit clamping (default 50, query-param override, hard cap 200, integer validation); rollback handler success path forwards reviewer's `user_id` to the orchestrator; rollback handler error mapping (400 on invalid JSON / missing / empty / non-string `target_version`; 404 on `not found`; 409 on `already on target version` with the conflict envelope's explicit error code **never** overwritten by the orchestrator's own `error` field — the spread order is load-bearing; 500 on `parameter save failed` / `invalid history row`); audit-event emission on success AND on conflict (status_code + outcome + request_summary all stamped); `signals_http_dispatch` routes both new endpoints to their handlers BEFORE the flat-route table (parameterized routes must match in dispatch order). **11 in `tests/api/handlers/test_weight_rotation_monitor.py`** locking in the scheduled-Lambda wiring: always returns HTTP 200 even on `RuntimeError` during evaluation OR ParameterStore failure (EventBridge must not retry-storm); previous-version resolution skips the current-live row in `ParameterHistory` and returns the next; returns `None` previous when history has only the current version (first-ever rotation); handles `ParameterHistory` scan failures gracefully with `previous=None`; CloudWatch publishing contract — namespace `Stocvest/Signals`, metric name `PostRotationAccuracyDelta`, `Status` + `Environment` dimensions match what Terraform's alarm filters on, value is the delta_pp (or 0.0 for null delta to keep dashboards continuous); publisher swallows boto3 errors silently; `STOCVEST_LAMBDA_MODULE=weight_rotation_monitor` reaches the right handler in `lambda_dispatch.py`. **16 in `tests/data/test_parameter_history_store_reads.py`** locking in the new reader APIs: `list_parameter_history_versions` (newest-first by `created_at`, limit respected, zero/negative limit returns empty, skips malformed rows defensively, coerces non-string numeric columns to 0/0.0, returns empty on ClientError or unconfigured table, paginates through LastEvaluatedKey); `get_parameter_history_version` (returns row when present, strips whitespace from lookup, returns `None` for missing / empty / invalid version, returns `None` on ClientError); `parameters_dict_from_history_row` (round-trips valid JSON, returns `None` for malformed JSON or non-dict payload). Previous baseline: **+52** over the prior 1187/3 baseline for the **Automated weight-tuning proposal pipeline — admin review surface (D10 Phase 3a)** shipment. All **52** new tests live in two new files. **24 in `tests/api/services/test_proposal_review.py`** locking in the load-bearing service that owns the only production code path that mutates the live `stocvest/signal-parameters` Secrets Manager secret under admin authority: **`ProposalSummaryRow` projection (6 tests)** — swing-only proposal (`has_swing=True`, `has_day=False`, `day_lift=None`), day-only proposal (symmetric), both-modes proposal (both lifts populated and distinct), defensive handling of missing evidence (lift fields collapse to `None`, no exceptions), float-to-int coercion for `val_signal_count` so the API surface always exposes `int`, stable `to_dict()` key set the BFF + UI consume; **`proposal_to_detail_dict` (1 test)** — full proposal projection carries every field including pre-review nulls; **`apply_proposal_to_parameters` surgical override application (5 tests)** — swing-only proposal preserves the existing `day_composite` (and vice versa), both-modes proposal sets both per-mode overrides, the shared `composite` fallback is **NEVER** mutated (the back-compat anchor — read paths that don't know about per-mode blocks keep working), and apply does NOT mutate the input `SignalParameters` (returns a NEW instance); **`promote_proposal` orchestration end-to-end (9 tests)** — happy path (one secret-save call, one mark_promoted call, new version returned, audit-breadcrumb reason string includes proposal_id + admin sub + which modes rotated), proposal-not-found → `success=False, error="not found"`, already-promoted → `success=False, error="not pending: promoted"` (no secret write happens), secret-save failure → proposal stays pending (the save's atomic guarantee), supersedes other pending proposals (sibling pending rows transitioned to `superseded` so the queue is one-promotion-deep by design), no-siblings case has empty `superseded_pending_ids`, **mark_promoted failure AFTER successful secret write surfaces `new_parameter_version` set so the admin can reconcile** (the load-bearing transactional caveat — secret rotation is the gating step, mark_promoted is best-effort post-flight), single-mode proposal applies only the swing block (preserves a sentinel day_composite at 0.99 technical_weight — locks the per-mode isolation invariant), reason string carries the audit breadcrumb shape; **`reject_proposal` (3 tests)** — happy path delegates to `mark_rejected` with the review_note threaded through, ValueError on non-pending state bubbles (atomic constraint), optional `review_note=None` accepted. **28 in `tests/api/handlers/test_admin_proposals.py`**: **Auth gate (1 parametrized test × 4 handlers)** — every handler returns 403 without admin authorization (parametrized across `admin_proposals_list_handler` / `admin_proposals_get_handler` / `admin_proposals_promote_handler` / `admin_proposals_reject_handler` — closes the four-way attack surface); **List handler (6 tests)** — pending status returns 200 with summary rows, defaults to pending when `?status=` absent, invalid status outside the closed set → 400, non-integer limit → 400, over-large limit clamped to `_MAX_LIST_LIMIT=100` (defends against DOS-style scans), store explosion mid-list → 500; **Get handler (3 tests)** — happy path returns full detail dict, missing proposal_id in table → 404, missing path-param → 400; **Promote handler (6 tests)** — happy path returns 200 with `PromotionResult` body shape, `error="not found"` → 404, `error="not pending: promoted"` → 409 (conflict not bad-input), `error="parameter save failed"` → 500 (server-side not bad-input), missing path-param → 400, **successful promotion emits exactly one `AuditEvent` for chain-of-custody** (admin actions are high-value targets); **Reject handler (5 tests)** — happy path returns 200 + post-rejection detail dict, ValueError from atomic constraint → 409, malformed JSON body → 400, non-string `review_note` → 400, empty body (no `review_note`) is valid → 200; **Dispatch wiring (4 tests)** — `GET /v1/admin/proposals`, `GET /v1/admin/proposals/{proposal_id}`, `POST /v1/admin/proposals/{proposal_id}/promote`, `POST /v1/admin/proposals/{proposal_id}/reject` ALL reach their respective handlers through `signals_http_dispatch` (without these the API Gateway routes would silently fall through to "Unknown route" 404s in production — parameterized routes are checked BEFORE the flat route table). **Production wiring**: 4 new API Gateway routes added in `infra/apigateway_6e.tf` (all under `module_key="signals"`, all `authorizer=true`, admin gating inside the handlers via `analysis_authorized()` — the same gate that protects `PATCH /v1/admin/users/{user_id}/beta-access` and `GET /v1/admin/audit/*`). No new IAM resources needed: the shared `lambda_api_data_access` policy already has `secretsmanager:UpdateSecret`/`PutSecretValue` on `stocvest/signal-parameters*` (live before D10) AND DDB read/write on `parameter_proposal` (added in D10 Phase 1). Verified 2026-05-12 after the **Automated weight-tuning proposal pipeline — scheduled worker (D10 Phase 2b)** shipment — **+26** over the prior 1161/3 baseline. All **26** new tests live in the new `tests/api/services/test_weight_proposer.py` under named sections: **`build_historical_rows_for_mode` projection (9 tests)** — mode filter (swing+day mixed input bucketed correctly), unresolved-row exclusion (missing `price_1d_after` dropped), trailing-window filter (60d cutoff drops 120d-old rows), empty-layer-scores skip (can't re-score without scores), chronological ASC sort (walk_forward_split's invariant), 1h-horizon switch (picks `price_1h_after` when requested), regime-label translation (`risk_on` → `bull`, `risk_off` → `bear`, `neutral`/`None` → `sideways`), unknown-mode loud-reject (`mode="options"` raises), non-positive-trailing-days loud-reject (0 / -1); **`load_baseline_for_mode` resolution (3 tests)** — no-override case (swing and day both lift the shared `composite` block — identical baselines), with-override case (per-mode blocks produce distinct baselines — leakage check on technical weight), thresholds lift from the resolved block (not the shared fallback when an override is present); **`run_weight_proposer` orchestration (10 tests)** — returns `ProposalRunSummary` with exactly one `ModeRunOutcome` per mode in canonical (swing, day) order, records `rows_evaluated` per mode reflecting post-filter counts, skips write + reports error when zero rows for a mode (swing-only dataset → day's outcome has `"no resolved rows"` error), skips write when every candidate ties baseline (uniformly-bullish-and-correct synthetic data → all-modes acceptance=False, no DDB writes), writes proposal when the gate accepts (synthetic news-dominant dataset; the test asserts plumbing — IF accepted, proposal exists with correct version + per-mode block + status), per-mode composite block routing via patched `accept_proposal` (forced-accept on swing run → proposal has `proposed_swing_composite` set + `proposed_day_composite=None`, evidence keyed by mode, all 8 weight/threshold fields present in the block), per-mode error isolation via flaky-optimizer pattern (swing raises `RuntimeError("synthetic swing failure")`, day still completes with an acceptance decision — the orchestrator does NOT swallow the failure and does NOT block the second mode), `run_at` defaults to current UTC when `now` not provided (`before <= summary.run_at <= after` invariant), summary's `to_dict()` shape matches documented keys (`run_at`, `modes`, `proposals_written` plus per-mode `mode`, `rows_evaluated`, optional `accepted`/`reason`/`proposal_id`/`error`), `DEFAULT_TRAILING_DAYS == 60` constant pinned; **Lambda handler shape (4 tests)** — happy path returns 200 with summary body, returns 200 even when `build_default_proposal_store` raises (prevents EventBridge retry storms — failure surfaced in `body.error`), returns 200 on top-level orchestrator exception (same anti-storm property), dispatch wiring sanity check that `lambda_dispatch.py` source contains the `module == "weight_proposer"` branch and its handler import. Verified 2026-05-12 after the **Automated weight-tuning proposal pipeline — algorithmic core (D10 Phase 2a)** shipment — **+44** over the prior 1117/3 baseline. All **44** new tests live in the new `tests/signals/test_weight_optimizer.py` under named sections: `WeightSet` invariants (6 tests — normalize sum-to-1, normalize rescales unscaled inputs preserving ratios, sum-must-be-positive guard, duck-typed `from_composite_block` lift, JSON-friendly `to_composite_block_dict` serialization with threshold pass-through, canonical `WEIGHT_LAYERS` order pin), engine-mirror equivalence (4 tests — `NEUTRAL_MOVE_PCT == 0.1` pinned, `outcome_from_prices` cross-module equivalence vs `stocvest.api.services.signal_recorder` across the full direction-x-move grid, neutral fallback when post-resolution price missing, `regime_for_engine_from_application_label` across the full application vocabulary including fallback for unknown strings/None), `score_weight_candidate` accuracy math (5 tests — unambiguously-bullish row set scores 1.0, anti-bullish set scores 0.0, neutral outcomes excluded from denominator preventing accuracy penalty for "no move" rows, empty layer-scores rows skipped not zero'd, all-neutral row set returns 0.0 = "no information" not "perfect score"), `walk_forward_split` chronological correctness (5 tests — 75/25 default, custom fraction, invalid-fraction rejection at all four boundaries, empty input returns empty tuple no exception, unsorted-input loud rejection defending against random-shuffle leak), `regime_distribution` + `regime_distributions_match` (5 tests — counts per regime key, close distributions pass, wildly divergent distributions fail, empty-side permissive — the caller's job to check signal counts, tolerance-boundary just-over fails / just-under passes), `generate_candidate_weights` constrained grid (6 tests — baseline first as no-rotation candidate, all candidates normalized to sum=1, no non-positive weights survive filtering, deduped by 4-decimal rounded fingerprint, ≤729 upper bound with >50 actual to prove the perturbation loop fires, non-positive delta loud-rejected), `accept_proposal` three-gate enforcement (6 tests — happy-path with human-readable lift reason, too-few-val-signals reject with row count surfaced, insufficient-lift reject, regime-drift reject, negative-lift reject, custom-threshold kwargs sanity covering both tighten + loosen), and `optimize_weights_for_mode` end-to-end on synthetic data (6 tests — uniform-rows scenario returns baseline weights unchanged with equal val accuracies, news-dominant synthetic scenario rotates weight toward news layer with val acc ≥ baseline, all `OptimizationResult` fields populated for Phase-2b serialization, empty input loud-rejected, too-few-rows-for-split rejected via empty-side check, evidence dict shape contains the Phase-1 proposal contract's required keys), and 1 module-level constant pin (`DEFAULT_DELTA == 0.05`, `DEFAULT_TRAIN_FRACTION == 0.75`, `DEFAULT_MIN_VAL_ACCURACY_IMPROVEMENT == 0.02`, `DEFAULT_MIN_VAL_SIGNAL_COUNT == 30`, `DEFAULT_REGIME_TOLERANCE == 0.20` — forces explicit doc/BACKLOG update on default change). Verified 2026-05-12 after the **Automated weight-tuning proposal pipeline — foundation (D10 Phase 1)** shipment — **+24** over the prior 1093/3 baseline. All **24** new tests live in the new `tests/data/test_parameter_proposal_store.py` under named sections: factory invariants (`test_new_pending_requires_at_least_one_per_mode_block`, `test_new_pending_accepts_swing_only_proposal`, `test_new_pending_accepts_day_only_proposal`, `test_new_pending_auto_generates_id_and_timestamp` — the factory's `proposal_id`/`created_at` defaults), round-trip serialization (`test_to_item_round_trip_preserves_every_field` — identity transform on a fully-populated proposal including all review-state fields, `test_to_item_omits_optional_fields_when_unset` — fresh pending rows don't carry review-state attributes, `test_to_item_encodes_nested_dicts_as_json_strings` — sidesteps DDB Decimal coercion, `test_from_item_tolerates_raw_dict_in_nested_fields` — defensive for hand-written DDB rows), CRUD (`test_put_then_get_returns_equivalent_proposal`, `test_get_returns_none_for_missing_id`, `test_put_is_idempotent_for_same_id` — retry-safety for EventBridge retries), GSI semantics (`test_list_by_status_returns_desc_by_created_at` — newest first, `test_list_by_status_filters_by_status`, `test_list_by_status_rejects_invalid_status_loud` — typo raises ValueError not silent-empty, `test_list_by_status_honors_limit`), transitions + atomicity (`test_mark_promoted_transitions_pending_to_promoted` — promoted rows have NO TTL, `test_mark_rejected_transitions_pending_to_rejected_with_ttl` — TTL ≈ +90 days, `test_mark_superseded_transitions_pending_to_superseded_with_pointer` — `review_note` records the displacing proposal id, `test_cannot_promote_already_promoted_proposal`, `test_cannot_reject_already_rejected_proposal`, `test_cannot_promote_rejected_proposal` — **the load-bearing atomicity guarantee**: rejected cannot be silently re-promoted by a racing admin click, `test_cannot_transition_missing_proposal` — non-existent id raises ValueError not silent no-op), closed-set invariants (`test_proposal_status_values_closed_set`, `test_proposal_ttl_days_documented_invariant`). Test approach uses an in-process `_FakeTable` that implements the boto3 Table subset (`put_item` / `get_item` / `query` / `update_item`) with realistic semantics including `ConditionalCheckFailedException` raised on `update_item` when the condition fails — that's the only way the lifecycle atomicity is actually exercised. The `query` implementation asserts `IndexName == GSI_STATUS_INDEX` so a future caller refactor that accidentally queries the base table or a wrong GSI is caught loud. Previous **+14** over the 1079/3 baseline from the **Gap-card mode-fit classifier + per-row Evidence routing (B30 Phase 4)** shipment: — **+14** over the prior 1079/3 baseline. All **13** new tests live in `tests/signals/test_gap_intelligence.py` under a new "B30 Phase 4 — classify_mode_best_fit lock-ins" section: `test_mode_best_fit_returns_closed_set_value` (defensive: every input combo resolves to one of `MODE_BEST_FIT_VALUES`), `test_mode_best_fit_earnings_catalyst_with_large_gap_picks_swing` (3 swing signals fire, day chips DO NOT leak into reasoning), `test_mode_best_fit_merger_catalyst_is_structural_swing`, `test_mode_best_fit_insider_catalyst_is_structural_swing` (all three structural-catalyst families locked in), `test_mode_best_fit_no_catalyst_with_heavy_volume_picks_day` (3 day signals, swing chips DO NOT leak), `test_mode_best_fit_macro_catalyst_with_heavy_volume_picks_day` (tape-level catalyst rule), `test_mode_best_fit_analyst_catalyst_with_heavy_volume_picks_day` (symmetric), `test_mode_best_fit_balanced_signals_returns_either` (1-vs-1 → either, reasons concat both sides for transparency), `test_mode_best_fit_margin_below_two_returns_either` (2-vs-1 still either — the margin-2 rule is the load-bearing invariant that prevents the classifier from over-claiming), `test_mode_best_fit_unknown_catalyst_category_does_not_score_as_structural` (only earnings / merger / insider count as structural — unknown categories fall through), `test_mode_best_fit_quiet_market_micro_gap_returns_either` (0-vs-0 default), `test_mode_best_fit_negative_gap_uses_absolute_magnitude` (gap-down treated symmetrically — direction does NOT affect mode fit), `test_mode_best_fit_huge_gap_no_volume_still_swing` (9% earnings gap with 1.1× vol still swing — magnitude alone doesn't flip to day), `test_mode_best_fit_in_build_gap_intelligence_items` (end-to-end wire-up: the new `mode_best_fit` + `mode_best_fit_reasons` fields are present on the row dict and a 5% gap surfaces a `large gap` chip in the reasoning). Previous **+18** over the prior 1061/3 baseline from the **Per-mode composite weight overrides (B30 Phase 3 — Suggestion 4 audit)** shipment: **+11 in `tests/signals/test_composite_score.py`** under a new "B30 Phase 3 — per-mode composite override blocks (Suggestion 4 audit)" section locking in the new `resolve_composite_block(params, mode)` + the `mode` kwarg on `build_composite_score_engine_from_params`: `test_resolve_composite_block_falls_back_to_shared_when_no_per_mode_block` (no-op default for all three mode values swing / day / None), `test_resolve_composite_block_returns_swing_block_when_set` (only-`swing_composite`-set case with cross-mode isolation), `test_resolve_composite_block_returns_day_block_when_set` (symmetric), `test_resolve_composite_block_handles_both_overrides_independently` (both blocks set + each mode picks its own), `test_resolve_composite_block_ignores_unknown_mode` (case-sensitive defensive fallback for `"options"` / `""` / `"SWING"`), `test_build_engine_swing_and_day_identical_when_no_override` (the **production behavior guarantee** — same six weights + same thresholds across both modes until operators rotate Secrets Manager), `test_build_engine_swing_mode_uses_swing_override_weights` (all six weights + thresholds flow through), `test_build_engine_day_mode_uses_day_override_weights` (symmetric), `test_build_engine_swing_mode_does_not_pick_up_day_override` (cross-mode isolation lock at the builder layer), `test_build_engine_day_mode_does_not_pick_up_swing_override` (symmetric), `test_per_mode_overrides_actually_change_composite_score` (wire-is-live end-to-end — rotating swing override moves the swing engine's score while day engine's score is anchored to the shared baseline). **+7 in `tests/config/test_parameter_store.py`** under a new "B30 Phase 3" section locking in `signal_parameters_from_dict` parsing of `swing_composite` / `day_composite` JSON keys: `test_signal_parameters_from_dict_legacy_secret_has_no_per_mode_blocks` (the back-compat invariant — every existing secret keeps working unchanged), `test_signal_parameters_from_dict_with_swing_composite_block`, `test_signal_parameters_from_dict_with_day_composite_block`, `test_signal_parameters_from_dict_with_both_per_mode_blocks`, `test_signal_parameters_from_dict_per_mode_null_explicit` (JSON-`null` → `None` defensive), `test_signal_parameters_from_dict_per_mode_unknown_keys_ignored` (forward-compat for fields we don't know yet), `test_signal_parameters_to_dict_round_trip_with_per_mode_blocks` (serialize → parse round-trip preserves both override blocks). The prior 1061/3 baseline (2026-05-12, unrecorded in this table — see also the §13 entries below) reflected the **B29 Phase 2 dashboard card visual uniformity** shipment which was frontend-only on top of the **B29 Phase 1 signal-card explanation refinements (+6 in `tests/signals/test_technical_analyzer.py` + `tests/signals/test_confluence.py`)** baseline of 1055/3. The prior 1051/3 baseline (verified 2026-05-11 after the **Dashboard two-desk render + chatbot routing priority order** shipment — **+13** over the prior 1038/3 baseline, distributed across two commits. **Phase 0 — chatbot routing rules (commit `6a25041`)** added **+8** to `tests/signals/test_assistant_prompts.py` covering the new `MODE RESOLUTION PRIORITY ORDER (CHATBOT ROUTING)` section: `test_prompt_carries_mode_resolution_priority_order_section_header` (pins the section header + the one-sentence anchor "you resolve WHERE the question lives before deciding WHAT to say"); `test_prompt_priority_1_explicit_screen_context` (pins P1 mechanic + behavioral contract "you do NOT ask a clarifying question and you do NOT mention the other mode" + two example surfaces — Signals page with `trading_mode=swing` and Scanner with `scanner_focus=day`); `test_prompt_priority_2_explicit_mode_language` (pins the four trigger terms `swing` / `multi-day` / `day trade` / `intraday` + symmetric swing-only and day-only example responses); `test_prompt_priority_3_structured_dual_answer_template` (pins the verbatim two-paragraph template lines + the INDEPENDENT STATUS REPORTS framing); `test_prompt_priority_3_bans_comparative_and_connective_language` (pins four canonical violations — "Compare the two desks", "frame one as a fallback", "Suggest the user switch desks", "Headline a 'system overall' summary" + three connective-tissue connectives "on the other hand", "however", "instead"); `test_prompt_bans_mode_inference_from_market_behavior` (pins the NEVER section header + three inference patterns by shape — swing-is-quiet-so-user-means-day, high-intraday-volatility-so-day, choppy-markets-suggest-day-trades + the positive framing "Mode is resolved by Priority 1, Priority 2, or Priority 3 only"); `test_prompt_clarifying_question_fallback_is_one_shot_and_verbatim` (pins the exact wording "Do you mean swing (multi-day) or day (intraday) trading? STOCVEST evaluates those as independent decision engines." + "EXACTLY ONE clarifying question" + the dual-desk carve-out so the LLM does not use it as a stalling tactic on screens that already resolve mode); `test_prompt_deterministic_response_to_cross_mode_substitution` (pins the deterministic-shape contract "deterministic and short" + "refuses the comparison, explains the independence" + the validation-numbers gate "does not reference validation numbers as evidence for or against either engine"). **Phase 1 — Dashboard two-desk render** added **+5** to the same test file covering the new dual-desk page-context allowlist: `test_serialize_page_context_emits_dual_desk_posture_on_dashboard` (pins both `swing_desk_posture=active` AND `day_desk_posture=suppressed_no_confirmation` emitted together AND `trading_mode=` is deliberately absent so the LLM does not inherit a single mode via Priority 1); `test_serialize_page_context_dual_desk_session_closed_variant` (pins the distinct `suppressed_session_closed` variant from `suppressed_no_confirmation` — the LLM uses session-bound language for one and intraday-confirmation language for the other); `test_serialize_page_context_rejects_invalid_desk_posture_values` (pins that `swing_desk_posture=actionable_now` and `day_desk_posture=FOMO_BUY` are dropped silently — closes the attack surface where a malicious client could steer the LLM through freeform posture strings); `test_serialize_page_context_emits_day_setups_count_when_nonzero` (pins the day-setups-count field with value `3`); `test_serialize_page_context_dashboard_dual_desk_omits_swing_only_fields` (pins that the dashboard's dual-desk context does NOT accidentally turn on stale scanner-overview fields like `top_setup_1=` / `top_gap_1=` / `gap_with_catalyst_count=` when only the posture fields are supplied — guards against the serializer regressing into emitting unrelated fields). The prior 1038/3 baseline (2026-05-11) was the **mode-separation safety perimeter** upgrade — **+9** over the prior 1029/3 baseline, all in `tests/signals/test_assistant_prompts.py`. The upgrade adds a SECOND perpendicular safety axis on top of the existing LOGGED-OUT / LOGGED-IN two-context perimeter: **Swing trading (multi-day) and Day trading (intraday) are independent decision engines that share market context but never share decisions, readiness, validation, or activity permission.** Six new sections were merged into `ASSISTANT_SYSTEM_PROMPT` between `CRITICAL CONTEXT AWARENESS RULE` and `PRIMARY GOAL` (additive, no destruction of any prior section): (1) **MODE SEPARATION (SWING VS DAY) — ABSOLUTE DESIGN RULE** declares the separation NON-NEGOTIABLE, names the active mode as the authoritative scope, and tells the LLM to treat dashboards/scanners showing both modes as two separate desks; (2) **WHAT MAY BE SHARED ACROSS MODES** enumerates the 5 allowed-shared items (Market regime, Macro context, Sector rotation, Market internals, Risk posture) with the explicit qualifier "Sharing context does NOT imply shared permission to trade"; (3) **WHAT MUST NEVER BE SHARED ACROSS MODES** enumerates the 8 always-mode-specific items (Trade Readiness scores, Layer alignment percentages, Signal validity windows, Gating outcomes, Validation statistics, Accuracy metrics, Portfolio linkage, Journal entries); (4) **SCREEN-LEVEL MODE BEHAVIOR** spells out the per-screen rules (Dashboard = Swing Desk + Day Desk parallel posture reporting; Scanner = two sections not a merged mode-column table when `scanner_focus=both`; Signals = exactly one mode at a time authored by `trading_mode=swing|day`; Signal Validation = mode-isolated tracks; Portfolio = mode attribution per position; Journal = one mode per entry; Performance = mode-segmented never combined-headline); (5) **MODE-AWARE EMPTY-STATE LANGUAGE** requires suppression copy to reflect the suppressed engine's vocabulary (Swing emphasizes multi-day confirmation + regime/sector alignment + structure readiness; Day emphasizes intraday confirmation + volume/momentum timing + session-specific conditions; "Never use identical copy for both modes"); (6) **MODE-SEPARATION USER INTERACTION RULES (ADDITIVE)** stack on top of the existing USER INTERACTION RULES with the new MAY/MUST NOT pairs — explicitly bans "Suggest using Day signals because Swing is quiet (or vice versa)", "Headline a combined accuracy number or a 'system overall' verdict that averages across modes", "Recommend one mode over the other as a workaround for the other's suppression". The exemplar exchange "Swing is quiet — should I day-trade instead?" is written into the prompt with the correct refusal pattern (explain that the two engines gate independently, that Swing's quiet is a Swing decision not a Day permission). Additionally, one new bullet was grafted onto the existing `HISTORICAL VALIDATION CONTEXT (LOGGED-IN ONLY)` MUST NOT list: **"Use the figures to encourage activity during a suppressed regime"** — closes the loophole where the assistant might counter the user contrasting a 62% accuracy figure against today's empty board ("you say accuracy is 62% — why aren't there any setups today?") by using the accuracy figure to argue for activity instead of explaining the active gate. Past directional accuracy and current SUPPRESSION & GATING LOGIC are two independent surfaces that must never be played off against each other. **+9 lock-in tests** in `tests/signals/test_assistant_prompts.py`: `test_prompt_declares_mode_separation_as_absolute_design_rule` (pins "NON-NEGOTIABLE" + "Swing and Day must NEVER be blended"), `test_prompt_lists_what_may_be_shared_across_modes` (pins the 5-item allowed-shared list + "Sharing context does NOT imply shared permission to trade"), `test_prompt_lists_what_must_never_be_shared_across_modes` (pins the 8-item mode-specific list), `test_prompt_carries_dashboard_two_desk_rule` (pins "Swing Desk" + "Day Desk" + "Active / Monitor / Suppressed"), `test_prompt_carries_scanner_two_section_rule` (pins `scanner_focus=both` + "TWO sections" + the anti-pattern "not a single merged table with a mode column"), `test_prompt_carries_signals_page_single_mode_rule` (pins "operates in exactly one mode at a time" + `trading_mode=swing|day` + "Never reuse readiness, alignment, or conclusions across modes"), `test_prompt_carries_mode_specific_empty_state_rule` (pins both per-mode suppression vocabularies + "Never use identical copy for both modes"), `test_prompt_bans_cross_mode_substitution` (pins "Suggest using Day signals because Swing is quiet" + "the two engines gate independently" + the combined-accuracy headline ban), `test_prompt_bans_validation_to_encourage_activity_during_suppression` (pins the new historical-validation ban + "DESCRIPTIVE of past behavior" + "NEVER a reason to override the current SUPPRESSION & GATING LOGIC" + the exemplar user prompt "why aren't there any setups today"). **All 50 pre-existing prompt tests still green** — the merge-additive integration preserved every safety behavior locked in over the prior phases (LOGGED-OUT public-mode rules, six-layer naming model, verbatim decision states, banned-phrase list, payment-tier awareness, the detailed `=== HISTORICAL VALIDATION ===` block semantics including em-dash-as-no-resolved-trades, sentinel anchoring, "no block means no comment + never invent a number + never recall a number from a previous turn"). No production code changes, no Terraform / IAM / API contract / DynamoDB-schema changes, no frontend code touched. The prior 1029/3 baseline (2026-05-11) was D3 analyzer-wire-up lock-in — **+8** over the prior 1021/3 baseline: **2 in `tests/signals/test_technical_analyzer.py`** (`test_bullish_threshold_param_actually_drives_verdict` — same uptrend bars + snapshot through `TechnicalParameters(bullish_threshold=55)` vs `bullish_threshold=99` produces an identical raw score but a differing verdict, locking in that `TechnicalAnalyzer` reads the threshold from the `params` argument it was passed and not from a hardcoded constant; `test_vwap_score_delta_param_actually_moves_score` — same uptrend with `vwap_score_delta=20` (default) vs `vwap_score_delta=0` measurably shrinks the score's absolute distance from the neutral 50, locking in that the VWAP scoring contribution is param-driven), **2 in `tests/signals/test_macro_analyzer.py`** (`test_bullish_threshold_param_actually_drives_verdict` — same risk-on SPY/QQQ/VIX snapshot through `MacroParameters(bullish_threshold=60)` vs `bullish_threshold=99` produces an identical score but flips the verdict from "bullish" to "neutral"; `test_vix_score_band_params_actually_move_score` — same low-VIX (13.0) snapshot with `vix_low_score=80` (default) vs `vix_low_score=40` strictly lowers the macro composite score, locking in that the five VIX band scores in `MacroParameters` thread all the way through to the final macro score), **2 in `tests/signals/test_sector_analyzer.py`** (`test_bullish_threshold_param_actually_drives_verdict` — same SOXX outperforming SPY through `SectorParameters(bullish_threshold=65)` vs `bullish_threshold=99` produces identical score but differing verdict; `test_strong_outperform_score_param_actually_moves_score` — same +3.0% SOXX vs +0.5% SPY snapshot through `SectorParameters(strong_outperform_score=80)` (default) vs `strong_outperform_score=60` strictly lowers the sector score, locking in that the relative-strength scoring table is param-driven), **2 in `tests/signals/test_swing_technical_analyzer.py`** (`test_bullish_threshold_param_actually_drives_verdict` — a clean 210-bar daily uptrend saturates at score=100 under the default scoring contributions; the verdict flips from "bullish" at the default `bullish_threshold=60` to "neutral" at `bullish_threshold=101` (an unreachable threshold), locking in that the swing-mode verdict is param-threshold-driven; `test_above_sma50_score_param_actually_moves_score` — zeroing every scoring contribution in `SwingTechnicalParameters` collapses the score to the exact neutral baseline of 50, and bumping `above_sma50_score` from 0 to 30 on an uptrend where `price > SMA50` adds exactly that 30-point bonus producing score=80, locking in BOTH that the swing baseline math is honest (`+0 if hh else -0 == 0`, so neutral data really does land at 50) AND that the above-SMA50 contribution is param-driven). `NewsAnalyzer` already has the analogous `test_params_threshold_used` from earlier work, so all five analyzers now have at least one wire-is-live lock-in test on top of their existing param-positional-argument signatures. This finishes the D3 analyzer-wire-up theme: every `*Parameters` block is read from `ParameterStore.get_parameters_sync()` and threaded through to the analyzer that owns it (`Technical → TechnicalAnalyzer`, `News → NewsAnalyzer`, `Macro → MacroAnalyzer` and `InternalsAnalyzer`, `Sector → SectorAnalyzer`, `SwingTechnical → SwingTechnicalAnalyzer`, `Composite → CompositeScoreEngine` via the helper added in the D3 first slice), and a refactor that bypasses the params arg now trips a deterministic regression. The prior 1021/3 baseline (2026-05-11) was D3 first slice — **+6** over the prior 1015/3 baseline: **4 in `tests/signals/test_composite_score.py`** locking in the new `build_composite_score_engine_from_params(params)` helper (uses `params.composite.*_weight` and NOT `DEFAULT_BASE_WEIGHTS` as a regression guard, threads thresholds from params, end-to-end wire-is-live check that swapping `SignalParameters` actually moves the composite score, integer→float coercion so a malformed Secrets Manager JSON cannot crash the engine), and **2 in `tests/api/test_signal_recorder.py`** locking in the legacy `swing_composite_handler` integrity gap fix (handler stamps the same `parameter_version` whose `.composite.*` weights it just used — closes the "metadata lies about which weights actually ran" bug that Phase 4's cross-version diff was about to surface; identical payload + two different parameter versions through a monkeypatched `ParameterStore` produces detectably different composite scores, proving the engine is no longer reading hardcoded `DEFAULT_BASE_WEIGHTS`). The prior 1015/3 baseline (2026-05-11) was D2 **Historical Signal Validation** Phase 3c-2 — +21 over the prior 994/3 baseline, distributed across three test files. **9 new prompt lock-in tests** in `tests/signals/test_assistant_prompts.py` covering the new `HISTORICAL VALIDATION CONTEXT (LOGGED-IN ONLY)` section: the section header is present, the `=== HISTORICAL VALIDATION ===` sentinel the serializer emits is referenced in the prompt so the LLM knows what to pattern-match on, the em-dash rendering for NaN accuracy is spelled out as "no resolved non-neutral trades" and explicitly NOT as `"0%"`, dollar-P&L translation is banned (with the load-bearing clause "Directional accuracy is NOT a return number"), trend-prediction is banned (with "descriptive, not predictive"), per-symbol/per-pattern/per-regime/per-decision-state/per-readiness-bucket/per-direction stratifications are explicitly enumerated as "deliberately withheld" with a redirect to `/dashboard/signal-validation`, using the figures to "promote" or claim the system "works" is banned (the rule reads "evidence of behavior, not promotion"), the **"no block means no comment"** rule is locked in alongside "never invent a number" and "never recall a number from a previous turn", comparison to "the market" / "other users" / a benchmark is banned, and recommending mode choice based on accuracy ("Mode choice is not an advice surface") is banned. **8 new service-level tests** in `tests/signals/test_assistant_chat_service.py` for the new `serialize_historical_validation_summary()` helper and the new `reply(historical_validation_summary=...)` kwarg: `None` summary returns empty string, `rows_examined == 0` returns empty string, happy path renders header + `window_days=` + `horizon=` + denominator-transparent `overall=` + per-mode lines + `rows_examined=`, NaN accuracy renders as `—` and never as `0.0%` or `"NaN"`, empty `by_mode` buckets (Phase 1 pre-seeds swing/day vocabulary even when zero rows) are omitted from the serialized block, the authenticated `reply()` path appends the block to the system message when a non-empty summary is provided, the authenticated `reply()` path omits the block (no `window_days=` / `rows_examined=` / `overall=` field-syntax wording in the tail) when `historical_validation_summary=None`, the public `reply_public()` path never carries the block AND refuses the kwarg by signature, no-neutrals overall line still emits the count for transparency. **2 new handler-level tests** in `tests/api/handlers/test_signals_assistant_chat.py` locking in the wiring: the handler resolves a `HistoricalValidationService` against the live recorder, calls `summarize(user_id=rc.user_id, horizon="1d", from_at=now-90d, to_at=now)` (90-day window confirmed within 60s jitter), and forwards the exact summary object to `svc.reply(historical_validation_summary=...)`; when the service raises (slow/unavailable signal-history store), the chat turn still returns 200 and the handler forwards `historical_validation_summary=None` so the prompt's "if absent, do not comment" rule activates. The 3 pre-existing handler tests that monkey-patch `AssistantChatService.reply` now use `**_kwargs` in the fake signature so future kwarg additions do not break them. **Tests/prompt detection sharpness:** the prompt itself now references the `=== HISTORICAL VALIDATION ===` sentinel as a literal string so the LLM can anchor on it — to detect whether the *serialized block* was actually appended (vs just referenced in the prompt rules), tests look at the appended tail after `=== PAGE CONTEXT ===` and check for the field-syntax wording (`window_days=`, `rows_examined=`, `overall=`) that only the serializer emits. The prior 994/3 baseline was D2 Phase 3a — +14 over the prior 970/3 baseline, all in the new `tests/api/handlers/test_signals_historical_validation.py`: `GET /v1/signals/historical-validation/summary` requires auth (`rc.user_id` enforced — 401 without it), missing `horizon` → 400 with message naming the field, invalid `horizon` (`"1w"`) → 400, missing `from` → 400, unparseable `from` → 400, inverted window (`to <= from`) → 400 with "to must be strictly after from", trailing-`Z` ISO datetime form is accepted (JavaScript's `Date.toISOString()` emits it), happy-path 2-correct + 1-incorrect → directional accuracy = 2/3 with `parameter_versions = ["v1"]` and the standing `HISTORICAL_VALIDATION_DISCLAIMER = "Historical signal accuracy does not guarantee future results."` on every response (the wording is now single-sourced in `stocvest/api/legal_copy.py` so the assistant prompt, this endpoint, the Phase 3b dashboard tab, the optional public `/performance` mirror, and the Phase 4 cross-version diff cannot drift), **empty window serializes NaN accuracy as JSON `null` not the literal `NaN`** (the test asserts both `body["summary"]["overall"]["accuracy"] is None` AND `"NaN" not in resp["body"]` — Python's default `json.dumps` emits `NaN` which crashes browsers, so `_bucket_stats_to_dict` converts at serialization time), **per-user scope isolation** (user `u-alpha`'s 2 correct rows do not appear in user `u-beta`'s response and vice versa — locks in the security boundary so one tenant cannot read another tenant's tracked outcomes), `mode=day` filter passes through to the service so swing rows are excluded from totals, lowercase `symbol=msft` is uppercased and filters at the store, horizon `1d` routes to the `outcome_1d` column (a row with `outcome_1h="correct"` and `outcome_1d=None` returns NaN accuracy at 1d but still appears in `rows_examined=1`), and `by_version=true` returns the per-`parameter_version` map shape with v1=2/2 (accuracy=1.0), v2=1c+1i (accuracy=0.5), and the synthetic `__all__` bucket carrying 3/4 (accuracy=0.75, `parameter_versions=["v1","v2"]`). The prior 970/3 baseline (2026-05-11) was D2 Historical Signal Validation Phase 2 — +14 over the prior 956/3 baseline, all in the new `tests/api/services/test_historical_validation_service.py`: empty-store returns NaN accuracy (UI renders "—"), inverted window (`to_at <= from_at`) short-circuits without hitting the store, equal-bounds zero-width window short-circuits, happy-path 2-correct + 1-incorrect + 1-neutral → accuracy = 2/3 (neutrals excluded from denominator — matches `/v1/signals/performance/summary`), **inclusive-lower / exclusive-upper window edge cases** (rows at `from_at` kept, rows at `to_at` dropped, before/after/inside boundary rows handled correctly — same semantics as the GSI `scope_generated_at` predicate `generated_at >= cutoff_iso`), naive-`generated_at` rows are treated as UTC for comparison so legacy DynamoDB hydrates do not crash, `user_id` / `symbol` / `mode` pass straight through to the underlying store (the store is the auth perimeter — the service does not silently change scope), `days_back` is computed to cover `from_at` from now and clamped at `MAX_LOOKBACK_DAYS = 366`, horizon routing (a row with `outcome_1h` populated but `outcome_1d` pending counts at 1h, returns NaN at 1d, and still appears in `rows_examined`), `summarize_by_parameter_version` produces one `BucketStats` per version plus an `__all__` combined aggregate (v1=2/2 correct, v2=1c+1i, all=3/4 = 75%, `parameter_versions = ("v1", "v2")`), rows with `parameter_version = None` or empty/whitespace land in `UNKNOWN_VERSION_KEY = "unknown"` so legacy rows are never silently dropped, an empty window produces only an `__all__` bucket with NaN accuracy, and the window filter is applied BEFORE the version split (out-of-window v1 rows must not inflate the v1 bucket). The prior 956/3 baseline (2026-05-11) was D2 Historical Signal Validation Phase 1 — +14 over the prior 942/3 baseline, all in the new `tests/signals/test_historical_validation.py`: empty-input NaN accuracy, all-neutral NaN accuracy, horizon routing between `outcome_1h` / `outcome_1d` columns, neutrals excluded from the directional denominator (matches `/v1/signals/performance/summary`), stratification by Decision state at entry (incl. `unknown` for legacy rows without the validation-ledger columns), Macro regime at entry (engine vocabulary `risk_on` / `neutral` / `risk_off` / `avoid` + `unknown` fallback, NOT the UI Bullish/Neutral/Bearish form), trading Mode (`swing` / `day`), Pattern family (declared `swing_composite` / `orb` / `vwap` / `momentum` / `gap_with_catalyst` + `other` overflow for unknown patterns so nothing silently vanishes), Trade Readiness bucket (high ≥ 70 / moderate 40-69 / low < 40 — matches Evidence card thresholds), Direction (`bullish` / `bearish` / `neutral`), `parameter_versions` sorted-deduped provenance, and frozen-dataclass immutability for both `BucketStats` and `HistoricalValidationSummary`. The prior 942/3 baseline (2026-05-11) was the two-context safety perimeter upgrade; +7 over the 935/3 baseline — all in `tests/signals/test_assistant_prompts.py` (`test_prompt_declares_critical_context_awareness_two_context_perimeter`, `test_prompt_carries_logged_out_golden_rule`, `test_prompt_explicitly_bans_per_symbol_evaluation_when_logged_out`, `test_prompt_carries_homepage_safe_refusal_template_verbatim`, `test_prompt_lists_concrete_ticker_examples_in_public_mode_ban`, `test_prompt_carries_explain_why_boundary_exists_in_tone`, `test_prompt_keeps_public_mode_rules_label_for_section_routing`) and one tightened assertion in `tests/signals/test_assistant_chat_service.py` (`test_reply_public_calls_claude_with_public_mode_marker` now isolates the appended `=== PAGE CONTEXT ===` tail rather than scanning the whole prompt, because the static instruction block now mentions `session_mode=authenticated` as descriptive text inside the CRITICAL CONTEXT AWARENESS RULE — that mention is a guardrail, not a routing signal). The 935/3 baseline (2026-05-11, explanatory-voice prompt upgrade) was +19 over the 916/3 baseline; all 19 were in `tests/signals/test_assistant_prompts.py` locking in the new prompt sections — all in `tests/signals/test_assistant_prompts.py` locking in the new prompt sections (PRIMARY GOAL, CORE PRODUCT PHILOSOPHY, SUPPRESSION & GATING LOGIC, REGIME TRANSITIONS, BACKTESTING & VALIDATION, PAYWALL AWARENESS, USER INTERACTION RULES, MAY/MUST NOT lists), the verbatim Decision lines from `lib/signal-evidence/trade-decision.ts`, the real macro pulse states (Elevated/Upcoming/Known and absorbed/Unavailable), the real sector chip labels (Confirming/Non-confirming/Risk-on/Defensive/Narrow), the 0-100 Trade Readiness scale, the explicit ban list for invented concepts (System Confidence, VIC, Volatility Control, Symbol Readiness Score, Sector Fragmented), the suppression copy anchors, the validation page anchors (/performance, /dashboard/signal-validation) + standing disclaimer, the banned self-limitation phrases, the banned BAD/GOOD response shapes, the three-mode structure, and the foundational principle. The earlier 916/3 baseline: +10 from prior 906/3 baseline — 4 new in `tests/api/handlers/test_signals_assistant_chat.py` covering the new unauthenticated `POST /v1/public/assistant/chat` (no-auth happy path, `page_context` ignored, invalid body, empty-messages deterministic intro), 4 new in `tests/signals/test_assistant_chat_service.py` covering `AssistantChatService.reply_public` (Claude call carries `session_mode=public`, never `session_mode=authenticated`, deterministic intro / outage fallback, public path refuses a `page_context` kwarg by signature), and 2 new in `tests/signals/test_assistant_prompts.py` covering the `session_mode=public/authenticated` markers + invalid `session_mode` collapse) |
| Frontend tests | `cd frontend && npm run test` | **2058 passed**, **329** files (verified **2026-06-22** after **Layer verdict band on score bars** — +3: 2 threading cases in `frontend/tests/composite-layer-rows.test.ts` (bullish/bearish thresholds threaded onto layer rows / omitted when API omits them) + 1 render case in `frontend/tests/signals-layer-breakdown-ui.test.tsx` (neutral-band caption renders for layers carrying thresholds). Prior **2055 passed**, **329** files (verified **2026-06-22** after **Deep-dive brief UX fixes** — +5: brief de-dup + `buildPlainSummary` (4 in `frontend/tests/deep-dive-present.test.ts`) + leader-card bias reconcile (1 in `frontend/tests/feed-model.test.ts`); also folds in the +4 `scenarioGeometryIsShort` cases from PR #195 not previously counted in this row. Prior **2046 passed**, **329** files (verified **2026-06-22** after **B72-UI/B73-UI deep-dive surfacing** — +4: 2 threading cases in `frontend/tests/composite-layer-rows.test.ts` (sector_technical_calibration threaded onto the technical row / omitted when absent) + 2 render cases in `frontend/tests/signals-layer-breakdown-ui.test.tsx` (calibration chip shown for non-normal regimes / hidden for normal); `npm run build` clean. Prior **2035 passed**, **328** files (verified **2026-06-20** after **B70-fix** — `AiSetupRead` now resets to idle on symbol/direction/desk change so switching tickers no longer shows the prior ticker's AI read; +1 reset case in `frontend/tests/ai-setup-read.test.tsx`). Prior full suite **2034 passed**, **328** files (verified **2026-06-20** after **B70** AI setup read — new `frontend/tests/ai-setup-read.test.tsx` (3) + richer-brief cases in `frontend/tests/deep-dive-present.test.ts` (2)). Prior **2029 passed**, **327** files (full suite verified **2026-06-20** after **B58-fix** causal-narrative directional-mixed copy — new directional-mixed case in `frontend/tests/causal-narrative.test.ts`). Prior **2028 passed**, **327** files (full suite verified **2026-06-20** after **P68** — new `frontend/tests/signal-math-contract.test.ts` + trade-plan/thesis-alert frontend tests landed since 2026-06-10; the jump from 1909/290 folds in both). Prior **1909 passed**, **290** files (verified **2026-06-10** full suite; prior **1843** partial count same day). Prior **1838** / **277 files** (2026-06-06, B64). Prior **1824** (2026-06-02). |
| Frontend build | `cd frontend && npm run build` | **success** (verified **2026-05-19** after **B49**; prior **2026-05-18** L48; the sliding-session refresh work on 2026-05-11 adds one new BFF route at `frontend/app/api/auth/refresh/route.ts` plus three new client `lib/` modules — no Next.js page surface changes, same client routes). `tsc --noEmit` confirms zero new TypeScript errors (the 24 pre-existing errors in `tests/order-confirmation-modal.test.tsx`, `tests/pdt-api.test.ts`, `tests/signal-evidence.test.ts`, `tests/signal-evidence-layers.test.ts` are unchanged from prior baselines). |

---

## 14. Appendix: Business / entity (high level)

- Entity target: **STOCVEST LLC** (Delaware); formation and Stripe Atlas items **track outside this doc**.  
- **What STOCVEST is:** signal intelligence + execution tooling; user keeps custody via their broker.  
- **What it is not:** RIA, broker-dealer, custodian, fund manager.  
- Before **paid** users: attorney-reviewed ToS/Privacy, LLC, Stripe, production infra.

---

## 15. Codebase architecture map (maintainer reference)

This section is the high-level "where things live and how they connect" map for the entire repository.
Use it when onboarding, scoping changes, and reviewing cross-cutting impact before implementation.

### 15.1 Repository layout (top level)

| Path | Purpose |
|------|---------|
| `stocvest/` | Python domain + API package (handlers, services, engines, workers, brokers, config) |
| `frontend/` | Next.js App Router UI + BFF routes + frontend tests |
| `tests/` | Backend pytest suites by domain (`api`, `signals`, `data`, `brokers`, `workers`, `integration`) |
| `infra/` | Terraform for Lambda/API Gateway/Cognito/Dynamo/Redis/ECS/EventBridge |
| `docs/` | Product/architecture contracts (`CONTEXT`, `API_CONTRACTS`, `SIGNAL_ENGINE`, `PERFORMANCE`, plans) |
| `.github/workflows/` | CI + deployment workflows |
| `scripts/` | Packaging, parameter/bootstrap, and utility scripts |

### 15.2 Backend architecture

- **Dispatch model:** `stocvest/api/lambda_dispatch.py` routes by `STOCVEST_LAMBDA_MODULE` (health, signals, brokers, scanner, market_data, portfolio, journal, pdt, websocket, workers).
- **Route handlers:** grouped under `stocvest/api/handlers/` (notably `signals.py`, `scanner.py`, `brokers.py`, `desk.py`, `watchlists.py`, `orders.py`, `analytics.py`).
- **Core signal engine:** `stocvest/signals/` with layer analyzers (technical/news/macro/sector/geopolitical/internals), composite scoring, scanner engines, confluence, and setup judgment.
- **Opportunity Desk / scanner services:** `stocvest/api/services/opportunity_desk/` (`funnel.py`, `batch.py`, `desk_refresh.py`, `discovery_row.py`, `quiet_leaders.py`) power dashboard/scanner discovery, why-missing, retained-pool, and refresh cadence behavior.
- **Composite services:** `stocvest/api/services/*composite*` (day + swing service surfaces), with per-mode parameter resolution.
- **Assistant services:** `stocvest/signals/assistant_chat.py` (`AssistantChatService.reply` — Claude Messages API, locked `assistant_prompts.py` system prompt) + context builders `stocvest/api/services/assistant_symbol_context.py` (live snapshot/news/Benzinga + `build_symbol_chart` levels), `assistant_market_context.py` (market pulse), `assistant_watchlist_context.py` (status/opportunities), `assistant_discovery.py` (+ `discovery_payload` ranked card), `assistant_citations.py` (source chips), `assistant_watchlist_action.py` (carries resolved `company_name` into the confirmation card); intent routing in `stocvest/utils/intent_detector.py` + ticker extraction in `stocvest/utils/symbol_detector.py`. Wired through `stocvest/api/handlers/signals.py`.
- **Symbol validation + name resolution:** `stocvest/api/services/symbol_resolver.py` (`resolve_symbol` → `SymbolResolution{valid, found, active, name, reason}`) wraps Polygon `/v3/reference/tickers/{symbol}` via the 24h-cached `get_ticker_reference`. **Validation policy:** reject only on a definitive miss (HTTP 404 / empty reference) or delisting (`active==False`); **fail open** on transient failures and missing API key. Used to **validate a ticker before it is written to a watchlist** in both the assistant add path (`handlers/signals.py`) and the REST add handlers (`handlers/watchlists.py` → 422 `invalid_symbol`), and to surface `company_name` next to the ticker (assistant action card, watchlist add toast/`added_symbol` payload).
- **Company-name display (ticker → name everywhere):** batch endpoint **`GET /v1/market/symbol-names?symbols=A,B`** (`handlers/market_data.py` `symbol_names_handler`, 24h-cached reference lookup, best-effort → `{names:{SYM:name}}`, degrades to `{}` on failure) + BFF **`frontend/app/api/stocvest/market/symbol-names/route.ts`**. Frontend: shared session-cached hook **`frontend/lib/hooks/use-symbol-names.ts`** (`useSymbolNames`/`useSymbolName`, coalesced lookups) + presentational **`frontend/components/symbol-name.tsx`** (`SymbolName`, theme-agnostic — symbol inherits color, name muted via opacity; `inline`/`stacked`). Wired into Signals command bar + evidence header, Watchlist decision cards, Scanner mover lanes + near-qualification + retained pool, Dashboard opportunity rows + watchlist-radar cards, and the Assistant chart header + discovery rows. Surfaces that already carried a name in their payload (scanner gap cards/setups, signals/watchlist add typeahead) keep using it.
- **Data/integration clients:** `stocvest/data/` (Polygon primary; Benzinga/EDGAR/FRED/Finnhub/FMP support), plus Anthropic synthesis integration.
- **Brokers/execution:** `stocvest/brokers/` (`factory`, adapters, PDT enforcement), with API-level order safety hooks.
- **Config/runtime controls:** `stocvest/utils/config.py` (`Settings`) + `stocvest/config/parameter_store.py` / `signal_parameters.py` for dynamic signal weights/thresholds and history.

### 15.3 Frontend architecture

- **App shell + routes:** `frontend/app/` with dashboard surfaces (`/dashboard`, `/dashboard/scanner`, `/dashboard/signals`, `/dashboard/watchlists`, `/dashboard/portfolio`, `/dashboard/journal`, admin routes, etc.).
- **Scanner surface:** `frontend/components/scanner-page-client.tsx` + `components/scanner/*` (lanes, why-missing panel, quiet desk, near qualification, market cards).
- **Signals surface:** `frontend/components/signals-page-client.tsx` + `components/signals/*` (desk tabs, reference levels, setup readouts, judgment, evidence and action context).
- **Dashboard surface:** **live `/dashboard` is the Trading Room redesign** — `frontend/components/dashboard/trading-room/*` (`dashboard-trading-room.tsx` session header + signal feed + center Brief/Deep Dive, `market-brief.tsx`, `deep-dive.tsx`, `watchlist-rail.tsx`, `quiet-feed.tsx`, `symbol-search.tsx`, `feed-model.ts`, `session-selection.ts`) mounted by `app/dashboard/page.tsx` → `trading-room-preview-content.tsx`. The legacy `dashboard-redesign.tsx` / `dashboard-page-content.tsx` surface (opportunity pipeline/discovery/radar/market pulse) is retained for its **data-loading orchestration** (`fetchDashboardFirstSegment`, deferred scanner hydrate) which the Trading Room reuses; see **`DASHBOARD_TERMINAL_UX_PLAN.md` §9**.
- **Watchlists surface:** `frontend/components/watchlists-page-client.tsx` + `components/watchlists/*` (maturation cards/rails/queue and interactions).
- **Assistant surface:** `frontend/components/assistant/*` (`stocvest-assistant.tsx` orchestrator, `assistant-panel.tsx` composer, `assistant-conversation-rail.tsx` rail with `ChartCard` inline sparkline + reference-level chips, `full-price-chart.tsx` lazy `lightweight-charts` candlestick/volume/SMA/level chart) + `frontend/lib/assistant/*` (types incl. `AssistantChart`/`AssistantChartLevel`); the full chart is also embedded on the Signals page.
- **BFF/API routes:** `frontend/app/api/stocvest/**/route.ts` normalize browser calls to backend endpoints and preserve auth/session contracts (e.g. `market/bars/route.ts` feeds the assistant/Signals full chart).
- **Shared APIs/libs/hooks:** `frontend/lib/api/*`, `frontend/lib/hooks/*`, and presentation helpers in `frontend/lib/**`.

### 15.4 Cross-cutting system contracts

- **Auth/session:** Cognito/JWT + refresh flow; server-side auth context in BFF and backend authorizer paths.
- **Mode separation:** Swing vs Day is an explicit system contract (frontend mode state, URL/query plumbing, backend route/service mode handling, parameter resolution).
- **Resilience/degradation:** retry/fallback paths in browser + BFF fetch wrappers, plus graceful handling for partial upstream failures (especially scanner/desk flows).
- **Caching/refresh:** desk/scanner caches, scheduled jobs, and explicit manual refresh endpoints work together; UI should never assume single-source real-time freshness.
- **Legal/safety framing:** explanatory language and non-advisory constraints are intentional product behavior, not cosmetic copy.

### 15.5 Testing architecture

- **Backend tests:** `tests/` cover handlers, services, analyzers, stores, workers, integration edges.
- **Frontend tests:** `frontend/tests/` cover component behavior, BFF contracts, mode routing, scanner/dashboard/signals/watchlists UX invariants.
- **Regression posture:** when changing shared contracts (mode, desk/scanner payloads, composite routing, auth/session), update both backend + frontend tests together.

### 15.6 Deployment and operations

- **Infrastructure as code:** `infra/*.tf` defines API Gateway, Lambdas, Cognito, DynamoDB, Redis, ECS services/workers, schedules, and wiring.
- **CI/CD:** `.github/workflows/ci.yml` runs backend tests, frontend build/tests, Terraform checks, and deployment hooks on protected branches.
- **Secrets/runtime env:** managed outside git; handlers/services rely on documented env + secret keys in config loaders.

### 15.7 "Must stay in sync" checklist

When changing one side of these boundaries, verify and update the paired surfaces:

1. **Backend routes/shape** (`stocvest/api/handlers/**`) <-> **BFF routes/types** (`frontend/app/api/stocvest/**`, `frontend/lib/api/**`).
2. **Mode semantics** (swing/day routing + parameter resolution) <-> **frontend mode plumbing and links**.
3. **Opportunity desk payloads** (`desk/today`, why-missing, retained-pool) <-> **scanner/dashboard presenters and tests**.
4. **Composite/evidence route selection** <-> **signals/scanner UI chips and explanations**.
5. **Auth/session flow** (401/refresh/expiry behavior) <-> **browser fetch wrappers + session UI behavior**.
6. **Config parameters/weights** <-> **admin parameter tools + signal docs/tests**.
7. **Infra env/secrets contracts** <-> **runtime `Settings` assumptions in Python and frontend env usage**.

If a change crosses one of these boundaries, update:
- implementation,
- tests on both sides,
- and this context document (plus `docs/API_CONTRACTS.md` where relevant).
