# STOCVEST — Session Context

**Read this file at the start of each session.** It summarizes what exists, what is pending, and what is planned—without duplicating the whole repo tree.

**Cursor AI rules** for agents live in **`.cursorrules`** at the repo root (see **`docs/CURSOR_RULES.md`** for a short pointer—do not duplicate the full rules there).

**Last updated:** 2026-05-12 (latest — **Automated weight-tuning proposal pipeline — algorithmic core (D10 Phase 2a)** — ships the **pure-function walk-forward grid-search optimizer** that scores candidate composite-weight rotations against the historical signal record. Phase 2a is **algorithm-only** — no AWS, no IO, no Lambda — so the math is testable in isolation and the production code path is still untouched (every existing engine call still uses Secrets-Manager-loaded weights as before). The optimizer takes a chronologically-sorted list of resolved `SignalRecord` rows for one mode and replays the production `CompositeScoreEngine` against each row's stored layer scores with each candidate weight set, comparing the resulting verdict to the actual ±0.1% price direction over the resolution horizon (same threshold the live `outcome_from_prices` uses — pinned via a cross-module equivalence test). Candidates are generated as a constrained ±delta neighborhood around baseline (3^6 = 729 raw → filtered + normalized + deduped, ≤729 actual); the best-by-val-accuracy candidate is then run through a three-gate acceptance check (val accuracy lift ≥ +2pp, val signal count ≥ 30, train/val regime distribution within 20% per regime) before being eligible for proposal. **What's deliberately NOT in scope for Phase 2a:** verdict thresholds aren't rotated (only the six layer weights; thresholds affect gating, not just scoring — different change profile); cross-mode weight transfer isn't attempted (swing/day optimize independently — that's the entire point of B30 Phase 3's per-mode override blocks); multi-objective optimization is deferred (directional accuracy only — Goodhart's law applies, which is why the human-in-the-loop gate exists). **Lossy reconstruction caveat documented:** per-layer confidence isn't persisted on `SignalRecord` rows, so the optimizer reconstructs `LayerSignal` with `confidence=1.0` — this is a symmetric assumption across all candidates, so the *relative* ranking is preserved even though absolute val accuracies will differ from what the live engine produced at signal time. Phase 2b (next) wires this core into a scheduled Lambda + Terraform infrastructure (IAM role with NO `secretsmanager:UpdateSecret`, EventBridge weekly schedule, proposal writes via `ParameterProposalStore`). Previous — **Automated weight-tuning proposal pipeline — foundation (D10 Phase 1)** — ships the data-layer foundation for proposal-only weight tuning. Phase 1 ships **dark, zero production behavior change** — the table exists and the repo can read/write/transition, but nothing in production calls it yet. The pipeline architecture is **three-IAM-principal**: optimizer Lambda role (Phase 2 — writes proposals, NO `secretsmanager:UpdateSecret` IAM); admin endpoint role (Phase 3 — reads proposals + writes secrets after admin auth); read-path Lambda roles (existing — read-only on secrets). This separation is the load-bearing safety property — the optimizer can run unattended, but every rotation requires an admin to explicitly approve from the proposal review UI. **Why proposal-only, not full auto-tune:** 12-dim search space (6 layers × 2 modes) with ~150-300 resolved signals per mode = textbook overfitting risk on trailing 60-day data; regime-tied optimization (a bull-market-trained weight set silently underweights geo/internals layers, then fails when the regime flips); Goodhart's law (directional accuracy is one metric — optimizing for it degrades expectancy after drawdown, alpha decay in compressed regimes); cascading dependencies (composite weights interact with confluence + suppression gates in ways the optimizer doesn't see); loss of explainability (an optimizer-produced weight set with no narrative is impossible to defend in a postmortem). **Why Secrets Manager remains the live store:** weights aren't a security secret (an attacker who knew them gains ~nothing — engine logic + per-symbol data produces the signal, not the weights), but Secrets Manager gives us KMS encryption + IAM-policy-gated writes + CloudTrail audit log + atomic version bumps — properties we want for **operational integrity** (preventing accidental rotation, preserving an audit trail, gating who can write). **Phase 1 shipped:** (1) **Terraform** — new DDB table `ParameterProposal` in `infra/dynamodb.tf` with `proposal_id` PK + `status_index` GSI (`status` PK + `created_at` SK so the admin UI can list pending sorted by date without scanning) + TTL enabled on `ttl` attribute. Rejected/superseded rows auto-expire after 90 days; promoted rows have NO TTL (they're the audit trail of approvals and must persist alongside `ParameterHistory`). Lambda env in `infra/lambda_6e.tf` gains `DYNAMODB_PARAMETER_PROPOSAL_TABLE` and the new ARN is added to `lambda_dynamodb_resources` flatten. `terraform validate` passes clean. (2) **Settings** — `stocvest/utils/config.py` gains `dynamodb_parameter_proposal_table: str = Field("", alias="DYNAMODB_PARAMETER_PROPOSAL_TABLE")` next to the existing `dynamodb_parameter_history_table`. (3) **Repo module** — new `stocvest/data/parameter_proposal_store.py`: `ParameterProposal` dataclass with closed-set status values (`PROPOSAL_STATUS_PENDING` / `_PROMOTED` / `_REJECTED` / `_SUPERSEDED` + `PROPOSAL_STATUS_VALUES` tuple). `new_pending` factory validates at least one per-mode override block is set — a proposal with neither swing nor day overrides carries no actionable information and would just clutter the pending queue. `to_item`/`from_item` round-trip with JSON-encoded nested fields (`evidence`, `proposed_swing_composite`, `proposed_day_composite`) — sidesteps boto3's Decimal coercion + audit-friendly when an operator reads the row in the AWS console. `ParameterProposalStore` class with injected `table` for testability: `put` (idempotent for EventBridge retry-safety), `get` (returns None for missing), `list_by_status` (closed-set status validation, DESC by `created_at` via the GSI, asserts the right GSI name is queried), `mark_promoted` / `mark_rejected` / `mark_superseded` with **atomic DDB `ConditionalCheckExpression` on the `status` attribute** — load-bearing guarantee that a rejected proposal cannot be silently re-promoted by a racing admin click. `mark_rejected` and `mark_superseded` stamp a TTL ~90 days out; `mark_promoted` deliberately does NOT (audit trail). `build_default_proposal_store` for production wiring (raises ValueError on missing table env var since Phase 1 ships dark — no production paths should invoke it yet). (4) **Tests: +24 lock-ins** in new `tests/data/test_parameter_proposal_store.py` (uses an in-process `_FakeTable` that implements `put_item` / `get_item` / `query` / `update_item` with realistic semantics including `ConditionalCheckFailedException`): factory invariants (`test_new_pending_requires_at_least_one_per_mode_block`, `test_new_pending_accepts_swing_only_proposal`, `test_new_pending_accepts_day_only_proposal`, `test_new_pending_auto_generates_id_and_timestamp`), round-trip serialization (`test_to_item_round_trip_preserves_every_field`, `test_to_item_omits_optional_fields_when_unset`, `test_to_item_encodes_nested_dicts_as_json_strings`, `test_from_item_tolerates_raw_dict_in_nested_fields`), CRUD (`test_put_then_get_returns_equivalent_proposal`, `test_get_returns_none_for_missing_id`, `test_put_is_idempotent_for_same_id`), GSI semantics (`test_list_by_status_returns_desc_by_created_at`, `test_list_by_status_filters_by_status`, `test_list_by_status_rejects_invalid_status_loud`, `test_list_by_status_honors_limit`), transitions + atomicity (`test_mark_promoted_transitions_pending_to_promoted` — verifies promoted rows have NO TTL, `test_mark_rejected_transitions_pending_to_rejected_with_ttl` — verifies TTL ≈ +90 days, `test_mark_superseded_transitions_pending_to_superseded_with_pointer`, `test_cannot_promote_already_promoted_proposal`, `test_cannot_reject_already_rejected_proposal`, `test_cannot_promote_rejected_proposal` — the load-bearing atomicity guarantee, `test_cannot_transition_missing_proposal`), closed-set invariants (`test_proposal_status_values_closed_set`, `test_proposal_ttl_days_documented_invariant`). Backend baseline **1093 → 1117 passed** (+24, zero regressions). No Terraform / IAM / API contract / DynamoDB-schema changes that affect existing production paths — the new table + IAM grant are purely additive. **Phases 2-4 deferred** to subsequent commits: Phase 2 = weekly optimizer Lambda + walk-forward CV + grid search + EventBridge schedule; Phase 3 = admin BFF + minimal admin UI for proposal review + promote/reject endpoints + admin role gating; Phase 4 = CloudWatch alarm on post-rotation accuracy degradation + one-click rollback. Previous shipment — **Gap-card mode-fit classifier + per-row Evidence routing (B30 Phase 4)** — closes the last three B30 Severity-2 items in one shipment: (1) `scannerSetupMode === "both"` gap-card default routing, (2) "Best evaluated as: swing / day / either" advisory tag on gap cards, (3) per-row classifier-driven engine selection (no longer mirrors `panelNewsTradingMode` blindly). **The problem before Phase 4:** in `Both` view every gap card opened the swing engine (the `resolveEvidenceTradingMode("both") === "swing"` collapse rule from B30 Phase 2), regardless of whether the gap's character pointed to a swing-engine question (5% earnings gap with high narrative score — classic continuation play) or a day-engine question (2.5% gap with no catalyst and 3× volume — classic intraday tape play). The user reading the Evidence modal saw the swing engine's verdict on a gap the day engine should have answered. **Fix is a classifier seam + per-row routing:** (1) **Backend classifier** — new public `classify_mode_best_fit(*, gap_pct, volume_vs_avg, has_catalyst, catalyst_category, catalyst_narrative_score) -> tuple[str, list[str]]` in `stocvest/signals/gap_intelligence.py` counts swing-tilt signals (`structural catalyst (earnings|merger|insider)`, `large gap (≥3%)`, `high-conviction catalyst (narrative ≥60)`) vs day-tilt signals (`heavy volume (≥2× avg)`, `tradable intraday range ([1.5%, 5%))`, `momentum gap (no fundamental anchor)` OR `tape-level catalyst (macro|analyst)`). Decision rule: margin ≥2 picks the winner; anything closer (including the symmetric 2-vs-1, 1-vs-2 case) returns `"either"`. Returns reasoning chips so the verdict is auditable. Closed-set return values pinned as module-level constant `MODE_BEST_FIT_VALUES = ("swing", "day", "either")`. (2) **Backend wire-up** — `build_gap_intelligence_items` now stamps every row with `mode_best_fit: "swing" \| "day" \| "either"` + `mode_best_fit_reasons: list[str]`. The fields are added at the bottom of the row dict so older clients ignore them gracefully (back-compat). (3) **Frontend type + helper** — `GapIntelligenceItem` in `frontend/lib/api/scanner.ts` gained optional `mode_best_fit?: GapModeBestFit` + `mode_best_fit_reasons?: string[]` (optional because cached pre-Phase-4 responses don't carry them). New exported type `GapModeBestFit = "swing" \| "day" \| "either"`. New `resolveGapCardTradingMode(scannerSetupMode, modeBestFit)` helper in `frontend/lib/scanner-mode-resolution.ts` is the single source of truth for the per-row routing rule: explicit scanner mode (`"swing"` / `"day"`) ALWAYS wins; `"both"` view honors the classifier verdict; `"either"` or undefined verdict in `"both"` view falls back to swing (preserves the pre-Phase-4 default). (4) **Frontend wire-up** — `openGapEvidence` in `scanner-page-client.tsx` line 891-924 now computes `gapCardMode = resolveGapCardTradingMode(scannerSetupMode, item.mode_best_fit)` once at the top of the callback and threads it into `enrichEvidenceWithComposite(base, gapCardMode)`. `evidenceTradingMode` is no longer a dependency of the callback — `scannerSetupMode` is the new dep, since the per-row mode is derived from it + the row's `mode_best_fit`. (5) **Tag rendering** — gap cards now render a "Best evaluated as: Swing (multi-day) / Day (intraday) / Either desk" pill between the catalyst block and the action-button row, with role-colored background tint (swing → accent blue, day → caution amber, either → neutral muted) and a 1px solid border in the role color. Underneath, the reasoning chips render as a single muted line joined by middle-dots so the verdict is transparent. An `InfoTip` next to the pill explains the routing rule: when the user is in explicit `"swing"` / `"day"` scanner mode, the tooltip clarifies that the explicit mode overrides the verdict; when the user is in `"both"` mode and the verdict drives routing, the tooltip says "View Signal opens this engine"; when the verdict is `"either"` in `"both"` mode, the tooltip explains it falls back to swing and suggests switching to Day mode to evaluate the gap with the day engine instead. The pill is HIDDEN when `item.mode_best_fit` is `undefined` (cached pre-Phase-4 response) — no guess shown. **Tests: +13 backend + +6 frontend lock-ins, zero regressions.** Backend (`tests/signals/test_gap_intelligence.py`): `test_mode_best_fit_returns_closed_set_value` (defensive: every input combo resolves to a valid value), `test_mode_best_fit_earnings_catalyst_with_large_gap_picks_swing` (3 swing signals fire + day chips DO NOT leak into reasoning), `test_mode_best_fit_merger_catalyst_is_structural_swing`, `test_mode_best_fit_insider_catalyst_is_structural_swing`, `test_mode_best_fit_no_catalyst_with_heavy_volume_picks_day` (3 day signals + swing chips DO NOT leak), `test_mode_best_fit_macro_catalyst_with_heavy_volume_picks_day` (tape-level catalyst classification), `test_mode_best_fit_analyst_catalyst_with_heavy_volume_picks_day` (symmetric), `test_mode_best_fit_balanced_signals_returns_either` (1-vs-1 → either, reasons concat both sides), `test_mode_best_fit_margin_below_two_returns_either` (2-vs-1 still either — margin-2 rule), `test_mode_best_fit_unknown_catalyst_category_does_not_score_as_structural` (only earnings/merger/insider count as structural), `test_mode_best_fit_quiet_market_micro_gap_returns_either` (0-vs-0 default), `test_mode_best_fit_negative_gap_uses_absolute_magnitude` (gap-down treated symmetrically), `test_mode_best_fit_huge_gap_no_volume_still_swing` (9% earnings gap with 1.1× vol still swing — magnitude alone doesn't flip to day), `test_mode_best_fit_in_build_gap_intelligence_items` (end-to-end wire-up: the new fields are present on the row dict and a 5% gap surfaces a `large gap` chip in `mode_best_fit_reasons`). Frontend (`frontend/tests/scanner-mode-resolution.test.ts`): `explicit scannerSetupMode 'day' wins regardless of classifier verdict` (4-way: swing/day/either/undefined verdict all collapse to `"day"`), `explicit scannerSetupMode 'swing' wins regardless of classifier verdict` (symmetric), `'both' view honors classifier 'swing' verdict`, `'both' view honors classifier 'day' verdict (cross-engine routing)` (the load-bearing branch — without it, the day verdict would be cosmetic and every gap card on `"both"` view would still open swing), `'both' view with 'either' verdict falls back to swing`, `'both' view with undefined verdict (legacy / cached response) falls back to swing` (back-compat for the cache TTL window during deploy). Backend baseline **1079 → 1093 passed** (+14, zero regressions). Frontend baseline **336 → 342 passed** (+6, zero regressions). **B30 Severity-2 status: all three product-decision items now CLOSED.** No Terraform / IAM / API contract / DynamoDB-schema changes — the `/v1/scanner/gap-intelligence` response gained two optional fields (additive). The classifier is intentionally heuristic + small — it could grow more sophistication later (e.g. premarket volume ratio vs full-day volume vs avg, relative-to-ATR check, options activity confirmation) but the current 6-signal/3-each design is the right starting point: it covers the canonical patterns, every signal has plain-English reasoning, and the margin-2 rule preserves `"either"` for legitimately ambiguous gaps so the classifier never over-claims. Previous shipment — **Per-mode composite weight overrides (B30 Phase 3 — Suggestion 4 audit)** — closes the Severity-2 audit item from B30 ("Audit whether layer weights themselves should differ between engines — today both share `CompositeParameters`"). Audit finding: both the swing engine (`stocvest/api/services/swing_composite_engine.py`) and the day engine (`stocvest/api/services/real_composite_engine.py`) — plus the legacy client-supplied-scores swing handler (`stocvest/api/handlers/signals.py:301-340`) — read the **same** `params.composite` block (tech 0.30 / news 0.20 / macro 0.15 / sector 0.15 / geo 0.10 / internals 0.10). The per-layer **inputs** already diverge by mode (swing reads 120h news / 14d macro / 168h geo / weekly sector / daily-bar tech with `mode="swing"`; day reads 8h news / 1d macro / 8h geo / daily sector / intraday tech with `mode="day"`) but the **blend weights** stamped on every `SignalRecord.parameter_version` are identical, which is a known asymmetry: macro and sector deserve more weight for swing (multi-day holds carry through Fed days and sector rotation), while technical / internals / news deserve more weight for day (intraday confirmation IS the day-engine's primary truth and fresh 8h news has not yet absorbed). **Proposed rotation** (recorded in `docs/BACKLOG.md` row B30 Phase 3 with reasoning, NOT shipped as the default — operators rotate in Secrets Manager when ready): swing tech 0.28 / news 0.15 / macro 0.20 / sector 0.18 / geo 0.12 / internals 0.07; day tech 0.32 / news 0.25 / macro 0.10 / sector 0.12 / geo 0.08 / internals 0.13. Verdict thresholds (`bullish_threshold=+0.20` / `bearish_threshold=-0.20`) and confluence parameters intentionally NOT in scope — those are higher-stakes product-decision dials and ride a separate audit cycle. **Implementation is additive infrastructure only — zero production behavior change today.** (1) Schema: `stocvest/config/signal_parameters.py` `SignalParameters` gained two optional override fields `swing_composite: CompositeParameters | None = None` and `day_composite: CompositeParameters | None = None`. Both default to `None` so any Secrets Manager JSON without `swing_composite` / `day_composite` keys (i.e. every existing secret today) leaves the per-mode blocks unset and the engine falls back to the shared `composite` block — the **load-bearing back-compat invariant**. The dataclass field docstring explicitly documents this seam + the operator-rotation flow + the resolver function that consumes the fields. (2) Parser: `signal_parameters_from_dict` in `stocvest/config/parameter_store.py` now parses `data["swing_composite"]` / `data["day_composite"]` via the new `_parse_optional_composite_block` helper, which returns `None` for absent / null / non-dict values (defensive) and a fully-coerced `CompositeParameters` for dict values (unknown keys ignored per the existing `_coerce_dataclass` contract — forward-compat). (3) Resolver: new public `resolve_composite_block(params, mode)` in `stocvest/signals/composite_score.py` is the single source of truth for mode-aware composite parameter selection — if `mode == "swing"` and `params.swing_composite is not None` return it, else if `mode == "day"` and `params.day_composite is not None` return it, else return `params.composite`. Exported from `stocvest.signals.__init__`. Unknown mode strings (`"options"`, `""`, case-mismatched `"SWING"`) fall through to the shared block defensively. (4) Builder: `build_composite_score_engine_from_params(params, *, mode=None)` gained a keyword-only `mode` argument that threads through the resolver. `mode=None` preserves the legacy back-compat path (always returns the shared block) so any caller that forgets the kwarg still gets the previous behavior verbatim. (5) Call-site wiring: all three production callsites now declare their mode explicitly — `swing_composite_engine.py` line 251 + 261 + 262 use `resolve_composite_block(params, mode="swing")` for `min_available_layers` / `bullish_threshold` / `bearish_threshold` and pass `mode="swing"` to the builder; `real_composite_engine.py` line 335 + 366 + 367 use `mode="day"` symmetrically; `signals.py:339` (the legacy `swing_composite_handler` client-supplied-scores path) passes `mode="swing"` to the builder. The local variable `swing_composite` / `day_composite` is bound once per request so the engine and the threshold check are guaranteed to be derived from the same resolved block (closes the operator-rotates-secret-between-two-reads race as a bonus, same pattern as the D3 first slice). **Tests: +18 backend lock-ins, zero regressions.** **+11 in `tests/signals/test_composite_score.py`** under a new "B30 Phase 3 — per-mode composite override blocks" section covering: (a) `test_resolve_composite_block_falls_back_to_shared_when_no_per_mode_block` — pins the no-op default contract for all three mode values (swing / day / None) against a fresh `default_signal_parameters()` whose override fields are both `None`; (b) `test_resolve_composite_block_returns_swing_block_when_set` — when only `swing_composite` is set, `mode="swing"` returns the override, `mode="day"` falls back to shared, `mode=None` falls back to shared (cross-mode isolation); (c) `test_resolve_composite_block_returns_day_block_when_set` — symmetric; (d) `test_resolve_composite_block_handles_both_overrides_independently` — both blocks set, each mode picks its own; (e) `test_resolve_composite_block_ignores_unknown_mode` — defensive case-sensitive lock for `"options"` / `""` / `"SWING"` all falling through to shared; (f) `test_build_engine_swing_and_day_identical_when_no_override` — end-to-end no-op default lock (with shared block only, both modes build engines with identical `_base_weights` + thresholds — this is the **production behavior guarantee** until operators rotate); (g) `test_build_engine_swing_mode_uses_swing_override_weights` — verifies all six weights + both thresholds flow through the builder from the swing override; (h) `test_build_engine_day_mode_uses_day_override_weights` — symmetric; (i) `test_build_engine_swing_mode_does_not_pick_up_day_override` — cross-mode isolation at the builder layer (closes the regression where someone refactors the resolver to look at both blocks); (j) `test_build_engine_day_mode_does_not_pick_up_swing_override` — symmetric; (k) `test_per_mode_overrides_actually_change_composite_score` — wire-is-live end-to-end test: rotate swing override to heavily-bullish-technical / low-news, swing engine score moves significantly while day engine score (no override) stays anchored to the shared baseline. **+7 in `tests/config/test_parameter_store.py`** under a new "B30 Phase 3" section covering: (a) `test_signal_parameters_from_dict_legacy_secret_has_no_per_mode_blocks` — pins the back-compat contract that existing Secrets Manager JSON (no `swing_composite` / `day_composite` keys) parses with both override fields `None`; (b) `test_signal_parameters_from_dict_with_swing_composite_block` — new JSON with a `swing_composite` dict parses into a `CompositeParameters` instance with all six weights + thresholds; (c) `test_signal_parameters_from_dict_with_day_composite_block` — symmetric; (d) `test_signal_parameters_from_dict_with_both_per_mode_blocks` — both blocks coexist; (e) `test_signal_parameters_from_dict_per_mode_null_explicit` — JSON-`null` parses as `None` (defensive); (f) `test_signal_parameters_from_dict_per_mode_unknown_keys_ignored` — forward-compat for fields we don't know yet; (g) `test_signal_parameters_to_dict_round_trip_with_per_mode_blocks` — serialize → parse round-trip preserves both override blocks. Backend baseline **1061 → 1079 passed** (+18, zero regressions). Frontend baseline **336** unchanged (no frontend changes — backend infrastructure only). No Terraform / IAM / API contract / DynamoDB-schema changes — `Secrets Manager` `stocvest/signal-parameters` payload schema is forward-compatible (the new optional keys are additive). **Operator runbook for actually rotating weights when ready:** edit `Secrets Manager` `stocvest/signal-parameters`, add a `swing_composite` and/or `day_composite` object to the JSON payload with the proposed weights below, bump `version`, and `ParameterStore.invalidate_cache()` (or wait `CACHE_TTL_SECONDS = 300`). Every new `SignalRecord` will then carry an honest `parameter_version` stamped from the engine that actually scored it, the D2 Phase 4 cross-version diff view will surface the rotation as a clean v→v+1 transition, and the historical-validation aggregator can measure the impact of the per-mode weight change directly against the next 30/60/90 days of resolved outcomes. Previous shipment — **Dashboard card visual uniformity (B29 Phase 2)** — closes a user-reported design-language inconsistency: "the cards across the application should have same uniform look and feel. The cards on the dashboard doesn't resemble other cards in the application." The Phase 2b "rail-line" treatment (bright 2px role-accent border + 4px top rail strip + 3px left stripe + 9% role-tinted gradient + heavy halo + `borderRadius["2xl"]`) made dashboard cards visually loud relative to every other surface in the app (Signals page, Scanner page, Performance page, Evidence sub-panels — all of which sit on a canonical `colors.surface` + `1px solid colors.border` + `borderRadius.xl`/`lg` + `surfaceGlowClassName` shell). **Fix is structural:** (1) Promoted the Evidence-card-internal `elevatedCardStyle` helper to a top-level **`cardSurfaceStyle(colors, tone)`** export in `frontend/lib/design-system.ts` so the canonical "1px slate border + faint slate halo, with optional bullish/bearish/caution tone variants" shape is the single source of truth for the app-wide card visual contract. Local `elevatedCardStyle` in `signal-evidence-card.tsx` is now a one-liner alias so all existing call sites continue to work unchanged. (2) Rewrote `frontend/components/dashboard-card.tsx` to delegate its shell to `cardSurfaceStyle(colors, "neutral")` — same `background`, `border`, `boxShadow`, plus `borderRadius.xl` + `spacing[4]` padding to match Signals/Scanner cards. Role identity is now encoded by a single subtle channel: a **4px-wide role-tinted `borderLeft` accent** (matches the precedent of the Scanner gap card's `borderLeft: 4px solid caution`) plus the small labelled role pill at top-left (matches the small-pill pattern of NOT INVESTMENT ADVICE on the Evidence card). Retired DOM nodes: `dashboard-card-role-top-rail` and `dashboard-card-role-stripe`. Retired styles: 2px solid bright role-accent border, 9% role-tinted gradient, `borderRadius["2xl"]`. (3) Softened `SubsectionCard` inside `shared-context-master-card.tsx` (the wrapper for Sections B/C/D/E) to the same canonical shell — the 1.5px slate-tinted border + linear gradient was retired. (4) Softened the **Swing Desk Primary Read card** in `dashboard-redesign.tsx` — the 1.5px violet `borderAccent` border + 6% role-tinted gradient + heavy halo was replaced by the canonical neutral shell at `borderRadius.lg` (role identity is anchored by the parent Swing Desk panel's `borderLeft` accent + role pill, so the child card doesn't need to repeat the role hue). (5) `roleAccents` import + `theme` destructure dropped from `dashboard-redesign.tsx` (no longer referenced). Tests in `dashboard-role-color-language.test.tsx` rewritten — pins on the removed elements (`role-top-rail`, `role-stripe`, primary-read 1.5px violet border) replaced by positive pins on the new canonical shell + 4px `borderLeft` + role pill + `borderRadius: 1rem` (the new lock-in catches a regression that tries to reintroduce either the loud rail treatment OR a thick coloured border on the Primary Read card). `borderAccent` token retained in design-system but unused by components (kept as a known palette entry for future treatments). Test baselines: frontend `npm run test` → **315 passed** (was 314; +1 from the new `every_master_card_uses_borderRadius_xl_and_a_4px_role_tinted_borderLeft` lock-in). Backend baseline unchanged at 1061 (no Python touched). All visual hardening additive in terms of contracts; previous shipment — **Signal card explanation refinements (B29 Phase 1)** — closes four user-reported clarity / data-trust issues on the Evidence card surfaced by the GOOGL screenshot review. (1) **VWAP sanity guard:** `stocvest/signals/technical_analyzer.py` now wraps `_calculate_vwap` with a `_vwap_is_plausible(vwap, price)` defensive guard — when the computed intraday VWAP deviates from the latest close by more than `_VWAP_PRICE_DEVIATION_MAX_PCT = 0.30` (30%), the analyzer drops the value, logs a single-line `vwap_anomaly_suppressed` WARNING (carrying `symbol`, `computed`, `price`, `bars`, `max_deviation_pct` for triage), and the UI falls back to the existing "VWAP Forming" chip + tooltip. This protects users from misleading numbers like the reported `VWAP $28.77` on a stock trading near $389 while we triage the underlying upstream cause (mis-aligned bars cache, unadjusted prices crossing a corporate action, etc.). (2) **Verdict-tag reconciler:** new `buildVerdictTagReconciler(direction, confirming, conflicting, geopoliticalDragActive)` helper in `frontend/lib/signal-evidence.ts` produces a single calm sentence under the Confirming Signals chip group when the composite verdict polarity disagrees with the chip polarity — e.g. a long setup whose composite resolves bearish surfaces "Bullish inputs present, but overridden by VWAP conflict + Weak Volume + geopolitical drag." Parenthetical chip detail (`(0.42× avg)`) is stripped from the reconciler so the sentence reads cleanly; precision is preserved on the chip itself. The helper is symmetric (bullish verdict + bearish-conflicting chips also renders) and returns `null` for the neutral verdict or when no dissonance exists. Rendered under the chip group with `data-testid="verdict-tag-reconciler"`. (3) **Geopolitical drag badge:** when `evidence.layers` includes a geopolitical layer with `geoHasLiveEvents === true` AND `exposureBand !== "low"`, the Evidence card now renders a bearish-tinted `GEOPOLITICAL DRAG` pill in the verdict hero row (next to `NOT INVESTMENT ADVICE`), tying high-tier headline risk back to the composite verdict visually instead of leaving it stranded inside the geopolitical layer card. Pill carries `data-testid="geopolitical-drag-badge"`. The reconciler also auto-appends "geopolitical drag" to the override list when the badge is active and "geopolit" is not already present. (4) **Weak/Strong Volume precision:** new `_format_rel_vol(ratio)` helper in `stocvest/signals/confluence.py` replaces the lossy `:.1f` format on the volume confluence chips — ratios in `[0.05, +∞)` now render with two-decimal precision (`0.42×`, `1.83×`), the `(0, 0.05)` sliver floors to `<0.05×` so labels never collapse to a misleading `0.0x avg`, and the multiplication sign `×` replaces the ASCII `x` everywhere so the label is visually distinct from a stray letter. Mirrors a real user-reported case where `0.04` volume ratio rendered as `0.0x avg` and triggered doubt. Previous shipment — **Assistant on-card CTA routing rule (B28 Phase 2c follow-up)** — closes a user-reported chatbot UX regression. The user was on the scanner page looking at the GOOGL Gap Intelligence card (which has an inline "View Signal" button that opens the Evidence modal in place) and asked the chatbot "can you explain what googl card is saying". The chatbot's response was structurally wrong: "I can't see the GOOGL card details from the scanner view... To see GOOGL's complete analysis, click into the symbol on the Signals page." This routed the user off the page they were already on and asked them to re-find the symbol on a different surface, when the card right in front of them already had a button that does exactly the thing they were asking for. **Fix is prompt-side, no code-side changes:** added a new `ON-CARD CTAs — REFER USERS TO THE BUTTON ON THE CARD THEY'RE LOOKING AT` sub-section inside CONTEXTUAL MODE RULES in `stocvest/signals/assistant_prompts.py`. The section enumerates a per-surface CTA map (Scanner Gap Intelligence → "View Signal", Scanner setup card → "View Evidence" + "Open Signals", Dashboard Swing Desk row → "View Evidence", Dashboard Day Desk row → "Open Day Signals →", Performance ledger → "Open full ledger (Swing / Day) →", Signals page → no referral needed because the full Evidence card is already rendered) with each button label quoted verbatim so the LLM can reference the exact on-screen wording the user can scan for. The section ends with a CRITICAL one-line restatement: "when the page context indicates a multi-symbol overview (scanner, dashboard) and the user asks for a deeper read on one symbol on that page, the next step is the on-card CTA — NOT a navigation instruction. Do not say 'click into the symbol on the Signals page' or 'go to the Signals page and enter the symbol'. The symbol is already on the card the user is asking about; the card has the button." The fix also adds two new GOOD example responses (scanner-page user asking about a GOOGL gap card; dashboard Swing Desk user asking why a row is in Monitor only) and pins the exact failed production response shape as a BAD example with a parenthetical explanation tying the failure to the rule, plus a GOOD counterpart with the correct **View Signal** verbatim button label. **Tests:** +4 backend lock-ins in `tests/signals/test_assistant_prompts.py` covering (1) the section header + behavioral rule + critical one-line restatement; (2) the per-surface CTA map with verbatim button labels (six surfaces: Gap Intelligence "View Signal", setup card "View Evidence" + "Open Signals", Swing Desk row "View Evidence", Day Desk row "Open Day Signals →", Performance "Open full ledger (Swing / Day) →"); (3) the banned-shape lock-in for the exact GOOGL scanner regression + the GOOD counterpart that names the on-card **View Signal** button + reassures the user they don't need to leave the page; (4) the Signals-page carve-out (no CTA referral needed because the Evidence card is already rendered) so the LLM doesn't tell a Signals-page user to "click View Evidence" — that button on that page just re-renders what they already see. Backend baseline **1051 → 1055 passed (+4, zero regressions)**, frontend **301/41 unchanged** (no frontend code touched). `tsc --noEmit` zero new errors. No Terraform / IAM / API contract / DynamoDB-schema changes. The prior 2026-05-12 entry was **Sector behavioral reframe + Signal Validation Ledger moved off dashboard + sub-card highlighted borders (B28 Phase 2c)** — closes three concrete user directives layered on top of Phase 2b. (A) **Signal Validation Ledger off the dashboard.** Per the user's one-sentence test "a data element belongs in Shared Context if and only if it answers what kind of market environment are all traders operating in right now," tracked outcomes fail that test (they describe "did past signals work?", not market environment). The `SignalValidationLedgerTertiarySurface` component was removed from `frontend/components/dashboard-redesign.tsx` (the dashboard stack now contains exactly `SharedContextMasterCard` + `Swing Desk` + `DayDeskPanel` + `EarningsCalendar`; nothing about historical outcomes lives anywhere on the dashboard) and the export was deleted from `frontend/components/shared-context-master-card.tsx`. The full stratified ledger surface now lives on the logged-in **Performance page** (`/dashboard/performance`) as a `data-testid="performance-validation-ledger-link"` panel rendered only when `showHomeLink={false}` (i.e. authenticated traffic only — the logged-out `/performance` mirror deliberately does NOT render this link surface because the stratified breakdown is gated behind login at the API layer (Phase 3c-1) and the UI now matches that gating). The Performance page panel carries Performance-appropriate framing ("Validation ledger · stratified outcomes" eyebrow, "Open full ledger (Swing / Day) →" anchor pointing at `/dashboard/signal-validation`, explicit Mode Separation reminder, "tracked outcomes only — not a brokerage account" disclaimer). (B) **Sector Rotation behavioral reframe in Shared Context Section C.** Per the user's directive ("If sector rotation explains why the market feels the way it does, it's Shared Context. If it suggests where to put risk, it isn't"), Section C had been showing a per-sector chip row with raw % numbers (XLK +1.5%, XLE -0.6%, XLF +0.4%, …) — a relative-strength ranking explicitly banned ("No % rankings, no bars sorted by performance, no heatmap with winners"). The chip row was deleted entirely; ETF symbol names (XLK / XLC / XLE / XLF / XLY / XLI / XLV / XLP / XLU / XLB / XLRE) no longer appear anywhere inside the Shared Context master card. Section C now renders **two behavioral categorical readouts** instead: (1) **Rotation profile** ("Concentrated" / "Rotational" / "Mixed" / "Unknown") via the new `classifyRotationProfile(sectorPct5d: Array<number | null>)` pure helper — Concentrated when spread ≥ 3.0% AND only 1-2 sectors meaningfully positive (the "narrow leadership" pattern with broad follow-through unlikely), Rotational when mixed signs across sectors AND spread ≥ 1.5% (capital cycling, no single sector controlling), Mixed for the fallthrough, Unknown for fewer than 3 valid data points; (2) **Participation** category (the existing Broad / Mixed / Narrow / Unknown). Section C is retitled "Sector Participation (Last ~5 Sessions)" (was "Participation / Breadth Tone") — time-anchored and behavioral, per the user's exact wording. The `rotationProfilePlainLine` helper produces strictly descriptive sentences ("Narrow leadership — a few sectors carrying the move; broad follow-through unlikely", "Capital rotating across sectors — no single sector controlling the move; expect inconsistent follow-through", "Mixed sector behavior — partial leadership, no dominant pattern") with banned words (trending / leading / bullish / bearish / winners / losers / buy / sell / allocate / should / setup) enforced by tests. (C) **Highlighted borders on the requested cards.** Section A's three index tiles (SPY / QQQ / IWM) now carry **direction-aware highlighted borders**: green at 1.5px when `pct5d > 0.1` (the same threshold the existing `getChangeColor` uses for the % number, so the tile border and the % color are always in lockstep), red when `pct5d < -0.1`, neutral slate at 1.5px otherwise. Each tile also stamps a `data-tile-direction="up" | "down" | "flat" | "unknown"` attribute for stable test selectors. The role-color (slate rail) lives on the master card boundary; the direction-color lives on the inner tiles — orthogonal channels by design. New `indexTileBorderForDirection(pct5d, colors)` helper centralizes the threshold + color logic. Sections B / C / D / E each now render inside a `SubsectionCard` wrapper (new internal component) with a 1.5px slate-tinted border + linear-gradient surface + drop-shadow — each section reads as a discrete information unit instead of a paragraph in a wall of text. Section A is intentionally NOT wrapped (it already IS a row of three direction-bordered tiles; double-wrapping would create card-in-card-in-card nesting that defeats the at-a-glance scan). Each sub-section also stamps `data-subsection-card="B" | "C" | "D" | "E"` for test selectors. The Swing Desk's **"Primary read" card** (the dominant decision surface when no setups are firing) bumped from `1px solid color-mix(border 72%, transparent)` to `1.5px solid color-mix(roleAccents.swing.borderAccent 65%, border)` — picks up the swing role's bright violet `borderAccent` (same hue the master card uses on its rail-line border) plus a linear-gradient surface and a halo shadow, so the Primary read card reads as a structural unit not a paragraph. The card also stamps `data-testid="swing-desk-primary-read-card"` for the lock-in test. **Tests:** Frontend baseline **285 → 301 passed (+16)**, zero regressions, no new test file. **In `tests/dashboard-role-color-language.test.tsx`** the failing `section_C_renders_a_participation_CATEGORY_plus_the_sector_chip_row` was rewritten to assert (a) Section C now has BOTH `shared-context-rotation-profile-category` + `shared-context-participation-category` testids, (b) `shared-context-sector-chip-row` is NULL (the chip row removal contract), (c) NO sector ETF NAMES (XLK, XLC, XLE, XLF, XLY, XLI, XLV, XLP, XLU, XLB, XLRE) leak through the section text content (anti-naming-leakage); the failing `signal_validation_ledger_is_a_tertiary_surface_NOT_a_role_master_card` was rewritten to `dashboard_does_NOT_render_signal_validation_ledger_anymore` (asserts both that no `signal-validation-ledger-tertiary` testid exists in the dashboard root AND that NO `<a href="/dashboard/signal-validation">` anchor exists anywhere in the dashboard DOM — closes both forms of regression); +4 new tests: `section_C_title_uses_neutral_behavioral_language_NOT_breadth_tone` (locks the verbatim "Sector Participation (Last ~5 Sessions)" label), `section_C_rotation_profile_label_is_one_of_the_closed_set` (rotation category MUST be one of Concentrated / Rotational / Mixed / Unknown AND MUST NOT contain trending / leading / bullish / bearish / winners / losers / strong sector / weak sector / leadership emerging), `section_A_index_tiles_have_direction_aware_borders_green_red_neutral` (asserts `data-tile-direction` resolves to up / down / flat for fixture rows with pct5d +1.19 / -2.0 / +0.05), `subsections_B_C_D_E_each_render_as_their_own_bordered_subcard` (asserts the `data-subsection-card` attribute is present on B / C / D / E and explicitly absent on A — the no-double-wrap contract), `swing_desk_primary_read_card_renders_with_highlighted_role_border` (asserts the testid + a `border: 1.5px` style + `color-mix` swing-role hue in the style attribute). **In `tests/shared-context-derivations.test.ts`** +9 new tests for the rotation-profile helpers: `classifyRotationProfile` (Unknown for <3 valid points; Concentrated for narrow-leadership patterns; Rotational for mixed-signs-moderate-spread; Mixed for fallthrough; closed-set guarantee — only the four allowed strings; NaN / Infinity ignored in the valid-count); `rotationProfilePlainLine` (every category produces a behavioral non-actionable line with 14 banned words absent; Concentrated explicitly disclaims broad follow-through; Rotational explicitly disclaims single-sector control). **In `tests/performance-no-combined-headline.test.tsx`** +2 new tests under a `PerformanceTrackingContent — Validation ledger link (Phase 2c)` describe block: `logged-in Performance page renders the ledger link surface` (asserts the testid + "Open full ledger (Swing / Day)" text + `/dashboard/signal-validation` href + Mode Separation reminder text), `logged-out public Performance mirror does NOT render the ledger link` (with `showHomeLink={true}` the panel is structurally absent — UI gating matches the API gating). Backend baseline **1051/3 unchanged** (no backend changes — Phase 2c is purely frontend visual + structure + tests). `tsc --noEmit` zero new errors. No Terraform / IAM / API contract / DynamoDB-schema changes. The prior 2026-05-11 entry was **Dashboard Shared Context master card + bright rail-line border treatment (B28 Phase 2b)** — closes the user-flagged regression that the Phase 2 work didn't go far enough: the dashboard still scattered shared infrastructure across FIVE separate cards (Short-Horizon Market State, Market Pulse, Sector Rotation, Upcoming Earnings, Signal Validation Ledger), and the role-color treatment used 1px subtle border tints that the user explicitly called out as "very confusing" — "color combination is very confusing" and "Borders should be clearly visible even in peripheral vision. Think 'rail lines', not soft shadows." Phase 2b consolidates the dashboard to the user's exact specification: ONE Shared Context master card with five strictly-ordered sub-sections (A-E), ONE Swing Desk master card, ONE Day Desk master card, and a tertiary low-prominence Signal Validation Ledger surface below them. Nothing else lives at the same hierarchy level. **(A) ROLE-COLOR LANGUAGE — bright rail-line borders.** `frontend/lib/design-system.ts` `RoleAccent` interface gained a third field `borderAccent: string` — distinctly BRIGHTER than the surface `accent` so the master-card boundary is visible in peripheral vision without reading any text. Dark theme: shared border `#22d3ee` (electric cyan-steel) over slate surface, swing border `#a78bfa` (bright violet) over indigo surface, day border `#67e8f9` (bright aqua) over teal surface. Light theme: cyan `#0891b2`, violet `#7c3aed`, cyan `#0e7490`. `frontend/components/dashboard-card.tsx` `DashboardCard` was upgraded: when `role` is set, the card border becomes **2px solid** in `borderAccent` (vs the legacy 1px at 20% blend — the user directive "rail lines, not soft shadows"), the shadow halo picks up `borderAccent` for a 1px outer ring + 20px outer glow, and a new **4px-tall top accent strip** (`<span data-testid="dashboard-card-role-top-rail">`) flush to the rounded top corner is the third orthogonal-edge anchor of role identity (alongside the 3px left stripe and the role pill). Together the three edge anchors mean the user can answer "which family am I looking at?" from any peripheral glance without text. **(B) SHARED CONTEXT MASTER CARD.** New file `frontend/components/shared-context-master-card.tsx` (~470 lines) is the single owner of all environmental context the two desks read. It absorbs four previously-separate dashboard cards (Short-Horizon Market State, Market pulse, Sector rotation, Upcoming earnings) and replaces them with five strictly-ordered sub-sections inside ONE master card with header "Shared Context · All timeframes · used by both desks" and subtitle "Market environment and constraints used by all desks. Not a trade signal." Sub-sections: (A) **Recent Session Market State (Last ~5 Sessions)** — three index sub-cards SPY/QQQ/IWM, each with a small inline neutral-stroke SVG sparkline (5 daily closes, no axes, no labels) + net % change number; (B) **Volatility Environment** — CATEGORY-ONLY readout ("Contained / Expanding / Compressed / Unknown") derived from VIX level + VIX session %, NO ATR numbers per directive; (C) **Participation / Breadth Tone** — CATEGORY-ONLY readout ("Broad / Mixed / Narrow / Unknown") derived from sector rotation + index breadth, with sector chip row as supporting evidence — "Broad" requires BOTH ≥4-of-5 sectors positive AND ≥2-of-3 indices positive (the narrow-leadership-trap anti-leak); (D) **Risk / Event Horizon** — TIME-BASED, not directional. Category ("Elevated / Active / Quiet / Unknown") derived from macro warning + upcoming earnings count, with earnings list as supporting detail; (E) **Environment Summary** — single human-readable sentence joining A+B+C+D ("Short-horizon price drift up, volatility contained, participation broad, macro risk approaching."), followed by the `SHORT_HORIZON_TIMEFRAME_LINE` + `SHORT_HORIZON_WHY_THIS_MATTERS` guardrail clauses (both still exported from `weekly-market-context-widget.tsx` for back-compat). All four classification functions (`classifyVolatility`, `classifyParticipation`, `classifyRiskHorizon`, `buildEnvironmentSummary`) are exported as pure helpers for direct test coverage. The new file also exports `SignalValidationLedgerTertiarySurface` — a low-prominence link surface (no role color, no rail-line border) rendered BELOW the three master cards. Tracked outcomes are neither shared context (market facts) nor a decision engine, so they cannot live inside Shared Context AND cannot be a peer master card; tertiary placement is the honest answer. **(C) SPARKLINES.** New file `frontend/components/index-sparkline.tsx` is a 90-line lightweight SVG component: single `<polyline>` normalized to [0,1] of (min, max) closes, neutral desaturated stroke (`color-mix(in srgb, textMuted 70%, text)`), 80×22 default canvas, no axes, no labels, with a `<title>` element for screen-reader accessibility. Flat-session edge case renders the line midway down the canvas — never divide-by-zero. The dashboard `WeeklyIndexRow` type gained `closes5d?: number[]` (last 5 daily closes oldest → newest) and `dashboard-page-content.tsx`'s `buildWeeklyRows` slices `closes.slice(-5)` from the already-fetched 8-bar daily array — no new backend fetches required. **(D) DASHBOARD RESTRUCTURE.** `frontend/components/dashboard-redesign.tsx` lost ~250 lines: the 7fr/13fr grid containing 7 cards collapsed into a clean `<div className="dashboard-stack grid grid-cols-1 gap-7">` containing exactly `<SharedContextMasterCard /> + <DashboardCard role="swing">Swing Desk + <DayDeskPanel /> + <SignalValidationLedgerTertiarySurface /> + <EarningsCalendar />`. The legacy right-rail Market Pulse / Sector Rotation / Upcoming Catalysts / Signal Validation Ledger inline render blocks were deleted outright (git history preserves them for audit). **TESTS:** Full frontend baseline **253 → 285 passed (+32, 40 → 41 test files)**. New `frontend/tests/shared-context-derivations.test.ts` — 29 lock-ins on the pure helpers: `classifyVolatility` (8 tests including threshold boundaries at VIX level 22/13 + VIX pct ±5 + the "no ATR numbers in plain line" anti-leak); `classifyParticipation` (6 tests including the narrow-leadership-trap anti-leak — a single benchmark surge with thin sector follow-through MUST NOT classify as Broad); `classifyRiskHorizon` (7 tests including Elevated-wins-over-everything-when-macro-warning-present + the threshold-boundary lock-in at exactly 4 earnings); `buildEnvironmentSummary` (6 tests including drift mapping for all 4 cases — up/down/mixed/unknown — and a cartesian-product anti-leak that every (drift × vol × participation × risk) combination produces strategy-agnostic output with `["setup", "continuation", "trend intact", "constructive", "buy", "sell"]` all absent). `frontend/tests/dashboard-role-color-language.test.tsx` rewritten end-to-end — 15 tests covering: role + role-pill on each desk panel (swing → "SWING · MULTI-DAY", day → "DAY · INTRADAY"), shared master card → "SHARED CONTEXT"; **`dashboard_has_exactly_three_master_cards_at_top_hierarchy_level`** invariant (`document.querySelectorAll("[data-card-role]").length === 3`) — locks in the "nothing else at the same hierarchy level" rule structurally; `borderAccent` distinct from `accent` within each role + distinct across roles in both themes (the rail-line contrast contract); every master card renders BOTH the 4px top rail AND the 3px left stripe (two orthogonal-edge anchors); all five sub-sections A-E render in DOM order; Section A renders three index tiles each with `<polyline>` sparkline and NO `<text>` axes/labels; Section B is category-only without "ATR"; Section C renders both category label + sector chip row; Section D renders both category label + earnings list; Section E renders the environment summary + verbatim guardrails; master card never contains banned evaluative words ("constructive", "defensive 5-session tape", "tape (background)", "setup", "continuation", "trend intact"); subtitle disclaims trade-signal status; **`signal_validation_ledger_is_a_tertiary_surface_NOT_a_role_master_card`** — the moved ledger surface has `data-card-role === null`, preventing anyone from accidentally promoting it back to peer status. The existing `tests/dashboard-two-desk.test.tsx` `shared_market_context_is_role_tagged_NOT_swing_coded` test was updated to anchor on the new `data-testid="shared-context-master-card"` (was `shared-market-context-weekly`). Backend baseline **1051/3 unchanged** (no backend changes — Phase 2b is purely frontend visual + structure + tests). `tsc --noEmit` zero new errors in touched files (25 pre-existing in other test files unchanged). No Terraform / IAM / API contract / DynamoDB-schema changes. The prior 2026-05-11 entry was **Dashboard role-color language + Shared Context copy redesign (B28 Phase 2)** — closes the two visual-design regressions the user surfaced after Phase 1 shipped: (a) "the color combination is very confusing" — the Swing Desk and Shared Context cards rendered with the same theme-accent surface so the user had to read labels to know which family a card belonged to; (b) the Shared Context card's headline read as evaluative swing-coded language ("Constructive 5-session tape (background)") and the parent card was titled "Weekly market context" — both implied swing posture/holding-period intent on a surface that should describe what the market has been doing, not how a trader should trade it. Two intertwined fixes, one commit. **(A) ROLE-COLOR LANGUAGE.** Three orthogonal channels of meaning on the dashboard: (1) PRICE DIRECTION green/red (locked to `colors.bullish` / `colors.bearish`), (2) CAUTION amber (locked to `colors.caution`), (3) **DESK ROLE — new third channel — slate/indigo/teal** introduced in `frontend/lib/design-system.ts` as `roleAccents: Record<ThemeName, Record<CardRole, RoleAccent>>` with `CardRole = "shared" | "swing" | "day"` and `RoleAccent = { accent, accentStrong, pillLabel }`. Dark theme: shared `#64748b` (slate-500) + label `"SHARED CONTEXT"`; swing `#818cf8` (indigo-400) + label `"SWING · MULTI-DAY"`; day `#2dd4bf` (teal-400) + label `"DAY · INTRADAY"`. Light theme: slate `#475569`, indigo `#4f46e5`, teal `#0d9488` with the same verbatim pill labels. Teal was deliberately chosen over amber for the Day Desk so desk identity does not visually shout "warning" when posture is calm (amber is already locked to `colors.caution`). Hue families are intentionally distant from `colors.accent` (#3b82f6 / #2563eb) so the global interaction-cue blue cannot be confused with swing identity. **`frontend/components/dashboard-card.tsx`** extended with an optional `role?: CardRole` prop: when set, the card surface gradient leans on the role accent (9% tint into surface instead of the global 7% accent tint), the border picks up a 20% role-accent blend, the shadow halo uses the role accent for its 1px outer ring, a 3px-wide solid stripe is painted along the left edge (`<span data-testid="dashboard-card-role-stripe">`), and a top-anchored uppercase role pill (`<span data-testid="dashboard-card-role-pill" data-card-role-pill={role}>`) renders the verbatim label so screenshots are self-explanatory. The card also stamps `data-card-role={role}` on the wrapping `<article>` for stable test selectors. Omitting `role` preserves the legacy theme-accent surface for non-desk-taxonomy pages (settings, legal, future surfaces). **Role application across the dashboard:** `shared` on Short-Horizon Market State (renamed below), Market pulse, Sector rotation (5 sessions), Upcoming earnings this week, Signal validation ledger — five cards now form a single slate-tinted family of shared infrastructure both desks read; `swing` on the Swing Desk card (eyebrow trimmed from "Swing desk · Multi-day (evaluated on daily closes)" to "Multi-day · evaluated on daily closes" — the role pill carries the desk identity, so the eyebrow can shed the redundant "Swing desk" prefix); `day` on the Day Desk panel (eyebrow trimmed from "Day desk · Intraday (session-bound)" to "Intraday · session-bound" by the same logic). Cards that previously had no explicit `data-testid` (Market pulse, Sector rotation, Upcoming catalysts, Signal validation ledger) gained `data-testid="shared-{market-pulse|sector-rotation|upcoming-catalysts|signal-validation-ledger}-card"` for the new role-attribution tests. **(B) SHARED CONTEXT COPY REDESIGN.** `frontend/components/weekly-market-context-widget.tsx` retitled and rewritten. The parent card's title flips from `"Weekly market context"` → **`"Short-Horizon Market State (Last ~5 Sessions)"`** (timeframe-anchored, cadence-neutral — the user's directive: "Even though '~5 sessions' ≠ calendar week, the word weekly causes friction"). Subtitle replaced with the verbatim user-supplied: `"Daily-close price behavior across major indices. Shared background input for all desks; not a trade signal."` Eyebrow flips from `"Shared context"` → `"Recent Market State"` (the role pill now carries the SHARED CONTEXT label, freeing the eyebrow to carry the OBSERVATIONAL category). The status-line bucket function was renamed `weekTone` → `shortHorizonStatusLine` and its three labels were rewritten from evaluative ("Constructive 5-session tape (background)" / "Defensive 5-session tape (background)" / "Mixed 5-session tape (background)") to strictly observational: avg ≥ +0.6% → `"5-Session Outcome: Net upward price progress"`; avg ≤ −0.6% → `"5-Session Outcome: Net downward price progress"`; otherwise → `"5-Session Outcome: Mixed direction across indices"`. The `(background)` softening suffix is dropped — the role pill now carries that disambiguation. Two new verbatim guardrail constants are EXPORTED from the widget so a refactor cannot drop them and tests pin the exact wording: `SHORT_HORIZON_TIMEFRAME_LINE = "Timeframe: Calculated from daily closes over the last ~5 trading sessions. Purpose: Provides background bias only; does not imply trade duration."` and `SHORT_HORIZON_WHY_THIS_MATTERS = "Why this matters: Multi-session price direction influences risk appetite, follow-through probability, and position sizing across all trading horizons."` These two lines render under the index grid as footnote material (`data-testid="short-horizon-guardrails"`) and explicitly disclaim trade-duration intent — the single sentence that does the heavy lifting preventing day traders from misreading shared context as swing intent. The status line itself is stamped with `data-testid="short-horizon-status-line"`. The widget root carries `data-testid="short-horizon-market-state"`. The legacy export name `WeeklyMarketContextWidget` is preserved (no consumers needed to be renamed); only the COPY is observational now. **Tests:** +12 new lock-ins in new `tests/dashboard-role-color-language.test.tsx` covering (1) every desk carries the correct `data-card-role` + role pill text — `swing-desk-panel` → `data-card-role="swing"` + pill `"SWING · MULTI-DAY"`; `day-desk-panel` → `data-card-role="day"` + pill `"DAY · INTRADAY"`; `shared-market-context-weekly` → `data-card-role="shared"` + pill `"SHARED CONTEXT"`; (2) all four other shared cards (Market pulse, Sector rotation, Upcoming catalysts, Signal validation ledger) carry the same `data-card-role="shared"` and the same pill label — locks in the single slate-tinted shared-infrastructure family; (3) the three role accents are three DISTINCT hue families in both dark and light themes (`new Set([shared.accent, swing.accent, day.accent]).size === 3`) — closes the regression where a future refactor could accidentally alias one role to another and collapse the visual contract; (4) each role-tinted card renders the left-edge role stripe element (`data-testid="dashboard-card-role-stripe"`); (5) Short-Horizon card title contains `"short-horizon market state"` + `"last ~5 sessions"` and does NOT contain the legacy `"weekly market context"` swing-coded title; (6) status line for the avg ≥ +0.6% case contains `"5-session outcome"` + `"net upward price progress"` and does NOT contain any of `["constructive", "defensive", "tape", "(background)", "setup", "continuation"]` — six banned evaluative / swing-coded words; (7) threshold-boundary lock-in at avg ≤ −0.6% renders `"net downward price progress"` NOT `"defensive"` NOT `"tape"`; (8) threshold-boundary lock-in for mixed renders `"mixed direction"` NOT `"tape"`; (9) the two guardrail constants render verbatim under `data-testid="short-horizon-guardrails"`, including the explicit `"does not imply trade duration"` disclaimer in the timeframe-binding clause; (10) the subtitle contains the disclaimers `"not a trade signal"` + `"shared background input for all desks"`. The earlier `tests/dashboard-two-desk.test.tsx` `weekly_market_context_eyebrow_is_shared_context_NOT_swing_desk` test was rewritten as `shared_market_context_is_role_tagged_NOT_swing_coded`: the canonical SHARED-CONTEXT signal is now the role pill (verbatim "SHARED CONTEXT" label) and `data-card-role="shared"` attribute, with the eyebrow only needing to NOT carry swing-coded words ("swing", "multi-day"). Backend baseline **1051/3 unchanged** (no backend changes — Phase 2 is purely frontend visual + copy). Frontend baseline **241 → 253 passed (+12, 39 → 40 test files)**. Zero regressions. `tsc --noEmit` zero new errors (24 pre-existing). No Terraform / IAM / API contract / DynamoDB-schema changes. The prior 2026-05-11 entry was **Dashboard two-desk render + chatbot routing priority order (B28 Phase 0 + Phase 1)** — closes the Dashboard surface in the Mode Separation UI compliance shipment and codifies the LLM routing rules the dual-desk surface depends on. Two commits, in order. **Phase 0 — chatbot routing rules (commit `6a25041`):** added a new `MODE RESOLUTION PRIORITY ORDER (CHATBOT ROUTING)` section to `ASSISTANT_SYSTEM_PROMPT` between the Mode-Separation MAY/MUST NOT rules and PRIMARY GOAL, codifying P1 (explicit screen context wins — five enumerated surfaces) → P2 (explicit mode language in the user's question wins on dual-desk surfaces — four trigger terms: "swing", "multi-day", "day trade", "intraday") → P3 (ambiguous + both desks visible → STRUCTURED DUAL ANSWER using a verbatim two-paragraph template, INDEPENDENT STATUS REPORTS, no comparison / no fallback framing / no connective tradeoff language) → NEVER infer mode from market behavior (three banned inference patterns enumerated by shape so the LLM pattern-matches them) → CLARIFYING-QUESTION FALLBACK (one verbatim question allowed only when all three priorities fail, gated against use on dual-desk surfaces) → DETERMINISTIC RESPONSE TO THE CROSS-MODE-SUBSTITUTION QUESTION (refuses the comparison, explains the independence, MUST NOT reference validation numbers as evidence — closes the loophole where historical accuracy could be reinterpreted as a permission signal). One-sentence anchor at the top: "you resolve WHERE the question lives before deciding WHAT to say." +8 prompt lock-in tests in `tests/signals/test_assistant_prompts.py`. **Phase 1 — Dashboard two-desk render (Mode Separation B28):** `frontend/components/dashboard-page-content.tsx` flips `DASHBOARD_SCANNER_TUNING.scannerSetupLoadMode` from `"swing"` → `"both"` so the dashboard now pulls intraday setups alongside swing setups on every mount (also adds `daySetupsLimit: 4`). The Top Signals card on the dashboard is relabeled as the Swing Desk (eyebrow "Swing desk · Multi-day (evaluated on daily closes)", title "Swing Desk", subtitle calls out independence from the Day Desk below) with `data-testid="swing-desk-panel"` + `data-swing-desk-posture`. The Weekly market context card's eyebrow flips from "Swing desk" → "Shared context" because it is a shared input that both desks read (the prompt's "WHAT MAY BE SHARED ACROSS MODES" rule) — its subtitle now explicitly says "Shared input that both the Swing Desk and the Day Desk read; NOT a swing-only signal." New `frontend/components/day-desk-panel.tsx` (DayDeskPanel) renders the Day Desk with the 5-element anatomy from the spec: ① Header (eyebrow "Day desk · Intraday (session-bound)", title "Day Desk"), ② Posture pill (Active / Monitor-only / Suppressed — always above content), ③ Primary content (top day signals OR suppression copy — mutually exclusive — capped at 4 rows; intraday-flavored signal rows with score-percent / triggers / "Intraday" badge / per-symbol "Open Day Signals →" deep link carrying `?trading_mode=day&ref=dashboard-day-desk`), ④ "What would re-enable day setups" using DAY-VOCABULARY ONLY (volume / momentum / session structure / ORB qualification — explicitly NOT swing language like "regime alignment incomplete" or "sector confirmation"), ⑤ Footer link to `/dashboard/scanner?mode=day`. The panel imports nothing from swing-side helpers — Mode Separation safety perimeter is enforced at the import graph level. Day Desk posture state machine in `frontend/lib/dashboard-posture.ts` (new helpers): `dayDeskPostureKind` (returns `active` if market open + top score ≥ `DAY_DESK_ACTIVE_SCORE_FLOOR=0.55`, `monitor` if open + setups present but top score below floor, `suppressed_session_closed` if regular session not open — extended-hours treated as closed for Day Desk gating, `suppressed_no_confirmation` if open + no setups, `suppressed_scanner_error` on scanner failure), `emptyDayPostureHeadline`, `emptyDayOneLiner` (extended-hours called out separately from generic closed), `dayDeskSuppressionStatusLine`, `buildDayReenableBullets` + `buildDayReenableBulletsShort` (day vocabulary both for open and closed branches, never swing). The dashboard's assistant page-context publisher (`usePublishAssistantContext`) emits `swing_desk_posture` + `day_desk_posture` + `day_setups_count` and **deliberately OMITS `trading_mode`** on the dashboard — the dashboard is the canonical multi-mode surface and the LLM must use the Priority 3 STRUCTURED DUAL ANSWER path, never inherit a single mode via Priority 1. Backend `serialize_page_context` in `stocvest/signals/assistant_prompts.py` allowlists the three new fields (validates posture values against the closed allowed sets — invalid values dropped silently so the LLM never sees freeform strings under desk-posture fields). Frontend `AssistantPageContext` type in `frontend/lib/assistant/types.ts` declares the same three fields with narrow union types. `frontend/components/dashboard-card.tsx` extended to spread `data-*` attributes onto its wrapping `<article>` so the Swing Desk panel and shared Market Context card can carry stable test selectors. **Tests:** +5 backend lock-ins in `tests/signals/test_assistant_prompts.py` covering dual-desk page-context serialization (both `*_desk_posture` fields emitted together on dashboard with no `trading_mode=` line; `suppressed_session_closed` distinct from `suppressed_no_confirmation`; invalid posture values dropped — `swing_desk_posture=actionable_now` does not leak; `day_setups_count` emitted when non-zero; dashboard dual-desk context does NOT carry stale scanner-overview fields like `top_setup_1=` or `gap_with_catalyst_count=`). +20 frontend lock-ins: 11 in new `tests/dashboard-day-helpers.test.ts` (posture state machine — scanner-error wins over all else, market-closed wins over setup state, extended-hours treated as session-closed, market-open-no-setups → no_confirmation, top-score-at-or-above-floor → active, top-score-below-floor → monitor; day-vocabulary anti-leak — six swing phrases ["regime alignment", "sector confirmation", "weekly structure", "multi-day structure", "DailyBarScanner", "EMA200"] explicitly absent across every posture headline, both `buildDayReenableBullets` branches, and `dayDeskSuppressionStatusLine`; extended-hours one-liner explicitly mentions "extended"; suppression-status-line distinguishes "outside regular session" from "intraday confirmation absent"; three distinct re-enable bullets per branch). 9 in new `tests/dashboard-two-desk.test.tsx` (both desks render simultaneously; Day Desk follows Swing Desk in DOM order via `Node.DOCUMENT_POSITION_FOLLOWING` — guards stacked layout independently of CSS; Weekly market context eyebrow says "Shared context" NOT "Swing desk" — narrowed to the eyebrow element specifically because the subtitle legitimately names both desks; Day Desk posture `suppressed_session_closed` when market closed with "session closed" copy; Day Desk posture `suppressed_no_confirmation` when market open + no setups; Day Desk renders real setup symbols when market open + setups present + posture `active` with no suppression block; Day Desk re-enable copy uses DAY vocabulary [volume + momentum + session/ORB/intraday] AND explicitly NOT swing vocabulary [no "regime alignment", no "sector confirmation"]; Swing Desk eyebrow has "swing desk" + "multi-day" AND NOT "intraday (session-bound)"; Day Desk footer link points at `/dashboard/scanner?mode=day` not `?mode=swing`). Updated existing `tests/dashboard-swing-only.test.tsx` (1 test) — the "intraday setup not in DOM" assertion that was the swing-only-dashboard contract is now wrong; replaced with positive contract that the intraday setup renders INSIDE the Day Desk's signal section while the Swing Desk shows its empty-state copy, confirming the data partition by `scanner_mode` works end-to-end. Backend baseline **1046 → 1051 passed** (+5 over Phase 0's intermediate 1046; +13 over the prior 1038 long-term baseline). Frontend baseline **221 → 241 passed** (+20, 37 → 39 test files). Zero regressions. `tsc --noEmit` zero new errors (24 pre-existing). No Terraform / IAM / API contract / DynamoDB-schema changes. The prior 2026-05-11 entry was the four-phase Mode Separation UI compliance shipment described below. **Mode Separation UI compliance shipment (four-phase frontend cleanup)** — closes the screen-level violations of the mode-separation safety perimeter that was added to `ASSISTANT_SYSTEM_PROMPT` earlier the same day. The prompt declared the design rule ("Swing trading and Day trading are independent decision engines that share market context but never share decisions, readiness, validation, or activity permission"); this shipment makes the UI obey that rule structurally so the existing screens can no longer regress. Four sequential commits (`e7c9fe0` → `122ddc8` → `0f31098` → `7bf3775`) each addresses one screen-level violation surfaced by the post-prompt audit. **Phase A1 — Scanner "Both" split (commit `e7c9fe0`):** `scanner-page-client.tsx` previously merged swing+day setups into a single ranked list sorted by score and sliced to top 10 (`rankedSetups = [...overview.setups].filter(...).sort((a,b) => b.score - a.score).slice(0,10)`), so a day setup with score 0.9 ranked above a swing setup with score 0.7 even though they belong to different engines. The fix partitions by `setup.scanner_mode === "swing_daily"` and renders TWO mode-labelled sections in `both` mode ("Swing setups (daily cadence)" / "Day setups (intraday cadence)"), each top-5, each with its own mode-vocabulary empty-state copy ("No swing setups — regime and structure not aligned." / "No day setups — intraday confirmation and session timing not aligned."). Single-mode views (`swing` or `day`) keep the existing single-list rendering. The "Open Signals" deep link per row now appends `&trading_mode=${setup.scanner_mode === "swing_daily" ? "swing" : "day"}` so the user lands in the engine they clicked, never the other one's localStorage default. **Phase A2 — Signals page mode plumbing (commit `122ddc8`):** previously `tradingMode` was restored from `localStorage` only on mount (URL was not authoritative), `fetchUserEvaluatedSignals({ days: 30 })` was called without `mode` so the past-signal-states history could mix swing and day rows on a single-engine view, and the `signal_id` URL-strip after resolve nuked the entire query string (any `?trading_mode=` query was lost). Fix: (a) `app/dashboard/signals/page.tsx` reads `searchParams.trading_mode` server-side and passes `initialTradingMode: "day" | "swing" | null` on `signalsPrefill`; (b) `signals-page-client.tsx` initialises `tradingMode` state from `initialTradingMode ?? "swing"`, gates the localStorage-restore useEffect so it only runs when the URL did NOT specify a mode (URL beats localStorage), and `updateTradingMode` now mirrors the new mode into the URL via `window.history.replaceState` preserving other query params; (c) `fetchUserEvaluatedSignals({ days: 30, mode: tradingMode })` carries the mode and `tradingMode` is added to the effect deps so engine switches refetch; (d) the signal_id strip preserves `trading_mode` instead of nuking it; (e) `signal-validation-page-client.tsx` ledger-row deep links append `trading_mode=${tab}` (both tabs). **Phase C — Historical Validation no-combined-headline (commit `0f31098`):** `historical-validation-panel.tsx` previously defaulted to `mode = "all"` which omitted the mode filter from the API call and rendered a single "Overall directional accuracy" hero card averaging swing+day signals into one number — a direct violation of "Statistics, hit-rates, and outcomes must never be combined into a single headline number." Fix narrows `ModeFilter` from `"all" | "swing" | "day"` to `"swing" | "day"` (TypeScript-level guard), `MODE_OPTIONS` becomes `["swing", "day"]`, default mode is `swing` (the first-class engine in product framing), the fetcher always sends a concrete mode, and the hero card title is mode-specific ("Swing directional accuracy (multi-day cadence)" / "Day directional accuracy (intraday cadence)") with a `data-mode` attribute on the accuracy node. Dropdown labels expanded to "Swing (multi-day)" / "Day (intraday)" so the engine attribution is visible even without the cadence label. **Phase D — Performance no-combined-headline (commit `7bf3775`):** three surfaces — `/performance` dashboard top metric row, the `/performance` 90-day public validation block, and the homepage `landing-performance-section.tsx` — each rendered a single combined-engine accuracy number ("Directional Accuracy", "Overall accuracy (1d)", "Overall directional accuracy (resolved 1d)"). Fix: (a) `performance-tracking-content.tsx` top metric row drops the "Directional Accuracy" card (grid shrinks lg:grid-cols-4 → lg:grid-cols-3), and `PublicValidationSection` drops the "Overall accuracy (1d)" card (grid shrinks sm:grid-cols-3 → sm:grid-cols-2; only "Swing track (1d, multi-day cadence)" + "Day track (1d, intraday cadence)" remain; `overall` is no longer destructured from the validation summary so there is no path that can leak a combined number into the DOM); (b) `landing-performance-section.tsx` removes the fallback "Overall directional accuracy (resolved 1d): X.X% · n=Y" line (this was the most promotional violation since it lived on the LOGGED-OUT homepage) and replaces it with a mode-agnostic volume line plus a handoff pointing users at per-engine track records on the Signal Tracking page; the unused `overallPct = summary?.directional_accuracy_percent ?? 0` derivation is dropped. **Lock-in tests (+13 total):** Phase A1 +2 in `tests/scanner-swing-toggle.test.tsx` (two-section render with mode-isolated DOM order + mode-specific empty-state copy), Phase A2 +5 in new `tests/signals-page-mode-plumbing.test.ts` (`fetchUserSignalHistoryPage` forwards `mode=swing` / `mode=day` to URL query; omits mode when caller omits it (legacy contract preserved); `fetchUserEvaluatedSignals` forwards mode to EVERY paginated page request (no mid-pagination swap)), Phase C +2 in new `tests/historical-validation-panel-mode.test.ts` (`MODE_OPTIONS` exposes ONLY `["swing", "day"]`; `ModeFilter` type narrowness via TS-level guard), Phase D +4 in new `tests/performance-no-combined-headline.test.tsx` (`LandingPerformanceSection` does NOT contain "Overall directional accuracy" or "70.0%"; renders the new per-engine handoff line; `PerformanceTrackingContent` top row does NOT include "Directional Accuracy"; `PublicValidationSection` does NOT contain "Overall accuracy" and renders both "Swing track (1d, multi-day cadence)" + "Day track (1d, intraday cadence)" with the per-engine percentages from `by_mode`). Frontend baseline **208 → 221 passed (+13, zero regressions)**, backend **1038/3** unchanged (no backend changes). `tsc --noEmit` zero new errors (24 pre-existing). **Explicit non-goals deferred to backlog rows M1 + M2 below:** (1) Dashboard Day Desk — `dashboard-redesign.tsx` is intentionally swing-first today (per the comment in `dashboard-page-content.tsx:13-16`); adding a parallel Day Desk with its own posture (Active / Monitor / Suppressed), top day signals, and "what would re-enable" copy is a NEW product surface that requires a product decision + day-engine posture API, not a compliance refactor. (2) Portfolio + Journal mode attribution — `portfolio-page-client.tsx` positions and `journal-page-client.tsx` entries have no `strategy_trading_mode` field on the wire today; the journal's `is_day_trade` is a PDT-style flag, not an engine attribution; making either surface honour mode-isolation requires a backend schema change (new column on `JournalEntry` + `Position`, mode-filtering on analytics endpoints, backfill plan for legacy rows), not a frontend cleanup. **Prior commit (2026-05-11) — D3 first slice — composite-engine parameter wiring closes a real integrity gap** (backend-only, no Terraform, no IAM, no new dependencies, no frontend changes). Three composite-scoring callsites existed: `swing_composite_engine.py` and `real_composite_engine.py` already pulled layer weights + thresholds from `ParameterStore`-loaded `SignalParameters` (correct), but the legacy `swing_composite_handler` in `stocvest/api/handlers/signals.py` — the thin `POST /v1/signals/swing/composite` path that accepts client-supplied per-layer scores — called `CompositeScoreEngine()` with **no arguments** while still stamping `parameter_version = ParameterStore.get_parameters_sync().version` onto every recorded `SignalRecord`. The metadata claimed "rules vN" but the engine actually computed against the hardcoded `DEFAULT_BASE_WEIGHTS` constants from `composite_score.py`, which differ from `CompositeParameters` defaults on five of six layers (news 0.18 vs 0.20, sector 0.12 vs 0.15, macro 0.16 vs 0.15, geopolitical 0.12 vs 0.10, internals 0.12 vs 0.10). Phase 4's cross-version diff would have surfaced this drift as a phantom "v1 vs v2" delta even when no parameters had actually changed, because the recorded version string was untethered from the weights the engine used. **Fix:** new `build_composite_score_engine_from_params(params)` helper in `stocvest/signals/composite_score.py` is the single source of truth for translating a `SignalParameters` instance into a live `CompositeScoreEngine` (reads `params.composite.{technical|news|macro|sector|geopolitical|internals}_weight` + `bullish_threshold` + `bearish_threshold`, coerces ints to floats for operators editing JSON in Secrets Manager). All three production callsites — `swing_composite_handler`, `real_composite_engine.py`, `swing_composite_engine.py` — now route through this helper, and `swing_composite_handler` reads `active_params = ParameterStore.get_parameters_sync()` once so the engine and the stamped `parameter_version` are guaranteed to be derived from the same parameter snapshot (closes the operator-rotates-secret-between-two-reads race as a bonus). `DEFAULT_BASE_WEIGHTS` remains in the module as a documented test/no-args fallback so the existing engine-unit tests (`tests/signals/test_composite_score.py`, `test_composite_scorer.py`, `test_risk_factors.py`, `test_risk_reward.py`, etc.) keep working unchanged; the docstring on the helper explicitly states this contract. **Tests:** +6 backend lock-ins. **4 in `tests/signals/test_composite_score.py`** — helper produces weights matching `params.composite.*_weight` and explicitly NOT matching `DEFAULT_BASE_WEIGHTS` (regression guard against the drift coming back), helper threads `bullish_threshold` / `bearish_threshold` from params not engine defaults, identical-payload + different `SignalParameters` produces different composite scores end-to-end (the wire-is-live test that would catch any silent fallback to `DEFAULT_BASE_WEIGHTS`), helper coerces integer weights to float so a `technical_weight: 1` in the Secrets Manager JSON cannot crash the engine. **2 in `tests/api/test_signal_recorder.py`** — `swing_composite_handler` stamps the same `parameter_version` whose `.composite.*` weights it just used (closes the integrity gap end-to-end by writing a record with `parameter_version="9.9.9-d3-test"` via a custom params monkeypatch and asserting the stored `SignalRecord.parameter_version` matches), identical payload + two different parameter versions through `ParameterStore.get_parameters_sync` produces detectably different `response_body["score"]` values (proves the legacy handler is no longer reading hardcoded `DEFAULT_BASE_WEIGHTS` regardless of `parameter_version`). Backend baseline **1015 → 1021 passed (+6, zero regressions)**, frontend **208** unchanged (no frontend code touched). `tsc --noEmit` zero new errors (24 pre-existing). **D3 first slice closes the data integrity contract for the cross-version diff view shipped in Phase 4** — operators can now actually rotate `stocvest/signal-parameters` in Secrets Manager and the resulting `SignalRecord` rows will carry an honest `parameter_version` that reflects the actual scoring weights that produced them. **Still pending in D3:** wire the rest of the tunables (technical analyzer thresholds, news lookback hours, macro VIX bands, sector scoring tables, swing-technical SMA periods) through to their respective analyzer constructors; optional Claude prompts in Secrets Manager (P2). The composite weights were the single most important slice because the validation framework is built around exactly those numbers — every other tunable is additive on top. **Prior commit (2026-05-11) — D2 Historical Signal Validation Phase 4 (cross-version diff view on the dashboard's Historical accuracy tab — `bde04e4`)** — see backlog row D2 for the full Phase 4 breakdown. **Prior commit (2026-05-11) — D2 Historical Signal Validation Phase 3c-1** shipped: unauthenticated `GET /v1/signals/historical-validation/public-summary` HTTP endpoint backed by the platform signal scope (`user_id=None` → `scope_key == "PUBLIC"`), returning a **trimmed projection** (`overall` + `by_mode` only — every other stratification dropped at the response layer to honor the LOGGED-OUT golden rule), with `symbol` query parameter rejected with 400 (homepage-safe contract) and defaults of horizon=1d + trailing 90-day window so the homepage hits it without a query string. New "Historical signal validation (last 90 days)" section rendered on `/performance` showing the overall + swing-track + day-track accuracy cards plus the verbatim disclaimer. Terraform applied — 1 new unauthenticated route added.)  
**Repo:** https://github.com/roygurijala/stocvest  
**Test baseline (regression gate — must match §13):** Backend `pytest tests/ -q` → **1161 passed**, **3 skipped** (+44 over the prior 1117 baseline from the D10 Phase 2a walk-forward optimizer shipment — all 44 new tests in the new `tests/signals/test_weight_optimizer.py` covering: `WeightSet` invariants (normalize sum-to-1, sum-must-be-positive guard, duck-typed `from_composite_block` lift, JSON-friendly `to_composite_block_dict` serialization, canonical `WEIGHT_LAYERS` order), engine-mirror constants (`NEUTRAL_MOVE_PCT == 0.1` pin, `outcome_from_prices` cross-module equivalence vs `stocvest.api.services.signal_recorder`, `regime_for_engine_from_application_label` covering all live mappings + fallback), `score_weight_candidate` accuracy math (unambiguously-bullish row set → 1.0, anti-bullish → 0.0, neutral outcomes excluded from denominator, empty layer-scores skipped, all-neutral → 0.0), `walk_forward_split` chronological correctness (75/25 default, custom fraction, invalid-fraction rejection, empty input → `([], [])`, unsorted-input loud rejection), `regime_distribution` + `regime_distributions_match` (counts per regime key, close distributions pass, divergent distributions fail, empty-side permissive, tolerance boundary just-over fails / just-under passes), `generate_candidate_weights` constrained grid (baseline first, all normalized to sum=1, no non-positive weights survive, deduped by rounded fingerprint, ≤729 upper bound, non-positive delta rejected), `accept_proposal` three-gate enforcement (happy path with human-readable reason, too-few-val-signals reject, insufficient-lift reject, regime-drift reject, negative-lift reject, custom-threshold kwargs sanity), and `optimize_weights_for_mode` end-to-end on synthetic data (uniform rows return baseline, news-dominant scenario rotates toward news, all result fields populated, empty input rejected, too-few-rows-for-split rejected, evidence dict shape matches the Phase-1 proposal contract). +24 over the prior 1093 baseline from the D10 Phase 1 parameter-proposal store foundation shipment — all 24 new tests in the new `tests/data/test_parameter_proposal_store.py`). Previous +14 over the 1079 baseline from the B30 Phase 4 gap-card mode-fit classifier shipment — 13 new in `tests/signals/test_gap_intelligence.py` covering `classify_mode_best_fit` heuristic + closed-set return invariant + `build_gap_intelligence_items` end-to-end wire-up, 1 net offset accounting for prior 1079). Previous +18 over the 1061 baseline from the B30 Phase 3 per-mode composite weight override shipment — 11 new in `tests/signals/test_composite_score.py` covering `resolve_composite_block` + builder `mode` kwarg, 7 new in `tests/config/test_parameter_store.py` covering `swing_composite` / `day_composite` JSON parsing + back-compat). Previous baseline before this commit: 1061 (no backend changes in B29 Phase 2 — visual hardening only on the frontend). Frontend `npm run test` → **315 passed** (41 test files; latest +1 from the B29 Phase 2 dashboard card visual uniformity shipment — `every_master_card_uses_borderRadius_xl_and_a_4px_role_tinted_borderLeft` lock-in in `tests/dashboard-role-color-language.test.tsx`, plus rewrites of `role_accents_each_define_a_distinct_surface_accent_for_the_borderLeft_marker` (was `..._with_distinct_borderAccents`), `every_master_card_renders_a_role_pill_AND_NOT_the_legacy_loud_rail_treatment` (was `..._renders_top_rail_AND_left_stripe_role_anchors`), and `swing_desk_primary_read_card_renders_on_the_canonical_card_shell` (was `..._renders_with_highlighted_role_border`) to pin the new canonical shell + 4px `borderLeft` + role pill instead of the retired Phase 2b loud rail treatment). Previous: B29 Phase 1 signal-card explanation refinements — backend was +6 from the previous baseline 1055 (latest +6 from the B29 Phase 1 signal-card explanation refinements — +2 in `tests/signals/test_technical_analyzer.py` for the VWAP anomaly guard (`test_vwap_anomaly_suppressed_when_value_deviates_far_from_price` verifies the suppression + the `vwap_anomaly_suppressed` WARNING log, `test_vwap_plausible_value_is_preserved` verifies plausible VWAPs pass through untouched), +4 in `tests/signals/test_confluence.py` for the `_format_rel_vol` two-decimal-precision contract on Weak/Strong volume chips — `test_weak_volume_label_uses_two_decimal_precision`, `test_weak_volume_label_floors_sub_005_to_lt_005x`, `test_strong_volume_label_uses_two_decimal_precision_and_times_sign`, `test_volume_label_helper_directly`). Frontend `npm run test` → **314 passed** (41 test files; latest +13 from the B29 Phase 1 signal-card explanation refinements — +8 in `tests/signal-evidence.test.ts` for the `buildVerdictTagReconciler` helper (bearish-verdict / bullish-verdict / neutral-verdict branches, parenthetical-stripping, geopolitical-drag append + dedupe, symmetric bullish-verdict-with-bearish-chips path), +2 in `tests/signal-evidence.test.ts` for the SignalEvidenceCard rendering contract (`bearish_verdict_with_bullish_chips_renders_reconciler_line` + `aligned_verdict_does_NOT_render_reconciler`), +3 in `tests/signal-evidence.test.ts` for the geopolitical drag badge gate (live-high-tier rendering, no-live-events absence, exposure-band-low absence). Previous baselines: B28 Phase 2c follow-up — backend 1055, frontend 301 (41 test files; latest +16 from the Sector behavioral reframe + Signal Validation Ledger relocation shipment (B28 Phase 2c) — +9 in `tests/shared-context-derivations.test.ts` for `classifyRotationProfile` + `rotationProfilePlainLine` (closed-set guarantees + 14-word banned-evaluative-language anti-leak), +5 net new in `tests/dashboard-role-color-language.test.tsx` (Section C title contract, rotation-profile closed-set, direction-aware tile borders, sub-card B/C/D/E wrapper contract, swing-desk Primary Read highlighted border), +2 net new in `tests/performance-no-combined-headline.test.tsx` (ledger link present logged-in, absent logged-out — UI gating matches API gating). Prior 285/41 baseline was the Dashboard Shared Context master card + bright rail-line border shipment (B28 Phase 2b) — 29 in the new `tests/shared-context-derivations.test.ts` covering the four classification helpers + 3 net new in the rewritten `tests/dashboard-role-color-language.test.tsx` covering the new master-card structure + tertiary ledger contract. Prior 253/40 baseline was the Dashboard role-color language + Shared Context copy redesign shipment (B28 Phase 2) — 11 in `tests/dashboard-day-helpers.test.ts`, 9 in `tests/dashboard-two-desk.test.tsx`, 1 updated in `tests/dashboard-swing-only.test.tsx`. Backend +13 over the 1038 long-term baseline: +8 from Phase 0 chatbot routing rules + +5 from Phase 1 dual-desk page-context serializer — see §13 for the full breakdown). Historical thread: **994 passed** (33 test files; +15 over the 161/30 baseline — all in three new lock-in files: `tests/refresh-session.test.ts` (5 tests covering `refreshSession()` happy/401/network-error paths + parallel-coalescing single-flight + post-failure cooldown), `tests/browser-api-fetch-refresh.test.ts` (5 tests covering the 401 refresh-and-retry contract in `browserApiFetch` — happy 200 / 401-refresh-200 / 401-refresh-fail / 401-refresh-401 / 403-never-treated-as-auth), `tests/surface-auth-error.test.ts` (5 tests covering the async refresh-then-mark behavior of `surfaceAuthErrorIfAny` across null / 200 / 403 / 401-refresh-success / 401-refresh-fail)). **`npm run build`** last verified: success. **2026-05-11 latest — D2 Historical Signal Validation Phase 3c-1 (public mirror on `/performance`):** Adds an unauthenticated mirror of the Phase 3b dashboard tab so homepage visitors can see the platform-wide track record without leaving the LOGGED-OUT safety perimeter. **Backend handler** `public_historical_validation_summary_handler` in `stocvest/api/handlers/signals.py` serves `GET /v1/signals/historical-validation/public-summary` with no auth requirement; it calls the same Phase 2 `HistoricalValidationService.summarize(user_id=None, ...)` against the platform record set (`scope_key == "PUBLIC"`). The response is the **trimmed projection** produced by a new `_public_summary_to_dict` helper: `{horizon, overall, by_mode, rows_examined}` with `by_decision` / `by_regime` / `by_pattern` / `by_readiness` / `by_direction` / `parameter_versions` deliberately omitted at the response layer (single-sourced enforcement of the assistant prompt's LOGGED-OUT golden rule: "Explain the FRAMEWORK, not the DECISION"). The handler rejects any `symbol` query parameter with a calm 400 (the prompt forbids per-symbol queries from logged-out surfaces; we surface the rejection rather than silently ignoring it so callers wiring it up see the error). `by_version` is silently dropped (engineering-internal). Defaults: `horizon` → `"1d"` (matches the marketing-facing swing track) and the window → trailing `PUBLIC_HISTORICAL_VALIDATION_DEFAULT_DAYS = 90` days when both `from` AND `to` are omitted. Partial windows (only `from` OR only `to`) return 400 — ambiguity rather than silent default substitution. Invalid `horizon` returns 400 (does not silently fall back). NaN accuracy serialization re-uses the Phase 3a `_bucket_stats_to_dict` (NaN → JSON `null`) so the same em-dash-never-0% UI contract holds. The standing `HISTORICAL_VALIDATION_DISCLAIMER` rides verbatim from `stocvest.api.legal_copy`. **Route registration** in `signals_http_dispatch` is order-independent w.r.t. the authenticated `/summary` path (the two route strings share a prefix only up to `/historical-validation/`; `public-summary` and `summary` diverge there) but the public route is registered first for organizational clarity. **Terraform** `infra/apigateway_6e.tf` adds `"GET /v1/signals/historical-validation/public-summary" = { module_key = "signals", authorizer = false }`. Plan was `1 to add, 0 to change, 0 to destroy`; applied successfully — `authorization_type = "NONE"` on the new route, pointing to the existing `signals` Lambda integration. **Frontend typed client** `frontend/lib/api/historical-validation.ts` gains `PublicHistoricalValidationSummary` / `PublicHistoricalValidationResponse` interfaces and `fetchPublicHistoricalValidationSummary({ horizon?, daysBack?, mode? })` that fetches **directly from `publicApiBaseUrl()` (no BFF)** — homepage visitors have no JWT cookie to forward, so the route exactly mirrors the existing `fetchPerformanceSummary` pattern in `lib/api/public-signals.ts`. The fetcher does NOT route 401s through `surfaceAuthErrorIfAny` (this is a fully anonymous endpoint; there is no session to refresh — a 401 here represents an upstream misconfiguration and should collapse to `null` rather than trigger the sliding-session flow). Returns `null` on any non-200, malformed body (missing `summary`), or non-JSON response so the homepage component renders a calm empty state rather than crashing. **UI** `frontend/components/performance-tracking-content.tsx` gets a new `PublicValidationSection` rendered between the four existing metric cards and the "Accuracy Tracking Starts Now" / live signals table, fetched alongside the existing summary + signals via `Promise.all` so a slow / failing call does not block legacy content. The section shows three cards (Overall accuracy 1d / Swing track 1d / Day track 1d) with monospace big-format accuracy values, "X correct of Y resolved" subtitles, the verbatim backend disclaimer, and a calm "Sign in to see the full stratified breakdown — decision state, regime, setup pattern, readiness, and direction — for your own tracked outcomes" CTA at the bottom that nudges visitors toward the dashboard view without overriding the logged-out boundary. **Tests:** +10 backend lock-ins in `tests/api/handlers/test_signals_public_historical_validation.py` covering no-auth-required 200 with an empty store (NaN → null, "NaN" token absent from raw JSON), response-shape exactness via `set(summary.keys()) == {"horizon", "overall", "by_mode", "rows_examined"}` (locks in the trimmed projection so any future drift surfaces immediately), every forbidden stratification (`by_decision`, `by_regime`, `by_pattern`, `by_readiness`, `by_direction`, `parameter_versions`) absent from the response, `symbol` query parameter rejected with 400 and "symbol" in the error message, user-scoped rows (any non-None `user_id`) excluded from the public response (cross-scope leak gate), default horizon = `"1d"` when omitted (verified by seeding a row with only `outcome_1d` set), default trailing-`PUBLIC_HISTORICAL_VALIDATION_DEFAULT_DAYS`-day window excludes rows older than the cap, partial-window query returns 400, `mode=swing` filter passes through to the store (day rows excluded, but `by_mode["day"]` still present in the response shape with `total_signals=0` per the Phase 1 vocabulary contract), explicit `from` / `to` + `horizon` round-trip echoed and used, invalid horizon string returns 400 with `"horizon"` in the message. +6 frontend lock-ins added to `tests/historical-validation-api.test.ts` covering happy path (URL contains `/v1/signals/historical-validation/public-summary`, does NOT contain `/api/stocvest/`, no `credentials` forwarded — fully anonymous), `horizon` / `mode` / `daysBack` query-string forwarding, 401 collapses to `null` AND does NOT trigger `surfaceAuthErrorIfAny` (no session to refresh on a public endpoint), 500 collapses to `null`, missing-`summary` malformed body collapses to `null`, non-JSON body collapses to `null`. Backend baseline **984 → 994 passed (+10)**, frontend baseline **188 → 194 passed (+6)**. `tsc --noEmit` zero new errors (24 pre-existing). `npm run build`: success. Terraform `apply`: success. **Prior commit — D2 Historical Signal Validation Phase 3b (UI surface on `/dashboard/signal-validation`):** Phase 3a (commit `0292989`) shipped the auth-gated `GET /v1/signals/historical-validation/summary` HTTP endpoint; this commit wires it to the dashboard. **BFF route** `frontend/app/api/stocvest/signals/historical-validation/summary/route.ts` is a thin `stocvestAuthedFetch` proxy (same pattern as `/api/stocvest/signals/me/history`) — forwards the JWT cookie, passes the query string verbatim, returns the upstream JSON unchanged. The backend already validates and emits calm 4xx envelopes so the BFF deliberately does no request shaping. **Typed client** `frontend/lib/api/historical-validation.ts` exposes `fetchHistoricalValidationSummary({ horizon, from, to, mode?, symbol? })` and `fetchHistoricalValidationByVersion(...)` plus matching TypeScript interfaces (`BucketStats`, `HistoricalValidationSummary`, `HistoricalValidationResponse`, `HistoricalValidationByVersionResponse`). Runtime validation is defensive manual type-guards (no Zod — the codebase does not depend on it; matches the existing `public-signals.ts` / `journal.ts` style). `null` accuracy from the wire is preserved verbatim in the parsed `BucketStats`. Lowercase tickers are upcased before being sent. `mode` is forwarded only when it is `swing` or `day`. On 401 the client routes the response through the shared `surfaceAuthErrorIfAny` (so the sliding-session refresh flow from commit `c23ae2b` works uniformly) and returns `null` so the caller can render a calm "sign in" state. Malformed responses (missing `summary`, missing `__all__` aggregate bucket, non-JSON body) also collapse to `null` so the dashboard never crashes on backend drift. Two display helpers ride on the module: `formatAccuracyPercent(null) → "—"` (verbatim em-dash) and `formatAccuracyPercent(0.6666) → "66.7%"`, and `buildTrailingWindow(daysBack)` produces ISO-8601 `{ from, to }` matching the trailing-Z form the backend accepts. **Component** `frontend/components/historical-validation-panel.tsx` (new) renders the aggregate view: a filter row (Horizon = `1h` / `1d`, Window = `30` / `60` / `90` days, Mode = `all` / `swing` / `day`), an "Overall directional accuracy" hero card showing the big-format accuracy, an "X correct of Y resolved (neutrals excluded from the denominator)" subtitle, and `rows_examined` + observed `parameter_versions` for provenance, then a responsive 300px-minimum-column grid of six `StratificationCard` instances (Decision state at entry, Macro regime at entry, Trading mode, Setup pattern, Trade Readiness, Direction). Each card lists its buckets with a friendly label (the `BUCKET_LABELS` map translates engine vocabulary `risk_on` → "Risk-on", `swing_composite` → "Swing composite", etc.; unknown keys fall through to their raw form rather than being silently dropped), a calm accent-colored progress bar whose width is the bucket's accuracy in `[0, 1]`, a monospace `66.7% (correct/resolved)` label, and a sample-size / neutral-count footnote. Buckets are emitted in a `preferredOrder` per stratification so the UI is stable across reloads. The standing `HISTORICAL_VALIDATION_DISCLAIMER` is rendered verbatim from the backend response at the bottom of the panel (italicized, muted) — single-sourced wording across the assistant prompt, the API, and the UI. Loading state uses the shared `CuteLoader`. 401 collapses to a calm "Sign in to view your historical signal validation." line. Backend errors / malformed responses collapse to "No data available for this window." instead of a crash. **Wiring on `/dashboard/signal-validation`:** the existing `SignalValidationPageClient` gets a new top-level view state (`view: "ledger" | "historical"`) and a pair of view-tab buttons ("Tracked outcomes (ledger)" / "Historical accuracy") rendered immediately under the page description; the entire existing ledger UI is wrapped in a `view === "ledger"` conditional and the new view renders `<HistoricalValidationPanel />`. The page also skips the ledger fetch when the user is on the historical view (saves one network round-trip per mount in historical mode). The assistant page-context publisher (`usePublishAssistantContext`) only forwards `trading_mode` when the user is looking at the ledger view — the historical view has its own internal mode filter, so leaving `trading_mode` unset there avoids misleading the assistant's page-context routing. **Tests:** +12 in `tests/historical-validation-api.test.ts` covering `fetchHistoricalValidationSummary` happy path (query string contains `horizon` / `from` / `to` and no `by_version`, response parses, `parameter_versions` echoes), `mode` + lowercase `symbol` passthrough (client uppercases the ticker before sending), `null` accuracy preserved verbatim (locks in the "em-dash, never 0%" UI contract end-to-end from the wire), 401 returns `null` and routes through `surfaceAuthErrorIfAny` (so the sliding-session refresh flow stays uniform), missing `summary` returns `null`, non-JSON body returns `null`; `fetchHistoricalValidationByVersion` appends `?by_version=true` and parses the per-version map with `__all__` carrying combined `parameter_versions=["v1","v2"]`, missing `__all__` collapses to `null`; `formatAccuracyPercent` renders "—" for `null` / NaN / Infinity and one-decimal-percent for finite values; `buildTrailingWindow(30)` produces a 30-day delta. Frontend baseline **176 → 188 passed (+12)**, backend **984/3** unchanged (no backend changes). `tsc --noEmit` zero new errors (the 24 pre-existing errors in `tests/order-confirmation-modal.test.tsx`, `tests/pdt-api.test.ts`, `tests/signal-evidence.test.ts`, `tests/signal-evidence-layers.test.ts` are unchanged from prior baselines). **No Terraform, no backend changes** — Phase 3b is entirely frontend; the existing Phase 3a HTTP route serves the BFF proxy unchanged. **Prior commit (2026-05-11) — D2 Historical Signal Validation Phase 3a (auth-gated HTTP surface):** Phase 2 (commit `d8f8cb8`) shipped the service layer over `SignalHistory`; this commit wires it to API Gateway as `GET /v1/signals/historical-validation/summary`. New handler `historical_validation_summary_handler` in `stocvest/api/handlers/signals.py` requires `rc.user_id` (every query is scoped to the calling user, so one tenant cannot read another tenant's tracked outcomes — locked in by `test_scope_isolates_users`) and parses five query parameters: `horizon` ∈ `{"1h","1d"}` (required), `from` / `to` as ISO-8601 datetimes (required; trailing-`Z` accepted as JavaScript's `Date.toISOString()` emits it; naive treated as UTC), optional `mode` ∈ `{"swing","day"}` (other values silently ignored — matches the existing `/v1/signals/me/history` convention), optional `symbol` (uppercased single ticker), optional `by_version` ∈ `{"1","true","yes"}`. Returns either `{ summary: <HistoricalValidationSummary> }` (default) or `{ by_parameter_version: { "__all__": ..., "v1": ..., "v2": ... } }` (when `by_version=true`), plus echoed `horizon` / `from` / `to` / `mode` / `symbol` and the standing `HISTORICAL_VALIDATION_DISCLAIMER = "Historical signal accuracy does not guarantee future results."` from `stocvest/api/legal_copy.py` (newly added; single-sourced so the dashboard tab (Phase 3b), public mirror, and assistant cannot drift from the verbatim wording). **JSON safety:** Phase 1's `BucketStats.accuracy` is `math.nan` when there are no resolved-non-neutral rows, and Python's default `json.dumps` emits the literal `NaN` (invalid JSON, crashes browsers). The handler's `_bucket_stats_to_dict` converts NaN → `None` at serialization time so the wire response is valid JSON and the UI renders "—" instead of a misleading "0%" — locked in by `test_empty_window_serializes_nan_accuracy_as_null` which asserts both `body["summary"]["overall"]["accuracy"] is None` AND `"NaN" not in resp["body"]`. **Validation contract:** invalid / missing `horizon` → `400 { error: "bad_request", message: "horizon must be '1h' or '1d'." }`; unparseable / missing `from` or `to` → `400` with a message naming the missing field; inverted window (`to <= from`) → `400 { ..., message: "to must be strictly after from." }`; no auth → `401`. **Route registration:** `signals_http_dispatch` in `signals.py` dispatches the new route; `infra/apigateway_6e.tf` adds `"GET /v1/signals/historical-validation/summary" = { module_key = "signals", authorizer = true }` (one new `aws_apigatewayv2_route.http` resource — Terraform `plan` showed exactly `1 to add, 0 to change, 0 to destroy`; applied 2026-05-11). **No new DynamoDB tables, no new Lambda functions, no new IAM** — the route lands on the existing `aws_lambda_function.api["signals"]` integration that already serves the rest of `/v1/signals/*`. **No BFF route or UI in this commit** — those are Phase 3b. **Tests:** +14 in `tests/api/handlers/test_signals_historical_validation.py` covering auth requirement, missing/invalid `horizon`, missing/invalid `from`, inverted window 400, trailing-Z ISO acceptance, happy-path 2/3 directional accuracy with disclaimer + `parameter_versions = ["v1"]`, empty-window NaN→null JSON serialization, per-user scope isolation, `mode=day` filter passthrough, lowercase `symbol=msft` → uppercased + filtered, horizon `1d` routing for rows with only `outcome_1h` populated, and the `by_version=true` per-version map shape (v1=2/2, v2=1c+1i, `__all__`=3/4 with `parameter_versions=["v1","v2"]`). Backend baseline **970 → 984 passed (+14)**, frontend **176** (unchanged — no UI yet). **Prior commit (2026-05-11) — D2 Historical Signal Validation Phase 2 (service layer over `SignalHistory`):** Phase 1 (commit `7d0faf4`) shipped a pure aggregator over an in-memory list of `SignalRecord`; this commit shipped the thin seam between that aggregator and the live store. New module `stocvest/api/services/historical_validation_service.py` exposes a `HistoricalValidationService` built on a narrow `SignalHistoryReader` Protocol — both `InMemorySignalRecorder` and `DynamoDBSignalRecorder` already satisfy it via their existing `get_signal_history(user_id, symbol, days, limit, mode, ledger_qualified_only)` signature, so production wiring is one constructor call (`HistoricalValidationService(get_signal_recorder())`) and tests use a list-backed `_StubStore` double with no boto3. The service takes an absolute `[from_at, to_at)` window (**lower bound inclusive, upper bound exclusive — same semantics as the GSI `scope_generated_at` predicate** `generated_at >= cutoff_iso`, so a caller passing `from_at = midnight ET today, to_at = midnight ET tomorrow` gets "today's signals" with no off-by-one), translates it into the recorder's trailing-N-day convention (computes `days_back = ceil((now - from_at) / 1 day) + 1` and clamps at `MAX_LOOKBACK_DAYS = 366` so a buggy "last 10 years" call cannot drain the GSI), then post-filters to the requested window. `MAX_ROWS_PER_QUERY = 1000` is a deliberate guardrail — typical per-user windows are a few hundred signals over 30 days; cursor-based pagination for admin cross-user analytics is a Phase 3+ concern. Two methods: `summarize(user_id, from_at, to_at, horizon, mode=None, symbol=None) → HistoricalValidationSummary` returns the Phase 1 summary for the window directly, and `summarize_by_parameter_version(...)` groups the same window by `parameter_version` and returns `{"__all__": <combined>, "v1": <...>, "v2": <...>, "unknown": <...>}` so Phase 3 can render a "v1 vs v2 vs all versions" diff without recomputing — rows with `parameter_version is None` or empty/whitespace land in `UNKNOWN_VERSION_KEY = "unknown"` so legacy rows (emitted before the column shipped) are never silently dropped. Inverted / zero-width windows (`to_at <= from_at`) short-circuit to an empty summary without touching the store. `user_id` / `symbol` / `mode` pass straight through to the store unchanged — the store is the auth perimeter and the service does not silently change scope. **No HTTP route, no BFF route, no UI in this commit** — those are Phase 3. **No DynamoDB schema changes** — the existing GSI `scope_generated_at` already supports the date-range query we need. Test surface: `tests/api/services/test_historical_validation_service.py` adds 14 lock-in tests covering empty-store NaN, inverted-bounds short-circuit, equal-bounds short-circuit, happy-path 2/3 accuracy with neutrals excluded from the denominator, **inclusive-lower / exclusive-upper window edge cases** (rows at `from_at` kept, rows at `to_at` dropped, before / after / inside boundary rows behave correctly), naive-`generated_at` UTC normalization, `user_id`/`symbol`/`mode` passthrough, `days_back` computed to cover `from_at`, `MAX_LOOKBACK_DAYS` clamp, horizon routing (a row with `outcome_1h` populated but `outcome_1d` pending counts at 1h, not 1d, but still shows in `rows_examined`), the `__all__` combined aggregate alongside per-version buckets, `unknown` bucketing for missing/blank `parameter_version`, an empty window producing only an `__all__` bucket with NaN accuracy, and window-filter-before-version-split (out-of-window v1 rows must not inflate the v1 bucket). Backend test baseline: **956 → 970** (+14 new, zero regressions). Frontend test baseline unchanged at 176 (frontend-only work was prior commit). **Prior commit (2026-05-11) — sliding-session refresh (Cognito REFRESH_TOKEN_AUTH):** the user reported being signed out while actively working. Root cause was a three-part architecture flaw: (a) `infra/cognito.tf` never sets `id_token_validity` so Cognito's default of 60 minutes was in effect, (b) `frontend/lib/auth/cognito.ts:signIn()` captured only the `IdToken` and silently discarded the `RefreshToken` from the same Cognito response, and (c) `SessionExpiryWatcher` decoded the JWT `exp` claim and scheduled `setTimeout(() => markSessionExpired("expired"), exp)` — firing at exactly the wall-clock moment regardless of how active the user was. The fix implements a proper sliding-session contract: a continuously-active user with a valid Cognito refresh token stays signed in indefinitely (default 30-day refresh-token lifetime), while a truly-inactive user is signed out only when the refresh token expires or is revoked. **Implementation:** (1) **server-side** — `frontend/lib/auth/cognito.ts` now captures `RefreshToken` from both `signIn()` and `respondToNewPasswordChallenge()` and exposes a new `refreshIdToken(refreshToken)` that calls Cognito with `AuthFlow: REFRESH_TOKEN_AUTH`. `frontend/lib/auth/session-cookies.ts` now writes a third httpOnly cookie `stocvest_refresh_token` (`maxAge = 30 * 24 * 60 * 60` matching Cognito's default refresh-token validity) alongside the existing `stocvest_auth_token` (httpOnly) and `stocvest_ws_token` (non-httpOnly mirror), and clears all three on `clearSessionTokenCookies()`. `frontend/lib/auth/refresh-token-cookie.ts` (new) exposes `refreshTokenCookieName()` + `readRefreshTokenCookie()` for server-only consumers. The login (`frontend/app/login/actions.ts`) and new-password (`frontend/app/new-password/actions.ts`) actions thread the refresh token through to the cookie setter. (2) **BFF route** — `frontend/app/api/auth/refresh/route.ts` (new) is a `POST` handler that reads the refresh-token cookie, calls Cognito `refreshIdToken()`, rewrites the ID + mirror cookies via `setSessionTokenCookiesFromIdToken(idToken)` (no `refreshToken` argument — Cognito does not rotate refresh tokens on `REFRESH_TOKEN_AUTH` in our pool config, so the existing 30-day cookie stays valid), and returns `200 { exp }` on success. On any failure (no cookie, Cognito throws, Cognito returns 200 without `IdToken`) it clears all three cookies and returns `401 { error: "refresh_failed" | "no_refresh_token" }` so the client falls back to the calm `SessionExpiredBanner`. Same-origin only, no request body read, runtime "nodejs" because the AWS SDK requires Node APIs. (3) **client refresh helper** — `frontend/lib/auth/refresh-session.ts` (new) exposes `refreshSession()` with **single-flight coalescing** (parallel callers — e.g. five concurrent 401s on a busy dashboard — all await the same in-flight POST so we never kick off a refresh storm; the in-flight slot clears on `setTimeout(0)` so callers that resolved against the same promise can still observe the result) and a **5-second failure cooldown** (after a refresh fails, subsequent calls short-circuit to `false` without hitting the BFF, because the calm banner is already the right UX surface and we don't want to hammer Cognito with a refresh-storm). (4) **proactive watcher** — `frontend/components/auth/session-expiry-watcher.tsx` is rebuilt: instead of firing `markSessionExpired()` at `exp`, it schedules a `setTimeout` to fire `refreshSession()` `REFRESH_LEAD_MS = 2 minutes` *before* `exp`, re-evaluates on `visibilitychange → visible` + `focus`, and only marks expired if the refresh call returns `false`. On a successful refresh it re-runs `evaluate()` to reschedule from the new `exp` automatically. (5) **reactive 401 retry** — `frontend/lib/api/browser-api-fetch.ts` now attempts `refreshSession()` on 401 and retries the original request **once** with the freshly-rewritten mirror cookie; only marks expired if the refresh itself fails OR the retry also returns 401. **403 is unchanged** — STOCVEST uses 403 for product-rule denials (PDT lockout, margin, paid-tier gating) where the user IS authenticated, and those must never trigger refresh-or-expire (locked in by `tests/browser-api-fetch-refresh.test.ts` and `tests/surface-auth-error.test.ts`). (6) **shared fallback** — `frontend/lib/auth/surface-auth-error.ts` (used by the six client fetch helpers that don't go through `browserApiFetch` — `fetch-symbol-bars`, `fetch-symbol-snapshot`, `fetch-macro-context`, `ticker-news-panel`, `public-signals`, the STOCVEST Assistant) is now async and attempts a refresh first; on success it returns `true` without marking expired (the caller's next interaction picks up the new cookie automatically) and only marks expired if the refresh fails. The six callers were updated to `await` it (the assistant uses the return value to short-circuit) or `void` it (the five fire-and-forget helpers). **No Terraform changes** — `ALLOW_REFRESH_TOKEN_AUTH` is already in `infra/cognito.tf` `explicit_auth_flows` for both app clients, and the 30-day refresh-token validity is Cognito's default which matches the cookie `maxAge`. **Dev mock unchanged** — `loginAsDevUser` mints an unsigned JWT with `exp = now + 365d` and never goes through Cognito, so it has no refresh token; the refresh route gracefully returns `401 { error: "no_refresh_token" }` if a dev-mock session ever hits it, and the watcher never schedules a refresh because the dev `exp` is a year out. **Behavior contract:** continuously-active user → stays signed in indefinitely up to the 30-day refresh-token lifetime, never sees the banner. Inactive for the full 60-minute ID-token lifetime → next interaction refreshes silently, user does not notice. Inactive ≥ 30 days OR refresh token revoked / Cognito rejects → calm `SessionExpiredBanner` appears, user clicks "Sign in", lands on `/login?reason=expired&next=...`. **Prior commit (D2 Historical Signal Validation Phase 1 — `7d0faf4`, 2026-05-11):** pure aggregation module over `SignalRecord` rows producing a frozen `HistoricalValidationSummary` stratified six ways (renamed from "Backtesting" at the user's request to mirror the assistant's verbatim disclosure phrase "Historical signal accuracy does not guarantee future results"); the new framing mirrors the assistant's verbatim disclosure phrase "Historical signal accuracy does not guarantee future results" and the existing `/dashboard/signal-validation` page. The deliberate rename also de-risks the legal posture: the module **measures real emitted signals** (the rows `signal_recorder.resolve_signals` populates with `outcome_1h` / `outcome_1d` against real subsequent Polygon prices) — it does not generate synthetic signals against historical bars, and `BucketStats` is restricted to directional accuracy (`correct / (correct + incorrect)` with neutrals excluded from the denominator, matching the existing `GET /v1/signals/performance/summary` definition so the historical view and live tracked-outcomes view never disagree on the same data). Implementation: `stocvest/signals/historical_validation.py` — a pure, deterministic aggregator that takes a list of `SignalRecord` and a horizon (`1h` or `1d`) and returns a frozen `HistoricalValidationSummary` with `overall` plus six stratifications: Decision state at entry (`actionable` / `monitor` / `blocked` / `unknown`), Macro regime at entry (`risk_on` / `neutral` / `risk_off` / `avoid` / `unknown` — engine vocabulary, matches what `regime_label_at_entry` actually stores), trading Mode (`swing` / `day`), Pattern family (`swing_composite` / `orb` / `vwap` / `momentum` / `gap_with_catalyst`; unknown patterns overflow into `other` so nothing silently vanishes), Trade Readiness bucket (`high` ≥ 70 / `moderate` 40–69 / `low` < 40 — same thresholds as the Evidence card), and Direction (`bullish` / `bearish` / `neutral`). Empty / all-neutral strata return `accuracy = NaN` so the UI renders "—" rather than a misleading "0%". `rows_examined` and the sorted set of observed `parameter_versions` ride along on the summary so the UI can show users exactly which rules-bundle the number describes. **No new I/O, no new infra, no UI in this commit** — the next phases plug this aggregator into a thin service layer (Phase 2 — date-range slice from `SignalHistory` keyed by `parameter_version`), then into `GET /v1/signals/historical-validation/summary` + a new tab on `/dashboard/signal-validation` and an optional public mirror on `/performance` for the overall + by-mode bucket only (Phase 3), then into cross-version diffs (Phase 4 — rules-bundle A vs B). **Explicit non-goals:** win-rate / expectancy / dollar P&L (product never publishes these); re-resolution at synthetic horizons (T+5d, T+10d, T+30d) using simulated bars; anything that frames validation as a forecast. **Prior commit (B26 STOCVEST Assistant — in-memory-only conversation state — `1e0e4a6`, 2026-05-11):** removed the `sessionStorage`-backed conversation persistence so a fresh conversation starts on every page load / cross-surface navigation / login / logout / session expiry; see §2 below. STOCVEST Assistant is now mounted in the **root layout for every visitor** (logged-in and anonymous) — authenticated traffic hits `POST /v1/signals/assistant/chat` with the JWT + page context, and anonymous marketing visitors hit the new **`POST /v1/public/assistant/chat`** (no authorizer) so prospects on `/`, `/login`, `/signup`, etc. can ask what STOCVEST is, how it differs from signal-alert services, and for explanations of finance terms while the locked system prompt's PUBLIC MODE section refuses all trade advice, predictions, and accuracy claims. **2026-05-11 latest — in-memory-only conversation state:** the assistant no longer persists conversation history to `sessionStorage`. A fresh conversation starts on every page load (including hard refresh), every cross-surface navigation, every login/logout, every session expiry, and every explicit "Sign out" click. This closes two regressions reported by the user: (1) on refresh, stale conversation re-appeared even when the user expected a clean surface, and (2) on in-place login (auth flip without navigation), the previous hydrate effect was clobbering `prevAuthRef` — the ref the auth-change reset effect depended on — so the homepage public-mode conversation survived into the authenticated context. The simplest, safest fix was to remove the persistence layer entirely so no stale state can survive any kind of transition. Implementation: `stocvest-assistant.tsx` drops the `readPersistedState`/`writePersistedState`/`removePersistedState` helpers and the hydrate-on-mount + persist-on-change `useEffect`s; the component now has one one-shot mount effect that opportunistically wipes legacy `sessionStorage` data (`stocvest_assistant_state_v1`) for existing tabs, plus the three reset effects (pathname-change, auth-flip, session-expiry/explicit-clear) that already existed but now own `prevAuthRef` exclusively so the auth-flip detection is reliable. `lib/assistant/session-reset.ts` still exports `clearAssistantSession()` (logout buttons call it before the server-action redirect) and the `stocvest:assistant-reset` event — both continue to fire because they are defensive against any legacy data and against any future re-introduction of persistence. **2026-05-11 (earlier same day) — two-context safety perimeter:** the locked prompt now opens with a CRITICAL CONTEXT AWARENESS RULE that splits the world into two top-level safety contexts (LOGGED-OUT = `session_mode=public`, LOGGED-IN = `session_mode=authenticated`) and forbids the LLM from implying access to features unavailable in the current context. PUBLIC MODE RULES was strengthened with an explicit per-symbol MUST-NOT list (no evaluating any specific stock, no Evidence card reference for a specific stock, no Trade Readiness score for a specific symbol, no naming a specific layer as "currently blocking" a specific ticker, no sounding like a live demo of unavailable features) keyed by concrete ticker examples (AAPL / TSLA / NVDA / MSFT) so the LLM pattern-matches any ticker the visitor names. A HOMEPAGE-SAFE REFUSAL TEMPLATE — "I can't assess individual stocks here, and I don't give buy or sell answers. What I can explain is how STOCVEST decides whether trading conditions are aligned once a symbol is evaluated inside the platform." — is in the prompt verbatim so the LLM falls back to it for any per-symbol question on the homepage rather than improvising. The LOGGED-OUT GOLDEN RULE — "explain the FRAMEWORK, not the DECISION" — is also in the prompt verbatim. TONE & STYLE now also says "When refusing a request, explain WHY the boundary exists" so refusals build trust instead of feeling dismissive. The locked system prompt has now been **upgraded to an explanatory-voice contract** (2026-05-11 earlier the same day): it opens with PRIMARY GOAL + CORE PRODUCT PHILOSOPHY ("STOCVEST does not attempt to predict markets — STOCVEST evaluates alignment, risk, and confirmation, and decides WHEN trading is statistically worth risking capital", "Silence is an intentional system state"), enumerates the six real layers by their product names (Technical / News / Macro / Sector / Geopolitical / Market Internals), references the verbatim Decision lines from `lib/signal-evidence/trade-decision.ts` (`✅ Actionable — passes risk/reward and confirmation thresholds`, `⚠️ Monitor only — confirmation and/or risk gates are not fully cleared`, `🚫 Blocked — fails minimum synthesis and risk gates`), describes the shipped Macro pulse states (Unavailable / Elevated / Upcoming / Known and absorbed), the shipped sector chip labels (Confirming / Non-confirming / Mixed; Risk-on / Defensive / Mixed / Narrow), the real Trade Readiness 0-100 scale, the Layer alignment High/Moderate/Low buckets, and adds dedicated SUPPRESSION & GATING LOGIC, REGIME TRANSITIONS, BACKTESTING & VALIDATION, USER INTERACTION RULES (MAY / MUST NOT), and PAYWALL AWARENESS sections — while **explicitly forbidding invented concepts** that do not ship in STOCVEST: "System Confidence", "VIC", "Volatility Control", "Supportive / Neutral / Hostile" volatility states, "Symbol Readiness Score", "Sector Fragmented", a 0.0-1.0 readiness scale, or any made-up X% accuracy figure (the only validated outcomes live on `/performance` and `/dashboard/signal-validation` with the standing disclaimer "Historical signal accuracy does not guarantee future results."). All existing operational guardrails are preserved: the banned self-limitation phrases ("I don't have", "I can't see", "at this moment", "right now I lack", …), the banned response shapes block with the production BAD/GOOD pair, the three-mode structure (General / Contextual / Public), and the foundational principle "To help users understand how STOCVEST thinks — not to tell them what to do." Every dashboard page (`dashboard`, `dashboard/scanner`, `dashboard/signals` (+ history), `dashboard/watchlists`, `dashboard/signal-validation`, `dashboard/earnings`, `dashboard/portfolio`, `dashboard/journal`, `dashboard/performance`, `dashboard/options`, `dashboard/futures`, `dashboard/crypto`, `dashboard/settings`, `dashboard/legal`) calls `usePublishAssistantContext`; **cross-surface conversations now isolate by URL pathname** — the assistant pulls `usePathname()` from `next/navigation` and wipes the persisted conversation the moment the pathname changes, so navigating from `dashboard/signals/layers` (TTD) to `/`, `/dashboard`, `/dashboard/scanner`, `/login`, or any other route starts a fresh conversation. Pathname-based detection also catches routes that publish no page context at all (marketing surfaces like `/`, `/login`, `/signup`), which a `pageContext.page`-only signal would have missed. Hydrate also force-clears any legacy persisted state whose `isAuthenticated` field is missing (data written by a build before the field existed), so a logged-in conversation can never re-surface on the anonymous home page. The conversation also resets on logout (sign-out buttons fire `clearAssistantSession()` before the server action) and on session expiry (the assistant subscribes to `subscribeSessionExpired`).

---

## 1. Status at a glance

| Track | Status | Notes |
|--------|--------|--------|
| Core data + indicators | ✅ | Polygon client, `stocvest/indicators/` |
| Swing + day-trading signals | ✅ | `stocvest/signals/` (layers, scanner, briefing, journal engine, PDT tracker); **swing vs day composite** — same six layers; `SwingTechnicalAnalyzer` + daily bars; `POST /v1/signals/composite/swing`; dashboard **Day trade / Swing trade** toggle (`localStorage` `stocvest_trading_mode`) |
| Brokers | ✅ | Mock, IBKR, E*TRADE adapters; factory; PDT hook |
| HTTP API (Lambdas) | ✅ | Market, **signals** (HTTP submodule + public/user record routes + **`GET /v1/signals/founding-members`** paid-plan counter for landing + **`GET /v1/signals/me/history`** with optional **`mode=day|swing`** for the signal validation ledger + **`POST /v1/signals/assistant/chat`** (authenticated STOCVEST Assistant — locked server-side system prompt, whitelisted page-context tail, `has_ai_explanations` gating, deterministic fallback for free users; Anthropic **Messages API** via the proper `system` parameter, not a concat-into-user trick) + **`POST /v1/public/assistant/chat`** (unauthenticated STOCVEST Assistant for the marketing surface — same locked prompt with PUBLIC MODE section activated via `session_mode=public`; `page_context` is ignored server-side; deterministic-intro fallback when Claude is unreachable so the homepage chat never appears broken)), **brokers** (**broker portfolio** `POST /v1/portfolio/holdings|summary|allocation`), scanner, journal, PDT, orders, auth; **`UserProfile` beta override** (`beta_full_access`, `beta_access_until`, `beta_access_granted_at`) with **`has_full_access`** / **`has_ai_explanations`**; admin **`PATCH /v1/admin/users/{user_id}/beta-access`** (same auth as signal analysis); **`PATCH /v1/users/me`** blocks client writes to subscription + beta fields; **HTTP audit capture** — `audit_capture.py` + `audit_store.py` (DynamoDB when **`DYNAMODB_AUDIT_EVENTS_TABLE`** set, else in-memory), `_with_cors_and_audit` on HTTP dispatch; admin **`GET /v1/admin/audit/users/{user_id}`** and **`GET /v1/admin/audit/sessions/{session_id}`**; clients may send **`x-stocvest-session-id`** for session-scoped replay; journal `user_id` from JWT only; free-text **`text_sanitize`**; **`log_privacy`** for CloudWatch-safe logs |
| Frontend (Next.js) | ✅ | **STOCVEST Assistant** chatbot mounted globally in **`app/layout.tsx`** for **every** visitor (`isAuthenticated` derived from `getServerSession()` selects which BFF endpoint to call — authenticated → `/api/stocvest/signals/assistant/chat` with full page context, anonymous → `/api/stocvest/public/assistant/chat` with messages only). Anonymous marketing visitors on `/`, `/login`, `/signup`, etc. can now chat — locked system prompt's PUBLIC MODE section answers "what is STOCVEST?", "how is it different from signal-alert services?", and general finance/term explanations while continuing to refuse all trade advice and predictions. (`components/assistant/*` + `lib/assistant/*`): floating lens launcher with slow pulse, glassmorphism panel with **no-bubble** vertical conversation rail (now with **strong role-based visual distinction** — user turns sit in an accent-tinted panel with a solid accent-block "YOU" chip and right-aligned text, STOCVEST turns sit in a surface-muted panel with a quiet tone-tinted label and left-aligned text; the asymmetry plus contrasting backgrounds make the two roles unmistakable even in monochrome), **six-layer constellation context strip**, word-fade reveal on fresh assistant turns (respects **`prefers-reduced-motion`**), explicit **General / Contextual** mode badge; **cross-page isolation** — the assistant tracks the page identifier each conversation belongs to and wipes the history the moment the user navigates to a different page so an answer about TTD on Signals never leaks into a question on Dashboard; **session reset** — sign-out buttons in `sidebar.tsx` + `mobile-nav-drawer.tsx` call `clearAssistantSession()` before the server action and the assistant subscribes to `subscribeSessionExpired` so token expiry / any 401 also clears the conversation; **every** logged-in dashboard page publishes whitelisted context via `usePublishAssistantContext` so the chatbot is contextual mode-aware everywhere the user can navigate — **signals** (`dashboard/signals/layers` or `/history`) publishes per-symbol context (decision_state, decision_rationale, layer_status, trade_readiness, risk_reward, market_regime, layer_alignment_pct, analysis_status), **scanner** (`dashboard/scanner`) publishes the multi-symbol overview context (`scanner_focus`, `market_open`, gap-with/without-catalyst counts, `ranked_setups_count`, qualitative `top_setups`/`top_gaps_with_catalyst` capped at 3 each with bucketed strength/quality — no raw scores, `swing_setups_suppressed`, `setups_empty_message`), **home dashboard** (`dashboard`) publishes `trading_mode: "swing"` + `market_regime` + `ranked_setups_count`, **signal-validation** (`dashboard/signal-validation`) publishes `trading_mode` (from the active **swing/day** tab), **options** (`dashboard/options`) publishes the active `symbol`, **crypto** (`dashboard/crypto`) publishes the active `symbol`; **watchlists** / **earnings** / **portfolio** / **journal** / **performance** / **futures** / **settings** / **legal** publish just the page identifier (which is enough — the locked system prompt's "page identifier only" rule activates and the LLM answers in calm general terms about what that page does); the signals page derives the authoritative Decision through the shared **`lib/signal-evidence/trade-decision.ts`** (single source of truth used by both `signal-evidence-card.tsx` and the assistant context). Auth, dashboard redesign, **Gap Intelligence** scanner panel + **morning brief** on scanner paths when enabled (not on home dashboard); scanner/signals/**watchlists**/**dashboard/portfolio** (broker) / journal/options/crypto/futures, **public landing** (`landing-page.tsx` / `app/page.tsx` — hero, day vs swing modes, live engine **SWING/DAY** tabs, differentiation, workflows, transparency, comparison, yesterday’s signals, pricing + **founding-member** scarcity from **`GET /v1/signals/founding-members`** via **`lib/api/founding-members.ts`**, footer; no home-dashboard morning brief), legal pages; **`/dashboard/signal-validation`** (tracked signal outcomes; legacy **`/portfolio`** redirects); BFF **`/api/stocvest/signals/**`** (swing composite, **`composite/real`**, user history, record by id) + **`/api/stocvest/portfolio/**`** (broker portfolio only); **signals page** uses server **`composite/real`** for layer scores (no client `bullishBias` heuristic); **after-hours research panel** (`signals-after-hours-panel.tsx`) for closed-market insufficient-data states; **compact PDT status pill** beside Market/VIX (`pdt-status-pill.tsx`); **Market Pulse** strip (SPY/QQQ/VIX + regime from scanner overview) replaces the embedded **Market Intelligence** headline grid; **on-demand `NewsPanel`** (`news-panel.tsx`) — fetch on open, per-symbol **2m** client cache, wired from dashboard signal cards, evidence (incl. “View all news”), signals page; **`vercel.json`** security headers (+ HSTS on **stocvest.app**) |
| Journal automation (B1) | ✅ | `journal_order_hooks` on order submit (open/close rows, optional signal fields), `GET /v1/journal/analytics`, entry GET/PATCH, journal UI + scanner → portfolio order prefill |
| Onboarding + legal acknowledgment (B2) | ✅ | `UserProfile` legal + onboarding fields; `GET`/`PATCH /v1/users/me`; mandatory **legal acknowledgment modal** + optional **onboarding wizard** on `app/dashboard/layout.tsx`; BFF `/api/stocvest/users/me` |
| Order safety (Step 8) | ✅ | `order_safety.py`, validate/submit BFF, confirmation modal, paper/live mode |
| **Watchlists (B4)** | ✅ | `stocvest/data/watchlist_store.py` (**`scan_default_watchlists`**, **`find_users_with_default_watchlist_symbol`** for scheduled jobs only), `scan_symbols.get_scan_symbols`, `GET`/`POST`/`PATCH`/`DELETE` watchlists + default symbols API (brokers Lambda), dashboard **Watchlists** page, BFF `/api/stocvest/watchlists/**`, **dashboard scanner** loads default symbols in parallel with PDT and passes them into **`fetchScannerOverview`** / **`loadScannerDataWithoutBrief`**; scanner gap path defers Dynamo until Polygon 401/403 fallback; **B14** scheduled scan merges platform default-watchlist symbols (cap 30) + config + **`SYSTEM_DEFAULTS`** (cap 40 total); **`watchlist_scanner_alerts`** + **`UserProfile.email`** (optional Dynamo mirror) for best-effort signal emails within **2s** `asyncio.wait_for` + **4h** dedupe via **`had_signal_email_for_symbol_within_hours`** |
| **Alerts email (B3)** | ✅ | `stocvest/data/models.py` alert enums + prefs/record, `alert_store.py`, `stocvest/services/email_service.py` (SES), `alert_trigger.py`, `GET`/`PATCH /v1/alerts/preferences`, `GET /v1/alerts/history`, non-blocking signal + PDT emails; settings **Alert Preferences** + `docs/DEPLOYMENT.md` SES notes; Lambda IAM `ses:SendEmail` |
| Legal compliance pass | ✅ | `GlobalDisclaimer`, `SignalDisclaimerChip`, signal-oriented copy, public API field names + `disclaimer` |
| D1 Signal outcome pipeline | ✅ | `SignalRecord` / `SignalHistory`, `signal_recorder` (record + **`resolve_signals`** using Polygon **1m aggregates** at T+1h and next RTH 4:00 PM ET close for 1d via **`get_evaluated_price_after_signal`**), `GET /v1/signals/recent` + performance summary (**`correct_direction_count`** / **`incorrect_direction_count`** / **`neutral_direction_count`**), **`GET /v1/signals/records/{id}`**, **`GET /v1/signals/me/history`**, **`GET /v1/signals/me/records/{id}`**; resolution Lambda + EventBridge **`stocvest-signal-resolution`** **`ENABLED`**, **`rate(30 minutes)`** → **`stocvest-development-api-signal_resolution`**. Apply **2026-05-03** (see `docs/D1_SIGNAL_RESOLUTION_SCHEDULE.md`). **Optional later:** weekly horizon, extra analytics. |
| News ingestion pipeline | ✅ | **Combined** Benzinga WebSocket + SEC EDGAR 8-K feed in one worker: `stocvest/data/edgar_client.py`, `stocvest/data/news_triage.py`, `stocvest/workers/news_worker.py`, `stocvest/workers/news_consumer_lambda.py`; SQS queue `stocvest-news-triage-queue`; EDGAR filings map into canonical `NewsArticle`; Lambda consumer scores pre-triaged articles with Claude and best-effort pushes scored rows to Redis. |
| Terraform / AWS apply | 🚧 | **Development** now includes the news SQS/Lambda pipeline in `infra/` plus **`AuditEvents`** DynamoDB, **`DYNAMODB_AUDIT_EVENTS_TABLE`** on API Lambdas, and API Gateway routes for **`GET /v1/signals/founding-members`**, admin **beta** / **audit** paths, and **`POST /v1/signals/assistant/chat`** (apply to create/update — no new secrets needed: Anthropic key reused from `stocvest/lambda-runtime`). **Still pending for always-on ECS runtime:** publish a worker image and set `news_worker_container_image` / `news_worker_desired_count` so the Fargate service is actually created; also keep production/stage promotion, secret rotation, S3 artifact bucket policy review, and Cognito/Vercel env alignment in sync per environment. |
| Phase 7 (E2E, audits, paper validation) | 🚧 | **HTTP/API audit trail to DynamoDB** (`AuditEvents`, redacted summaries) + admin query routes shipped; E2E suite, security review, load tests, extended paper validation still open (**BACKLOG P1**) |

---

## 2. Implemented (where to look)

**Backend (`stocvest/`)**  
**Also shipped:** **STOCVEST Assistant chatbot** — **`stocvest/signals/assistant_prompts.py`** (system prompt held verbatim as `ASSISTANT_SYSTEM_PROMPT`; **2026-05-11 explanatory-voice upgrade** restructured the prompt into product-anchored sections — PRIMARY GOAL, CORE PRODUCT PHILOSOPHY ("Silence is an intentional system state"), ANALYSIS LAYERS (the six real product layer names — Technical / News / Macro / Sector / Geopolitical / Market Internals), MARKET REGIME (UI Bullish/Neutral/Bearish + engine risk_on/risk_off/neutral/avoid), SECTOR CONFIRMATION (Confirming / Non-confirming / Mixed; Risk-on / Defensive / Narrow), MARKET PULSE (Unavailable / Elevated / Upcoming / Known and absorbed; pulse never authorizes trades), INTERNALS & VOLATILITY (VIX is a signal inside Internals, NOT a standalone "VIC"), TRADE READINESS (0-100 scale, never 0.0-1.0), DECISION STATES (verbatim lines from `lib/signal-evidence/trade-decision.ts`), ALIGNMENT & CONFIDENCE (Layer alignment buckets + Trade Readiness, never an invented "System Confidence"), SUPPRESSION & GATING LOGIC (with the dashboard's exact empty-state copy: "No active swing setups right now", "System posture: Waiting for alignment", "Swing suppressed — risk-off tape", "Signal suppressed — regime not cleared"), REGIME TRANSITIONS (probabilistic language only — never forecast a flip), BACKTESTING & VALIDATION (route to `/performance` and `/dashboard/signal-validation` with the standing disclaimer; never invent a hit-rate), USER INTERACTION RULES (MAY / MUST NOT lists), PAYWALL AWARENESS (swing_pro / swing_day_pro / active beta — the LLM knows it's the paid path and never describes the deterministic free-tier fallback as an error), TONE & STYLE (frame inactivity as intentional and protective; frame silence as discipline; frame patience as skill), END GOAL & FOUNDATIONAL PRINCIPLE; **explicitly forbids invented concepts that do not ship**: "System Confidence", "VIC", "Volatility Control", "Supportive / Neutral / Hostile" volatility states, "Symbol Readiness Score", "Sector Fragmented", a 0.0-1.0 readiness scale, or any made-up "X% accuracy" figure; forbids bullet lists / numbered lists / section headings / markdown formatting in replies by default; **expanded ban-list of self-referential phrases** ("I don't have", "I can't see", "I can't access", "I would need to see", "to give you a precise explanation I would need", "at this moment", "right now I lack", etc.) with an explicit "stop and rewrite" instruction so the LLM cannot fall back to "I don't have access to live page data" even when the conversation history contains a different symbol than the current page; preserves the **banned response shapes** block with the concrete BAD/GOOD pair drawn from the exact failure mode we saw in production (LLM apologizing about TTD swing details after the user navigated to the home page); adds an explicit fallback rule when only a symbol/page is in context: explain STOCVEST in calm general terms instead of refusing; `sanitize_messages` drops any non-`user`/`assistant` turn so a client cannot inject a fake `system` role, caps single message at **2000 chars**, trims history to **12** turns; `serialize_page_context` whitelist-emits only known keys, including the new **`analysis_status`** = `loaded` \| `loading` \| `unavailable` \| `insufficient_data` so the assistant can distinguish "decision visible on screen" from "user has selected a symbol but the analysis has not loaded yet"), **`stocvest/signals/assistant_chat.py`** (`AssistantChatService.reply` builds `system = ASSISTANT_SYSTEM_PROMPT + serialize_page_context(...)` and posts to Anthropic Messages API with the proper top-level `system` parameter; free users — no `has_ai_explanations` — receive deterministic copy and never reach Claude; no conversation cache), **`assistant_chat_handler`** in **`stocvest/api/handlers/signals.py`** dispatched under **`POST /v1/signals/assistant/chat`** (returns `text`, `source`, `mode`, `upgrade_available`, `disclaimer`). **Tests:** **`tests/api/handlers/test_signals_assistant_chat.py`** (unauth 401, invalid body 400, free-user deterministic path, mode-from-context, paid-user AI path with service mock, sanitizer drops client-injected `system` role). **`AuditEvent`** + **`api/services/audit_store.py`** / **`api/services/audit_capture.py`** (writes when **`DYNAMODB_AUDIT_EVENTS_TABLE`** is set); **`get_founding_member_count`** + **`GET /v1/signals/founding-members`** (counts **paid** `swing_pro` / `swing_day_pro` / legacy `founding_*` only; **free** rows never increment the shared “first **100**” pool); admin **`PATCH /v1/admin/users/{user_id}/beta-access`** + audit query routes in **`orders.py`**; **`scripts/beta_access.py`**. **Tests:** **`tests/api/test_audit_capture.py`**, **`tests/api/test_user_profile_plan.py`**, **`tests/api/test_users_me.py`**. **Reliability pass:** `stocvest/data/benzinga_client.py`, `stocvest/data/ticker_name_resolver.py`, and `stocvest/workers/geo_themes_updater.py`; composite engines now merge Benzinga data, `NewsAnalyzer` returns neutral available states (not unavailable) for no-news windows, and `GeoAnalyzer` includes structural baseline + cached daily themes.  
`data/` (models incl. **`UserProfile`** legal/onboarding + optional **`email`** + **beta** fields (Dynamo mirror for scheduled alert delivery), **`SignalRecord`** (optional per-layer **JSON snapshot** columns + **`parameter_version`** for tuning; **`status`** `active` \| `incomplete` for incomplete reference-level payloads; optional **signal-validation ledger** fields such as **`closed_at`**, session dates, rationale, exit labels, MAE/MFE, **`gate_status`**, **`setup_type`**, **`exit_rule`**, **`hold_duration_minutes`**), **`AlertPreferences`** / **`AlertRecord`** / **`AlertType`**, **`EconomicCalendarEvent`**, **`watchlist_store`** + **`alert_store`**, **`edgar_client.py`** (SEC 8-K Atom polling + CIK→ticker cache + `EdgarFiling` → `NewsArticle` mapping), **`news_triage.py`** (high-signal keyword/category/watchlist gate + duplicate suppression), Polygon — snapshot parser may drop `day` OHLC/VWAP vs **`lastTrade.p`** only when last is a positive price and any session field is **>5×** off; missing last keeps session bar; **`get_evaluated_price_after_signal`** for D1 bar-based outcomes; **`get_economic_calendar_for_day`** on Benzinga economics when tier allows; **`BenzingaNewsStream`** in `polygon_client.py` for long-lived websocket ingestion), `indicators/`, `signals/` (sentiment, macro, geo, composite, **real layer analyzers** — `technical_analyzer`, **`swing_technical_analyzer`** (daily SMA/MACD/base/volume regime), `news_analyzer`, `macro_analyzer`, `sector_analyzer`, `sector_mapper`, `geo_analyzer`, `internals_analyzer`; **`confluence`** normalization helpers + multi-signal alignment in `confluence.py`, **`gap_intelligence`** + dynamic news lookback + **`news_catalyst_detector`** listicle-aware noise + company-name headline fallback, **`morning_brief`**, **`trade_journal`** + **`compute_journal_analytics`**, briefing markdown generator still in `daily_briefing.py` for legacy tests), `brokers/` (adapters, gateways, OAuth), **`services/email_service.py`**, **`services/alert_trigger.py`**, **`api/services/watchlist_scanner_alerts.py`** (scheduled intraday/EOD setup → watchlist user email; **4h** dedupe), **`api/services/news_relevance.py`** (**`calculate_article_relevance`**, **`categorize_article`**, **`deduplicate_articles`**, **`source_credibility_meta`** — dashboard headline ranking; distinct from composite **`news_analyzer`** filter), **`api/services/news_quality_filter.py`** (**`passes_market_intelligence_gate`** for `/v1/market/news` pool vs **`is_quality_article`** for analyzers), **`api/services/news_panel_format.py`** (panel **`age_label`**, source bucket + **`source_label`**, sentiment score/label, catalyst type for ticker news), `api/` (handlers incl. **`news_handler`** in **`market_data.py`** — **without `symbol`:** Polygon news limit **50**, **4h** lookback (**24h** widen when empty), merged liquid + watchlist tickers (cap **30**), relevance sort + topic dedupe, response **`{ "headlines": [...] }`** with at most **`min(limit, 20)`** rows (`limit` query **1–1000**), full headline fields incl. **`category`**, **`catalyst_category`**, **`relevance_score`**, **`credibility`**, **`matches_watchlist`**; **with `symbol`:** query **`days`** **1–20** (default **20**), **`limit`** **1–100**, rolling Polygon window, response **`{ "symbol", "articles", "has_recent_news", "recent_cutoff_hours", "total_found", "oldest_included" }`** with lean panel items (**`source`**, **`source_label`**, **`sentiment_score`**, **`sentiment_label`**, **`catalyst_type`**, **`is_recent`**, **`age_label`**, **`url`**); **`signal_resolution`**, **`signals`** → **`signals_http_dispatch`** for signal HTTP surface with **dynamic per-layer `reasoning`** in composite `contributions`, **`GET /v1/signals/analysis`** admin/internal tuning aggregate, **`watchlists`**, **`alerts`**, **`GET`/`PATCH /v1/users/me`**, **`text_sanitize.py`**, **`services/signal_recorder.py`** (public lists exclude **`incomplete`** rows), **`services/composite_market_context.py`** (Polygon market status for swing composite insufficient-data responses), **`services/morning_brief_fetch.py`** (VIX via **`get_vix_snapshot_with_fallback`** / **`VIX_SNAPSHOT_FALLBACK_SYMBOLS`**), **`services/scanner_scheduled_pipeline.py`** (platform watchlist symbol merge + notify), **`services/journal_order_hooks.py`** (auto journal on fill/close; swallowed errors), **`services/gap_intelligence_news.py`** global + per-ticker Polygon news merge, **`POST /v1/scanner/gap-intelligence`**, structured **`POST /v1/signals/day/briefing`** / **`POST /v1/scanner/briefing`**, **`POST /v1/signals/swing/composite`** (client-supplied layer scores) and **`POST /v1/signals/composite/real`** (intraday bars, server-side layers via `real_composite_engine.py`) and **`POST /v1/signals/composite/swing`** (`swing_composite_engine.py`, daily bars, extended news/macro/geo windows) return **`status: insufficient_data`** (HTTP 200) when fewer than three layers are available — no composite record or alert; **`lambda_dispatch.py`** now routes SQS news-consumer events; **`workers/news_worker.py`** is the long-running ECS entrypoint and **`workers/news_consumer_lambda.py`** is the SQS-triggered Claude scoring consumer; journal **`GET /v1/journal/analytics`** + entry by id + PATCH notes, auth, `legal_copy.py`, order safety integration), **`utils/log_privacy.py`**. **Sliding-session auth surface** (2026-05-11) — the user's "I was actively working and got logged out" regression is fixed by **`frontend/lib/auth/refresh-token-cookie.ts`** (server-only refresh-cookie name + reader), **`frontend/lib/auth/refresh-session.ts`** (client-side `refreshSession()` with single-flight coalescing + 5-second post-failure cooldown), **`frontend/app/api/auth/refresh/route.ts`** (BFF POST handler that reads the httpOnly `stocvest_refresh_token` cookie, calls Cognito `REFRESH_TOKEN_AUTH` via `refreshIdToken()` in `frontend/lib/auth/cognito.ts`, rewrites the ID + mirror cookies, returns `200 { exp }` on success or `401` after clearing all cookies on failure), and a **rebuilt `frontend/components/auth/session-expiry-watcher.tsx`** that schedules a refresh ~2 min before the JWT `exp` instead of firing the expired event at `exp`. `frontend/lib/api/browser-api-fetch.ts` now does refresh-and-retry on 401 (one retry); `frontend/lib/auth/surface-auth-error.ts` (used by `fetch-symbol-bars`, `fetch-symbol-snapshot`, `fetch-macro-context`, `ticker-news-panel`, `public-signals`, and the STOCVEST Assistant) is async and attempts a refresh before marking expired. **403 is intentionally not treated as auth failure** — STOCVEST uses 403 for product-rule denials (PDT, margin, paid-tier gating) where the user IS authenticated. Tests: `frontend/tests/refresh-session.test.ts` + `frontend/tests/browser-api-fetch-refresh.test.ts` + `frontend/tests/surface-auth-error.test.ts` (+15 over the 161 baseline). **`stocvest/signals/historical_validation.py`** — **D2 Historical Signal Validation (Phase 1)**: pure deterministic aggregator over a list of `SignalRecord` rows, returns a frozen `HistoricalValidationSummary` per horizon (`1h` / `1d`) with `overall` + six stratifications (Decision state at entry, Macro regime at entry — engine vocabulary `risk_on` / `neutral` / `risk_off` / `avoid`, trading Mode, Pattern family, Trade Readiness bucket high≥70 / moderate 40-69 / low<40, Direction); accuracy is `correct / (correct + incorrect)` with neutrals excluded from the denominator and NaN for empty / all-neutral strata; carries `rows_examined` + sorted `parameter_versions` provenance; no I/O — Phase 2 wraps this with a thin service layer (`stocvest/api/services/historical_validation_service.py`) that slices `SignalHistory` by date range and parameter version. Phase 3a exposes it at `GET /v1/signals/historical-validation/summary` (auth-gated, in `stocvest/api/handlers/signals.py::historical_validation_summary_handler`); Phase 3b ships the dashboard tab (`frontend/components/historical-validation-panel.tsx` + `frontend/lib/api/historical-validation.ts` + BFF route at `frontend/app/api/stocvest/signals/historical-validation/summary/route.ts`); Phase 3c-1 ships the public mirror (`GET /v1/signals/historical-validation/public-summary` in the same handler module, surfaced on `/performance` via `frontend/components/performance-tracking-content.tsx::PublicValidationSection` with a trimmed `overall + by_mode + rows_examined` projection enforced by `_public_summary_to_dict` so per-decision / per-regime / per-pattern / per-readiness / per-direction / per-symbol detail can never reach a logged-out visitor — `symbol` is explicitly 400-rejected at the public endpoint). **Phase 3c-2 (2026-05-11)** — the assistant chatbot is now aware of the user's per-user summary on the LOGGED-IN path: `stocvest/api/handlers/signals.py::assistant_chat_handler` fetches `HistoricalValidationService(...).summarize(user_id=rc.user_id, horizon="1d", from_at=now-90d, to_at=now)` on every authenticated chat turn (defensive try/except — a slow / unavailable signal-history store must NOT break the chat reply, and on failure the handler forwards `historical_validation_summary=None` so the prompt's "if absent, do not comment" rule activates); `stocvest/signals/assistant_chat.py::serialize_historical_validation_summary` emits a calm tail block `=== HISTORICAL VALIDATION ===\nwindow_days=90\nhorizon=1d\noverall=<pct>% (<correct> correct of <resolved> resolved; <neutral> neutral; <total> total)\nswing=...\nday=...\nrows_examined=<n>` — same trimmed projection as the public mirror but scoped to the calling user (overall + by_mode only, no per-decision / per-regime / per-pattern / per-readiness / per-direction stratifications reach the LLM); NaN accuracy renders as the em-dash `—` and is NEVER rendered as `"0.0%"` or the literal `"NaN"` string; empty `by_mode` buckets are omitted from the serialized text (Phase 1 pre-seeds the vocabulary so swing/day always appear as keys even when zero rows, but the serializer drops them to avoid the LLM paraphrasing zero-rows as "your day track has no data"); the `=== HISTORICAL VALIDATION ===` sentinel is referenced inside the new `HISTORICAL VALIDATION CONTEXT (LOGGED-IN ONLY)` section of `stocvest/signals/assistant_prompts.py::ASSISTANT_SYSTEM_PROMPT` so the LLM knows what to pattern-match on. The new prompt section enumerates what the assistant **MAY** reference (the user's overall directional accuracy with denominator transparency, the swing-vs-day shape in qualitative terms, the sample size as a small-window caveat, and a redirect to `/dashboard/signal-validation` for the stratified breakdown) and what it **MUST NOT** do (translate accuracy into dollar P&L / expected returns / win-rates — "Directional accuracy is NOT a return number"; predict whether the trend will continue or mean-revert — "descriptive, not predictive"; compare to "the market" / other users / a benchmark; recommend mode choice based on accuracy — "Mode choice is not an advice surface"; use the figures to claim the system "works" — "evidence of behavior, not promotion"; discuss per-symbol / per-pattern / per-regime / per-decision-state / per-readiness-bucket / per-direction performance because those are "deliberately withheld" from the assistant's context; reference any validation figures when the block is absent — "No block means no comment; never invent a number, never recall a number from a previous turn"). The public chat path (`AssistantChatService.reply_public`) is untouched — it refuses the kwarg by signature, never carries the sentinel block, and the public prompt path's `session_mode=public` marker still routes the LLM into PUBLIC MODE RULES where per-user numbers cannot be discussed regardless. **Phase 4 (2026-05-11)** — cross-version diff view on `/dashboard/signal-validation`'s Historical accuracy tab. Purely a dashboard UI + math-helpers slice; reuses the existing Phase 3a `GET /v1/signals/historical-validation/summary?by_version=true` endpoint and the Phase 3b `fetchHistoricalValidationByVersion` client without any backend / Terraform / IAM / BFF changes. `frontend/lib/api/historical-validation.ts` grew four pure helpers (`diffBucketStats(a, b)` returns a `BucketDelta` with `accuracyDelta` / `totalDelta` / `resolvedDelta` / `neutralDelta` / `smallSampleA` / `smallSampleB`; `formatAccuracyDelta(delta)` renders percentage-point strings; `selectComparableVersions(map)` excludes `__all__` and sorts `v<n>` numerically with `unknown` always last; `defaultCompareSelection(map)` picks the last two comparable versions) and exposed the constants `SMALL_SAMPLE_THRESHOLD = 10` / `ALL_VERSIONS_KEY = "__all__"` / `UNKNOWN_VERSION_KEY = "unknown"`. `frontend/components/historical-validation-panel.tsx` grew an inline **Compare versions** checkbox (off by default — the panel still opens to the calm single-version view) that swaps the fetch from `fetchHistoricalValidationSummary` to `fetchHistoricalValidationByVersion` and replaces the body with a `CompareBody` component: two version selectors (defaulting to the latest pair via `defaultCompareSelection`, preserving the user's explicit selections when only the window changes), an A / B / Δ tri-column overall hero card, a stratification grid with `CompareStratificationCard`s that union the keys across the two versions (a pattern family that only fired on one side still shows as a row with `—` on the side it did not fire), and a `CompareBucketRow` per stratum showing A's accuracy + `(correct/resolved)`, B's accuracy + `(correct/resolved)`, and the Δ formatted as `+5.2pp` / `-3.1pp` / `0.0pp` / `—`. Empty / not-meaningful states are handled inline: fewer than two comparable versions in the window renders a calm "need at least two parameter versions in this window to compare … widen the window" message; same-version-on-both-sides renders an italic "pick a different version on either side to see a delta" banner; user-changes-A-or-B while in compare mode does not refetch (selections are pure local state). Small-sample annotations (`< 10` resolved-non-neutral signals on either side) attach a muted "small sample" pill next to that side's cell without obscuring the delta. Color discipline matches the rest of the validation surface — no red/green (accuracy isn't profit), all calm accent tints and monospace deltas. The same verbatim `HISTORICAL_VALIDATION_DISCLAIMER` from the backend response renders below the compare body so the wording cannot drift between the single-version and compare views. The assistant chatbot is deliberately **NOT** wired to consume cross-version diffs — parameter_versions are an engine implementation detail and surfacing them through the LLM would be a leaky abstraction; the Phase 3c-2 user-scoped historical summary already covers the LLM's needs. **No Terraform, no backend, no BFF, no new dependencies.** Tests: +14 in `tests/historical-validation-api.test.ts` (covered in the frontend baseline cell above). Backend baseline 1015/3 unchanged. **D2 functionally complete end-to-end** with Phase 4 shipped (Phase 1 aggregator → Phase 2 service → Phase 3a auth API → Phase 3b dashboard tab → Phase 3c-1 public mirror → Phase 3c-2 assistant awareness → Phase 4 cross-version diff). **`composite_score.py`**: layer/verdict contradiction penalty, **`alignment_ratio`**, **`conflicted_layers`**; **`swing_composite_evidence.py`**: reference-level completeness, R/R quality + warning, catalyst headline pass-through, derived **`risk_factors`** / **`risk_factors_detailed`**; **`user_profile_store`**: lazy singleton (no boto3 on handler import); **`tests/conftest.py`**: Windows **`platform.uname()`** prime to avoid botocore/WMI stderr noise with Python 3.14. **Tests:** **`tests/api/test_article_relevance.py`**, **`tests/services/test_news_relevance.py`**, **`tests/api/services/test_news_panel_format.py`**, **`tests/api/handlers/test_market_data.py`**, **`tests/data/test_edgar_client.py`**, **`tests/data/test_news_triage.py`**, **`tests/signals/test_composite_scorer.py`**, **`tests/signals/test_signal_validation.py`**, **`tests/signals/test_risk_reward.py`**, **`tests/signals/test_catalyst_headlines.py`**, **`tests/signals/test_risk_factors.py`**. **Signal validation ledger:** DynamoDB **`SignalHistory`** only (**`SignalRecord`**); optional ledger columns (**`closed_at`**, **`ledger_entry_date_et`** / **`ledger_exit_date_et`**, **`entry_rationale`**, **`exit_reason`**, **`decision_state_entry`** / **`decision_state_exit`**, **`market_regime_exit`**, **`gate_status_json`** → API **`gate_status`**, **`setup_type`**, **`exit_rule`**, **`max_adverse_excursion_pct`**, **`max_favorable_excursion_pct`**, **`hold_duration_minutes`**) are populated when enrichment jobs write them; **`signal_resolution`** runs **D1** 1h/1d outcome resolution only. UI: **`/dashboard/signal-validation`** (Swing/Day tabs). The retired notional model book (**`ModelPortfolio`**, signals-lambda **`/v1/portfolio/*`**, portfolio reversal / composite sidecar jobs) has been removed from code and infra.

**Frontend (`frontend/`)**  
**STOCVEST Assistant chatbot UI** lives under **`frontend/components/assistant/`** + **`frontend/lib/assistant/`**: **`stocvest-assistant.tsx`** (top-level orchestrator — open state, conversation state, sessionStorage hydration, `surfaceAuthErrorIfAny` on 401, **status-aware fallback copy** when the API call fails; **auth-aware endpoint switching** — when the server-rendered `isAuthenticated` is true the component POSTs to `/api/stocvest/signals/assistant/chat` with the full page-context payload, when false it POSTs to the new `/api/stocvest/public/assistant/chat` with `messages` only; **pathname-based cross-surface isolation** — the conversation is anchored to the URL pathname via `usePathname()` and wiped the moment the pathname changes, so a TTD answer on `/dashboard/signals/layers` cannot leak into a question on `/`, `/dashboard`, `/dashboard/scanner`, `/login`, or anywhere else; the hydrate effect also force-clears persisted state when (a) `persisted.isAuthenticated` is missing entirely (legacy build), (b) `persisted.isAuthenticated` does not match the current session, or (c) `persisted.lastPathname` differs from the current pathname — together these three conditions guarantee the anonymous home page never re-hydrates a logged-in dashboard conversation; **session reset** — the assistant subscribes to `subscribeSessionExpired` and `subscribeAssistantReset` so logout / token expiry / any 401 from any fetch clears messages + composer + sessionStorage; the empty state on the marketing surface now opens with a welcoming three-paragraph pitch — "Welcome to STOCVEST" → the six-layer thesis → the kinds of questions visitors can ask — instead of the old one-line "ask me anything"), **`lib/assistant/session-reset.ts`** (shared `clearAssistantSession` + `ASSISTANT_STORAGE_KEY` + `stocvest:assistant-reset` custom event; sign-out buttons in `sidebar.tsx` + `mobile-nav-drawer.tsx` call `clearAssistantSession()` in their `onClick` before the server logout action runs so the next visitor on the same tab never sees the prior conversation), **`assistant-launcher.tsx`** (custom concentric-ring lens glyph + slow 3.6s pulse + tone dot when contextual), **`assistant-panel.tsx`** (glassmorphism panel, header with **Mode badge**, **constellation context strip** showing the six signal layers tone-colored from page state, quick-prompts that change with mode — anonymous visitors get marketing-flavored prompts like "What is STOCVEST?" / "How is STOCVEST different from signal-alert services?" / "Explain risk/reward in plain terms"; calm composer + disclaimer footer), **`assistant-conversation-rail.tsx`** (no-bubble vertical timeline with gradient rail, small filled speaker nodes, **role-colored speaker labels** — accent blue for `YOU`, page-tone for `STOCVEST` — and **a 2px role-tinted left ribbon** on each message body with a faint user-only tint so input vs output is unmissable at a glance; **word-fade reveal** for the latest assistant turn). **`lib/assistant/context.tsx`** exposes **`AssistantContextProvider`**, **`useAssistantContext`**, and **`usePublishAssistantContext`** so any dashboard page can publish a whitelisted page-context payload while mounted. **`lib/signal-evidence/trade-decision.ts`** is the **shared source of truth** for `synthTradeDecision` / `deriveDecisionRationale` — `signal-evidence-card.tsx` and the signals-page assistant context both read from it, so the Decision shown on the Evidence card and the context handed to the chatbot can never drift. Motion is gated behind **`prefers-reduced-motion: reduce`** in **`app/globals.css`**. BFF proxy at **`app/api/stocvest/signals/assistant/chat/route.ts`** forwards to **`/v1/signals/assistant/chat`** via **`stocvestAuthedFetch`**. The launcher is mounted globally in **`app/dashboard/layout.tsx`** inside **`AssistantContextProvider`**, so it follows the user across every dashboard route. App router pages (dashboard **`layout.tsx`** wraps routes with **legal acknowledgment** + **onboarding** gate; **`dashboard-page-content.tsx`** uses time-bounded **`fetchMarketOverview`** / scanner / earnings loads so the shell is not blocked by a slow BFF; incl. **`/dashboard/performance`**, scanner (**`/dashboard/scanner`** server page: **`fetchDefaultWatchlistSymbols`** in parallel with **`fetchPdtStatus`**, then **`fetchScannerOverview(..., watchlistSymbols)`**), **`/dashboard/watchlists`**, signals with **Signal history** tab (BFF history + record fetch) + **`POST /api/stocvest/signals/composite/real`** or **`/api/stocvest/signals/composite/swing`** per **Day trade / Swing trade** toggle (and legacy **`swing-composite`**) check for **`insufficient_data`** (amber “Market Data Unavailable” callout when fewer than three layers have live scores); **`hasValidSignal`** now hides stale AI/radar/reference/evidence when status is insufficient and clears signal state; closed-session insufficient path renders **`components/signals-after-hours-panel.tsx`** with last-session levels/news/watchlist CTA; portfolio, **journal** with analytics + cumulative P&amp;L + trade table, settings (**Alert Preferences** + `#alerts`), earnings, public `/performance`, terms, etc.), **landing** (server **`app/page.tsx`** runs **`Promise.all`**: **`GET /v1/signals/recent?landing=true`**, performance summary, **`getFoundingMemberCount`** → **`GET /v1/signals/founding-members`** with **30m** revalidate; **`landing-signals.ts`** + **`FALLBACK_SIGNALS`** (marketing-only examples, **5** rows with ET wall-clock **`generated_at`**, incl. one **incorrect** demo outcome), **`LandingSignalExplorer`** (dynamic 1h % from prices; “(example data)” when API empty), **`LandingBeforeAfterSection`**, **`LandingActivityFeedSection`** (empty states + accuracy copy), `LandingHowItWorksSection`, `LandingPerformanceSection` + optional **`pattern_breakdown`**; shared glow card utilities in **`app/globals.css`** (`.landing-glow-card`, gate/pledge/before-after variants); marketing cards on **`landing-page.tsx`**; no client polling on `/`), design system + theme, API clients under `lib/api/` (incl. **`client.ts`** default **`apiFetch`** timeout; **`market.ts`** — **`fetchMarketOverview`** loads market status + indices only (no `/v1/market/news`); **`ticker-news-panel.ts`** + **`panel-article-to-news-payload.ts`** for **`GET /v1/market/news?symbol=`**; **`fetch-symbol-news.ts`** uses the panel API; **`watchlists.ts`** default symbols fetch, **`public-signals.ts`**, **`swing-composite.ts`**, client-safe **`fetch-symbol-snapshot.ts`**), **`lib/signal-evidence.ts`** + **`signal-evidence-card.tsx`** (R/R warning styling, incomplete-levels messaging, catalyst/risk empty copy), **`lib/snapshot-reference-levels.ts`** (same **5×** + valid-last guard as backend), **`components/pdt-status-pill.tsx`**, **`components/dashboard-redesign.tsx`** (PDT pill + **Market Pulse**; morning brief + signal-card **News** affordances + **`NewsPanel`**; **empty swing Top signals** “Primary read” card—posture headline, regime-tuned one-liner, institutional **Signal suppressed — …** line; circled‑i on **Primary read** uses layman copy from **`lib/ui-tooltips.ts`** (`PRIMARY_READ_SWING_CONTEXT_TIP`); alignment ladder, watchlist readiness, top-signals card, and **What would re-enable swing setups** (circled‑i beside heading only—no “Why?” label) tips rewritten for non-experts; re-enable tooltip uses intro + **short** bullets only; watchlist readiness detail tip prefixed with plain-language intro), **`components/news-panel.tsx`**, **`components/news-headline-drawer.tsx`** (credibility + relevance line), BFF **`POST /api/stocvest/journal/entries`**, **`GET`/`PATCH /api/stocvest/users/me`**, **`/api/stocvest/watchlists/**`**, **`/api/stocvest/alerts/**`**, **`/api/stocvest/signals/swing-composite`**, **`/api/stocvest/signals/me/history`**, **`/api/stocvest/signals/me/records/[signalId]`**, **`add-to-watchlist-button`** on signals + gap cards, Crisp when `NEXT_PUBLIC_CRISP_WEBSITE_ID` is set. **Sign out** redirects to **`/`** (landing). Scanner: **`fetchScannerOverview`** uses **`POST /v1/scanner/gap-intelligence`**, **`POST /v1/signals/day/setups`** (with snapshots + regime for confluence), structured **`POST /v1/signals/day/briefing`** (context includes gap items + intraday setups); intraday setup cards, signal evidence modal, gap cards, and dashboard morning brief show **confluence** when `is_confluence_alert`; **Open order entry** deep-links to **`/dashboard/portfolio`** with symbol + optional signal query params.

**Docs**  
`docs/API_CONTRACTS.md` — HTTP + broker contracts. **`docs/BACKLOG.md`** — detailed planned work (no duplicate of this file’s status tables). **`docs/SIGNAL_ENGINE.md`** — real composite + layer contracts/limitations (incl. **sector SIC** resolution chain and **`SicMappingTier`**: `exact` → `prefix` → `coarse` → `fallback_spy`, optional **`sic_mapping_tier`** on composite sector layer rows). **`docs/DEPLOYMENT.md`** — SES / email alert setup for production. **`docs/TUNING_PLAYBOOK.md`** — monthly signal-parameter review using **`GET /v1/signals/analysis`** and Secrets **`stocvest/signal-parameters`**. **DynamoDB `ParameterHistory`** (Terraform) stores versioned parameter JSON + audit fields. **User-facing tooltips:** copy for circled‑i **InfoTip** / related dashboard strings is centralized in **`frontend/lib/ui-tooltips.ts`**; **`frontend/components/info-tip.tsx`** renders multiline tips with **`white-space: pre-line`**. **Detailed file-by-file history was removed from this file** to avoid drift; use README + git.

**Public signal API (trust)**  
`GET /v1/signals/recent` (last **50** public `SignalRecord` rows as JSON array when `landing` is absent; **`?landing=true`** returns **`{ "items": [...] }`** with up to **5** rows where **`outcome_1h`** is set — allowlisted fields for marketing only, **`ai_summary`** truncated to **120** chars), `GET /v1/signals/performance/summary` — directional accuracy from **1d** outcomes; JSON **`correct_direction_count`**, **`incorrect_direction_count`**, **`neutral_direction_count`** (and related totals); accuracy = correct ÷ (correct + incorrect). **`GET /v1/signals/founding-members`** — public JSON **`founding_member_count`**, **`founding_spots_total`** (**100**), **`founding_spots_remaining`** (paid subscribers only; see **§1** HTTP row). Authenticated **`GET /v1/signals/me/history`** (optional **`mode`**, **`days`**, **`limit`**, **`symbol`**) and per-id **`GET /v1/signals/me/records/{signal_id}`** for user-scoped rows; optional ledger fields (`closed_at`, `gate_status`, `exit_rule`, MAE/MFE, …) appear when stored on the row (see **`docs/SIGNAL_ENGINE.md`** § Signal validation ledger). Responses use `signal_strength`, `disclaimer`, etc. (see Legal section).

---

## 3. Pending (near-term ops / engineering)

**Deploy checklist (after you push to `main`):** GitHub Actions runs tests, then **`deploy-lambda`** (zip → S3 → `update-function-code` for every `stocvest-development-api-*` module, including **`signal_resolution`**) and optionally **`deploy-vercel`** (production deploy hook). Full secret/variable names and IAM needs: [root `README.md` § CI/CD](../README.md#cicd-github-actions).

| Step | Action |
|------|--------|
| 1 | **Terraform:** `cd infra && terraform apply` for the target env so DynamoDB (incl. **`SignalHistory`**, **`TradeJournal`**, **`PDTState`**, **`AuditEvents`**, …), **every** `api_handler_modules` Lambda (incl. **`signal_resolution`** → name `stocvest-development-api-signal_resolution`), API Gateway (incl. **`GET /v1/signals/founding-members`**, admin **beta** / **audit** routes), and Lambda env (**`DYNAMODB_AUDIT_EVENTS_TABLE`**, etc.) exist. CI updates **code** only (`update-function-code`); it does **not** create functions. If Actions fails with `ResourceNotFoundException` for that name, apply Terraform **before** re-running deploy. |
| 2 | **GitHub:** Repository **variable** `STOCVEST_LAMBDA_S3_BUCKET`; **secrets** `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`, `AWS_REGION`; optional `VERCEL_DEPLOY_HOOK_URL`. |
| 3 | **Push `main`:** Open **Actions** → confirm backend, frontend, `deploy-lambda`, and `deploy-vercel` (or skip if hook unset). |
| 4 | **Smoke:** `GET /v1/health` on the HTTP API base URL; optional `GET /v1/signals/recent` if D1 table is applied. |
| 5 | **Scheduled resolution — APPLIED ✅** **`terraform apply -var-file=terraform.tfvars -input=false -auto-approve`** completed **2026-05-03** (**19 added, 14 changed, 2 destroyed**). Rule **`stocvest-signal-resolution`**: **`ENABLED`**, **`rate(30 minutes)`**, target **`stocvest-development-api-signal_resolution`**. Details: [`docs/D1_SIGNAL_RESOLUTION_SCHEDULE.md`](./D1_SIGNAL_RESOLUTION_SCHEDULE.md). |
| 6 | **Model portfolio retirement (2026-05-08+)** — **`ModelPortfolio`** DynamoDB table, signals-lambda **`/v1/portfolio/*`**, **`portfolio_reversal`** / **`run_portfolio_composite`** scheduler payloads, and related code removed; accountability is **`SignalHistory`** + **`/dashboard/signal-validation`**. Historical note: row 6 previously recorded the **2026-05-05** apply of portfolio-reversal + `run_portfolio_composite`; that stack is **no longer** deployed targets. |
| 7 | **AuditEvents + founding/audit Gateway routes — APPLIED ✅** **`terraform apply -var-file=terraform.tfvars -input=false -auto-approve`** completed **2026-05-07** (**5 added, 14 changed, 0 destroyed**): DynamoDB **`AuditEvents`**, **`DYNAMODB_AUDIT_EVENTS_TABLE`** on all API Lambdas, HTTP API routes **`GET /v1/signals/founding-members`**, **`PATCH /v1/admin/users/{user_id}/beta-access`**, **`GET /v1/admin/audit/users/{user_id}`**, **`GET /v1/admin/audit/sessions/{session_id}`**, CORS header **`x-stocvest-session-id`**. |
| 8 | **STOCVEST Assistant chat route — APPLIED ✅** **`terraform apply -var-file=terraform.tfvars -input=false -auto-approve`** completed **2026-05-10** (**1 added, 0 changed, 0 destroyed**): `aws_apigatewayv2_route.http["POST /v1/signals/assistant/chat"]` (route id `x6uhho2`) under the `signals` module with `authorizer = true`. Verified end-to-end via an unauthenticated probe to the new route that returned **`401 Unauthorized`** — the JWT authorizer rejected the call after the route accepted it, which proves the route is wired (a missing route returns API Gateway's native 404 instead). Signals Lambda code (`assistant_chat_handler` + `AssistantChatService`) deployed via CI `deploy-lambda` on commit `b6f83e7`; subsequent polish in `0485c95` (client-side status-aware fallback + console diagnostic) and `d1b6856` (prompt hardening, role-distinct rail, `analysis_status`) ride the same CI pipeline. No new secret required — `ANTHROPIC_API_KEY` already lives in `stocvest/lambda-runtime |
| 9 | **STOCVEST Assistant public route — APPLIED ✅** **`terraform apply -var-file=terraform.tfvars -input=false -auto-approve`** completed **2026-05-10** (**1 added, 0 changed, 0 destroyed**): `aws_apigatewayv2_route.http["POST /v1/public/assistant/chat"]` (route id `69hco4j`, integration `rnikyps` reusing the existing `signals` Lambda) with `authorizer = false`. The route lets anonymous marketing visitors (`/`, `/login`, `/signup`, etc.) chat with the assistant from the homepage so prospects can ask what STOCVEST is, how it differs from signal-alert services, and for explanations of common finance terms — the locked system prompt's new **PUBLIC MODE** section activates via the appended `session_mode=public` marker and continues to refuse trade recommendations, predictions, and accuracy claims. The route post-deploy returned **`{"error":"not_found","message":"Unknown route: POST /v1/public/assistant/chat"}`** from the signals Lambda — expected, because the Lambda code containing `public_assistant_chat_handler` is shipped on the next `deploy-lambda` CI run that lands the commit. No new secret required.`. |

1. **Infrastructure:** Same as checklist rows 1–2; keep API Gateway URLs and Cognito/Vercel env aligned with the deployed stage. **News pipeline note:** queue + consumer Lambda can be applied now; the optional ECS news worker still requires `news_worker_container_image` to be set before Terraform will create the task/service.
2. **Broker/runtime services:** IBKR path needs ECS/Fargate + TWS/ibeam where applicable; the new always-on news worker also needs an image publish/push path plus Redis reachability from the app security group.
3. **CI hardening (if not done):** setuptools upgrade in CI before `pip install -e .`; optional skip for Vercel deploy when hook secret missing. Add worker image build/push automation if you want the ECS runtime deployed from CI.
4. **Legal / launch:** Terms at `/terms` are a **draft** — attorney review before paid subscribers; **mandatory legal acknowledgment (B2)** is implemented before dashboard use; optional onboarding wizard remains dismissible per session until completed.

---

## 4. Legal compliance (product & API)

- STOCVEST is **not** a registered investment adviser; frame as **signal intelligence** and data tooling.
- **UI:** Prefer signal summary, signal strength, reference levels, signal parameters—not “verdict,” “recommendation,” “you should,” or “confidence” in **signal** context.
- **`SignalDisclaimerChip`** on signal cards; **`GlobalDisclaimer`** last in `<body>` in root layout.
- **Performance page:** Directional accuracy only; not dollar P&L as “signal performance.”
- **Serialized API:** Use `signal_summary` / `signal_strength` (and related names) where applicable; include `disclaimer`: *Signal data for informational purposes only. Not investment advice.*
- **Logging / persistence:** Sanitize user-supplied free text before Dynamo (`text_sanitize`); keep CloudWatch logs free of unnecessary PII (`log_privacy`). Server-side identity for journal and protected routes comes from the **JWT**, not from client-supplied `user_id` body fields.

---

## 5. Roadmap & backlog

**Full prioritized themes, sub-tasks, and IDs:** [`docs/BACKLOG.md`](./BACKLOG.md) — **single source** for planned work so this file stays short.

**Directional summary:** After infra/broker production readiness (`§3`), focus shifts to **subscriptions**, **sector/internals**, **D2 backtesting / analytics**, **Phase 7 hardening**, and the post-beta **trade brief** UI (`BACKLOG.md` sections B, D, P, G, M, T). **D1** signal outcomes, **B1** journal automation, **B2** onboarding + legal acknowledgment, **B3** alerts (email), and **B4** watchlists are shipped (see status table). **B13** (SMS) remains pending until phone collection is ready.

---

## 6. Key decisions (unchanged summary)

1. Stack: Next.js (Vercel) + Python Lambdas (AWS).  
2. Brokers: IBKR (TWS / ib_insync) + E*TRADE (OAuth REST).  
3. Market data: Polygon.io (stocks advanced + options + crypto tiers as configured).  
4. Multi-tenant isolation end-to-end.  
5. Asset scope: stocks, ETFs, options, futures, crypto (scope varies by data source).  
6. Six signal layers: Technical, News, Macro, Sector, Geopolitical, Internals.  
7. AI: Claude (Sonnet) → structured JSON for synthesis; **public APIs use compliance field names** (`signal_summary`, `signal_strength`, …).  
8. Domain: **stocvest.app**.  
9. Day trading: Phase 2.5 scanner + PDT tracker are first-class.  
10. News ingestion is now multi-source: Benzinga websocket for live news plus SEC EDGAR 8-K polling, with Claude scoring kept in the SQS-triggered Lambda consumer.  
11. **Composite signal architecture:** **Stage A** = per-layer analyzers (each speaks from its own domain); **Stage B** = `CompositeScoreEngine` and handlers reconcile after the fact. Regime multipliers **scale influence**, not the **sign** of a layer’s directional input (default multipliers stay positive). **Eligibility gates** (e.g. `insufficient_data`) vs **degradation** (e.g. contradiction penalty, R/R warnings) are separate ideas—see **`docs/SIGNAL_ENGINE.md`** (§ Architecture, § Composite, § Future enhancements).

---

## 7. Immutable contracts

Do **not** change without discussion and coordinated updates:

- **`BrokerAdapter`** — eight methods, types, exceptions (`docs/API_CONTRACTS.md` §1).  
- **HTTP paths** — versioned under `/v1/`; breaking changes need `/v2/` or migration.  
- **DynamoDB table names** — `Users`, `BrokerConnections`, `Watchlists`, `Alerts`, `Orders`, **`SignalHistory`**, **`TradeJournal`**, **`PDTState`**, **`AuditEvents`** (HTTP audit items; `pk`/`sk` per `audit_store.py`), etc.  
- **Secrets path** — `/stocvest/{userId}/brokers/{brokerId}`.  
- **Canonical types** — `stocvest/data/models.py` (no raw Polygon dicts in core logic).  
- **Snapshot + intraday + VIX invariants** (verified; do not re‑discover in prompts):
  1. **`Snapshot` model** — Pydantic `Snapshot` in `stocvest/data/models.py` (not a separate parser class). Raw Polygon ticker dicts map only through `PolygonClient._parse_snapshot()` → `Snapshot`. **Do not** add a `SnapshotParser` class, **do not** read raw Polygon keys in layer/analyzer code, **do** use `Snapshot` field names everywhere: `symbol`, `last_trade_price`, `last_trade_size`, `last_quote_bid`, `last_quote_ask`, `day_open`, `day_high`, `day_low`, `day_close`, `day_volume`, `day_vwap`, `prev_close`, `change`, `change_percent`, `pre_market_price`, `pre_market_change_percent`, `after_hours_price`, `after_hours_change_percent`, `market_status`, `company_name`, `prev_day_volume`. Layer analyzers take **`Snapshot`** instances (or derived inputs), not raw dicts.
  2. **`IntradaySetupScanner`** — Does **not** fetch bars. Signature: `scan(bars_by_symbol: dict[str, list[Bar]], …)`. Handlers fetch bars then pass them in. To reuse intraday math inside something like a technical layer, import **`IntradayVWAPCalculator`**, **`IntradayEMA9Calculator`**, **`OpeningRangeBreakoutDetector`** (and related helpers in `day_trading_scanner.py`) — **not** the scanner class as a nested “analyzer.”
  3. **VIX** — Reuse `get_vix_snapshot_with_fallback` and tuple `VIX_SNAPSHOT_FALLBACK_SYMBOLS` from `stocvest/api/services/morning_brief_fetch.py` (`I:VIX` → `^VIX` → `VIX`). **Do not** add another VIX fetcher or a single hardcoded/configured VIX ticker as the primary approach; the **ordered fallback list** is the contract.
  4. **Day vs last trade scale** — `_parse_snapshot` already drops inconsistent `day` OHLC/VWAP when out of line with `lastTrade.p` (5× bound via `_session_day_prices_align_with_last`). Use **`snapshot.last_trade_price`** for current/near price; **do not** duplicate that sanity check downstream.
  5. **Extended hours** — `preMarket`/`premarket` and `afterHours`/`afterhours` key variants are normalized inside `_parse_snapshot` into `pre_market_*` / `after_hours_*` on **`Snapshot`**; use those fields, not raw dict access.

Signal **layer enums** and internal composite types are stable; **customer-facing** naming follows the Legal section above.

---

## 8. Known issues / technical debt

- `pyproject.toml` / setuptools: may need `pip install setuptools --upgrade` before editable install on some machines.  
- PDT rolling window is weekday-based; exchange holidays not modeled—align with broker if needed.  
- Stochastic implementation has a small style debt (not correctness).  
- **Mitigated (local pytest, Windows + Python 3.14):** botocore’s user-agent path could trigger WMI via `platform.uname()` from threads and print “access violation” to stderr; **`tests/conftest.py`** primes `platform.uname()` on import and clears **`DYNAMODB_USERS_TABLE`** in the autouse fixture so handler imports do not eagerly create Dynamo clients.

---

## 9. Open questions

- **AWS account model:** dedicated org account vs existing (confirm for production).  
- **Crypto on-chain metrics:** deferred.  
- **E*TRADE / IBKR production:** credentials, redirect URLs, and ECS capacity for TWS.

---

## 10. Development environment

```bash
python -m venv .venv
pip install -e ".[dev]"
cp .env.example .env   # fill Polygon, Claude, etc.
pytest tests/ -q
```

Frontend: `cd frontend && npm ci && npm run build && npm run test`

---

## 11. Cost picture (approximate / year)

| Item | ~USD/yr | Purpose |
|------|---------|---------|
| Polygon (stocks + options + crypto tiers) | ~2.7k | Market data |
| IBKR | $0 (account) | Execution, futures, L2 |
| Claude | ~200–600 | Synthesis |
| Vercel + AWS + domain | varies | Hosting + compute |

---

## 12. Day-trading scan rhythm (reference)

- **Pre-market:** gap + catalyst scan → ranked candidates.  
- **RTH:** intraday setup scanner (~5 min cadence per design), VWAP/OR/EMA logic in engine.  
- **PDT:** warn at 2 day trades, block at 3 (non-exempt) in rolling window.

---

## 13. Session rules

### STEP 0 — Read and verify (no guessing)

Do this **before** designing or naming APIs against external data. **§7 “Snapshot + intraday + VIX invariants”** is the canonical checklist (field names, `IntradaySetupScanner` boundaries, VIX helper, no duplicate day/last checks, extended hours on `Snapshot`).

1. **Open the real implementation** — e.g. `polygon_client.py`, `models.py` (`Snapshot`), `day_trading_scanner.py` (`IntradaySetupScanner` + calculators), `morning_brief_fetch.py` (`get_vix_snapshot_with_fallback`).
2. **Polygon JSON keys** — Only keys read in `_parse_snapshot` (and other explicit parsers in `polygon_client.py`) are authoritative. New surface area → extend `_parse_snapshot` + tests.
3. **Decisions in code reviews** — Prefer commit messages or PR notes with **why** and **rejected alternatives** where non-obvious.
4. **Tests assert design, not trivia** — e.g. VIX fallback order, dropped `day` when scale mismatches, scanner RVOL gates.
5. **Commits** — When several separable decisions land in one session, **order commits** for independent `git revert`. One cohesive change may still use a single commit (§13 STEP 4).

### Before writing any code

1. Read `docs/CONTEXT.md` and `docs/BACKLOG.md` in full.
2. Confirm exact test counts:
   - Run: `pytest tests/ -q`
   - Run: `cd frontend && npm run test`
   - Record actual numbers — do not use “~” approximations.
   - If counts are **lower** than the baseline in **§13 Test baseline** (and the summary line under the document title), stop and report the regression before doing anything else.
3. Do not implement anything already marked ✅ in §1.
4. Do not implement anything not in `BACKLOG.md` without flagging it to the user first.
5. Do not change any contract in `docs/API_CONTRACTS.md` without explicit instruction.

### Every prompt must end with ALL of these steps

 **STEP 1 — Tests**  
Run `pytest tests/ -q`  
Run `cd frontend && npm run test` (Vitest; do not pass Jest’s `--watchAll=false`)  
Run `cd frontend && npm run build`  
Report exact counts. If any count dropped, fix before proceeding to documentation updates.

**STEP 2 — Update `docs/CONTEXT.md`**  
- §1 Status table: mark completed tracks ✅; update Notes if anything changed.  
- §2 Implemented: add new files/modules built this session.  
- §3 Pending: remove items that are now resolved; add new blockers discovered this session.  
- §13 Test baseline: update exact backend and frontend test counts (and note if `npm run build` was verified).  
- Document title summary line: keep test baseline in sync with §13.  
- **Last updated** date: set to today.

**STEP 3 — Update `docs/BACKLOG.md`**  
- Items completed this session: change status to **DONE**; add: `Done YYYY-MM-DD · commit: <hash>`.  
- Items discovered this session: add with new ID, description, and acceptance criteria.  
- Items in progress but not finished: change status to **In progress**.  
- **Do not delete any row — ever.**  
- **Last updated** date.

**STEP 4 — Commit**  
- Stage: all code + `docs/CONTEXT.md` + `docs/BACKLOG.md` together.  
- Message format (required):  
  `"<feature> | backend: <N> tests | frontend: <N> tests"`  
  where **backend N** = pytest **passed** count and **frontend N** = Vitest **passed** count (from the respective summaries).  
  Example:  
  `"B2 onboarding wizard | backend: 468 tests | frontend: 50 tests"`  
- Never commit code without committing updated docs.  
- Never commit docs without committing the code they describe.

### What goes where — hard rules

| Content type | File | Rule |
|--------------|------|------|
| What is built and working | CONTEXT.md §1–2 | Reality only |
| Near-term blockers & ops | CONTEXT.md §3 | Keep current; not duplicated in BACKLOG |
| Planned work & acceptance notes | BACKLOG.md | Full detail; **never delete rows** (mark DONE + date + commit) |
| HTTP & broker contracts | API_CONTRACTS.md | Change only with explicit instruction |
| Legal framing (copy & public API) | CONTEXT.md §4 | Align product language and payloads |

### Test baseline (exact counts — update after every verified run)

| Suite | Command | Last verified |
|-------|---------|---------------|
| Backend | `pytest tests/ -q` | **1161 passed**, **3 skipped** (verified 2026-05-12 after the **Automated weight-tuning proposal pipeline — algorithmic core (D10 Phase 2a)** shipment — **+44** over the prior 1117/3 baseline. All **44** new tests live in the new `tests/signals/test_weight_optimizer.py` under named sections: `WeightSet` invariants (6 tests — normalize sum-to-1, normalize rescales unscaled inputs preserving ratios, sum-must-be-positive guard, duck-typed `from_composite_block` lift, JSON-friendly `to_composite_block_dict` serialization with threshold pass-through, canonical `WEIGHT_LAYERS` order pin), engine-mirror equivalence (4 tests — `NEUTRAL_MOVE_PCT == 0.1` pinned, `outcome_from_prices` cross-module equivalence vs `stocvest.api.services.signal_recorder` across the full direction-x-move grid, neutral fallback when post-resolution price missing, `regime_for_engine_from_application_label` across the full application vocabulary including fallback for unknown strings/None), `score_weight_candidate` accuracy math (5 tests — unambiguously-bullish row set scores 1.0, anti-bullish set scores 0.0, neutral outcomes excluded from denominator preventing accuracy penalty for "no move" rows, empty layer-scores rows skipped not zero'd, all-neutral row set returns 0.0 = "no information" not "perfect score"), `walk_forward_split` chronological correctness (5 tests — 75/25 default, custom fraction, invalid-fraction rejection at all four boundaries, empty input returns empty tuple no exception, unsorted-input loud rejection defending against random-shuffle leak), `regime_distribution` + `regime_distributions_match` (5 tests — counts per regime key, close distributions pass, wildly divergent distributions fail, empty-side permissive — the caller's job to check signal counts, tolerance-boundary just-over fails / just-under passes), `generate_candidate_weights` constrained grid (6 tests — baseline first as no-rotation candidate, all candidates normalized to sum=1, no non-positive weights survive filtering, deduped by 4-decimal rounded fingerprint, ≤729 upper bound with >50 actual to prove the perturbation loop fires, non-positive delta loud-rejected), `accept_proposal` three-gate enforcement (6 tests — happy-path with human-readable lift reason, too-few-val-signals reject with row count surfaced, insufficient-lift reject, regime-drift reject, negative-lift reject, custom-threshold kwargs sanity covering both tighten + loosen), and `optimize_weights_for_mode` end-to-end on synthetic data (6 tests — uniform-rows scenario returns baseline weights unchanged with equal val accuracies, news-dominant synthetic scenario rotates weight toward news layer with val acc ≥ baseline, all `OptimizationResult` fields populated for Phase-2b serialization, empty input loud-rejected, too-few-rows-for-split rejected via empty-side check, evidence dict shape contains the Phase-1 proposal contract's required keys), and 1 module-level constant pin (`DEFAULT_DELTA == 0.05`, `DEFAULT_TRAIN_FRACTION == 0.75`, `DEFAULT_MIN_VAL_ACCURACY_IMPROVEMENT == 0.02`, `DEFAULT_MIN_VAL_SIGNAL_COUNT == 30`, `DEFAULT_REGIME_TOLERANCE == 0.20` — forces explicit doc/BACKLOG update on default change). Verified 2026-05-12 after the **Automated weight-tuning proposal pipeline — foundation (D10 Phase 1)** shipment — **+24** over the prior 1093/3 baseline. All **24** new tests live in the new `tests/data/test_parameter_proposal_store.py` under named sections: factory invariants (`test_new_pending_requires_at_least_one_per_mode_block`, `test_new_pending_accepts_swing_only_proposal`, `test_new_pending_accepts_day_only_proposal`, `test_new_pending_auto_generates_id_and_timestamp` — the factory's `proposal_id`/`created_at` defaults), round-trip serialization (`test_to_item_round_trip_preserves_every_field` — identity transform on a fully-populated proposal including all review-state fields, `test_to_item_omits_optional_fields_when_unset` — fresh pending rows don't carry review-state attributes, `test_to_item_encodes_nested_dicts_as_json_strings` — sidesteps DDB Decimal coercion, `test_from_item_tolerates_raw_dict_in_nested_fields` — defensive for hand-written DDB rows), CRUD (`test_put_then_get_returns_equivalent_proposal`, `test_get_returns_none_for_missing_id`, `test_put_is_idempotent_for_same_id` — retry-safety for EventBridge retries), GSI semantics (`test_list_by_status_returns_desc_by_created_at` — newest first, `test_list_by_status_filters_by_status`, `test_list_by_status_rejects_invalid_status_loud` — typo raises ValueError not silent-empty, `test_list_by_status_honors_limit`), transitions + atomicity (`test_mark_promoted_transitions_pending_to_promoted` — promoted rows have NO TTL, `test_mark_rejected_transitions_pending_to_rejected_with_ttl` — TTL ≈ +90 days, `test_mark_superseded_transitions_pending_to_superseded_with_pointer` — `review_note` records the displacing proposal id, `test_cannot_promote_already_promoted_proposal`, `test_cannot_reject_already_rejected_proposal`, `test_cannot_promote_rejected_proposal` — **the load-bearing atomicity guarantee**: rejected cannot be silently re-promoted by a racing admin click, `test_cannot_transition_missing_proposal` — non-existent id raises ValueError not silent no-op), closed-set invariants (`test_proposal_status_values_closed_set`, `test_proposal_ttl_days_documented_invariant`). Test approach uses an in-process `_FakeTable` that implements the boto3 Table subset (`put_item` / `get_item` / `query` / `update_item`) with realistic semantics including `ConditionalCheckFailedException` raised on `update_item` when the condition fails — that's the only way the lifecycle atomicity is actually exercised. The `query` implementation asserts `IndexName == GSI_STATUS_INDEX` so a future caller refactor that accidentally queries the base table or a wrong GSI is caught loud. Previous **+14** over the 1079/3 baseline from the **Gap-card mode-fit classifier + per-row Evidence routing (B30 Phase 4)** shipment: — **+14** over the prior 1079/3 baseline. All **13** new tests live in `tests/signals/test_gap_intelligence.py` under a new "B30 Phase 4 — classify_mode_best_fit lock-ins" section: `test_mode_best_fit_returns_closed_set_value` (defensive: every input combo resolves to one of `MODE_BEST_FIT_VALUES`), `test_mode_best_fit_earnings_catalyst_with_large_gap_picks_swing` (3 swing signals fire, day chips DO NOT leak into reasoning), `test_mode_best_fit_merger_catalyst_is_structural_swing`, `test_mode_best_fit_insider_catalyst_is_structural_swing` (all three structural-catalyst families locked in), `test_mode_best_fit_no_catalyst_with_heavy_volume_picks_day` (3 day signals, swing chips DO NOT leak), `test_mode_best_fit_macro_catalyst_with_heavy_volume_picks_day` (tape-level catalyst rule), `test_mode_best_fit_analyst_catalyst_with_heavy_volume_picks_day` (symmetric), `test_mode_best_fit_balanced_signals_returns_either` (1-vs-1 → either, reasons concat both sides for transparency), `test_mode_best_fit_margin_below_two_returns_either` (2-vs-1 still either — the margin-2 rule is the load-bearing invariant that prevents the classifier from over-claiming), `test_mode_best_fit_unknown_catalyst_category_does_not_score_as_structural` (only earnings / merger / insider count as structural — unknown categories fall through), `test_mode_best_fit_quiet_market_micro_gap_returns_either` (0-vs-0 default), `test_mode_best_fit_negative_gap_uses_absolute_magnitude` (gap-down treated symmetrically — direction does NOT affect mode fit), `test_mode_best_fit_huge_gap_no_volume_still_swing` (9% earnings gap with 1.1× vol still swing — magnitude alone doesn't flip to day), `test_mode_best_fit_in_build_gap_intelligence_items` (end-to-end wire-up: the new `mode_best_fit` + `mode_best_fit_reasons` fields are present on the row dict and a 5% gap surfaces a `large gap` chip in the reasoning). Previous **+18** over the prior 1061/3 baseline from the **Per-mode composite weight overrides (B30 Phase 3 — Suggestion 4 audit)** shipment: **+11 in `tests/signals/test_composite_score.py`** under a new "B30 Phase 3 — per-mode composite override blocks (Suggestion 4 audit)" section locking in the new `resolve_composite_block(params, mode)` + the `mode` kwarg on `build_composite_score_engine_from_params`: `test_resolve_composite_block_falls_back_to_shared_when_no_per_mode_block` (no-op default for all three mode values swing / day / None), `test_resolve_composite_block_returns_swing_block_when_set` (only-`swing_composite`-set case with cross-mode isolation), `test_resolve_composite_block_returns_day_block_when_set` (symmetric), `test_resolve_composite_block_handles_both_overrides_independently` (both blocks set + each mode picks its own), `test_resolve_composite_block_ignores_unknown_mode` (case-sensitive defensive fallback for `"options"` / `""` / `"SWING"`), `test_build_engine_swing_and_day_identical_when_no_override` (the **production behavior guarantee** — same six weights + same thresholds across both modes until operators rotate Secrets Manager), `test_build_engine_swing_mode_uses_swing_override_weights` (all six weights + thresholds flow through), `test_build_engine_day_mode_uses_day_override_weights` (symmetric), `test_build_engine_swing_mode_does_not_pick_up_day_override` (cross-mode isolation lock at the builder layer), `test_build_engine_day_mode_does_not_pick_up_swing_override` (symmetric), `test_per_mode_overrides_actually_change_composite_score` (wire-is-live end-to-end — rotating swing override moves the swing engine's score while day engine's score is anchored to the shared baseline). **+7 in `tests/config/test_parameter_store.py`** under a new "B30 Phase 3" section locking in `signal_parameters_from_dict` parsing of `swing_composite` / `day_composite` JSON keys: `test_signal_parameters_from_dict_legacy_secret_has_no_per_mode_blocks` (the back-compat invariant — every existing secret keeps working unchanged), `test_signal_parameters_from_dict_with_swing_composite_block`, `test_signal_parameters_from_dict_with_day_composite_block`, `test_signal_parameters_from_dict_with_both_per_mode_blocks`, `test_signal_parameters_from_dict_per_mode_null_explicit` (JSON-`null` → `None` defensive), `test_signal_parameters_from_dict_per_mode_unknown_keys_ignored` (forward-compat for fields we don't know yet), `test_signal_parameters_to_dict_round_trip_with_per_mode_blocks` (serialize → parse round-trip preserves both override blocks). The prior 1061/3 baseline (2026-05-12, unrecorded in this table — see also the §13 entries below) reflected the **B29 Phase 2 dashboard card visual uniformity** shipment which was frontend-only on top of the **B29 Phase 1 signal-card explanation refinements (+6 in `tests/signals/test_technical_analyzer.py` + `tests/signals/test_confluence.py`)** baseline of 1055/3. The prior 1051/3 baseline (verified 2026-05-11 after the **Dashboard two-desk render + chatbot routing priority order** shipment — **+13** over the prior 1038/3 baseline, distributed across two commits. **Phase 0 — chatbot routing rules (commit `6a25041`)** added **+8** to `tests/signals/test_assistant_prompts.py` covering the new `MODE RESOLUTION PRIORITY ORDER (CHATBOT ROUTING)` section: `test_prompt_carries_mode_resolution_priority_order_section_header` (pins the section header + the one-sentence anchor "you resolve WHERE the question lives before deciding WHAT to say"); `test_prompt_priority_1_explicit_screen_context` (pins P1 mechanic + behavioral contract "you do NOT ask a clarifying question and you do NOT mention the other mode" + two example surfaces — Signals page with `trading_mode=swing` and Scanner with `scanner_focus=day`); `test_prompt_priority_2_explicit_mode_language` (pins the four trigger terms `swing` / `multi-day` / `day trade` / `intraday` + symmetric swing-only and day-only example responses); `test_prompt_priority_3_structured_dual_answer_template` (pins the verbatim two-paragraph template lines + the INDEPENDENT STATUS REPORTS framing); `test_prompt_priority_3_bans_comparative_and_connective_language` (pins four canonical violations — "Compare the two desks", "frame one as a fallback", "Suggest the user switch desks", "Headline a 'system overall' summary" + three connective-tissue connectives "on the other hand", "however", "instead"); `test_prompt_bans_mode_inference_from_market_behavior` (pins the NEVER section header + three inference patterns by shape — swing-is-quiet-so-user-means-day, high-intraday-volatility-so-day, choppy-markets-suggest-day-trades + the positive framing "Mode is resolved by Priority 1, Priority 2, or Priority 3 only"); `test_prompt_clarifying_question_fallback_is_one_shot_and_verbatim` (pins the exact wording "Do you mean swing (multi-day) or day (intraday) trading? STOCVEST evaluates those as independent decision engines." + "EXACTLY ONE clarifying question" + the dual-desk carve-out so the LLM does not use it as a stalling tactic on screens that already resolve mode); `test_prompt_deterministic_response_to_cross_mode_substitution` (pins the deterministic-shape contract "deterministic and short" + "refuses the comparison, explains the independence" + the validation-numbers gate "does not reference validation numbers as evidence for or against either engine"). **Phase 1 — Dashboard two-desk render** added **+5** to the same test file covering the new dual-desk page-context allowlist: `test_serialize_page_context_emits_dual_desk_posture_on_dashboard` (pins both `swing_desk_posture=active` AND `day_desk_posture=suppressed_no_confirmation` emitted together AND `trading_mode=` is deliberately absent so the LLM does not inherit a single mode via Priority 1); `test_serialize_page_context_dual_desk_session_closed_variant` (pins the distinct `suppressed_session_closed` variant from `suppressed_no_confirmation` — the LLM uses session-bound language for one and intraday-confirmation language for the other); `test_serialize_page_context_rejects_invalid_desk_posture_values` (pins that `swing_desk_posture=actionable_now` and `day_desk_posture=FOMO_BUY` are dropped silently — closes the attack surface where a malicious client could steer the LLM through freeform posture strings); `test_serialize_page_context_emits_day_setups_count_when_nonzero` (pins the day-setups-count field with value `3`); `test_serialize_page_context_dashboard_dual_desk_omits_swing_only_fields` (pins that the dashboard's dual-desk context does NOT accidentally turn on stale scanner-overview fields like `top_setup_1=` / `top_gap_1=` / `gap_with_catalyst_count=` when only the posture fields are supplied — guards against the serializer regressing into emitting unrelated fields). The prior 1038/3 baseline (2026-05-11) was the **mode-separation safety perimeter** upgrade — **+9** over the prior 1029/3 baseline, all in `tests/signals/test_assistant_prompts.py`. The upgrade adds a SECOND perpendicular safety axis on top of the existing LOGGED-OUT / LOGGED-IN two-context perimeter: **Swing trading (multi-day) and Day trading (intraday) are independent decision engines that share market context but never share decisions, readiness, validation, or activity permission.** Six new sections were merged into `ASSISTANT_SYSTEM_PROMPT` between `CRITICAL CONTEXT AWARENESS RULE` and `PRIMARY GOAL` (additive, no destruction of any prior section): (1) **MODE SEPARATION (SWING VS DAY) — ABSOLUTE DESIGN RULE** declares the separation NON-NEGOTIABLE, names the active mode as the authoritative scope, and tells the LLM to treat dashboards/scanners showing both modes as two separate desks; (2) **WHAT MAY BE SHARED ACROSS MODES** enumerates the 5 allowed-shared items (Market regime, Macro context, Sector rotation, Market internals, Risk posture) with the explicit qualifier "Sharing context does NOT imply shared permission to trade"; (3) **WHAT MUST NEVER BE SHARED ACROSS MODES** enumerates the 8 always-mode-specific items (Trade Readiness scores, Layer alignment percentages, Signal validity windows, Gating outcomes, Validation statistics, Accuracy metrics, Portfolio linkage, Journal entries); (4) **SCREEN-LEVEL MODE BEHAVIOR** spells out the per-screen rules (Dashboard = Swing Desk + Day Desk parallel posture reporting; Scanner = two sections not a merged mode-column table when `scanner_focus=both`; Signals = exactly one mode at a time authored by `trading_mode=swing|day`; Signal Validation = mode-isolated tracks; Portfolio = mode attribution per position; Journal = one mode per entry; Performance = mode-segmented never combined-headline); (5) **MODE-AWARE EMPTY-STATE LANGUAGE** requires suppression copy to reflect the suppressed engine's vocabulary (Swing emphasizes multi-day confirmation + regime/sector alignment + structure readiness; Day emphasizes intraday confirmation + volume/momentum timing + session-specific conditions; "Never use identical copy for both modes"); (6) **MODE-SEPARATION USER INTERACTION RULES (ADDITIVE)** stack on top of the existing USER INTERACTION RULES with the new MAY/MUST NOT pairs — explicitly bans "Suggest using Day signals because Swing is quiet (or vice versa)", "Headline a combined accuracy number or a 'system overall' verdict that averages across modes", "Recommend one mode over the other as a workaround for the other's suppression". The exemplar exchange "Swing is quiet — should I day-trade instead?" is written into the prompt with the correct refusal pattern (explain that the two engines gate independently, that Swing's quiet is a Swing decision not a Day permission). Additionally, one new bullet was grafted onto the existing `HISTORICAL VALIDATION CONTEXT (LOGGED-IN ONLY)` MUST NOT list: **"Use the figures to encourage activity during a suppressed regime"** — closes the loophole where the assistant might counter the user contrasting a 62% accuracy figure against today's empty board ("you say accuracy is 62% — why aren't there any setups today?") by using the accuracy figure to argue for activity instead of explaining the active gate. Past directional accuracy and current SUPPRESSION & GATING LOGIC are two independent surfaces that must never be played off against each other. **+9 lock-in tests** in `tests/signals/test_assistant_prompts.py`: `test_prompt_declares_mode_separation_as_absolute_design_rule` (pins "NON-NEGOTIABLE" + "Swing and Day must NEVER be blended"), `test_prompt_lists_what_may_be_shared_across_modes` (pins the 5-item allowed-shared list + "Sharing context does NOT imply shared permission to trade"), `test_prompt_lists_what_must_never_be_shared_across_modes` (pins the 8-item mode-specific list), `test_prompt_carries_dashboard_two_desk_rule` (pins "Swing Desk" + "Day Desk" + "Active / Monitor / Suppressed"), `test_prompt_carries_scanner_two_section_rule` (pins `scanner_focus=both` + "TWO sections" + the anti-pattern "not a single merged table with a mode column"), `test_prompt_carries_signals_page_single_mode_rule` (pins "operates in exactly one mode at a time" + `trading_mode=swing|day` + "Never reuse readiness, alignment, or conclusions across modes"), `test_prompt_carries_mode_specific_empty_state_rule` (pins both per-mode suppression vocabularies + "Never use identical copy for both modes"), `test_prompt_bans_cross_mode_substitution` (pins "Suggest using Day signals because Swing is quiet" + "the two engines gate independently" + the combined-accuracy headline ban), `test_prompt_bans_validation_to_encourage_activity_during_suppression` (pins the new historical-validation ban + "DESCRIPTIVE of past behavior" + "NEVER a reason to override the current SUPPRESSION & GATING LOGIC" + the exemplar user prompt "why aren't there any setups today"). **All 50 pre-existing prompt tests still green** — the merge-additive integration preserved every safety behavior locked in over the prior phases (LOGGED-OUT public-mode rules, six-layer naming model, verbatim decision states, banned-phrase list, payment-tier awareness, the detailed `=== HISTORICAL VALIDATION ===` block semantics including em-dash-as-no-resolved-trades, sentinel anchoring, "no block means no comment + never invent a number + never recall a number from a previous turn"). No production code changes, no Terraform / IAM / API contract / DynamoDB-schema changes, no frontend code touched. The prior 1029/3 baseline (2026-05-11) was D3 analyzer-wire-up lock-in — **+8** over the prior 1021/3 baseline: **2 in `tests/signals/test_technical_analyzer.py`** (`test_bullish_threshold_param_actually_drives_verdict` — same uptrend bars + snapshot through `TechnicalParameters(bullish_threshold=55)` vs `bullish_threshold=99` produces an identical raw score but a differing verdict, locking in that `TechnicalAnalyzer` reads the threshold from the `params` argument it was passed and not from a hardcoded constant; `test_vwap_score_delta_param_actually_moves_score` — same uptrend with `vwap_score_delta=20` (default) vs `vwap_score_delta=0` measurably shrinks the score's absolute distance from the neutral 50, locking in that the VWAP scoring contribution is param-driven), **2 in `tests/signals/test_macro_analyzer.py`** (`test_bullish_threshold_param_actually_drives_verdict` — same risk-on SPY/QQQ/VIX snapshot through `MacroParameters(bullish_threshold=60)` vs `bullish_threshold=99` produces an identical score but flips the verdict from "bullish" to "neutral"; `test_vix_score_band_params_actually_move_score` — same low-VIX (13.0) snapshot with `vix_low_score=80` (default) vs `vix_low_score=40` strictly lowers the macro composite score, locking in that the five VIX band scores in `MacroParameters` thread all the way through to the final macro score), **2 in `tests/signals/test_sector_analyzer.py`** (`test_bullish_threshold_param_actually_drives_verdict` — same SOXX outperforming SPY through `SectorParameters(bullish_threshold=65)` vs `bullish_threshold=99` produces identical score but differing verdict; `test_strong_outperform_score_param_actually_moves_score` — same +3.0% SOXX vs +0.5% SPY snapshot through `SectorParameters(strong_outperform_score=80)` (default) vs `strong_outperform_score=60` strictly lowers the sector score, locking in that the relative-strength scoring table is param-driven), **2 in `tests/signals/test_swing_technical_analyzer.py`** (`test_bullish_threshold_param_actually_drives_verdict` — a clean 210-bar daily uptrend saturates at score=100 under the default scoring contributions; the verdict flips from "bullish" at the default `bullish_threshold=60` to "neutral" at `bullish_threshold=101` (an unreachable threshold), locking in that the swing-mode verdict is param-threshold-driven; `test_above_sma50_score_param_actually_moves_score` — zeroing every scoring contribution in `SwingTechnicalParameters` collapses the score to the exact neutral baseline of 50, and bumping `above_sma50_score` from 0 to 30 on an uptrend where `price > SMA50` adds exactly that 30-point bonus producing score=80, locking in BOTH that the swing baseline math is honest (`+0 if hh else -0 == 0`, so neutral data really does land at 50) AND that the above-SMA50 contribution is param-driven). `NewsAnalyzer` already has the analogous `test_params_threshold_used` from earlier work, so all five analyzers now have at least one wire-is-live lock-in test on top of their existing param-positional-argument signatures. This finishes the D3 analyzer-wire-up theme: every `*Parameters` block is read from `ParameterStore.get_parameters_sync()` and threaded through to the analyzer that owns it (`Technical → TechnicalAnalyzer`, `News → NewsAnalyzer`, `Macro → MacroAnalyzer` and `InternalsAnalyzer`, `Sector → SectorAnalyzer`, `SwingTechnical → SwingTechnicalAnalyzer`, `Composite → CompositeScoreEngine` via the helper added in the D3 first slice), and a refactor that bypasses the params arg now trips a deterministic regression. The prior 1021/3 baseline (2026-05-11) was D3 first slice — **+6** over the prior 1015/3 baseline: **4 in `tests/signals/test_composite_score.py`** locking in the new `build_composite_score_engine_from_params(params)` helper (uses `params.composite.*_weight` and NOT `DEFAULT_BASE_WEIGHTS` as a regression guard, threads thresholds from params, end-to-end wire-is-live check that swapping `SignalParameters` actually moves the composite score, integer→float coercion so a malformed Secrets Manager JSON cannot crash the engine), and **2 in `tests/api/test_signal_recorder.py`** locking in the legacy `swing_composite_handler` integrity gap fix (handler stamps the same `parameter_version` whose `.composite.*` weights it just used — closes the "metadata lies about which weights actually ran" bug that Phase 4's cross-version diff was about to surface; identical payload + two different parameter versions through a monkeypatched `ParameterStore` produces detectably different composite scores, proving the engine is no longer reading hardcoded `DEFAULT_BASE_WEIGHTS`). The prior 1015/3 baseline (2026-05-11) was D2 **Historical Signal Validation** Phase 3c-2 — +21 over the prior 994/3 baseline, distributed across three test files. **9 new prompt lock-in tests** in `tests/signals/test_assistant_prompts.py` covering the new `HISTORICAL VALIDATION CONTEXT (LOGGED-IN ONLY)` section: the section header is present, the `=== HISTORICAL VALIDATION ===` sentinel the serializer emits is referenced in the prompt so the LLM knows what to pattern-match on, the em-dash rendering for NaN accuracy is spelled out as "no resolved non-neutral trades" and explicitly NOT as `"0%"`, dollar-P&L translation is banned (with the load-bearing clause "Directional accuracy is NOT a return number"), trend-prediction is banned (with "descriptive, not predictive"), per-symbol/per-pattern/per-regime/per-decision-state/per-readiness-bucket/per-direction stratifications are explicitly enumerated as "deliberately withheld" with a redirect to `/dashboard/signal-validation`, using the figures to "promote" or claim the system "works" is banned (the rule reads "evidence of behavior, not promotion"), the **"no block means no comment"** rule is locked in alongside "never invent a number" and "never recall a number from a previous turn", comparison to "the market" / "other users" / a benchmark is banned, and recommending mode choice based on accuracy ("Mode choice is not an advice surface") is banned. **8 new service-level tests** in `tests/signals/test_assistant_chat_service.py` for the new `serialize_historical_validation_summary()` helper and the new `reply(historical_validation_summary=...)` kwarg: `None` summary returns empty string, `rows_examined == 0` returns empty string, happy path renders header + `window_days=` + `horizon=` + denominator-transparent `overall=` + per-mode lines + `rows_examined=`, NaN accuracy renders as `—` and never as `0.0%` or `"NaN"`, empty `by_mode` buckets (Phase 1 pre-seeds swing/day vocabulary even when zero rows) are omitted from the serialized block, the authenticated `reply()` path appends the block to the system message when a non-empty summary is provided, the authenticated `reply()` path omits the block (no `window_days=` / `rows_examined=` / `overall=` field-syntax wording in the tail) when `historical_validation_summary=None`, the public `reply_public()` path never carries the block AND refuses the kwarg by signature, no-neutrals overall line still emits the count for transparency. **2 new handler-level tests** in `tests/api/handlers/test_signals_assistant_chat.py` locking in the wiring: the handler resolves a `HistoricalValidationService` against the live recorder, calls `summarize(user_id=rc.user_id, horizon="1d", from_at=now-90d, to_at=now)` (90-day window confirmed within 60s jitter), and forwards the exact summary object to `svc.reply(historical_validation_summary=...)`; when the service raises (slow/unavailable signal-history store), the chat turn still returns 200 and the handler forwards `historical_validation_summary=None` so the prompt's "if absent, do not comment" rule activates. The 3 pre-existing handler tests that monkey-patch `AssistantChatService.reply` now use `**_kwargs` in the fake signature so future kwarg additions do not break them. **Tests/prompt detection sharpness:** the prompt itself now references the `=== HISTORICAL VALIDATION ===` sentinel as a literal string so the LLM can anchor on it — to detect whether the *serialized block* was actually appended (vs just referenced in the prompt rules), tests look at the appended tail after `=== PAGE CONTEXT ===` and check for the field-syntax wording (`window_days=`, `rows_examined=`, `overall=`) that only the serializer emits. The prior 994/3 baseline was D2 Phase 3a — +14 over the prior 970/3 baseline, all in the new `tests/api/handlers/test_signals_historical_validation.py`: `GET /v1/signals/historical-validation/summary` requires auth (`rc.user_id` enforced — 401 without it), missing `horizon` → 400 with message naming the field, invalid `horizon` (`"1w"`) → 400, missing `from` → 400, unparseable `from` → 400, inverted window (`to <= from`) → 400 with "to must be strictly after from", trailing-`Z` ISO datetime form is accepted (JavaScript's `Date.toISOString()` emits it), happy-path 2-correct + 1-incorrect → directional accuracy = 2/3 with `parameter_versions = ["v1"]` and the standing `HISTORICAL_VALIDATION_DISCLAIMER = "Historical signal accuracy does not guarantee future results."` on every response (the wording is now single-sourced in `stocvest/api/legal_copy.py` so the assistant prompt, this endpoint, the Phase 3b dashboard tab, the optional public `/performance` mirror, and the Phase 4 cross-version diff cannot drift), **empty window serializes NaN accuracy as JSON `null` not the literal `NaN`** (the test asserts both `body["summary"]["overall"]["accuracy"] is None` AND `"NaN" not in resp["body"]` — Python's default `json.dumps` emits `NaN` which crashes browsers, so `_bucket_stats_to_dict` converts at serialization time), **per-user scope isolation** (user `u-alpha`'s 2 correct rows do not appear in user `u-beta`'s response and vice versa — locks in the security boundary so one tenant cannot read another tenant's tracked outcomes), `mode=day` filter passes through to the service so swing rows are excluded from totals, lowercase `symbol=msft` is uppercased and filters at the store, horizon `1d` routes to the `outcome_1d` column (a row with `outcome_1h="correct"` and `outcome_1d=None` returns NaN accuracy at 1d but still appears in `rows_examined=1`), and `by_version=true` returns the per-`parameter_version` map shape with v1=2/2 (accuracy=1.0), v2=1c+1i (accuracy=0.5), and the synthetic `__all__` bucket carrying 3/4 (accuracy=0.75, `parameter_versions=["v1","v2"]`). The prior 970/3 baseline (2026-05-11) was D2 Historical Signal Validation Phase 2 — +14 over the prior 956/3 baseline, all in the new `tests/api/services/test_historical_validation_service.py`: empty-store returns NaN accuracy (UI renders "—"), inverted window (`to_at <= from_at`) short-circuits without hitting the store, equal-bounds zero-width window short-circuits, happy-path 2-correct + 1-incorrect + 1-neutral → accuracy = 2/3 (neutrals excluded from denominator — matches `/v1/signals/performance/summary`), **inclusive-lower / exclusive-upper window edge cases** (rows at `from_at` kept, rows at `to_at` dropped, before/after/inside boundary rows handled correctly — same semantics as the GSI `scope_generated_at` predicate `generated_at >= cutoff_iso`), naive-`generated_at` rows are treated as UTC for comparison so legacy DynamoDB hydrates do not crash, `user_id` / `symbol` / `mode` pass straight through to the underlying store (the store is the auth perimeter — the service does not silently change scope), `days_back` is computed to cover `from_at` from now and clamped at `MAX_LOOKBACK_DAYS = 366`, horizon routing (a row with `outcome_1h` populated but `outcome_1d` pending counts at 1h, returns NaN at 1d, and still appears in `rows_examined`), `summarize_by_parameter_version` produces one `BucketStats` per version plus an `__all__` combined aggregate (v1=2/2 correct, v2=1c+1i, all=3/4 = 75%, `parameter_versions = ("v1", "v2")`), rows with `parameter_version = None` or empty/whitespace land in `UNKNOWN_VERSION_KEY = "unknown"` so legacy rows are never silently dropped, an empty window produces only an `__all__` bucket with NaN accuracy, and the window filter is applied BEFORE the version split (out-of-window v1 rows must not inflate the v1 bucket). The prior 956/3 baseline (2026-05-11) was D2 Historical Signal Validation Phase 1 — +14 over the prior 942/3 baseline, all in the new `tests/signals/test_historical_validation.py`: empty-input NaN accuracy, all-neutral NaN accuracy, horizon routing between `outcome_1h` / `outcome_1d` columns, neutrals excluded from the directional denominator (matches `/v1/signals/performance/summary`), stratification by Decision state at entry (incl. `unknown` for legacy rows without the validation-ledger columns), Macro regime at entry (engine vocabulary `risk_on` / `neutral` / `risk_off` / `avoid` + `unknown` fallback, NOT the UI Bullish/Neutral/Bearish form), trading Mode (`swing` / `day`), Pattern family (declared `swing_composite` / `orb` / `vwap` / `momentum` / `gap_with_catalyst` + `other` overflow for unknown patterns so nothing silently vanishes), Trade Readiness bucket (high ≥ 70 / moderate 40-69 / low < 40 — matches Evidence card thresholds), Direction (`bullish` / `bearish` / `neutral`), `parameter_versions` sorted-deduped provenance, and frozen-dataclass immutability for both `BucketStats` and `HistoricalValidationSummary`. The prior 942/3 baseline (2026-05-11) was the two-context safety perimeter upgrade; +7 over the 935/3 baseline — all in `tests/signals/test_assistant_prompts.py` (`test_prompt_declares_critical_context_awareness_two_context_perimeter`, `test_prompt_carries_logged_out_golden_rule`, `test_prompt_explicitly_bans_per_symbol_evaluation_when_logged_out`, `test_prompt_carries_homepage_safe_refusal_template_verbatim`, `test_prompt_lists_concrete_ticker_examples_in_public_mode_ban`, `test_prompt_carries_explain_why_boundary_exists_in_tone`, `test_prompt_keeps_public_mode_rules_label_for_section_routing`) and one tightened assertion in `tests/signals/test_assistant_chat_service.py` (`test_reply_public_calls_claude_with_public_mode_marker` now isolates the appended `=== PAGE CONTEXT ===` tail rather than scanning the whole prompt, because the static instruction block now mentions `session_mode=authenticated` as descriptive text inside the CRITICAL CONTEXT AWARENESS RULE — that mention is a guardrail, not a routing signal). The 935/3 baseline (2026-05-11, explanatory-voice prompt upgrade) was +19 over the 916/3 baseline; all 19 were in `tests/signals/test_assistant_prompts.py` locking in the new prompt sections — all in `tests/signals/test_assistant_prompts.py` locking in the new prompt sections (PRIMARY GOAL, CORE PRODUCT PHILOSOPHY, SUPPRESSION & GATING LOGIC, REGIME TRANSITIONS, BACKTESTING & VALIDATION, PAYWALL AWARENESS, USER INTERACTION RULES, MAY/MUST NOT lists), the verbatim Decision lines from `lib/signal-evidence/trade-decision.ts`, the real macro pulse states (Elevated/Upcoming/Known and absorbed/Unavailable), the real sector chip labels (Confirming/Non-confirming/Risk-on/Defensive/Narrow), the 0-100 Trade Readiness scale, the explicit ban list for invented concepts (System Confidence, VIC, Volatility Control, Symbol Readiness Score, Sector Fragmented), the suppression copy anchors, the validation page anchors (/performance, /dashboard/signal-validation) + standing disclaimer, the banned self-limitation phrases, the banned BAD/GOOD response shapes, the three-mode structure, and the foundational principle. The earlier 916/3 baseline: +10 from prior 906/3 baseline — 4 new in `tests/api/handlers/test_signals_assistant_chat.py` covering the new unauthenticated `POST /v1/public/assistant/chat` (no-auth happy path, `page_context` ignored, invalid body, empty-messages deterministic intro), 4 new in `tests/signals/test_assistant_chat_service.py` covering `AssistantChatService.reply_public` (Claude call carries `session_mode=public`, never `session_mode=authenticated`, deterministic intro / outage fallback, public path refuses a `page_context` kwarg by signature), and 2 new in `tests/signals/test_assistant_prompts.py` covering the `session_mode=public/authenticated` markers + invalid `session_mode` collapse) |
| Frontend tests | `cd frontend && npm run test` | **342 passed** (43 test files; verified 2026-05-12 after the **Gap-card mode-fit classifier + per-row Evidence routing (B30 Phase 4)** shipment — **+6** new tests appended to the existing `tests/scanner-mode-resolution.test.ts` under a new "resolveGapCardTradingMode — per-gap-row classifier-driven routing (B30 Phase 4)" describe block locking in the new `resolveGapCardTradingMode(scannerSetupMode, modeBestFit)` helper: `explicit scannerSetupMode 'day' wins regardless of classifier verdict` (4-way: swing/day/either/undefined verdict all collapse to `"day"` — explicit user context ALWAYS overrides advisory classifier), `explicit scannerSetupMode 'swing' wins regardless of classifier verdict` (symmetric), `'both' view honors classifier 'swing' verdict`, `'both' view honors classifier 'day' verdict (cross-engine routing)` — **the load-bearing branch**: without it, the day verdict in `"both"` view would be cosmetic and every gap card would still open swing, `'both' view with 'either' verdict falls back to swing` (preserves pre-Phase-4 default), `'both' view with undefined verdict (legacy / cached response) falls back to swing` (back-compat for the cache TTL window during deploy). Previous baseline **336** from the **Mode-resolution helpers extracted as a documented contract (B30 Phase 2)** shipment — **+12** new tests in the new `tests/scanner-mode-resolution.test.ts`. Closes the Severity-3 #8 gap I called out in my own B30 audit ("lock-in tests for the call sites — the kinds of regressions that survive code review because the screen looks fine"). Pure refactor with zero production behavior change — the mode-resolution rules that were one-line ternaries inline inside `scanner-page-client.tsx` are now extracted into the new module `frontend/lib/scanner-mode-resolution.ts` with two named exports: (1) `resolveEvidenceTradingMode(scannerSetupMode)` collapses `"day" → "day"`, `"swing" → "swing"`, `"both" → "swing"` (the "both → swing" rule is the load-bearing product decision deferred from Phase 1 Severity 2 — picking swing here is reversible if a future side-by-side dual Evidence modal is shipped); (2) `resolveSetupRowTradingMode(groupKey, fallback)` handles the per-row case where in `scannerSetupMode === "both"` view a swing-group row MUST open the swing engine and a day-group row MUST open the day engine even though both groups render on the same screen. `scanner-page-client.tsx` now imports these and uses them at the call sites (`evidenceTradingMode = resolveEvidenceTradingMode(scannerSetupMode)` for the gap card; `groupTradingMode = resolveSetupRowTradingMode(group.key, evidenceTradingMode)` for the setup card). The module carries a detailed file-level JSDoc explaining WHY each rule exists — anchoring on both load-bearing invariants (the composite engine selected by `compositeRouteForMode` AND the chip-filter that fires on `body.mode === "swing"` inside `applySwingCompositeEnrichment`), and explicitly noting that `panelNewsTradingMode` MUST stay in lockstep with `resolveEvidenceTradingMode` so news lookback + composite engine + chip filter are all consistent on every Evidence-modal open. The 12 new tests cover: `resolveEvidenceTradingMode` for all three `ScannerSetupLoadMode` inputs (day/swing/both); a parametric lock-in that the resolution matches the `panelNewsTradingMode` pattern across all three inputs (the lockstep invariant); `resolveSetupRowTradingMode` for all four `SetupRenderGroup.key` values (swing / swing-only / day / day-only), each asserted under both swing and day fallback values to prove the per-row rule wins regardless of fallback; the load-bearing "day-group rows in scannerSetupMode === 'both' override the 'both → swing' fallback" invariant; the symmetric "swing-group rows in scannerSetupMode === 'day' override the day fallback" guard; the defensive-fallback behavior for unknown group keys (`"intermarket"` / empty string); and a case-sensitivity lock-in (`"Swing"` / `"DAY"` do NOT prefix-match, so a future caller sending mixed-case keys produces a visible test failure rather than a silent fallback). The lock-in tests directly close the regression mode that caused B30 in the first place — a refactor that changes the resolution rule but leaves the JSX wiring untouched now fails one of these tests immediately. Frontend baseline **324 → 336 passed (+12)**, backend **1061 passed / 3 skipped** unchanged (no Python touched). `npm run build`: Compiled successfully. `tsc --noEmit` zero new errors. No Terraform / IAM / API contract / DynamoDB-schema changes. Prior baseline verified 2026-05-12 after the **Evidence-card composite mode routing fix (B30 Phase 1)** shipment — **+9** new tests in the new `tests/signal-evidence-composite-mode-routing.test.ts`. Closes a cross-mode data-leakage bug discovered during a user-prompted audit: while the **Signals page** correctly branched `tradingMode === "swing" ? "/api/stocvest/signals/composite/swing" : "/api/stocvest/signals/composite/real"`, the **Scanner gap card "View Signal" button**, the **Scanner setup-card "View Signal" button**, and the **Dashboard Swing Desk "View Evidence" button** all hard-coded `enrichEvidenceWithRealComposite(base)` which only ever hits `/api/stocvest/signals/composite/real` → backend `/v1/signals/composite/real` → day engine. So a user clicking the View Signal CTA on a swing setup row was silently reading the day composite — VWAP / EMA9 / EMA20 / ORB chips and intraday lookback windows leaked into a swing-context modal, the chip-filter (`filterChipsForMode` on `body.mode === "swing"`) never fired because `body.mode` arrived as `"day"` from the wrong route, and the verdict math reflected day-tier news / macro / geo lookback windows + daily-cadence sector posture instead of the swing engine's 120h news / 14d macro / 168h geo / weekly sector configuration. The four wiring changes: (1) `frontend/lib/signal-evidence.ts` introduces `enrichEvidenceWithComposite(evidence, mode: "swing" \| "day")` as a type-safe parameterised helper — the `mode` argument is required so future call sites cannot omit it without a compile error; the route selector is a pure `compositeRouteForMode(mode)` function; the legacy `enrichEvidenceWithRealComposite` name is kept as a `@deprecated` thin alias that hard-codes `"day"` for any external consumers but is no longer called from any first-party surface in the repo. (2) `frontend/components/dashboard-redesign.tsx` Swing Desk row now calls `enrichEvidenceWithComposite(base, "swing")` (the news fetch on the line above already correctly used `newsTradingMode: "swing"`, so the inconsistency where news was mode-correct and composite was not is also fixed). (3) `frontend/components/scanner-page-client.tsx` introduces a top-level `evidenceTradingMode = scannerSetupMode === "day" ? "day" : "swing"` mirroring the existing `panelNewsTradingMode` resolution so news lookback and composite engine stay in lockstep across the surface; the **gap card** `openGapEvidence` callback uses this top-level mode (the scanner is swing-first when `scannerSetupMode === "both"`, matching Section 7's stacked-desks render order). (4) The **scanner setup card** "View Signal" button goes one step further — it derives a per-row `groupTradingMode = group.key.startsWith("swing") ? "swing" : group.key.startsWith("day") ? "day" : evidenceTradingMode` from the `setupRenderGroups` group key, so in `scannerSetupMode === "both"` view a swing-group row opens the swing engine and a day-group row opens the day engine even though both groups render on the same screen; the `fetchSymbolNews` call inside that button was simultaneously upgraded from `newsTradingMode: panelNewsTradingMode` to `newsTradingMode: groupTradingMode` for the same lockstep reason. The 9 new lock-in tests in `tests/signal-evidence-composite-mode-routing.test.ts` (using `vi.fn()`-based `globalThis.fetch` spy): `swing mode posts to /api/stocvest/signals/composite/swing` + `day mode posts to /api/stocvest/signals/composite/real` (the positive route assertions); `never hits the wrong route — swing call must not reach /composite/real` + `never hits the wrong route — day call must not reach /composite/swing` (the negative assertions — these are the canary tests that would fire on the original bug if it were reintroduced); `forwards upper-cased symbol payload and uses same-origin credentials` (locks in the `JSON.stringify({ symbol: "TSLA" })` body shape so the BFF can attach the Cognito cookie, and `tsla` → `TSLA` is canonicalized client-side to match the backend's symbol normalization); `returns evidence unchanged (with insight fallback) when symbol is empty` (no fetch is dispatched if `symbol.trim()` is empty — defensive against an empty-string row); `returns evidence with insight fallback when fetch throws` + `returns evidence with insight fallback when response is non-OK` (both error paths preserve `evidence.symbol` and emit the local `deriveEvidenceInsightFallback` so the Evidence modal still renders something coherent on a 503 / network error); `deprecated enrichEvidenceWithRealComposite still routes to the day engine` (back-compat lock-in — the alias must continue to hit `/composite/real` so any out-of-repo consumer doesn't silently break, but the typed `enrichEvidenceWithComposite(evidence, mode)` signature on the first-party surface is the regression guard). No backend code, no Terraform / IAM / API contract changes; backend test baseline holds at **1061 passed / 3 skipped**. The prior 315/41 baseline reflected the **B29 Phase 1 signal-card explanation refinements + B29 Phase 2 dashboard card visual uniformity** shipments which weren't reflected in this row (counts climbed from 301 → 314 → 315 over the two commits before today's fix). Prior baseline verified 2026-05-12 after the **Assistant on-card CTA routing rule (B28 Phase 2c follow-up)** — frontend baseline unchanged at 301 because the fix was prompt-side, backend baseline went **1051 → 1055 passed** (+4 in `tests/signals/test_assistant_prompts.py`); prior baseline verified 2026-05-12 after the **Sector behavioral reframe + Signal Validation Ledger relocation + sub-card borders (B28 Phase 2c)** shipment — **+16** over the prior 285/41 baseline (9 new in `tests/shared-context-derivations.test.ts` for `classifyRotationProfile` (closed-set: Concentrated / Rotational / Mixed / Unknown; NaN-safe input filtering; threshold lock-ins at spread ≥ 3 / positives ≤ 2 for Concentrated, positives ≥ 2 + negatives ≥ 1 + spread ≥ 1.5 for Rotational) + `rotationProfilePlainLine` (14-word anti-evaluative-language ban: trending / leading / bullish / bearish / winners / losers / buy / sell / long / short / allocate / should / setup / trend intact); +5 net new in `tests/dashboard-role-color-language.test.tsx`: `section_C_title_uses_neutral_behavioral_language_NOT_breadth_tone` locks the verbatim "Sector Participation (Last ~5 Sessions)" rename; `section_C_rotation_profile_label_is_one_of_the_closed_set` rejects strategy-coded labels in DOM; `section_A_index_tiles_have_direction_aware_borders_green_red_neutral` locks the `data-tile-direction` attribute resolving to up / down / flat against fixture pct5d values +1.19 / -2.0 / +0.05; `subsections_B_C_D_E_each_render_as_their_own_bordered_subcard` asserts the `data-subsection-card` attribute present on B/C/D/E and explicitly absent on A (no double-wrap contract); `swing_desk_primary_read_card_renders_with_highlighted_role_border` locks the testid + 1.5px style + color-mix swing-role hue; the existing `section_C_renders_a_participation_CATEGORY_plus_the_sector_chip_row` was rewritten to require BOTH rotation-profile + participation categories AND assert the chip-row testid is NULL AND assert no sector ETF NAMES (XLK / XLC / XLE / XLF / XLY / XLI / XLV / XLP / XLU / XLB / XLRE) appear in Section C text content; the existing `signal_validation_ledger_is_a_tertiary_surface_NOT_a_role_master_card` was rewritten as `dashboard_does_NOT_render_signal_validation_ledger_anymore` asserting absence of both the testid AND any `<a href="/dashboard/signal-validation">` anchor in the dashboard root; +2 net new in `tests/performance-no-combined-headline.test.tsx`: `logged-in Performance page renders the ledger link surface` (testid + "Open full ledger (Swing / Day)" + `/dashboard/signal-validation` href + Mode Separation reminder) and `logged-out public Performance mirror does NOT render the ledger link` (UI gating matches API gating from Phase 3c-1). Prior 285/41 baseline was the **Dashboard Shared Context master card + bright rail-line border treatment (B28 Phase 2b)** shipment — **+32** over the prior 253/40 baseline (29 new in `tests/shared-context-derivations.test.ts` for the pure helpers `classifyVolatility` / `classifyParticipation` / `classifyRiskHorizon` / `buildEnvironmentSummary` with threshold-boundary lock-ins + anti-leak assertions; +3 net new in `tests/dashboard-role-color-language.test.tsx` for the three-master-card invariant + tertiary ledger contract + borderAccent uniqueness). The full Phase 2b test suite locks in: ONE shared master card with five sub-sections A-E in DOM order; bright 2px rail-line border in `borderAccent` distinct from `accent` per role per theme; both top-rail AND left-stripe edge anchors on every role-tinted card; Section A renders three index tiles each with an SVG sparkline (and no axes); Section B is category-only with no "ATR" text leak; Section C carries both category label and sector chips; Section D carries both category label and earnings list; Section E carries environment summary + verbatim guardrail clauses; banned evaluative words audited across master-card text; Signal Validation Ledger lives as tertiary surface with `data-card-role === null`. Prior 253/40 baseline was the **Dashboard role-color language + Shared Context copy redesign (B28 Phase 2)** shipment: `swing_desk_panel_carries_role_swing_and_role_pill_SWING_MULTI_DAY` (data-card-role + verbatim role-pill label); `day_desk_panel_carries_role_day_and_role_pill_DAY_INTRADAY`; `short_horizon_card_carries_role_shared_and_role_pill_SHARED_CONTEXT`; `market_pulse_and_sector_rotation_and_catalysts_and_validation_ledger_are_all_role_shared` (all four other shared cards carry the same role + pill — locks the single-family contract); `role_accents_are_three_distinct_hue_families_not_aliased_to_each_other` (set-cardinality assertion across both dark and light themes — closes the regression where a future refactor could accidentally collapse two roles to the same accent); `role_tinted_card_renders_a_left_edge_role_stripe` (peripheral-glance contract via `data-testid="dashboard-card-role-stripe"`); `card_title_is_short_horizon_market_state_NOT_weekly_market_context` (timeframe-anchored title + legacy swing-coded title forbidden); `status_line_is_observational_NOT_evaluative_constructive_or_setup_language` (six banned words enumerated — `["constructive", "defensive", "tape", "(background)", "setup", "continuation"]`); `status_line_for_net_downward_average_uses_net_downward_language` (avg ≤ −0.6% threshold-boundary lock-in); `status_line_for_mixed_average_uses_mixed_direction_language` (mixed threshold lock-in); `timeframe_binding_clause_and_why_this_matters_hint_are_rendered_verbatim` (`SHORT_HORIZON_TIMEFRAME_LINE` + `SHORT_HORIZON_WHY_THIS_MATTERS` exported constants pinned + the explicit `"does not imply trade duration"` clause assertion); `subtitle_explicitly_disclaims_trade_signal_status` (`"not a trade signal"` + `"shared background input for all desks"` both present). Plus one existing test in `tests/dashboard-two-desk.test.tsx` was rewritten to anchor on the role pill + `data-card-role` instead of eyebrow text — the canonical SHARED-CONTEXT signal is now the role pill, not the eyebrow. The prior 241/39 baseline was the **Dashboard two-desk render** shipment — **+20** over the prior 221/37 baseline, distributed across two new test files and one updated existing file. **+11 in new `tests/dashboard-day-helpers.test.ts`** locking in the day-side posture state machine and the day-vocabulary anti-leak contract: `dayDeskPostureKind` posture mapping (scanner-error wins over everything else, market-closed wins over setup state — "Day Desk is session-bound and closed-market posture wins", extended-hours treated as session-closed for Day Desk gating, market-open + no setups → `suppressed_no_confirmation`, top-score-at-or-above-`DAY_DESK_ACTIVE_SCORE_FLOOR=0.55` → `active`, top-score-below-floor → `monitor`); `empty_day_posture_headline_uses_day_vocabulary_only` (six swing phrases ["regime alignment", "sector confirmation", "weekly structure", "multi-day structure", "DailyBarScanner", "EMA200"] explicitly ABSENT from every posture headline across all five `DayDeskPostureKind` variants AND "Day Desk" present in every headline — a regression that reuses swing copy on the day side trips this immediately); `empty_day_one_liner_extended_hours_explicitly_called_out` (the extended-hours one-liner mentions "extended" verbatim so the user doesn't read "closed" and assume weekend/holiday); `day_desk_suppression_status_line_distinguishes_session_vs_confirmation` (session-closed line contains "outside regular session" while no-confirmation line contains "intraday confirmation absent" — two distinct verbal categories pinned); `buildDayReenableBullets_uses_day_vocabulary_in_both_open_and_closed_branches` (open branch and closed branch both emit exactly 3 bullets; combined text contains "volume" + "momentum" + at least one of session/intraday/orb; combined text contains NONE of the six swing phrases — locks the anti-leak invariant across both posture branches); `buildDayReenableBulletsShort_returns_three_distinct_bullets`. **+9 in new `tests/dashboard-two-desk.test.tsx`** locking in the dashboard render contract: `dashboard_renders_swing_desk_AND_day_desk_simultaneously` (both `data-testid="swing-desk-panel"` and `data-testid="day-desk-panel"` exist in the DOM at the same time); `day_desk_follows_swing_desk_in_dom_order_stacked` (uses `Node.DOCUMENT_POSITION_FOLLOWING` to assert Day Desk comes after Swing Desk regardless of CSS — guards screen-reader order and tab order against a future re-flow); `weekly_market_context_eyebrow_is_shared_context_NOT_swing_desk` (narrowed to the eyebrow element via `data-testid="dashboard-card-eyebrow"` because the subtitle legitimately names both desks for explanatory purposes — locks in the eyebrow text "shared context" + ABSENCE of "swing desk" on the eyebrow specifically); `day_desk_posture_when_market_closed_is_suppressed_session_closed` (data-attribute `data-day-desk-posture=suppressed_session_closed` + posture pill label "suppressed" + "session closed" copy visible in the suppression block); `day_desk_posture_when_market_open_no_setups_is_suppressed_no_confirmation` (data-attribute + "intraday confirmation absent" copy); `day_desk_renders_real_setups_when_market_open_and_intraday_setups_present` (data-attribute flips to `active` + the setup symbols DAYAAA + DAYBBB render inside the `data-testid="day-desk-signals"` region + suppression block ABSENT — mutually exclusive content rule); `day_desk_reenable_copy_uses_DAY_vocabulary_not_swing_vocabulary` (the "what would re-enable day setups" heading uses the exact phrase from the spec; day-vocabulary tokens "volume" + "momentum" + at least one of "session"/"orb"/"intraday" all present; swing-vocabulary phrases "regime alignment" + "sector confirmation" both ABSENT from inside the day re-enable block); `swing_desk_panel_has_swing_eyebrow_NOT_day_eyebrow` (swing eyebrow contains "swing desk" + "multi-day" but explicitly NOT "intraday (session-bound)"); `day_desk_footer_points_at_day_scanner_not_swing_scanner` (the "View day scanner →" link's href is exactly `/dashboard/scanner?mode=day` — a regression that defaulted to swing would silently route Day Desk users into the wrong engine and is caught here). **+1 updated in `tests/dashboard-swing-only.test.tsx`**: the `test_dashboard_shows_empty_state_when_no_swing_setups` test previously asserted `expect(screen.queryByText("INTRADAY")).not.toBeInTheDocument()` (swing-only-dashboard contract) — that contract is now wrong because Day Desk renders intraday setups; replaced with a positive contract that the intraday setup is rendered INSIDE the Day Desk's `data-testid="day-desk-signals"` region while the Swing Desk shows its empty-state copy, confirming the data partition by `scanner_mode` produces TWO desks fed by ONE scanner payload. The prior 221/37 baseline was the **Mode Separation UI compliance shipment** — **+13** over the prior 208/34 baseline, distributed across four commits: Phase A1 (+2 in `tests/scanner-swing-toggle.test.tsx`) locks in the scanner `Both` two-section render and mode-specific empty-state copy; Phase A2 (+5 in new `tests/signals-page-mode-plumbing.test.ts`) locks in `fetchUserSignalHistoryPage` + `fetchUserEvaluatedSignals` forwarding `mode` to the URL query through every paginated page (no mid-pagination swap, legacy mode-omission contract preserved); Phase C (+2 in new `tests/historical-validation-panel-mode.test.ts`) locks in `MODE_OPTIONS = ["swing", "day"]` with no `"all"` and the narrow `ModeFilter` TypeScript type; Phase D (+4 in new `tests/performance-no-combined-headline.test.tsx`) locks in the absence of combined-engine wording in `LandingPerformanceSection`, `PerformanceTrackingContent` top row, and `PublicValidationSection`, plus the presence of the new per-engine handoff line + the per-mode track cards with their percentages. The prior 208/34 baseline was D2 **Historical Signal Validation** Phase 4 — +14 over the prior 194/34 baseline, all in the existing `tests/historical-validation-api.test.ts` covering the new cross-version diff helpers: `diffBucketStats` (B−A subtraction across accuracy and counts, null-accuracy-on-either-side collapses `accuracyDelta` to null for the em-dash UI contract, identical buckets produce all-zero deltas, missing-on-one-side buckets are treated as zero-rows with null accuracy, small-sample flags are computed independently per side using `SMALL_SAMPLE_THRESHOLD = 10`), `formatAccuracyDelta` (renders `+5.2pp` / `-3.1pp` for finite deltas with one-decimal precision, `0.0pp` for exact-zero so the user sees a real measurement rather than an em-dash, em-dash for null / NaN / Infinity matching `formatAccuracyPercent`'s discipline), `selectComparableVersions` (excludes the synthetic `__all__` aggregate, sorts `v<n>` numerically so `v10` follows `v9` rather than landing between `v1` and `v2`, always places `unknown` last so legacy unstamped rows are an explicit choice never a default), and `defaultCompareSelection` (picks the last two comparable versions as the "latest pair of changes" lens, returns null when fewer than two comparable versions exist so the panel can render a calm "need at least two parameter versions" empty state). The prior 194/34 baseline was D2 Phase 3c-1 — +6 over the prior 188/34 baseline, all in the existing `tests/historical-validation-api.test.ts` — +6 over the prior 188/34 baseline, all in the existing `tests/historical-validation-api.test.ts` covering the new public fetcher: happy path (URL contains `/v1/signals/historical-validation/public-summary` and does NOT contain `/api/stocvest/`, fully anonymous — no `credentials`), `horizon` / `mode` / `daysBack` forwarding, 401 collapses to `null` AND does NOT call `surfaceAuthErrorIfAny` (no session to refresh on a public endpoint), 500 / malformed / non-JSON collapse to `null`. Prior 188/34 baseline was D2 Phase 3b — +12 over the prior 176/33 baseline, all in the new `tests/historical-validation-api.test.ts`: `fetchHistoricalValidationSummary` happy path (query string carries `horizon` / `from` / `to`, response parses with `parameter_versions=["v1"]`, `credentials: "include"`), `mode` + lowercase `symbol` passthrough (client uppercases the ticker before sending — `symbol=msft` → `symbol=AAPL` equivalent), **`null` accuracy preserved verbatim from the wire** (locks in the em-dash-never-0% UI contract end-to-end from the backend NaN→null serialization in `_bucket_stats_to_dict`), 401 returns `null` AND routes the response through the shared `surfaceAuthErrorIfAny` (so the Phase 2 sliding-session refresh flow works uniformly here too), missing-`summary` malformed body returns `null`, non-JSON body returns `null`; `fetchHistoricalValidationByVersion` appends `?by_version=true` and parses the per-version map (`v1` accuracy 2/3, `v2` accuracy 0.5, `__all__` carries combined `parameter_versions=["v1","v2"]`), missing `__all__` collapses to `null` so the dashboard never renders a partial-aggregate diff; `formatAccuracyPercent` renders "—" for `null` and NaN and Infinity, one-decimal percent for finite values (`0.6666` → `"66.7%"`, `1` → `"100.0%"`, `0` → `"0.0%"`); `buildTrailingWindow(30)` produces ISO-8601 `{ from, to }` with a 30-day delta. The prior 176/33 baseline was the sliding-session refresh work — +15 over the prior 161/30 baseline, all in three new lock-in files: `tests/refresh-session.test.ts` (5 — happy 200 / 401 / network-error / parallel-coalescing single-flight / post-failure cooldown), `tests/browser-api-fetch-refresh.test.ts` (5 — happy 200 / 401-refresh-200 / 401-refresh-fail / 401-refresh-401 / 403-never-treated-as-auth), `tests/surface-auth-error.test.ts` (5 — null / 200 / 403 / 401-refresh-success / 401-refresh-fail). Vitest **`fileParallelism: false`** in **`vitest.config.ts`** (avoids **`global.fetch`** races across files). |
| Frontend build | `cd frontend && npm run build` | **success** (last verified 2026-05-10; the sliding-session refresh work on 2026-05-11 adds one new BFF route at `frontend/app/api/auth/refresh/route.ts` plus three new client `lib/` modules — no Next.js page surface changes, same client routes). `tsc --noEmit` confirms zero new TypeScript errors (the 24 pre-existing errors in `tests/order-confirmation-modal.test.tsx`, `tests/pdt-api.test.ts`, `tests/signal-evidence.test.ts`, `tests/signal-evidence-layers.test.ts` are unchanged from prior baselines). |

---

## 14. Appendix: Business / entity (high level)

- Entity target: **STOCVEST LLC** (Delaware); formation and Stripe Atlas items **track outside this doc**.  
- **What STOCVEST is:** signal intelligence + execution tooling; user keeps custody via their broker.  
- **What it is not:** RIA, broker-dealer, custodian, fund manager.  
- Before **paid** users: attorney-reviewed ToS/Privacy, LLC, Stripe, production infra.
