# STOCVEST — Backlog (planned work)

**Use with [`CONTEXT.md`](./CONTEXT.md) (current state, ops, test baselines) and [`IMPLEMENTED.md`](./IMPLEMENTED.md) (shipped work archive).**

This file holds **planned work only**, grouped by priority. Shipped rows live in `IMPLEMENTED.md`; status-at-a-glance lives in `CONTEXT.md` §1–§3.

**Last updated:** 2026-06-30 — **B80 — Unified structure engine (SHIPPED; PR #215)** — see [`IMPLEMENTED.md`](./IMPLEMENTED.md) B80. Prior — **B79 — Direction confidence (High/Moderate/Low) (SHIPPED; presentation-only, no signal-math change)** (Trigger: user proposed an elite-desk directional model — *"direction = weighted consensus of trend/structure/participation/context, a continuous score with confidence, not a vote"* — and asked (a) do we already do this, (b) should we differentiate day vs swing weights, (c) should we **show how confident the direction is**. **Audit finding:** our `CompositeScoreEngine` *already is* that model — `directional_score = Σ(score·base·regime·confidence·sensitivity)/Σweights`, ±0.20 verdict band, a `confidence` measure, `alignment_ratio` + `conflicted_layers` + a contradiction penalty, regime-aware weights, B73 mean-reversion subtype. Two gaps the proposal exposed: **(1)** participation/structure aren't standalone layers (they live inside `technical`) — left as-is (re-architecture, signal already captured); **(2)** *direction confidence wasn't surfaced as its own read* — `composite.confidence` was only exposed as `signal_strength`/Trade Readiness, conflating data-quality with conviction. **Built:** new pure `stocvest/signals/direction_confidence.py::assess_direction_confidence` — gate-based tiering over **conviction** `|score|` (High ≥0.35 / Mod ≥0.20), **agreement** `alignment_ratio` (≥0.67 / ≥0.50), **data quality** `confidence` (≥0.60 / ≥0.40); a tier requires ALL three to clear its bar; neutral verdicts are always Low; emits `tier` + a 0–100 tooltip `score` + a plain-English `reason` naming the limiting dimension. Wired into `build_swing_composite_evidence_fields` (so **both** day + swing engines emit `direction_confidence` / `_score` / `_reason`) and plumbed onto desk discovery rows (`opportunity_desk/discovery_row.py`) → feed cards. Frontend twin `frontend/lib/signal-evidence/direction-confidence.ts` (parse + deterministic client fallback mirroring the backend gates) wired through `parseSwingCompositeInsight` + `deriveEvidenceInsightFallback`; **chip** in the deep-dive verdict banner (`deep-dive.tsx`) and the trading-room signal card (`dashboard-trading-room.tsx`, "High/Mod/Low conf"). Backend **2628**/4 (+12: `tests/signals/test_direction_confidence.py` (11) + 1 wiring case in `tests/api/test_swing_composite_evidence.py`); frontend +9 (`frontend/tests/direction-confidence.test.ts`). **Weight-differentiation finding (ran the D10 walk-forward against the live `SignalHistory` ledger, 11,456 records):** the proposal's day/swing weight *intent is already live* — Secrets Manager carries differentiated per-mode blocks (day `tech 0.35 / internals 0.21 / news 0.12 / macro 0.12`; swing balanced `tech 0.22 / macro 0.20 / sector 0.18 / news 0.18`); an intent-mapped six-layer candidate AND the optimizer's own ±0.05 grid both produced **zero val lift** over baseline (swing Δ0.000 n_val=7; day train −0.023 / val 0.000 n_val=22 — both below the 30-row accept gate) → **no weight change made** (current weights already at/near the local optimum on the trailing-60d window; correcting the earlier "day/swing ship identical defaults" note — the code DEFAULT_BASE_WEIGHTS are only the test fallback). **Display-only — no Terraform, no API contract break, no Lambda redeploy required for the math** (the new field rides the existing composite/desk payloads; ships via CI `deploy-lambda` on merge for the backend field + Vercel for the chips). Prior — **B78 — Target geometry v3 (volatility-normalized, structure-derived T1/T2) (SHIPPED; flag default ON, goes live on merge → CI `deploy-lambda`)** (Trigger: user flagged two recurring geometry defects on live emails/cards — (a) **CYTK** T2 = $118 carried `reference_target_2_provenance="resistance"` but was actually a Mizuho analyst PT (~+38.7%, just inside the B76 40% cap) → the R/R gate treated an analyst PT as structural resistance; (b) **T1 too close to entry** for days — a high-of-day breakout collapses the session-high T1 onto entry, and the B76 `_MIN_T1_REWARD_PCT` 0.3% floor was far too small. After a design review the user approved a refined elite-desk model: **STOP = where you're wrong, T1 = where the market first reacts, T2 = where the trend exhausts — all scaled by ATR and bounded by structure.** **Implementation:** new pure `stocvest/api/services/target_geometry.py` — **T1** = nearest clustered structural resistance whose distance ∈ [α·ATR, β·ATR] (α/β = 0.8/2.0 day, 1.5/3.0 swing), else `entry+α·ATR` (so T1 has an enforced volatility floor AND a ceiling — never glued to entry, never absurdly far); **T2** = `min(valid candidates)` among {next structural resistance above T1, `entry+β₂·ATR` (β₂ = 2.5 day / 4.0 swing), `entry+2R`} with provenance = whichever won (`resistance` / `atr_extension` / `2r_extension`); **resistance enrichment** = all daily-bar highs (30 sessions) + SMA20/50/200 (when above entry), clustered with adaptive ε = `max(0.3·ATR, price·0.2%)`, capped to the **top-5 nearest** within the ATR window, `strength = touch_count + recency`; **analyst PTs are EXCLUDED** from the gated T2 (new `analyst_target` provenance label "Analyst-target-implied — not structural"; they stay in `analyst_target_levels` for display) — only `resistance` clears the desk R/R gate (`target_2_eligible_for_gate` unchanged); **ATR-normalized distances** `reference_target_1/target_2/stop_distance_atr` added to the evidence payload for calibration/debugging. Mirrored for **shorts** (support below entry, `entry−α·ATR`). Wired into both long+short paths in `swing_composite_evidence.py`; honest provenance labels updated (`atr_extension` reads "ATR projection — unanchored"); `sma200` now exposed top-level in `real_composite_engine.py` + `swing_composite_engine.py`; frontend mirror in `frontend/lib/target-provenance.ts`. Flag-gated **`STOCVEST_TARGET_GEOMETRY_V3_ENABLED`** (`config.py` default **ON** — B78 ships live; requires ATR, falls back to v2 byte-identical when ATR missing). Terraform: new `var.target_geometry_v3_enabled` (default true) wired to the signals Lambda (`infra/ecs_news_worker.tf` + `infra/lambda_6e.tf`), **set true in `terraform.tfvars`** (`terraform fmt` clean; `validate` needs AWS region env — environmental). Backend **2616**/4 (+17: `tests/api/services/test_target_geometry.py` (13) — day/swing params, adaptive ε, distance-in-ATR, T1 in-band / too-close / too-far / SMA-enriched, T2 structural-preferred / 2R-fallback / ATR-extension, short mirror, day-band-tighter-than-swing; + 4 v3-wiring in `tests/api/test_swing_composite_evidence.py` — breakout T1 not glued + distance fields, analyst-PT excluded from gated T2, distance-None-without-ATR); frontend `target-provenance.test.ts` (+1: `atr_extension`/`analyst_target` parse/label/never-gate). **Behavior change (intended):** an analyst-PT-only T2 at new highs no longer clears R/R, so CYTK-type setups drop Actionable→Monitor unless real structure/ATR/2R supports the geometry. **Deferred (per design review):** options/call-wall caps, volume-profile, confidence scaling, volume in strength scoring. **Activation:** `terraform apply` to set the env on the signals Lambda (code ships via CI `deploy-lambda` on merge to main). Prior — **A7 — Assistant breadth-with-restraint (SHIPPED)** (web-search fallback (Perplexity Sonar, flag-gated `STOCVEST_ASSISTANT_WEB_SEARCH_ENABLED`, env var applied to the signals Lambda 2026-06-28; citation-backed, facts-no-verdicts) + multi-symbol comparison ("compare NVDA vs AMD" → each ticker's price + STOCVEST cached read + analyst consensus fetched in parallel, reports the contrast but **never picks a winner**) + STOCVEST-read `not_yet_confirmed` uncertainty caveats (missing/split layers, neutral verdict, staleness — voiced in plain English by the locked prompt) + plan-aware watchlist-add cap (100 for swing_day_pro/beta, was a hardcoded 50). New services `assistant_web_context.py` / `assistant_multi_symbol_context.py`; new detectors `detect_symbols` + `is_comparison_query` (joining `is_web_search_query`); new locked-prompt sections (WEB CONTEXT RULES, MULTI-SYMBOL COMPARISON RULES, STOCVEST-READ uncertainty). All additive; web search ships dark (env var ON now, code goes live on merge → CI `deploy-lambda`). Backend **2599**/4 (+38). Prior — **B77 — Day-desk midday session-phase quality gate (SHIPPED; ships dark, flag default OFF)** (Trigger: user asked "~40% of outcomes would have made profit — right? how do we improve?" → approved a per-layer attribution study + an R:R-surfacing-gate prototype on the 60-day as-traded replay cohort (`scripts/analyze_signal_attribution.py`, read-only). Findings: **(1)** the R:R gate is **blocked on geometry v2** — only 4% of stored signals had planned reward > risk (the pre-B76 session-high-hugging-entry geometry), so gating on `reference_structure_level` discards 96% for no gain; re-test on B76 v2 targets later. **(2)** No single layer separates winners from losers (all |corr|<0.08; composite is saturated — every qualified signal scores ~max). **(3)** The clean lever is **time-of-day**: day-desk expectancy is opening 9:30–10:00 **+0.27%**, morning 10–12 −0.54%, **midday 12–14 −1.03%** (the 12:00 hour alone −1.19%/74 trades), close 14–16 −0.21%. Also reconciled two earlier misreads: `setup_type=unavailable` (85%) is **not a bug** — ORB is only computed for the static `STOCVEST_SCANNER_SYMBOLS` list while signals fire across a dynamic universe, so `setup_type` is just a time-of-day proxy (do **not** expand ORB coverage — wrong fix); and the `post_market` vwap label is the **swing desk** (EOD entries, market closed), not after-hours day trades (do **not** suppress it — would break swing). **Fix (per user design review — soft penalty, not a hard block):** day signals firing in the midday dead-zone (default 12:00–14:00 ET, env-configurable) get a **decision-score penalty** (default −12 on the 0–100 score, vs MIN_ACTIONABLE 72) so only low-conviction setups fall below threshold; an **RVOL override** (volume vs ADV ≥ 2.0, or sector-calibrated `volume_surge`) bypasses the penalty so news/momentum surges still qualify; suppressed signals are still recorded as **shadow** (no alert). Validated on the cohort: midday [72,84) band = −1.5%/trade; penalty −12 drops those 50 worst signals → day expectancy **−0.53% → −0.38%** while keeping ~2× more optionality than a hard block (which only reaches −0.32%). Applied in `evaluate_day_ledger_entry` (new `session_phase_penalty_0_100` + `session_phase_context` args; byte-identical when 0/None) wired from `real_composite_engine` (firing-time + RVOL computed before the gate); helpers `is_day_ledger_dead_zone_et`/`parse_et_hhmm` + `DAY_DEADZONE_START/END_ET` in `validation_timing.py`. Flags (all `config.py`, default dark): **`STOCVEST_DAY_SESSION_PHASE_GATE_ENABLED`** (OFF), `STOCVEST_DAY_DEADZONE_START_ET`/`_END_ET` (12:00/14:00), `STOCVEST_DAY_DEADZONE_SCORE_PENALTY` (12), `STOCVEST_DAY_DEADZONE_RVOL_OVERRIDE` (2.0); Terraform vars wired to the `signals` Lambda (`infra/ecs_news_worker.tf` + `infra/lambda_6e.tf`, all default OFF/identical). Backend **2545**/4 (+20 `tests/api/services/test_day_session_phase_gate.py`: dead-zone window/boundary/weekend/override/parse + penalty drops-marginal / keeps-strong / rvol-override-no-penalty / no-penalty-byte-identical). **Phase 2/3 (future, not shipped):** RVOL-confirmation refinements; replace pure time-gate with a participation/regime gate (RVOL + index volume). **Activation:** set `day_session_phase_gate_enabled=true` in `terraform.tfvars` + `terraform apply` (code ships via CI `deploy-lambda` on merge). **B77 step-1 DONE 2026-06-28 (instrumentation for Phase 2/3):** investigation found Phase 2/3 were blocked — the only persisted volume feature, `tech.volume_vs_adv`, is last-bar/ADV (structurally <1; **all 343** cohort day signals <1.0), so the Phase-1 RVOL override (≥2.0) **never fired** and there was nothing to validate a participation gate on. Built `stocvest/signals/intraday_rvol.py` — a **time-of-day-normalized** RVOL (`day_volume / (ADV × typical-cumulative-fraction)`, ≈1.0 = average pace) from a static US-equity intraday volume curve + the already-fetched `sym_snap`/`spy_snap` (no new Polygon calls). Now persisted on every day `SignalRecord` as `intraday_rvol` (symbol) + `market_rvol` (SPY broad-market participation) — additive optional fields, no contract/Terraform change — and the Phase-1 dead-zone override now uses the **real** `intraday_rvol`. This is the instrumentation prerequisite: once a few weeks of `intraday_rvol`/`market_rvol` accrue, validate whether midday losses are participation-driven, then build Phase 3 (participation/regime gate, possibly generalizing the time gate) on measured ground. Backend **2561**/4 (+16 `tests/signals/test_intraday_rvol.py`). **Index participation curve is static/approximate** (not per-symbol) — acceptable for a relative measure; a learned per-symbol profile is a later refinement if validation needs it. Prior — **B76-followup — short-side T2 provenance label flips to "Support-anchored" (SHIPPED)** (Trigger: user reviewing an INTC **short** in the deep-dive Scenario Geometry block — the B76 honesty layer was working correctly (T1 flagged too-close, T2 shown as honestly below the 2.0:1 gate, not silently promoted), but the T2 provenance line read **"T2: Resistance-anchored"** for a short, where the downside target is anchored to **support**. Root cause: `frontend/lib/target-provenance.ts::target2ProvenanceLabel` mapped the internal `target_2_provenance === "resistance"` token to the literal "Resistance-anchored" with **no direction awareness**. The token is load-bearing — it's the T2 *gate-eligibility* key checked in `target2EligibleForGate` / `structureRiskReward*` / the chosen-target promotion — and the **backend was already correct** (short geometry scans `scan_nearest_support_below` and labels `reference_target_provenance` "structural support (T2)"); only the frontend enum→label display never flipped. **Fix (display-only, gate token untouched):** `target2ProvenanceLabel(provenance, direction)` now returns **"Support-anchored"** when `direction === "bearish"` (keeps "Resistance-anchored" for longs; unanchored labels stay direction-agnostic); threaded the setup direction through both deep-dive call sites (the `T2:` line + the gate-fail reason) and `evaluateScenarioDeskGate`. Backend parity helper `stocvest/api/services/target_provenance.py::target_2_provenance_label` kept in sync (no runtime callers; file is the maintained mirror). The `"resistance"` enum value is unchanged → all gate-eligibility logic byte-identical. Frontend-only, +2 tests (`frontend/tests/target-provenance.test.ts`: direction-aware label, short gate reason); targeted suites green (target-provenance / risk-reward-structure / scenario-verdict 13/13). No Lambda redeploy needed (ships via Vercel on merge). Prior — **B76 — Swing/day target geometry v2: analyst-PT T2 cap + degenerate session-high T1 rebuild (SHIPPED; flag enabled 2026-06-26)** (Trigger: user asked why ATAI swing showed a target of **$14** for a **$5.31** entry. Live probe of the deployed signals Lambda confirmed the served geometry: entry $5.31 **at the high of day**, stop $3.75, **T1 = $5.31** (literally the session high = entry → zero reward), **T2 = $14.0** with `reference_target_2_provenance="resistance"` but `analyst_target_levels=[14.0]`, `analyst_target_source="perplexity"` → R/R **5.6**. So $14 is **not** a chart level — it's a Perplexity ~12-month analyst PT (+163%) that reached T2 because `scan_nearest_resistance_above` deliberately lets `extra_levels` (analyst targets) **bypass** its 25% structural proximity band; combined with a session-high T1 collapsed onto entry, the planner promotes the distant T2 and shows a fantasy 5.6:1. **Fix (`swing_composite_evidence._long_side_geometry`, shared by swing + day via `build_swing_composite_evidence_fields`; both desks affected):** (A) analyst `extra_levels` are now bounded by a new `extra_proximity_pct` arg on `scan_nearest_resistance_above` (`structure_resistance_scanner.py`) — beyond `analyst_max_pct` above entry they're dropped as a swing T2 (ATAI: $14 > $5.31×1.40 → T2 falls to the 2R extension ≈ $8.43, R/R ≈ 2.0; the PT stays visible in `analyst_target_levels`); (B) a session-high T1 that doesn't clear entry by `_MIN_T1_REWARD_PCT` (0.3%) is rebuilt from nearest in-band structural resistance, then a 1R projection, then the % fallback — so T1 never equals entry; (C) `_long_target_provenance_label` now words T1 honestly ("Structural level (T1)" when it's no longer the raw session high). Flag-gated **`STOCVEST_SWING_TARGET_GEOMETRY_V2_ENABLED`** (`config.py`, default **OFF** → served geometry byte-identical to legacy) + tunable `STOCVEST_ANALYST_TARGET_MAX_PCT_FROM_ENTRY` (default **40.0**). Terraform: new `var.swing_target_geometry_v2_enabled` wired to the `signals` Lambda env (`infra/ecs_news_worker.tf` + `infra/lambda_6e.tf`); **enabled in `terraform.tfvars` 2026-06-26**. Backend **2525**/4 (+5 `tests/api/services/test_analyst_target_levels.py`: v2-off-is-legacy-ATAI, analyst-T2 cap, degenerate-T1 rebuild, keeps-session-high-when-rewarding, scanner extra-proximity cap; relevant composite/geometry/risk suites green 152/152). **Activation:** `terraform apply` to set the env on the signals Lambda (code ships via CI `deploy-lambda` on merge to main). Prior — **Trading-room company-name resolution: degraded-on-empty BFF fix (SHIPPED)** (Trigger: user reported company names missing on feed cards + the center deep-dive; screenshot also caught the center pane in a transient "signal service temporarily unavailable" state. Not a data bug — Polygon `/v3/reference/tickers` and the deployed `market_data` `GET /v1/market/symbol-names` both return the names when invoked directly. Root cause: names are backfilled by a separate async lookup (`useSymbolNames` → BFF `/api/stocvest/market/symbol-names`) because the desk-leader payload carries no company field; the BFF only flagged `degraded` on `!upstreamOk`, but the backend handler returns **HTTP 200 with `{names:{}}`** on its own best-effort reference miss, so a 200-with-empty slipped through and the client hook cached those symbols as "resolved, no name" **permanently** until a full reload. Fix in `frontend/app/api/stocvest/market/symbol-names/route.ts`: return `degraded:true` whenever **zero** names resolved (incl. the 200-with-empty case) so the client retries instead of caching empties → names self-heal after any backend blip. Frontend-only, +1 regression test (`tests/symbol-names-bff.test.ts`); relevant suites green (symbol-names-bff / feed-model / symbol-name 15/15; full Vitest run hung locally — ran targeted). No backend / Terraform change. **Bulletproof follow-up queued as B75** (carry `company_name` on `DeskDiscoveryLeader` from `discovery_row.py` so names ship inside the desk payload and survive a full backend outage). Prior — **Alert-email delivery hardening + tradeable-only prefs (SHIPPED; alarm + pref-migration live, code pending deploy)** (Trigger: user expected an email whenever a signal becomes tradeable but had received none in ~10 days. Root cause: the Postmark "Stocvest Production" server **froze at 110 messages on 2026-06-15** — the account hit its monthly send limit, so the API kept returning HTTP 200/OK while the app recorded alerts as "sent" → a silent outage. User upgraded the Postmark plan (verified: a test send moved the count 110→111, Status=Sent). Hardening: (1) execution-actionable emails now render **Stop loss / Take profit / Entry zone** (`email_service`); (2) `postmark_client.send_postmark_html_email` counts success **only** on HTTP 200 *and* `ErrorCode 0`, so soft failures (over-limit / inactive recipient) persist **`status=failed`** instead of a misleading "sent"; (3) every send emits **`Stocvest/Alerts EmailSendOutcome`** (Result=sent|failed; `stocvest/services/alert_metrics.py`, gated to Lambda runtime) with a CloudWatch alarm on Result=failed + SNS topic + optional email subscription (`infra/alert_email_health.tf`, `var.alert_email_failure_alarm_email`; **terraform apply 2026-06-25 → 3 added**); (4) all **13** users migrated to **execution-actionable-only** prefs (signal_fired/confluence/maturation/gap/tracked-plan OFF, PDT safety kept ON, watchlist_only OFF) via `scripts/set_all_users_tradeable_alerts.py`. Backend **2520**/4 (+2 `tests/services/test_email_service.py`, `tests/services/test_postmark_client.py`). **Alarm/SNS + the user-pref migration are already live; the code (email content / strict-fail / metric emission) deploys via CI `deploy-lambda` on merge to main.** Prior — **Day ledger profit-target exit + as-traded replay tooling (ships dark)** (Trigger: after the resolver fix, the user asked whether the surfaced signals would have been profitable. Investigation showed the publicly-surfaced **qualified** rows are horizon-resolved PUBLIC mirrors (`price_1d_after`, no risk management) while the user-scoped twins already carry rule-based exits — and the live **day** monitor has VWAP-violation + time-flatten + session-close but **no profit target**, so winners only exit on a VWAP break or the bell. Built a read-only **as-traded replay** (`scripts/replay_qualified_exits.py`): de-dupes twins, re-derives each exit from Polygon minute/daily bars under the full bracket (stop / target / VWAP / flatten / close for day; structure-stop / target / max-hold for swing), and reports realized expectancy, R:R, R-multiple, and MAE/MFE. 60-day read: qualified **day** cohort ~39% hit / R:R ~0.83 / **−0.42%/trade** (~−0.02R, near scratch in R terms) with **avg MFE +1.60% > avg MAE −1.33%** — favorable excursion the current exits surrender; the replay's profit-target exits explain the jump from −0.79% (live, no target) to ~−0.40%/trade. So binding constraint is **directional accuracy**, not exit plumbing — but the target is a clear, evidence-backed win. Implemented `day_profit_target` in `ledger_position_monitor.run_ledger_position_monitor` (close at snapshot last price when it reaches `reference_structure_level`, checked **before** the VWAP rule), flag-gated **`STOCVEST_DAY_PROFIT_TARGET_EXIT_ENABLED`** (`config.py`, default **OFF** → legacy exits byte-identical). Backend **2518**/4 (+2 `tests/api/services/test_ledger_position_monitor.py`: target fires when enabled / inert when disabled). **Code-only — no Terraform; flag default OFF, enable via lambda env when ready (then needs Lambda deploy / CI `deploy-lambda` on merge).** Prior — **Signal outcome resolver throughput fix** (Trigger: while reviewing the last 3 days of surfaced signals, the user noted day-desk 1d outcomes weren't resolving even though day signals should resolve within a day. Probe confirmed: Jun 22 qualified day rows were only **8/29** 1d-resolved (due by Jun 23). Root cause: the `signal_resolution` Lambda (EventBridge `rate(30 minutes)`, **120s** timeout, `infra/eventbridge.tf` + `infra/lambda_6e.tf`) resolved outcomes with **sequential** Polygon REST lookups over a **full-table scan** — 9,656 rows with a backlog of **~1,215 unresolved 1h + ~1,359 unresolved 1d**. The 1h pass (~1,215 × ~0.2s ≈ 4 min) alone exceeds the 120s timeout, so the 1d pass — which runs *after* 1h in the same invocation — never gets time → 1d starves while 1h looks current. **Not a price bug** (verified `get_evaluated_price_after_signal` returns valid prices). Fix in `DynamoDBSignalRecorder.resolve_signals` (`stocvest/api/services/signal_recorder.py`): (1) build the unresolved set network-free, (2) sort **newest-first** so recent rows (what accuracy/reporting reads) resolve promptly while older backlog drains across runs, (3) cap **2500/pass** (`_RESOLVE_MAX_PER_PASS`), (4) fetch with **bounded concurrency 8** (`_RESOLVE_CONCURRENCY`) in chunks whose writes persist incrementally (durable if the Lambda still times out). Outcome math, `_item_ledger_validation_still_open` skip, and PUBLIC-mirror writes unchanged; InMemory store mirrors the cap/order for signature parity. Backend **2516**/4 (+2 `tests/api/test_signal_recorder.py`: concurrent-resolves-all asserts real fan-out via max-inflight, cap-newest-first). **Code-only — no Terraform change; needs Lambda deploy (CI `deploy-lambda` on merge to main). The ~2.5k backlog clears on the first run after deploy; recent rows then resolve every 30 min.** Prior — **Swing technical display fix — lagging golden cross** (Trigger: user asked whether **SPCH** showing Technicals **0/100** was right. Live probe (`scripts/probe_spch_technical.py`) confirmed **0 is correct** — price below SMA20/50/200, **−55%** from its 60-day high, MACD below signal, RSI ~34 = a genuine breakdown — but the brief/chips were **misleading**: because SMA50 was still above SMA200 the card showed a bullish **"Golden Cross"** chip and the brief read **"uptrend structure"**, contradicting the bearish verdict. The cross is a **lagging artifact** (the averages haven't caught the crash yet). **Display-only fix** in `swing_technical_analyzer.py`: a new `below_key_mas` flag (price below both SMA50 & SMA200) makes the chip read **"Golden Cross (lagging)"** and the brief say **"broken structure (50-day still above 200-day, but that golden cross is lagging — price has broken below both)"**; durable real uptrends (price above both MAs) keep the plain "Golden Cross"/"uptrend" wording. **Score and verdict unchanged** (SPCH stays 0/bearish). **Score-path audited** (reviewer flagged a lagging cross could be feeding the score, not just the label): the golden-cross structural credit is gated behind `durable_uptrend` (price *above* both MAs), so it never fires for a broken-down name — SPCH raw composite **−42 → clamped to 0**, invariant to `golden_cross_score` ∈ {0,8,200}; `confluence_pattern="golden_cross"` is inert in `ConfluenceDetector` (needs an "orb" pattern). 0 is the calibrated magnitude (the B73 graded floor is gated on RSI<30; SPCH RSI ~34 = still-falling knife, not a stabilized oversold bounce → stays at clamp). Backend **2514**/4 (+3 `tests/signals/test_swing_technical_analyzer.py`: lagging-cross-on-breakdown reads broken, durable-uptrend-not-regressed, **credit-invariance gate**); no Terraform / API / gate change. Prior — **B74 News relevance × impact × age weighting (ships dark / flag-gated OFF)** (Trigger: user asked whether a single 4-day-old opinion article should give NextEra a **100/100** News score. Root cause: the News layer averaged per-article polarity weighted only by recency × mention × source, so one extreme article = extreme score regardless of relevance/coverage/age. **Validation-first** — `scripts/validate_news_relevance_impact.py` (no-lookahead, 72 names / 1,158 news-day cases): weighting each headline by **relevance × impact** and shrinking thin evidence toward 50 raised forward-10d edge **+2.12% → +2.97%**, the calls it neutralizes were ~coin-flip noise (**+0.57% / 51%**), and it tames **79%** of extreme 100/0 prints — so unlike a flat coverage-count haircut (rejected earlier), this earns its place. Implemented as pure `stocvest/signals/news_impact.py` (heuristic relevance = publisher credibility, impact = catalyst-keyword magnitude; Claude per-article `relevance`/`impact` preferred when present via read-through cache) + a **confidence shrink** in `news_analyzer.py` applied only in the pure-headline regime (analyst-active reads never neutralized). The Claude scorer (`news_sentiment.py`) now emits `relevance`/`impact` (back-compat 1.0 defaults), the sentiment cache stores + reads them (`enrich_rows_with_cached_impact`, wired in both engines), and `news_consumer_lambda` persists them. New flag **`STOCVEST_NEWS_IMPACT_WEIGHTING_ENABLED`** (default OFF → News score byte-identical to legacy). Backend **2511**/4 (+20 across `test_news_impact.py`, `test_news_analyzer.py`, `test_news_sentiment.py`, `test_news_sentiment_cache.py`); API_CONTRACTS §News layer documented; **no Terraform change**. **Remaining (activation):** Terraform `var.news_impact_weighting_enabled` is now wired to the `signals` Lambda (`infra/ecs_news_worker.tf` + `infra/lambda_6e.tf`, default OFF). Three independent tiers: **(1)** set the var → heuristic relevance×impact + shrink live (captures the validated +2.97% edge, ~$0, no worker); **(2)** also set `news_sentiment_cache_enabled` + `news_sentiment_prime_enabled` → the existing `news_consumer` Lambda self-prime path scores articles with Claude (now incl. relevance/impact) into the cache (bounded Claude cost, Redis already on, **no ECS worker needed** — `ANTHROPIC_API_KEY` already on the lambda-runtime secret); **(3)** deploy the ECS news worker for real-time Benzinga-WS + EDGAR coverage (Fargate cost): a managed **`aws_ecr_repository.news_worker`** now exists (`infra/ecs_news_worker.tf`, output `news_worker_ecr_repository_url`) → `terraform apply` to create the repo, build/push with **`scripts/build_push_news_worker.ps1 -AccountId <id> -Region <region>`** (uses existing `docker/news_worker/Dockerfile`), then set `news_worker_container_image = <repo_url>:<tag>` + `news_worker_desired_count = 1` and apply again. Worker only ingests→SQS; **Claude scoring stays in the `news_consumer` Lambda** (which already has `ANTHROPIC_API_KEY`), so no Anthropic key is needed on the worker task. Tiers 1/2 are pure `terraform.tfvars` flips + `terraform apply`. Prior — **Layer verdict band on score bars** (additive contract + display-only. Trigger: user asked why APGE shows Sector **62/100** and Geopolitical **58/100** yet both read "Neutral". Root cause is *not* a bug: the 0–100 layer score is a **magnitude**, while the Bullish/Bearish/Neutral read only leaves neutral once the score clears a calibrated cutoff that sits **well above the 50 midpoint** — sector bullish needs ≥65 (or ≥58 *only if* strengthening), geo bullish needs ≥60 — so 62/58 are correctly "positive but neutral". The progress bar filled proportionally (>50%) made it *look* like a contradiction. Fix: both composite engines now emit per-layer **`bullish_threshold`/`bearish_threshold`** on each `layers[]` row via a shared `stocvest/api/services/layer_verdict_bands.py::layer_verdict_band` helper (Technical resolves swing vs day params by request mode; news/macro/sector use their `*Parameters`; geo/internals reference new module constants `GEO_BULLISH/BEARISH_THRESHOLD` + `INTERNALS_BULLISH/BEARISH_THRESHOLD` extracted from the analyzers). The deep-dive score bar (`signals-layer-breakdown.tsx`) now shades the neutral dead-band, marks the two cutoffs, and shows a `Neutral band ≤N – ≥M · score in between has no directional edge` caption (both the compact row bar and the modal bar). Threaded through `composite-layer-rows.ts` → `SignalsLayerRowInput.bullishThreshold/bearishThreshold`. Display-only, no gate/threshold change (thresholds themselves are the immutable Signal-Math contract — left untouched). Backend +5 unit (`tests/api/services/test_layer_verdict_bands.py`), frontend **2058**/329 (+3), API_CONTRACTS §`layers[]` verdict band documented. Prior — **Trading-room feed: market-closed Day-lane handling** (frontend-only, display-only. Trigger: user noted day-trading cards showing while the market is closed. The Day lane was gated only by subscription (`subscriptionAllowsDayTradingSurfaces`), never by market hours. Now when `marketOpen === false` the feed **defaults to the Swing lane** (one-shot ref-guarded effect, only flips when lane is still the "all" default) and the Day lane shows a `Market closed · resumes next open` pill on its `FeedLaneSection` header — Day stays reachable, just clearly marked not-tradable-now. `dashboard-trading-room.tsx` only; build clean, dashboard/trading-room suite green, §13 stays **2055**/329. Prior — **Deep-dive brief UX fixes: plain-English summary + bias reconcile** (frontend-only, display-only, no gate/contract change. Trigger: user feedback on the WYFI deep-dive — "3.9× ATR shown twice (caps + lowercase)", "the brief looks too technical", "left card shows BULLISH but the brief says short". (1) `dedupeWatchForAgainstBlocker()` stops the Plain-English brief repeating the same blocker twice — the session-move/ATR fact was emitted once as `Primary check:` and again, lower-cased, inside the backend `watch_for` tail (`deep-dive-present.ts`); (2) new jargon-free **`buildPlainSummary()`** is now the default brief — "what it is / how much agrees / the one thing to do", deliberately no EMA/VWAP/ATR/R-R/"layers"/"gates" — with the prior technical brief moved behind a **"Show the detailed read"** toggle in `deep-dive.tsx`; (3) `feed-model.ts` **`leaderBias`** makes the left-rail leader card pill follow the composite **signal** (`verdict`/`signal_summary`) instead of the session/gap **move** direction, and stay **neutral** when no composite read is cached — so a +17.9% green day the engine reads SHORT no longer shows a contradictory BULLISH pill. Frontend **2055**/329, +5 tests (4 `deep-dive-present.test.ts`, 1 `feed-model.test.ts`). **Remaining (optional backend follow-up):** leader cards show *neutral* (not the actual SHORT/LONG) when the desk batch didn't cache a composite for that leader — to show the real signal direction up front the desk pipeline (`discovery_row.py`) would need to carry the composite direction for every leader. Prior — **B72-UI / B73-UI: surface new technical signals in the deep-dive** (both composite engines emit a new additive top-level **`sector_technical_calibration`** object — `{sic_bucket, regime, rvol_threshold_multiplier, overbought_penalty_multiplier}` — mirroring B71's `news_geo_sensitivity`; the Signals deep-dive threads it onto the Technical row and shows a `…-CAL` regime chip + InfoTip + "Sector calibration" fact-line for non-normal regimes only. B73's "Oversold bounce watch" chip already auto-surfaces via the existing layer-chip drawer path (verified). Display-only, no gate change. Frontend **2046**/329, `npm run build` clean, backend **2491**/4 unchanged). Prior — **P66-followup: sector benchmark-ETF cache coverage** (warm all `DEFAULT_SECTOR_TO_ETF` benchmarks — biotech/pharma/medical-devices/retail/airlines/transport/defense/mining + SOXX — so those sectors get multi-day persistence momentum instead of the degraded single-day fallback; drift-guard test added; SOXX/SMH confirmed not-a-bug, two separate subsystems. Backend **2491**/4, no Terraform/API change). Prior — **B73 Mean-reversion bounce floor (swing)** (answers "should a stock below SMA20/50/200 score ~0? what about the pull-back chance?" — the swing scorer floored *every* deep downtrend at 0 with no resolution between an oversold-at-support bounce candidate and a free-falling knife. **Validation-first** no-lookahead sweep (`scripts/validate_mean_reversion_credit.py`, 64 names / ~2.4y / 4,340 cases) showed RSI depth monotonically tracks forward 10-day bounce — RSI<20 → +4.8%/84% positive, 25–30 → +1.6%/63% — and fresh-low "knives" underperform stabilizing names. Implemented as a **non-additive floor** in `swing_technical_analyzer.py`: when price < SMA50 & SMA200, MACD < signal, RSI < 30, and today is **not a fresh 10-day low**, `score = max(trend, floor)` where floor is oversold-led (0.8) + extension (0.2), **hard-capped at 38 < bearish_threshold 40** so it adds resolution but never flips the bearish verdict. Swing-only; non-oversold downtrends (MSFT/TSLA) and uptrends untouched. "Oversold bounce watch" chip + `mean_reversion_floor_applied` flag (UI not yet wired). Backend **2488**/4 (+5 swing tests), no Terraform/API change. Prior — **B72 Technical layer accuracy** (swing recalibration fixes NVDA Technicals=5/100 → ~41 neutral: magnitude-scaled below-SMA20, durable-uptrend golden-cross credit, pullback-vs-breakdown softening, MACD de-overlap, lighter distribution; + new sector-relative technical calibration `HIGH_BETA/NORMAL/DEFENSIVE` scaling RVOL surge threshold + overbought persistence, plumbed through both analyzers + composite engines, neutral for unknown sectors). Backend **2483**/4, no Terraform/API change. Prior — **B71 News/Geo sensitivity UI surfacing** (composite responses emit additive top-level `news_geo_sensitivity` via `layer_sensitivity_payload`; deep-dive Layers tab shows a per-stock sensitivity chip + drawer fact-line on the News & Geopolitical rows; display-only, no gate/contract break). Backend **2463**, frontend +2. Prior — **B71 Phase C scheduled report** (weekly `news_event_study_report` api-Lambda module → reports S3 bucket via EventBridge Scheduler, Sun 18:00 ET; new `infra/s3_reports.tf` + `infra/eventbridge_scheduler_news_event_study.tf`; gated `var.news_event_study_report_enabled`, read-only / not wired into scoring). Backend **2462**. Prior — **B71 Phase C scaffold + cache EMF metrics** (read-only offline **news event-study** `stocvest/signals/news_event_study.py` + `scripts/news_event_study_report.py` reporting per-symbol sensitivity ratio + sentiment predictiveness from stored snapshots/returns — **not wired into live scoring**; EMF CloudWatch metrics for the sentiment cache). Prior — **B71 Phase D activation + self-prime** (flags wired in Terraform via `var.news_sentiment_cache_enabled` / `var.news_sentiment_prime_enabled`; cache flag on `news_consumer` + `signals` + ECS worker, prime flag + triage queue URL on `signals`; TTL raised to 6 days to cover the 120h swing lookback; composite **self-primes** the cache by enqueuing abstaining misses for async scoring; hit-rate logged `news_sentiment_cache enrich candidates=N hits=M`, enqueues logged `prime enqueued=N`). Activation still gated on the **ECS news worker running** to populate/drain the queue — see B71 row. Prior — **B71 Phase D SHIPPED (dark / flag-gated OFF)** (read-through **Claude sentiment cache**: the async news consumer now also writes a content-keyed (URL→sha1) sentiment entry, and the day+swing composite engines fill otherwise-abstaining (`insights == []`, e.g. Benzinga-only) headlines from it via one batched `MGET` before scoring — so the already-running Claude scorer reaches the composite **without** a synchronous Claude call on the hot path. New `stocvest/signals/news_sentiment_cache.py`; gated by `STOCVEST_NEWS_SENTIMENT_CACHE_ENABLED` (default OFF), fully fail-open, labels normalized bullish/bearish→positive/negative. Ships dark pending hit-rate validation. Backend **2437** (+11 `tests/signals/test_news_sentiment_cache.py`); **no Terraform**. Prior — **B71 Phase B SHIPPED** (article-level **news-event capture**: the composite engines now persist their top `catalyst_headlines` — `published_at` + per-ticker `sentiment_score` + source + catalyst type — into a new `NewsSnapshot.top_events` (`NewsEventCapture`) blob on `SignalHistory`, so a later event study can join each headline to the symbol's subsequent price move. Additive JSON only — **no Terraform**, no scoring change, no API contract change; backend **2426** (+4 `tests/api/services/test_signal_snapshot_news_events.py`)). Prior — **B71 Phase A SHIPPED** (per-symbol **News/Geo sensitivity weighting**, static sector priors: a new `stocvest/signals/news_sensitivity.py` resolves coarse `HIGH/MEDIUM/LOW` bands per layer from the symbol's SIC/sector bucket (+ ADR geo override) into a **down-only** influence multiplier (`HIGH=1.0`, `MEDIUM=0.85`, `LOW=0.6`); `CompositeScoreEngine.compute` gains an optional `sensitivity_multipliers` kwarg that scales **effective layer weight** (renormalizes, never a gate, never flips a layer's sign), wired into both the day (`real_composite_engine`) and swing (`swing_composite_engine`) paths. Unknown/`default` sectors stay neutral `1.0` → identical to pre-B71. Up-weighting + the data-driven event study are **Phases B/C** below. Backend **2422** (+10 `tests/signals/test_news_sensitivity.py`); **no Terraform**, no API contract change. Prior — **B70-fix SHIPPED** (`AiSetupRead` resets to idle on symbol/direction/desk change — switching tickers no longer shows the prior ticker's AI read). Prior — **B70 AI setup read + richer deep-dive brief SHIPPED** (the Plain-English brief is no longer a fixed template: `buildRichBrief` now threads real specifics — market regime, named confirming/conflicting signals, the freshest catalyst — and picks a deterministic per-symbol phrasing variant so different tickers read differently; plus a new optional, paid-gated **AI read** (`POST /v1/signals/ai/explanations` `type=setup_read` → `AIExplanationService.explain_setup_read`, Claude `AI_MODEL_FAST` @ temp 0.7, Redis-cached, deterministic `fallback_text` for free users / outage) surfaced via the shared `AiSetupRead` expander on **both** the Trading Room Deep Dive and the Signals evidence card — reuses the existing route/BFF/gating, **no Terraform**). Prior — **B58-fix causal-narrative directional-mixed copy SHIPPED** (a directional layer in a neutral setup now describes its own read — e.g. "Sector reads bullish, but the overall setup is neutral …" — instead of the misleading "no clear leadership versus SPY"; backend `causal_narrative.py` + frontend `causal-narrative.ts`; backend 2406 / frontend 2029; the X/6 directional-alignment count is unchanged and correct). Prior — **P68 Signal Math Contract + financial-math accuracy SHIPPED** (new internal scoring-math contract mirrored backend↔frontend; ORB strength now ATR-normalized + volume-gated, `gap_pct` wired from prev close, true weekly RSI, confluence tier floors recalibrated, neutral-at-50 fixes; see [`IMPLEMENTED.md`](./IMPLEMENTED.md) P68 + [`SIGNAL_ENGINE.md`](./SIGNAL_ENGINE.md) § Signal Math Contract) + **B69 tracked trade plans + thesis alerts** code complete + tested + **Terraform applied 2026-06-20** (`terraform apply` → 11 added, 21 changed, 0 destroyed: `TradePlans` + `SystemSignalTransition` tables, `trade_plans` Lambda, `/v1/trade-plans/*` routes; `trade_plans` Lambda code ships via CI `deploy-lambda` on merge to `main`; see [`IMPLEMENTED.md`](./IMPLEMENTED.md) B69). Prior **2026-06-10** — **B68 Setup Evolution score analytics tab SHIPPED** + **B67 Trading Room Market Brief reset SHIPPED** (PR #153) + **B66 layer drawer rich evidence SHIPPED** (PR #152). Prior — **P66** signal-engine fixes (PR #121–#122) + **P67** Next.js dev cache (PR #123) + **OPS-LEDGER** daily verification runbook. Prior **2026-06-08** — **P65 pre-beta signal engine P0 SHIPPED** (six-layer trust, split ledger capture, Secrets v1.1.0, dedupe + proposer filters) + **B65 dashboard BFF/CORS reliability SHIPPED** (see [`IMPLEMENTED.md`](./IMPLEMENTED.md) P65, B65). Prior **2026-06-05** — **B63 Trading Room dashboard redesign SHIPPED** (live `/dashboard` now renders `DashboardTradingRoom`: session header + signal feed + Market Brief/Deep Dive + watchlist rail; `first_name` greeting + AI `GET /v1/market/brief`; see [`IMPLEMENTED.md`](./IMPLEMENTED.md) B63 and [`DASHBOARD_TERMINAL_UX_PLAN.md`](./DASHBOARD_TERMINAL_UX_PLAN.md) §9). Prior **2026-06-02** — **A2/A3/A4 + A1-tests SHIPPED** (watchlist actions, discovery queries, deep-link routing, A1 test coverage). Prior — **A1 STOCVEST Assistant capability upgrade SHIPPED** (live symbol context, image attachment, voice input, UI redesign — see `CONTEXT.md` §1 A1 row + §2 A1 notes). Prior **2026-05-22** — **B62 validation ledger capture + execution quality SHIPPED** (see [`IMPLEMENTED.md`](./IMPLEMENTED.md) B62). Prior **2026-05-19** — **B49 Scanner quiet-desk IA SHIPPED**. Prior **2026-05-17** — **B46 SHIPPED** (setup analytics — evolution + outcomes; `terraform apply` **2 added, 1 changed**; see [`IMPLEMENTED.md`](./IMPLEMENTED.md) B46). **B46-Phase6** remains here (SignalHistory retirement). Prior **FUND-C**. Prior **FUND-B + FUND-D**. Prior **FUND-A** same day. — **B38 SHIPPED — Gap Intelligence scanner batch + DynamoDB read-through cache + EventBridge warmer + debounced ScenarioBuilderDisabled metric** (`POST /v1/signals/gap-intel/batch`, `GapIntelCache`, `rate(2)` tick, `useScannerGapIntelBatch` on scanner). See `IMPLEMENTED.md` B38. **B37 SHIPPED — Admin Users per-user activity-errors** (`GET /v1/admin/users/{user_id}/activity-errors`, UI **Show error logs**, **Last activity** copy; run **`terraform apply`** for the new API Gateway route) + dashboard **`stocvestAuthedFetch`** import fix. See `CONTEXT.md` row 23 + `IMPLEMENTED.md` B37. **B36 SHIPPED — Evidence Card Signal price drift row** (scanner `last_price` vs snapshot `lastTradePrice`, signed Δ%, magnitude tiers, a11y). See `CONTEXT.md` row 22 + `IMPLEMENTED.md` B36. Full Vitest **1018/1018**, **91** files. **B35 SHIPPED earlier same day — Evidence Card layer-card refactor** (see `CONTEXT.md` row 21 + `IMPLEMENTED.md` B35). Earlier today — **BRK.B Evidence Card — second-pass UX polish (items 1/2/5) shipped.** Build Scenario button gated at R/R < 2.0 (in lock-step with `synthTradeDecision::rrFail`), "Why hold:" / "Why blocked:" copy across all 5 rationale variants rewritten to drop the legally-loaded "trade permission" language, and the conflicting chip rail now sorts by canonical priority (`vwap > volume > ema > internals > sector > regime > news > gap > orb`) with PRIMARY / SECONDARY / TERTIARY badges on the first 3. +73 new lock-in tests in three new files; existing tests 84/84 still green. **Items 3 / 4 / 6 parked here as `B35`** below — they need a coherent layer-card-render refactor, not bolt-on edits, so they get a dedicated next pass. See `CONTEXT.md` row 20. Same day previously — **Tier 1.A + Tier 1.B + Layer 4 (first slice) + Layer 4 (second slice) all shipped.** The second slice extends SWR coverage from `useSymbolSnapshot` (first slice) to four more imperative `useEffect → fetch` blocks: the signals page composite POST (`useSignalComposite`), the after-hours news fetch (`useSymbolNews`), the dashboard polling loop (`useDashboardPayload`), and the dashboard macro snapshot (`useMacroContext`). 23 new lock-in tests; impacted existing tests all green. The dashboard live-hint SSE stream now invalidates the SWR cache via `mutate(dashboardPayloadKey(mode))` instead of running its own polling closure; `/api/signals/live` itself stays as `EventSource` (push channel, not GET-and-cache). See [`CONTEXT.md`](./CONTEXT.md) row 15. Same day previously: Together they turn the worst-case `/dashboard` → `/dashboard/signals` navigation from a 16s freeze into a sub-second perceived load with live content streaming in. **Tier 1.A** killed the dashboard `<Link>` prefetch storm — every `<Link>` in the ribbon chips, day-desk top-signal rows, and desk scanner footers now carries `prefetch={false}` so Next.js 14.2 no longer fires 5–10 parallel SSR prefetches of `/dashboard/signals` and `/dashboard/scanner` on dashboard mount. **Tier 1.B** split `/dashboard/signals/page.tsx` into a fast outer shell (auth + URL parsing) + an async data island (`SignalsPageData`) wrapped in `<Suspense fallback={<SignalsPageShell />}>`, paired with `app/dashboard/signals/loading.tsx` so the inter-route transition shows the same skeleton instead of freezing on the previous page. `SignalsPageClient` itself is unchanged — same props, same chatbot context publishing, same Mode Separation perimeter. Per-section island split (e.g. earnings streaming independently of scanner) is documented as **Tier 1.B+** in `PERFORMANCE.md` §2 and stays parked until a real user reports the current "swap everything at once" feels janky. Lock-ins: `frontend/tests/dashboard-prefetch.test.tsx` (5 tests on the prefetch flag, mocks `next/link` to expose the prop as a `data-prefetch` attribute), `frontend/tests/signals-page-streaming.test.tsx` (12 tests on the shell DOM + the source-level invariant that the four heavy fetchers must live inside `SignalsPageData`, never in the outer page). Full architecture, sequencing, and 7 cross-cutting invariants are codified in [`docs/PERFORMANCE.md`](./PERFORMANCE.md) — that doc is the single source of truth for every future perf PR. **Same chat also ran `terraform apply` (35 added / 18 changed / 0 destroyed)** to deploy all 14 admin API Gateway routes + the D10 Phase 2b/4 weight proposer + rotation monitor Lambdas + the ParameterProposal DynamoDB table — full ops note in [`CONTEXT.md`](./CONTEXT.md) §3 row 10. The user-visible 404 on `/dashboard/admin/users` is fixed; admin error UI now surfaces real HTTP status with actionable hints (`AdminApiErrorCard`) so future deploy gaps are visible immediately. Previously shipped this session: **Admin Users page now lists all by default, paginated 25/page (Prev / Next via Cognito `PaginationToken`).** Backend `GET /v1/admin/users/search` now accepts empty `q` to return the full user pool (new `list_users_page` helper in `services/admin_user_directory.py` unifies "list" + "search" + pagination behind one Cognito `ListUsers` call); response carries `next_token` round-tripped from Cognito's opaque token. Frontend `searchUsers` client + `<AdminUsersPageClient />` page rewired around a token stack (push on Next, pop on Prev), with a new reusable `<AdminListPager />` component (`components/admin/admin-list-pager.tsx`) that handles 0-vs-1 indexed display, loading state, and Prev/Next disable rules. 19 new backend tests (`test_admin_user_directory.py`, `test_admin_users.py`) + 18 new frontend tests (`admin-list-pager.test.tsx`, `admin-users-page.test.tsx` — render contracts, pager keyboard / disable rules, token-stack Prev/Next, filter submit resets page index, Show-all clears filter). See **B34** below for the replication plan to Audit log + Weight proposals + Parameter history (the pager component is reusable; only the upstream services + page clients need work per surface). Previously shipped this session: **Gap Intelligence empty-state distinct copy** (the Scanner gap column no longer shares text with the Swing setups column — `buildGapIntelEmptyStateContext` + `surface: "gap"` discriminator in `lib/scanner-empty-state.ts`, regression-pinned with a side-by-side `gapText !== swingText` assertion). Earlier this session: **Scanner empty-state + terminology pass shipped** → the bland "No swing setups — regime and structure not aligned." / "No gap intelligence matches right now." lines on `/dashboard/scanner` are replaced with a rich `ScannerEmptyStateCard` (universe-scanned strip, regime + tape chips, mode-specific "Why nothing fired" headline + one-liner, a `<details>` re-enable bullet list lifted from the dashboard's existing `buildSwingReenableBullets`/`buildDayReenableBullets` so the Scanner and Dashboard never drift, and a cross-link nav to Signal Validation / Watchlist / Dashboard). The Swing / Day / Both tabs are now role-color coded (indigo-violet for Swing, teal-cyan for Day, neutral for Both) using the same `roleAccents` tokens the dashboard's Shared Context / Swing Desk / Day Desk cards use — so a Swing pill anywhere in the app reads as the same engine at a glance. Each tab also carries a small uppercase cadence sub-line (`Multi-day` / `Intraday` / `Two desks`) without bloating the accessible name (pinned via `aria-label`). Terminology drift fixed: the load-bearing distinction is now pinned in `frontend/lib/mode-terminology.ts` — `TAB_LABEL_SWING` / `TAB_LABEL_DAY` / `TAB_LABEL_BOTH`, `SECTION_LABEL_*_DESK`, `SUBHEADING_*_CADENCE`, and crucially `OUTCOME_LABEL_*_SIGNALS` (the Performance page used to say "Awaiting resolved swing trades" / "day trades" — wrong because STOCVEST resolves **signals**, not trades; now says "swing signals" / "day signals"). PDT terminology ("day trade(s)", "Pattern Day Trader") is correctly out of scope — that's an SEC term of art and stays unchanged in the PDT widget, PDT pill, order-confirmation modal, and settings copy. 40 new lock-in tests across `tests/mode-terminology.test.ts`, `tests/scanner-empty-state.test.ts`, `tests/scanner-empty-state-card.test.tsx` — closed-set terminology constants, mode-vocabulary anti-leak (swing card never uses "intraday confirmation" / "VWAP-aligned" / "ORB qualification" / "RVOL" / "session-structure"; day card never uses "regime alignment" / "multi-day structure" / "DailyBarScanner" / "weekly RSI" / "EMA200"), no-recommendation-words assertion across all visible card text. Frontend baseline now **624 passing tests across 62 files** (was 584 / 59). See **B33** below for the per-symbol funnel transparency follow-up that is intentionally **not** shipped in this pass. **D10 Phase 5 (Admin hub)** previously shipped → `/dashboard/admin/*` consolidated maintenance surface gated on the existing `signal-analytics-admin` Cognito group; `scripts/grant_admin.py` is the bootstrap path for the first admin; row in `IMPLEMENTED.md`. **Broker integration paused** — every broker-coupled UI surface is gated behind `brokersEnabled` in `frontend/lib/nav-features.ts` defaulting to `false`, env-overridable via `NEXT_PUBLIC_STOCVEST_FEATURE_BROKERS_ENABLED=true`. See **B31** below for the re-enable checklist. **Scenario Builder shipped** (paired with the broker pause) — `BuildScenarioButton` on Scanner gap cards, setup rows, and the Signal Evidence modal opens a planning-only modal with pure eligibility / compute helpers. Eligibility gates **only on structural completeness**, never on conviction signals, and the terminal actions are `Copy scenario` / `Reset` / `Close` with **no submit-to-broker path**. See **B32** for follow-up surface area. The next P0 candidate remains **P1 (Phase 7 hardening)**.

---

## Conventions

- IDs (B1, D10, P1, …) are **stable handles** for PRs/commits — they do not change when a row is reprioritized.
- When a row ships, **move it to `IMPLEMENTED.md`** rather than deleting it; preserve the ID.
- When a row is partially shipped (e.g. Phase 1 done, Phase 2 pending), keep it here with the **remaining work** described and a back-reference to `IMPLEMENTED.md` for what's already done.
- **Do not duplicate `CONTEXT.md` §3** (Terraform apply, secrets, broker production runtime, CI tweaks, attorney review) — that file owns near-term ops.

---

## P0 — In flight / immediate next

| ID | Theme | Notes |
|----|-------|-------|
| B62-Phase3-soak | **Ledger gate telemetry soak (2–3 weeks)** | **P66 deployed 2026-06-09.** Run **`python scripts/ledger_signal_report.py --period daily`** after 4:15 PM ET (see **`LEDGER_DAILY_VERIFICATION.md`**). **2026-06-09 spot-check:** day **4** qualified; swing **0** qualified / **118** shadow — jobs healthy, gates strict. **Do not** lower swing R/R until shadow-row gate breakdown reviewed. |
| P66-followups | **Small signal-engine tech debt** | **Sector benchmark-ETF cache coverage — DONE 2026-06-22.** The daily relative-strength cache warmed only 11 SPDR sectors + SMH/KBE/GLD, but `DEFAULT_SECTOR_TO_ETF` also maps biotech→XBI, pharma→XPH, medical-devices→IHI, retail→XRT, airlines→JETS, transport→XTN, defense→ITA, mining→XME — so those ~8 sectors always missed the cache and degraded to the single-day live-snapshot fallback instead of multi-day **persistence momentum**. Added them (+ SOXX defensively) to `SECTOR_ETFS_TO_TRACK` with display names + a drift-guard test (`tests/workers/test_sector_daily_cache.py`: every `DEFAULT_SECTOR_TO_ETF` value is warmed). **SOXX/SMH cache mapping** investigated → **not a bug**: laggard intelligence uses a separate `PriceCache` (`stocvest:price:SOXX:*`, warmed by `warm_price_cache_job`), the composite uses `sector_daily_cache` (SMH) — two independent subsystems by design. Backend **2491**/4, no Terraform/API change. **Deferred (with reasons):** `is_composite_layer_signal_scale()` heuristic is intentionally conservative (refining the [-1,1] vs 0–100 disambiguation risks the sector gate for no clear accuracy win); **`sector_layer_score` on day shadow rows** is telemetry-only — `evaluate_day_ledger_entry` has **no sector gate by design** (day gates on intraday structure, not daily sector RS), so adding one is a gate-contract change, not a bug fix; maturation `signal_summary` fallback is **already partially handled** (`watchlist_maturation_sync` derives the decision from `signal_summary` when `decision_state` is absent) and only affects watchlist display state, not signal accuracy. |

_Next major lane when soak completes: **P1 (Phase 7 hardening)**._

---

## P1 — High priority (next major lanes)

| ID | Theme | Notes |
|----|-------|-------|
| D13 | **Opportunity Desk + Dashboard Radar** | **Master plan:** [`OPPORTUNITY_DESK_AND_DASHBOARD_RADAR.md`](./OPPORTUNITY_DESK_AND_DASHBOARD_RADAR.md). **Phases 0–7 shipped** (0–6: 2026-05-26; **Phase 7 Quiet leaders + watchlist reserve:** 2026-05-27 — see [`IMPLEMENTED.md`](./IMPLEMENTED.md) **D13b**). **Deploy:** `terraform apply` + **scanner** Lambda (desk batch + quiet leaders) + frontend. **Ops:** CloudWatch namespace `OpportunityDesk` for batch SLOs. |
| P1 | **Phase 7 hardening** | E2E suite; security review (tenant isolation, PII); load tests; **minimum 2 weeks paper per trading mode**; staged real-money rollout. D1 EventBridge rule is applied in development (`CONTEXT.md` §3) but does **not** substitute for E2E. **Blocks real-money launch.** |
| B73 | **Mean-reversion bounce floor (swing technical)** | **DONE 2026-06-22.** Trigger: user asked whether a stock below SMA20/50/200 should score ~0, given it may be due for a pull-back/bounce and support proximity wasn't in the score. Root cause: the swing scorer is trend-following and *asymmetric* (penalizes upside extension, no downside mirror), so it floored **every** deep downtrend at 0 with zero resolution between an oversold-at-support bounce candidate and a free-falling knife. **Validation-first (per the user's request):** built a no-lookahead historical sweep (`scripts/validate_mean_reversion_credit.py`, 64 liquid names / ~2.4y / 4,340 confirmed-downtrend cases). Findings: forward 10-day bounce rises **monotonically** with RSI depth (RSI<20 → +4.8% mean / 84% positive; 20–25 → +3.9%/76%; 25–30 → +1.6%/63%; ≥30 → +1.5%/57%) and still-falling **fresh-low "knives" underperform** stabilizing names — so the credit is **oversold-led** with a knife filter; extension barely separated (kept minor). Honest caveats logged: net-up window + survivorship inflate *absolute* bounce size (the **separation** is the trustworthy part), argues for staying conservative. **Implementation (swing only, `swing_technical_analyzer.py`):** a **non-additive floor** — `score = max(trend_score, floor)` (a `max` *cannot* stack with other terms, unlike the originally-proposed additive credit which is also invisible after the 0-clamp on a −40/−60 raw). Gated on a **confirmed, stabilizing, oversold** downtrend: price < SMA50 & SMA200, MACD < signal, **RSI < 30**, and today is **not a fresh 10-day low**. Floor size = `base + round(mr * span)` where `mr = 0.8*oversold_depth + 0.2*downside_extension` (RSI-led), **hard-capped at 38 < `bearish_threshold` 40** so it adds resolution among oversold names but **never flips the bearish verdict**. New params on `SwingTechnicalParameters` (`mean_reversion_*`, `atr_period`); surfaces an "Oversold bounce watch" chip + reasoning line + `mean_reversion_floor_applied` result flag. **Day mode untouched** (mean reversion is time-based); non-oversold downtrends (MSFT/TSLA RSI ~32–50) and all uptrends are unaffected (gate shut → byte-identical). Tests: swing +5 (now 33 — floor lifts a stabilizing oversold downtrend into the low-bearish band, knife/RSI gates hold, ceiling caps below threshold, non-additive). Backend **2488**/4. No Terraform / no API schema change. **UI surfacing — DONE 2026-06-22 (B73-UI):** the "Oversold bounce watch" chip already flows to the deep-dive via the existing layer-chip drawer path (`composite-layer-rows.ts` maps technical `chips` → `filterDisplayChips`); verified it is not filtered as redundant, so no extra wiring was needed. **Remaining (data-dependent):** revisit bands + the extension/stabilization weights once a larger / bear-inclusive sample accrues (the sweep script is reusable); consider a support-proximity term once `structure_resistance_scanner.py` exposes level *quality* (touch count / volume / duration) — deferred as agreed. | 
| B72 | **Technical layer accuracy — swing recalibration + sector calibration** | **DONE 2026-06-22.** Trigger: user reported **NVDA Technicals = 5/100** despite NVDA being above SMA50/200 with a golden cross and RSI ~50. Root cause (not a data bug): the swing scorer stacked multiple overlapping pullback penalties (below-SMA20 −20 flat, −11%-from-high −15, MACD negative −10 + fading −5, distribution −15) against tiny structural credit (+15), flooring a healthy consolidation. **Fix (swing, `swing_technical_analyzer.py`):** (1) below-SMA20 penalty **magnitude-scaled** (`below_sma20_min_penalty` floor → `below_sma20_score` cap at `below_sma20_full_break_pct` below); (2) **durable-uptrend credit** (`golden_cross_score` when price > SMA50 & SMA200 with golden cross); (3) %-from-high caps at the **moderate** tier inside a durable uptrend (pullback ≠ breakdown); (4) MACD **fading penalty only while histogram ≥ 0** (no double-count of a negative histogram); (5) `volume_distribution_penalty` split lighter than `volume_accumulation_score`. NVDA now ~41 (neutral), verified live; MSFT/TSLA (genuinely below all MAs, death cross) still correctly bearish. **Sector calibration (new `sector_technical_calibration.py`):** coarse `HIGH_BETA/NORMAL/DEFENSIVE` regime (reuses `normalize_sector_for_geo`) scales the day-analyzer **RVOL surge threshold** (high-beta needs a bigger spike; defensives surge on less) and the **RSI overbought penalty** (high-beta overbought persists → lighter; defensives mean-revert → heavier), in both analyzers. `sic_bucket` plumbed through `TechnicalAnalyzer`/`SwingTechnicalAnalyzer`/`resolve_day_technical_layer`/both composite engines; unknown sectors neutral (1.0 — unchanged). Tests: swing +3 (now 28), new `test_sector_technical_calibration.py` (17). Backend **2483**/4. No Terraform / no API schema change (composite *values* shift for recognized sectors). **UI surfacing — DONE 2026-06-22 (B72-UI):** both composite engines now emit additive top-level **`sector_technical_calibration`** (`{sic_bucket, regime, rvol_threshold_multiplier, overbought_penalty_multiplier}`); the deep-dive threads it onto the Technical row (`composite-layer-rows.ts`) and renders a `…-CAL` regime chip + InfoTip + "Sector calibration" fact-line for non-normal regimes only (`signals-layer-breakdown.tsx`); +4 frontend tests; documented in API_CONTRACTS §4.3. **Remaining (data-dependent):** validate sector regime/overbought multipliers against the B71 Phase C event-study once data accrues; consider 5-min/RSI(9) day bars (low priority — `as_of_close` daily fallback dominates outside RTH). |
| B71 | **Per-symbol News/Geo sensitivity (event-driven layer weighting)** | **Goal:** some names reprice hard on a single headline (biotech FDA, semis on export controls, energy on OPEC, EM ADRs on sovereign risk) while others (utilities, staples, REITs) barely move — so News/Geo influence should vary per symbol, not be flat (`news 0.20`, `geo 0.10`) for all. **Phase A — SHIPPED 2026-06-21** (static sector priors → coarse down-only multiplier on News/Geo effective weight; `news_sensitivity.py` + `CompositeScoreEngine.sensitivity_multipliers`; see [`IMPLEMENTED.md`](./IMPLEMENTED.md)/header). **Phase B — capture — SHIPPED 2026-06-21** (chose the low-footprint option: extended the existing `news_snapshot_json` blob rather than a new DynamoDB table. The day + swing engines pass their top `catalyst_headlines` into `build_real_composite_snapshot_payload(news_events=…)` → persisted as `NewsSnapshot.top_events: list[NewsEventCapture]` (title/source/`published_at`/`sentiment_score`/sentiment/catalyst_type/url, capped at 8). Additive, no scoring change, no Terraform). **Remaining Phase B polish (optional):** capture is only on the real day/swing composite paths — the legacy client-supplied-scores swing handler (`build_swing_composite_snapshot_payload`) still stores aggregate-only; backfill not attempted (no historical article store). **Phase C — scaffold SHIPPED 2026-06-21 (read-only, not wired):** offline `stocvest/signals/news_event_study.py` + `scripts/news_event_study_report.py` join captured `top_events` → that signal's stored forward return to report per-symbol **sensitivity ratio** (mean |fwd return| on news vs quiet days) and **sentiment predictiveness** (sentiment-sign vs realized-move-sign agreement); supports `--since-days/--horizon/--mode/--min-samples/--fixture/--csv/--json`. **Now scheduled (2026-06-21):** a `news_event_study_report` api-Lambda module runs the same analysis **weekly** (EventBridge Scheduler `cron(0 18 ? * SUN *)` ET, `infra/eventbridge_scheduler_news_event_study.tf`) and writes a dated JSON report to a new reports S3 bucket (`aws_s3_bucket.reports`, `infra/s3_reports.tf`; `s3:PutObject` on the shared api role; 365-day lifecycle). Shared `build_sensitivity_report()` keeps CLI + Lambda identical; gated by `var.news_event_study_report_enabled` (default OFF), fail-safe (always 200). **Data-map caveats found:** forward returns are keyed off `generated_at` not each event's `published_at` (so v1 is a **signal-co-temporal** join, not event-time abnormal returns); **MAE/MFE columns exist but no writer populates them**; `atr_pct` derived from `technical_snapshot_json.atr` (day path only). **Phase C remaining (pending):** validate the numbers once data accrues, then (optionally, event-time precise via offline Polygon bars) learn → bounded per-symbol up-weight (only when *both* sensitivity + predictiveness clear thresholds + min sample; overrides sector default; engine clamps `[0.1,2.0]`; decays stale). **Cache observability SHIPPED 2026-06-21:** EMF CloudWatch metrics (`Stocvest/NewsSentimentCache`: `CacheCandidates`/`CacheHits`/`PrimeEnqueued`) emitted as zero-latency stdout lines (Lambda auto-extracts; no IAM) for dark-launch hit-rate validation. **Phase D — input quality — SHIPPED 2026-06-21 (dark / flag-gated OFF):** read-through Claude sentiment cache (`news_sentiment_cache.py`) — consumer writes a content-keyed sentiment entry, composite fills abstaining (`insights == []`) headlines via a batched `MGET` before scoring; fail-open, gated by `STOCVEST_NEWS_SENTIMENT_CACHE_ENABLED` (default OFF), labels normalized bullish/bearish→positive/negative. **Activation prerequisites (2026-06-21 map):** (1) flag now wired in Terraform via `var.news_sentiment_cache_enabled` → sets `STOCVEST_NEWS_SENTIMENT_CACHE_ENABLED` on **both** `news_consumer` (writer) and `signals` (reader) Lambdas + ECS worker; flip the var, `terraform apply`. (2) **The cache is only populated by the ECS news worker** (`infra/ecs_news_worker.tf`), which is `desired_count=0` / no image by default — until it runs, the cache stays empty and reads are no-ops. (3) **Coverage gap:** the worker ingests **Benzinga WebSocket + SEC EDGAR**, but the composite reads **Benzinga REST + Polygon REST** — a headline only hits the cache if it was WS-scored *and* later appears in REST with a matching URL/title (Polygon rows usually carry `insights` and skip the cache by design; EDGAR SEC URLs don't align with composite rows). (4) TTL raised to **6 days** (`518400`) so it exceeds the 120h swing lookback. (5) hit-rate now observable via `news_sentiment_cache enrich candidates=N hits=M` info log. **Phase D self-prime — SHIPPED 2026-06-21 (dark / flag-gated OFF):** the composite now enqueues abstaining cache-*miss* articles to the triage SQS for async Claude scoring (`prime_missing_news_sentiment` in `news_sentiment_cache.py`; called after enrich in both engines), so a missed Benzinga-REST headline becomes a *hit* on a later pass — closing the WS-vs-REST coverage gap without a synchronous Claude call. Per-article Redis pending markers (6h) prevent re-enqueue spam; one `send_message_batch` (≤10/pass); markers released on send failure; gated by `STOCVEST_NEWS_SENTIMENT_PRIME_ENABLED` (default OFF, also requires the cache flag) + a triage queue URL; IAM `SendMessage` already on the shared API role. **Phase D remaining (pending):** optionally a richer sentiment scale than the 3-way the composite reads today. Keep everything **soft** — News/Geo never hard-gate (consistent with the layer analysis: missing/quiet News/Geo are excluded/neutral, not blockers). |
| P1-MACRO | **FOMC / macro event intelligence (directional layer)** | **Assessed 2026-06-08 — not shipped.** Today: calendar warnings + macro **event-risk** score (20% of macro blend) + reactive regime from SPY/QQQ/VIX; **`MacroEventDetector`** (hawkish/dovish keywords) exists in news/AI paths but is **not** wired into live composite scoring. **Planned:** (1) contextual layer weight boosts around high-impact events (FOMC, CPI, NFP); (2) evidence-card **layer polarity** consistency (chip vs score sign); (3) lightweight "felt wrong" feedback widget for calibration; (4) optional pre-event posture copy (not directional prediction). **Deferred:** per-symbol FOMC beta, actual-vs-forecast reaction model, hard entry gates before Fed. See **`SIGNAL_ENGINE.md`** § Macro / FOMC limitations. |
| B31 | **Re-enable broker integration (Paper/Live, Portfolio, Journal, orders)** | **Currently paused.** Every broker-coupled UI surface is hidden behind `brokersEnabled` in `frontend/lib/nav-features.ts` (env-overridable via `NEXT_PUBLIC_STOCVEST_FEATURE_BROKERS_ENABLED`). Backend `/v1/orders/*` + `/v1/portfolio/*` routes + the entire `brokers` Lambda + IBKR/E*TRADE OAuth wiring are **intact and tested** — only the UI is gated. The Scanner gap cards, setup rows, and Signal Evidence modal currently host the **Scenario Builder** (`components/scenario-builder/*`, see **B32**) where the "Open order entry" CTAs used to live; when this flag flips back on the Scenario Builder stays (it's a parallel planning surface, not a replacement for execution) and the order-entry CTAs render alongside it. **Re-enable checklist** (run in order, do not skip): (1) flip `brokersEnabled` default to `true` (or set the env var per environment); (2) audit copy on the landing page, dashboard subtitle, and Onboarding wizard for "watchlist only" language that needs to revert to "watchlist + execution" — search for the diff vs the commit that shipped this flag; (3) verify E*TRADE OAuth callback domain is in the Cognito callback list and the production secret is rotated; (4) run the order-confirmation-modal + portfolio-multi-broker-panel test suites against the live backend (Phase 7 hardening prerequisite); (5) re-enable `/dashboard/portfolio` + `/dashboard/journal` direct-URL access (the server pages already redirect to `/dashboard` when the flag is off — flipping the flag is sufficient, no code change); (6) verify the Scanner "Open order entry" CTAs route correctly for both swing and day modes; (7) review `tests/nav-features.test.tsx` + `tests/top-bar-brokers-disabled.test.tsx` + `tests/settings-brokers-disabled.test.tsx` — these lock-ins must be inverted (or deleted) once the flag is on, otherwise they will fail loud as intended. **Depends on:** Phase 7 hardening (P1), counsel-approved execution copy. **Do NOT** rip out the broker code while paused — keeping it tested and dark is the cheap path back. |
| B5 | **Subscriptions** | Stripe (Atlas-linked), tier gating (map B20 tier IDs → entitlements: swing limits, day-trading preview vs full), usage limits. **Depends on** attorney-reviewed ToS and entity readiness (`CONTEXT.md` §14). **Blocks G1.** |
| B29 | **Portfolio + Journal `strategy_trading_mode` attribution** | **Blocked on backend schema change.** Mode Separation rule from `ASSISTANT_SYSTEM_PROMPT` requires positions and journal entries to carry mode attribution. Current state: `portfolio-page-client.tsx` flattens broker positions with no swing/day attribution; `JournalEntryPayload` has `is_day_trade` (PDT-style boolean) but no `trading_mode`; `journal-page-client.tsx` hardcodes `is_day_trade: true` on manual entries; analytics endpoint has no `mode` parameter; `UserMePayload.trading_mode` is `paper \| live` (execution mode) — a **naming collision** with signals-side `swing \| day` strategy mode. **Scope:** (1) backend schema — add nullable `strategy_trading_mode` to `Position` + `JournalEntry` with backfill plan deriving mode from `signal_id` linkage where present (surface "Unattributed" for legacy rows, do not fabricate). (2) API surface — `GET /v1/portfolio/holdings`/`summary`/`allocation` return mode; `GET /v1/journal/entries` accepts `mode` filter; `GET /v1/journal/analytics` accepts `mode` filter + returns per-mode metrics; `POST /v1/orders/submit` writes mode to position + journal entry. (3) Frontend — Portfolio mode chips + filter + "Unattributed" state; Journal Swing/Day tab pair with per-mode analytics; manual entry replaces hardcoded `is_day_trade` with explicit mode selector. (4) Execution-mode rename to resolve `trading_mode` namespace clash. |
| D4 | **Audit trail — extend beyond HTTP summaries** (In progress) | **Shipped slice:** HTTP/API replay audit — `AuditEvent`, `audit_store.py`/`audit_capture.py`, DynamoDB `AuditEvents` when `DYNAMODB_AUDIT_EVENTS_TABLE` set, `_with_cors_and_audit` on `lambda_dispatch`, admin `GET /v1/admin/audit/users/{user_id}` + `GET /v1/admin/audit/sessions/{session_id}`, extra row on beta toggle. **Still pending:** immutable order/PDT/broker archival, long-term retention (e.g. S3 Glacier), counsel-approved policy. |
| B75 | **Trading-room company names — bulletproof during full backend outage** | **Reliability fix shipped 2026-06-26** (BFF degraded-on-empty so the async name lookup self-heals instead of caching empties forever — see `CONTEXT.md` header + `frontend/app/api/stocvest/market/symbol-names/route.ts`). **Remaining (this row):** company names on feed cards + the center deep-dive still come from a **live** side-channel (`useSymbolNames` → `/api/stocvest/market/symbol-names`), so during a *full* backend outage (when even the desk cards are served from cache) the names can still be blank. To make them bulletproof, enrich the desk payload itself: carry `company_name` on **`DeskDiscoveryLeader`** by populating it in **`stocvest/api/services/opportunity_desk/discovery_row.py`** at desk-build time from the cached reference name (reuse the scanner's `SymbolLiquidityContext.company_name` source, which already resolves names for scanner setups — no extra per-symbol Polygon calls), then read `leader.company_name` first in `feed-model.ts::cardFromLeader`. Backend + frontend type change; needs `signals`/desk-batch Lambda deploy. Same shape as the parked "leader cards show neutral, not the real composite direction" follow-up (`discovery_row.py` could also carry composite direction) — worth doing both in one desk-payload-enrichment pass. |

---

| A1-tests | **A1 follow-up test coverage** | **DONE 2026-06-02.** (1) `tests/utils/test_symbol_detector.py` — `detect_symbol` dollar-sign priority, blocklist correctness, `detect_symbol_from_messages` multi-turn fallback, empty/None input. (2) `tests/api/services/test_assistant_symbol_context.py` — `fetch_assistant_symbol_context` timeout returns partial data, empty symbol returns None, all sub-calls wrapped in try/except. (3) `tests/api/handlers/test_signals_assistant_chat.py` — image MIME validation (accepts PNG/JPG/WebP, rejects PDF), symbol detection wiring (mock `fetch_assistant_symbol_context`, verify it's called with detected symbol), `symbol_context` passed to `svc.reply`. (4) `frontend/tests/use-voice-input.test.ts` — `isSupported` false when SpeechRecognition absent, toggle start/stop, `onTranscript` called with result, FRIENDLY_ERRORS map. (5) `frontend/tests/assistant-panel-redesign.test.tsx` — placeholder text "Ask me anything about stocks", + button triggers file input, mic button present, image preview shows when attachment set, remove clears attachment. |
| A2 | **Watchlist management via assistant chat** | **DONE 2026-06-02.** User types "add PE to my watchlist" → assistant detects intent + symbol, calls `POST /v1/watchlists/{default_id}/symbols`, confirms with company name. Patterns: "add X", "add X to watchlist", "watch X", "track X", "remove X from watchlist". Symbol validation before write. Response includes action_type=watchlist_add + confirmation text. |
| A3 | **Discovery queries from scanner cache** | **DONE 2026-06-02.** User asks "what stocks have momentum today?" / "any gap setups?" → assistant pulls from cached desk/scanner results (no new scan), returns top 3–5 symbols with brief context. Patterns: "momentum stocks", "gap stocks", "what's moving", "top setups". Pull from opportunity desk cache; if no cache, route to scanner. |
| A4 | **Assistant deep-link routing** | **DONE 2026-06-02.** For entry/stop/target questions, return brief signal context + a `navigate_to` field pointing to `/dashboard/signals?symbol=X`. Frontend renders a "→ Open on Signals" CTA button below the response. Pattern: any question about entry/stop/target/setup/trade plan → always include navigate_to. |
| A7 | **Assistant breadth-with-restraint** | **DONE 2026-06-28.** Widen assistant scope while making restraint a visible feature. (1) **Web search fallback** — out-of-envelope macro/policy/sector/"latest on…" questions (no symbol/discovery/market/watchlist intent) consult Perplexity Sonar via `assistant_web_context.py`; `is_web_search_query` detector; flag-gated `STOCVEST_ASSISTANT_WEB_SEARCH_ENABLED` (default OFF; env var applied to signals Lambda 2026-06-28, code live on merge); citation-backed (`web_sources`), WEB CONTEXT RULES keep it facts-no-verdicts. (2) **Multi-symbol comparison** — `assistant_multi_symbol_context.py` + `detect_symbols` + `is_comparison_query`: "compare NVDA vs AMD" fetches each ticker's price + STOCVEST cached read + analyst consensus in parallel, returns `compared_symbols`; MULTI-SYMBOL COMPARISON RULES present the contrast but never rank/pick a winner; fires only with ≥2 distinct tickers and skips the single-symbol path. (3) **Uncertainty surface** — `fetch_stocvest_composite_read` now derives plain-English `not_yet_confirmed` caveats (missing/split layers, neutral verdict, staleness); the locked prompt voices them alongside the verdict (no fabricated "confidence" score). (4) **Plan-aware watchlist cap** — `execute_watchlist_add` takes the plan cap from `watchlist_symbol_cap_for_profile` (100 for swing_day_pro/beta) instead of a hardcoded 50. Backend +38 (`tests/api/services/test_assistant_web_context.py`, `test_assistant_multi_symbol_context.py`, `test_assistant_watchlist_action.py`; symbol/intent detector, symbol-context, broader-coverage, assistant-chat handler additions). Additive — no API contract break (new optional response fields `web_sources` / `compared_symbols`). |

---

## P2 — Medium priority (scoped value)

| ID | Theme | Notes |
|----|-------|-------|
| B46-Phase6 | **Setup analytics — retire `SignalHistory`** | **Prep done 2026-05-17** — deprecate `GET /v1/signals/me/history`, dead validation UI removed, retirement doc + inventory: [`SIGNAL_HISTORY_RETIREMENT.md`](./SIGNAL_HISTORY_RETIREMENT.md). **Still pending:** stop writes, drop table, weight proposer / public performance migration. Do **not** remove DynamoDB until step 7 in that doc. **Note:** B62 **increases** `SignalHistory` write volume (shadow gate-attempt rows) — Phase 6 retirement must migrate or archive B62 telemetry first. |
| B68-followups | **Setup Evolution — deferred analytics** | **B68 core tab shipped 2026-06-10.** **Deferred:** (1) **Comparable setups** — “N similar setups reached actionable in M sessions” (needs ledger volume). (2) **Aggregate pattern performance** on the per-symbol tab — historical accuracy by setup type (counsel review before any hypothetical-return framing). (3) Hub **`showSummary`** stat tiles if product wants summary cards on `/dashboard/setup-evolution`. See **`SETUP_ANALYTICS_SPEC.md`** § Evolution tab UX. |
| B62-Phase3 | **Validation ledger — gate telemetry review + threshold decisions** | **Phase 1–2 shipped 2026-05-22**; **P66 fixes 2026-06-09** (swing rows now captured; sector scores no longer cluster at 20). **Ops:** daily **`scripts/ledger_signal_report.py`** + saved reports in **`reports/ledger/`** (**`LEDGER_DAILY_VERIFICATION.md`**). **Wait ~2–3 weeks** from P66 deploy for soak. **Then:** query shadow rows (`ledger_only=false`, pattern **`*:ledger_capture_shadow`**, or **`gate_status_json.evaluation_source == "ledger_capture"`**) for gate-failure breakdown (timing, score, R/R, sector, etc.). **Decision gate:** whether to change swing R/R (**hold at 2.0** until telemetry reviewed). **Optional follow-ups:** expand capture beyond default watchlists; admin study coverage report. See **`IMPLEMENTED.md` B62**, **`SIGNAL_ENGINE.md`** § Signal validation ledger. |
| B41 | **Watchlist maturation — push / in-app alerts + counsel gating beyond email** | **Email + lightweight in-app strip shipped 2026-05-15** — see **`IMPLEMENTED.md` B41**. Dedupe: **ET calendar day + exact transition** (mode + from + to states). Default **Watchlists** page shows recent maturation **email** rows from alert history. **Still pending:** browser push, richer in-app center; idempotency keyed with **per-mode finer buckets** if product asks; plan noise rules (**B5**). |
| D11 | **Performance architecture — Tier 1.B+ → Tier 5 (long-term plan, see [`docs/PERFORMANCE.md`](./PERFORMANCE.md); dashboard IA + SLOs + click model in [`docs/DASHBOARD_TERMINAL_UX_PLAN.md`](./DASHBOARD_TERMINAL_UX_PLAN.md))** | **Tier 1.C Phase 0–1 shipped 2026-05-14** — Phase 0: server-side dashboard fetch timings (`frontend/lib/dashboard/load-timing.ts`; logs `[dashboard-load] <phase> <ms>ms` when `STOCVEST_DASHBOARD_TIMING=1` or `NODE_ENV=development`). Phase 1: `/dashboard` RSC first segment **market + daily**; **earnings** + **scanner** stream after (`dashboard-page-content.tsx`, `lib/dashboard/dashboard-page-data.ts`, `DashboardEarningsDeferredFetch` + `DashboardScannerDeferredFetch` + hydrates); lock-in `tests/dashboard-scanner-deferred-hydrate.test.tsx`, `tests/dashboard-earnings-deferred-hydrate.test.tsx`, `tests/dashboard-tier1c-dual-deferred-hydrate.test.tsx`; Vitest stubs `DashboardEdgeSync` in `vitest.setup.ts`. See **`IMPLEMENTED.md` D12** (Tier 1.C Phases 0–5 complete, 2026-05-15). **Tier 1.A shipped 2026-05-13** (dashboard `<Link>` prefetch storm killed, lock-in tests in `dashboard-prefetch.test.tsx`). **Tier 1.B shipped 2026-05-13** (`/dashboard/signals/page.tsx` split into a fast outer shell + async `SignalsPageData` island wrapped in `<Suspense fallback={<SignalsPageShell />}>`, plus `app/dashboard/signals/loading.tsx` for the inter-route transition state. `SignalsPageClient` itself was not refactored — same props, same chatbot context publishing, same Mode Separation perimeter. Lock-ins: `signals-page-streaming.test.tsx` 12 tests including a source-level invariant that the four heavy fetchers live inside `SignalsPageData`, not the outer page). **Layer 4 first slice shipped 2026-05-13** (SWR `^2.4.1` chosen over TanStack Query for bundle size + Vercel-native fit; app-wide defaults pinned in `lib/swr/config.ts` — `revalidateOnFocus: false`, `dedupingInterval: 30_000`, `errorRetryCount: 1`, `keepPreviousData: true`, never-retry on 401; `<StocvestSwrProvider>` mounted in `app/layout.tsx`; `useSymbolSnapshot(symbol)` is the first SWR-backed hook and replaces the imperative `useEffect → fetchSymbolSnapshot` on the signals page so repeat symbol views are now cache-hit; new `useHoverPrefetch(href)` wired onto every Tier-1.A heavy `<Link prefetch={false}>` — ribbon chips, ribbon empty-state CTA, day-desk rows, day-desk footer, swing-desk footer — so the route warms on hover / focus / pointer-down. The Tier 1.A ↔ Layer 4 contract is locked in by `dashboard-hover-prefetch.test.tsx` 5 tests that assert BOTH `data-prefetch="false"` AND `data-hover-prefetch="true"` on every heavy dashboard link. 34 new lock-in tests across `swr-config.test.ts` (10), `swr-fetcher.test.ts` (7), `use-hover-prefetch.test.tsx` (6), `use-symbol-snapshot.test.tsx` (6), `dashboard-hover-prefetch.test.tsx` (5). `vitest.setup.ts` now ships a DEFAULT `next/navigation` mock so subtrees containing `useHoverPrefetch` work in jsdom without each test file declaring its own — existing per-file mocks are hoisted and take precedence). **Queued follow-ups, in priority order:** **(a) Tier 1.B+ — per-section islands on `/dashboard/signals`** (only if a real user reports the current "swap everything at once" feels janky). Split `SignalsPageData` into 2–3 parallel async children — `<MarketOverviewIsland />`, `<ScannerIsland />`, `<EarningsIsland />` — each in its OWN Suspense boundary. Requires refactoring `SignalsPageClient` into matching slice client components. Real risk: medium (touches the chatbot context publisher which expects the full settled payload). Estimated effort: 1–2 days. **(b) Layer 4 second slice — extend SWR coverage. SHIPPED 2026-05-13.** Four new hooks under `frontend/lib/hooks/` — `useSignalComposite`, `useSymbolNews`, `useDashboardPayload`, `useMacroContext` — replace the imperative `useEffect → fetch` blocks on the signals page (composite POST + after-hours news) and the dashboard (polling loop + macro snapshot). The live-hint SSE stream now invalidates the dashboard cache via `mutate(dashboardPayloadKey(mode))` instead of running a parallel polling closure. `useSignalComposite` overrides the global `keepPreviousData:true` to `false` to preserve the prior user-requested "clear screen between mode pills" UX. 23 new lock-in tests across `tests/use-signal-composite.test.tsx`, `tests/use-symbol-news.test.tsx`, `tests/use-dashboard-payload.test.tsx`, `tests/use-macro-context.test.tsx`; impacted existing tests (signals-page-mode-clears-screen, signals-page-streaming, dashboard-cache, dashboard-prefetch, dashboard-hover-prefetch, use-symbol-snapshot) all green. `signals-page-mode-clears-screen.test.tsx` got one targeted infra fix: each render now wraps in a fresh `<SWRConfig provider={() => new Map()} dedupingInterval={0}>` so SWR cache state does NOT leak between tests — the "clear screen between modes" UX rule applies to UNCACHED flips; a cached flip is instant (a strictly better UX) and the test would otherwise oscillate based on prior-test side effects. Full architecture context lives in [`CONTEXT.md`](./CONTEXT.md) row 15. Original second-slice scope note kept here for archive: previously read "Add useSymbolNews + useSignalComposite (currently imperative inside signals-page-client.tsx's mode-toggle flow), and convert the dashboard's /api/dashboard?mode=... + /api/macro-context + /api/live?mode=day BFF calls to use <SWRConfig>-provided defaults. Infra is in place; remaining work is mechanical per call site. Estimated effort: 1–2 days." Note that `/api/signals/live` is an SSE EventSource — push, not GET-and-cache — so it remains as `useLiveSignals`, but its hint now feeds the SWR cache via `mutate`. **(c) Layer 2 — thin API projections.** Add `?view=summary` to high-traffic list endpoints (scanner, signals, watchlist). Slim payload target: < 500 bytes/row. Update ribbon chips, table rows, scanner cards to consume the slim variant. Existing `?view=detail` clients (signal-detail page, evidence modal) keep working. Estimated effort: 3–4 days backend + 1 day frontend wiring. **(d) Layer 1 — CloudFront edge cache for user-agnostic reads.** Cache VIX, regime, breadth, sector heatmap, macro context with 10–30s TTL. Strict invariant: never include user identifiers in the cache key for these endpoints. Add API Gateway HTTP API response compression (`MinimumCompressionSize`) since today raw JSON ships uncompressed — verify via `aws apigatewayv2 get-api`. Estimated effort: 2–3 days ops. **(e) Layer 5 — WebSocket push for live regime + active-signal deltas.** API Gateway WebSocket → Lambda fan-out → DynamoDB connection table. Do NOT ship until layers 1–4 are in; polling is fine and 10× cheaper. Estimated effort: 1–2 weeks. **Cross-cutting invariants** (every perf PR must respect): (1) no `<Link prefetch="auto">` to a heavy SSR target from an N-of-N container, (2) no user-identified data in shared cache keys, (3) ≤ 4 parallel heavy fetches per page render, (4) chatbot context publishing must survive streaming refactor, (5) Mode Separation must survive island split (day-side islands consume day-side data only), (6) `data-prefetch="false"` AND `data-hover-prefetch="true"` MUST both hold on every heavy dashboard `<Link>` (Tier 1.A ↔ Layer 4 contract). Lock-in tests in `dashboard-prefetch.test.tsx`, `dashboard-hover-prefetch.test.tsx`, `signals-page-streaming.test.tsx`, `dashboard-two-desk.test.tsx`, `dashboard-redesign-phase-b-c.test.tsx`, and the chatbot-payload tests cover (1) + (4) + (5) + (6); (2) and (3) need code-review discipline plus a future static-check. |
| B-swing-portfolio | **Swing ledger enrichment (MAE/MFE, daily exit dates, rule labels)** | `SignalRecord.mode=swing` + `SignalHistory` ledger columns already exist; user UI is **setup outcomes** (B46), admin D2 stratification at `/dashboard/admin/historical-validation`. **Pending:** jobs to fill `ledger_*_date_et`, `exit_reason`, `max_adverse_excursion_pct`/`max_favorable_excursion_pct`, swing-specific `exit_rule` from daily-bar paths. **Do not** reinstate the removed `ModelPortfolio`. |
| B34 | **Replicate token-based pagination to Audit log, Weight proposals, Parameter history** | **Pattern shipped on `/dashboard/admin/users`.** The Admin Users page now defaults to listing everyone (no "type to search" empty state), 25 per page, with `<AdminListPager />` (`components/admin/admin-list-pager.tsx`) driving Prev / Next via a client-side token stack. Backend contract: `GET /v1/admin/users/search` accepts empty `q` + opaque `page_token`, returns `next_token`; service helper is `list_users_page(query="", *, limit=25, page_token=None)` in `services/admin_user_directory.py`. **Pending replication targets:** (1) **Audit log** — `GET /v1/admin/audit/recent` today does a bounded DynamoDB Scan with a server-side `Limit` and returns the full result with no continuation token. To paginate properly we need to capture and round-trip DynamoDB's `LastEvaluatedKey` as an opaque `next_token` (base64 the dict), so the UI's pager can drive Prev / Next without re-scanning from scratch each page. Same `<AdminListPager />` component plugs in unchanged. (2) **Weight proposals** — the upstream proposal list is already small (proposals/week × retention), but it's tabbed by status; when any tab grows past 25 rows we should plumb the same pager rather than infinite-scroll. The proposals query already lives on a DynamoDB GSI keyed by `(status, occurred_at)`, so `LastEvaluatedKey` plumbing is again the right shape. (3) **Parameter history / rollback** — `GET /v1/admin/parameters/history` returns the full append-only audit; today it's small enough that pagination is theoretical, but the same shape applies once it grows beyond a screen. **Non-scope:** non-admin surfaces (Dashboard performance, Scanner setup rows, Signal Validation ledger). Those are bounded by business semantics (one Swing Desk row per day, capped intraday setup count, etc.) and adding a pager there is noise. **When implementing:** the frontend `<AdminListPager />` is reusable as-is — only the page client and the upstream service need work per surface. Audit log first since its empty state is the most visible (it's the default sub-page admins land on after the Hub overview). |
| B32 | **Scenario Builder follow-ups (local-storage save, scenarios dashboard, freshness tuning)** | MVP shipped — `BuildScenarioButton` + `ScenarioBuilderModal` on Scanner gap cards, setup rows, and the Signal Evidence modal, with pure helpers in `frontend/lib/scenario/{types,eligibility,compute}.ts` and 52 lock-in tests in `frontend/tests/scenario-*`. **MVP is intentionally stateless** (terminal action is "Copy scenario" → clipboard text) to keep STOCVEST out of every plausible "trade staging" reading. **Follow-up surface area to consider:** (1) **Local-storage save** — let users keep a small ring buffer of recent scenarios in the browser only (no server write), with a "My scenarios" inspect panel and a confirm-before-delete step. (2) **`/dashboard/scenarios` page** — surface the saved scenarios with a side-by-side comparison view (symbol, R-multiple, total $ at risk, freshness) so the user can prune to the best 2–3 before deciding what to actually plan around. (3) **Per-mode freshness window tuning** — the current windows (swing: 7d, day: 18h rolling) are mechanical defaults; a future pass could pull the canonical window from the engine's `signal_valid_until` / `signal_expires` field on every payload so the eligibility gate and engine stay in lockstep. (4) **Position-as-%-of-ADV warning** — when `shares × entry` exceeds a configurable % of the symbol's 20-day average dollar volume, the computed block could surface an informational warning. **Strict non-features (do NOT add):** any submit-to-broker, queue, or persistence-to-server affordance; any field labeled "Recommended"; any gating on conviction signals (confluence, accuracy, layer alignment, engine verdict). The whole point of the Scenario Builder is that STOCVEST stays out of the execution / endorsement path even when it's a great signal. |
| B3 (Phase 2) | **Alerts delivery — push + webhooks** | Phase 1 SES email shipped (see `IMPLEMENTED.md`). **Pending:** browser push, webhooks. **SMS deferred to B13** (depends on phone collection). |
| B6 | **Sector & market internals** | Sector rotation view, breadth (A/D, highs/lows), VIX regime, optional put/call — **data-only** per legal framing. Backend already exposes internal SIC → sector ETF resolution with `SicMappingTier` (see D9 in `IMPLEMENTED.md`). |
| B7 | **Risk management UX** | Position sizing helper (% of portfolio), optional max-daily-loss guardrails, concentration / correlation hints — **never** framed as advice; copy reviewed with counsel. |
| B8 | **Scanner order placeholder** | Replace "Order modal placeholder" on scanner with real order entry or deep-link to portfolio panel; align with Step 8 validation flow. |
| B20 | **Swing-first UX + plan preview + news window** (In progress) | Most product/UX/news-window work shipped (see `IMPLEMENTED.md` B19/B25 and history). **Pending follow-ups:** wire `DEFAULT_UI_PLAN` to billing (depends on B5); optional Claude tooltip generator behind BFF with caching (depends on B22). |
| B22 | **Claude-generated metric explanations (cached)** | BFF (e.g. `POST /api/stocvest/explain-metric`) accepting metric key + numeric context + layer snapshot hash; return plain English for tooltips; **fallback** to `metric-decision-copy.ts` on error/rate-limit; enforce privacy (no account numbers); cost controls. AI **signal** explanations already gate on `has_ai_explanations` (paid or beta full access). |
| D5 | **Anonymized behavioral data policy** | Operationalize user deletion → anonymize behavioral rows; document in Privacy; engineering checklist only — do **not** duplicate legal disclaimers from `CONTEXT.md` §4. |

---

## P3 — Low priority / deferred / conditional

| ID | Theme | Notes |
|----|-------|-------|
| B33 | **Per-symbol funnel transparency / near-miss panel on Scanner** | **DONE 2026-05-16 · commit: df1bb2c** — see **`IMPLEMENTED.md` B33**. Counsel sign-off before broad promotion remains product-only. (B42 near-qualification + progression lanes remain separate). **Phase 1:** request-scoped **`evaluation_trace`** on setups v2. **Phase 2:** **`GET /v1/signals/scanner-trace`** + DynamoDB **`ScannerEvaluationTrace`** (48h TTL per user × desk × ET session); POST setups persists trace when authenticated; scanner hydrates from GET when the live scan returns no trace rows. **Ops:** run **`terraform apply`** for table + API route. **Remaining (product):** counsel sign-off on copy before broader promotion — not a code blocker. When the Scanner is empty, today's `ScannerEmptyStateCard` answers _aggregate_ "why nothing fired" (regime, session state, universe scanned, mode-specific re-enable bullets). The next-level version would answer _per-symbol_: "we evaluated NVDA today; it failed at the volume / momentum gate by N% RVOL," or "AAPL passed regime but failed the score floor at 0.42 vs the 0.48 minimum." **Why this is not P1/P2:** (a) **implicit-recommendation risk** — a per-symbol "almost made it" panel is the same UX class as "near-miss," which the user could reasonably read as "watch this one closely." That crosses the line we just protected the Scenario Builder against (B32). The legal / framing copy would have to be exceptional ("symbols our engine evaluated but did not qualify — this list is not a watchlist"). (b) **backend telemetry not currently captured** — the scanner today returns successful candidates only; it does NOT emit a per-symbol audit row of "evaluated X, failed at gate Y by margin Z." Shipping this requires a new structured trace from `DailyBarScanner` + the day-side scanner, plus a `GET /v1/signals/scanner-trace?mode=swing\|day` endpoint with bounded retention (probably 24-48h rolling, in DynamoDB or S3 Parquet). **Scope when promoted:** (1) backend trace emission + endpoint + IAM. (2) Frontend: optional "Show today's evaluations" expander under `ScannerEmptyStateCard`, capped to 20 rows, with explicit "Evaluated · did not qualify · NOT a watchlist" copy. (3) Lock-in tests: cards never use "almost made it" / "near miss" / "watch closely" / "consider" wording. **Do NOT** ship this without counsel review of the per-symbol copy. |
| P2 | **Secrets-managed prompts** | Claude system/user prompts loaded from AWS Secrets Manager; version field on API calls; cache on cold start. (D3 marked P2 as deferred — same workstream.) |
| B13 | **Phone number collection + SMS alerts** | **Depends on** B3 Phase 2. Phase 1 — optional phone in Settings → Notifications, verify via AWS SNS 6-digit code before storing (never collect unverified). Phase 2 — required for Pro SMS tier. Channels: PDT warning (free), signal state change (Pro), confluence alert (Pro), morning brief (Pro). ~$0.00645/SMS US. CCPA/GDPR Privacy Policy disclosure required before collecting. Do **NOT** add to signup form. |
| P3 | **Mobile / PWA** | Responsive web exists; native or PWA shell, push for alerts when B3 Phase 2 exists. |
| P4 | **Enhanced auth** | Optional SMS OTP, WebAuthn, magic link — Cognito capabilities; cost/compliance review. |
| P5 | **Infrastructure naming consistency** | Align DynamoDB table names with Terraform tag conventions **or** update tags to match table names. **Low priority — no user impact.** Example: tag `Name = "stocvest-development-ddb-signal-history"` vs actual table `SignalHistory`. |
| G1 | **Referral program** | Invite trader → credit. **Depends on** B5 (Stripe). |
| M1 | **Cross-asset signal views** | Options flow vs spot, futures basis vs spot, crypto vs risk proxies — **correlation / divergence as data**, not recommendations. |
| T | **Post-beta "trade brief" (rich UI)** | Single theme — rich card: reference levels (historical entry zone, reference targets, reference stop) as **data**, signal-parameters block, technical grid (VWAP, EMA, RSI, etc.), catalysts vs risks columns, risk/reward **chart as visualization only**. **Depends on** D1 data maturity + counsel on display copy. Legal review required before customer-facing price language. |

---

## Cross-references

- **Shipped work archive:** [`IMPLEMENTED.md`](./IMPLEMENTED.md)
- **Status at a glance / what's implemented / near-term ops / test baselines:** [`CONTEXT.md`](./CONTEXT.md) §1–§3, §13
- **Legal rules / session rules:** [`CONTEXT.md`](./CONTEXT.md) §4, §13
- **API contracts:** [`API_CONTRACTS.md`](./API_CONTRACTS.md)
- **Signal engine reference:** [`SIGNAL_ENGINE.md`](./SIGNAL_ENGINE.md)
- **Ledger daily verification:** [`LEDGER_DAILY_VERIFICATION.md`](./LEDGER_DAILY_VERIFICATION.md)
- **Doc index + dashboard terminal plan:** [`README.md`](./README.md), [`DASHBOARD_TERMINAL_UX_PLAN.md`](./DASHBOARD_TERMINAL_UX_PLAN.md)
