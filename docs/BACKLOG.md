# STOCVEST — Backlog

**Use with [`CONTEXT.md`](./CONTEXT.md).**  
`CONTEXT.md` holds **status-at-a-glance**, **what’s implemented**, **near-term ops** (Terraform, secrets, CI, legal), **legal rules**, and **session rules**.  
**This file** holds **planned work only**: themes, sub-items, and notes—**without** repeating the CONTEXT status table or §3 pending list.

**Last updated:** 2026-05-12 latest (**Automated weight-tuning proposal pipeline — scheduled worker (D10 Phase 2b)** — ships the Lambda + IAM + EventBridge wiring that turns the Phase-2a optimizer into a weekly scheduled job. New service module orchestrates load-params → scan-history → optimize-per-mode → gate → write-proposals with **per-mode error isolation** (swing failure does NOT block day completion). New Lambda handler is a thin EventBridge wrapper that always returns HTTP 200 to prevent retry-storms — failures surface in the response body for CloudWatch. Terraform creates a DEDICATED minimal-privilege IAM role for the worker (`lambda_weight_proposer_execution`) — read on SignalHistory + write on ParameterProposal + **READ-ONLY** on the signal-parameters secret. **NO `secretsmanager:UpdateSecret`, NO `PutSecretValue`, NO `CreateSecret`, NO `DeleteSecret`** — the worker is physically incapable of mutating production weights. EventBridge cron `cron(0 8 ? * MON *)` runs Monday 08:00 UTC. +26 lock-in tests covering projection rules, baseline resolution, end-to-end orchestration, per-mode error isolation, and Lambda handler anti-retry-storm guarantees. Ships dark in production behavior — proposals get written to DDB, but until Phase 3 admin BFF, no path exists to promote them. Previous — **Automated weight-tuning proposal pipeline — algorithmic core (D10 Phase 2a)** — ships the pure-function walk-forward grid-search optimizer that scores candidate composite-weight rotations against the historical signal record. No AWS, no IO, no Lambda — algorithm only. Replays the production `CompositeScoreEngine` against each stored `SignalRecord.layer_scores` to derive a candidate verdict per row, then compares to the actual ±0.1% price direction (same `outcome_from_prices` threshold the live system uses — equivalence pinned via cross-module test). Constrained ±delta grid (3^6 = 729 raw candidates → filtered + normalized + deduped), three-gate acceptance check (val accuracy lift ≥ +2pp + val signal count ≥ 30 + train/val regime distribution within 20% per regime). Verdict thresholds and multi-objective scoring are deliberately deferred. Lossy reconstruction caveat (confidence not persisted on `SignalRecord` so reconstructed as 1.0) is symmetric across candidates so relative ranking is preserved. +44 lock-in tests in `tests/signals/test_weight_optimizer.py`. Production code path unchanged. Previous — **Automated weight-tuning proposal pipeline — foundation (D10 Phase 1)** — ships the data-layer foundation for proposal-only weight tuning, dark, with zero production behavior change. Closes user's question "Can this be automated without manual intervention? Do you suggest doing it?" with my recommendation NOT to fully automate — instead build a proposal pipeline with a human-in-the-loop gate (rationale: 12-dim search space with ~150-300 resolved signals per mode = overfitting risk; regime-tied optimization on trailing 60d; Goodhart's law on accuracy metric; loss of explainability; survivorship bias). Closes user's question "where are we storing these weights? They should be read from Secrets Manager" with confirmation that yes, weights ARE in `stocvest/signal-parameters` Secrets Manager + `ParameterHistory` DDB version log; we're staying there (KMS + IAM gating + CloudTrail audit are the right tradeoffs at our scale; AppConfig is a future upgrade, not now). Closes user's question "If they are stored in secretsmanager, how will automation update these values?" with a three-IAM-principal pattern: optimizer Lambda role (writes proposals, NO secrets-write IAM) → admin endpoint role (reads proposals + writes secrets via admin auth) → read-path Lambda roles (read-only). **Phase 1 shipped 2026-05-12:** new DDB table `ParameterProposal` with `proposal_id` PK + `status_index` GSI (status PK + created_at SK, used by admin UI to list pending sorted by date), TTL on `ttl` attribute so rejected/superseded auto-expire after 90 days (promoted rows have NO TTL — they're the audit trail). Lambda env wires `DYNAMODB_PARAMETER_PROPOSAL_TABLE` + adds the ARN to `lambda_dynamodb_resources`. Settings field `dynamodb_parameter_proposal_table`. New module `stocvest/data/parameter_proposal_store.py`: `ParameterProposal` dataclass with `new_pending` factory (validates at least one per-mode override is set — proposals with neither carry no information); `to_item`/`from_item` with JSON-encoded nested fields (sidesteps DDB Decimal coercion + reads cleanly in AWS console); `ParameterProposalStore` class with `put` (idempotent for retry-safety), `get`, `list_by_status` (closed-set status validation, DESC by `created_at` via the GSI), `mark_promoted` / `mark_rejected` / `mark_superseded` with **atomic DDB `ConditionalCheckExpression`** on `status` — the load-bearing guarantee that a rejected proposal cannot be silently re-promoted by a racing admin click. `build_default_proposal_store` for production wiring (raises on missing table env var — Phase 1 ships dark, so this is never invoked from production paths yet). **Tests:** +24 lock-ins in new `tests/data/test_parameter_proposal_store.py` — factory invariants (proposal-with-neither-block raises; swing-only & day-only both valid; auto-id + auto-timestamp), round-trip serialization (full-fields identity, omits optional review-state when unset, JSON-encodes nested dicts, tolerates raw-dict inputs from hand-written rows), CRUD (round-trip through store, missing-id returns None, idempotent put), GSI semantics (DESC by created_at, status filter, invalid-status raises loud not silent-empty, limit honored, asserts the right GSI name is queried), transitions + atomicity (each of promoted/rejected/superseded transitions stamps the right audit fields + TTL; rejected stamps ~90d TTL; promoted has NO TTL; cannot promote already-promoted; cannot reject already-rejected; cannot promote rejected; missing proposal raises ValueError), closed-set invariants (status values + TTL days documented). Backend baseline **1093 → 1117** (+24, zero regressions). Terraform `validate` clean. **Production behavior change: NONE today.** Phase 1 is pure data-plumbing — the table exists, the repo can read/write/transition, but nothing in production calls it yet. Phases 2-4 (optimizer Lambda, admin BFF + UI, CloudWatch safety net) ship on subsequent approvals. Previous shipment — **Gap-card mode-fit classifier + per-row Evidence routing (B30 Phase 4)** — closes the last three B30 Severity-2 items in one shipment: (1) `scannerSetupMode === "both"` gap-card default routing (no longer mirrors `panelNewsTradingMode` blindly), (2) "Best evaluated as: swing / day / either" advisory tag on gap cards, (3) per-row classifier-driven engine selection. **Backend:** new public `classify_mode_best_fit(*, gap_pct, volume_vs_avg, has_catalyst, catalyst_category, catalyst_narrative_score) -> tuple[str, list[str]]` in `stocvest/signals/gap_intelligence.py` counts swing-tilt signals (structural catalyst earnings/merger/insider, large gap ≥3%, high-conviction catalyst narrative ≥60) vs day-tilt signals (heavy volume ≥2× avg, tradable intraday range [1.5%, 5%), momentum gap with no catalyst OR tape-level macro/analyst catalyst). Decision rule: margin ≥2 picks the winner, anything closer returns `"either"`. Returns reasoning chips for transparency. Closed-set return values pinned as `MODE_BEST_FIT_VALUES = ("swing", "day", "either")`. `build_gap_intelligence_items` now stamps every row with `mode_best_fit` + `mode_best_fit_reasons`. **Frontend:** `GapIntelligenceItem` gained optional `mode_best_fit?: GapModeBestFit` + `mode_best_fit_reasons?: string[]` (optional for back-compat with cached responses). New `resolveGapCardTradingMode(scannerSetupMode, modeBestFit)` in `frontend/lib/scanner-mode-resolution.ts` is the single source of truth for per-row routing — explicit scanner mode wins; `"both"` view honors classifier verdict; `"either"` or undefined falls back to swing. `openGapEvidence` in `scanner-page-client.tsx` now uses the new helper per row. Gap cards render a role-colored "Best evaluated as" pill between the catalyst block and the action button row, with reasoning chips below in muted text and an `InfoTip` explaining the routing rule context-aware (different copy for explicit-mode vs both-mode vs either-verdict). **Tests:** +13 backend (`tests/signals/test_gap_intelligence.py` "B30 Phase 4 — classify_mode_best_fit lock-ins" section) + +6 frontend (`frontend/tests/scanner-mode-resolution.test.ts` "resolveGapCardTradingMode" section), zero regressions. Backend baseline **1079 → 1093**, frontend baseline **336 → 342**. **All three B30 Severity-2 items now CLOSED.** Previous shipment — **Per-mode composite weight overrides — schema + resolver + wiring (B30 Phase 3 — Suggestion 4 audit)** — closes the Severity-2 audit item "Audit whether layer weights themselves should differ between engines — today both share `CompositeParameters`." Audit finding (no code change to confirm — verified via `Grep` of `params.composite\.` across the codebase): both `swing_composite_engine.py` and `real_composite_engine.py`, plus the legacy `swing_composite_handler` in `signals.py:301-340`, read the **same** `params.composite` block (tech 0.30 / news 0.20 / macro 0.15 / sector 0.15 / geo 0.10 / internals 0.10). The per-layer **inputs** already diverge by mode at the analyzer layer (swing reads 120h news / 14d macro / 168h geo / weekly sector / daily-bar tech with `mode="swing"`; day reads 8h news / 1d macro / 8h geo / daily sector / intraday tech with `mode="day"`), but the **blend weights** stamped on every `SignalRecord.parameter_version` are identical — a known asymmetry: macro + sector deserve more weight for swing (multi-day holds carry through Fed days and sector rotation), while technical / internals / news deserve more weight for day (intraday confirmation IS the day-engine's primary truth and fresh 8h news has not yet been absorbed). **Proposed rotation** (recorded here in this row with reasoning; NOT shipped as the default — operators rotate in Secrets Manager when ready to validate against historical outcomes): swing tech 0.28 / news 0.15 / macro 0.20 / sector 0.18 / geo 0.12 / internals 0.07 (sums to 1.00); day tech 0.32 / news 0.25 / macro 0.10 / sector 0.12 / geo 0.08 / internals 0.13 (sums to 1.00). The verdict thresholds (`bullish_threshold=+0.20` / `bearish_threshold=-0.20`) and confluence parameters were intentionally NOT in scope for this audit — those are higher-stakes product-decision dials (they shift what counts as Actionable) and ride a separate audit cycle. **Implementation is additive infrastructure only — zero production behavior change today.** Schema: `SignalParameters` gained two optional override fields `swing_composite: CompositeParameters | None = None` and `day_composite: CompositeParameters | None = None`, both defaulting to `None` so any Secrets Manager JSON without those keys (i.e. every existing secret today) leaves the per-mode blocks unset and the engine falls back to the shared `composite` block — the load-bearing back-compat invariant pinned by `test_resolve_composite_block_falls_back_to_shared_when_no_per_mode_block` and `test_build_engine_swing_and_day_identical_when_no_override`. Parser: `signal_parameters_from_dict` now parses `data["swing_composite"]` / `data["day_composite"]` via the new `_parse_optional_composite_block` helper (returns `None` for absent / null / non-dict values, returns a fully-coerced `CompositeParameters` for dict values, unknown keys ignored per the existing `_coerce_dataclass` contract for forward-compat). Resolver: new public `resolve_composite_block(params, mode)` in `stocvest/signals/composite_score.py` is the single source of truth for mode-aware composite parameter selection (mode `"swing"` → swing_composite if set else composite; mode `"day"` → day_composite if set else composite; mode `None` or any other value → composite). Unknown mode strings (`"options"` / `""` / `"SWING"`) fall through to shared defensively. Builder: `build_composite_score_engine_from_params(params, *, mode=None)` gained a keyword-only `mode` argument that threads through the resolver; `mode=None` preserves the legacy back-compat path verbatim so any caller that forgets the kwarg gets the previous behavior. Call-site wiring (3 surfaces): `swing_composite_engine.py` line 251 + 261 + 262 now declare `swing_composite = resolve_composite_block(params, mode="swing")` and use it for `min_available_layers` / `bullish_threshold` / `bearish_threshold`, and pass `mode="swing"` to the builder on line 251; `real_composite_engine.py` line 335 + 366 + 367 do the symmetric thing for `mode="day"`; `signals.py:339` (the legacy `swing_composite_handler` client-supplied-scores path) passes `mode="swing"` to the builder. The local variable `swing_composite` / `day_composite` is bound once per request so the engine and the threshold check are guaranteed to be derived from the same resolved block (closes the operator-rotates-secret-between-two-reads race as a bonus, same pattern as the D3 first slice). **Tests: +18 backend lock-ins, zero regressions.** 11 in `tests/signals/test_composite_score.py` (under "B30 Phase 3 — per-mode composite override blocks" header): resolver back-compat default, swing-only override + cross-mode isolation, day-only override + cross-mode isolation, both-overrides-coexist, unknown-mode-defensive-fallback (case-sensitive), build-engine-swing-and-day-identical-with-no-override (the production behavior guarantee), build-engine-swing-uses-swing-override-weights (all six weights + thresholds), build-engine-day-uses-day-override-weights, build-engine-swing-does-NOT-pick-up-day-override, build-engine-day-does-NOT-pick-up-swing-override, per-mode-overrides-actually-change-composite-score (wire-is-live end-to-end — rotating swing override moves swing engine's score while day engine stays anchored to shared baseline). 7 in `tests/config/test_parameter_store.py` (under "B30 Phase 3" header): legacy-secret-has-no-per-mode-blocks (back-compat invariant), with-swing-composite-block, with-day-composite-block, with-both-per-mode-blocks, per-mode-null-explicit, per-mode-unknown-keys-ignored (forward-compat), to_dict-round-trip-preserves-both-overrides. Backend baseline **1061 → 1079 passed (+18, zero regressions)**, frontend **336** unchanged (no frontend changes — backend infrastructure only). No Terraform / IAM / API contract / DynamoDB-schema changes — Secrets Manager `stocvest/signal-parameters` payload schema is forward-compatible (the new optional keys are additive). **Operator runbook for actually rotating weights when ready:** edit `Secrets Manager` `stocvest/signal-parameters`, add a `swing_composite` and/or `day_composite` object to the JSON payload with the proposed weights above, bump `version`, and `ParameterStore.invalidate_cache()` (or wait the 5-min TTL). Every new `SignalRecord` will then carry an honest `parameter_version` stamped from the engine that actually scored it; the D2 Phase 4 cross-version diff view will surface the rotation as a clean v→v+1 transition; the historical-validation aggregator can measure the impact of the per-mode weight change directly against the next 30/60/90 days of resolved outcomes. The remaining Severity-2 product decisions for B30 (gap-card behavior in "both" view → currently routes to swing mirroring `panelNewsTradingMode`; "Best evaluated as" tag on the gap card itself; backend `mode` echo on `/v1/scanner/gap-intelligence` metadata) are still deferred to user input. Prior 2026-05-12 entry was **Mode-resolution helpers extracted as a documented contract (B30 Phase 2)** — closes my own Severity-3 #8 audit gap from the B30 Phase 1 shipment ("lock-in tests for the call sites — the kinds of regressions that survive code review because the screen looks fine"). Pure refactor with zero production behavior change. The mode-resolution rules that were one-line ternaries buried inline inside `scanner-page-client.tsx` (`evidenceTradingMode = scannerSetupMode === "day" ? "day" : "swing"` and `groupTradingMode = group.key.startsWith("swing") ? "swing" : group.key.startsWith("day") ? "day" : evidenceTradingMode`) are extracted into a new module `frontend/lib/scanner-mode-resolution.ts` with two named, documented exports: `resolveEvidenceTradingMode(scannerSetupMode)` and `resolveSetupRowTradingMode(groupKey, fallback)`. The module carries a detailed file-level JSDoc explaining WHY each rule exists — both load-bearing invariants (`/composite/swing` vs `/composite/real` engine selection AND the chip-filter `filterChipsForMode` that fires on `body.mode === "swing"` inside `applySwingCompositeEnrichment`) and the lockstep contract with `panelNewsTradingMode` (news lookback + composite engine + chip filter must all be tagged the same on every Evidence-modal open). `scanner-page-client.tsx` now calls the helpers at both call sites — gap card uses `evidenceTradingMode = resolveEvidenceTradingMode(scannerSetupMode)`; setup card uses `groupTradingMode = resolveSetupRowTradingMode(group.key, evidenceTradingMode)`. Net effect: the rules are now discoverable for future surfaces (Day Desk inline evidence, future engines like Options, etc.) and any future refactor that changes the rules has a deterministic failing test rather than a silent behavior change. **Tests +12 in new `tests/scanner-mode-resolution.test.ts`:** `resolveEvidenceTradingMode` covers all three `ScannerSetupLoadMode` inputs (day/swing/both); a parametric lock-in proves the resolution matches the `panelNewsTradingMode` pattern (the load-bearing news-composite lockstep); `resolveSetupRowTradingMode` covers all four `SetupRenderGroup.key` values (swing / swing-only / day / day-only) under both swing and day fallback values to prove the per-row rule wins regardless of fallback; explicit assertions of the "day-group rows in scannerSetupMode === 'both' override the 'both → swing' fallback" invariant (the most consequential Severity-2 product decision encoded in Phase 1) and the symmetric swing-group-under-day-mode guard; defensive-fallback behavior for unknown group keys; case-sensitivity lock-in so `"Swing"` / `"DAY"` do NOT prefix-match (a future caller sending mixed-case keys produces a visible test failure rather than a silent fallback). Frontend baseline **324 → 336 passed (+12)**, backend **1061 / 3** unchanged (no Python touched). `npm run build`: Compiled successfully. `tsc --noEmit` zero new errors. No Terraform / IAM / API contract / DynamoDB-schema changes. The remaining Severity-2 product decisions for B30 (gap-card behavior in "both" view; "Best evaluated as" tag; backend `mode` echo on gap-intel; mode-specific layer weights audit) are still deferred to user input. Prior 2026-05-12 entry was **Evidence-card composite mode routing fix (B30 Phase 1)** — closes a cross-mode data-leakage bug surfaced by a user-prompted audit ("the gap cards and signal detail cards are looking exactly same for both swing and day trade. As the criteria and calculations are different for both, shouldn't we be showing different details ... give me your assessment and let me know what you find about swing and day trade cards"). The audit traced two findings: (a) the **Gap Intelligence card itself** is correctly mode-agnostic by product design — a 4% pre-market gap is a market event, not a trade plan, and the `/v1/scanner/gap-intelligence` handler does not take a mode parameter; identical content on the card itself is defensible. (b) the **Evidence-card composite enrichment**, however, was hard-coded to the day engine from three of four call sites — the **Scanner gap card "View Signal"**, the **Scanner setup card "View Signal"**, and the **Dashboard Swing Desk "View Evidence"** all called `enrichEvidenceWithRealComposite(base)` which only ever hits `/api/stocvest/signals/composite/real` → backend `/v1/signals/composite/real` → day engine, regardless of the row's actual trading mode. Only the **Signals page** branched correctly between `/composite/swing` and `/composite/real` based on the active `tradingMode`. So a user clicking the **View Signal** CTA on a swing setup row was silently reading day-engine output: VWAP / EMA9 / EMA20 / ORB chips and intraday lookback windows leaked into a swing-context modal, the chip-filter (`filterChipsForMode` on `body.mode === "swing"`) never fired because `body.mode` arrived as `"day"` from the wrong route, and the verdict math reflected day-tier news / macro / geo lookback windows + daily-cadence sector posture instead of the swing engine's 120h news / 14d macro / 168h geo / weekly sector posture. The two engines genuinely diverge on inputs (Daily bars + SMA50/200/MACD for swing vs 1-min bars + RSI/VWAP/EMA9/EMA20/ORB for day; different lookback windows for news/macro/geo; weekly vs daily sector posture; engine response tagged `body.mode = "swing"` vs `"day"`) but converge on `CompositeParameters` (layer weights 0.30/0.20/0.15/0.15/0.10/0.10 and verdict thresholds ±0.20) by design. **Fix — wiring (4 surfaces):** (1) `frontend/lib/signal-evidence.ts` introduces a parameterised helper `enrichEvidenceWithComposite(evidence, mode: "swing" \| "day")` — the `mode` argument is required so future call sites cannot omit it without a compile error; a pure `compositeRouteForMode(mode)` function selects between `/api/stocvest/signals/composite/swing` (swing engine) and `/api/stocvest/signals/composite/real` (day engine). The legacy `enrichEvidenceWithRealComposite` name is kept as a `@deprecated` thin alias that hard-codes `"day"` for any external consumers but is no longer called from any first-party surface in the repo — the deprecation comment names the regression this rename was intended to prevent. (2) `frontend/components/dashboard-redesign.tsx` Swing Desk row's "View Evidence" button now calls `enrichEvidenceWithComposite(base, "swing")` (the news fetch on the line above already correctly used `newsTradingMode: "swing"`, so the inconsistency where news was mode-correct and composite was not is also fixed). (3) `frontend/components/scanner-page-client.tsx` introduces a top-level `evidenceTradingMode = scannerSetupMode === "day" ? "day" : "swing"` mirroring the existing `panelNewsTradingMode` resolution so the news lookback window and the composite engine stay in lockstep across the surface; the **gap card** `openGapEvidence` callback uses this top-level mode (the scanner is swing-first in `scannerSetupMode === "both"`, matching the stacked-desks render order — reversible later if you want side-by-side dual reads as a product decision). (4) The **scanner setup card** "View Signal" button derives a per-row `groupTradingMode = group.key.startsWith("swing") ? "swing" : group.key.startsWith("day") ? "day" : evidenceTradingMode` from the `setupRenderGroups` group key, so in `scannerSetupMode === "both"` view a swing-group row opens the swing engine and a day-group row opens the day engine even though both groups render on the same screen; the `fetchSymbolNews` call inside that button was simultaneously upgraded from `newsTradingMode: panelNewsTradingMode` to `newsTradingMode: groupTradingMode` for the same lockstep reason. **Tests:** +9 frontend lock-ins in the new `tests/signal-evidence-composite-mode-routing.test.ts` covering swing-mode-routes-to-composite-swing, day-mode-routes-to-composite-real, never-hits-the-wrong-route negative assertions both directions, upper-cased-symbol + same-origin credentials payload contract, empty-symbol short-circuit (no fetch dispatched), fetch-throw error path, non-OK response error path, deprecated-alias-still-routes-to-day back-compat. Frontend baseline **315 → 324 passed (+9, 41 → 42 test files)**, backend **1061 passed / 3 skipped** unchanged (no Python touched). `tsc --noEmit` zero new errors. No Terraform / IAM / API contract / DynamoDB-schema changes. Severity 2 product decisions for B30 (next phases) — whether the `scannerSetupMode === "both"` "View Signal" should default to swing or render two engines side-by-side ("If you swing this" + "If you intraday this"), and whether the gap card itself should carry a "Best evaluated as: swing / intraday / either" tag — are deferred to user input; the current default-to-swing matches the existing `panelNewsTradingMode` resolution. Earlier 2026-05-12 entry was **Dashboard card visual uniformity (B29 Phase 2)** — closes a user-reported design-language inconsistency: dashboard cards (Phase 2b "rail-line" treatment with bright 2px role border + 4px top rail + 3px left stripe + 9% gradient + `borderRadius["2xl"]`) were visually loud relative to every other surface in the app. (1) Promoted Evidence-card-internal `elevatedCardStyle` to a top-level `cardSurfaceStyle(colors, tone)` export in `frontend/lib/design-system.ts` — single source of truth for the app-wide card visual contract (1px slate border + faint slate halo, with optional bullish/bearish/caution tone variants). (2) Rewrote `DashboardCard` to delegate its shell to the canonical helper at `borderRadius.xl` + `spacing[4]`; role identity now anchored by a single subtle 4px-wide role-tinted `borderLeft` accent (matches Scanner gap card precedent) + the existing labelled role pill. Retired `dashboard-card-role-top-rail` and `dashboard-card-role-stripe` DOM nodes; retired 2px solid bright role-accent border, 9% role-tinted gradient, `borderRadius["2xl"]`. (3) Softened `SubsectionCard` (Shared Context Sections B/C/D/E) — same canonical shell, dropped 1.5px slate-tinted border + gradient. (4) Softened Swing Desk Primary Read card in `dashboard-redesign.tsx` — same canonical shell at `borderRadius.lg`, dropped 1.5px violet `borderAccent` border + 6% role-tinted gradient + heavy halo (role identity is anchored by parent Swing Desk panel). (5) `roleAccents` import + `theme` destructure dropped from `dashboard-redesign.tsx` (no longer referenced). Tests in `dashboard-role-color-language.test.tsx` rewritten — pins on removed elements replaced by positive pins on the new canonical shell + 4px `borderLeft` + role pill + `borderRadius: 1rem`. `borderAccent` token retained in design-system but unused by components. Frontend `npm run test` → **315 passed** (was 314; +1 net new lock-in). Backend baseline unchanged at 1061 (no Python touched). All visual hardening additive in terms of contracts. Previous shipment — **Signal card explanation refinements (B29 Phase 1)** — closes four user-reported clarity / data-trust issues on the Evidence card surfaced by the GOOGL screenshot review. (1) Defensive **VWAP sanity guard** in `stocvest/signals/technical_analyzer.py` (`_vwap_is_plausible(vwap, price)` + `_VWAP_PRICE_DEVIATION_MAX_PCT = 0.30`) drops the value and logs a `vwap_anomaly_suppressed` WARNING (with `symbol`/`computed`/`price`/`bars`/`max_deviation_pct`) when computed intraday VWAP deviates >30% from the latest close — the UI falls back to "VWAP Forming" instead of rendering a misleading number like the reported `VWAP $28.77` on a stock trading near $389, and the WARNING gives us a triage trail for the upstream root cause. (2) **Verdict-tag reconciler** (`buildVerdictTagReconciler` in `frontend/lib/signal-evidence.ts`) renders a single calm italic sentence under the Confirming Signals chip group when the composite verdict polarity disagrees with the chip polarity (e.g. "Bullish inputs present, but overridden by VWAP conflict + Weak Volume + geopolitical drag." for a bearish-verdict-with-bullish-chips card). Symmetric, neutral-verdict-safe, strips parenthetical chip detail, auto-appends "geopolitical drag" when the badge is active, carries `data-testid="verdict-tag-reconciler"`. (3) **Geopolitical drag badge** — bearish-tinted `GEOPOLITICAL DRAG` pill in the Evidence card verdict hero row (next to NOT INVESTMENT ADVICE) when `evidence.layers` has a geopolitical layer with `geoHasLiveEvents === true` AND `exposureBand !== "low"`. Ties high-tier headline risk back to the composite verdict visually instead of leaving it stranded in the layer card; `data-testid="geopolitical-drag-badge"`. (4) **Weak/Strong Volume precision** — `_format_rel_vol(ratio)` helper in `stocvest/signals/confluence.py` replaces lossy `:.1f` with two-decimal precision (`0.42×`, `1.83×`), floors `(0, 0.05)` to `<0.05×` so labels never collapse to a misleading `0.0x avg`, and uses the multiplication sign `×` so the label is visually distinct from a stray letter. Test baselines: backend `pytest tests/ -q` → **1061 passed** (+6: 2 VWAP guard, 4 volume precision), frontend `npm run test` → **314 passed** (+13: 8 reconciler helper, 2 reconciler render, 3 geo badge). All hardening additive, no public contract changes. Previous shipment — **Assistant on-card CTA routing rule (B28 Phase 2c follow-up)** — closes a user-reported chatbot UX regression where the chatbot, when asked to explain a GOOGL Gap Intelligence card visible on the scanner, replied "click into the symbol on the Signals page" — routing the user off the page they were on instead of pointing at the inline **View Signal** button right there on the card. Prompt-side fix only, no code-side changes. Added an `ON-CARD CTAs — REFER USERS TO THE BUTTON ON THE CARD THEY'RE LOOKING AT` sub-section inside CONTEXTUAL MODE RULES in `stocvest/signals/assistant_prompts.py` with a per-surface CTA map (Scanner Gap card "View Signal", Scanner setup card "View Evidence" + "Open Signals", Dashboard Swing Desk row "View Evidence", Dashboard Day Desk row "Open Day Signals →", Performance "Open full ledger (Swing / Day) →", Signals page no referral needed — Evidence card already rendered) — each button label quoted verbatim. Section ends with a CRITICAL one-line restatement: "the next step is the on-card CTA — NOT a navigation instruction." Also added two new GOOD examples + one BAD example pinning the exact GOOGL scanner regression with its GOOD counterpart referencing the **View Signal** button. **Tests:** +4 backend lock-ins in `tests/signals/test_assistant_prompts.py` covering section + behavioral rule, per-surface CTA map verbatim labels, banned-shape regression lock-in + GOOD counterpart, Signals-page carve-out (no CTA referral needed). Backend **1051 → 1055 passed (+4, zero regressions)**, frontend **301/41 unchanged** (no frontend code touched). `tsc --noEmit` zero new errors. No Terraform / IAM / API contract / DynamoDB-schema changes. Earlier 2026-05-12 entry was **Sector behavioral reframe + Signal Validation Ledger moved off dashboard + sub-card highlighted borders (B28 Phase 2c)** — closes three layered user directives on top of Phase 2b. **(A) Ledger off the dashboard.** Per the user's one-sentence test ("a data element belongs in Shared Context if and only if it answers what kind of market environment are all traders operating in right now") tracked outcomes fail that test. `SignalValidationLedgerTertiarySurface` was removed from `dashboard-redesign.tsx` and its export was deleted from `shared-context-master-card.tsx`; the dashboard stack is now exactly `SharedContextMasterCard + Swing Desk + DayDeskPanel + EarningsCalendar` with no historical-outcomes surface anywhere. The full stratified ledger now lives on the logged-in `/dashboard/performance` page as a `data-testid="performance-validation-ledger-link"` panel (eyebrow "Validation ledger · stratified outcomes", "Open full ledger (Swing / Day) →" anchor, explicit Mode Separation reminder, tracked-outcomes-not-a-brokerage disclaimer); rendered only when `showHomeLink={false}` so the logged-out `/performance` mirror gating matches the Phase 3c-1 API gating. **(B) Sector behavioral reframe in Section C.** Per the user's directive ("If sector rotation explains why the market feels the way it does, it's Shared Context. If it suggests where to put risk, it isn't"), the per-sector chip row (XLK +1.5%, XLE -0.6%, …) — a ranked relative-strength strip explicitly banned by the directive — was deleted entirely. ETF symbol names no longer appear anywhere in the Shared Context master card. Section C now renders TWO behavioral categorical readouts: (1) **Rotation profile** ("Concentrated" / "Rotational" / "Mixed" / "Unknown") via the new `classifyRotationProfile` pure helper (Concentrated when spread ≥ 3% with ≤2 positives, Rotational when mixed-sign + spread ≥ 1.5%, Mixed for fallthrough); (2) **Participation** (existing Broad/Mixed/Narrow). Section C retitled "Sector Participation (Last ~5 Sessions)" — time-anchored and behavioral. `rotationProfilePlainLine` produces strictly descriptive sentences ("Narrow leadership — broad follow-through unlikely", "Capital rotating — no single sector controlling", "Mixed sector behavior — partial leadership") with 14 banned words enforced by tests (trending / leading / bullish / bearish / winners / losers / buy / sell / long / short / allocate / should / setup / trend intact). **(C) Highlighted borders.** SPY/QQQ/IWM tiles in Section A now carry direction-aware highlighted borders (green at 1.5px when pct5d > 0.1, red when < -0.1, neutral slate otherwise) via the new `indexTileBorderForDirection` helper; each tile stamps `data-tile-direction="up" | "down" | "flat" | "unknown"`. Sections B/C/D/E each render inside a `SubsectionCard` wrapper (1.5px slate-tinted border + linear-gradient surface + drop-shadow) with `data-subsection-card="B" | "C" | "D" | "E"` stamped on the DOM; Section A intentionally NOT wrapped to avoid card-in-card nesting. Swing Desk "Primary read" card bumped from 1px subtle to 1.5px bright violet (the swing role's `borderAccent`) + halo shadow + linear-gradient surface; testid `swing-desk-primary-read-card` for the lock-in test. **Tests:** frontend baseline **285 → 301 passed (+16)**, zero regressions. +9 in `tests/shared-context-derivations.test.ts` for `classifyRotationProfile` (closed-set guarantee + NaN-safe input + threshold lock-ins) + `rotationProfilePlainLine` (14-word anti-evaluative-language ban). +5 net new in `tests/dashboard-role-color-language.test.tsx`: Section C title contract, rotation-profile closed-set, direction-aware tile borders, sub-card B/C/D/E wrapper contract, Primary Read highlighted border. The existing Section-C-chip-row test was rewritten to require BOTH rotation-profile + participation categories AND assert chip row is gone AND no sector ETF names leak through; the existing tertiary-ledger test was rewritten as `dashboard_does_NOT_render_signal_validation_ledger_anymore` asserting absence of both the testid AND any `/dashboard/signal-validation` anchor in the dashboard root. +2 net new in `tests/performance-no-combined-headline.test.tsx` for the new ledger link panel (present logged-in, absent logged-out). Backend baseline **1051/3 unchanged**. No Terraform / IAM / API contract / DynamoDB-schema changes. Earlier 2026-05-11 entry was **Dashboard Shared Context master card + bright rail-line border treatment (B28 Phase 2b)** — closes the user-flagged regression that Phase 2 didn't go far enough: the dashboard still scattered shared infrastructure across FIVE separate cards (Short-Horizon Market State, Market Pulse, Sector Rotation, Upcoming Earnings, Signal Validation Ledger), and the role-color treatment used 1px subtle border tints the user explicitly called "very confusing". Phase 2b consolidates to the user's exact spec: ONE Shared Context master card (sub-sections A-E), ONE Swing Desk, ONE Day Desk, and a low-prominence tertiary Signal Validation Ledger surface. **(A) Bright rail-line borders:** `RoleAccent` gained `borderAccent` (electric cyan/violet/aqua per role × theme); `DashboardCard` renders a **2px solid** border + 4px top accent strip + halo glow whenever `role` is set — three orthogonal-edge anchors (top rail + left stripe + role pill) so role identity is visible in peripheral vision without reading text. **(B) Shared Context master card** in new `shared-context-master-card.tsx`: five sub-sections A (Recent Session Market State with neutral SVG sparklines for SPY/QQQ/IWM via new `index-sparkline.tsx`), B (Volatility category — VIX-derived, no ATR numbers), C (Participation/Breadth category — sector + index breadth, with the narrow-leadership-trap anti-leak), D (Risk/Event Horizon — time-based, macro warning + earnings count), E (Environment Summary — single strategy-agnostic sentence joining A+B+C+D, followed by timeframe-binding + why-this-matters guardrails). **(C)** `dashboard-redesign.tsx` lost ~250 lines: legacy 7fr/13fr grid with 7 cards collapsed to a clean stack of `<SharedContextMasterCard /> + <DashboardCard role="swing" /> + <DayDeskPanel /> + <SignalValidationLedgerTertiarySurface /> + <EarningsCalendar />`. Daily closes already fetched server-side surface as `WeeklyIndexRow.closes5d` for the sparklines — no new backend fetches. Tests: +32 (29 in `tests/shared-context-derivations.test.ts` covering the four classification helpers with threshold-boundary lock-ins and a cartesian-product anti-leak that EVERY summary combination is strategy-agnostic; +3 net new in `tests/dashboard-role-color-language.test.tsx` including the `dashboard_has_exactly_three_master_cards_at_top_hierarchy_level` structural invariant and the tertiary-ledger contract). Backend **1051/3 unchanged**, frontend **253 → 285 passed (+32, 40 → 41 files)**, zero regressions. `tsc --noEmit` zero new errors. No Terraform / IAM / API contract / DynamoDB-schema changes. Earlier 2026-05-11 entry was **Dashboard role-color language + Shared Context copy redesign (B28 Phase 2)** — closes two visual-design regressions the user surfaced after Phase 1 shipped: the Swing Desk and Shared Context cards rendered with the same theme-accent surface (had to read labels to disambiguate), and the Shared Context headline ("Constructive 5-session tape (background)") + parent title ("Weekly market context") both leaked swing strategy-coded language onto a surface that should describe what the market has been doing, not how a trader should trade it. **(A) Role-color language:** added a third orthogonal color channel (PRICE DIRECTION green/red + CAUTION amber + **DESK ROLE slate/indigo/teal**) via `roleAccents` in `lib/design-system.ts`. `DashboardCard` accepts an optional `role: "shared" | "swing" | "day"` prop that tints the surface, paints a 3px left-edge stripe, and renders an uppercase role pill with the verbatim labels `"SHARED CONTEXT"` / `"SWING · MULTI-DAY"` / `"DAY · INTRADAY"`. Role is `data-card-role={role}` on the wrapping `<article>` for tests. Applied across the dashboard: five `shared` cards (Short-Horizon Market State, Market pulse, Sector rotation, Upcoming earnings, Signal validation ledger) form a single slate-tinted family; one `swing` card (Swing Desk panel); one `day` card (Day Desk panel). Numbers + arrows keep their P/L green/red — color encodes role, not signal strength. **(B) Shared Context copy redesign in `weekly-market-context-widget.tsx`:** card retitled `"Weekly market context"` → `"Short-Horizon Market State (Last ~5 Sessions)"` (timeframe-anchored, cadence-neutral). Subtitle: `"Daily-close price behavior across major indices. Shared background input for all desks; not a trade signal."` Status-line buckets rewritten from evaluative to observational — `"Constructive 5-session tape"` → `"5-Session Outcome: Net upward price progress"` and parallel renames for the downward + mixed buckets. New verbatim guardrail constants exported (`SHORT_HORIZON_TIMEFRAME_LINE` with `"does not imply trade duration"` disclaimer + `SHORT_HORIZON_WHY_THIS_MATTERS`) rendered as footnote material so day traders cannot misread shared context as swing intent. Tests: +12 in new `tests/dashboard-role-color-language.test.tsx` covering role attributes, three-distinct-hue invariant, left-edge stripe, six banned evaluative words on the shared status line, threshold-boundary lock-ins for all three observational variants, verbatim guardrail clause + why-this-matters hint, subtitle's disclaimers. Backend **1051/3 unchanged**, frontend **241 → 253 passed (+12, 39 → 40 files)**, zero regressions. No backend / Terraform / IAM / API contract / DynamoDB-schema changes. Earlier 2026-05-11 entry — **Dashboard two-desk render + chatbot routing priority order (B28 Phase 0 + Phase 1)** — closes the Dashboard surface in the Mode Separation UI compliance shipment and codifies the LLM routing rules the dual-desk surface depends on. **Phase 0 (commit `6a25041`)** added the new `MODE RESOLUTION PRIORITY ORDER (CHATBOT ROUTING)` section to `ASSISTANT_SYSTEM_PROMPT` codifying P1 (screen context) → P2 (explicit mode language) → P3 (ambiguous + both desks visible → STRUCTURED DUAL ANSWER verbatim template) → NEVER infer mode from market behavior → CLARIFYING-QUESTION FALLBACK → DETERMINISTIC RESPONSE TO CROSS-MODE-SUBSTITUTION QUESTION — backend baseline 1038 → 1046 passed (+8). **Phase 1** flipped `dashboard-page-content.tsx` to `scannerSetupLoadMode: "both"` so the dashboard pulls intraday setups alongside swing setups; introduced a new `DayDeskPanel` component rendering the 5-element anatomy (header / posture pill / primary content / re-enable bullets / footer link); relabeled the existing Top Signals card as the Swing Desk with proper eyebrow + identity; flipped the Weekly market context eyebrow to "Shared context"; added day-side posture state machine to `lib/dashboard-posture.ts` (`dayDeskPostureKind` + `DAY_DESK_ACTIVE_SCORE_FLOOR=0.55`, plus day-vocabulary helpers `emptyDayPostureHeadline` / `emptyDayOneLiner` / `dayDeskSuppressionStatusLine` / `buildDayReenableBullets[Short]` that explicitly never reuse swing vocabulary); extended the assistant page-context publisher to emit `swing_desk_posture` + `day_desk_posture` + `day_setups_count` and OMIT `trading_mode` (so the LLM hits the P3 dual-answer path) — backend `serialize_page_context` allowlists the three new fields and validates posture values against closed sets so a malicious client cannot steer the LLM through freeform posture strings; `AssistantPageContext` type mirrors the fields with narrow unions. Tests: +5 backend lock-ins in `tests/signals/test_assistant_prompts.py` covering dual-desk page-context serialization, +20 frontend lock-ins (11 in new `tests/dashboard-day-helpers.test.ts` for the posture state machine + day-vocabulary anti-leak, 9 in new `tests/dashboard-two-desk.test.tsx` for the dashboard render contract including DOM order via `Node.DOCUMENT_POSITION_FOLLOWING`, +1 updated assertion in `tests/dashboard-swing-only.test.tsx`). Backend **1046 → 1051 passed** (+5; +13 over the 1038 long-term baseline). Frontend **221 → 241 passed** (+20, 37 → 39 test files). Zero regressions. **B28 marked Done.** **B29** (Portfolio + Journal `strategy_trading_mode` attribution) remains backend-blocked. Earlier 2026-05-11 entry — **Mode Separation UI compliance shipment — four-phase frontend cleanup (commits `e7c9fe0`, `122ddc8`, `0f31098`, `7bf3775`)**. This shipment closes the screen-level violations of the **Mode Separation safety perimeter** that was added to `ASSISTANT_SYSTEM_PROMPT` earlier the same day. The prompt declared the design rule ("Swing trading and Day trading are independent decision engines that share market context but never share decisions, readiness, validation, or activity permission"); this shipment makes the UI obey that rule structurally so the existing screens can no longer regress. **Phase A1 (`e7c9fe0`)** — Scanner `Both` mode now renders two mode-labelled sections (Swing setups + Day setups) instead of a single merged ranked list; per-row Open Signals deep link propagates `trading_mode`. **Phase A2 (`122ddc8`)** — `/dashboard/signals` is now URL-authoritative on `trading_mode`: `searchParams.trading_mode` beats localStorage on mount, mode changes mirror back to the URL via `replaceState`, signal_id strip preserves `trading_mode`, history fetch carries `mode=tradingMode` end-to-end so the past-signal-states list cannot mix engines on a single-engine view. Validation-ledger deep links now append `trading_mode=${tab}`. **Phase C (`0f31098`)** — Historical validation panel's `ModeFilter` type narrowed from `"all" | "swing" | "day"` to `"swing" | "day"`, `MODE_OPTIONS = ["swing", "day"]`, default `swing`, fetcher always sends a concrete mode, hero card title is mode-specific. **Phase D (`7bf3775`)** — Three combined-engine accuracy surfaces are now mode-segmented or removed: `performance-tracking-content.tsx` top metric row drops "Directional Accuracy" card (grid 4→3); `PublicValidationSection` drops "Overall accuracy (1d)" card (grid 3→2) leaving only Swing-track + Day-track cards (`overall` deliberately not destructured); `landing-performance-section.tsx` removes the "Overall directional accuracy (resolved 1d): X.X% · n=Y" fallback line on the LOGGED-OUT homepage and replaces with mode-agnostic volume line + handoff to Signal Tracking page. **Tests +13 total** (A1 +2, A2 +5, C +2, D +4) — frontend baseline **208 → 221 passed (+13, zero regressions)**, backend **1038/3** unchanged. `tsc --noEmit` zero new errors (24 pre-existing). **No Terraform, no IAM, no backend code changes, no new dependencies.** Tracked as **B27** below; deferred work tracked as **B28** (Dashboard Day Desk — blocked on product decision + day-engine posture API) and **B29** (Portfolio + Journal `strategy_trading_mode` attribution — blocked on backend schema change including resolution of the `trading_mode` naming collision between execution-mode `paper | live` and strategy-mode `swing | day`). **Prior commit (2026-05-11) — D3 first slice — composite-engine parameter wiring (backend-only)**. The Phase 4 cross-version diff view shipped yesterday (`bde04e4`) but the legacy `swing_composite_handler` in `stocvest/api/handlers/signals.py` was scoring with hardcoded `DEFAULT_BASE_WEIGHTS` while stamping `parameter_version = ParameterStore.get_parameters_sync().version` onto every recorded `SignalRecord` — so the metadata claimed "rules vN" but the engine actually used the hardcoded baseline (which differs from `CompositeParameters` defaults on 5 of 6 layers: news 0.18 vs 0.20, sector 0.12 vs 0.15, macro 0.16 vs 0.15, geopolitical 0.12 vs 0.10, internals 0.12 vs 0.10). This commit ships a single source of truth — `build_composite_score_engine_from_params(params)` in `stocvest/signals/composite_score.py` — and routes all three production composite-scoring callsites through it (`swing_composite_handler`, `swing_composite_engine.py`, `real_composite_engine.py`). The handler now reads `active_params = ParameterStore.get_parameters_sync()` once and uses the same snapshot for both the engine and the version stamp, eliminating the operator-rotates-secret-between-two-reads race as a bonus. `DEFAULT_BASE_WEIGHTS` remains as a documented test/no-args fallback so the existing engine-unit tests continue to pass unchanged. **Tests:** +6 backend lock-ins (4 in `tests/signals/test_composite_score.py` covering the helper itself — weights match params not legacy constants, thresholds threaded from params, end-to-end score change when params change, int→float coercion; 2 in `tests/api/test_signal_recorder.py` covering the handler integrity gap — stamped version matches the params actually used, identical payload + different versions produces different scores). Backend baseline **1015 → 1021 passed (+6, zero regressions)**, frontend **208** unchanged (no frontend code touched). `tsc --noEmit` zero new errors. **D3 first slice closes the data integrity contract for Phase 4's cross-version diff view** — operators can now actually rotate `stocvest/signal-parameters` in Secrets Manager and the resulting `SignalRecord` rows will carry an honest `parameter_version` that reflects the actual weights that produced them. **Still pending in D3:** wire the rest of the tunables (technical analyzer thresholds, news lookback hours, macro VIX bands, sector scoring tables, swing-technical SMA periods) through to their respective analyzer constructors; optional Claude prompts in Secrets Manager (P2). The composite weights were the most important slice because the validation framework is built around exactly those numbers. **Prior commit:** 2026-05-11 (**D2 Historical Signal Validation — Phase 4 cross-version diff view on the dashboard's Historical accuracy tab — `bde04e4`**; frontend-only slice, no backend / Terraform / IAM / new dependencies. D2 functionally complete end-to-end** through Phase 1 → 2 → 3a → 3b → 3c-1 → 3c-2 → 4. Prior 2026-05-11 commit: **D2 Phase 3c-1 public mirror on `/performance`** (Terraform applied). Backend handler `public_historical_validation_summary_handler` serves an unauthenticated `GET /v1/signals/historical-validation/public-summary` backed by the platform signal scope (`user_id=None` → `scope_key == "PUBLIC"`). The response is a **trimmed projection** (`{horizon, overall, by_mode, rows_examined}` + disclaimer) with every other stratification (decision / regime / pattern / readiness / direction) and `parameter_versions` deliberately omitted at the response layer — single-sourced enforcement of the assistant prompt's LOGGED-OUT golden rule. `symbol` query parameter rejected with 400 (homepage-safe contract). Defaults: horizon = `"1d"`, window = trailing 90 days when both `from` AND `to` are omitted (partial windows return 400). Invalid horizon returns 400. NaN accuracy serialization re-uses Phase 3a's `_bucket_stats_to_dict`. Terraform adds `authorizer = false` route on the existing signals Lambda integration (1 to add, 0 change, 0 destroy — applied). Frontend `fetchPublicHistoricalValidationSummary` in `lib/api/historical-validation.ts` fetches **directly from `publicApiBaseUrl()` (no BFF, no JWT)** — mirrors the existing `fetchPerformanceSummary` pattern; does NOT route 401s through `surfaceAuthErrorIfAny` (no session to refresh on a public endpoint); collapses to `null` on any non-200 / malformed / non-JSON. `PublicValidationSection` on `/performance` renders three cards (Overall 1d / Swing 1d / Day 1d) with monospace big-format accuracy values, "X correct of Y resolved" subtitles, the verbatim backend disclaimer, and a "Sign in to see the full stratified breakdown" CTA nudging visitors to the dashboard without overriding the logged-out boundary. Tests: +10 backend (response shape exactness, symbol-gate 400, cross-scope leak gate, default horizon = 1d, default trailing-90-day window, partial-window 400, mode filter passthrough preserving the Phase 1 vocabulary contract, invalid horizon 400, explicit window round-trip, empty store NaN → null without "NaN" token in raw JSON) + 6 frontend (URL not via BFF, anonymous fetch with no credentials, no auth-refresh on 401, query-string forwarding, malformed-body / non-JSON / 500 collapse). Backend baseline **984 → 994 passed (+10)**, frontend baseline **188 → 194 passed (+6)**. `tsc --noEmit` zero new errors. **Prior commit:** 2026-05-11 (**D2 Historical Signal Validation — Phase 3b UI surface** — `609a406`). Phase 3a (`0292989`) shipped the auth-gated `GET /v1/signals/historical-validation/summary` HTTP endpoint; this commit wires it to the dashboard with no backend changes, no Terraform changes, and no new dependencies. **BFF route** `frontend/app/api/stocvest/signals/historical-validation/summary/route.ts` is a thin `stocvestAuthedFetch` proxy (forwards the JWT cookie, passes the query string verbatim, returns the upstream JSON unchanged — backend already validates and emits calm 4xx envelopes). **Typed client** `frontend/lib/api/historical-validation.ts` exposes `fetchHistoricalValidationSummary({ horizon, from, to, mode?, symbol? })` and `fetchHistoricalValidationByVersion(...)` plus matching TypeScript interfaces (`BucketStats`, `HistoricalValidationSummary`, `HistoricalValidationResponse`, `HistoricalValidationByVersionResponse`). Runtime validation is defensive manual type-guards (matches the existing `public-signals.ts` / `journal.ts` style — no Zod dependency added). `null` accuracy from the wire is preserved verbatim in the parsed `BucketStats` so the UI can render an em-dash without ever showing a misleading 0%. Lowercase tickers are upcased before being sent. `mode` is forwarded only when it is `swing` or `day`. On 401 the client routes through the shared `surfaceAuthErrorIfAny` (so the sliding-session refresh flow from `c23ae2b` works uniformly here too) and returns `null` so the caller renders a calm "sign in" state. Malformed responses (missing `summary`, missing `__all__` aggregate bucket, non-JSON body) also collapse to `null` so the dashboard never crashes on backend drift. **Component** `frontend/components/historical-validation-panel.tsx` (new) renders the aggregate view: a filter row (Horizon = `1h` / `1d`, Window = `30` / `60` / `90` days, Mode = `all` / `swing` / `day`), an "Overall directional accuracy" hero card showing the big-format accuracy with an "X correct of Y resolved (neutrals excluded from the denominator)" subtitle and `rows_examined` + observed `parameter_versions` provenance, then a responsive 300px-minimum-column grid of six `StratificationCard`s (Decision state at entry, Macro regime at entry, Trading mode, Setup pattern, Trade Readiness, Direction). Each card lists its buckets in a stable `preferredOrder` per stratification with a friendly engine-vocabulary label translation (`risk_on` → "Risk-on", `swing_composite` → "Swing composite", `actionable` → "Actionable", etc.; unknown keys fall through to their raw form rather than being silently dropped) and a calm accent progress bar whose width is the bucket's accuracy in `[0, 1]`. The standing `HISTORICAL_VALIDATION_DISCLAIMER` is rendered verbatim from the backend response at the bottom of the panel — single-sourced wording across the assistant prompt, the API, and the UI. **Wiring on `/dashboard/signal-validation`:** the existing `SignalValidationPageClient` gets a new top-level view state (`view: "ledger" | "historical"`) and a pair of view-tab buttons ("Tracked outcomes (ledger)" / "Historical accuracy") rendered immediately under the page description; the entire existing ledger UI is wrapped in a `view === "ledger"` conditional and the new view renders `<HistoricalValidationPanel />`. The page skips the ledger fetch when the user is on the historical view (saves one network round-trip per mount in historical mode). The assistant page-context publisher only forwards `trading_mode` when the user is on the ledger view (the historical view has its own internal mode filter). **Tests:** +12 in `tests/historical-validation-api.test.ts` (happy path with query-string contents asserted, `mode` + lowercase `symbol` passthrough, `null` accuracy preserved end-to-end from the wire, 401 routes through `surfaceAuthErrorIfAny` and returns `null`, missing-`summary` malformed body returns `null`, non-JSON body returns `null`, `?by_version=true` per-version map parses with `__all__` combined aggregate, missing `__all__` returns `null`, `formatAccuracyPercent` renders "—" for `null`/NaN/Infinity and one-decimal percent for finite values, `buildTrailingWindow(30)` produces a 30-day delta). Frontend baseline **176 → 188 passed (+12)**, backend **984/3** unchanged. `tsc --noEmit` zero new errors. **No Terraform, no backend changes, no new dependencies** — Phase 3b is entirely frontend on top of the existing Phase 3a HTTP surface. **Prior commit:** 2026-05-11 (**D2 Historical Signal Validation — Phase 3a backend HTTP surface** — `0292989`). Phase 2 (`d8f8cb8`) shipped the service layer over `SignalHistory`; **this commit** wires it to API Gateway as `GET /v1/signals/historical-validation/summary`. New handler `historical_validation_summary_handler` in `stocvest/api/handlers/signals.py` requires `rc.user_id` (one tenant cannot read another tenant's tracked outcomes — locked in by `test_scope_isolates_users`) and parses `horizon` ∈ `{"1h","1d"}` (required), `from` / `to` as ISO-8601 datetimes (required; trailing-`Z` accepted; naive treated as UTC), optional `mode` ∈ `{"swing","day"}` (other values silently ignored — matches the existing `/v1/signals/me/history` convention), optional `symbol` (uppercased single ticker), and optional `by_version` ∈ `{"1","true","yes"}`. Returns either `{ summary: <HistoricalValidationSummary> }` (default) or `{ by_parameter_version: { "__all__": ..., "v1": ..., "v2": ... } }` (when `by_version=true`), plus echoed window parameters and the standing `HISTORICAL_VALIDATION_DISCLAIMER` from `stocvest/api/legal_copy.py` (newly added; single-sourced so the assistant prompt, this endpoint, the dashboard tab, the public mirror, and the cross-version diff cannot drift from the verbatim wording "Historical signal accuracy does not guarantee future results."). **JSON safety:** Phase 1's `BucketStats.accuracy` is `math.nan` when there are no resolved-non-neutral rows; Python's default `json.dumps` emits the literal `NaN` (invalid JSON, crashes browsers). The handler's `_bucket_stats_to_dict` converts NaN → `None` at serialization time — locked in by `test_empty_window_serializes_nan_accuracy_as_null` which asserts both the parsed body has `accuracy is None` AND the raw response string contains no `"NaN"` token. **Validation contract:** invalid / missing `horizon` → 400 with message naming the field; unparseable / missing `from` or `to` → 400; inverted window (`to <= from`) → 400 ("to must be strictly after from"); no auth → 401. **Route registration:** `signals_http_dispatch` in `signals.py` dispatches the new route; `infra/apigateway_6e.tf` adds `"GET /v1/signals/historical-validation/summary" = { module_key = "signals", authorizer = true }` (one new `aws_apigatewayv2_route.http` resource — Terraform `plan` showed exactly `1 to add, 0 to change, 0 to destroy`; applied 2026-05-11). **No new DynamoDB tables, no new Lambda functions, no new IAM** — the route lands on the existing `aws_lambda_function.api["signals"]` integration that already serves the rest of `/v1/signals/*`. **No BFF route or UI in this commit** — those are Phase 3b (next commit; new tab on `/dashboard/signal-validation` rendering all six stratifications via `client/lib/api/historical-validation.ts` + Recharts on the existing surface). **Tests:** +14 in `tests/api/handlers/test_signals_historical_validation.py` covering auth requirement, missing/invalid `horizon`, missing/invalid `from`, inverted window 400, trailing-Z ISO acceptance, happy-path 2/3 directional accuracy with disclaimer + `parameter_versions = ["v1"]`, empty-window NaN→null JSON serialization, per-user scope isolation, `mode=day` filter passthrough, lowercase `symbol=msft` uppercased + filtered, horizon `1d` routing, and the `by_version=true` per-version map shape with the synthetic `__all__` bucket. Backend baseline **970 → 984 passed (+14)**, frontend **176** (unchanged — no UI yet). **Prior commit:** 2026-05-11 (**D2 Historical Signal Validation — Phase 2 service layer** — `d8f8cb8`). Phase 1 (`7d0faf4`) shipped a pure deterministic aggregator over `SignalRecord` rows; **this commit** lands the thin seam between that aggregator and the live `SignalHistory` store so Phase 3 (HTTP route + UI tab) has a single testable surface to call. New module `stocvest/api/services/historical_validation_service.py` exposes a `HistoricalValidationService` built on a narrow `SignalHistoryReader` Protocol — both `InMemorySignalRecorder` and `DynamoDBSignalRecorder` in `stocvest/api/services/signal_recorder.py` already satisfy it via their existing `get_signal_history(user_id, symbol, days, limit, mode, ledger_qualified_only)` signature, so production wiring is one constructor call (`HistoricalValidationService(get_signal_recorder())`) and tests use a list-backed `_StubStore` double with no boto3. The service takes an absolute `[from_at, to_at)` window — **lower bound inclusive, upper bound exclusive — same semantics as the underlying GSI `scope_generated_at` predicate `generated_at >= cutoff_iso`** so callers can pass `from_at = midnight ET today, to_at = midnight ET tomorrow` and get "today's signals" with no off-by-one — translates it into the recorder's trailing-N-day convention (`days_back = ceil((now - from_at)/1d) + 1`, clamped at `MAX_LOOKBACK_DAYS = 366` so a buggy "last 10 years" call cannot drain the GSI), then post-filters to the requested window. `MAX_ROWS_PER_QUERY = 1000` is a deliberate guardrail — typical per-user windows are a few hundred signals over 30 days; cursor-based pagination for admin cross-user analytics is a Phase 3+ concern. Two methods: `summarize(user_id, from_at, to_at, horizon, mode=None, symbol=None) → HistoricalValidationSummary` returns the Phase 1 summary for the window directly, and `summarize_by_parameter_version(...)` groups the same window by `parameter_version` and returns `{"__all__": <combined>, "v1": <...>, "v2": <...>, "unknown": <...>}` so Phase 3 can render a "v1 vs v2 vs all versions" diff without recomputing — rows with `parameter_version is None` or empty/whitespace land in `UNKNOWN_VERSION_KEY = "unknown"` so legacy rows (emitted before that column shipped) are never silently dropped, matching the Phase 1 aggregator's "unknown" / "other" overflow convention. Inverted / zero-width windows (`to_at <= from_at`) short-circuit to an empty summary without touching the store. `user_id` / `symbol` / `mode` pass straight through to the store unchanged — **the store is the auth perimeter and the service does not silently change scope.** Naive `generated_at` rows are normalized to UTC before comparison so legacy DynamoDB hydrates do not crash. **No HTTP route, no BFF route, no UI in this commit** — those are Phase 3. **No DynamoDB schema changes** — the existing GSI `scope_generated_at` already supports the date-range query we need. **No Terraform changes.** **Tests:** +14 new in `tests/api/services/test_historical_validation_service.py` covering empty-store NaN, inverted-bounds short-circuit, equal-bounds short-circuit, happy-path 2-correct + 1-incorrect + 1-neutral → accuracy = 2/3 (neutrals excluded from denominator), **inclusive-lower / exclusive-upper window edge cases** (rows at `from_at` kept, rows at `to_at` dropped, before/after/inside boundary rows handled correctly), naive-`generated_at` UTC normalization, `user_id`/`symbol`/`mode` passthrough, `days_back` computed to cover `from_at`, `MAX_LOOKBACK_DAYS` clamp, horizon routing (a row with `outcome_1h` populated but `outcome_1d` pending counts at 1h, returns NaN at 1d, still appears in `rows_examined`), `__all__` combined aggregate alongside per-version buckets (v1=2/2, v2=1c+1i, all=3/4 = 75%, `parameter_versions = ("v1", "v2")`), `unknown` bucketing for missing/blank `parameter_version`, empty window producing only an `__all__` bucket with NaN accuracy, and window-filter-before-version-split (out-of-window v1 rows must not inflate the v1 bucket). Backend baseline **956 → 970 passed (+14)**, frontend **176 passed** (unchanged — no UI in Phase 2). **Prior commit:** 2026-05-11 (**sliding-session refresh** — Cognito `REFRESH_TOKEN_AUTH` plumbing — `c23ae2b`). The user reported a regression: "I am actively working on the app and I got the message that my session has expired. How can that happen. it should happen only on inactivity." The audit traced the root cause to a three-part architectural flaw — `infra/cognito.tf` never overrode Cognito's default `id_token_validity` (60 min), `frontend/lib/auth/cognito.ts:signIn()` captured only the `IdToken` from the Cognito response and silently discarded the `RefreshToken`, and `SessionExpiryWatcher` scheduled a hard `setTimeout(() => markSessionExpired("expired"), exp)` at the JWT's `exp` claim regardless of activity. The fix introduces a proper sliding-session contract: a continuously-active user with a valid Cognito refresh token stays signed in indefinitely (default 30-day refresh-token lifetime), while a truly-inactive user is signed out only when the refresh token expires or is revoked. Implementation lands across six surfaces — (1) server-side capture of `RefreshToken` in `cognito.ts:signIn()` + `respondToNewPasswordChallenge()`, plus a new `refreshIdToken(refreshToken)` that calls Cognito with `AuthFlow: REFRESH_TOKEN_AUTH`; (2) `session-cookies.ts` now writes a third httpOnly cookie `stocvest_refresh_token` (30-day `maxAge`) alongside the existing auth + mirror cookies, and `clearSessionTokenCookies()` clears all three; new `lib/auth/refresh-token-cookie.ts` for server-only reads; login + new-password actions thread the refresh token through; (3) new `POST /api/auth/refresh` BFF route — reads the refresh cookie, calls Cognito, rewrites ID + mirror cookies, returns `200 { exp }` on success or clears all cookies + returns `401` on failure; (4) new client `lib/auth/refresh-session.ts` with single-flight coalescing (parallel callers all await the same in-flight POST so we never trigger a refresh storm) and a 5-second post-failure cooldown (so a hard-expired refresh token doesn't get hammered); (5) `SessionExpiryWatcher` rebuilt to schedule a refresh ~2 min before `exp` instead of firing the expired event, re-evaluates on `visibilitychange → visible` + `focus`, only marks expired if the refresh call returns `false`; (6) `browserApiFetch` now does refresh-and-retry on 401 (one retry with the freshly-rewritten mirror cookie); only marks expired if the refresh fails OR the retry also returns 401. The shared `surfaceAuthErrorIfAny` (used by six fetch helpers that don't go through `browserApiFetch`) is now async and attempts a refresh first, marking expired only on refresh failure. **403 is unchanged** — STOCVEST uses 403 for product-rule denials (PDT, margin, paid-tier gating) where the user IS authenticated, and those must never trigger refresh-or-expire. **No Terraform changes** — `ALLOW_REFRESH_TOKEN_AUTH` is already in both `aws_cognito_user_pool_client.frontend` and `.authorizer` `explicit_auth_flows`, and the 30-day refresh-token validity is Cognito's default. **No backend changes** — the API Gateway authorizer + Lambda code are unchanged. **Tests:** +15 new across three lock-in files — `tests/refresh-session.test.ts` (5), `tests/browser-api-fetch-refresh.test.ts` (5), `tests/surface-auth-error.test.ts` (5). Frontend baseline **161 → 176 passed** (+15); backend **956 / 3** unchanged; `tsc --noEmit` zero new errors (24 pre-existing errors in the same four test files are unchanged). **Prior commit:** 2026-05-11 (D2 **Historical signal validation** — Phase 1 pure-aggregation module — `7d0faf4`). The user explicitly renamed D2 from "Backtesting" to **Historical Signal Validation** so the surface matches the assistant's verbatim disclosure phrase ("Historical signal accuracy does not guarantee future results") and the existing `/dashboard/signal-validation` page. The new framing also de-risks the legal posture: validation **measures** real emitted signals — it does not generate synthetic ones — and `BucketStats` is restricted to directional accuracy (no win-rate / no expectancy / no dollar P&L, ever). Phase 1 lands `stocvest/signals/historical_validation.py` as a pure deterministic aggregator over `SignalRecord` rows that `signal_recorder.resolve_signals` already populates, so there is zero new I/O and zero new infrastructure in this commit. Phase 2 (next commit, planned) adds a thin service layer that slices `SignalHistory` by date range + `parameter_version` and feeds the aggregator. Phase 3 adds the API + UI surface (new tab on `/dashboard/signal-validation` + optional public mirror on `/performance` for the overall + by-mode bucket only). Phase 4 adds cross-version diffs (rules-bundle A vs. B). Tests: backend baseline **942 → 956 passed (+14)** with the new `tests/signals/test_historical_validation.py` locking in the contract (NaN accuracy on empty / all-neutral strata, neutrals excluded from the denominator, horizon-routing, six stratifications, parameter-version provenance, frozen-dataclass immutability). Frontend baseline **161 passed** (unchanged — no UI in Phase 1). No Terraform changes — no new tables, no new routes in this commit. **Prior commit:** 2026-05-11 (B26 STOCVEST Assistant — in-memory-only conversation state — `1e0e4a6`). **This commit** closes two user-reported bugs that both stemmed from the `sessionStorage`-backed persistence layer: (1) **refresh kept showing old conversation** — by design `sessionStorage` survives `F5`, but the user expected a clean surface after a refresh; (2) **homepage messages persisted after login** — when the user signed in *in place* (auth flag flips without the URL changing, e.g. a sign-in modal on `/`), the hydrate-from-`sessionStorage` effect was clobbering `prevAuthRef.current` (it ran first and set the ref to the *new* `isAuthenticated` value); the auth-change reset effect then ran second, found `prevAuthRef === isAuthenticated`, and early-returned without clearing the conversation. The simplest, safest fix is to remove the persistence layer entirely so no stale state can survive any kind of transition. **Implementation:** `frontend/components/assistant/stocvest-assistant.tsx` drops the `PersistedState` interface, the `readPersistedState`/`writePersistedState`/`removePersistedState` helpers, the hydrate-on-mount `useEffect`, and the persist-on-change `useEffect`. The component now has a one-shot mount effect that opportunistically wipes legacy `sessionStorage` data for existing tabs that still have the old `stocvest_assistant_state_v1` key cached, plus three reset effects that already existed but now own `prevAuthRef` exclusively — the pathname-change reset, the auth-flip reset (this is the one that was previously broken), and the session-expiry / explicit-clear reset. `prevAuthRef` is now updated by only ONE effect, so the auth-flip detection is reliable. `frontend/lib/assistant/session-reset.ts` keeps `clearAssistantSession()` and the `stocvest:assistant-reset` event — both fire defensively against any legacy persisted data and against any future re-introduction of persistence, and the logout buttons in `sidebar.tsx` + `mobile-nav-drawer.tsx` continue to call `clearAssistantSession()` before the server-action redirect. **Behavior contract:** a fresh conversation starts on every (a) hard refresh / page load, (b) cross-surface navigation (`/dashboard/signals/layers` → `/`, etc.), (c) login (anonymous → authenticated), (d) logout (authenticated → anonymous), (e) session expiry (`subscribeSessionExpired`), (f) explicit "Sign out" click. Within a single page view, the conversation accumulates in React state as expected. **No backend changes**, no Terraform changes — the BFF endpoints and Lambda code are unchanged. **Tests:** backend baseline **942 passed / 3 skipped** (unchanged — the bug was frontend-only); frontend baseline **161 passed** (unchanged — there are no existing assistant component tests, and the changes are pure refactors to existing effect logic that no test exercised); `tsc --noEmit` zero new errors (the 24 pre-existing errors in `tests/order-confirmation-modal.test.tsx`, `tests/pdt-api.test.ts`, `tests/signal-evidence.test.ts`, `tests/signal-evidence-layers.test.ts` are unchanged from prior baselines). **Earlier today (`cedb365`)** added the two-context safety perimeter: **This commit** layers a top-level safety perimeter on top of the explanatory-voice prompt that landed earlier in the day (`72fe502`). The user asked for stronger separation between LOGGED-OUT (homepage) and LOGGED-IN behavior, because the existing PUBLIC MODE rules said "refuse trade recommendations" but did not explicitly forbid the LLM from inventing an Evidence card / Trade Readiness / blocking-layer narrative for a specific stock on the homepage — i.e. a logged-out visitor could ask "what does STOCVEST think about AAPL?" and the LLM might still narrate a fake decision rather than refuse. The new prompt adds (a) a `CRITICAL CONTEXT AWARENESS RULE` section right after the three-mode declaration that splits context into LOGGED-OUT (`session_mode=public`) vs LOGGED-IN (`session_mode=authenticated`) and ties them explicitly to the `session_mode` field in the page-context block; (b) the LOGGED-OUT golden rule "explain the FRAMEWORK, not the DECISION" verbatim; (c) an expanded `PUBLIC MODE RULES (LOGGED-OUT / HOMEPAGE)` block with an explicit per-symbol MUST-NOT list — no evaluating or discussing any specific stock by ticker (with AAPL / TSLA / NVDA / MSFT as concrete examples so the LLM pattern-matches any ticker), no stating or implying a current Decision for any symbol, no Evidence card reference for a specific stock, no Trade Readiness score for a specific symbol, no naming a specific layer as "currently blocking" a specific ticker, no sounding like a live demo of unavailable features; (d) the `HOMEPAGE-SAFE REFUSAL TEMPLATE` verbatim — "I can't assess individual stocks here, and I don't give buy or sell answers. What I can explain is how STOCVEST decides whether trading conditions are aligned once a symbol is evaluated inside the platform." — so the LLM falls back to it for any per-symbol question on the homepage instead of improvising; (e) an addition to TONE & STYLE that refusals must briefly explain WHY the boundary exists ("STOCVEST's role is to explain whether conditions are aligned, not to outsource your judgment"), because refusal-with-reason builds trust where refusal-without-reason feels dismissive. **No frontend changes** — the new guardrails are entirely prompt-side and the existing `session_mode=public` routing on the BFF (`frontend/app/api/stocvest/public/assistant/chat/route.ts`) and the existing root-layout `isAuthenticated` prop already deliver the right context flag. **Tests:** +7 new in `tests/signals/test_assistant_prompts.py` (`test_prompt_declares_critical_context_awareness_two_context_perimeter`, `test_prompt_carries_logged_out_golden_rule`, `test_prompt_explicitly_bans_per_symbol_evaluation_when_logged_out`, `test_prompt_carries_homepage_safe_refusal_template_verbatim`, `test_prompt_lists_concrete_ticker_examples_in_public_mode_ban`, `test_prompt_carries_explain_why_boundary_exists_in_tone`, `test_prompt_keeps_public_mode_rules_label_for_section_routing`); plus one tightened assertion in `tests/signals/test_assistant_chat_service.py::test_reply_public_calls_claude_with_public_mode_marker` (the assertion now isolates the appended `=== PAGE CONTEXT ===` tail rather than scanning the whole prompt, because the static instruction block now mentions `session_mode=authenticated` as descriptive text inside the CRITICAL CONTEXT AWARENESS RULE — that mention is a guardrail, not a routing signal). Backend baseline **935 → 942 passed (+7)**, frontend **161 passed** (unchanged), `tsc --noEmit` zero new errors. **Prior commit (`72fe502`, earlier 2026-05-11)** rewrote `ASSISTANT_SYSTEM_PROMPT` from a feature-list contract into a product-anchored explanatory-voice contract, addressing the user request to "make our application more powerful" by giving the assistant a much sharper trust-building voice. The new prompt is organized around PRIMARY GOAL → CORE PRODUCT PHILOSOPHY ("Silence is an intentional system state") → ANALYSIS LAYERS (six real product layer names verified against the codebase: Technical / News / Macro / Sector / Geopolitical / Market Internals) → MARKET REGIME / SECTOR CONFIRMATION / MARKET PULSE / INTERNALS & VOLATILITY / TRADE READINESS / DECISION STATES (verbatim lines from `lib/signal-evidence/trade-decision.ts`) / ALIGNMENT & CONFIDENCE → SUPPRESSION & GATING LOGIC (with the dashboard's exact empty-state copy) → REGIME TRANSITIONS (no-forecast) → BACKTESTING & VALIDATION (route to /performance + /dashboard/signal-validation with the standing disclaimer) → USER INTERACTION RULES (MAY / MUST NOT) → PAYWALL AWARENESS (swing_pro / swing_day_pro / active beta) → TONE & STYLE → END GOAL & FOUNDATIONAL PRINCIPLE. Before writing the new prompt I ran a thorough product audit and discovered five concepts in the user's initial proposal that would have contradicted shipped UI if adopted verbatim — "VIC / Volatility Control" with Supportive/Neutral/Hostile states (does not exist; VIX is inside Internals), Macro Pulse "Active / Weak / Unavailable" (real states: Unavailable / Elevated / Upcoming / Known and absorbed), "Symbol Readiness 0.0-1.0" (real product is Trade Readiness 0-100), "System Confidence: High/Medium/Low" (does not exist; product surfaces Layer alignment buckets + Trade Readiness), and "Sector State: Leading / Mixed / Fragmented" (real chips: Confirming / Non-confirming / Mixed; tape framing Risk-on / Defensive / Mixed / Narrow). The user followed up with a refined product-accurate prompt which I then merged with the existing operational guardrails — banned self-limitation phrases, banned BAD/GOOD response shapes block, three-mode structure, contextual-mode examples, foundational principle. The five invented concepts are now in an explicit ban list inside GENERAL BEHAVIOR RULES so the LLM is told by name not to invent them. **Tests:** +19 new in `tests/signals/test_assistant_prompts.py` locking in PRIMARY GOAL + CORE PRODUCT PHILOSOPHY, the six-layer enumeration, the verbatim Decision lines, the 0-100 Trade Readiness scale (and the negative assertion that 0.0-1.0 is explicitly forbidden), the real Macro pulse states, the real sector chip labels, the explicit ban-list, the suppression-copy anchors, the /performance + /dashboard/signal-validation routing + standing disclaimer, the PAYWALL AWARENESS section, the banned self-limitation phrases, the banned response shapes block, the three-mode structure, the foundational principle, the calm-refusal pattern, the SUPPRESSION & GATING LOGIC + REGIME TRANSITIONS + BACKTESTING & VALIDATION sections, and the USER INTERACTION MAY/MUST NOT lists. Backend baseline **916 → 935 passed (+19)**, frontend **161 passed** (unchanged), `tsc --noEmit` zero new errors. No frontend changes needed — the existing page-context publishers already carry every field the upgraded prompt references. **Prior polish (`212b4d9`, earlier 2026-05-11)** fixed the user-reported regression where the anonymous home page chatbot still showed prior logged-in TTD-on-Signals conversation turns. Root cause was two-fold: (a) the page-change reset was keyed off `pageContext.page`, which is `null` on marketing routes that never publish context, so navigating from `dashboard/signals/layers` to `/` never fired the wipe; (b) the hydrate force-clear guarded on `persisted.isAuthenticated !== null && persisted.isAuthenticated !== isAuthenticated`, which let legacy persisted data (where the field was missing entirely) rehydrate freely. Fix: rebuild reset signal on `usePathname()` so every URL change wipes history; record `lastPathname` in persisted state and force-clear on hydrate when `persistedAuth !== isAuthenticated` (including legacy missing-field case) **or** `persistedPath !== pathname`. Empty state on the marketing surface is also a welcoming "Welcome to STOCVEST → six-layer thesis → what you can ask" three-paragraph pitch. **Earlier commit (`018ea89`)** fixed the user-reported regression: after landing `018ea89`, the **anonymous home page chatbot was still showing the previous logged-in conversation** (screenshot showed TTD-on-Signals turns persisting on `/`). Root cause was two-fold: (a) the page-change reset was keyed off `pageContext.page`, which is `null` on marketing routes that never publish context, so navigating from `dashboard/signals/layers` to `/` never fired the wipe; (b) the hydrate force-clear guarded on `persisted.isAuthenticated !== null && persisted.isAuthenticated !== isAuthenticated`, which let legacy persisted data (where the field was missing entirely) rehydrate freely. Fix: rebuild reset signal on `usePathname()` so every URL change wipes history; record `lastPathname` in persisted state and force-clear on hydrate when `persistedAuth !== isAuthenticated` (including legacy missing-field case) **or** `persistedPath !== pathname`; also strengthen the system prompt — promote the "never describe own access to data / I don't have / I can't see / would need to see" rule to a top-level General Behavior bullet and add an explicit BAD/GOOD pair drawn from the exact failure mode shown in the screenshot. Empty state on the marketing surface is also rebuilt as a welcoming "Welcome to STOCVEST → six-layer thesis → what you can ask" three-paragraph pitch instead of a one-line "ask me anything". Backend baseline **916 passed / 3 skipped** (unchanged — existing assistant tests still cover the prompt + service surface). Frontend baseline **161 passed** (unchanged). Original `018ea89` notes (preserved): four user-feedback fixes — (1) **cross-page conversation isolation** — the assistant tracks the page identifier the persisted conversation belongs to and wipes history when the user navigates to a different page so a TTD answer on Signals can no longer leak into a question on Dashboard; (2) **stronger visual distinction** between user and assistant turns — user lives in an accent-tinted panel with a solid accent-block "YOU" chip and right-aligned text, STOCVEST lives in a surface-muted panel with a quiet tone-tinted label and left-aligned text; the asymmetry plus contrasting backgrounds make the two roles unmistakable even in monochrome; (3) **session reset** — sign-out buttons in `sidebar.tsx` + `mobile-nav-drawer.tsx` call `clearAssistantSession()` before the server action and the assistant subscribes (1) **cross-page conversation isolation** — the assistant tracks the page identifier the persisted conversation belongs to and wipes history when the user navigates to a different page so a TTD answer on Signals can no longer leak into a question on Dashboard; (2) **stronger visual distinction** between user and assistant turns — user lives in an accent-tinted panel with a solid accent-block "YOU" chip and right-aligned text, STOCVEST lives in a surface-muted panel with a quiet tone-tinted label and left-aligned text; the asymmetry plus contrasting backgrounds make the two roles unmistakable even in monochrome; (3) **session reset** — sign-out buttons in `sidebar.tsx` + `mobile-nav-drawer.tsx` call `clearAssistantSession()` before the server action and the assistant subscribes to `subscribeSessionExpired`, so logout / token expiry / any 401 wipes the conversation + composer + sessionStorage; (4) **homepage chatbot for anonymous visitors** — `app/layout.tsx` no longer gates the assistant on `getServerSession()`; instead `isAuthenticated` is passed as a prop and selects the endpoint (authenticated → existing JWT route, anonymous → new `POST /v1/public/assistant/chat` with `authorizer = false`, PUBLIC MODE prompt activated via `session_mode=public`). Terraform applied for the public route (route id `69hco4j`, 1 added). Backend baseline 906 → **916** (+10: 4 in `test_signals_assistant_chat.py` covering the public handler, 4 in new `test_assistant_chat_service.py` covering `reply_public`, 2 in `test_assistant_prompts.py` covering `session_mode` markers). Frontend baseline **161** (unchanged this round).)

---

## Conventions

- **Done** items live in git + `CONTEXT.md` §1–§2; they are **not** re-listed here unless needed as dependency context.
- Update this file when you **add**, **reprioritize**, or **finish** a backlog theme.
- IDs (B1, P3, …) are stable handles for PRs/commits; not sprints.
- **`CONTEXT.md` §13** defines end-of-session steps: completed rows → status **DONE** plus `Done YYYY-MM-DD · commit: <hash>`; in-progress → **In progress**; **never delete a row**.

---

## Near-term operations

Tracked in **`CONTEXT.md` §3** only (Terraform apply, GitHub/AWS/Vercel secrets, broker production runtime, CI tweaks, attorney review). **Do not duplicate §3 here.**

---

## B — Product / UX

| ID | Theme | Status | Notes |
|----|--------|--------|--------|
| B1 | **Journal automation** | Done 2026-05-02 · commit: 3602db38c839209d3bae3d67fa2d50eee0f93022 | Auto-capture on fill, auto-close on exit, optional `signal_id` / `pattern` / `confluence_score` / `signal_strength` / `signal_direction` on `POST /v1/orders/submit`, `GET /v1/journal/analytics`, entry GET/PATCH, journal page wired to API + scanner → portfolio order prefill + confirmation modal signal block. |
| B2 | **Onboarding** | Done 2026-05-02 (feat(B2) commit on `main`; `git log --oneline -5` for hash) | **Legal:** mandatory full-screen acknowledgment (5 checkboxes + ToS link) before any dashboard interaction; `PATCH /v1/users/me` with `legal_acknowledged_version` **1.0**. **Onboarding:** optional wizard after legal ack; “Remind me later” hides for session until next login; `PATCH` sets `onboarding_completed`. **Backend:** `UserProfile` fields + Dynamo merge via `user_profile_store.put_profile`. **Routes:** `GET`/`PATCH /v1/users/me` (brokers Lambda), API Gateway + `dev_local_api` PATCH. **Frontend:** `app/dashboard/layout.tsx`, `DashboardComplianceClient`, `legal-acknowledgment-modal`, `onboarding-wizard-modal`, BFF `/api/stocvest/users/me`. |
| B3 | **Alerts delivery** | Done 2026-05-03 (B3/B4 on `main`; `git log -1 --oneline` for hash) | **Phase 1 shipped:** SES email via `stocvest/services/email_service.py`, prefs + history in `alert_store` (`preferences` + `hist#` SK), `AlertTriggerService`, non-blocking hooks on swing composite record + order validate (2 trades) / submit PDT 403; `GET`/`PATCH /v1/alerts/preferences`, `GET /v1/alerts/history`; settings Alert Preferences UI; Lambda IAM SES send; `STOCVEST_EMAIL_SENDER` / `STOCVEST_PUBLIC_APP_URL` in `lambda_common_env`; `docs/DEPLOYMENT.md`. **Phase 2 / still pending:** push, webhooks, **SMS (B13)** when phone collection exists. |
| B4 | **Watchlists** | Done 2026-05-03 (B3/B4 on `main`; `git log -1 --oneline` for hash) | **Shipped:** `watchlist_store` + API under brokers Lambda + API Gateway routes; `get_scan_symbols` / scanner defers Dynamo until Polygon aggregate 401/403 fallback; dashboard `/dashboard/watchlists`, sidebar **Watchlists**, BFF routes, **Add to watchlist** on signals + gap cards; limits 5 lists / 50 symbols. |
| B14 | **Watchlist monitoring (scanner + scheduled + alerts)** | Done 2026-05-03 (watchlist monitoring on `main`; `git log --oneline -3` for hash) | **Shipped:** Dashboard **`/dashboard/scanner`** — `fetchDefaultWatchlistSymbols` (`GET /v1/watchlists/default/symbols`) in `Promise.all` with `fetchPdtStatus`, then `fetchScannerOverview(pdt, watchlistSymbols)`; `lib/api/watchlists.ts`. **Scheduled scan:** `scanner_scheduled_pipeline._resolve_scheduled_scan_symbols` merges Lambda `scanner_symbols` + `scan_default_watchlists` (≤100 rows, ≤30 symbols) + `SYSTEM_DEFAULTS`, cap **40**; `merge_scheduled_scan_symbol_universe`. **Alerts:** `watchlist_scanner_alerts.notify_intraday_setups_for_watchlist_users` after intraday + EOD setups, `asyncio.wait_for(..., 2.0)`; `find_users_with_default_watchlist_symbol`; `UserProfile.email` optional in Dynamo; `alert_store.had_signal_email_for_symbol_within_hours` (4h dedupe). **Tests:** `tests/api/test_watchlist_monitoring.py`. |
| B15 | **Market Intelligence headlines (quality + affected stocks)** | Done 2026-05-04 · follow-ups 2026-05-05 · commits: `e9728ad`, `9fe90e2` | **Original (2026-05-04):** `news_handler` ticker-scoped news, 4h lookback, watchlist merge, `news_impact_analyzer` chips + summaries, publisher cap, noise phrases, ticker aliases. **2026-05-05 — relevance & UX:** `stocvest/api/services/news_relevance.py` (catalyst/recency/source/watchlist scoring 0–100, PR-wire penalty, topic dedupe with word-overlap, `categorize_article` → API **`category`**); Polygon prefetch **50**, response cap **20**; `passes_market_intelligence_gate` vs `is_quality_article` split; headline fields **`relevance_score`**, **`credibility`**, **`matches_watchlist`**, legacy **`catalyst_category`**. **Frontend:** tabs All / Watchlist / Earnings / Analyst / Macro; category emoji; ✓ / dimmed PR on source line; per-tab empty copy; **`fetchMarketOverview`** `limit=20`; wider intel column (`13fr`/`7fr`). **Tests:** `tests/api/test_article_relevance.py`, `tests/services/test_news_relevance.py`, handler tests. |
| B16 | **Signals page after-hours research panel** | Done 2026-05-04 | Added `frontend/components/signals-after-hours-panel.tsx` and wired signals-page insufficient-data closed-session rendering to show last-session levels, earnings window, recent news, tomorrow watch levels, and watchlist CTA with graceful per-section fallbacks. |
| B17 | **PDT Guardian → compact status pill** | Done 2026-05-04 · commit: `3b7551f` | Removed large dashboard PDT card; added **`pdt-status-pill.tsx`** beside Market/VIX (color bands 0–1 / 2 / 3 day trades, desktop tooltip, mobile tap panel). PDT fetch unchanged. |
| B18 | **Scheduled scanner → portfolio composite pass** | **Retired 2026-05-08** — superseded by signal validation ledger | Shipped briefly as **`run_portfolio_composite`** + model-book hooks; **removed** with **`ModelPortfolio`** and related tests. Scheduled scanner today: watchlist merge + gap/setups + notify only (**`scanner_scheduled_pipeline`**). |
| B19 | **Ticker news panel + Market Pulse** | Done 2026-05-06 · commit: e1217cd | **Backend:** **`GET /v1/market/news?symbol=`** returns historical panel JSON (`days`/`limit`, **`articles`** with age/source/sentiment); no-symbol path unchanged **`headlines`**. **`news_panel_format.py`** + tests. **Frontend:** **`news-panel.tsx`**, **`ticker-news-panel.ts`**, per-ticker **2m** cache, fetch on open; **`fetchMarketOverview`** drops news; dashboard **Market Pulse** (SPY/QQQ/VIX/regime) replaces embedded Market Intelligence grid; triggers on dashboard cards, evidence, signals page. **Vitest:** **`fileParallelism: false`** to stabilize **`global.fetch`** mocks. |
| B20 | **Swing-first UX + plan preview + news window** | In progress 2026-05-06 | **Product:** positioning swing as primary; optional **day** tier adds intraday surfaces; preview **three tiers** in Settings (`subscription-plans.ts`, `DEFAULT_UI_PLAN`). **Dashboard:** no home **morning brief** block; **NewsPanel** remains the right-hand drawer on catalyst/news actions. **News API/UI:** default **8h** “recent” vs **20-day** archive labeling; optional query **`recent_hours`** (1–168). **Evidence:** deterministic **`metric-decision-copy.ts`** tooltips for **R/R**, signal score, trend strength, regime (aligned with `signal-evidence` R/R gate **2:1**). **Signals page:** default **`localStorage` trading mode** → **swing**. **2026-05-08:** empty-swing **Primary read** strip + layman **InfoTip** pass shipped under **`dashboard-redesign.tsx`** / **`lib/ui-tooltips.ts`** (see **B25**). **Follow-up:** wire **`DEFAULT_UI_PLAN`** to billing; optional **Claude** tooltip generator behind BFF with caching (see B22). |
| B26 | **STOCVEST Assistant (locked-prompt chatbot)** | Done 2026-05-10 · commit: `b6f83e7` · Polish 2026-05-10 · commits `0485c95`, `d1b6856`, `b7d57b7`, full-app coverage `22fce13`, homepage + isolation + visual + session reset `018ea89`, pathname-based reset + legacy-state hydrate fix `212b4d9`, explanatory-voice prompt upgrade `72fe502`, two-context safety perimeter + homepage-safe refusal template `cedb365`, **in-memory-only conversation state (this commit, 2026-05-11 latest)** · Terraform applied 2026-05-10 (route id `x6uhho2` authenticated, route id `69hco4j` public, 2 routes total in `aws_apigatewayv2_route.http`) · backend baseline 906 → 916 → 935 → **942** (unchanged — the bug fixed by this commit was frontend-only) · frontend baseline **161** (unchanged). **This round adds:** anonymous `POST /v1/public/assistant/chat` with PUBLIC MODE prompt activated via `session_mode=public`; root-layout mount for every visitor (auth-aware endpoint switching via `isAuthenticated` prop); cross-page conversation isolation (the assistant tracks the page identifier the persisted conversation belongs to and wipes history when the user navigates to a different page); `clearAssistantSession()` helper hooked into both sign-out buttons + the `subscribeSessionExpired` event so logout / 401 / token expiry wipes conversation + composer + sessionStorage; markedly stronger visual distinction in the conversation rail (accent-block "YOU" chip + right-aligned user panel vs surface-muted left-aligned STOCVEST panel) so the two roles are unmistakable at a glance. | **Backend:** **`POST /v1/signals/assistant/chat`** on signals Lambda. `stocvest/signals/assistant_prompts.py` holds `ASSISTANT_SYSTEM_PROMPT` verbatim with the explanation scope, refusal rules, contextual / general modes, and the "explain how STOCVEST thinks, not what to do" principle; `sanitize_messages` drops any non-`user`/`assistant` turn (blocks client-side `system`-role injection), caps single messages at **2000 chars**, trims history to **12** turns; `serialize_page_context` whitelist-emits only known keys (symbol, decision_state, decision_rationale, layer_status, risk_reward, trade_readiness, market_regime, etc.). `stocvest/signals/assistant_chat.py` (`AssistantChatService.reply`) builds `system = ASSISTANT_SYSTEM_PROMPT + serialize_page_context(...)` and POSTs to Anthropic Messages API with the proper top-level `system` parameter (not the concat-into-user trick used by `ai_explanations`). Free users (no `has_ai_explanations`) never reach Claude — they get a deterministic reply with `upgrade_available: true`. **Frontend:** `components/assistant/{stocvest-assistant,assistant-launcher,assistant-panel,assistant-conversation-rail}.tsx`, `lib/assistant/{types.ts,context.tsx}`, BFF route `app/api/stocvest/signals/assistant/chat/route.ts`. Visual language: floating lens-glyph launcher with slow pulse, glassmorphism panel with no-bubble vertical conversation rail, **six-layer constellation context strip**, word-fade reveal capped at 3.6s stagger; explicit **General / Contextual** mode badge; all motion gated behind `prefers-reduced-motion: reduce`. **Shared source of truth:** `synthTradeDecision` / `deriveDecisionRationale` extracted from `signal-evidence-card.tsx` into `lib/signal-evidence/trade-decision.ts`; the Evidence card and the signals page's assistant context both read it, so the chatbot can never contradict the displayed Decision. **Infra:** `infra/apigateway_6e.tf` adds the new route under `signals` with `authorizer = true`; no new secret needed (`ANTHROPIC_API_KEY` reused from `stocvest/lambda-runtime`). **Tests:** `tests/api/handlers/test_signals_assistant_chat.py` (6 tests: unauth 401, invalid body 400, free-user deterministic, contextual mode detection, paid-user AI path, sanitizer drops client `system` role). `terraform fmt` + `validate` clean. **Polish (commit `0485c95`):** `stocvest-assistant.tsx` now emits a `[STOCVEST Assistant]` `console.warn` with HTTP status + a 200-char body excerpt on any non-2xx, and shows **status-aware** fallback copy — `404` → "isn't enabled yet", `5xx` → "temporarily unavailable", other → calm generic — so an unapplied route surfaces a distinct diagnostic instead of the generic "couldn't reach" line. **Polish (commit `d1b6856`) — context, prompt, role distinction:** (1) **Page context publisher (`components/signals-page-client.tsx`)** always emits `layer_status` (derived from `signalEvidence.layers`) even when `insight` is null, and adds **`analysis_status`** (`loaded` / `loading` / `unavailable` / `insufficient_data`) so the assistant can answer in qualitative terms while analysis is still pending instead of refusing. (2) **System prompt hardening (`assistant_prompts.py`)** adds an explicit "**symbol-or-page-only context**" rule (answer in general terms about that symbol/page, never refuse, never narrate missing metrics, never ask the user to restate context), tighter **TONE AND STYLE** rules (default 1-4 sentences, plain prose only — no bullets / headings / markdown unless the user explicitly asks), and a banned-phrase list ("I don't have…", "I can't see…", "What you can do…"). `serialize_page_context` extended to include `analysis_status`. (3) **Role distinction (`assistant-conversation-rail.tsx`)** — the speaker label for **YOU** now renders in the accent color and **STOCVEST** in the page-tone color; each message body gains a **2px role-tinted left ribbon** and user turns get a faint role-tinted background tint, so user vs assistant is unmistakable without speech bubbles. (4) **`AssistantPageContext`** type gains `analysis_status`. **Terraform applied 2026-05-10:** `terraform apply -var-file=terraform.tfvars -input=false -auto-approve` → **1 added, 0 changed, 0 destroyed** (`aws_apigatewayv2_route.http["POST /v1/signals/assistant/chat"]`, route id **`x6uhho2`**); verified live with an unauthenticated probe that returned **`401 Unauthorized`** from the JWT authorizer (proves the route exists — a missing route returns API Gateway's own 404). Signals Lambda redeployed by CI on each push; no new secret required. **Polish (scanner page context — this commit):** Reported issue — when the user was on the scanner page the chatbot did not understand the page context. **Root cause** was three-fold: (1) `components/scanner-page-client.tsx` never called `usePublishAssistantContext`, so the global provider held `context = null` even though the user was clearly on a STOCVEST page with rich state; (2) the existing `AssistantPageContext` type and the server-side `serialize_page_context` whitelist had no fields for multi-symbol overview pages — even if the scanner did publish, the relevant fields would have been dropped silently; (3) `_mode_from_context` in `stocvest/signals/assistant_chat.py` only flipped to `contextual` when `symbol` or a recognized `decision_state` was present, so a scanner page (which has no single symbol or decision_state) would still resolve to `general` mode regardless. **Fix:** (a) **`AssistantPageContext`** gained scanner-specific optional fields: `scanner_focus` (`swing`/`day`/`both`; supports `both` which `trading_mode` cannot), `market_open` (boolean), `gap_with_catalyst_count`, `gap_without_catalyst_count`, `ranked_setups_count`, `swing_setups_suppressed` (boolean, mirrors the on-screen banner), `setups_empty_message` (the calm one-liner displayed when the setups list is empty), `top_setups` (`AssistantScannerSetupSummary[]` capped at 3 — qualitative `strength_bucket` of `strong`/`moderate`/`weak`, never raw scores, plus `confluence` + `orb_expired`), `top_gaps_with_catalyst` (`AssistantScannerGapSummary[]` capped at 3 — qualitative `quality_bucket` of `high`/`medium`/`low`, plus `catalyst_category` + `catalyst_sentiment`). (b) **`stocvest/signals/assistant_prompts.py` `serialize_page_context`** extended to emit those new fields under the same whitelist discipline — unknown direction / bucket values are dropped silently so the LLM never receives free-form strings from the client. (c) **`_mode_from_context`** now treats a non-empty `page` as sufficient evidence of context (so the scanner's `page = "dashboard/scanner"` flips mode to `contextual`); existing tests on `symbol`/`decision_state` still hold. (d) **`ASSISTANT_SYSTEM_PROMPT`** gained one new CONTEXTUAL MODE RULES bullet plus one new scanner-flavored example so the LLM knows how to answer in terms of what the page is showing (counts, top items, scanner focus, swing-suppression banner) instead of inventing per-symbol decisions. (e) **`components/scanner-page-client.tsx`** builds a memoized `assistantContext` from scanner state and calls `usePublishAssistantContext(assistantContext)`; the memo deps are limited to inputs that actually change (`scannerSetupMode`, `overview.regimeLabel`, `marketOpen`, `gapIntelGrouped.withCat`, `gapIntelGrouped.without.length`, `rankedSetups`, `showSwingScanContextBanner`, `setupsEmptyMessage`) so the 1Hz `forceTick` interval used for the countdown doesn't republish. (f) **Tests:** **+11** new in `tests/signals/test_assistant_prompts.py` (whitelist drops unknown keys; signals fields emit cleanly; scanner fields emit with `ranked_setups_count=0` as a meaningful zero; invalid buckets are silently dropped; `_mode_from_context` page-alone → contextual; sanitize bounds), **+1** new in `tests/api/handlers/test_signals_assistant_chat.py` (scanner-only `page_context` end-to-end → mode=contextual + payload reaches service intact). Backend baseline 894 → **906**. **Polish (full-app coverage — this commit):** Reported issue — assistant should be context-aware on every user-specific logged-in page and operate in general mode on home / marketing pages. **Architectural change:** `<AssistantContextProvider>` and `<StocvestAssistant>` were **hoisted from `app/dashboard/layout.tsx` to `app/layout.tsx`** (root layout) and **gated on `getServerSession()`** — logged-in users now get the assistant on every authenticated route (dashboard pages AND any marketing route they navigate back to like `/about`, `/security`, `/how-it-works`, etc.); logged-out marketing visitors (the only audience on `/`, `/login`, `/signup`, `/forgot-password`, etc.) continue to see **CrispChat only** and never trigger client-side calls to the JWT-authenticated `/v1/signals/assistant/chat` route. `app/page.tsx` already redirects logged-in users to `/dashboard`, so the home page is only ever rendered for anonymous visitors and the session-gate matches the security model. The dashboard layout no longer mounts the assistant — its only remaining role is dashboard-specific compliance (`DashboardComplianceClient`, `SessionExpiryWatcher`, `SessionExpiredBanner`). **Per-page context publishers** added on every previously-uncovered dashboard page (Each just calls `usePublishAssistantContext({ page: "dashboard/X", ...cheap fields })`; the hook's internal `JSON.stringify` diff handles memoization so no `useMemo` boilerplate per page): `dashboard` (home) — `trading_mode: "swing"` + `market_regime` + `ranked_setups_count`; `dashboard/signal-validation` — `trading_mode` from the active swing/day tab; `dashboard/options` — active `symbol`; `dashboard/crypto` — active `symbol`; `dashboard/watchlists`, `dashboard/earnings`, `dashboard/portfolio`, `dashboard/journal`, `dashboard/performance`, `dashboard/futures`, `dashboard/settings`, `dashboard/legal` — page identifier only (which is enough — the locked system prompt's "page identifier only" rule activates `_mode_from_context = contextual` and the LLM answers in calm general terms about what that page does, exactly as designed by the previous polish round). **Marketing-side behavior:** logged-in users on `/about`, `/security`, `/how-it-works`, `/terms`, `/privacy`, `/legal/risk-disclosure` see the assistant in **general mode** because no provider is wrapped around a publishing page — the global provider holds `context: null`, `_mode_from_context(null) = "general"`, and the LLM answers about STOCVEST in product terms without speculation. **Tests:** baselines unchanged — backend **906 passed, 3 skipped**; frontend **161 passed** (30 files); `tsc --noEmit` reports 24 pre-existing errors in `order-confirmation-modal.test.tsx` / `pdt-api.test.ts` / `signal-evidence.test.ts` / `signal-evidence-layers.test.ts` (verified identical with and without these changes by stashing and re-running), zero new typecheck errors introduced. **Files touched (16):** `frontend/app/layout.tsx` (assistant mount + gate), `frontend/app/dashboard/layout.tsx` (mount removed; only dashboard chrome remains), `frontend/components/{dashboard-redesign,watchlists-page-client,signal-validation-page-client,earnings-page-client,portfolio-page-client,journal-page-client,performance-tracking-content,options-page-client,futures-dashboard-panel,crypto-panel,settings-page-client,legal-agreements-review}.tsx` (each: one import + one `usePublishAssistantContext({...})` call). **Polish (in-memory-only conversation state — this commit, 2026-05-11 latest):** user reported two regressions: (1) refresh kept showing the old conversation, and (2) homepage messages persisted after login. Root cause was the `sessionStorage`-backed persistence layer plus a state-ordering bug — the hydrate-from-sessionStorage `useEffect` was clobbering `prevAuthRef.current` (it ran first on a re-render, set the ref to the *new* `isAuthenticated` value), so the auth-flip reset `useEffect` ran second, found `prevAuthRef === isAuthenticated`, and early-returned without clearing the conversation. The simplest, safest fix is to drop the persistence layer entirely so no stale state can survive any kind of transition. **Files touched (2):** `frontend/components/assistant/stocvest-assistant.tsx` (drops `PersistedState`, `readPersistedState`, `writePersistedState`, `removePersistedState`, the hydrate-on-mount effect, and the persist-on-change effect; adds a one-shot `wipeLegacyPersistedState()` mount effect that removes any legacy `stocvest_assistant_state_v1` key from existing tabs; the three reset effects — pathname-change, auth-flip, session-expiry / explicit-clear — remain, and `prevAuthRef` is now owned by exactly one effect so auth-flip detection is reliable); `frontend/lib/assistant/session-reset.ts` (doc comment rewritten to reflect the in-memory-only design; the `ASSISTANT_STORAGE_KEY` export is kept and `clearAssistantSession()` still removes it defensively so a logged-out tab cannot show legacy persisted messages). **Behavior contract:** a fresh conversation starts on every (a) hard refresh / page load, (b) cross-surface navigation, (c) login transition (anonymous → authenticated), (d) logout transition (authenticated → anonymous), (e) session expiry, (f) explicit "Sign out" click. Within a single page view the conversation accumulates in React state as expected. No backend or Terraform changes. Backend baseline **942** unchanged; frontend baseline **161** passed (unchanged — no existing assistant component tests); `tsc --noEmit` zero new errors. **Prior polish (two-context safety perimeter — earlier 2026-05-11, commit `cedb365`):** the user provided a refined blueprint emphasizing that the assistant must adjust its behavior based on a TOP-LEVEL context split (LOGGED-OUT vs LOGGED-IN) that sits ABOVE the existing three modes, and that the LOGGED-OUT case in particular needs explicit per-symbol guardrails to close the per-symbol-hallucination risk. **Risk closed:** the prior PUBLIC MODE rules said "refuse trade recommendations" but did not explicitly forbid the LLM from inventing an Evidence card / Trade Readiness / blocking-layer narrative for a specific stock on the homepage — a logged-out visitor could ask "what does STOCVEST think about AAPL?" and the LLM might still narrate a fake decision. **Prompt changes (`stocvest/signals/assistant_prompts.py`):** (a) added a `CRITICAL CONTEXT AWARENESS RULE` section right after the three-mode declaration, splitting context into LOGGED-OUT (`session_mode=public`) vs LOGGED-IN (`session_mode=authenticated`) and tying them explicitly to the `session_mode` field in the page-context block; the LOGGED-OUT golden rule "explain the FRAMEWORK, not the DECISION" is in this section verbatim. (b) renamed and expanded `PUBLIC MODE RULES` → `PUBLIC MODE RULES (LOGGED-OUT / HOMEPAGE)` with an explicit per-symbol MUST-NOT list — no evaluating or discussing any specific stock by ticker or company name (with AAPL / TSLA / NVDA / MSFT as concrete examples so the LLM pattern-matches any ticker the visitor names), no stating or implying a current Decision for any symbol, no Evidence card reference for a specific stock, no Trade Readiness score for a specific symbol, no naming a specific layer as "currently blocking" a specific ticker, no sounding like a live demo of unavailable features. (c) added the `HOMEPAGE-SAFE REFUSAL TEMPLATE` verbatim ("I can't assess individual stocks here, and I don't give buy or sell answers. What I can explain is how STOCVEST decides whether trading conditions are aligned once a symbol is evaluated inside the platform.") so the LLM falls back to it for any per-symbol question on the homepage instead of improvising. (d) added a TONE & STYLE rule: "When refusing a request, explain WHY the boundary exists" with concrete example phrasing ("STOCVEST's role is to explain whether conditions are aligned, not to outsource your judgment") because refusal-with-reason builds trust where refusal-without-reason feels dismissive. **No frontend changes** — the existing `session_mode=public` routing on the BFF (`frontend/app/api/stocvest/public/assistant/chat/route.ts`) and the existing root-layout `isAuthenticated` prop already deliver the right context flag; the new guardrails are entirely prompt-side. **Test changes (`tests/signals/test_assistant_prompts.py`):** +7 new lock-in tests — `test_prompt_declares_critical_context_awareness_two_context_perimeter` (verifies the section is present and ties the two contexts to `session_mode=public` and `session_mode=authenticated`), `test_prompt_carries_logged_out_golden_rule` (verifies "explain the FRAMEWORK, not the DECISION" is in the prompt verbatim), `test_prompt_explicitly_bans_per_symbol_evaluation_when_logged_out` (verifies the per-symbol MUST-NOT bullets are in the prompt), `test_prompt_carries_homepage_safe_refusal_template_verbatim` (verifies the refusal opener is in the prompt verbatim), `test_prompt_lists_concrete_ticker_examples_in_public_mode_ban` (verifies AAPL / TSLA / NVDA / MSFT all appear so the LLM pattern-matches arbitrary tickers), `test_prompt_carries_explain_why_boundary_exists_in_tone` (verifies the new TONE & STYLE rule), `test_prompt_keeps_public_mode_rules_label_for_section_routing` (verifies the section header remains so prior tests still pass). **One tightened assertion** in `tests/signals/test_assistant_chat_service.py::test_reply_public_calls_claude_with_public_mode_marker` — the existing test asserted `"session_mode=authenticated" not in system_text` but the new prompt now mentions `session_mode=authenticated` as descriptive text inside the CRITICAL CONTEXT AWARENESS RULE; we tightened the test to isolate the appended `=== PAGE CONTEXT ===` tail (which is the actual routing signal the BFF controls) and only assert on that tail — the static-prompt mention is a guardrail, not a routing signal. Backend baseline 935 → **942 passed, 3 skipped** (+7); frontend 161 passed (unchanged); `tsc --noEmit` zero new errors. **Prior polish (explanatory-voice prompt upgrade — earlier 2026-05-11, commit `72fe502`):** the user provided a richer system-prompt blueprint emphasizing that STOCVEST decides WHEN trading is statistically worth risking capital, that silence is an intentional system state, and that the assistant must frame inactivity as intentional and protective. **Audit:** before adopting the new prompt I ran a full product audit (recorded in the agent transcript) and identified five concepts in the initial proposal that would have contradicted shipped UI — "VIC / Volatility Control" with Supportive/Neutral/Hostile (does not exist; VIX is a signal inside the Market Internals layer), Macro Pulse "Active / Weak / Unavailable" (real states: Unavailable / Elevated / Upcoming / Known and absorbed from `dashboard-posture.ts` `macroRiskStateHeadline`), "Symbol Readiness Score 0.0-1.0" (real product surface is Trade Readiness 0-100 as `{score}/100` on the Evidence card via `signal_score`), "System Confidence: High / Medium / Low" (does not exist; the product expresses confidence through Layer alignment buckets + Trade Readiness only), and "Sector State: Leading / Mixed / Fragmented" (real chips: Confirming / Non-confirming / Mixed; tape framing Risk-on / Defensive / Mixed / Narrow). The user then sent a refined product-accurate blueprint that I merged with the existing operational guardrails. **Prompt rewrite (`stocvest/signals/assistant_prompts.py`):** `ASSISTANT_SYSTEM_PROMPT` is now organized into PRIMARY GOAL → CORE PRODUCT PHILOSOPHY ("Silence is an intentional system state") → ANALYSIS LAYERS (six real product layer names verified against the codebase: Technical, News, Macro, Sector, Geopolitical, Market Internals) → MARKET REGIME (UI Bullish/Neutral/Bearish + engine risk_on/risk_off/neutral/avoid) → SECTOR CONFIRMATION (Confirming / Non-confirming / Mixed; Risk-on / Defensive / Narrow) → MARKET PULSE (Unavailable / Elevated / Upcoming / Known and absorbed; pulse never authorizes trades) → INTERNALS & VOLATILITY (VIX is a signal inside Internals; product-accurate terms: Internal participation / Volatility stability or stress / Risk sensitivity) → TRADE READINESS (0-100, never 0.0-1.0) → DECISION STATES (verbatim lines from `lib/signal-evidence/trade-decision.ts`: "✅ Actionable — passes risk/reward and confirmation thresholds", "⚠️ Monitor only — confirmation and/or risk gates are not fully cleared", "🚫 Blocked — fails minimum synthesis and risk gates") → ALIGNMENT & CONFIDENCE (Layer alignment High/Moderate/Low + Trade Readiness; explicit ban on inventing "System Confidence") → SUPPRESSION & GATING LOGIC (with the dashboard's exact empty-state copy: "No active swing setups right now", "System posture: Waiting for alignment", "Swing suppressed — risk-off tape; desk idle until structure aligns", "Signal suppressed — regime not cleared") → REGIME TRANSITIONS (probabilistic-only — never forecast a flip) → BACKTESTING & VALIDATION (route to `/performance` + `/dashboard/signal-validation` with the standing disclaimer "Historical signal accuracy does not guarantee future results."; never invent a hit-rate) → USER INTERACTION RULES (explicit MAY / MUST NOT lists) → GENERAL BEHAVIOR RULES (preserves the expanded ban-list of self-limitation phrases + the explicit ban on inventing the five concepts above) → CONTEXTUAL MODE RULES (preserved, with new examples for the empty-swing dashboard posture and the suppressed-scanner case + the production BAD/GOOD banned-response-shapes block) → GENERAL MODE RULES → PUBLIC MODE RULES → PAYWALL AWARENESS (swing_pro / swing_day_pro / active beta — the LLM knows it is the paid path and never describes the deterministic free-tier fallback as an error) → TONE & STYLE → END GOAL & FOUNDATIONAL PRINCIPLE (foundational principle preserved verbatim: "To help users understand how STOCVEST thinks — not to tell them what to do."). **No frontend changes** — the existing page-context publishers already carry every field the upgraded prompt references (`decision_state`, `decision_rationale`, `market_regime`, `trade_readiness`, `layer_alignment_pct`, `layer_status`, `scanner_focus`, `swing_setups_suppressed`, `setups_empty_message`, `top_setup_*`, `top_gap_*`, etc.); the upgrade is pure prompt-side. **Tests:** +19 new in `tests/signals/test_assistant_prompts.py` locking in PRIMARY GOAL + CORE PRODUCT PHILOSOPHY, the six-layer enumeration with their exact product names, the verbatim Decision lines, the 0-100 Trade Readiness scale (and the negative assertion that 0.0-1.0 is explicitly forbidden), the real Macro pulse states, the real sector chip labels, the explicit ban-list of invented concepts, the suppression-copy anchors, the `/performance` + `/dashboard/signal-validation` routing + standing disclaimer, the PAYWALL AWARENESS section with swing_pro / swing_day_pro / active beta, the preserved banned self-limitation phrases, the preserved banned response shapes block, the three-mode structure, the foundational principle, the calm-refusal pattern, the SUPPRESSION & GATING LOGIC + REGIME TRANSITIONS + BACKTESTING & VALIDATION sections, and the USER INTERACTION MAY/MUST NOT lists. Backend baseline 916 → **935 passed, 3 skipped** (+19); frontend 161 passed (unchanged); `tsc --noEmit` zero new errors outside the pre-existing untouched test files. **Polish (pathname-based reset + legacy-state hydrate fix — earlier 2026-05-11, commit `212b4d9`):** Reported regression — after `018ea89` shipped the per-page isolation + homepage chatbot, the user still saw **prior logged-in conversation turns (TTD on Signals) persist on the anonymous home page**. **Root cause:** (1) the cross-page reset effect was keyed off `pageContext?.page`, but `/`, `/login`, `/signup`, and other marketing routes never publish a page context, so the transition from `dashboard/signals/layers` to `/` left `currentPage` at `null` and the early-return ran every time — the wipe never fired; (2) the hydrate force-clear was guarded by `persistedAuth !== null && persistedAuth !== isAuthenticated`, so legacy persisted state written before the `isAuthenticated` field existed (e.g. data from `0485c95` or earlier) had `persistedAuth = null` and slipped past every clearing condition. **Fix:** (a) `frontend/components/assistant/stocvest-assistant.tsx` rebuilt around `usePathname()` from `next/navigation` — every URL change wipes messages/composer/notice/unread; persisted state now stores `lastPathname` instead of `lastPage`, and the hydrate effect force-clears when **any** of three conditions hold: `persistedAuth !== isAuthenticated` (covers legacy `persistedAuth = null` because `null !== true|false`), persisted `lastPathname` differs from current `pathname`, or the auth-state-flip effect fires. (b) `stocvest/signals/assistant_prompts.py` — the prohibition against describing own access to data is promoted to a top-level General Behavior bullet with an expanded ban-list ("I don't have", "I can't see", "I can't access", "I would need to see", "to give you a precise explanation I would need", "at this moment", "right now I lack", and similar), plus a new **banned response shapes** block under the Contextual Mode examples that contains the exact BAD/GOOD pair drawn from the production failure mode in the user screenshot ("I don't have access to live page data at this moment, so I can't see the current metrics, decision state, or signal details for TTD on the swing timeframe" → "STOCVEST evaluates every setup across six independent layers — technical, news, macro, sector, geopolitical, and internals — and surfaces a Decision (Actionable, Monitor, or Blocked) only when those layers agree"). (c) `frontend/components/assistant/assistant-panel.tsx` — anonymous empty state is rebuilt as a welcoming three-paragraph pitch ("Welcome to STOCVEST." → six-layer thesis → kinds of questions visitors can ask), plus a fourth marketing-flavored quick prompt ("How do the six layers work together?") so the first impression on the home page clearly tells visitors what the assistant can help with. **Tests:** all existing assistant tests still pass — backend **916 passed, 3 skipped** (verified 2026-05-11); frontend **161 passed** (verified 2026-05-11). `tsc --noEmit` reports identical pre-existing error count (no new typecheck regressions). |
| B25 | **Dashboard empty swing — Primary read + layman tooltips** | Done 2026-05-08 · commit: `6eba555` | **Shipped:** **`dashboard-redesign.tsx`** — when **Top signals** is empty and the scanner has no error, **Primary read** shows posture headline, regime one-liner, and a single institutional suppression line (**Signal suppressed — …**); no separate chip + “Why?” on that filter line. Circled‑i beside **Primary read** opens plain-language copy (`PRIMARY_READ_SWING_CONTEXT_TIP`: what “swing suppressed” and per-stock checks mean). Same **Top signals** card footer (**Alignment**, **Watchlist readiness**, **What would re-enable swing setups**): **SWING_REENABLE_CALLOUT_TIP**, **ALIGNMENT_LADDER_TIP**, **WATCHLIST_READINESS_TIP**, **TOP_SIGNALS_CARD_TIP** rewritten for non-experts; **What would re-enable swing setups** uses a circled‑i **InfoTip** beside the heading (no “Why?” label) with intro + **short** re-enable bullets only (full technical bullets removed from that tooltip); watchlist readiness detail tip prefixed with **`WATCHLIST_READINESS_DETAIL_INTRO`**. **`info-tip.tsx`:** tooltip body **`white-space: pre-line`** so `\n\n` paragraphs render. |
| B21 | **Public landing v2 + founding paid counter** | Done 2026-05-07 · commit: 3e21d5cd4a2d41f1fb8cddd71cbdce61531a34e8 | **Landing:** `landing-page.tsx` rewrite (hero, swing vs day positioning, engine demo **SWING/DAY** tabs, differentiation/workflows/transparency/comparison, yesterday’s signals block, pricing + founding strip, footer). **API:** `GET /v1/signals/founding-members` on signals Lambda; **`user_profile_store.get_founding_member_count`** scans **`Users.subscriptionPlan`** for **paid** tiers only (`swing_pro`, `swing_day_pro`, legacy `founding_*`). **Frontend:** `app/page.tsx` **`Promise.all`** + `lib/api/founding-members.ts`. **Tests:** `frontend/tests/landing-page.test.tsx`. **Terraform:** API Gateway public route. |
| B24 | **Beta tester full-app override** | Done 2026-05-07 · commit: 3e21d5cd4a2d41f1fb8cddd71cbdce61531a34e8 | **`UserProfile`** beta fields + **`has_full_access`** / **`has_ai_explanations`**; **`PATCH /v1/admin/users/{user_id}/beta-access`** (analysis admin auth); **`PATCH /v1/users/me`** blocks client mutation of subscription + beta keys; Dynamo read/write in **`user_profile_store`**; script **`scripts/beta_access.py`**; tests **`tests/api/test_user_profile_plan.py`**, **`tests/api/test_users_me.py`**. |
| B-swing-portfolio | **Swing ledger enrichment (MAE/MFE, daily exit dates, rule labels)** | Not done | **`SignalRecord.mode=swing`** + UI **`/dashboard/signal-validation`** exist. **Still pending:** jobs to fill **`ledger_*_date_et`**, **`exit_reason`**, **`max_adverse_excursion_pct`** / **`max_favorable_excursion_pct`**, and swing-specific **`exit_rule`** from daily-bar paths (no reinstatement of removed **`ModelPortfolio`**). |
| B5 | **Subscriptions** | Not done | Stripe (Atlas-linked), tier gating (map **B20** tier IDs → entitlements: swing limits, day-trading preview vs full), usage limits; **after** attorney-reviewed ToS and entity readiness (`CONTEXT.md` §14). |
| B22 | **Claude-generated metric explanations (cached)** | Not done | **POST** BFF (e.g. `/api/stocvest/explain-metric`) accepting metric key + numeric context + layer snapshot hash; return plain English for tooltips; **fallback** to `metric-decision-copy.ts` on error/rate-limit; enforce privacy (no account numbers); cost controls. **Note:** on-demand AI **signal** explanations already gate on **`has_ai_explanations`** (paid or beta full access). |
| B6 | **Sector & market internals** | Not done | Sector rotation view, breadth (A/D, highs/lows), VIX regime, optional put/call—**data-only** copy per legal framing. **2026-05-08:** Backend already exposes internal **SIC → sector ETF** resolution with **`SicMappingTier`** (`exact` / `prefix` / `coarse` / `fallback_spy`) on `resolve_sector_bucket_from_sic`, **`SectorMapper.get_sector_etf`** (5-tuple), resolution logs, and optional **`sic_mapping_tier`** on composite **`layers[]`** sector objects — see **D9**, **`docs/SIGNAL_ENGINE.md`** § Sector. |
| B7 | **Risk management UX** | Not done | Position sizing helper (% of portfolio), optional max daily loss guardrails, concentration / correlation hints—**never** framed as advice; copy reviewed with counsel. |
| B8 | **Scanner order placeholder** | Not done | Replace “Order modal placeholder” on scanner with real order entry or deep-link to portfolio panel; align with Step 8 validation flow. |
| B9 | **Earnings calendar UI** | Done 2026-05-02 | Redesign: default **Upcoming** filter, Mon–Fri **This Week**, grouped sections (Today / This Week / Upcoming), grid rows with beat/miss bars, monospace figures. **Polish (density, section/header separation, Actual & Surprise alignment, hover):** resolved 2026-05-02 — same commit as PF3. |
| B10 | **Gap Intelligence + morning brief** | Done 2026-05-02 · commit: 4122c05 | **Backend:** `POST /v1/scanner/gap-intelligence` merges dynamic gaps + 24h news, quality score + filters, catalyst block; `news_catalyst_detector` noise phrases, categories (insider/analyst/fda/merger/earnings/macro), narrative sentiment 0–100; structured morning brief from `morning_brief.py` + `morning_brief_fetch` (SPY/QQQ/VIX, Benzinga economics stub, earnings today, gap-intel top watch, PDT); default intraday `min_score` **0.55**. **Frontend:** single Gap Intelligence panel (2-col scanner with intraday); dashboard morning brief sections. **Terraform:** API route `gap-intelligence`. **Follow-up shipped 2026-05-02:** shared-news headline dedupe across gap cards (`gap_intelligence`), `company_name` snapshot fallback (`polygon_client`), merger/acquirer narrative tweaks (`news_catalyst_detector`), gap card symbol/company typography (`scanner-page-client`). |
| B13 | **Phone number collection + SMS alerts** | Not done | **Priority: low** — do not build until **B3** is complete. **Phase 1** (when B3 alerts ships): Add optional phone field to **Settings → Notifications**. Verify via **AWS SNS** 6-digit SMS code before storing. **Never collect unverified numbers.** **Phase 2:** Make phone required for **Pro** SMS alert tier. **Do NOT** add to signup form — collect only when SMS alerts are ready to deliver. **Framing:** “Add your phone to receive PDT warnings and signal alerts.” **Channels when ready:** PDT warning (free), signal state change (Pro), confluence alert (Pro), morning brief summary (Pro). **Dependencies:** B3 alerts delivery must ship first. **Infrastructure:** AWS SNS for SMS, ~**$0.00645/SMS** in US. **Privacy:** CCPA/GDPR disclosure required in Privacy Policy before collecting. |
| B11 | **Confluence Score** | Done 2026-05-02 | **Confluence Score** — multi-signal alignment: `stocvest/signals/confluence.py`, intraday + swing payloads, morning brief `top_watch` fields; setup cards, signal evidence modal, morning brief styling, gap intelligence **CONFLUENCE** chip (frontend cross-ref by symbol). Tests: `tests/signals/test_confluence.py`. |
| B12 | **Landing: historical signals + trust sections** | Done 2026-05-02 · commit: 4579d1c | **`GET /v1/signals/recent?landing=true`** → resolved-only allowlisted payload; **`SignalRecord.ai_summary`** optional. Landing: **`LandingSignalExplorer`**, **`LandingBeforeAfterSection`**, **`LandingActivityFeedSection`**, server fetch + **`FALLBACK_SIGNALS`**, nav removes How It Works / Performance links; tests in **`tests/api/test_signal_recorder.py`**. **Polish 2026-05-03 · commit `313db1e`:** shared glow card CSS (`.landing-glow-card` / gate / pledge / before-after), consistent cards on landing sections + **`landing-page.tsx`** marketing blocks; realistic fallback tick prices + dynamic 1h % line; explorer “(example data)” on API fallback; Live Engine empty state + accuracy sparkline copy. **Explorer fallback 2026-05-03 · commit `d5a363e`:** ET wall-clock **`generated_at`** for demo rows; **5** fallbacks (AMD, SPY with **incorrect** 1h outcome); tab strip note (4 correct / 1 incorrect vs live “Showing N…”). |
| B27 | **Mode Separation UI compliance shipment (A1/A2/C/D)** | Done 2026-05-11 · commits: `e7c9fe0`, `122ddc8`, `0f31098`, `7bf3775` | **Goal:** close the screen-level violations of the **Mode Separation safety perimeter** the prompt declared earlier the same day in `ASSISTANT_SYSTEM_PROMPT` ("Swing trading and Day trading are independent decision engines that share market context but never share decisions, readiness, validation, or activity permission"). **Audit covered seven surfaces** (Dashboard, Scanner, Signals, Signal Validation Ledger, Signal Validation Historical, Portfolio, Journal, Performance dashboard, Performance landing); four were structural violations that could be fixed as pure compliance refactors and shipped here; two are product+backend-blocked and tracked separately as **B28** (Dashboard Day Desk) and **B29** (Portfolio/Journal mode attribution). **Phase A1 — Scanner "Both" split (`e7c9fe0`):** `scanner-page-client.tsx` previously merged swing+day setups into a single ranked list sorted by score and sliced to top 10 — a day setup with score 0.9 ranked above a swing setup with score 0.7 even though they belong to different engines. Fix partitions by `setup.scanner_mode === "swing_daily"`, renders TWO mode-labelled sections in `both` mode ("Swing setups (daily cadence)" / "Day setups (intraday cadence)"), each top-5, each with mode-specific empty-state copy. Single-mode views (`swing` or `day`) keep the existing single-list rendering. "Open Signals" deep link per row now appends `&trading_mode=${setup.scanner_mode === "swing_daily" ? "swing" : "day"}`. **Phase A2 — Signals page mode plumbing (`122ddc8`):** `app/dashboard/signals/page.tsx` reads `searchParams.trading_mode` server-side and passes `initialTradingMode: "day" | "swing" | null` on `signalsPrefill`; `signals-page-client.tsx` initialises `tradingMode` from `initialTradingMode ?? "swing"`, gates the localStorage-restore useEffect so it only runs when URL did NOT specify a mode (URL beats localStorage), `updateTradingMode` now mirrors into URL via `window.history.replaceState` preserving other query params, `fetchUserEvaluatedSignals({ days: 30, mode: tradingMode })` carries the mode (added to effect deps so engine switches refetch), the signal_id strip preserves `trading_mode` instead of nuking the whole query string, and `signal-validation-page-client.tsx` ledger-row deep links append `trading_mode=${tab}`. **Phase C — Historical Validation no-combined-headline (`0f31098`):** `historical-validation-panel.tsx` previously defaulted to `mode = "all"` which omitted the mode filter from the API and rendered a single "Overall directional accuracy" hero card averaging swing+day. Fix narrows `ModeFilter` from `"all" | "swing" | "day"` to `"swing" | "day"` (TypeScript-level guard), `MODE_OPTIONS = ["swing", "day"]`, default mode `swing`, fetcher always sends concrete mode, hero card title is mode-specific ("Swing directional accuracy (multi-day cadence)" / "Day directional accuracy (intraday cadence)") with `data-mode` attribute, dropdown labels expanded to "Swing (multi-day)" / "Day (intraday)". **Phase D — Performance no-combined-headline (`7bf3775`):** `performance-tracking-content.tsx` top metric row drops the "Directional Accuracy" card (lg:grid-cols-4 → lg:grid-cols-3), `PublicValidationSection` drops the "Overall accuracy (1d)" card (sm:grid-cols-3 → sm:grid-cols-2) leaving only Swing track + Day track cards (`overall` deliberately not destructured), `landing-performance-section.tsx` removes the "Overall directional accuracy (resolved 1d): X.X% · n=Y" fallback line on the LOGGED-OUT homepage (the most promotional violation) and replaces with a mode-agnostic volume line + handoff pointing users at per-engine track records, `overallPct` derivation dropped as unused. **Tests +13 total:** A1 +2 in `tests/scanner-swing-toggle.test.tsx`, A2 +5 in new `tests/signals-page-mode-plumbing.test.ts`, C +2 in new `tests/historical-validation-panel-mode.test.ts`, D +4 in new `tests/performance-no-combined-headline.test.tsx`. Frontend baseline **208 → 221 passed (+13, zero regressions)**, backend **1038/3** unchanged (no backend changes). `tsc --noEmit` zero new errors. **No Terraform, no IAM, no API contract changes, no new dependencies, no backend code changes.** **Compliance status after this shipment:** Scanner = COMPLIANT, Signals page = COMPLIANT, Signal Validation Ledger = COMPLIANT (already was — D2 Phase 3b shipped mode-isolated tabs), Signal Validation Historical = COMPLIANT, Performance dashboard = COMPLIANT, Performance landing = COMPLIANT. Remaining gaps tracked in **B28** + **B29** below — Dashboard is intentionally swing-first today and adding a Day Desk requires product design + API, Portfolio + Journal have no `strategy_trading_mode` field on the wire today and require backend schema changes. |
| B28 | **Dashboard Day Desk (parallel-engine home surface)** | Done 2026-05-11 · Phase 0 commit `6a25041` (chatbot routing rules) + Phase 1 (Dashboard two-desk render) — see `docs/CONTEXT.md` line 7 for the full breakdown. Pre-shipment scope notes preserved below for historical context. | **Blocked on product decision + day-engine posture API.** The Mode Separation prompt rule declares "The dashboard contains TWO parallel desks: a Swing Desk (multi-day) and a Day Desk (intraday). Each desk independently reports posture (Active / Monitor / Suppressed), top signals or suppression reason, and what would re-enable setups. It is valid for one desk to be Active while the other is Suppressed." Today `dashboard-page-content.tsx:13-25` deliberately loads `scannerSetupLoadMode: "swing"` only (the comment states "Top Signals are daily swing only, no day setups on the home surface") and `dashboard-redesign.tsx:506-510` filters `swingTopSignals` to `scanner_mode === "swing_daily"`. The empty-swing **Primary read** strip from B25 (`dashboard-redesign.tsx:250-265`, `1006-1028`) uses posture-style copy but only for swing — there is no parallel Day desk block. **Scope to ship a real Day Desk:** (1) **product decision** on whether the home page should surface intraday setups at all (today's home is intentionally a "calmer than the scanner" surface — adding a Day desk reverses that choice; this is a UX trade-off that needs explicit signoff before code lands), (2) **API surface** — `dashboard-page-content.tsx` would need a second `loadScannerDataWithoutBrief` call with `scannerSetupLoadMode: "day"` running in parallel (with its own cap + timeout), or a new `GET /v1/signals/day-posture` endpoint that returns the day-engine analog of swing's `Active / Monitor / Suppressed` posture without the full setup list payload, (3) **copy** — Day suppression vocabulary emphasizes "intraday confirmation, volume/momentum timing, session-specific conditions" (per the prompt's Mode-Aware Empty-State Language rule), distinct from Swing's "regime / sector alignment, structure readiness" — these need to be drafted at the same quality bar as B25's swing empty-state copy, not glued on, (4) **alignment ladder + watchlist readiness** — today's `buildAlignmentLadder` (`dashboard-redesign.tsx:603-612`) is swing-setup-count-driven; reusing it for the Day desk would silently violate engine independence; the day desk needs its own posture inputs. **Out of scope for B27 (the compliance refactor)** — adding a Day Desk is a new product surface, not a fix. **Tracked here** so the rule from `ASSISTANT_SYSTEM_PROMPT` does not silently drift back into violation. The assistant prompt rule is the SOURCE OF TRUTH; this row is the implementation tracker. |
| B30 | **Evidence-card composite mode routing + dual-engine product decisions** | Phase 1 Done 2026-05-12 · commit `f4ca874` · Phase 2 Done 2026-05-12 (mode-resolution helpers extracted with 12 lock-in tests) · Phase 3 Done 2026-05-12 (per-mode composite weight overrides — Suggestion 4 audit complete — schema + parser + resolver + 18 lock-in tests; proposed weights documented for operator rotation, **NOT shipped as default** — zero production behavior change today) · Phase 4 Done 2026-05-12 (gap-card mode-fit classifier + per-row Evidence routing — closes all three remaining Severity-2 items: classifier seam in `stocvest/signals/gap_intelligence.py`, per-row `resolveGapCardTradingMode` helper, "Best evaluated as" pill + InfoTip on gap cards, +19 lock-in tests). **All Severity-2 items now CLOSED.** Open follow-ups: automated weight-tuning proposal pipeline (new theme — agreed not to fully automate; build proposal-only with human-in-the-loop). | **Phase 1 (Done):** Wiring fix that closes the cross-mode data-leakage bug discovered during a user-prompted audit ("the gap cards and signal detail cards are looking exactly same for both swing and day trade"). The Signals page was correctly branching `tradingMode === "swing" ? "/composite/swing" : "/composite/real"`, but the **Scanner gap card "View Signal"**, **Scanner setup card "View Signal"**, and **Dashboard Swing Desk "View Evidence"** were hard-coded to `enrichEvidenceWithRealComposite(base)` (day engine), silently leaking VWAP / EMA9 / EMA20 / ORB chips and day-tier news / macro / geo lookback windows into swing-context modals; the chip-filter (`filterChipsForMode` on `body.mode === "swing"`) never fired because the response arrived tagged `body.mode = "day"` from the wrong route. Phase 1 introduces `enrichEvidenceWithComposite(evidence, mode)` (required `mode` argument so future call sites cannot omit it without a compile error), retires `enrichEvidenceWithRealComposite` as a `@deprecated` thin alias, wires the dashboard Swing Desk row to `"swing"`, wires the scanner top-level surface to `evidenceTradingMode = scannerSetupMode === "day" ? "day" : "swing"` (mirrors `panelNewsTradingMode` so news and composite stay in lockstep), wires the scanner setup card to a per-row `groupTradingMode` derived from `setupRenderGroups.group.key` so `scannerSetupMode === "both"` view routes each group's rows to the correct engine, and locks in the behavioral contract with +9 tests in `tests/signal-evidence-composite-mode-routing.test.ts` (positive route assertions, negative wrong-route assertions both directions, payload contract, error paths, deprecated-alias back-compat). Frontend baseline **315 → 324 passed (+9)**, backend **1061 / 3** unchanged. No Terraform / IAM / API contract / DynamoDB-schema changes. **Severity-2 product decisions deferred (not started):** (a) **"Both"-mode default for gap cards.** In `scannerSetupMode === "both"` view, a pre-market gap is environmental (mode-agnostic) but the trade plan you'd build on it is mode-specific. The current default-to-swing matches the existing `panelNewsTradingMode` resolution and is reversible. Options: **(a-i)** keep default-to-swing for the simpler / swing-first UX (current behavior); **(a-ii)** render two engines side-by-side in the Evidence modal — two Composite Score tiles, two reasoning blocks, two R/R rows — explicitly labelled "If you swing this" and "If you intraday this", matching the dashboard's two-desks-stacked philosophy. This is the most honest representation and the most work. Decision blocked on UX signoff. (b) **Gap card "Best evaluated as" tag.** Should the gap card itself carry a small `Best evaluated as: swing / intraday / either` tag so users see at a glance that the same event has different trade-plan implications across modes (e.g. a gap with a sustained catalyst is more swing-friendly; a gap with weak volume is more intraday-faded)? Cosmetic but informative. Decision blocked on UX signoff. (c) **Backend `mode` echo on gap-intel endpoint metadata.** Today the frontend invents a synthetic setup (`gapSyntheticSetup`) to feed `buildEvidenceFromSetup`; carrying mode through end-to-end via a small metadata field on `GapIntelligenceItem` would let the gap card stop synthesizing and pass real provenance. Low priority. (d) **Audit signal weights for mode-specific tuning.** Phase 1 confirmed both engines share `CompositeParameters` (layer weights 0.30/0.20/0.15/0.15/0.10/0.10 and verdict thresholds ±0.20) by design. Worth a separate audit pass on whether swing should weight macro/sector higher and weight news lower than day (intuitively yes — swing holds through more macro releases). Tracked separately as a future D-series tunable rotation candidate. |
| B29 | **Portfolio + Journal strategy_trading_mode attribution** | Not done | **Blocked on backend schema change.** The Mode Separation prompt rule declares "Portfolio: positions and actions carry mode attribution (Day position / Swing position). Day positions must NOT be interpreted using swing gates; swing positions must NOT be interpreted using intraday signals. Journal: every journal entry is associated with exactly one mode. Metrics, expectancy, streaks, and reviews filter by mode." **Current state — Portfolio:** `portfolio-page-client.tsx:36-45`, `119-167` flattens broker positions with columns Symbol / Qty / Cost / P&L / Earnings risk — no swing/day attribution. `order-entry-panel.tsx:47-64` `TradingModeBadge` is `paper | live` (execution mode), not swing/day (strategy mode) — a **naming collision** that compounds risk: `UserMePayload.trading_mode` (`contracts.ts:18`) is `paper | live`, while signals page `tradingMode` is `swing | day`. Two concepts, same field name, different surfaces — fix should resolve the namespace clash too (rename one of them, or add explicit `strategy_trading_mode` alongside `execution_mode`). `PortfolioOrderPrefill` (`order-entry-panel.tsx:11-20`) has no `strategy_trading_mode` field. `portfolio-multi-broker-panel.tsx` aggregate exposure has no mode dimension. **Current state — Journal:** `JournalEntryPayload` (`lib/api/contracts.ts:31-70`) has `is_day_trade` (PDT-style boolean for tax/regulatory tracking), `setup_type`, `strategy_tags` — but NO `trading_mode: swing | day` field. **`journal-page-client.tsx:160-168` hardcodes `is_day_trade: true` on every manual entry add** — wrong for swing manual logs AND wrong as engine attribution. Analytics endpoint `fetchJournalAnalytics` accepts no `mode` parameter; the page-level grid (`journal-page-client.tsx:211-236`, `239-274`) shows win rate / expectancy / streak / best-worst-setup as global numbers — a direct violation of "metrics filter by mode". **Scope to ship real mode attribution:** (1) **backend schema** — add `strategy_trading_mode: "swing" | "day" | null` to both `Position` and `JournalEntry` (nullable for legacy rows), with a backfill plan that derives mode from `signal_id` linkage where present and leaves it null elsewhere (don't fabricate attribution for legacy rows — surface "Unattributed" in the UI instead), (2) **API surface** — `GET /v1/portfolio/holdings` / `summary` / `allocation` return mode per position; `GET /v1/journal/entries` accepts `mode` filter; `GET /v1/journal/analytics` accepts `mode` filter and returns per-mode metrics (win rate / expectancy / streak / best-worst-setup); `POST /v1/orders/submit` accepts `strategy_trading_mode` on the order prefill and writes it to the resulting position + journal entry, (3) **frontend** — Portfolio gets mode chips on each position row, mode filter at the top of the table, and a calm "Unattributed" state for legacy rows; Journal page gets a Swing tab / Day tab pair (mirroring the validation ledger pattern from D2 Phase 3b) with per-mode analytics; manual entry add UI replaces the hardcoded `is_day_trade: true` with an explicit mode selector, (4) **execution-mode rename or namespace fix** — resolve the `trading_mode` naming collision so `paper | live` and `swing | day` are unambiguous in the codebase. **Out of scope for B27 (the compliance refactor)** — making either surface honour mode-isolation requires a backend schema migration with a backfill, not a frontend cleanup. **Tracked here** so the rule from `ASSISTANT_SYSTEM_PROMPT` does not silently drift back into violation. |

---

## D — Data, performance & moat

| ID | Theme | Status | Notes |
|----|--------|--------|--------|
| D1 | **Signal outcome pipeline** | Done 2026-05-02 · commit: 62b7e65 · **ledger columns 2026-05-08** | **Shipped:** `SignalRecord` + DynamoDB `SignalHistory` (GSI `scope_generated_at`), `signal_recorder.record_signal` / **`resolve_signals`** (Polygon **1m bar** close at T+1h / T+24h, **`get_evaluated_price_after_signal`**), `get_signal_history` / `get_by_id` / `public_signal_detail_dict`, optional **ledger** attributes (see **`docs/SIGNAL_ENGINE.md`** § Signal validation ledger), **`GET /v1/signals/me/history`** optional **`mode=day|swing`**, auto-record on composites when `symbol` + `price_at_signal` present, `GET /v1/signals/recent`, `GET /v1/signals/performance/summary`, **`GET /v1/signals/records/{signal_id}`**, **`GET /v1/signals/me/records/{signal_id}`**, BFF **`/api/stocvest/signals/**`**, dashboard Signal history + **`/dashboard/signal-validation`**. **Scheduled resolution:** EventBridge **`stocvest-signal-resolution`** → **`stocvest-development-api-signal_resolution`** (`infra/eventbridge.tf`); see `docs/D1_SIGNAL_RESOLUTION_SCHEDULE.md`. **Removed:** model-book **`portfolio_reversal`** / **`ModelPortfolio`** path (historical **`CONTEXT.md`** §3 row 6 note). Journal optional signal fields (B1). **Optional later:** weekly horizon, swing/day enrichment jobs writing MAE/MFE and exit labels. |
| D2 | **Historical signal validation** (formerly *Backtesting*) | Done 2026-05-11 | **Renamed** to mirror the assistant's verbatim disclosure phrase "Historical signal accuracy does not guarantee future results" and the existing `/dashboard/signal-validation` surface; "Backtesting" was avoided as a marketing claim. Scope is **measurement of real emitted signals**, not synthetic signal generation. **Phase 1 shipped 2026-05-11:** `stocvest/signals/historical_validation.py` — a pure aggregation module that takes a list of `SignalRecord` rows and produces a frozen `HistoricalValidationSummary` for one horizon (`1h` or `1d`), stratified by Decision state at entry (`actionable` / `monitor` / `blocked` / `unknown`), Macro regime at entry (`risk_on` / `neutral` / `risk_off` / `avoid` / `unknown` — engine vocabulary, matches what `regime_label_at_entry` actually stores), trading Mode (`swing` / `day`), Pattern family (`swing_composite` / `orb` / `vwap` / `momentum` / `gap_with_catalyst`; unknown patterns overflow into `other` so nothing silently vanishes), Trade Readiness bucket (`high` ≥ 70 / `moderate` 40–69 / `low` < 40 — same thresholds as the Evidence card), and Direction (`bullish` / `bearish` / `neutral`). `BucketStats` is **directional accuracy only** — `correct / (correct + incorrect)`, neutrals excluded from the denominator (matches the existing `GET /v1/signals/performance/summary` definition so the historical view and live tracked-outcomes view can never disagree on the same data); empty / all-neutral strata return `accuracy = NaN` so the UI renders "—" rather than a misleading "0%". Summary also carries `rows_examined` and the sorted set of `parameter_versions` observed so the UI can show users exactly which rules-bundle the number describes. Zero new I/O — operates on rows that `signal_recorder.resolve_signals` already populates with `outcome_1h` / `outcome_1d`. **Tests:** +14 in `tests/signals/test_historical_validation.py` covering empty / all-neutral / horizon-routing / decision / regime / mode / readiness / pattern-overflow / direction / parameter-version provenance / frozen-dataclass immutability. Backend baseline **942 → 956 passed (+14)**. **Phase 2 shipped 2026-05-11:** `stocvest/api/services/historical_validation_service.py` — thin service layer over the Phase 1 aggregator. Exposes a `HistoricalValidationService` class built on a narrow `SignalHistoryReader` Protocol (both `InMemorySignalRecorder` and `DynamoDBSignalRecorder` already satisfy it via their existing `get_signal_history(...)` signature, so production wiring is one constructor call and tests use a list-backed `_StubStore` double with no boto3). Takes an absolute `[from_at, to_at)` window — lower bound inclusive, upper bound exclusive, same semantics as the underlying GSI `scope_generated_at` predicate — translates it into the recorder's trailing-N-day convention (`days_back = ceil((now - from_at)/1d) + 1`, clamped at `MAX_LOOKBACK_DAYS = 366`), then post-filters to the requested window. Two methods: `summarize(user_id, from_at, to_at, horizon, mode=None, symbol=None) → HistoricalValidationSummary` returns the Phase 1 summary for the window directly; `summarize_by_parameter_version(...)` returns `{"__all__": <combined>, "v1": <...>, ...}` so Phase 3 can render a cross-version diff without recomputing — rows with missing or blank `parameter_version` land in `UNKNOWN_VERSION_KEY = "unknown"`. Inverted / zero-width windows short-circuit to an empty summary without touching the store. `user_id` / `symbol` / `mode` pass straight through to the store — the store is the auth perimeter and the service does not silently change scope. `MAX_ROWS_PER_QUERY = 1000` is a guardrail; cursor-based pagination for admin cross-user analytics is a Phase 3+ concern. **No HTTP route, no BFF route, no UI in this commit.** **No DynamoDB schema changes** — the existing GSI already supports the query. Tests: +14 in `tests/api/services/test_historical_validation_service.py` (empty store / inverted bounds / equal bounds / happy-path neutrals-excluded / inclusive-lower exclusive-upper edge cases / naive-`generated_at` UTC normalization / `user_id` `symbol` `mode` passthrough / `days_back` computation / `MAX_LOOKBACK_DAYS` clamp / horizon routing / per-version buckets + `__all__` aggregate / `unknown` bucket / empty-window only-`__all__` / window-filter-before-version-split). Backend baseline **956 → 970 passed (+14)**. **Phase 3a shipped 2026-05-11:** auth-gated `GET /v1/signals/historical-validation/summary` HTTP surface on the existing signals Lambda. New handler `historical_validation_summary_handler` in `stocvest/api/handlers/signals.py` requires `rc.user_id` (every query is scoped to the caller — `test_scope_isolates_users` locks the security boundary). Query params: `horizon` ∈ `{"1h","1d"}` (required), `from` / `to` as ISO-8601 datetimes (required; trailing-`Z` accepted, naive treated as UTC), optional `mode` ∈ `{"swing","day"}`, optional `symbol`, optional `by_version` ∈ `{"1","true","yes"}`. Returns either a single `summary` (default) or a `by_parameter_version` map containing one bucket per version plus a synthetic `__all__` (when `by_version=true`), plus echoed window parameters and the new `HISTORICAL_VALIDATION_DISCLAIMER` constant from `stocvest/api/legal_copy.py` (single-sourced wording). JSON safety: `BucketStats.accuracy` NaN is converted to `None` at serialization so the wire response is valid JSON (default `json.dumps` would emit the literal `NaN` and crash browsers). Validation 400s for invalid / missing `horizon`, unparseable / missing `from` / `to`, and inverted window (`to <= from`). No auth → 401. Terraform: one new `aws_apigatewayv2_route.http` row (`"GET /v1/signals/historical-validation/summary" = { module_key = "signals", authorizer = true }`); `plan` showed exactly `1 to add, 0 to change, 0 to destroy`; `apply` succeeded 2026-05-11. No new tables, no new Lambdas, no new IAM. Tests: +14 in `tests/api/handlers/test_signals_historical_validation.py` (auth requirement, all 4xx validation paths, trailing-Z ISO acceptance, happy path with disclaimer, **empty-window NaN → null JSON serialization** (asserts `accuracy is None` AND raw body contains no `"NaN"` token), per-user scope isolation, `mode` / `symbol` passthrough, horizon `1d` routing, `by_version=true` per-version map + `__all__` bucket). Backend baseline **970 → 984 passed (+14)**. **Phase 3b shipped 2026-05-11:** UI surface on `/dashboard/signal-validation` as a new top-level "Historical accuracy" view tab. Three pieces ship together: (1) BFF proxy `frontend/app/api/stocvest/signals/historical-validation/summary/route.ts` — thin `stocvestAuthedFetch` pipe, forwards the JWT cookie + query string verbatim. (2) Typed client `frontend/lib/api/historical-validation.ts` — `fetchHistoricalValidationSummary` / `fetchHistoricalValidationByVersion` + matching TS interfaces, defensive manual type-guards (matches the existing `public-signals.ts` style, no Zod added), `null` accuracy preserved verbatim from the wire so the UI never shows a misleading 0%, lowercase tickers upcased before send, 401 routes through `surfaceAuthErrorIfAny` so the sliding-session refresh flow works uniformly, malformed responses (missing `summary`, missing `__all__`, non-JSON body) collapse to `null` so the dashboard never crashes on backend drift. (3) Component `frontend/components/historical-validation-panel.tsx` — filter row (Horizon `1h`/`1d`, Window `30`/`60`/`90` days, Mode `all`/`swing`/`day`), Overall hero card with "X correct of Y resolved (neutrals excluded)" subtitle and `parameter_versions` provenance, responsive 6-card stratification grid with engine-vocabulary→friendly-label translation (`risk_on`→"Risk-on", `swing_composite`→"Swing composite", etc.) and a calm accent progress bar per bucket, and the verbatim `HISTORICAL_VALIDATION_DISCLAIMER` from the backend response at the bottom. Wired into `SignalValidationPageClient` as a new `view: "ledger" | "historical"` top-level tab pair above the existing swing/day ledger UI; the ledger fetch is skipped when the user is on the historical view (saves a round-trip), and `usePublishAssistantContext` only forwards `trading_mode` on the ledger view (the historical view has its own internal mode filter). Tests: +12 in `tests/historical-validation-api.test.ts` (happy path with asserted query-string contents, `mode`+lowercase-`symbol` passthrough, `null` accuracy preserved end-to-end, 401 routes through `surfaceAuthErrorIfAny` and returns `null`, malformed/non-JSON bodies return `null`, `?by_version=true` per-version map parses with `__all__` aggregate, missing `__all__` returns `null`, `formatAccuracyPercent` renders "—" for null/NaN/Infinity and one-decimal percent for finite values, `buildTrailingWindow(30)` produces a 30-day delta). Frontend baseline **176 → 188 passed (+12)**, backend **984/3** unchanged. `tsc --noEmit` zero new errors. No Terraform changes, no backend changes, no new dependencies. **Phase 3c-1 shipped 2026-05-11** — public mirror on `/performance` exposing only `overall` + `by_mode` to unauthenticated visitors (per-symbol queries rejected with 400; per-decision / per-regime / per-pattern / per-readiness / per-direction stratifications stripped at the response layer to honor the LOGGED-OUT golden rule). See the header summary at the top of this file for the full breakdown. **Phase 3c-2 shipped 2026-05-11:** assistant read-only awareness of the user-scoped summary inside the LOGGED-IN flow. `stocvest/api/handlers/signals.py::assistant_chat_handler` now fetches the user's Phase 2 summary on every authenticated chat turn — `HistoricalValidationService(get_signal_recorder()).summarize(user_id=rc.user_id, horizon="1d", from_at=now-90d, to_at=now)` — same defaults as the public mirror (trailing 90 days, 1d horizon) but **scoped to the calling user** instead of platform-wide. Fetch failures are caught and swallowed (defensive try/except) so a slow / unavailable signal-history store cannot break the chat reply; on failure the handler forwards `historical_validation_summary=None` to the service and the prompt's "if absent, do not comment" rule activates. `stocvest/signals/assistant_chat.py` grew a new `serialize_historical_validation_summary(summary, *, window_days)` helper and a new optional `historical_validation_summary` / `historical_validation_window_days` kwarg pair on `AssistantChatService.reply(...)`; the serializer emits a compact tail block `=== HISTORICAL VALIDATION ===\nwindow_days=90\nhorizon=1d\noverall=<pct>% (<correct> correct of <resolved> resolved; <neutral> neutral; <total> total)\nswing=...\nday=...\nrows_examined=<n>` and the chat service appends it to the system message after `serialize_page_context(...)`. The serializer is **deliberately trimmed** — only `overall` + `by_mode` + `rows_examined` + `horizon` reach the LLM (same projection as the public `/performance` mirror), no per-decision / per-regime / per-pattern / per-readiness / per-direction stratifications; the assistant's job is the framework-level summary, not the stratified detail view (the dashboard already renders that for the user). Edge cases the serializer handles: `summary is None` → empty string (no block); `summary.rows_examined == 0` → empty string (no block); NaN accuracy (no resolved-non-neutral trades in the window) renders as the em-dash `—`, **never** as `"0.0%"` and **never** as the literal `"NaN"` string; empty `by_mode` buckets are omitted from the serialized text (Phase 1's aggregator pre-seeds the vocabulary so swing/day always appear as keys even with zero rows, but the serializer drops them to avoid the LLM paraphrasing zero-rows as "your day track has no data"). `stocvest/signals/assistant_prompts.py::ASSISTANT_SYSTEM_PROMPT` grew a new `HISTORICAL VALIDATION CONTEXT (LOGGED-IN ONLY)` section between `BACKTESTING & VALIDATION (STRICT RULES)` and `USER INTERACTION RULES`; the section references the `=== HISTORICAL VALIDATION ===` sentinel so the LLM knows what to pattern-match on, explicitly tells the LLM to read the em-dash as "no resolved trades yet" and never as "0%", and enumerates the allowances and bans: **MAY** quote `overall` with the resolved-count denominator alongside the standing disclaimer, note the swing-vs-day shape in qualitative terms when both lines are present, reference the sample size as a small-window caveat, redirect users asking for stratified detail to `/dashboard/signal-validation`; **MUST NOT** translate accuracy into dollar P&L / expected returns / win-rates ("Directional accuracy is NOT a return number"), predict whether the trend will continue or mean-revert ("descriptive, not predictive"), compare to "the market" / other users / a benchmark, recommend mode choice from accuracy ("Mode choice is not an advice surface"), use the figures to "promote" the system or defend against skepticism ("evidence of behavior, not promotion"), discuss per-symbol / per-pattern / per-regime / per-decision-state / per-readiness-bucket / per-direction performance ("deliberately withheld from your context"), or reference any validation figures when the block is absent from the turn ("No block means no comment; never invent a number, never recall a number from a previous turn"). The public path (`AssistantChatService.reply_public`) is untouched — it refuses the new kwarg by signature (existing lock-in test still passes), never carries the sentinel block (new lock-in test asserts the appended tail after `=== PAGE CONTEXT ===` contains no `window_days=` / `rows_examined=` / `overall=` field-syntax wording), and the `session_mode=public` marker still routes the LLM into PUBLIC MODE RULES where per-user numbers cannot be discussed regardless. **No Terraform changes, no new tables, no new Lambdas, no new IAM, no frontend code changes, no BFF route changes, no new dependencies** — Phase 3c-2 is purely a backend chat-composition change reusing the Phase 2 service. **Tests: +21** distributed across three files. **+9 prompt lock-in tests** in `tests/signals/test_assistant_prompts.py` (section header present, sentinel referenced, em-dash semantics spelled out, dollar-P&L ban with "NOT a return number" load-bearing clause, trend-prediction ban with "descriptive, not predictive", per-symbol/pattern/regime/decision/readiness/direction stratification ban with `/dashboard/signal-validation` redirect, system-promotion ban with "evidence of behavior, not promotion", "no block means no comment" + "never invent a number" + "never recall a number from a previous turn", market/other-user comparison ban, mode-choice-from-accuracy ban). **+8 service-level tests** in `tests/signals/test_assistant_chat_service.py` (None → empty string, zero-rows → empty string, happy path renders header + window_days + horizon + denominator-transparent overall + per-mode lines + rows_examined, NaN renders em-dash never 0.0% never "NaN", empty by_mode buckets omitted, authenticated `reply()` appends block to system message when summary provided, authenticated `reply()` omits field-syntax wording when summary=None, public `reply_public()` never carries the block AND refuses the kwarg by signature, no-neutrals overall line still emits the count for transparency). **+2 handler-level tests** in `tests/api/handlers/test_signals_assistant_chat.py` (handler resolves a `HistoricalValidationService` against the live recorder, calls `summarize(user_id=rc.user_id, horizon="1d", from_at=now-90d, to_at=now)` with the 90-day window confirmed within 60s jitter, forwards the exact summary object to `svc.reply(historical_validation_summary=...)`; service raises → chat turn still returns 200 with `historical_validation_summary=None` forwarded). The 3 pre-existing handler tests that monkey-patch `AssistantChatService.reply` were updated to use `**_kwargs` in the fake signature so future kwarg additions do not break them. **Detection sharpness:** the prompt itself now references the `=== HISTORICAL VALIDATION ===` sentinel as a literal string so the LLM can anchor on it — to detect whether the *serialized block* was actually appended (vs just referenced in the prompt rules), tests look at the appended tail after `=== PAGE CONTEXT ===` and check for the field-syntax wording (`window_days=`, `rows_examined=`, `overall=`) that only the serializer emits. Backend baseline **994 → 1015 passed (+21)**, frontend **194** unchanged (no frontend code touched), `tsc --noEmit` zero new errors (24 pre-existing), `npm run build` green. **Phase 4 (planned):** cross-version diff view (parameter_version A vs. B) so the validation page can show the effect of a rules change without ever simulating new signals.  **Phase 4 shipped 2026-05-11:** cross-version diff view on `/dashboard/signal-validation`'s Historical accuracy tab. Purely a frontend slice — **no backend, no Terraform, no BFF, no IAM, no new tables, no new Lambdas, no new dependencies**. Reuses Phase 3a's `GET /v1/signals/historical-validation/summary?by_version=true` endpoint and Phase 3b's `fetchHistoricalValidationByVersion` client unchanged. Four new pure helpers in `frontend/lib/api/historical-validation.ts`: (1) `diffBucketStats(a, b)` → `BucketDelta` with `accuracyDelta` / `totalDelta` / `resolvedDelta` / `neutralDelta` / `smallSampleA` / `smallSampleB`; `B − A` subtraction across counts, `accuracyDelta = null` whenever either side's `accuracy` is `null` so the UI renders the em-dash rather than a misleading `0.0pp`, missing-on-one-side bucket (a stratum may exist in version A's data but not B's, e.g. a pattern family that only fires under the new rules) is treated as zero-rows + null-accuracy so the diff math has uniform inputs. (2) `formatAccuracyDelta(delta)` → `"+5.2pp"` / `"-3.1pp"` / `"0.0pp"` (exact zero is a real measurement, never an em-dash) / `"—"` (null / NaN / Infinity). Units are **percentage points**, not "percent of percent". (3) `selectComparableVersions(map)` excludes the synthetic `__all__` aggregate (offering it would create a misleading "compare a version against the average that includes it" scenario), sorts `v<n>` keys numerically so `v10` follows `v9` not `v1`, falls through to alphabetic for unknown shapes, always places `unknown` last so legacy unstamped rows are an explicit user choice never a default. (4) `defaultCompareSelection(map)` picks the last two comparable versions as the "latest pair of changes" lens; returns `null` when fewer than two comparable versions exist so the panel renders a calm "need at least two parameter versions" empty state. Three exported constants: `SMALL_SAMPLE_THRESHOLD = 10` (the resolved-non-neutral count below which a bucket gets the "small sample" annotation), `ALL_VERSIONS_KEY = "__all__"` and `UNKNOWN_VERSION_KEY = "unknown"` (so tests and components reference the magic strings without typo risk). `frontend/components/historical-validation-panel.tsx` grew an inline **Compare versions** checkbox (off by default — the panel still opens to the calm single-version view; the user opts into the more detailed A-vs-B view). When ON, the panel swaps `fetchHistoricalValidationSummary` for `fetchHistoricalValidationByVersion`, persists the user's explicit A / B selections across horizon / window / mode changes (only re-defaulting when one of the chosen versions has fallen out of the new window), and renders a new `CompareBody` component: two `<select>` dropdowns (defaulting to the latest comparable pair via `defaultCompareSelection`), an A / B / Δ tri-column overall hero card (`CompareOverallCard` + `OverallColumn` + `DeltaColumn`), a stratification grid with `CompareStratificationCard`s that union the keys across the two versions (a pattern family that only fired on one side still shows as a row with `—` on the side it did not fire), `CompareBucketRow`s showing A's accuracy + `(correct/resolved)`, B's accuracy + `(correct/resolved)`, and the Δ on the right (right-aligned monospace). Empty / not-meaningful states are inline: fewer than two comparable versions renders "Need at least two parameter versions in this window to compare — widen the window to a longer trailing period to span a rules change"; same-version-on-both-sides renders an italic "pick a different version on either side to see a delta" banner. Small-sample annotation: when either side of a row has fewer than `SMALL_SAMPLE_THRESHOLD = 10` resolved-non-neutral signals in that stratum, a muted "small sample" pill attaches to that side's cell (title tooltip explains the threshold) without obscuring the delta itself. Color discipline matches the rest of the validation surface — no red/green (accuracy isn't profit), all calm accent tints and monospace deltas. The same verbatim `HISTORICAL_VALIDATION_DISCLAIMER` from the backend response renders below the compare body so the wording cannot drift between the single-version and compare views. **The assistant chatbot is deliberately NOT wired to consume cross-version diffs** — parameter_versions are an engine implementation detail; surfacing them through the LLM would be a leaky abstraction. The Phase 3c-2 user-scoped historical summary already covers the LLM's needs. Tests: +14 in `tests/historical-validation-api.test.ts` covering all four new helpers (5 for `diffBucketStats`: happy-path B-minus-A, null-accuracy-either-side → null delta, identical buckets → all-zero, missing-on-one-side → zero-rows-with-null-accuracy fallback, small-sample flagged independently per side; 4 for `formatAccuracyDelta`: signed percentage-points for finite deltas, `0.0pp` for exact zero never em-dash, em-dash for null/NaN/Infinity, one-decimal precision; 3 for `selectComparableVersions`: excludes `__all__`, sorts `v<n>` numerically, places `unknown` last; 2 for `defaultCompareSelection`: picks last two with `unknown`-as-default-B sanity check, returns null when fewer than two comparable versions). Backend baseline **1015/3 unchanged** (no backend code touched), frontend baseline **194 → 208 passed (+14)**, `tsc --noEmit` 24 errors unchanged (pre-existing baseline, zero new), `npm run build` green. **D2 functionally complete end-to-end** (Phase 1 pure aggregator → Phase 2 service layer → Phase 3a auth API → Phase 3b dashboard tab → Phase 3c-1 public mirror → Phase 3c-2 assistant awareness → Phase 4 cross-version diff). **Explicit non-goals:** win-rate / expectancy / dollar P&L (product never publishes these); re-resolution at synthetic horizons (T+5d, T+10d, T+30d) using simulated bars; anything that frames validation as a forecast. |
| D3 | **Weight / prompt iteration** | Done 2026-05-11 (P2 deferred) | **2026-05-04:** Schema + storage shipped — `stocvest/config/signal_parameters.py` (defaults), Secrets Manager **`stocvest/signal-parameters`**, **`ParameterStore`** (5m cache), DynamoDB **`ParameterHistory`**, **`SignalRecord`** snapshot JSON columns + **`parameter_version`**, **`GET /v1/signals/analysis`** (admin/internal), `docs/TUNING_PLAYBOOK.md`, scripts `init_signal_parameters.py` / `update_parameters.py`. **First slice shipped 2026-05-11 (pending commit) — composite-engine parameter wiring:** the two "real" engines (`stocvest/api/services/swing_composite_engine.py` for `POST /v1/signals/composite/swing` and `stocvest/api/services/real_composite_engine.py` for `POST /v1/signals/composite/real`) had been threading `params.composite.{technical,news,macro,sector,geopolitical,internals}_weight` + `bullish_threshold` + `bearish_threshold` from `ParameterStore` since 2026-05-04, but the legacy `swing_composite_handler` in `stocvest/api/handlers/signals.py` (the thin `POST /v1/signals/swing/composite` path that accepts pre-computed client-supplied per-layer scores) was still calling `CompositeScoreEngine()` with no arguments — using the hardcoded `DEFAULT_BASE_WEIGHTS` constants from `composite_score.py` — while simultaneously stamping `parameter_version = ParameterStore.get_parameters_sync().version` onto every recorded `SignalRecord`. The metadata claimed "rules vN" but the engine actually computed against constants that differ from `CompositeParameters` defaults on **five of six layers** (news 0.18 vs 0.20, sector 0.12 vs 0.15, macro 0.16 vs 0.15, geopolitical 0.12 vs 0.10, internals 0.12 vs 0.10). Phase 4's cross-version diff view from `bde04e4` would have surfaced this drift as a phantom "v1 vs v2" delta even when nothing had actually changed in the rules bundle, because the recorded version string was untethered from the weights that produced the row. **Implementation:** new `build_composite_score_engine_from_params(params: SignalParameters) -> CompositeScoreEngine` helper in `stocvest/signals/composite_score.py` is the single source of truth for translating active `SignalParameters` into a live `CompositeScoreEngine` — reads `params.composite.{technical|news|macro|sector|geopolitical|internals}_weight` + `bullish_threshold` + `bearish_threshold`, coerces ints to floats so a `technical_weight: 1` (int) in the Secrets Manager JSON cannot crash the engine. All three production callsites converge on this helper: `swing_composite_handler` (previously unwired; now reads `active_params = ParameterStore.get_parameters_sync()` once and uses the same snapshot for both the engine and the version stamp, eliminating the operator-rotates-secret-between-two-reads race as a bonus), `swing_composite_engine.py` (replaced the inline `base_weights = {...}` dict + four-arg `CompositeScoreEngine(...)` constructor with the one-line helper call), `real_composite_engine.py` (same simplification). The legacy `DEFAULT_BASE_WEIGHTS` constant remains in `composite_score.py` as a documented test/no-args fallback so the existing engine-unit tests (`tests/signals/test_composite_score.py`, `test_composite_scorer.py`, `test_risk_factors.py`, `test_risk_reward.py`, `test_ai_synthesis.py`, `test_swing_composite_evidence.py`, `test_signal_engine_integration.py`) keep passing unchanged — the helper docstring explicitly states this contract so future readers do not mistake the legacy constants for a production fallback. **Tests:** +6 backend lock-ins. **4 in `tests/signals/test_composite_score.py`** — (1) helper produces weights matching `params.composite.*_weight` AND explicitly NOT matching `DEFAULT_BASE_WEIGHTS` (regression guard against the drift coming back), (2) helper threads `bullish_threshold` / `bearish_threshold` from `params.composite` rather than the `CompositeScoreEngine.__init__` defaults, (3) identical signals payload through two different `SignalParameters` instances (default vs tuned with `technical_weight=0.60`) produces detectably different composite scores AND the verdict actually flips from neutral to bullish (the wire-is-live test that would catch any silent fallback to `DEFAULT_BASE_WEIGHTS`), (4) integer weights in `CompositeParameters` are coerced to float by the helper so all `engine._base_weights` values pass `isinstance(value, float)`. **2 in `tests/api/test_signal_recorder.py`** — (5) `swing_composite_handler` stamps the same `parameter_version` whose `.composite.*` weights it just used (end-to-end test that monkeypatches `ParameterStore.get_parameters_sync` to return a `SignalParameters` with `version="9.9.9-d3-test"` and bumped weights, then asserts the stored `SignalRecord.parameter_version == "9.9.9-d3-test"` — closes the integrity gap), (6) identical payload through two different parameter versions produces different `response_body["score"]` values (proves the legacy handler is no longer reading hardcoded `DEFAULT_BASE_WEIGHTS` regardless of `parameter_version`). Backend baseline **1015 → 1021 passed (+6, zero regressions, 3 skipped unchanged)**, frontend **208** unchanged (no frontend code touched — pure backend slice). `tsc --noEmit` zero new errors (24 pre-existing). **No Terraform, no IAM, no API contract changes, no new dependencies, no DynamoDB schema changes.** **D3 first slice closes the data integrity contract for Phase 4's cross-version diff view** — operators can now actually rotate `stocvest/signal-parameters` in Secrets Manager and the resulting `SignalRecord` rows will carry an honest `parameter_version` that reflects the weights that produced them. **Second slice shipped 2026-05-11 (this commit) — analyzer wire-up lock-in:** the audit of `stocvest/signals/{technical,news,macro,sector,swing_technical}_analyzer.py` revealed that **every analyzer already accepts its `*Parameters` block as a positional `params: <T>Parameters` argument** and reads every relevant field — `TechnicalAnalyzer.analyze(symbol, bars, snapshot, params: TechnicalParameters, *, adv)` reads `rsi_period` / `rsi_overbought` / `rsi_bullish_zone` / `rsi_oversold` / `rsi_bearish_zone` / `rsi_strong_delta` / `rsi_moderate_delta` / `ema_fast_period` / `ema_slow_period` / `ema_score_delta` / `ema_crossover_bonus` / `atr_period` / `orb_buffer_pct` / `orb_score_delta` / `orb_atr_qualification_ratio` / `volume_lookback_bars` / `volume_surge_multiplier` / `volume_amplifier` / `vwap_score_delta` / `bullish_threshold` / `bearish_threshold`; `NewsAnalyzer.analyze(symbol, articles, params: NewsParameters, *, lookback_hours, mode, benzinga_data)` reads `recency_1h_weight` / `recency_4h_weight` / `recency_8h_weight` / `recency_old_weight` / `direct_mention_weight` / `indirect_mention_weight` / `max_articles` / `lookback_hours` / `bullish_threshold` / `bearish_threshold`; `MacroAnalyzer.analyze(spy_snap, qqq_snap, vix_snap, events, params: MacroParameters, *, events_lookback_days, macro_context)` reads `vix_high` / `vix_elevated` / `vix_normal` / `vix_low` / `vix_extreme_score` / `vix_high_score` / `vix_elevated_score` / `vix_normal_score` / `vix_low_score` / `vix_falling_bonus` / `vix_rising_penalty` / `vix_trend_threshold_pct` / `event_today_score` / `no_event_score` / `momentum_weight` / `volatility_weight` / `event_weight` / `bullish_threshold` / `bearish_threshold`; `SectorAnalyzer.analyze(symbol, sector_snap, spy_snap, params: SectorParameters, *, ...)` reads `strong_outperform` / `moderate_outperform` / `moderate_underperform` / `strong_outperform_score` / `moderate_outperform_score` / `inline_score` / `moderate_underperform_score` / `strong_underperform_score` / `absolute_up_threshold` / `absolute_down_threshold` / `absolute_adjustment` / `bullish_threshold` / `bearish_threshold`; `SwingTechnicalAnalyzer.analyze(symbol, daily_bars, snapshot, params: SwingTechnicalParameters)` reads `sma_fast_period` / `sma_slow_period` / `rsi_period` / `rsi_bullish_zone` / `rsi_score_delta` / `above_sma50_score` / `above_sma200_score` / `higher_highs_lows_score` / `volume_accumulation_score` / `near_52w_high_score` / `base_formation_score` / `base_min_days` / `base_max_days` / `base_max_range_pct` / `volume_lookback_days` / `bullish_threshold` / `bearish_threshold`. All three production composite engines (`swing_composite_engine.py`, `real_composite_engine.py`, `swing_composite_handler` after the first slice) thread the live `ParameterStore.get_parameters_sync()` params blocks through to every analyzer call — `SwingTechnicalAnalyzer().analyze(sym, daily_bars, snap_for_tech, params.swing_technical)`, `NewsAnalyzer().analyze(sym, news_rows, params.news, mode="swing", benzinga_data=bz_data)`, `MacroAnalyzer().analyze(spy_snap, qqq_snap, vix_snap, econ, params.macro, ...)`, `SectorAnalyzer().analyze(sym, sector_snap, spy_snap, params.sector, ...)`, `InternalsAnalyzer().analyze(vix_snap, spy_snap, qqq_snap, params.macro)`, and `params.swing_geo_lookback_hours` / `params.swing_technical.daily_bars_lookback` / `params.composite.min_available_layers` are also read live. `ledger_position_monitor.py` reads `ParameterStore.get_parameters_sync().macro` to determine the current `market_regime`. **The remaining D3 work was therefore not new wire-up code at all — it was just lock-in tests** that prove the wires stay live across future refactors, because today's positional-argument signature on every `analyze(...)` method enforces "you cannot call this analyzer without supplying a params block" (Python `TypeError` at call time) but does NOT enforce "the params block you supplied came from `ParameterStore`" (a refactor could regress to `params=<T>Parameters()` defaults and Python would not complain). **Tests: +8** wire-is-live lock-ins, all of the form "same input data + two `<T>Parameters` instances differing in one field → analyzer output differs in the expected direction": **2 in `tests/signals/test_technical_analyzer.py`** (`test_bullish_threshold_param_actually_drives_verdict` — same uptrending 60-bar window through `TechnicalParameters(bullish_threshold=55, bearish_threshold=45)` vs `TechnicalParameters(bullish_threshold=99, bearish_threshold=1)` produces identical raw score but differing verdict; `test_vwap_score_delta_param_actually_moves_score` — same uptrend through `vwap_score_delta=20` (default) vs `vwap_score_delta=0` measurably shrinks the score's absolute distance from neutral 50), **2 in `tests/signals/test_macro_analyzer.py`** (`test_bullish_threshold_param_actually_drives_verdict` — same risk-on SPY/QQQ/VIX snapshot through default vs `bullish_threshold=99` produces identical score but flips "bullish" → "neutral"; `test_vix_score_band_params_actually_move_score` — same low-VIX (13.0) snapshot through `vix_low_score=80` (default) vs `vix_low_score=40` strictly lowers the macro composite score), **2 in `tests/signals/test_sector_analyzer.py`** (`test_bullish_threshold_param_actually_drives_verdict` — same SOXX-outperforming-SPY snapshot through default vs `bullish_threshold=99` produces identical score but differing verdict; `test_strong_outperform_score_param_actually_moves_score` — same +3.0% SOXX vs +0.5% SPY through `strong_outperform_score=80` (default) vs `strong_outperform_score=60` strictly lowers the sector score, locking in that the relative-strength scoring table is param-driven), **2 in `tests/signals/test_swing_technical_analyzer.py`** (`test_bullish_threshold_param_actually_drives_verdict` — a clean 210-bar daily uptrend saturates at score=100; the verdict flips from "bullish" at default `bullish_threshold=60` to "neutral" at `bullish_threshold=101` (unreachable threshold); `test_above_sma50_score_param_actually_moves_score` — zeroing every scoring contribution in `SwingTechnicalParameters` collapses the score to the exact neutral baseline of 50, AND bumping `above_sma50_score` from 0 to 30 on an uptrend where `price > SMA50` adds exactly that 30-point bonus producing score=80, locking in BOTH that the swing baseline math is honest AND that the above-SMA50 contribution is param-driven). `NewsAnalyzer` already has the analogous `test_params_threshold_used` from earlier work — all five analyzers now have at least one wire-is-live lock-in test. Backend baseline **1021 → 1029 passed (+8, zero regressions, 3 skipped unchanged)**, frontend **208** unchanged (no frontend code touched — pure backend slice). `tsc --noEmit` zero new errors (24 pre-existing). **No Terraform, no IAM, no API contract changes, no new dependencies, no DynamoDB schema changes, no production code changes** — purely test additions confirming that the existing production code is wired correctly. **D3 functionally complete:** every `*Parameters` block in `SignalParameters` (Technical, News, Macro, Sector, SwingTechnical, Composite) is read from `ParameterStore.get_parameters_sync()` and threaded through to the analyzer / engine that owns it, and a refactor that bypasses the params arg now trips a deterministic regression test. **Deferred to P2 (NOT in this lane):** parameterized Claude prompts in Secrets Manager (`assistant_prompts.py::ASSISTANT_SYSTEM_PROMPT` is currently a Python string constant — moving it to Secrets so prompt iteration becomes an ops action rather than a deploy is a separate ergonomic improvement, not a tuning blocker; D2's parameter_version-stamping work is unaffected because prompts are an LLM-side concern, not a signal-engine concern). **Defined-but-not-yet-used tunables (acknowledged technical debt, not D3-blocking):** `TechnicalParameters.pdh_pdl_score_delta` and `volume_adv_surge_multiplier`, `MacroParameters.event_tomorrow_score`, `SwingTechnicalParameters.ema_period` / `rsi_overbought` / `rsi_oversold` — these are aspirational fields wired into the dataclass but not read by any analyzer today; left in place for forward compatibility (a future tuning pass can wire them without a `signal_parameters.py` migration). |
| D4 | **Audit trail** | In progress 2026-05-07 | **Shipped slice:** HTTP/API **replay audit** — `AuditEvent`, `audit_store.py` / `audit_capture.py`, DynamoDB **`AuditEvents`** when **`DYNAMODB_AUDIT_EVENTS_TABLE`** set, `_with_cors_and_audit` on **`lambda_dispatch`**, admin **`GET /v1/admin/audit/users/{user_id}`** + **`GET /v1/admin/audit/sessions/{session_id}`**, extra row on beta toggle, tests **`tests/api/test_audit_capture.py`**, Terraform **`AuditEvents`** + env. **Still pending:** immutable **order/PDT/broker** archival, long-term retention (e.g. **S3 Glacier**), counsel-approved policy — extend beyond HTTP summaries. |
| D5 | **Anonymized behavioral data policy** | Not done | Operationalize user deletion → anonymize behavioral rows; document in Privacy; engineering checklist only—**no** duplicate of legal disclaimers in `CONTEXT.md` §4. |
| D6 | **Dynamic per-layer reasoning in swing composite** | Done 2026-05-04 | Added backend per-layer `reasoning` generation in `swing_composite_handler` contributions and wired frontend layer breakdown to consume dynamic reasoning when present instead of static generic text. |
| D7 | **Multi-source news ingestion (Benzinga + SEC EDGAR)** | Done 2026-05-05 · commit: pending | **Combined pipeline shipped:** `stocvest/data/edgar_client.py` polls SEC 8-K Atom feed with SEC-compliant User-Agent, resolves CIK→ticker via cached `company_tickers.json`, and maps filings into canonical `NewsArticle`; `stocvest/data/news_triage.py` performs fast keyword/category/watchlist/duplicate filtering before Claude; `stocvest/data/polygon_client.py` now includes `BenzingaNewsStream`; `stocvest/workers/news_worker.py` runs Benzinga + EDGAR together as one long-running ECS process; `stocvest/workers/news_consumer_lambda.py` consumes `stocvest-news-triage-queue`, scores via Claude, and best-effort stores scored rows in Redis. **Infra:** `infra/news_pipeline.tf`, `infra/ecs_news_worker.tf`, `infra/lambda_6e.tf`, `stocvest/api/lambda_dispatch.py`, Docker entrypoint under `docker/news_worker/Dockerfile`. **Acceptance:** one worker process, EDGAR bypasses triage, Benzinga passes triage, SQS decouples ingestion from scoring. |
| D8 | **News + geopolitical reliability hardening** | Done 2026-05-07 · commit: PENDING_HASH | **Goal:** eliminate chronically blank News/Geo layers in composite requests. **Shipped:** `stocvest/data/ticker_name_resolver.py` (SEC `company_tickers.json` variants), `stocvest/data/benzinga_client.py` (news/WIM/ratings/guidance/earnings with Polygon fallback), composite engines merge Benzinga data before layer scoring, `NewsAnalyzer` now returns meaningful neutral available states (not unavailable) for no-news windows with recent analyst/guidance/earnings adjustments, `GeoAnalyzer` adds structural baseline + cached daily themes, `stocvest/workers/geo_themes_updater.py` stores Perplexity themes in Redis (`stocvest:geo_themes:today`), and scheduler hook via `stocvest_job=geo_themes_update` in `signal_resolution` handler. **Tests:** `tests/signals/test_news_analyzer.py`, `tests/signals/test_swing_news_lookback.py`, `tests/signals/test_geo_analyzer.py`, `tests/workers/test_geo_themes_updater.py`, `tests/data/test_ticker_name_resolver.py`, `tests/utils/test_config.py`. |
| D9 | **SIC → sector mapping confidence tiers** | Done 2026-05-08 | **`stocvest.signals.sector_sic_fallback`:** **`SicMappingTier`** enum; **`resolve_sector_bucket_from_sic`** returns **`(bucket, tier)`** (`exact` → **`SIC_TO_SECTOR`**, `prefix` → 3-digit table, `coarse` → 2-digit OSHA-style proxy, `fallback_spy` → default/SPY path incl. exclusions **9999** etc.). **`SectorMapper.get_sector_etf`** returns a fifth value (**`SicMappingTier | None`** for pending cache refresh). Sector resolution **logs** include **`sic_mapping_tier`** when known. Day + swing composites pass optional **`sic_mapping_tier`** into **`sector_layer_api_extras`** (see **`docs/API_CONTRACTS.md`** §4.3). **Tests:** `tests/signals/test_sector_sic_fallback.py`, `test_sector_mapper.py`, `test_sector_sic_mapping.py`, `tests/api/test_swing_composite.py` (mock 5-tuple). |
| D10 | **Automated weight-tuning proposal pipeline (human-in-the-loop)** | Phase 1 Done 2026-05-12 (foundation: `ParameterProposal` DDB table + `parameter_proposal_store.py` repo + 24 lock-in tests; ships dark, zero behavior change) · Phase 2a Done 2026-05-12 (algorithmic core: pure-function walk-forward grid-search optimizer in `stocvest/signals/weight_optimizer.py` + 44 lock-in tests covering `WeightSet` invariants, engine-mirror equivalence vs `signal_recorder.outcome_from_prices`, `score_weight_candidate` accuracy math, `walk_forward_split` chronological correctness, `regime_distributions_match` 20% tolerance gate, `generate_candidate_weights` constrained ±delta grid with 729-upper-bound dedup, `accept_proposal` three-gate enforcement, and `optimize_weights_for_mode` end-to-end on synthetic data; no AWS/IO dependencies, no Lambda yet — Phase 2b wires this into a scheduled handler + IAM + EventBridge) · Phase 2b Done 2026-05-12 (scheduled worker: `stocvest/api/services/weight_proposer.py` orchestrator with per-mode error isolation + `stocvest/api/handlers/weight_proposer.py` Lambda entry + dispatch wiring in `lambda_dispatch.py` + `infra/lambda_weight_proposer.tf` with dedicated `lambda_weight_proposer_execution` IAM role NO `secretsmanager:UpdateSecret`/`PutSecretValue`/`CreateSecret`/`DeleteSecret` + EventBridge cron `(0 8 ? * MON *)` Monday 08:00 UTC + 26 lock-in tests covering projection rules, baseline resolution, end-to-end orchestration, per-mode error isolation, and Lambda handler anti-retry-storm guarantees) · Phases 3-4 pending | **Why proposal-only, not full auto-tune:** the instinct to "re-tune until we get the most accurate numbers possible" is exactly the pattern that produces overfitting — 12-dim weight search space (6 layers × 2 modes) with ~150-300 resolved signals per mode; regime overfit (trailing 60 days are usually one regime); Goodhart's law on a single accuracy metric; cascading dependencies with confluence + suppression gates; survivorship bias (we only see resolved-outcome rows for signals we emitted). The pipeline produces **candidate** rotations; a human admin approves or rejects each one with a reasoned note. **Why Secrets Manager remains the live store:** weights aren't a security secret (an attacker who knew them gains ~nothing), but Secrets Manager gives us KMS encryption + IAM-policy-gated writes + CloudTrail audit log + atomic version bumps — all properties we want for **operational integrity**. The optimizer never gets `secretsmanager:UpdateSecret` IAM; only the admin endpoint does. **Phase 1 (Done):** New DDB table `ParameterProposal` with `proposal_id` PK + `status_index` GSI (status PK + created_at SK) so the admin UI can list pending sorted by date without scanning. Terraform (`infra/dynamodb.tf`) adds the table + GSI + TTL on `ttl` attribute (rejected/superseded auto-expire after 90 days; promoted rows have NO TTL — they're the audit trail). Lambda env (`infra/lambda_6e.tf`) gains `DYNAMODB_PARAMETER_PROPOSAL_TABLE` and the new ARN is in `lambda_dynamodb_resources` flatten. Settings field `dynamodb_parameter_proposal_table` (`stocvest/utils/config.py`). New `stocvest/data/parameter_proposal_store.py`: `ParameterProposal` dataclass + `new_pending` factory (validates at least one per-mode override present) + `to_item`/`from_item` JSON-encoded-nested round-trip; `ParameterProposalStore` class with `put` (idempotent), `get`, `list_by_status` (closed-set validation, DESC by `created_at`, uses the GSI), `mark_promoted` / `mark_rejected` / `mark_superseded` (atomic via `ConditionalCheckExpression` on the `status` attribute — load-bearing guarantee that rejected can't be re-promoted, etc.); `build_default_proposal_store` for production. Tests in `tests/data/test_parameter_proposal_store.py` (+24): factory invariants (3 tests), round-trip serialization (4 tests, including raw-dict tolerance for hand-written rows), CRUD (3 tests: round-trip, missing-id None, idempotent put), GSI semantics (4 tests: DESC sort, status filter, invalid-status loud, limit), transitions + atomicity (6 tests: each transition + 3 cannot-transition-out-of-non-pending guards), closed-set invariants (2 tests). **Phase 2 (Pending):** Weekly optimizer Lambda (`weight_proposer`) — runs Sunday night via EventBridge, pulls trailing 8 weeks from `SignalHistory`, walk-forward CV (train weeks 1-6, val weeks 7-8), constrained grid search (each layer weight ∈ {current −0.05, current, current +0.05}), proposes if val accuracy ≥2% better with ≥30 signals in val window AND regime distribution roughly matches train. Writes via `ParameterProposalStore.put` only — Lambda IAM has `dynamodb:PutItem` on `ParameterProposal` but explicitly **no** `secretsmanager:UpdateSecret`. **Phase 3 (Pending):** Admin BFF + minimal UI at `/admin/parameter-proposals`. New handlers: `GET /v1/admin/parameter-proposals` (list pending), `GET /v1/admin/parameter-proposals/{id}` (detail with evidence), `POST /v1/admin/parameter-proposals/{id}/promote` (calls `ParameterStore.save_parameters_sync()` then `mark_promoted`), `POST /v1/admin/parameter-proposals/{id}/reject` (calls `mark_rejected` with required note). Admin role gating (Cognito group check). All four routes need `secretsmanager:UpdateSecret` IAM on `stocvest/signal-parameters` (only the promote path actually invokes it, but the route lives in the same admin Lambda for cohesion). **Phase 4 (Pending):** Safety net. CloudWatch metric on rolling-14-day post-rotation accuracy vs pre-rotation baseline; alarm fires when degradation ≥5%; SNS email notification to admin email + admin UI surfaces a one-click "Roll back to previous version" button that calls `ParameterStore.save_parameters_sync()` with the previous `ParameterHistory` row's params. **Defer decisions for later phases:** (a) whether to walk-forward over a **multi-regime** window (would require selecting from `SignalHistory` across regime-balanced eras vs strict trailing chronology) — Phase 2 will start strict-trailing and revisit if val/train regime imbalance is observed; (b) whether to include **confluence parameters** in the rotation (not just composite weights) — explicitly OUT OF SCOPE for now; (c) whether the admin UI exposes a "manually compose a proposal" path for ad-hoc tuning — almost certainly yes, but Phase 3 ships read-only first. |

---

## P — Platform & quality

| ID | Theme | Status | Notes |
|----|--------|--------|--------|
| P1 | **Phase 7 hardening** | Not done | E2E suite; security review (tenant isolation, PII); load tests; **minimum 2 weeks paper** per trading mode; staged real-money rollout. **Infra note:** D1 EventBridge rule is **applied** in development (`CONTEXT.md` §3); still not a substitute for Phase 7 E2E. |
| P2 | **Secrets-managed prompts** | Not done | Claude system/user prompts loaded from AWS Secrets Manager; version field on API calls; cache on cold start. |
| P3 | **Mobile / PWA** | Not done | Responsive web exists; native or PWA shell, push for alerts when B3 exists. |
| P4 | **Enhanced auth** | Not done | Optional SMS OTP, WebAuthn, magic link—Cognito capabilities; cost/compliance review. |
| P5 | **Infrastructure naming consistency** | Not done | Align DynamoDB table names with Terraform tag conventions **or** update tags to match table names. **Low priority** — no user impact. Example: tag `Name = "stocvest-development-ddb-signal-history"` vs actual table name **`SignalHistory`**. |
| P6 | **HTTP/API audit DynamoDB + gateway wiring** | Done 2026-05-07 · commit: 3e21d5cd4a2d41f1fb8cddd71cbdce61531a34e8 | **`infra/dynamodb.tf`** `AuditEvents` table (**`pk`/`sk`**); **`lambda_6e.tf`** **`DYNAMODB_AUDIT_EVENTS_TABLE`** + IAM **`lambda_dynamodb_resources`**; **`apigateway_6e.tf`** admin audit routes + **`GET /v1/signals/founding-members`** + CORS **`x-stocvest-session-id`**; **`stocvest/api/cors.py`** allow-headers parity. Apply Terraform before relying on prod audit persistence. |
| PF1 | **Production: reference levels wrong vs spot** | Resolved | **Cause:** Polygon snapshot sometimes returned `day` OHLC/VWAP on a different price scale than `lastTrade.p`; UI preferred `day_*` so VWAP/Support/Resistance could show ~$700s while last was ~$200. **Original fix:** drop session bar when scale off vs last (initially **2.5×**). **Superseded by PF2** (looser **5×**, keep session when last missing). Done 2026-05-02 (commit message: *Fix reference level prices + Greeks precision*). |
| PF2 | **Regression: reference levels all n/a (9f80282)** | Resolved | **Cause:** **2.5×** check too aggressive and/or compared against missing/stale `lastTrade.p`, stripping valid `day` → UI n/a. **Fix:** Only run ratio check when `last_trade_price` is **> 0**; if last missing, **keep** session bar; threshold **5×**; warning log includes ratio when dropping. Frontend `snapshot-reference-levels.ts` mirrors. **Tests:** `tests/signals/test_reference_levels.py`. Done 2026-05-02 (PF2; see git history). |
| PF3 | **Scanner: ORB EXPIRED badge overlap** | Resolved | Intraday setup card: badge row layout (`flex-wrap`, `margin-left: auto` on amber badge), dimmed card + disabled **Open order entry** styling, italic ET copy line. Done 2026-05-02 (with B9 polish). |
| PF4 | **Scanner: low-quality intraday / micro-cap gaps** | Resolved | Backend: RVOL vs prior-day volume, min score 0.5 (50%), ORB only before 10:00 ET + first-30m volume vs ADV, optional `liquidity_by_symbol` + `company_name` on setups; gap scan requires `prev_day_volume` ≥ 1M when present. Frontend: snapshots → liquidity, 120×1m bars, company on card. Done 2026-05-02. |
| PF5 | **Gap Intelligence: catalyst matching** | Done 2026-05-03 | **Cause:** Tight 24h lookback vs evening/weekend views, global-only Polygon news missing per-ticker articles, broad substring noise (e.g. “this week”). **Fix:** `_catalyst_lookback_hours_at` / `_get_catalyst_lookback_hours` (24h RTH Mon–Fri 9:30–16:00 ET, else 48h); `collect_news_for_gap_intelligence` merges global + per-symbol `get_news(ticker=)` with dedupe; `NewsCatalystDetector` listicle regexes + trimmed noise list + optional `company_name` headline fallback (0.8× narrative penalty vs ticker match); debug log when no catalyst. **Tests:** `tests/signals/test_gap_intelligence.py`, scanner handler assertion on per-symbol fetches. |
| PF6 | **Swing composite: minimum live layers + insufficient UI** | Done 2026-05-02 | **`POST /v1/signals/swing/composite`:** require **3** layers with `score` set and `status` ≠ `unavailable` before composite/confluence/record/alert; otherwise HTTP **200** with `status: insufficient_data`, counts, message, `market_status` (`is_market_open`, `next_open`, `market_session` from Polygon via `composite_market_context.py`). **Frontend:** `POST /api/stocvest/signals/swing-composite`, `lib/api/swing-composite.ts`, signals page amber callout (Lucide clock). **Tests:** `tests/api/handlers/test_signals.py`, `test_signal_recorder` composite payload uses three layers. |
| PF7 | **Signals stale data leak on insufficient state** | Done 2026-05-04 | Fixed `frontend/components/signals-page-client.tsx` stale rendering by introducing `hasValidSignal` gating and clearing `compositeResult`, `signalEvidence`, and `radarData` on insufficient-data responses; kept watchlist CTA visible. |
| PF8 | **Scanner API test timeout flake (local/CI variance)** | Done 2026-05-04 | Increased explicit timeout in `frontend/tests/scanner-api.test.ts` from default 5s to 15s for both scanner overview cases to stabilize intermittently slow runs. |
| PF9 | **CONTEXT market-data invariants + reusable VIX snapshot helper** | Done 2026-05-04 (PF9 on `main`; `git log -1 --oneline` for hash) | **`docs/CONTEXT.md` §7 / §13:** codified `Snapshot` + `_parse_snapshot` contract (field names, no raw dicts), `IntradaySetupScanner` bar-fetch boundary + calculator reuse guidance, extended-hours on `Snapshot` only, no duplicate day/last scale check. **`morning_brief_fetch`:** `VIX_SNAPSHOT_FALLBACK_SYMBOLS`, `SupportsPolygonSnapshotFetch`, `get_vix_snapshot_with_fallback` (replace ad-hoc VIX loops). **`tests/api/test_morning_brief.py`:** VIX fallback order / empty last / `PolygonError` chain. *(Row once recorded 536/56 tests; use `CONTEXT.md` §13 for current baselines.)* |
| PF10 | **Real 6-layer engine + `POST /v1/signals/composite/real`** | Done 2026-05-04 | Server-side analyzers (`technical_analyzer`, `news_analyzer`, `macro_analyzer`, `sector_analyzer` + `sector_mapper`, `geo_analyzer`, `internals_analyzer`), `real_composite_engine.py`, DynamoDB **`SectorCache`** + **`DYNAMODB_SECTOR_CACHE_TABLE`**, API Gateway route, BFF `composite/real`, signals UI uses real endpoint (removed `bullishBias` / `buildSwingCompositeRequestBody` heuristic). **`confluence.py`:** `normalize_direction`, ORB pattern helpers. **`docs/SIGNAL_ENGINE.md`**. **`sector_etf_defaults`**, extended `SectorParameters.sector_to_etf`, `TechnicalParameters` ATR/PDH fields. Tests: `tests/signals/*`, `tests/api/test_real_composite.py`. Evidence modal: `applySwingCompositeEnrichment` merges `layers[].chips` from composite; placeholders until enriched. *(Row once recorded 587/57 tests; use `CONTEXT.md` §13 for current baselines.)* |
| MP1 | **Model portfolio (signal tracking / notional)** | **Retired 2026-05-08** | Replaced by **signal validation ledger** on **`SignalHistory`** + **`GET /v1/signals/me/history?mode=`** + **`/dashboard/signal-validation`**. Do not re-add **`ModelPortfolio`** or signals-lambda **`/v1/portfolio/*`** book routes; broker **`POST /v1/portfolio/holdings|summary|allocation`** unchanged. |
| PF11 | **Signal card / composite data integrity (5 bugs)** | Done 2026-05-05 · commit: `313cf0f` | **Backend:** `composite_score.py` contradiction penalty + **`alignment_ratio`** / **`conflicted_layers`**; evidence builder completeness + VWAP fallback from intraday bars (no new external calls), **`rr_warning`** / **`rr_quality`**, catalyst headline list, derived **`risk_factors`** / **`risk_factors_detailed`**; **`SignalRecord.status`** + recorder filters **`incomplete`** from public feeds; lazy **`user_profile_store`**; **`tests/conftest.py`** Windows **`platform.uname()`** prime + test env **`DYNAMODB_USERS_TABLE`**. **Frontend:** dashboard time-bounded data loads, **`apiFetch`** default timeout, signal evidence card R/R + incomplete + catalyst/risk copy. **Tests:** `tests/signals/test_composite_scorer.py`, `test_signal_validation.py`, `test_risk_reward.py`, `test_catalyst_headlines.py`, `test_risk_factors.py`; `frontend/tests/signal-evidence.test.ts`. |

---

## ST — Swing trading (multi-day)

| ID | Theme | Status | Notes |
|----|--------|--------|-------|
| ST1 | **Swing composite Phase 1 (six layers, daily data)** | Done 2026-05-05 · commit: `07f3b19` | **`SwingTechnicalAnalyzer`** (`stocvest/signals/swing_technical_analyzer.py`), **`SwingTechnicalParameters`** + swing lookback fields on **`SignalParameters`**, **`POST /v1/signals/composite/swing`** + BFF **`/api/stocvest/signals/composite/swing`**, Terraform API route, **`swing_composite_engine.py`**: daily **`get_bars`**, 7d news / 14d econ calendar / weekly sector vs SPY / geo lookback; response **`mode`**, **`signal_expires`**; day **`composite/real`** adds **`mode: day`**, **`signal_valid_until`**. Analyzers: **`NewsAnalyzer`** / **`GeoAnalyzer`** time window kwargs; **`MacroAnalyzer`** `events_lookback_days`; **`SectorAnalyzer`** `use_weekly` + weekly % overrides. **`PolygonClient.get_economic_calendar_range`**. Tests: **`tests/signals/test_swing_technical_analyzer.py`**, **`tests/api/test_swing_composite.py`**. |

---

## G — Growth

| ID | Theme | Status | Notes |
|----|--------|--------|--------|
| G1 | **Referral program** | Not done | e.g. invite trader → credit; implement **after** Stripe/B5. |

---

## M — Multi-asset & advanced signals

| ID | Theme | Status | Notes |
|----|--------|--------|--------|
| M1 | **Cross-asset signal views** | Not done | Optional: options flow vs spot, futures basis vs spot, crypto vs risk proxies—**correlation / divergence as data**, not recommendations. |

---

## T — Post-beta “trade brief” (rich UI)

Single theme (legal review required before customer-facing price language):

- Rich card: reference levels (historical entry zone, reference targets, reference stop) as **data**, signal-parameters block, technical grid (VWAP, EMA, RSI, etc.), catalysts vs risks columns, risk/reward **chart as visualization only**.

Dependencies: D1 data maturity, counsel on display copy.

---

## Done (reference only — do not reopen)

Shipped themes are **not** backlog; see **`CONTEXT.md` §1–§2** (e.g. core signals, brokers, API, frontend redesign, order safety Step 8, legal compliance pass, earnings integration, public performance page shell, PDT enforcement, Crisp, etc.). If something regresses, file a **bug** or a **small** CONTEXT update—not a new backlog row.


