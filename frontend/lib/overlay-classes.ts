/** Shared Tailwind/CSS class names for overlays and floating surfaces. */

/** Dim + blur backdrop for full modals and drawers. */
export const MODAL_BACKDROP_CLASS = "app-modal-backdrop-blur";

/** Scrollable modal body — momentum scrolling on iOS. */
export const MODAL_DIALOG_SCROLL_CLASS = "app-modal-dialog-scroll";

/** Dropdown / typeahead / popover list surfaces. */
export const FLOATING_SURFACE_CLASS = "app-floating-surface";
